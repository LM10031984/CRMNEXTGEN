# Mini reverse-proxy Bearer devant Gotenberg (Gotenberg 8 = basic-auth only).
# Image officielle Caddy alpine ; l'ENTRYPOINT par defaut lance
# `caddy run --config /etc/caddy/Caddyfile --adapter caddyfile`.
FROM caddy:2-alpine

COPY Caddyfile /etc/caddy/Caddyfile
