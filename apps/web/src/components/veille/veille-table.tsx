/**
 * STUB Task 1 — implémentation complète en Task 2.
 * Tableau veille 5 colonnes + inline edit Exploitation.
 */

import type { RegulatoryWatch } from '@qualiof/db';

interface Props {
  watches: RegulatoryWatch[];
  canEdit: boolean;
}

export function VeilleTable(_props: Props) {
  return null;
}
