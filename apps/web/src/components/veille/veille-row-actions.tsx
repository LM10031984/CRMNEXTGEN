'use client';

/**
 * STUB Task 1 — implémentation complète en Task 2.
 * Kebab menu : Éditer / Archiver (Radix DropdownMenu).
 */

import type { RegulatoryWatch } from '@qualiof/db';

interface Props {
  watch: RegulatoryWatch;
}

export function VeilleRowActions(_props: Props) {
  return null;
}
