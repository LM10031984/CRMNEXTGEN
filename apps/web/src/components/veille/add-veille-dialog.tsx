'use client';

/**
 * STUB Task 1 — implémentation complète en Task 2.
 * Dialog "+ Ajouter une source" (Radix + RHF + zodResolver(createWatchSchema)).
 */

interface Props {
  defaultTheme?: 'INDIC_23' | 'INDIC_24' | 'INDIC_25' | 'INDIC_26';
}

export function AddVeilleDialog(_props: Props) {
  return null;
}
