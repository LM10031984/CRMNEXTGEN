'use client';

/**
 * STUB Task 1 — implémentation complète en Task 2.
 * Liste suggestions DRAFT/AUTO + Valider/Rejeter.
 */

import type { RegulatoryWatch } from '@qualiof/db';

interface Props {
  suggestions: RegulatoryWatch[];
}

export function VeilleInbox(_props: Props) {
  return null;
}
