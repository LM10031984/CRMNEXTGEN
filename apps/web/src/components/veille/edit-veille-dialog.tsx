'use client';

/**
 * STUB Task 1 — implémentation complète en Task 2.
 * Dialog "Modifier" une entrée veille (Radix + RHF + zodResolver(updateWatchSchema)).
 */

import type { RegulatoryWatch } from '@qualiof/db';

interface Props {
  watch: RegulatoryWatch;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function EditVeilleDialog(_props: Props) {
  return null;
}
