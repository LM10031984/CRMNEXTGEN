'use client';

/**
 * STUB Task 1 — implémentation complète en Task 2.
 * Onglets URL state veille (4 thématiques + inbox conditionnel D-03).
 */

interface Props {
  activeTab: string;
  canSeeInbox: boolean;
  inboxCount: number;
}

export function VeilleTabsClient(_props: Props) {
  return null;
}
