'use client';

interface ActiveBatchesBadgeProps {
  collapsed: boolean;
}

// Stub minimal — composant initialement absent du commit 63a437e.
// À remplacer par une vraie implémentation (polling batches PENDING/RUNNING).
export function ActiveBatchesBadge(_props: ActiveBatchesBadgeProps) {
  return null;
}
