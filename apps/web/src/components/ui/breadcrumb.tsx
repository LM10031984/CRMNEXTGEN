// Stub — composant absent du commit. À remplacer par la vraie implémentation.
export interface BreadcrumbItem {
  label: string;
  href?: string;
}
export function Breadcrumb(_props: { items: BreadcrumbItem[] }) {
  return null;
}
