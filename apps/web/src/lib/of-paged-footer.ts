// Stub — module absent du commit. À remplacer par la vraie implémentation
// CSS Paged Media (footer répété sur chaque page PDF).
import type { OfConfig } from './of-config';

export const OF_PAGED_FOOTER_STYLES = '';
export const OF_PAGED_PAGE_RULE = '';

export function renderOfPagedFooter(_config: OfConfig): string {
  return '';
}
