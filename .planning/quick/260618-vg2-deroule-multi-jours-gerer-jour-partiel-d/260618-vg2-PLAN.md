---
phase: quick-260618-vg2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/closure/ollama-generators.ts
  - apps/web/src/lib/closure/__tests__/deroule-jour-partiel.test.ts
  - apps/web/scripts/_gen-session-pack.ts
autonomous: true
requirements:
  - DEROULE-PARTIAL-DAY
must_haves:
  truths:
    - "Une formation 105h (14 jours, jour 14 = 1h) s'assemble en déroulé quand le dernier jour renvoie peu de séquences (ex: 2)"
    - "Un jour PLEIN (8h) avec < 5 séquences est TOUJOURS rejeté (le min adaptatif ne relâche pas tout)"
    - "Le cas nbJours===1 et le DerouleSchema global restent strictement inchangés"
    - "Un jour réellement null/vide continue d'abandonner le déroulé global (pas de troncature silencieuse)"
    - "Un échec de figeage d'un produit dans _gen-session-pack.ts n'avorte plus tout le run multi-produits"
  artifacts:
    - path: "apps/web/src/lib/closure/ollama-generators.ts"
      provides: "buildDerouleJourSchema(heuresJour) pur + min adaptatif dans la boucle multi-jours + prompt jour court"
      contains: "buildDerouleJourSchema"
    - path: "apps/web/src/lib/closure/__tests__/deroule-jour-partiel.test.ts"
      provides: "Test de puissance déterministe (sans LLM) du min adaptatif + mutation"
      contains: "buildDerouleJourSchema"
  key_links:
    - from: "generateDerouleContent (boucle multi-jours)"
      to: "buildDerouleJourSchema(scaffold.jours[k-1].travailTotalMin / 60)"
      via: "schema par jour passé à runOllamaJson POUR CE JOUR"
      pattern: "buildDerouleJourSchema"
    - from: "buildDerouleJourPrompt"
      to: "instruction jour court (heuresJour)"
      via: "branche conditionnelle quand heuresJour < 8"
      pattern: "heuresJour"
---

<objective>
Débloquer la génération du déroulé pour les produits dont `durationHours` n'est pas multiple de 8 (le DERNIER jour est PARTIEL). Aujourd'hui `generateDerouleContent` valide CHAQUE jour avec `DerouleSchema` qui impose `sequences.min(5)` : un jour de 1h (ex. PROD-0670 = 105h → 14 jours, jour 14 = 1h) ne peut pas produire ≥5 séquences → échec des 2 tentatives → déroulé GLOBAL null → `freezeProductAssets` throw "Corps déroulé null" → produit jamais généré. Reproduit 2× de façon déterministe.

Purpose: Rendre le plancher de séquences PROPORTIONNÉ à la durée du jour (jour plein 8h → min 5 inchangé ; jour court → min réduit), sans relâcher la rigueur sur les jours pleins ni toucher le `DerouleSchema` global (utilisé par le cas `nbJours===1` et ailleurs).

Output:
- `buildDerouleJourSchema(heuresJour)` (helper pur dans `ollama-generators.ts`) + câblage dans la boucle multi-jours.
- Prompt jour court (informe le LLM, pas de remplissage artificiel).
- Test de puissance déterministe (sans Ollama/Claude) + mutation.
- (secondaire, si peu coûteux) try/catch par produit dans `_gen-session-pack.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Source à modifier
@apps/web/src/lib/closure/ollama-generators.ts
@apps/web/src/lib/closure/freeze-product-assets.ts
@apps/web/scripts/_gen-session-pack.ts

<interfaces>
<!-- Contrats déjà présents dans le codebase — l'exécuteur les utilise tels quels, pas d'exploration. -->

Depuis apps/web/src/lib/closure/ollama-generators.ts (l.280-319) :
```typescript
// PLANCHERS de cellules (Kaïna 16/06) — NE PAS modifier, réutilisé tel quel
const DerouleSequenceSchema = z.object({ /* duree, objectifs, contenu, outils, exercice, evaluation, isPause? */ })
  .superRefine(/* minimums BAS par champ, pauses tolérées */);

// GLOBAL — utilisé par le cas nbJours===1 (l.834) et exporté de fait : NE PAS TOUCHER
const DerouleSchema = z.object({
  jours: z.array(z.object({
    theme: z.string().min(10),
    sequences: z.array(DerouleSequenceSchema).min(5),   // ← le min(5) rigide qui bloque le jour partiel
  })).min(1),
});
```

Depuis apps/web/src/lib/programme-normalize.ts (importé l.32-34 de ollama-generators) :
```typescript
export interface HoraireJour {
  matin: HoraireBloc;
  dejeuner: { start: string; end: string; durationMin: number };
  apresMidi: HoraireBloc;
  travailTotalMin: number;   // ← heures du jour = travailTotalMin / 60 (jour plein = 480 → 8h ; jour 1h = 60)
}
export interface HoraireScaffold {
  durationHours: number;
  nbJours: number;           // ceil(durationHours / 8)
  jours: HoraireJour[];      // (N-1) pleins + 1 dernier (plein ou partiel)
  multiDayDeferred: boolean;
}
export function buildHoraireScaffold(durationHours: number): HoraireScaffold;
```

Boucle multi-jours actuelle (l.852-880) — schéma à rendre adaptatif PAR JOUR :
```typescript
const scaffold = buildHoraireScaffold(formation.nombreHeures);
for (let k = 1; k <= nbJours; k++) {
  const jourGrille = renderHoraireScaffoldMd({ ...jours: [scaffold.jours[k - 1]!]... });
  const jourPrompt = buildDerouleJourPrompt(formation, k, nbJours, jourGrille, hasProgramme);
  const partiel = await runOllamaJson(
    'generate-deroule', SYSTEM_PROMPT_DEROULE, jourPrompt,
    DerouleSchema,        // ← REMPLACER par buildDerouleJourSchema(heuresJour) POUR CE JOUR
    refTable, refId, tenantId, MODEL_DEROULE, 'quality',
  );
  if (!partiel) { console.error(...); return null; }   // ← abandon global INCHANGÉ (pas de troncature)
  partiels.push(partiel);
}

runOllamaJson<T>(taskName, systemPrompt, userPrompt, schema: z.ZodSchema<T>, refTable, refId, tenantId, modelOverride?, tier?): Promise<T | null>
```

Prompt jour (l.771-798) — `buildDerouleJourPrompt(formation, k, nbJours, jourGrille, hasProgramme)` :
- l.794 : accueil au Jour 1 uniquement.
- l.797 : `k === nbJours` → "Dernier bloc : Évaluation des acquis et clôture" (À CONSERVER).

Script _gen-session-pack.ts : boucle `for (const SES of CODES) { ... }` (l.95-…), corps commençant par `freezeProductAssets` (l.175). Un throw "Corps déroulé null" propage et tue tout le run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test de puissance déterministe du min adaptatif (RED)</name>
  <files>apps/web/src/lib/closure/__tests__/deroule-jour-partiel.test.ts</files>
  <behavior>
    Cible : un helper PUR `buildDerouleJourSchema(heuresJour: number): z.ZodSchema` (à créer en Task 2, exporté de ollama-generators.ts). Tests déterministes SANS LLM, validant le schéma retourné directement contre des fixtures de "jour" (objet `{ theme, sequences[] }`).

    Fixtures helper : `makeSeq()` renvoie une séquence VALIDE (champs ≥ planchers DerouleSequenceSchema : objectifs ≥15, contenu ≥20, outils ≥6, exercice ≥15, evaluation ≥8, duree ≥3). `makeJour(n)` = `{ theme: '...'(≥10), sequences: Array(n).fill(makeSeq()) }`.

    - Test 1 (jour partiel 1h passe avec 2 séquences — LE BUG À DÉBLOQUER) :
      `buildDerouleJourSchema(1).safeParse(makeJour(2)).success === true`.
    - Test 2 (jour partiel 2h passe avec 2 séquences) :
      `buildDerouleJourSchema(2).safeParse(makeJour(2)).success === true`.
    - Test 3 (jour PLEIN 8h REJETTE 4 séquences — le min adaptatif ne relâche PAS tout) :
      `buildDerouleJourSchema(8).safeParse(makeJour(4)).success === false`.
    - Test 4 (jour PLEIN 8h accepte 5 séquences — non-régression) :
      `buildDerouleJourSchema(8).safeParse(makeJour(5)).success === true`.
    - Test 5 (plancher minimum >= 2 même pour un jour très court) :
      `buildDerouleJourSchema(1).safeParse(makeJour(1)).success === false` (1 séquence rejetée : on veut Accueil/contenu + bloc clôture).
    - Test 6 (assemblage 105h via assembleDeroule, déterministe pur) :
      construire 14 partiels `DerouleContent` (jours 1..13 pleins = makeJour(5), jour 14 = makeJour(2)) puis vérifier `assembleDeroule(partiels).jours.length === 14`. Prouve que la boucle, une fois le schéma assoupli, aboutit à un déroulé assemblé. (assembleDeroule est déjà exporté l.762.)

    MUTATION (documentée en commentaire dans le test, PAS dans le code prod) : si on remet `sequences.min(5)` rigide pour tous les jours, Test 1/2/6 deviennent ROUGE. Vérifie que le test garde réellement le comportement adaptatif.
    Inversement, Test 3/5 prouvent que le plancher n'est pas supprimé (un jour plein laxiste resterait rouge si on mettait min(0)).
  </behavior>
  <action>
    Créer `apps/web/src/lib/closure/__tests__/deroule-jour-partiel.test.ts`.

    Importer depuis `'../ollama-generators'` : `buildDerouleJourSchema` (créé Task 2) et `assembleDeroule` (déjà exporté l.762). Aucun import de Prisma, d'Ollama, de callLlm — test 100% pur. Si un mock est nécessaire pour que le module se charge (ex. `@qualiof/db` est importé en tête de ollama-generators), reproduire le pattern de mock minimal déjà utilisé dans les tests voisins (`vi.mock('@qualiof/db', ...)` — voir __tests__/freeze-product-assets.test.ts ou single-participant.test.ts pour le pattern exact, l'enums LegalForm/UserRole inclus si requis).

    Écrire les 6 tests décrits dans &lt;behavior&gt;. Cette tâche est RED : `buildDerouleJourSchema` n'existe pas encore → l'import casse / les tests échouent. C'est attendu.

    En tête du fichier, commenter le protocole de mutation (remettre min(5) rigide → Test 1/2/6 rouges) façon mémoire `feedback_test_de_puissance_mutation`.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm vitest run src/lib/closure/__tests__/deroule-jour-partiel.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Le fichier de test existe, les 6 cas sont écrits, la suite échoue (RED) car buildDerouleJourSchema n'est pas encore exporté.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Min de séquences adaptatif + prompt jour court (GREEN)</name>
  <files>apps/web/src/lib/closure/ollama-generators.ts</files>
  <behavior>
    Faire passer AU VERT les 6 tests de Task 1 + ne régresser AUCUN test existant (notamment freeze-product-assets, single-participant, programme-normalize, gen-session-pack-pure).
  </behavior>
  <action>
    Dans `apps/web/src/lib/closure/ollama-generators.ts` :

    1) Ajouter un helper PUR exporté `buildDerouleJourSchema(heuresJour: number): z.ZodSchema` JUSTE après `DerouleSchema` (l.319). Il construit une VARIANTE LOCALE de la forme `{ theme: z.string().min(10), sequences: z.array(DerouleSequenceSchema).min(N) }` (réutilise le `DerouleSequenceSchema` existant — on ne touche AUCUN plancher de cellule). N = min adaptatif :
       - `const minSeq = Math.max(2, Math.min(5, Math.round(heuresJour)));`
       - Jour plein 8h → `min(5, round(8))` = 5 (inchangé). Jour 1h → `max(2, 1)` = 2. Jour 2h → 2. Jour 5h → 5 plafonné (palier simple, conforme corrections §1).
       - Renvoyer `z.object({ jours: z.array(z.object({ theme, sequences: z.array(DerouleSequenceSchema).min(minSeq) })).min(1) })` afin que la forme soit IDENTIQUE à `DerouleSchema` (un seul jour dans `jours`, comme aujourd'hui en boucle). Garder le type de retour générique `z.ZodSchema<...>` compatible avec `runOllamaJson<T>` (le type de sortie reste un objet `{ jours: [...] }`).
       NE PAS modifier `DerouleSchema` global (l.310, utilisé par le cas nbJours===1 l.834 et la forme attendue ailleurs).

    2) Dans la boucle multi-jours (l.854-872), calculer `const heuresJour = scaffold.jours[k - 1]!.travailTotalMin / 60;` et passer `buildDerouleJourSchema(heuresJour)` à `runOllamaJson` À LA PLACE de `DerouleSchema` POUR CE JOUR. Le `if (!partiel) { console.error(...); return null; }` (abandon global) reste STRICTEMENT inchangé (pas de troncature silencieuse).

    3) Adapter `buildDerouleJourPrompt` (l.771) : ajouter un paramètre `heuresJour: number` et, quand `heuresJour < 8`, insérer une instruction dédiée : "Ce jour ne fait que {heuresJour}h — produis un nombre de séquences PROPORTIONNÉ à cette durée (pas de remplissage artificiel)." CONSERVER l'instruction l.797 "dernier bloc = Évaluation des acquis et clôture" pour `k === nbJours`, et l.794 accueil Jour 1. Mettre à jour l'appel l.861 pour passer `heuresJour`.

    Respecter TS strict (`noUncheckedIndexedAccess` → `scaffold.jours[k-1]!` déjà non-null asserted dans le code). Pas de `any`.

    Ne PAS lancer de génération de masse ni d'appel LLM réel.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm vitest run src/lib/closure/__tests__/deroule-jour-partiel.test.ts 2>&amp;1 | tail -20 &amp;&amp; pnpm vitest run src/lib/closure/__tests__/freeze-product-assets.test.ts src/lib/closure/__tests__/single-participant.test.ts src/lib/__tests__/programme-normalize.test.ts src/lib/closure/__tests__/gen-session-pack-pure.test.ts 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Les 6 tests de deroule-jour-partiel passent (GREEN). Les tests existants (freeze/single-participant/programme-normalize/gen-session-pack-pure) restent verts. `buildDerouleJourSchema` exporté, boucle multi-jours câblée, prompt jour court ajouté, DerouleSchema global et cas nbJours===1 inchangés.</done>
</task>

<task type="auto">
  <name>Task 3: tsc vert + (secondaire) try/catch par produit dans _gen-session-pack.ts</name>
  <files>apps/web/scripts/_gen-session-pack.ts</files>
  <action>
    1) SECONDAIRE (peu coûteux — l'inclure) : dans `apps/web/scripts/_gen-session-pack.ts`, envelopper le CORPS de la boucle `for (const SES of CODES) { ... }` (à partir du chargement session / freezeProductAssets jusqu'à la fin de l'itération) dans un `try { ... } catch (e: any) { log('✗', \`${SES} — échec produit/session : ${e?.message ?? e}\`); continue; }`. Objectif : un échec de figeage produit (ex. throw "Corps déroulé null") n'avorte plus TOUT le run multi-produits — on logge ✗ et on passe à la session suivante. Préserver les `continue` existants (session introuvable / sans produit / dry-run) qui restent valides à l'intérieur du try.
       Si l'enveloppement s'avère lourd (réorganisation profonde du flux, indentation massive cassant la lisibilité), NE PAS forcer : le différer et le NOTER dans le SUMMARY comme dette, et passer au point 2.

    2) Vérifier la compilation TypeScript stricte de tout le périmètre touché (tsc vert, conventions projet).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <done>`tsc --noEmit` est vert. Si fait : un échec produit dans _gen-session-pack.ts logge ✗ et continue au produit suivant (try/catch par itération). Sinon : différé et noté en dette dans le SUMMARY.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lib/closure/__tests__/deroule-jour-partiel.test.ts` : 6/6 verts.
- Suite ciblée existante (freeze-product-assets, single-participant, programme-normalize, gen-session-pack-pure) : verte (non-régression chunking 260618-jy1 + figeage 260618-rkj + rapport formateur 260618-skk).
- `pnpm tsc --noEmit` (apps/web) : vert.
- Mutation prouvée : remettre `sequences.min(5)` rigide → deroule-jour-partiel Test 1/2/6 rouges (documenté en commentaire, NON appliqué au code livré).
</verification>

<success_criteria>
- Min de séquences ADAPTATIF à la durée du jour dans la boucle multi-jours, via `buildDerouleJourSchema(heuresJour)` (jour 8h → 5 inchangé ; jour court → palier `max(2, min(5, round(h)))`).
- `DerouleSchema` global et cas `nbJours===1` STRICTEMENT inchangés.
- Abandon global sur jour null/vide PRÉSERVÉ (pas de troncature silencieuse).
- Prompt jour court informe le LLM (pas de remplissage artificiel) ; "dernier bloc = Évaluation/clôture" conservé sur le dernier jour.
- Test de puissance déterministe (sans LLM) + mutation en place.
- (secondaire) un échec produit ne tue plus le run multi-produits, OU différé+noté.
- Aucun appel LLM réel, aucune génération de masse lancée. tsc vert, aucun test existant régressé.
</success_criteria>

<output>
After completion, create `.planning/quick/260618-vg2-deroule-multi-jours-gerer-jour-partiel-d/260618-vg2-SUMMARY.md`
</output>
