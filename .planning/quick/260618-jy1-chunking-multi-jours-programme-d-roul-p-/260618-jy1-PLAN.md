---
phase: quick-260618-jy1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/programme-normalize.ts
  - apps/web/src/lib/__tests__/programme-normalize.test.ts
  - apps/web/src/lib/closure/ollama-generators.ts
  - apps/web/src/lib/closure/qualiopi-prompts.ts
autonomous: true
requirements: [T1, T2, T3]

must_haves:
  truths:
    - "buildHoraireScaffold(72) rend 9 journées pleines (8h chacune), pas 1 seule"
    - "buildHoraireScaffold(105) rend 14 journées : 13 pleines + 1 dernier jour partiel de 1h (9h00→10h00)"
    - "buildHoraireScaffold(8) reste inchangé : 1 jour, multiDayDeferred retiré/false (non-régression)"
    - "renderHoraireScaffoldMd liste chaque jour (### Jour K) avec ses créneaux"
    - "Une fonction pure réassemble N DerouleContent partiels en 1 DerouleContent à N jours, ordre préservé"
    - "generateDerouleContent garde sa signature (formation, refTable, refId, tenantId) — les 4 callers + script témoin en bénéficient sans modif"
  artifacts:
    - path: "apps/web/src/lib/programme-normalize.ts"
      provides: "buildHoraireScaffold multi-jours déterministe + renderHoraireScaffoldMd N jours + reliquat figé"
      contains: "nbJours"
    - path: "apps/web/src/lib/__tests__/programme-normalize.test.ts"
      provides: "tests de puissance déterministes scaffold (8/40/72/105) + reliquat"
      contains: "buildHoraireScaffold"
    - path: "apps/web/src/lib/closure/ollama-generators.ts"
      provides: "generateNormalizedProgramme multi-jours + generateDerouleContent chunké par jour + assembleDeroule pur"
      contains: "assembleDeroule"
  key_links:
    - from: "apps/web/src/lib/closure/ollama-generators.ts (generateDerouleContent)"
      to: "assembleDeroule (réassemblage pur)"
      via: "boucle d'appels LLM jour par jour quand nbJours>1 puis concat"
      pattern: "assembleDeroule"
    - from: "apps/web/src/lib/closure/ollama-generators.ts (generateNormalizedProgramme)"
      to: "renderHoraireScaffoldMd(scaffold) multi-jours"
      via: "grille N jours injectée dans le prompt user"
      pattern: "renderHoraireScaffoldMd"
---

<objective>
Lever le différé multi-jours sur la chaîne grille horaire → programme normalisé → déroulé pédagogique, pour produire des packs Qualiopi corrects sur formations longues (ex PROD-0042 9j) sans timeout ni troncature.

Trois volets :
1. **Grille horaire** déterministe sur N=ceil(h/8) jours (reliquat figé, hardcodé).
2. **Programme normalisé** réparti sur les N jours (fidélité conservée).
3. **Déroulé** généré JOUR PAR JOUR (1 appel LLM/jour) puis réassemblé en 1 seul DerouleContent → 1 PDF.

Purpose: les formations longues (PROD-0042 = 72h/9j, jusqu'à 105h/14j) doivent générer un pack Qualiopi complet et lisible. Aujourd'hui le multi-jours est explicitement différé (`multiDayDeferred=true`, scaffold tronqué à 1 jour, déroulé en 1 seul appel LLM → risque timeout/troncature).
Output: scaffold N jours, programme N jours, déroulé chunké + réassemblage pur, tous testés en déterministe. Les parties LLM sont vérifiées au témoin runtime (post-build), PAS unitairement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Convention métier FIGÉE (NON-NÉGOCIABLE — hardcode, pas de smart calc) :
# Jour plein = 9h00–13h00 (matin 4h, pause café ~10h45) / déjeuner 13h00–14h00 / 14h00–18h00 (après-midi 4h, pause café ~15h45) = 8h pile.
# Cohérent avec l'émargement (formation-horaires.ts) et SYSTEM_PROMPT_DEROULE.
# Mémoire Laurent : "Anti-pattern smart calc sur convention métier" + "Émargement règles non-négociables".

<interfaces>
<!-- Contrats existants à NE PAS casser. Extraits du code. -->

Depuis apps/web/src/lib/programme-normalize.ts :
```typescript
export const HORAIRE_MATIN_PROG = '9h00–13h00';   // matin 4h
export const HORAIRE_APREM_PROG = '14h00–18h00';  // après-midi 4h
export const PAUSE_CAFE_MATIN_PROG = { at: '10h45', durationMin: 15 };
export const PAUSE_CAFE_APREM_PROG = { at: '15h45', durationMin: 15 };
export interface HoraireBloc { label: string; travailMin: number; pauseCafe: { at: string; durationMin: number }; }
export interface HoraireJour { matin: HoraireBloc; dejeuner: { start: string; end: string; durationMin: number }; apresMidi: HoraireBloc; travailTotalMin: number; }
export interface HoraireScaffold { durationHours: number; nbJours: number; jours: HoraireJour[]; multiDayDeferred: boolean; }
export function buildHoraireScaffold(durationHours: number): HoraireScaffold;
export function renderHoraireScaffoldMd(scaffold: HoraireScaffold): string;
// PAUSE_DEJEUNER importé de '@/lib/formation-horaires' : { start:'13h00', end:'14h00', durationMin:60 }
```

Depuis apps/web/src/lib/closure/deroule-template.ts :
```typescript
export interface DerouleSequence { duree: string; objectifs: string; contenu: string; outils: string; exercice: string; evaluation: string; isPause?: boolean; }
export interface DerouleJour { theme: string; sequences: DerouleSequence[]; }
export interface DerouleContent { jours: DerouleJour[]; }
// renderDerouleHtml(ctx, content) pagine déjà N jours (renderDerouleDays itère content.jours) ;
// renderBilanFormateur ("Rapport formateur") est appelé UNE seule fois en fin, APRÈS la boucle jours. RAS à changer côté template.
```

Signature à PRÉSERVER (4 callers prod : renderer.ts, generate-deroule-session.ts, deroule-product-generator.ts, _gen-temoin-cloud.ts + 1 mock test gen-session-pack-pure.test.ts) :
```typescript
export async function generateDerouleContent(
  formation: FormationCtx,            // { titre, programmeMd, nombreHeures }
  refTable = 'PedagogicalAsset',
  refId: string | null = null,
  tenantId: string | null = null,
): Promise<DerouleContent | null>;
```

Schéma Zod existant (1 jour partiel = sortie LLM d'un appel) :
```typescript
const DerouleSchema = z.object({ jours: z.array(z.object({ theme: z.string().min(10), sequences: z.array(DerouleSequenceSchema).min(5) })).min(1) });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grille horaire scaffold multi-jours déterministe + tests de puissance</name>
  <files>apps/web/src/lib/programme-normalize.ts, apps/web/src/lib/__tests__/programme-normalize.test.ts</files>
  <behavior>
    Tests déterministes à étendre/ajouter dans programme-normalize.test.ts (RED d'abord) :
    - 8h → nbJours=1, jours.length===1, jour plein inchangé (matin 9h00–13h00 240min, déjeuner 60, après-midi 14h00–18h00 240, travailTotalMin 480) — NON-RÉGRESSION.
    - 40h → nbJours=5, jours.length===5, TOUS pleins (travailTotalMin 480 chacun, dernier non partiel).
    - 72h → nbJours=9, jours.length===9, tous pleins (PROD-0042).
    - 105h → nbJours=14, jours.length===14, 13 jours pleins (travailTotalMin 480) + dernier jour partiel reliquat=105-8*13=1h : matin 9h00→10h00 (travailMin 60), PAS d'après-midi (apresMidi.travailMin===0 ou bloc absent — choix exécuteur, documenter dans le type).
    - Reliquat >4h : ex 21h → nbJours=3, dernier reliquat=21-16=5h → matin complet 9h00–13h00 (240) + après-midi partiel 14h00→15h00 (travailMin 60).
    - Reliquat ≤4h : ex 12h → nbJours=2, dernier reliquat=4h → matin seul 9h00→13h00 (240), pas d'après-midi.
    - Reliquat exactement 4h figé matin complet ; reliquat exactement 0 IMPOSSIBLE (ceil garantit dernier jour ≥1h… sauf multiple de 8 → dernier jour PLEIN). Ex 16h → 2 jours pleins (16=8*2, reliquat=8 → dernier jour plein, PAS partiel).
    - renderHoraireScaffoldMd : contient "### Jour 1" et "### Jour 9" pour 72h ; chaque jour liste ses créneaux ; garde "recopier" + horaires figés.
    - Déterminisme : buildHoraireScaffold(72) toEqual buildHoraireScaffold(72) ; rendu idem.
    - TEST DE PUISSANCE reliquat : un test qui vérifie le travailMin du dernier jour de 105h===60 ; si la formule reliquat est cassée (ex 8*(nbJours-1) → 8*nbJours), ce test DOIT virer rouge. Ajouter un commentaire grep-able `// test de puissance reliquat`.
  </behavior>
  <action>
    Modifier buildHoraireScaffold(durationHours) dans programme-normalize.ts :
    - nbJours = Math.max(1, Math.ceil(durationHours / 8)) (inchangé).
    - reliquat = durationHours - 8*(nbJours-1) (heures du DERNIER jour). Si durationHours est multiple de 8, reliquat===8 → dernier jour PLEIN.
    - Construire un helper interne pur `buildJourPlein()` (= le jour figé actuel, 8h, factorisé) et `buildJourPartiel(reliquatHeures)` :
        * reliquatHeures >= 8 → jour plein.
        * reliquatHeures <= 4 → matin SEUL : matin label `9h00–${9+reliquatHeures}h00`, travailMin=reliquatHeures*60 ; apresMidi label '' (ou marqueur absent) travailMin=0 ; pas de pause café aprem ; travailTotalMin=reliquatHeures*60 ; déjeuner conservé OU omis si <5h (suivre getDayStartEnd hasPause = heuresParJour>=5 ; pour ≤4h matin seul PAS de déjeuner). Documenter le choix.
        * reliquatHeures > 4 (et <8) → matin complet 9h00–13h00 (240) + déjeuner + après-midi PARTIEL label `14h00–${14+(reliquatHeures-4)}h00`, travailMin=(reliquatHeures-4)*60 ; travailTotalMin=reliquatHeures*60.
      Horaires HARDCODÉS via concaténation déterministe sur l'entier d'heures (PAS de smart calc sur minutes — reliquat est toujours un entier d'heures car durationHours entier). Si durationHours non entier → Math.round défensif documenté.
    - jours = [...(nbJours-1) jours pleins, buildJourPartiel(reliquat)] (si nbJours===1 → [buildJourPartiel(durationHours)] qui pour 8h rend exactement le jour plein actuel = non-régression).
    - RETIRER multiDayDeferred OU le forcer à false partout (plus de différé). Garder le champ dans HoraireScaffold pour compat type mais documenter "déprécié, toujours false" — OU le supprimer et adapter le test 'multi-jours DIFFÉRÉ' existant (le réécrire en test multi-jours implémenté). Choix exécuteur : préférer SUPPRIMER multiDayDeferred et réécrire le test obsolète (lignes 43-54 du test actuel).
    Adapter HoraireJour si besoin pour exprimer un jour partiel (ex apresMidi optionnel ou travailMin=0). Mettre à jour le commentaire de tête du fichier (PÉRIMÈTRE : multi-jours désormais IMPLÉMENTÉ).
    Réécrire renderHoraireScaffoldMd pour itérer scaffold.jours : pour chaque jour, `### Jour ${K} — Organisation de la journée` + créneaux (matin / déjeuner si présent / après-midi si présent) + total travail du jour. En tête, rappeler nbJours et durée totale. Garder mots-clés "recopier" et horaires figés.
    Lancer les tests Vitest (RED→GREEN). Casser la formule reliquat localement pour vérifier le rouge du test de puissance, puis restaurer.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/programme-normalize.test.ts</automated>
  </verify>
  <done>
    Tests verts : 8h non-régression (1 jour plein), 40h (5 pleins), 72h (9 pleins), 105h (14 jours, dernier reliquat 1h matin seul), reliquat >4h (après-midi partiel) et ≤4h (matin seul) couverts. Test de puissance reliquat présent (casser la formule → rouge prouvé puis restauré). multiDayDeferred retiré ou forcé false. renderHoraireScaffoldMd liste chaque Jour K. Déterminisme prouvé (toEqual sur 2 appels). Convention horaire 100% hardcodée, aucun smart calc sur minutes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Programme normalisé réparti sur N jours (prompt + generateNormalizedProgramme)</name>
  <files>apps/web/src/lib/closure/ollama-generators.ts, apps/web/src/lib/closure/qualiopi-prompts.ts</files>
  <action>
    generateNormalizedProgramme reçoit déjà `const scaffold = buildHoraireScaffold(durationHours); const grilleImposee = renderHoraireScaffoldMd(scaffold);`. Avec Task 1, grilleImposee est désormais MULTI-JOURS automatiquement (N journées listées). Vérifier que le userPrompt injecte bien cette grille N jours (RAS structurel sauf wording).
    Adapter le wording du userPrompt et SYSTEM_PROMPT_NORMALIZE_PROGRAMME (qualiopi-prompts.ts) pour le multi-jours :
    - Remplacer la phrase figée "matin 9h00–13h00 / pause déjeuner / après-midi 14h00–18h00 = 8h pile" du SYSTEM_PROMPT_NORMALIZE_PROGRAMME par une formulation qui dit : "La grille te donne N journées (certaines partielles) ; tu RÉPARTIS le contenu source sur CES N journées, en recopiant les horaires de chaque jour tels quels." Conserver les 3 RÈGLES ABSOLUES existantes (horaires imposés / fidélité décliner-pas-enrichir / verbes d'action).
    - Ajouter au FORMAT DE RENDU : "Structure le markdown par JOUR (### Jour K) quand la grille comporte plusieurs jours ; répartis les thèmes source sur les jours sans en inventer ni en retirer."
    - Le post-traitement enforceProgrammeFidelity (NON bloquant, warn) est CONSERVÉ tel quel — sourceModuleTitles inchangé.
    Robustesse gros programmes (105h) : UN seul appel LLM de mapping par défaut. Si l'exécuteur juge le risque de troncature réel sur 14 jours, OPTION : chunker le mapping par jour (boucle sur scaffold.jours, 1 appel/jour, concat des markdowns avec séparateur "### Jour K"). Choix exécuteur — privilégier la robustesse ; documenter le choix retenu dans le commentaire de fonction. Si chunking retenu, NE PAS changer la signature publique de generateNormalizedProgramme.
    NE PAS toucher au schéma NormalizedProgrammeSchema (markdown plat) ni à programme-core.ts.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | grep -v -E "redirect-308\.test|sessions\.ts:804|shared-template\.test" | grep -E "error TS" || echo "TSC OK (préexistants ignorés)"</automated>
  </verify>
  <done>
    Le prompt système et le userPrompt parlent de répartition multi-jours (N journées), grille N jours injectée (vient de Task 1). Fidélité enforceProgrammeFidelity conservée. Signature de generateNormalizedProgramme inchangée. tsc vert (hors 3 préexistants tolérés). PARTIE LLM (qualité du mapping réparti) vérifiée au TÉMOIN RUNTIME post-build — PAS testable unitairement (documenté ici).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Déroulé chunké par jour + réassemblage pur (assembleDeroule) + test de puissance</name>
  <files>apps/web/src/lib/closure/ollama-generators.ts, apps/web/src/lib/__tests__/programme-normalize.test.ts</files>
  <behavior>
    Test déterministe PUR (sans LLM) à ajouter pour la fonction de réassemblage `assembleDeroule(partiels: DerouleContent[]): DerouleContent` :
    - 3 partiels [{jours:[J1]},{jours:[J2]},{jours:[J3]}] → 1 DerouleContent {jours:[J1,J2,J3]}, ORDRE préservé (themes dans l'ordre d'entrée).
    - 1 partiel → identité (jours inchangés).
    - 0 partiel → { jours: [] } (ou null géré par l'appelant — documenter).
    - Un partiel à plusieurs jours (cas dégénéré LLM) → tous ses jours aplatis dans l'ordre.
    - TEST DE PUISSANCE ordre : construire 3 partiels themes ['A','B','C'] → résultat themes ['A','B','C']. Si la concat inverse l'ordre (ex .reverse() ou unshift), le test DOIT virer rouge. Commentaire grep-able `// test de puissance ordre réassemblage`.
    NB : la GÉNÉRATION LLM d'un jour n'est PAS testée unitairement (runtime témoin).
  </behavior>
  <action>
    Dans ollama-generators.ts :
    1. Exporter une fonction PURE `export function assembleDeroule(partiels: DerouleContent[]): DerouleContent { return { jours: partiels.flatMap((p) => p.jours) }; }` (flatMap préserve l'ordre, gère partiel multi-jours et liste vide). Importer le type DerouleContent (déjà importé en type).
    2. Refondre generateDerouleContent en GARDANT sa signature `(formation, refTable='PedagogicalAsset', refId=null, tenantId=null): Promise<DerouleContent|null>` :
       - nbJours = Math.max(1, Math.ceil(formation.nombreHeures / 8)).
       - Si nbJours === 1 → comportement ACTUEL inchangé (1 appel runOllamaJson, prompt actuel) — non-régression stricte.
       - Si nbJours > 1 → utiliser buildHoraireScaffold(formation.nombreHeures) pour obtenir la grille figée de CHAQUE jour. Boucle for k=1..nbJours :
           * Construire un prompt user PAR JOUR (entrées : titre formation, programme normalisé complet formation.programmeMd en référence, "Tu génères UNIQUEMENT le Jour k sur N", contenu attendu de ce jour, grille figée du jour k (créneaux issus de scaffold.jours[k-1] : matin/déjeuner/après-midi à RECOPIER), consigne "Accueil seulement au Jour 1", "bloc Évaluation des acquis et clôture (QCM Kahoot) UNIQUEMENT au dernier jour").
           * Appeler runOllamaJson avec un schéma 1-jour (réutiliser DerouleSchema qui accepte jours.min(1) ; le LLM renvoie { jours:[1 jour] }) → DerouleContent partiel. tier 'quality', MODEL_DEROULE (idem actuel).
           * GESTION D'ÉCHEC : si un jour retourne null → NE PAS tronquer silencieusement. Logguer `console.error([generate-deroule] jour k/N échoué)` et retourner null pour TOUT le déroulé (l'appelant fallback sur stub, signal explicite). Documenter ce choix dans <done> (pas de PDF partiel trompeur).
       - Concaténer les partiels via assembleDeroule(partiels) → DerouleContent { jours:[...N] }.
       - Renormaliser les thèmes "Jour K — …" est déjà géré côté renderDerouleDays (strip préfixe) — RAS.
       - Le "Rapport formateur" est rendu UNE fois en fin par renderDerouleHtml (vérifié : renderBilanFormateur appelé après la boucle jours, hors renderDerouleDays) — RAS à changer.
    3. NE PAS modifier deroule-template.ts (pagination N jours + rapport unique déjà corrects). NE PAS modifier les 4 callers ni le mock test (signature préservée). NE PAS toucher worker.ts ni au WIP Laurent.
    Optionnel doc : extraire le prompt "1 jour" dans une const locale ou réutiliser SYSTEM_PROMPT_DEROULE tel quel (il décrit déjà une journée 9h-18h) — préférer réutiliser SYSTEM_PROMPT_DEROULE et cadrer le périmètre "jour k" dans le userPrompt.
    Lancer les tests Vitest sur assembleDeroule (RED→GREEN), casser l'ordre pour prouver le rouge du test de puissance, restaurer. Puis tsc.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/programme-normalize.test.ts && pnpm --filter @qualiof/web exec vitest run src/lib/closure/__tests__/gen-session-pack-pure.test.ts</automated>
  </verify>
  <done>
    assembleDeroule exportée et PURE (flatMap, ordre préservé, gère 0/1/N partiels + partiel multi-jours). Test de puissance ordre présent (casser → rouge prouvé puis restauré). generateDerouleContent : signature IDENTIQUE (4 callers + mock test inchangés), nbJours===1 strictement non-régressé, nbJours>1 boucle 1 appel LLM/jour + assembleDeroule + 1 seul PDF. Échec d'un jour = null global explicite (console.error), PAS de troncature silencieuse. Rapport formateur rendu 1 fois (RAS template). gen-session-pack-pure.test.ts toujours vert (mock signature compatible). tsc vert hors 3 préexistants. GÉNÉRATION LLM par jour vérifiée au TÉMOIN RUNTIME post-build (PROD-0042 9j, lancé par Laurent après revue) — PAS testable unitairement (documenté).
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/programme-normalize.test.ts` vert (scaffold multi-jours + reliquat + assembleDeroule + 2 tests de puissance).
- `pnpm --filter @qualiof/web exec vitest run src/lib/closure/__tests__/gen-session-pack-pure.test.ts` vert (non-régression callers/mock).
- `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 nouvelle erreur (ignorer préexistants : redirect-308.test.ts ×6, sessions.ts:804 legalName, shared-template.test.ts Test6 mime jpeg).
- Aucun fichier hors `files_modified` touché (template, callers, worker, scripts non modifiés — sauf si signature inchangée le permet).
- NE PAS lancer de génération réelle d'un produit long (témoin PROD-0042 lancé par Laurent après revue).
</verification>

<success_criteria>
- buildHoraireScaffold rend N=ceil(h/8) jours déterministes, reliquat figé (≤4h matin seul / >4h après-midi partiel), multiple de 8 → dernier jour plein. multiDayDeferred levé.
- renderHoraireScaffoldMd liste chaque Jour K avec ses créneaux.
- generateNormalizedProgramme injecte la grille N jours + prompt mis à jour pour répartition multi-jours, fidélité conservée, signature inchangée.
- generateDerouleContent chunke par jour (1 appel LLM/jour) puis réassemble via assembleDeroule pur en 1 DerouleContent → 1 PDF ; cas 1 jour inchangé ; échec d'un jour signalé (null global) ; signature préservée → 4 callers + script témoin en bénéficient sans modif.
- 2 tests de puissance déterministes (reliquat scaffold + ordre réassemblage) prouvés rouge-quand-cassé.
- Commits atomiques sur cloud-migration (1 par tâche), pas de worktree.
</success_criteria>

<output>
After completion, create `.planning/quick/260618-jy1-chunking-multi-jours-programme-d-roul-p-/260618-jy1-SUMMARY.md`
</output>
