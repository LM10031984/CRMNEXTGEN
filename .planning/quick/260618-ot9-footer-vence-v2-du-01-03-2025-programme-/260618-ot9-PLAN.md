---
phase: quick-260618-ot9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - apps/web/src/lib/doc-version.ts
  - apps/web/src/lib/of-paged-footer.ts
  - apps/web/src/lib/of-pdf-footer.ts
  - apps/web/src/lib/programme-normalize.ts
  - apps/web/src/lib/closure/qualiopi-prompts.ts
  - apps/web/src/lib/__tests__/programme-normalize.test.ts
autonomous: true
requirements: [COR-1, COR-2, COR-3]
must_haves:
  truths:
    - "L'adresse OF affichée PARTOUT (footers PDF + corps des docs) est « 618 Bd Jean Maurel Inférieur, 06140 Vence »"
    - "Les 2 footers PDF affichent une ligne « V2 du 01/03/2025 » après le contact"
    - "Un programme multi-jours affiche la grille horaire UNE SEULE FOIS sous « Organisation des journées », pas un bloc par jour"
    - "Un programme d'1 jour reste inchangé (validé PROD-0062)"
  artifacts:
    - path: ".env"
      provides: "OF_ADDRESS_* = Vence (footers via getOfConfig ENV-only)"
      contains: "618 Bd Jean Maurel Inférieur"
    - path: "apps/web/src/lib/doc-version.ts"
      provides: "Const partagée DOC_VERSION='V2 du 01/03/2025'"
      exports: ["DOC_VERSION"]
    - path: "apps/web/src/lib/of-paged-footer.ts"
      provides: "renderOfPagedFooter avec ligne DOC_VERSION"
      contains: "DOC_VERSION"
    - path: "apps/web/src/lib/of-pdf-footer.ts"
      provides: "renderOfStandardFooterHtml avec ligne DOC_VERSION"
      contains: "DOC_VERSION"
    - path: "apps/web/src/lib/programme-normalize.ts"
      provides: "renderHoraireScaffoldMd dédupliqué multi-jours (Organisation des journées)"
      contains: "Organisation des journées"
  key_links:
    - from: "of-paged-footer.ts / of-pdf-footer.ts"
      to: "doc-version.ts"
      via: "import { DOC_VERSION }"
      pattern: "DOC_VERSION"
    - from: "DB Tenant address (jsonb)"
      to: "corps des docs via loadOfConfig"
      via: "UPDATE Tenant SET address = jsonb_build_object(...)"
      pattern: "618 Bd Jean Maurel"
---

<objective>
3 corrections sur les documents Qualiopi générés (branche cloud-migration, PAS de worktree) :
1. **COR-1** — Adresse OF = Vence (618 Bd Jean Maurel Inférieur, 06140 Vence) PARTOUT : `.env` (footers via `getOfConfig` ENV-only) + DB Tenant (corps des docs via `loadOfConfig`).
2. **COR-2** — Ajouter « V2 du 01/03/2025 » aux 2 footers PDF via une const partagée `DOC_VERSION` (un seul endroit).
3. **COR-3** — Programme multi-jours : afficher la grille horaire UNE SEULE FOIS (« Organisation des journées ») au lieu d'un bloc par jour ; 1 jour inchangé.

Purpose: aligner les docs sur la réalité (site Vence) + versionner les docs + retirer la répétition redondante de l'horaire confirmée par Laurent.
Output: `.env` + DB Tenant à jour, footers versionnés, `renderHoraireScaffoldMd` dédupliqué, tests adaptés.

NE PAS lancer de génération réelle (Laurent régénère après). NE PAS toucher au worker Ollama ni au WIP Laurent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contrats à utiliser directement, déjà lus — pas d'exploration codebase. -->

of-config.ts (légataire) :
```typescript
// getOfConfig() : sync ENV-only → footers (pas de tenantId au call site)
// loadOfConfig(tenantId) : async, lit Tenant.address jsonb { street, postalCode|cp, city|ville }, fallback ENV
//   → corps des docs (programme, convention, attestation, facture…)
// Donc : footers = .env ; corps = DB Tenant.
```

of-paged-footer.ts (WeasyPrint running element) — renderOfPagedFooter() ~64-71 :
```
<footer class="corp">
  <strong>{name}</strong> – Siège social : {addressFull} - SIRET : {siret} – NDA {rnq}<br>
  Coordonnées de contact : {contactNom} - {email} - {phone}
</footer>
```

of-pdf-footer.ts (Gotenberg footer.html, font 36pt) — renderOfStandardFooterHtml() ~27-41 :
```
<div ...>
  <strong>{name}</strong> – Siège social : {addressFull} - SIRET : {siret} – NDA {rnq}<br>
  Coordonnées de contact : {contactNom} - {email} - {phone}
</div>
```

programme-normalize.ts :
```typescript
export interface HoraireJour { matin: HoraireBloc; dejeuner: {...}; apresMidi: HoraireBloc; travailTotalMin: number; }
export interface HoraireScaffold { durationHours: number; nbJours: number; jours: HoraireJour[]; multiDayDeferred: boolean; }
// renderJourLines(j, k) — rend "### Jour K — Organisation de la journée" + Matin/déjeuner/après-midi/Total
// renderHoraireScaffoldMd(scaffold) — itère TOUS les jours via renderJourLines → 1 bloc par jour (À DÉDUPLIQUER)
// Jour plein : travailTotalMin===480, matin.label='9h00–13h00', apresMidi.label='14h00–18h00'
```

Consommateurs de renderHoraireScaffoldMd (NE PAS CASSER) :
- ollama-generators.ts:734 → appelle avec `{ nbJours:1, jours:[scaffold.jours[k-1]] }` (slice 1 jour par appel LLM déroulé). DOIT rester format "### Jour 1" mono-jour.
- ollama-generators.ts:801 → appelle avec le scaffold COMPLET (programme normalisé). C'est CE call qui doit dédupliquer en multi-jours.
=> La dédup s'applique UNIQUEMENT quand `nbJours > 1`. `nbJours === 1` STRICTEMENT inchangé.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COR-1 adresse OF Vence (.env + DB Tenant)</name>
  <files>.env</files>
  <action>
    (a) Éditer `.env` racine : remplacer les 3 valeurs OF_ADDRESS_* (lignes ~94-96) par :
      OF_ADDRESS_STREET="618 Bd Jean Maurel Inférieur"
      OF_ADDRESS_CP="06140"
      OF_ADDRESS_VILLE="Vence"
    (Ce sont les footers via getOfConfig ENV-only qui consomment ces valeurs.)
    Il n'existe PAS de .env.local avec OF_ADDRESS_* (vérifié) → rien à faire côté .env.local.

    (b) Mettre à jour la DB Tenant (corps des docs via loadOfConfig). Exécuter EN UNE COMMANDE
    l'UPDATE puis le SELECT de vérification :
      docker exec -e PGPASSWORD=qualiof_dev qualiof_postgres psql -U qualiof -d qualiof -c "UPDATE \"Tenant\" SET address = jsonb_build_object('street','618 Bd Jean Maurel Inférieur','postalCode','06140','city','Vence','country','France') WHERE id='db191440-a144-48d1-93c1-767e6f647f2c';"
      docker exec -e PGPASSWORD=qualiof_dev qualiof_postgres psql -U qualiof -d qualiof -c "SELECT id, address FROM \"Tenant\" WHERE id='db191440-a144-48d1-93c1-767e6f647f2c';"
    Le SELECT DOIT montrer address = {"city":"Vence","street":"618 Bd Jean Maurel Inférieur","country":"France","postalCode":"06140"}.
    Si UPDATE retourne "UPDATE 0" → STOP : l'id Tenant ne correspond pas, signaler à Laurent (ne rien forcer).

    NE PAS toucher la base Docker au-delà de cet UPDATE Tenant. NE PAS lancer de génération.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q '618 Bd Jean Maurel Inférieur' .env && grep -q 'OF_ADDRESS_CP="06140"' .env && grep -q 'OF_ADDRESS_VILLE="Vence"' .env && docker exec -e PGPASSWORD=qualiof_dev qualiof_postgres psql -U qualiof -d qualiof -tAc "SELECT address->>'city' FROM \"Tenant\" WHERE id='db191440-a144-48d1-93c1-767e6f647f2c';" | grep -q 'Vence' && echo OK-COR1</automated>
  </verify>
  <done>`.env` contient les 3 valeurs Vence ; SELECT DB Tenant retourne city=Vence, street=618 Bd Jean Maurel Inférieur, postalCode=06140, country=France.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: COR-2 const partagée DOC_VERSION dans les 2 footers</name>
  <files>apps/web/src/lib/doc-version.ts, apps/web/src/lib/of-paged-footer.ts, apps/web/src/lib/of-pdf-footer.ts</files>
  <behavior>
    - Nouveau fichier `doc-version.ts` exporte `export const DOC_VERSION = 'V2 du 01/03/2025' as const;` (un seul endroit).
    - `renderOfPagedFooter()` produit une 3e ligne discrète après le contact contenant 'V2 du 01/03/2025'.
    - `renderOfStandardFooterHtml()` produit la même ligne 'V2 du 01/03/2025' après le contact.
    - Aucune régression : name/siret/rnq/contact toujours présents.
  </behavior>
  <action>
    (1) Créer `apps/web/src/lib/doc-version.ts` :
      ```ts
      /** Version documentaire affichée dans les footers PDF (source unique). */
      export const DOC_VERSION = 'V2 du 01/03/2025' as const;
      ```
    (2) `of-paged-footer.ts` : importer `import { DOC_VERSION } from './doc-version';`. Dans
    `renderOfPagedFooter()`, ajouter une ligne discrète APRÈS la ligne "Coordonnées de contact".
    Insérer un `<br>` + un span de version, ex après la ligne contact :
      `<br><span style="font-size:9pt;color:#64748B;">${escapeHtml(DOC_VERSION)}</span>`
    (NE PAS échapper DOC_VERSION via une fonction absente — réutiliser le `escapeHtml` local du fichier.)
    (3) `of-pdf-footer.ts` : même import + même ligne discrète après le contact dans le `<div>`
    (footer Gotenberg à 36pt — la version peut rester à taille réduite via un span, ex
    `<br><span style="font-size:24pt;color:#64748B;">${escapeHtml(DOC_VERSION)}</span>` pour
    rester proportionnellement discret face au 36pt du corps du footer).
    Ne PAS dupliquer la chaîne littérale ailleurs : les 2 footers DOIVENT importer DOC_VERSION.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f apps/web/src/lib/doc-version.ts && grep -q "DOC_VERSION" apps/web/src/lib/of-paged-footer.ts && grep -q "DOC_VERSION" apps/web/src/lib/of-pdf-footer.ts && ! grep -q "'V2 du 01/03/2025'" apps/web/src/lib/of-paged-footer.ts && ! grep -q "'V2 du 01/03/2025'" apps/web/src/lib/of-pdf-footer.ts && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | grep -v "redirect-308.test.ts\|sessions.ts:804\|shared-template.test.ts" | grep -c "error TS" | grep -qx 0 && echo OK-COR2</automated>
  </verify>
  <done>`doc-version.ts` exporte DOC_VERSION ; les 2 footers l'importent (aucune chaîne littérale dupliquée) et affichent la ligne version après le contact ; tsc vert (hors préexistants).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: COR-3 dédup grille horaire multi-jours + prompt + tests</name>
  <files>apps/web/src/lib/programme-normalize.ts, apps/web/src/lib/closure/qualiopi-prompts.ts, apps/web/src/lib/__tests__/programme-normalize.test.ts</files>
  <behavior>
    - 72h (9 jours pleins, tous identiques) → `renderHoraireScaffoldMd` produit UNE SEULE occurrence du
      bloc horaire : `(md.match(/Matin : 9h00/g) || []).length === 1` ET `(md.match(/9h00–13h00/g) || []).length === 1`.
    - Le titre est `### Organisation des journées` (pluriel) et NON plus `### Jour 1 … ### Jour 9`.
    - 105h (13 jours pleins identiques + dernier reliquat 1h partiel) → grille pleine 1× + UNE ligne dédiée
      pour le dernier jour partiel (ses horaires : matin 9h00–10h00). Le bloc plein n'est PAS répété 13×.
    - 8h (1 jour) → STRICTEMENT inchangé : un seul `### Jour 1`, format actuel (non-régression PROD-0062).
    - Appel slice mono-jour (nbJours:1) depuis le déroulé → inchangé (format "### Jour 1").
  </behavior>
  <action>
    Dans `programme-normalize.ts`, modifier UNIQUEMENT `renderHoraireScaffoldMd(scaffold)` :

    (1) Cas `scaffold.nbJours === 1` → comportement ACTUEL inchangé (titre "## Grille horaire imposée…"
    + `renderJourLines(jour, 1)`). NE PAS toucher cette branche (préserve déroulé slice + PROD-0062).

    (2) Cas `scaffold.nbJours > 1` :
      - Détecter les journées PLEINES (helper local pur, ex `isJourPlein(j)` = `j.travailTotalMin === 480
        && j.matin.label === HORAIRE_MATIN_PROG && j.apresMidi.label === HORAIRE_APREM_PROG`).
      - Compter `pleins` (les (N-1) ou N premiers jours pleins) et identifier un éventuel dernier jour PARTIEL.
      - Rendre UN SEUL bloc sous le titre `### Organisation des journées` avec une ligne synthétique :
        `Chaque journée : 9h00–13h00 / déjeuner 13h00–14h00 / 14h00–18h00 — 8h (pauses café ~10h45 et ~15h45 incluses).`
        (Réutiliser HORAIRE_MATIN_PROG / HORAIRE_APREM_PROG / PAUSE_DEJEUNER / PAUSE_CAFE_*_PROG — pas de littéral magique.)
      - Si le dernier jour est PARTIEL (non plein) : ajouter UNE seule ligne supplémentaire dédiée à ce jour-là
        avec ses horaires réels (réutiliser les labels du `HoraireJour` partiel : matin.label, et après-midi.label
        si travailMin>0, sinon mentionner « matin seul »). Ex : `Dernier jour (jour ${N}) : 9h00–10h00 — 1h.`
        Ne PAS répéter le bloc plein par jour.
      - Conserver l'en-tête de comptage `## Grille horaire imposée — ${nbJours} jours (${durationHours}h au total)`
        et la consigne finale, mais adapter la consigne pour dire de mentionner l'horaire UNE SEULE FOIS
        (« … les horaires sont identiques chaque jour ; ne les répète pas par jour »).

    (3) `qualiopi-prompts.ts` — `SYSTEM_PROMPT_NORMALIZE_PROGRAMME` : ajuster la règle FORMAT DE RENDU pour que
    le LLM mentionne l'horaire UNE SEULE FOIS sous une section « Organisation des journées » et NE le répète PAS
    par jour ; le CONTENU (modules) reste réparti par jour (### Jour K). Garder la règle FIDÉLITÉ
    (décliner-pas-enrichir) et la règle VERBES D'ACTION intactes — enforceProgrammeFidelity préservé.
    Bumper `PROMPT_VERSION` (ex `qualiopi-gen-v9-2026-06-18`) puisque le prompt change.

    (4) `__tests__/programme-normalize.test.ts` — adapter le bloc `renderHoraireScaffoldMd` :
      - Remplacer le test « liste chaque Jour K » (72h) par : titre `### Organisation des journées` présent,
        PAS de `### Jour 9`, et `(md.match(/9h00–13h00/g) || []).length === 1` (1 seule occurrence du bloc plein).
      - Ajouter un test 105h : 1 bloc plein (1 occurrence `9h00–13h00` pour le générique pleins) + 1 ligne
        dernier jour partiel contenant `9h00–10h00`. (Attention : si la ligne générique ET la ligne partielle
        contiennent `9h00`, compter sur un motif spécifique au bloc plein, ex `/Chaque journée/` length===1,
        et asserter la présence de `9h00–10h00` pour le partiel.)
      - Conserver/adapter le test 8h « un seul ### Jour 1 » (non-régression mono-jour) — il doit rester vert.
      - Garder le test de déterminisme (renderHoraireScaffoldMd(72) === renderHoraireScaffoldMd(72)).
    NE PAS lancer de génération réelle. enforceProgrammeFidelity et significantTokens NON modifiés.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "Organisation des journées" apps/web/src/lib/programme-normalize.ts && pnpm --filter @qualiof/web exec vitest run programme-normalize 2>&1 | tail -20 | grep -qE "passed|PASS" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | grep -v "redirect-308.test.ts\|sessions.ts:804\|shared-template.test.ts" | grep -c "error TS" | grep -qx 0 && echo OK-COR3</automated>
  </verify>
  <done>renderHoraireScaffoldMd : multi-jours = 1 bloc « Organisation des journées » (horaire 1×) + ligne dernier jour si partiel ; 1 jour inchangé ; prompt bumpé ; tests programme-normalize verts ; tsc vert (hors préexistants).</done>
</task>

</tasks>

<verification>
- `.env` : 3 valeurs OF_ADDRESS_* = Vence. DB Tenant SELECT → address Vence.
- `doc-version.ts` créé ; DOC_VERSION importé par les 2 footers (aucun littéral dupliqué) ; ligne version rendue.
- `renderHoraireScaffoldMd` : nbJours>1 → 1 occurrence du bloc horaire sous « Organisation des journées » + 1 ligne dernier jour partiel le cas échéant ; nbJours===1 inchangé.
- `SYSTEM_PROMPT_NORMALIZE_PROGRAMME` ajusté + PROMPT_VERSION bumpé ; fidélité/verbes préservés.
- `pnpm --filter @qualiof/web exec tsc --noEmit` vert (ignorer préexistants : redirect-308.test.ts ×6, sessions.ts:804, shared-template.test.ts Test6 jpeg).
- `pnpm --filter @qualiof/web exec vitest run programme-normalize` vert.
- Commits atomiques sur cloud-migration (1 par COR). PAS de worktree. Worker Ollama / WIP Laurent non touchés. AUCUNE génération réelle lancée.
</verification>

<success_criteria>
- COR-1 : adresse Vence dans `.env` (footers) ET DB Tenant (corps des docs), vérifiée au SELECT.
- COR-2 : « V2 du 01/03/2025 » présent dans les 2 footers via const partagée unique DOC_VERSION.
- COR-3 : programme multi-jours affiche l'horaire 1× (« Organisation des journées ») ; 1 jour inchangé ; prompt LLM ajusté ; tests déterministes adaptés et verts.
- tsc vert (hors préexistants connus) ; vitest programme-normalize vert.
</success_criteria>

<output>
After completion, create `.planning/quick/260618-ot9-footer-vence-v2-du-01-03-2025-programme-/260618-ot9-SUMMARY.md`
</output>
