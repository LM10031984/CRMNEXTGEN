---
phase: quick-260703-g65
plan: 01
subsystem: google-calendar
tags: [calendar, qualiopi, rappels, attachments, drive-links]
requires:
  - "apps/web/src/lib/calendar/texts.ts (renderRappelText, DRIVE_DOCS, driveViewUrl)"
  - "apps/web/src/lib/calendar/event-builders.ts (buildFormationAttachments, buildRappelEvents, toTextCtx)"
provides:
  - "Rappels quotidiens J-15…J-1 avec 3 docs Qualiopi cliquables + ligne programme optionnelle"
  - "Attachments Drive (programme + Charte/RI/CGV) portés par les 15 rappels, comme l'événement [Formation]"
affects:
  - "Synchronisation Google Calendar (Phase 14) — corps et pièces jointes des rappels quotidiens"
tech-stack:
  added: []
  patterns:
    - "Réutilisation du helper buildFormationAttachments (pas de re-implémentation de la liste docs)"
    - "programmeUrl optionnel dérivé de programmeDriveId (undefined si programme en MinIO)"
key-files:
  created: []
  modified:
    - "apps/web/src/lib/calendar/texts.ts"
    - "apps/web/src/lib/calendar/__tests__/texts.test.ts"
    - "apps/web/src/lib/calendar/event-builders.ts"
    - "apps/web/src/lib/calendar/__tests__/event-builders.test.ts"
decisions:
  - "Ligne programme insérée AU-DESSUS de la ligne Charte, uniquement si programmeUrl fourni"
  - "Attachments des rappels = buildFormationAttachments(ctx.programmeDriveId) — identiques à l'événement formation"
  - "Aucun changement de signature ni de call-site : programmeDriveId circule déjà dans SessionEventCtx"
metrics:
  duration: "~8 min"
  completed: "2026-07-03"
  tasks: 2
  files: 4
  commits: 4
---

# Quick 260703-g65 : Rappels calendrier — liens docs (Charte/RI/CGV) + pièces jointes Summary

**One-liner :** Les 3 documents Qualiopi des rappels Google Calendar quotidiens deviennent des liens Drive cliquables (Charte accueil handicap / Règlement intérieur / CGV), une ligne « Programme de la formation » s'ajoute quand l'URL programme existe, et les 15 rappels J-15…J-1 portent désormais les mêmes pièces jointes Drive que l'événement [Formation].

## Ce qui a été fait

### Task 1 — `rappelBody` : libellés cliquables + ligne programme optionnelle
- Ajout de `programmeUrl?: string` à `CalendarTextCtx`.
- Dans `rappelBody`, les 3 libellés en texte brut (Charte accueil handicap / Règlement intérieur / Conditions générales de vente) sont rendus en `<a href="{driveViewUrl(DRIVE_DOCS.*)}">…</a>` vers les bons ids Drive (`chartePsh`, `ri`, `cgv`).
- Ligne `<a href="{programmeUrl}">Programme de la formation</a>` insérée juste après « Merci de bien vouloir consulter et lire les documents suivants : » et AU-DESSUS de la ligne Charte, uniquement si `programmeUrl` est fourni.
- Formulation Qualiopi figée intacte au mot près (phrases d'accroche, contact, signature « Emma de Start Academy » inchangées).
- Module resté pur / worker-safe (0 nouvel import ; `driveViewUrl` et `DRIVE_DOCS` déjà présents).

### Task 2 — Rappels quotidiens : attachments + programmeUrl dans le contexte texte
- `toTextCtx` alimente `programmeUrl: ctx.programmeDriveId ? driveViewUrl(ctx.programmeDriveId) : undefined` — la ligne programme bénéficie ainsi à la formation ET aux rappels de façon cohérente (les événements froid n'utilisent pas `rappelBody`, donc sans effet).
- `buildRappelEvents` ajoute `attachments: buildFormationAttachments(ctx.programmeDriveId)` à chaque event (réutilisation du helper existant, pas de re-implémentation).
- Aucun changement de signature de `buildRappelEvents` ni des call-sites (`sync-session.ts` / `load-session-ctx.ts` inchangés — `programmeDriveId` reste `undefined` aujourd'hui côté chargement, comportement voulu : 3 docs statiques sans programme).

## Vérification

- `vitest run src/lib/calendar/__tests__/texts.test.ts` : 20/20 verts (16 baseline + 4 nouveaux).
- `vitest run src/lib/calendar/__tests__/event-builders.test.ts` : 12/12 verts (9 baseline + 3 nouveaux).
- Suite calendar complète : **66/66 verts**, aucune régression (idempotence Phase 14 intacte).
- `tsc --noEmit` (apps/web) : **clean** (exit 0).
- Garde-fou worker-safe : `grep -rnE "requireRole|validateRequest|from 'react'|use client"` sur les 2 fichiers source = **0 match**.
- **Test de puissance (mutation)** : `DRIVE_DOCS.chartePsh` → `DRIVE_DOCS.cgv` dans `rappelBody` → le test « 3 hrefs bons IDs » vire ROUGE (1 failed) → restauré → 66/66 verts. Le test garde bien le bon mapping libellé→id.

## TDD (RED → GREEN)

- Task 1 : `db55b6d` (test RED) → `1b53d1c` (feat GREEN).
- Task 2 : `dba6d0d` (test RED) → `0868b8c` (feat GREEN).

Chaque RED a été prouvé rouge avant l'implémentation ; chaque GREEN confirmé vert.

## Deviations from Plan

### Blocking issue auto-résolu (Rule 3 — environnement)

**1. [Rule 3 - Blocking] Worktree isolé créé sur la mauvaise branche (`main`) au lieu de `cloud-migration`**
- **Trouvé pendant :** initialisation (avant Task 1).
- **Problème :** l'agent a été isolé dans le worktree `worktree-agent-a20a2163d6f8a909f`, basé sur `main` (commit `065f184`, 2026-06-10), qui est 284 commits derrière `cloud-migration` et NE CONTIENT PAS le module `apps/web/src/lib/calendar/` (livré en Phase 14). Le plan cible précisément `texts.ts` et `event-builders.ts` de ce module → fichiers absents du worktree.
- **Vérification de non-destruction :** les 43 commits divergents du worktree (déploiement Vercel/Railway, migration OpenRouter) sont tous préservés sur `origin/main` (`git branch --contains 065f184` → `origin/main`). Repointer la branche jetable de l'agent est donc sans perte.
- **Correctif :** `git reset --hard cloud-migration` dans le worktree → baseline autoritaire (calendar module + toutes dépendances présentes, identiques à ce que le plan décrit). Puis installation des deps (`pnpm install`, ~3.5s, hardlinks store) + `prisma generate` pour permettre tests et tsc.
- **Fichiers modifiés :** aucun fichier code (opération git/env uniquement).
- **Impact :** les 4 commits du plan sont posés sur la branche du worktree, désormais = `cloud-migration` + mes commits. ⚠ Ils ne sont PAS encore sur `cloud-migration` (main checkout) — l'orchestrateur/parent doit récupérer/merger la branche `worktree-agent-a20a2163d6f8a909f`.

### Note d'environnement (bookkeeping)

- Ce SUMMARY est écrit dans le worktree (isolation harness). Les 4 commits de code vivent aussi sur la branche du worktree. STATE.md n'a PAS été muté : (a) tâche « quick » ≠ plan de phase (pas de `advance-plan`) ; (b) contrainte explicite « Do NOT update ROADMAP.md » ; (c) le STATE.md du worktree est une copie figée de `cloud-migration`, le muter divergerait de la version live du main checkout. Bookkeeping STATE à la discrétion de l'orchestrateur.

## Known Stubs

Aucun. Le champ `programmeDriveId` reste `undefined` au chargement (programme en MinIO, pas d'id Drive) — c'est le comportement métier voulu et documenté dans le plan (les rappels portent alors 3 docs statiques, sans ligne/attachment programme), pas un stub non câblé.

## Self-Check: PASSED

- 4 fichiers source/test modifiés : tous FOUND.
- SUMMARY.md : FOUND.
- 4 commits (`db55b6d`, `1b53d1c`, `dba6d0d`, `0868b8c`) : tous FOUND.
