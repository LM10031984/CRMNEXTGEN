---
phase: quick-260703-g65
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/calendar/texts.ts
  - apps/web/src/lib/calendar/__tests__/texts.test.ts
  - apps/web/src/lib/calendar/event-builders.ts
  - apps/web/src/lib/calendar/__tests__/event-builders.test.ts
autonomous: true
requirements: [QUICK-CAL-LINKS]
must_haves:
  truths:
    - "Les 3 documents (Charte accueil handicap / Règlement intérieur / Conditions générales de vente) du corps de rappel sont des liens cliquables vers Drive"
    - "Une ligne « Programme de la formation » cliquable apparaît dans la liste des docs UNIQUEMENT si l'URL programme est fournie"
    - "Les 15 événements rappel quotidiens portent les mêmes attachments que l'événement [Formation] (programme si dispo + les 3 docs)"
    - "Aucun autre mot de la formulation Qualiopi figée n'est modifié"
  artifacts:
    - path: "apps/web/src/lib/calendar/texts.ts"
      provides: "rappelBody avec libellés en <a href> + champ programmeUrl optionnel"
      contains: "programmeUrl"
    - path: "apps/web/src/lib/calendar/event-builders.ts"
      provides: "attachments sur les rappels + programmeUrl alimenté dans toTextCtx"
      contains: "buildFormationAttachments"
  key_links:
    - from: "apps/web/src/lib/calendar/event-builders.ts (buildRappelEvents)"
      to: "buildFormationAttachments(ctx.programmeDriveId)"
      via: "attachments sur chaque event rappel"
      pattern: "attachments:\\s*buildFormationAttachments"
    - from: "apps/web/src/lib/calendar/event-builders.ts (toTextCtx)"
      to: "driveViewUrl(ctx.programmeDriveId)"
      via: "programmeUrl injecté dans CalendarTextCtx quand programmeDriveId présent"
      pattern: "programmeUrl"
---

<objective>
Rendre cliquables les 3 documents mentionnés en texte brut dans les rappels quotidiens Google Calendar (Charte accueil handicap / Règlement intérieur / Conditions générales de vente), ajouter une ligne « Programme de la formation » cliquable quand l'URL programme est disponible, et faire porter aux 15 rappels quotidiens les mêmes pièces jointes Drive que l'événement [Formation].

Purpose: aujourd'hui les rappels J-15…J-1 mentionnent les documents en texte brut sans lien et ne portent AUCUNE pièce jointe. L'apprenant ne peut ni cliquer ni ouvrir les documents obligatoires (Qualiopi). Seuls l'événement formation et les événements froid portent des attachments.
Output: 2 fichiers source modifiés + 2 fichiers de tests mis à jour, tsc vert, vitest vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@apps/web/src/lib/calendar/texts.ts
@apps/web/src/lib/calendar/event-builders.ts
@apps/web/src/lib/calendar/__tests__/texts.test.ts
@apps/web/src/lib/calendar/__tests__/event-builders.test.ts

<interfaces>
<!-- Contrats déjà en place — l'exécuteur les utilise directement, pas d'exploration. -->

Dans texts.ts :
```typescript
export const DRIVE_DOCS = {
  cgv: '11mfi7rl8BQFhETty4vGat3GmoGclBuFx',
  ri: '1o44Zg9dXdbyJpQ-U5Tpfbx8lZjcm-Qyf',
  chartePsh: '1HxT_uy6UNIZBYGSl9gchT0sS9_DaidNM',
  questionnaireFroid: '1uNEa7QemfEYKyYf5ywGIdvyshWTjhYjd',
} as const;

export function driveViewUrl(fileId: string): string; // -> https://drive.google.com/file/d/{id}/view

export type CalendarTextCtx = {
  formation: string; dates: string; heureDebut: string; lieu: string;
  dureeJours: string; dureeH: string; formateur: string; countdown: string;
  salutationPrenoms: string; isSiege: boolean;
  // À AJOUTER : programmeUrl?: string
};
```

Dans event-builders.ts :
```typescript
// buildRappelEvents(ctx) reçoit DÉJÀ le SessionEventCtx complet, qui porte programmeDriveId?: string.
// AUCUN changement de signature ni de call-site (sync-session.ts / load-session-ctx.ts) n'est requis :
// le champ programmeDriveId circule déjà. load-session-ctx le laisse à `undefined` aujourd'hui
// (programme en MinIO, pas d'id Drive) → dans ce cas AUCUNE ligne programme / AUCUN attachment programme,
// seulement les 3 docs statiques. C'est le comportement voulu.
function buildFormationAttachments(programmeDriveId?: string): calendar_v3.Schema$EventAttachment[];
function toTextCtx(ctx: SessionEventCtx, countdown: string): CalendarTextCtx;
```
</interfaces>

Corps de rappel figé (texts.ts, `rappelBody`) — le bloc à modifier est EXACTEMENT :
```
Merci de bien vouloir consulter et lire les documents suivants :
Charte accueil handicap
Règlement intérieur
Conditions générales de vente
```
Seuls les 3 libellés deviennent des `<a href>` ; une ligne programme s'insère au-dessus si `programmeUrl` fourni. AUCUN autre mot ne bouge.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rappelBody — libellés cliquables + ligne programme optionnelle</name>
  <files>apps/web/src/lib/calendar/texts.ts, apps/web/src/lib/calendar/__tests__/texts.test.ts</files>
  <behavior>
    - `renderRappelText(ctx)` : le libellé « Charte accueil handicap » devient `<a href="{driveViewUrl(DRIVE_DOCS.chartePsh)}">Charte accueil handicap</a>` (idem RI → DRIVE_DOCS.ri, CGV → DRIVE_DOCS.cgv). Le texte des 3 libellés reste identique au mot près.
    - Test : la sortie contient les 3 IDs Drive (`DRIVE_DOCS.chartePsh`, `.ri`, `.cgv`) chacun dans un `href` accolé au bon libellé.
    - `programmeUrl?: string` ajouté à `CalendarTextCtx`. Quand fourni, une ligne `<a href="{programmeUrl}">Programme de la formation</a>` s'insère dans le bloc « Merci de bien vouloir consulter et lire les documents suivants : », AU-DESSUS de la ligne Charte.
    - Test : `renderRappelText({ ...baseCtx, programmeUrl: 'https://drive.google.com/file/d/PROGID/view' })` contient « Programme de la formation » ET « PROGID » ; `renderRappelText(baseCtx)` (sans programmeUrl) NE contient PAS « Programme de la formation ».
    - Garde-fou (test de non-régression) : la sortie contient toujours mot pour mot « Merci de bien vouloir consulter et lire les documents suivants : » et « Emma de Start Academy ».
  </behavior>
  <action>
    Dans `texts.ts` :
    1. Ajouter `programmeUrl?: string;` à `CalendarTextCtx` (avec commentaire de doc, dans la même veine que les autres champs).
    2. Dans `rappelBody(ctx)`, remplacer le bloc des 3 libellés en texte brut par des liens HTML via `driveViewUrl(DRIVE_DOCS.chartePsh|ri|cgv)`. Construire une ligne programme optionnelle : `const programmeLine = ctx.programmeUrl ? \`<a href="${ctx.programmeUrl}">Programme de la formation</a>\n\` : '';` insérée juste après la ligne « Merci de bien vouloir consulter et lire les documents suivants : » et AVANT la ligne Charte. NE PAS toucher au reste du template (accroche, lieu, durée, formateur, checklist, contact, signature, ligne countdown, bloc siège).
    3. Le module reste PUR / worker-safe : aucun nouvel import serveur/auth/React. `driveViewUrl` et `DRIVE_DOCS` sont déjà dans le fichier.
    Dans `texts.test.ts` : ajouter les tests décrits dans <behavior> au describe `renderRappelText` (3 hrefs avec bons IDs, ligne programme présente/absente selon programmeUrl, non-régression des phrases figées). Ne pas casser les tests existants (countdown, siège, formateur).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter web exec vitest run src/lib/calendar/__tests__/texts.test.ts</automated>
  </verify>
  <done>Les 3 libellés sont des liens vers les bons IDs Drive, la ligne programme apparaît ssi programmeUrl fourni, les phrases Qualiopi figées sont intactes, tests verts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rappels quotidiens — attachments + programmeUrl dans le contexte texte</name>
  <files>apps/web/src/lib/calendar/event-builders.ts, apps/web/src/lib/calendar/__tests__/event-builders.test.ts</files>
  <behavior>
    - Chaque événement produit par `buildRappelEvents(ctx)` porte `attachments: buildFormationAttachments(ctx.programmeDriveId)` — mêmes pièces jointes que `buildFormationEvent` (programme si dispo + Charte + RI + CGV).
    - Test : avec `baseCtx` (programmeDriveId défini), chaque rappel a `attachments.length === 4` et l'un des `fileUrl` contient l'ID programme.
    - Test : avec `{ ...baseCtx, programmeDriveId: undefined }`, chaque rappel a `attachments.length === 3` (les 3 docs statiques, pas de programme).
    - Le corps texte du rappel contient la ligne programme quand programmeDriveId est présent (programmeUrl alimenté dans toTextCtx) et ne la contient pas sinon.
  </behavior>
  <action>
    Dans `event-builders.ts` :
    1. Dans `toTextCtx(ctx, countdown)`, ajouter `programmeUrl: ctx.programmeDriveId ? driveViewUrl(ctx.programmeDriveId) : undefined,` au retour (driveViewUrl est déjà importé). Cela fait bénéficier formation ET rappels de la ligne programme de façon cohérente (les événements froid n'utilisent pas rappelBody, donc sans effet).
    2. Dans `buildRappelEvents(ctx)`, ajouter à chaque event poussé : `attachments: buildFormationAttachments(ctx.programmeDriveId),`. Réutiliser le helper existant (NE PAS réimplémenter la liste des docs). Aucun changement de signature de `buildRappelEvents` ni des call-sites (`sync-session.ts` passe déjà `ctx` complet ; `load-session-ctx.ts` continue de fixer `programmeDriveId: undefined` — comportement voulu, à ne pas modifier).
    3. Le module reste worker-safe (aucun import interdit ajouté).
    Dans `event-builders.test.ts` : ajouter au describe `buildRappelEvents` les tests décrits dans <behavior> (attachments 4 avec ID programme ; attachments 3 quand programmeDriveId undefined ; corps contient/omets la ligne programme). Ne pas casser les tests existants (15 rappels, colorId 6, « demain », popup only).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter web exec vitest run src/lib/calendar/__tests__/event-builders.test.ts src/lib/calendar/__tests__/texts.test.ts</automated>
  </verify>
  <done>Les 15 rappels portent les attachments de la formation (4 si programme dispo, 3 sinon) et le corps intègre la ligne programme quand programmeDriveId est présent ; tests verts.</done>
</task>

</tasks>

<verification>
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter web exec tsc --noEmit` reste vert.
- `pnpm --filter web exec vitest run src/lib/calendar` : toute la suite calendar verte (67 tests baseline Phase 14 + nouveaux, aucune régression).
- Garde-fou worker-safe : `grep -rnE "requireRole|validateRequest|from 'react'|use client" apps/web/src/lib/calendar/texts.ts apps/web/src/lib/calendar/event-builders.ts` = 0.
- Test de puissance (au gate) : casser un lien (ex. remplacer `DRIVE_DOCS.chartePsh` par `DRIVE_DOCS.cgv` dans rappelBody) → le test « 3 hrefs bons IDs » DOIT virer rouge → restaurer.
</verification>

<success_criteria>
- Les 3 documents du corps de rappel sont des liens `<a href>` vers les bons IDs Drive.
- La ligne « Programme de la formation » apparaît ssi `programmeUrl`/`programmeDriveId` fourni.
- Les 15 rappels quotidiens portent `buildFormationAttachments(ctx.programmeDriveId)`.
- Formulation Qualiopi figée intacte au mot près (hors mise en lien des libellés + ligne programme).
- tsc vert, vitest calendar vert, module resté pur/worker-safe.
</success_criteria>

<output>
After completion, create `.planning/quick/260703-g65-rappels-calendrier-liens-docs-charte-ri-/260703-g65-SUMMARY.md`
</output>
