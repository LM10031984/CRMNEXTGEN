---
phase: quick-260525-j9d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/settings/settings-section.tsx
  - apps/web/src/app/app/parametres/page.tsx
autonomous: true
requirements:
  - QUICK-260525-j9d
must_haves:
  truths:
    - "Laurent ouvre /app/parametres sans voir l'erreur RSC 'Functions cannot be passed directly to Client Components'"
    - "Les 11 sections de la page Paramètres s'affichent avec leur icône Lucide en haut à gauche (même rendu visuel qu'avant)"
    - "Le styling des icônes est identique : taille h-5 w-5, marge mt-0.5, couleur text-primary, shrink-0"
    - "Les tests smoke (page.smoke.test.ts) restent verts"
    - "tsc --noEmit passe sans erreur sur apps/web"
  artifacts:
    - path: "apps/web/src/components/settings/settings-section.tsx"
      provides: "Composant SettingsSection accepte un icon ReactNode (sérialisable RSC) au lieu d'un LucideIcon (fonction)"
      contains: "icon: ReactNode"
    - path: "apps/web/src/app/app/parametres/page.tsx"
      provides: "Page Paramètres qui passe l'icône en JSX préformaté <Icon className=... /> aux 11 SettingsSection"
      contains: "icon={<"
  key_links:
    - from: "apps/web/src/app/app/parametres/page.tsx"
      to: "apps/web/src/components/settings/settings-section.tsx"
      via: "prop icon={<Building2 className=... />}"
      pattern: "icon=\\{<[A-Z]"
---

<objective>
Fix de l'erreur runtime RSC sur `/app/parametres` : `SettingsSection` est un Client Component qui reçoit une référence de fonction Lucide (`LucideIcon`) depuis un Server Component, ce qui est interdit en Next.js 14 (les fonctions ne sont pas sérialisables au passage RSC → Client).

Solution : changer la signature de `SettingsSection` pour accepter un `ReactNode` déjà rendu (l'icône JSX est construite côté Server Component), et adapter les 11 call sites de `parametres/page.tsx` pour passer `icon={<X className="h-5 w-5 mt-0.5 text-primary shrink-0" aria-hidden="true" />}`.

Purpose : débloquer Laurent qui ne peut plus ouvrir l'onglet Paramètres (page entièrement cassée).
Output : page Paramètres fonctionnelle, styling des icônes inchangé, tests smoke verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/settings/settings-section.tsx
@apps/web/src/app/app/parametres/page.tsx
@apps/web/src/app/app/parametres/__tests__/page.smoke.test.ts

<interfaces>
<!-- Signature actuelle (à changer) -->
```typescript
// settings-section.tsx - AVANT
import { type LucideIcon } from 'lucide-react';

export interface SettingsSectionProps {
  icon: LucideIcon;  // ← Fonction React, non-sérialisable RSC
  title: string;
  description?: string;
  readView: ReactNode;
  editView?: (onSaved: () => void, onCancel: () => void) => ReactNode;
  allowEdit?: boolean;
  className?: string;
}

export function SettingsSection({ icon: Icon, ... }: SettingsSectionProps) {
  // Usage interne :
  // <Icon className="h-5 w-5 mt-0.5 text-primary shrink-0" aria-hidden="true" />
}
```

<!-- Signature cible -->
```typescript
// settings-section.tsx - APRÈS
// (LucideIcon supprimé de l'import — plus utilisé)

export interface SettingsSectionProps {
  icon: ReactNode;  // ← JSX déjà rendu, sérialisable
  title: string;
  description?: string;
  readView: ReactNode;
  editView?: (onSaved: () => void, onCancel: () => void) => ReactNode;
  allowEdit?: boolean;
  className?: string;
}

export function SettingsSection({ icon, ... }: SettingsSectionProps) {
  // Usage interne : {icon}  (plus de destructuration en alias Icon)
}
```

<!-- Call sites parametres/page.tsx (11 occurrences) -->
Lignes : 97 (Building2), 126 (MapPin), 172 (Image), 209 (Hash), 241 (Receipt),
284 (FileText), 329 (CreditCard), 363 (Mail), 398 (Users), 419 (Sparkles), 448 (Settings).

Pattern de remplacement (DRY via constante locale en haut du fichier) :
```typescript
const ICON_CLASS = "h-5 w-5 mt-0.5 text-primary shrink-0";

<SettingsSection
  icon={<Building2 className={ICON_CLASS} aria-hidden="true" />}
  ...
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrer SettingsSection icon prop LucideIcon → ReactNode + adapter 11 call sites parametres/page.tsx</name>
  <files>
    apps/web/src/components/settings/settings-section.tsx,
    apps/web/src/app/app/parametres/page.tsx
  </files>
  <action>
**Étape A — Refactor `apps/web/src/components/settings/settings-section.tsx` :**

1. Ligne 4 : supprimer l'import `import { type LucideIcon } from 'lucide-react';` (devient inutile).
2. Ligne 26 : remplacer `icon: LucideIcon;` par `icon: ReactNode;` dans `SettingsSectionProps`.
3. Ligne 42 : remplacer `icon: Icon,` par `icon,` dans la destructuration des props de `function SettingsSection({...})`.
4. Lignes 64-67 : remplacer le bloc :
   ```tsx
   <Icon
     className="h-5 w-5 mt-0.5 text-primary shrink-0"
     aria-hidden="true"
   />
   ```
   par simplement :
   ```tsx
   {icon}
   ```
5. Conserver le commentaire JSDoc en haut du fichier (lignes 7-23) — toujours valide.

**Étape B — Adapter `apps/web/src/app/app/parametres/page.tsx` :**

1. Juste après l'import block (après la ligne 27 `import { formatIban } from '@/lib/iban-format';`), ajouter une ligne :
   ```typescript
   const ICON_CLASS = 'h-5 w-5 mt-0.5 text-primary shrink-0';
   ```
   (constante locale pour DRY, évite de répéter 11 fois la même chaîne).

2. Remplacer les 11 props `icon={X}` par `icon={<X className={ICON_CLASS} aria-hidden="true" />}` :
   - Ligne 97 : `icon={Building2}` → `icon={<Building2 className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 126 : `icon={MapPin}` → `icon={<MapPin className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 172 : `icon={Image}` → `icon={<Image className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 209 : `icon={Hash}` → `icon={<Hash className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 241 : `icon={Receipt}` → `icon={<Receipt className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 284 : `icon={FileText}` → `icon={<FileText className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 329 : `icon={CreditCard}` → `icon={<CreditCard className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 363 : `icon={Mail}` → `icon={<Mail className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 398 : `icon={Users}` → `icon={<Users className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 419 : `icon={Sparkles}` → `icon={<Sparkles className={ICON_CLASS} aria-hidden="true" />}`
   - Ligne 448 : `icon={Settings}` → `icon={<Settings className={ICON_CLASS} aria-hidden="true" />}`

3. Ne PAS modifier les imports Lucide (les 11 icônes restent importées et désormais utilisées en JSX inline dans la page).

4. Ne pas toucher la fonction `Field` ni la balise `<FileText className="h-3.5 w-3.5" aria-hidden="true" />` ligne 459 (déjà inline, pas concernée).

**Étape C — Vérifications :**

1. Confirmer que `SettingsSection` n'est utilisé NULLE PART ailleurs (sinon il faudra adapter aussi) :
   ```bash
   grep -rn "SettingsSection" apps/web/src --include="*.tsx" --include="*.ts" | grep -v "settings-section.tsx" | grep -v "parametres/page.tsx"
   ```
   Si d'autres consommateurs existent, les adapter au même pattern.

2. Lancer `tsc --noEmit` (via `pnpm --filter @qualiof/web build` ou `pnpm -w typecheck` selon ce qui est dispo).

3. Lancer le test smoke :
   ```bash
   pnpm --filter @qualiof/web test -- page.smoke.test.ts
   ```
   Les 4 tests doivent passer (imports lucide-react inchangés, 6 forms toujours référencés, JSX tags valides).

4. Commit unique :
   ```bash
   git add apps/web/src/components/settings/settings-section.tsx apps/web/src/app/app/parametres/page.tsx
   git commit -m "fix(parametres): SettingsSection accepts ReactNode icon (RSC compat)

   Server→Client passage de LucideIcon (fonction) déclenchait :
   'Functions cannot be passed directly to Client Components'.

   Solution : icon prop devient ReactNode, le JSX <Icon … /> est construit
   côté Server Component (sérialisable RSC).

   Styling préservé via constante locale ICON_CLASS dans page.tsx.
   Tests smoke verts. Aucun autre consommateur de SettingsSection."
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- src/app/app/parametres/__tests__/page.smoke.test.ts --run 2>&1 | tail -30</automated>
    Et : `grep -c "icon={<" apps/web/src/app/app/parametres/page.tsx` doit retourner `11`.
    Et : `grep -c "LucideIcon" apps/web/src/components/settings/settings-section.tsx` doit retourner `0`.
    Et : `pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | grep -c "error TS"` doit retourner `0`.
  </verify>
  <done>
    - settings-section.tsx : import `LucideIcon` supprimé, `icon: ReactNode`, usage interne `{icon}`.
    - parametres/page.tsx : constante `ICON_CLASS` définie, 11 props `icon={<X className={ICON_CLASS} aria-hidden="true" />}`.
    - 4 tests smoke verts.
    - `tsc --noEmit` propre.
    - Laurent peut ouvrir `/app/parametres` sans erreur RSC après `pnpm dev:full` (validation visuelle hors-périmètre Claude — Laurent teste lui-même).
    - 1 commit `fix(parametres): SettingsSection accepts ReactNode icon (RSC compat)`.
  </done>
</task>

</tasks>

<verification>
**Tests automatisés (CI):**
- `pnpm --filter @qualiof/web test -- page.smoke.test.ts --run` → 4/4 verts
- `pnpm --filter @qualiof/web exec tsc --noEmit` → 0 erreur

**Vérifs grep:**
- `grep -c "icon={<" apps/web/src/app/app/parametres/page.tsx` = 11
- `grep "LucideIcon" apps/web/src/components/settings/settings-section.tsx` = vide
- `grep -rn "SettingsSection" apps/web/src --include="*.tsx"` confirme un seul consommateur (parametres/page.tsx)

**Vérif manuelle Laurent (hors-périmètre Claude):**
- Ouvrir `http://localhost:3000/app/parametres` après `pnpm dev:full` → la page s'affiche avec les 11 cards et leurs icônes, sans bandeau d'erreur.
</verification>

<success_criteria>
1. La page `/app/parametres` n'émet plus l'erreur runtime « Functions cannot be passed directly to Client Components ».
2. Les 11 icônes restent visibles, alignées, à la bonne taille (h-5 w-5), avec la bonne couleur (text-primary).
3. Les boutons « Modifier » fonctionnent toujours sur les 8 sections éditables.
4. Le test smoke (page.smoke.test.ts) reste vert (4/4).
5. `tsc --noEmit` passe sans erreur.
6. Commit atomique unique.
</success_criteria>

<output>
After completion, create `.planning/quick/260525-j9d-fix-rsc-error-page-param-tres-settingsse/260525-j9d-SUMMARY.md` documenting :
- Fichiers modifiés (2)
- Root cause RSC (fonction non-sérialisable Server→Client)
- Pattern de fix appliqué (icon: ReactNode + JSX construit côté Server)
- Tests verts
- Validation Laurent en attente (ouverture /app/parametres)
</output>
