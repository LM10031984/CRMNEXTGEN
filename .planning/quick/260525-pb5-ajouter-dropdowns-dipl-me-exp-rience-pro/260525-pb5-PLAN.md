---
phase: quick-260525-pb5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/agefice-options.ts
  - apps/web/src/lib/agefice-form-fill.ts
  - apps/web/src/server/actions/agefice-generator.ts
  - apps/web/src/server/actions/crud-edits.ts
  - apps/web/src/components/forms/create-person-button.tsx
  - apps/web/src/components/forms/edit-person-button.tsx
autonomous: true
requirements:
  - PB5-01  # Dropdown Diplôme AGEFICE 6 options (création + édition)
  - PB5-02  # Dropdown Expérience pro AGEFICE 4 tranches (création + édition)
  - PB5-03  # Constante partagée DIPLOME_OPTIONS / EXPERIENCE_OPTIONS source unique
  - PB5-04  # Rétro-compat 291 fiches texte libre existant

must_haves:
  truths:
    - "Lors de la création d'un apprenant, Laurent voit un dropdown 'Dernier diplôme' avec exactement 6 options identiques au PDF AGEFICE"
    - "Lors de la création d'un apprenant, Laurent voit un dropdown 'Expérience pro' avec exactement 4 tranches identiques au PDF AGEFICE"
    - "Lors de l'édition d'un apprenant, les champs diplôme + expérience sont devenus des dropdowns alignés AGEFICE (plus de texte libre)"
    - "La valeur saisie en création est persistée sur Person.diplomas / Person.professionalExperience"
    - "Une fiche apprenant existante avec valeur texte libre (legacy) reste éditable sans perte de donnée — la valeur courante est visible en option spéciale 'Valeur actuelle'"
    - "La génération AGEFICE PDF (resolveDiplome + inferExperience) accepte directement les nouvelles valeurs exactes sans heuristique de matching"
    - "Le tableau DIPLOME_OPTIONS n'existe plus qu'à un seul endroit (lib/agefice-options.ts) — import partagé"
  artifacts:
    - path: "apps/web/src/lib/agefice-options.ts"
      provides: "Source unique DIPLOME_OPTIONS (6 strings) + EXPERIENCE_OPTIONS (4 tuples value/label) + types DiplomeOption / ExperienceValue"
      min_lines: 20
    - path: "apps/web/src/lib/agefice-form-fill.ts"
      provides: "Refactor : import DIPLOME_OPTIONS depuis agefice-options.ts (suppression tableau local lignes 160-167), resolveDiplome conservé en fallback heuristique"
    - path: "apps/web/src/server/actions/agefice-generator.ts"
      provides: "inferExperience(raw) — check exact en amont sur EXPERIENCE_OPTIONS.value avant l'heuristique mots-clés (rétro-compat texte libre)"
    - path: "apps/web/src/server/actions/crud-edits.ts"
      provides: "createPerson — accepte 2 nouveaux champs optionnels diplomas + professionalExperience persistés sur Person"
    - path: "apps/web/src/components/forms/create-person-button.tsx"
      provides: "2 <select> diplomas + professionalExperience dans la section infos pro, useState + reset(), payload createPerson étendu"
    - path: "apps/web/src/components/forms/edit-person-button.tsx"
      provides: "Remplacement des 2 text inputs (diplomas + professionalExperience) par dropdowns avec option 'Valeur actuelle' si legacy hors options"
  key_links:
    - from: "apps/web/src/components/forms/create-person-button.tsx"
      to: "apps/web/src/lib/agefice-options.ts"
      via: "import { DIPLOME_OPTIONS, EXPERIENCE_OPTIONS }"
      pattern: "from ['\"]@/lib/agefice-options['\"]"
    - from: "apps/web/src/components/forms/edit-person-button.tsx"
      to: "apps/web/src/lib/agefice-options.ts"
      via: "import { DIPLOME_OPTIONS, EXPERIENCE_OPTIONS }"
      pattern: "from ['\"]@/lib/agefice-options['\"]"
    - from: "apps/web/src/lib/agefice-form-fill.ts"
      to: "apps/web/src/lib/agefice-options.ts"
      via: "import { DIPLOME_OPTIONS }"
      pattern: "from ['\"]\\./agefice-options['\"]"
    - from: "apps/web/src/components/forms/create-person-button.tsx"
      to: "apps/web/src/server/actions/crud-edits.ts (createPerson)"
      via: "payload diplomas + professionalExperience passés à createPerson"
      pattern: "diplomas:\\s*\\w+|professionalExperience:\\s*\\w+"
---

<objective>
Aligner les formulaires de création et d'édition d'apprenant sur les valeurs exactes AGEFICE pour les 2 champs critiques restants identifiés dans l'audit AGEFICE 2026-05-25 : **Dernier diplôme** (dropdown 6 options) et **Expérience pro** (dropdown 4 tranches).

**Purpose** : éliminer les 2 derniers champs "absents UI" sur 60 du formulaire AGEFICE PDF (cf. audit). Aujourd'hui : texte libre côté UI + heuristique de mots-clés côté générateur PDF → risque que l'IA "invente" la tranche. Demain : choix discret aligné mot pour mot sur le dropdown PDF AGEFICE → zéro friction, zéro perte d'info.

**Output** :
- 1 nouvelle constante partagée `lib/agefice-options.ts` (source unique de vérité)
- 2 refactors discrets côté lib (`agefice-form-fill.ts` + `agefice-generator.ts`) qui consomment la constante
- 1 extension server action `createPerson` (2 nouveaux champs optionnels)
- 2 formulaires alignés : création (2 nouveaux dropdowns) + édition (text → dropdown avec fallback "Valeur actuelle")
- Rétro-compat préservée : 291 fiches en texte libre legacy continuent de fonctionner (heuristique conservée en fallback + option "Valeur actuelle" affichée si la valeur ne match aucune option canonique)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/_audit-agefice-260525.md
@apps/web/src/lib/agefice-form-fill.ts
@apps/web/src/server/actions/agefice-generator.ts
@apps/web/src/server/actions/crud-edits.ts
@apps/web/src/components/forms/create-person-button.tsx
@apps/web/src/components/forms/edit-person-button.tsx

<interfaces>
<!-- Contrats existants extraits du code — l'exécuteur consomme ces interfaces sans explorer le codebase. -->

From apps/web/src/lib/agefice-form-fill.ts (lignes 28, 160-182) :
```typescript
export type ExperienceTranche = 'MOINS_1_AN' | '1_3_ANS' | '4_10_ANS' | 'PLUS_10_ANS';

// Tableau LOCAL à supprimer après refactor (sera importé depuis agefice-options.ts)
const DIPLOME_OPTIONS = [
  'Fin de scolarité obligatoire',
  'BEP-CAP',
  'Bac-Bac pro-BT-BP',
  'Bac+2 : BTS-DUT-DEUG',
  'Bac+3 : Licence ou maîtrise',
  'Bac+5 : Supérieur à la maîtrise',
];

// Conservé tel quel — fallback heuristique pour les 291 fiches texte libre legacy
function resolveDiplome(v: string | null | undefined): string {
  if (!v) return '';
  const exact = DIPLOME_OPTIONS.find((o) => o === v);
  if (exact) return exact;
  // ... heuristique par mots-clés (lower.includes('bac+5'), etc.)
}
```

From apps/web/src/server/actions/agefice-generator.ts (lignes 27-42) :
```typescript
// Map "1-3 ans" / "4 ans" / "Plus de 10 ans" → tranche AGEFICE
function inferExperience(raw: string | null | undefined): ExperienceTranche | null {
  if (!raw) return null;
  const s = raw.toLowerCase();
  if (s.includes('moins') || s.match(/<\s*1/)) return 'MOINS_1_AN';
  // ... heuristique mots-clés
}
```

From apps/web/src/server/actions/crud-edits.ts (lignes 28-29, 53-55) :
```typescript
// updatePerson INPUT — déjà OK
interface UpdatePersonInput {
  diplomas?: string | null;
  professionalExperience?: string | null;
}
// Logique de persistence (déjà en place lignes 53-55)
if (input.diplomas !== undefined) data.diplomas = input.diplomas?.trim() || null;
if (input.professionalExperience !== undefined)
  data.professionalExperience = input.professionalExperience?.trim() || null;

// createPerson INPUT (lignes 280-313) — MANQUE diplomas + professionalExperience
export async function createPerson(input: {
  firstName: string; lastName: string;
  // ... 20+ champs existants
  // ❌ diplomas / professionalExperience non présents → À AJOUTER
}): Promise<{ ok: boolean; personId?: string; ... }>;

// Persistence Person dans tx.person.create (lignes 334-348)
// → ajouter diplomas + professionalExperience dans le data: {} de create
```

From `apps/web/src/components/forms/create-person-button.tsx` (pattern useState) :
- Pas de react-hook-form
- Tous les champs : `const [field, setField] = useState('')` + `<input/select value={field} onChange={(e) => setField(e.target.value)}>`
- reset() ligne 64-71 réinitialise tous les useState
- onSubmit ligne 117 → payload `createPerson({...})` ligne 138-159
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Créer la constante partagée agefice-options.ts + refactor lib + simplification inferExperience</name>
  <files>
    apps/web/src/lib/agefice-options.ts (nouveau)
    apps/web/src/lib/agefice-form-fill.ts (modif : import + suppr tableau local)
    apps/web/src/server/actions/agefice-generator.ts (modif : check exact dans inferExperience)
  </files>
  <action>
**Étape 1 — Créer `apps/web/src/lib/agefice-options.ts`** (per PB5-03) :

```typescript
/**
 * Valeurs exactes des dropdowns AGEFICE — alignées mot pour mot sur le PDF officiel.
 * Source unique consommée par :
 *  - lib/agefice-form-fill.ts (resolveDiplome, mapping checkbox expérience)
 *  - server/actions/agefice-generator.ts (inferExperience)
 *  - components/forms/create-person-button.tsx (dropdown saisie)
 *  - components/forms/edit-person-button.tsx (dropdown édition)
 *
 * NE PAS MODIFIER sans vérifier dans le PDF source `apps/web/src/assets/agefice-template.pdf`.
 */

export const DIPLOME_OPTIONS = [
  'Fin de scolarité obligatoire',
  'BEP-CAP',
  'Bac-Bac pro-BT-BP',
  'Bac+2 : BTS-DUT-DEUG',
  'Bac+3 : Licence ou maîtrise',
  'Bac+5 : Supérieur à la maîtrise',
] as const;

export type DiplomeOption = typeof DIPLOME_OPTIONS[number];

export const EXPERIENCE_OPTIONS = [
  { value: 'MOINS_1_AN', label: '< 1 an' },
  { value: '1_3_ANS', label: '1 à 3 ans' },
  { value: '4_10_ANS', label: '4 à 10 ans' },
  { value: 'PLUS_10_ANS', label: '+ de 10 ans' },
] as const;

export type ExperienceValue = typeof EXPERIENCE_OPTIONS[number]['value'];

// Helper pour vérifier si une valeur texte libre matche une des 4 valeurs canoniques
export function isCanonicalExperience(v: string | null | undefined): v is ExperienceValue {
  if (!v) return false;
  return EXPERIENCE_OPTIONS.some((o) => o.value === v);
}
```

**Étape 2 — Refactor `apps/web/src/lib/agefice-form-fill.ts`** (per PB5-03) :
- Ajouter en tête de fichier : `import { DIPLOME_OPTIONS } from './agefice-options';`
- Supprimer le tableau local `const DIPLOME_OPTIONS = [...]` (lignes 160-167)
- Conserver `resolveDiplome(v)` intact (lignes 169-182) — il consomme désormais le tableau importé
- **Ne pas toucher** au reste du fichier (les `set.check('< 1an', ...)` lignes 355-358 utilisent déjà la valeur canonique `ExperienceTranche` — pas de regression possible)

**Étape 3 — Simplifier `inferExperience` dans `apps/web/src/server/actions/agefice-generator.ts`** (per PB5-04) :
- Ajouter en tête : `import { isCanonicalExperience } from '@/lib/agefice-options';`
- Dans `inferExperience(raw)` (ligne 27-42), ajouter un check **AVANT** l'heuristique :
  ```typescript
  function inferExperience(raw: string | null | undefined): ExperienceTranche | null {
    if (!raw) return null;
    // Nouveau : si raw est déjà une valeur canonique (saisie via dropdown), on retourne direct
    if (isCanonicalExperience(raw)) return raw as ExperienceTranche;
    // Sinon, heuristique mots-clés conservée pour les 291 fiches legacy texte libre
    const s = raw.toLowerCase();
    if (s.includes('moins') || s.match(/<\s*1/) || s.includes('< 1')) return 'MOINS_1_AN';
    // ... reste inchangé
  }
  ```

**Pourquoi** : on évite que l'heuristique mots-clés réinterprète une valeur canonique (ex: si Laurent saisit `1_3_ANS`, l'heuristique actuelle ne match rien et retourne `null` → bug invisible).

**Anti-régression** : conserver l'heuristique intacte pour les 291 fiches en texte libre.
  </action>
  <verify>
<automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -20</automated>

Vérifications additionnelles :
- `grep -n "const DIPLOME_OPTIONS" apps/web/src/lib/agefice-form-fill.ts` → doit retourner **0 résultat** (supprimé)
- `grep -n "from.*agefice-options" apps/web/src/lib/agefice-form-fill.ts apps/web/src/server/actions/agefice-generator.ts` → doit retourner **2 imports**
- `grep -n "isCanonicalExperience" apps/web/src/server/actions/agefice-generator.ts` → doit retourner **1 import + 1 usage**
  </verify>
  <done>
- `apps/web/src/lib/agefice-options.ts` existe et exporte `DIPLOME_OPTIONS` (6 strings), `EXPERIENCE_OPTIONS` (4 tuples), `DiplomeOption`, `ExperienceValue`, `isCanonicalExperience`
- `agefice-form-fill.ts` importe `DIPLOME_OPTIONS` depuis le nouveau fichier, tableau local supprimé
- `inferExperience` check exact en amont via `isCanonicalExperience`, heuristique conservée en fallback
- `tsc --noEmit` clean (zéro erreur sur ces 3 fichiers)
  </done>
</task>

<task type="auto">
  <name>Task 2 : Étendre createPerson + ajouter les 2 dropdowns au formulaire de création apprenant</name>
  <files>
    apps/web/src/server/actions/crud-edits.ts (modif : signature createPerson + persistence)
    apps/web/src/components/forms/create-person-button.tsx (modif : useState + 2 selects + payload + reset)
  </files>
  <action>
**Étape 1 — Étendre `createPerson` dans `apps/web/src/server/actions/crud-edits.ts`** (per PB5-01, PB5-02) :

- Dans la signature `createPerson(input: { ... })` (ligne 280), ajouter 2 champs optionnels (au niveau "infos pro", proche de `professionalStatus` ligne 285) :
  ```typescript
  professionalStatus?: string | null;
  // Nouveaux champs AGEFICE — valeurs canoniques DIPLOME_OPTIONS / EXPERIENCE_OPTIONS
  // (saisies via dropdowns), persistées telles quelles sur Person.
  diplomas?: string | null;
  professionalExperience?: string | null;
  ```

- Dans le `tx.person.create({ data: {...} })` (ligne 334-348), ajouter :
  ```typescript
  professionalStatus: input.professionalStatus?.trim() || null,
  diplomas: input.diplomas?.trim() || null,
  professionalExperience: input.professionalExperience?.trim() || null,
  ```

- **Ne pas modifier** `updatePerson` (déjà OK lignes 53-55).

**Étape 2 — Ajouter les dropdowns à `apps/web/src/components/forms/create-person-button.tsx`** (per PB5-01, PB5-02) :

- Ajouter en tête de fichier : `import { DIPLOME_OPTIONS, EXPERIENCE_OPTIONS } from '@/lib/agefice-options';`

- Ajouter 2 useState après `socialSecurityNb` ligne 53 :
  ```typescript
  const [diplomas, setDiplomas] = useState('');
  const [professionalExperience, setProfessionalExperience] = useState('');
  ```

- Dans `reset()` ligne 64-71, ajouter à la suite des resets existants :
  ```typescript
  setDiplomas(''); setProfessionalExperience('');
  ```

- Dans le JSX, après le bloc "Statut professionnel" (ligne 359-362), insérer un nouveau bloc grille 2 colonnes :
  ```tsx
  <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
    <div>
      <label className="block text-xs font-medium text-muted-foreground mb-1">Dernier diplôme (AGEFICE)</label>
      <select
        value={diplomas}
        onChange={(e) => setDiplomas(e.target.value)}
        className="w-full px-3 py-2 border border-border rounded-lg text-sm"
      >
        <option value="">— Choisir —</option>
        {DIPLOME_OPTIONS.map((o) => (
          <option key={o} value={o}>{o}</option>
        ))}
      </select>
    </div>
    <div>
      <label className="block text-xs font-medium text-muted-foreground mb-1">Expérience pro (AGEFICE)</label>
      <select
        value={professionalExperience}
        onChange={(e) => setProfessionalExperience(e.target.value)}
        className="w-full px-3 py-2 border border-border rounded-lg text-sm"
      >
        <option value="">— Choisir —</option>
        {EXPERIENCE_OPTIONS.map((o) => (
          <option key={o.value} value={o.value}>{o.label}</option>
        ))}
      </select>
    </div>
  </div>
  ```

- Dans le payload `createPerson({...})` (ligne 138-159), ajouter 2 lignes (proche de `professionalStatus` ligne 143) :
  ```typescript
  professionalStatus: professionalStatus.trim() || null,
  diplomas: diplomas || null,
  professionalExperience: professionalExperience || null,
  ```

**Note** : on n'appelle pas `.trim()` sur ces 2 champs car ils sortent toujours d'un `<option value="...">` — valeur figée.
  </action>
  <verify>
<automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -20</automated>

Vérifications additionnelles :
- `grep -n "diplomas\|professionalExperience" apps/web/src/components/forms/create-person-button.tsx | wc -l` → doit retourner **≥ 6** (2 useState + 2 reset + 2 JSX + 2 payload + import)
- `grep -n "diplomas\s*[?:]" apps/web/src/server/actions/crud-edits.ts` → doit montrer le nouveau champ dans createPerson (ligne ~285) en plus de la ligne 28 existante d'updatePerson
- Test manuel : `pnpm dev:full`, créer apprenant test "Diplôme = Bac+2", vérifier en BDD (`select diplomas, "professionalExperience" from "Person" where ...`) que les 2 valeurs canoniques sont persistées
  </verify>
  <done>
- `createPerson` accepte 2 nouveaux champs optionnels et les persiste sur `Person.diplomas` / `Person.professionalExperience`
- Le formulaire de création affiche 2 nouveaux dropdowns côte à côte sous "Statut professionnel"
- Les 6 options diplôme et 4 options expérience sont visibles et identiques au PDF AGEFICE
- `reset()` réinitialise les 2 nouveaux états
- `tsc --noEmit` clean
  </done>
</task>

<task type="auto">
  <name>Task 3 : Remplacer text inputs par dropdowns dans edit-person-button.tsx (avec fallback "Valeur actuelle")</name>
  <files>
    apps/web/src/components/forms/edit-person-button.tsx (modif lignes 60-62)
  </files>
  <action>
**Objectif** (per PB5-01, PB5-02, PB5-04) : remplacer les 2 text inputs `professionalExperience` (ligne 60) et `diplomas` (ligne 62) par des dropdowns alignés AGEFICE, **tout en préservant les valeurs legacy texte libre des 291 fiches existantes** via une option "Valeur actuelle".

**Étape 1 — Imports** : ajouter en tête de fichier :
```typescript
import { DIPLOME_OPTIONS, EXPERIENCE_OPTIONS } from '@/lib/agefice-options';
```

**Étape 2 — Construire les options dynamiquement** (juste avant le `return <EditModal ...>`) :

```typescript
// Construit les options diplôme : si la valeur courante est legacy texte libre
// (ne match aucune option canonique), on la garde en tête avec un label explicite.
const diplomaCurrentIsLegacy =
  !!current.diplomas && !DIPLOME_OPTIONS.includes(current.diplomas as (typeof DIPLOME_OPTIONS)[number]);
const DIPLOMA_OPTIONS_FOR_EDIT = [
  ...(diplomaCurrentIsLegacy
    ? [{ value: current.diplomas!, label: `Valeur actuelle : ${current.diplomas}` }]
    : []),
  ...DIPLOME_OPTIONS.map((o) => ({ value: o, label: o })),
];

// Idem pour expérience : on regarde si la valeur courante match une des 4 value canoniques
const experienceCurrentIsLegacy =
  !!current.professionalExperience &&
  !EXPERIENCE_OPTIONS.some((o) => o.value === current.professionalExperience);
const EXPERIENCE_OPTIONS_FOR_EDIT = [
  ...(experienceCurrentIsLegacy
    ? [{ value: current.professionalExperience!, label: `Valeur actuelle : ${current.professionalExperience}` }]
    : []),
  ...EXPERIENCE_OPTIONS.map((o) => ({ value: o.value, label: o.label })),
];
```

**Étape 3 — Remplacer les 2 champs dans le tableau `fields`** :

Avant (lignes 60-62) :
```typescript
{ name: 'professionalExperience', label: 'Expérience pro', defaultValue: current.professionalExperience, placeholder: 'Entre 4 et 10 ans' },
{ name: 'educationLevel', label: 'Niveau d\'étude', defaultValue: current.educationLevel },
{ name: 'diplomas', label: 'Diplômes (texte libre)', defaultValue: current.diplomas },
```

Après :
```typescript
{
  name: 'professionalExperience',
  label: 'Expérience pro (AGEFICE)',
  type: 'select',
  options: EXPERIENCE_OPTIONS_FOR_EDIT,
  defaultValue: current.professionalExperience,
},
{ name: 'educationLevel', label: 'Niveau d\'étude', defaultValue: current.educationLevel },
{
  name: 'diplomas',
  label: 'Dernier diplôme (AGEFICE)',
  type: 'select',
  options: DIPLOMA_OPTIONS_FOR_EDIT,
  defaultValue: current.diplomas,
},
```

**Notes importantes** :
- `educationLevel` reste en text libre (non critique AGEFICE — pas demandé dans l'audit)
- On garde le `name: 'diplomas'` et `name: 'professionalExperience'` pour ne pas casser le `onSubmit` qui mappe `values.diplomas`/`values.professionalExperience` → `updatePerson({...})`
- Si une fiche legacy a "Bac+2 plus DUT informatique" (texte libre), l'option "Valeur actuelle : Bac+2 plus DUT informatique" apparaîtra en tête de liste, suivie des 6 options canoniques. Laurent peut soit garder la valeur legacy soit migrer vers une option canonique.
- Pas de migration de données : on laisse les 291 fiches telles quelles, Laurent migre au fil de l'eau lors des éditions ponctuelles.
  </action>
  <verify>
<automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -20</automated>

Vérifications additionnelles :
- `grep -n "DIPLOME_OPTIONS\|EXPERIENCE_OPTIONS" apps/web/src/components/forms/edit-person-button.tsx` → doit retourner ≥ 4 lignes (import + 2x build options + 2x usage dans fields)
- `grep -n "from.*@/lib/agefice-options" apps/web/src/components/forms/` → doit retourner 2 fichiers (create + edit)
- `grep -rn "from.*['\"]\\./agefice-options['\"]\\|from.*['\"]@/lib/agefice-options['\"]" apps/web/src` → minimum 4 imports (form-fill + generator + create + edit)
- Test manuel :
  1. Ouvrir une fiche apprenant avec `diplomas = NULL` → 1ère option "— Choisir —" (ou équivalent), 6 options AGEFICE
  2. Ouvrir une fiche avec `diplomas = "Bac+2 plus DUT"` (legacy) → 1ère option "Valeur actuelle : Bac+2 plus DUT", puis 6 options AGEFICE
  3. Modifier une fiche pour passer en "Bac+5 : Supérieur à la maîtrise", sauvegarder, recharger → valeur conservée
  </verify>
  <done>
- Les 2 inputs text de `edit-person-button.tsx` sont devenus des `select` alignés AGEFICE
- Pour les fiches legacy texte libre, l'option "Valeur actuelle : X" apparaît en tête (rétro-compat OK)
- `educationLevel` reste en text libre (non critique AGEFICE)
- `tsc --noEmit` clean
- Aucune perte de donnée sur les 291 fiches legacy
  </done>
</task>

</tasks>

<verification>

**Vérifications globales du plan** :

1. **TypeScript clean** :
   ```bash
   cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit
   ```
   Doit sortir 0 erreur.

2. **Source unique respectée** :
   ```bash
   grep -rn "'Fin de scolarité obligatoire'" apps/web/src
   ```
   Doit retourner exactement **1 fichier** : `lib/agefice-options.ts`.

3. **Imports en place** (au moins 4 consommateurs) :
   ```bash
   grep -rn "from.*agefice-options" apps/web/src
   ```
   Doit lister au minimum :
   - `apps/web/src/lib/agefice-form-fill.ts`
   - `apps/web/src/server/actions/agefice-generator.ts`
   - `apps/web/src/components/forms/create-person-button.tsx`
   - `apps/web/src/components/forms/edit-person-button.tsx`

4. **Tests Vitest** (si tests existent sur create/edit Person) :
   ```bash
   cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test --run 2>&1 | tail -10
   ```
   Aucune régression. Si tests cassent à cause des nouveaux champs, mettre à jour les fixtures (ajouter `diplomas: null, professionalExperience: null` aux mocks `createPerson`).

5. **Validation manuelle E2E par Laurent** (pas de checkpoint formel, Laurent valide en environnement de dev) :
   - Créer un nouvel apprenant test (ex: "Test AGEFICE Dropdown") avec :
     - Diplôme = `Bac+2 : BTS-DUT-DEUG`
     - Expérience = `1 à 3 ans` (value `1_3_ANS`)
   - L'inscrire à une session AGEFICE existante
   - Générer le PDF AGEFICE depuis la fiche session
   - Ouvrir le PDF : vérifier que le dropdown "Sélectionner le dernier diplôme obtenu" est sur "Bac+2 : BTS-DUT-DEUG" et que la case `1 à 3 ans` est cochée
   - Éditer un apprenant legacy avec texte libre (ex: `professionalExperience = "Entre 4 et 10 ans"`), vérifier que l'option "Valeur actuelle : Entre 4 et 10 ans" apparaît en tête

</verification>

<success_criteria>

Plan considéré comme livré quand :

- [ ] `apps/web/src/lib/agefice-options.ts` existe avec `DIPLOME_OPTIONS` (6), `EXPERIENCE_OPTIONS` (4), types + `isCanonicalExperience`
- [ ] `agefice-form-fill.ts` : tableau local supprimé, import depuis `agefice-options.ts`, `resolveDiplome` conservé en fallback
- [ ] `agefice-generator.ts` : `inferExperience` fait un check exact via `isCanonicalExperience` avant l'heuristique
- [ ] `crud-edits.ts` : `createPerson` signature étendue (+2 champs) + persistence (+2 lignes dans `tx.person.create`)
- [ ] `create-person-button.tsx` : 2 nouveaux `useState` + 2 `<select>` côte à côte + reset + payload
- [ ] `edit-person-button.tsx` : 2 anciens text inputs → dropdowns avec fallback "Valeur actuelle" si legacy
- [ ] `tsc --noEmit` clean (zéro nouvelle erreur)
- [ ] `grep "'Fin de scolarité obligatoire'" apps/web/src` retourne 1 seul fichier (source unique)
- [ ] 4 imports `from .../agefice-options` détectés dans le codebase
- [ ] Aucune migration Prisma nécessaire (colonnes `Person.diplomas` et `Person.professionalExperience` déjà existantes)
- [ ] Test manuel Laurent : création apprenant + génération PDF AGEFICE → diplôme + expérience corrects dans le PDF

</success_criteria>

<output>
After completion, create `.planning/quick/260525-pb5-ajouter-dropdowns-dipl-me-exp-rience-pro/260525-pb5-SUMMARY.md` documenting :
- Fichier `agefice-options.ts` créé avec 6 + 4 valeurs canoniques
- 4 fichiers consommateurs câblés (refactor, generator, create form, edit form)
- Pattern "Valeur actuelle : X" pour rétro-compat 291 fiches legacy
- Pas de migration Prisma
- Validation E2E manuelle Laurent OK / KO + résultat PDF AGEFICE généré
</output>
