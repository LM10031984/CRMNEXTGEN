---
phase: quick-260820-j8w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/docs/convention-coverage.ts
  - apps/web/src/server/actions/convention-generator.ts
  - apps/web/src/lib/closure/convention-core.ts
  - apps/web/src/server/actions/opco-submission.ts
  - apps/web/src/server/actions/prepare-training.ts
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/lib/docs/__tests__/convention-coverage.test.ts
autonomous: true
requirements:
  - CODEX-REVIEW-PR13
must_haves:
  truths:
    - "generateConventionEntreprise exige un rôle en écriture (ADMIN/MANAGER/COMMERCIAL) côté serveur"
    - "La convention groupe et la facture groupée affichent TOUJOURS le même total, ou la génération est refusée"
    - "Régénérer une convention individuelle pour un participant couvert par une convention groupe ne crée PAS de doublon"
    - "Le dossier OPCO d'un salarié couvert par une convention groupe joint le PDF au lieu de le déclarer manquant"
    - "Le statut de préparation ne compte plus la convention comme manquante quand la convention groupe existe"
    - "La couverture convention (individuelle OU groupe) est résolue par UN seul helper partagé"
  artifacts:
    - path: "apps/web/src/lib/docs/convention-coverage.ts"
      provides: "Source unique de la couverture convention individuelle/groupe"
      contains: "expandGroupConventions"
  key_links:
    - from: "apps/web/src/server/actions/opco-submission.ts"
      to: "apps/web/src/lib/docs/convention-coverage.ts"
      via: "import de la clause de couverture"
      pattern: "convention-coverage"
---

<objective>
Corriger les 5 findings de la revue Codex sur la PR #13, **déjà en production**. Tous vérifiés dans le code.

Purpose: la quick 260817-mm0 a introduit un document de convention rattaché à l'organisation (`participantId=null`) mais n'a traité qu'un seul consommateur (la fiche session). Les autres résolvent la convention par `participantId` et ne la voient pas — dont le dossier OPCO, c'est-à-dire le cas OPTIMMO / OPCO EP qui a motivé le chantier.

Output: un helper de couverture partagé, utilisé par les quatre consommateurs, plus le RBAC et l'alignement de prix manquants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

## Task 1 — Helper partagé + RBAC + alignement du prix

files:
  - apps/web/src/lib/docs/convention-coverage.ts
  - apps/web/src/server/actions/convention-generator.ts
  - apps/web/src/lib/closure/convention-core.ts

action:
  - Créer `convention-coverage.ts` (module NEUTRE, aucun 'use server'/'use client') : constante `GROUP_CONVENTION_ENTITY_TYPE`, `groupConventionWhere(tenantId, sessionId, sponsorOrgId)`, et `expandGroupConventions(docs, participants)` → `Map<participantId, documentId>`.
  - **F1 RBAC** : `generateConventionEntreprise` passe de `validateRequest()` seul à `requireRole(['ADMIN','MANAGER','COMMERCIAL'])`, avec le traitement `UnauthorizedError`/`ForbiddenError` → `{ ok:false, error }` du repo. L'action supprime des documents : elle ne doit pas être appelable par un lecteur.
  - **F2 Prix** : supprimer le fallback `pp > 0 ? pp : productPrice` du cœur groupe. Refuser si un participant n'a pas de prix HT, en listant les personnes. Aligne sur `createInvoiceForSponsorGroup`, qui somme les prix bruts — sinon la convention engage un montant supérieur à la facture.

verify: `pnpm --filter @qualiof/web exec tsc --noEmit`
done: helper créé, RBAC posé, fallback supprimé.

## Task 2 — F3 : garde anti-doublon au bon endroit

files:
  - apps/web/src/lib/closure/convention-core.ts

action:
  - Dans `generateConventionCore`, AVANT le `deleteMany` et la génération : si une convention groupe couvre déjà ce participant (même session + son `sponsorOrgId`), ne rien générer et retourner `{ ok: true, documentId: <doc groupe>, skipped: true }`.
  - **Succès, pas erreur** : les 5 appelants incluent des flux batch (pack de clôture, préparation de masse) qui ne doivent pas casser — le participant EST couvert.
  - Garde posée dans le cœur, donc les 5 appelants (`prepare-training` ×2, `qualiopi-matrix`, `sessions`, `closure-pack`) sont protégés sans être modifiés.
  - ⚠ Ne pas changer le comportement pour un participant NON couvert : `generators-idempotent.test.ts` dépend du `deleteMany` inconditionnel.

verify: `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- vitest run src/server/actions/__tests__/generators-idempotent.test.ts`
done: garde en place, test d'idempotence toujours vert.

## Task 3 — F4 + F5 + bascule de la fiche session sur le helper

files:
  - apps/web/src/server/actions/opco-submission.ts
  - apps/web/src/server/actions/prepare-training.ts
  - apps/web/src/app/app/sessions/[id]/page.tsx

action:
  - **F4 (le plus grave)** : `composeOpcoSubmission` accepte aussi le document groupe (`entityType='organization'` + `entityId = participant.sponsorOrgId` + `sessionId`), en gardant la priorité à une convention individuelle si les deux existent.
  - **F5** : `getSessionPreparationStatus` étend sa requête et sa comptabilisation aux conventions groupe du sponsor de chaque participant.
  - Remplacer la passe ad hoc de `page.tsx` par le helper — une seule source de vérité.

verify: `pnpm --filter @qualiof/web exec tsc --noEmit && pnpm --filter @qualiof/web lint`
done: les trois consommateurs passent par le helper.

## Task 4 — Tests

files:
  - apps/web/src/lib/docs/__tests__/convention-coverage.test.ts

action:
  - `expandGroupConventions` : projette le doc groupe sur les seuls participants du sponsor, n'écrase jamais une convention individuelle, ignore les autres types.
  - Garde anti-doublon : participant couvert ⇒ pas de création, succès avec l'id du doc groupe ; participant non couvert ⇒ comportement inchangé.
  - Refus si prix manquant ; RBAC refusé pour un lecteur.
  - Test de puissance sur la garde anti-doublon.

verify: `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- vitest run`
done: suite complète verte.

</tasks>

<pitfalls>
- `convention-core.ts` ne doit jamais importer `@/lib/auth`, même transitivement → le helper reste un module neutre.
- Toute requête Prisma scopée `tenantId`.
- Le code est DÉJÀ en production : ces correctifs comblent des trous actifs, priorité au F4 (dossier OPCO).
- Suite de tests avec `dotenv -e ../../.env --`.
</pitfalls>
