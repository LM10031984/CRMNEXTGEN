---
phase: quick-260618-a24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/closure/worker.ts
  - apps/web/src/lib/closure/__tests__/worker-idempotent-document.test.ts
  - apps/web/src/server/actions/convention-generator.ts
  - apps/web/src/server/actions/convocation-generator.ts
  - apps/web/src/server/actions/agefice-generator.ts
  - apps/web/src/server/actions/agefice-attendance-generator.ts
  - apps/web/src/server/actions/__tests__/generators-idempotent.test.ts
autonomous: true
requirements: [DOC-IDEMPOTENT-01]
must_haves:
  truths:
    - "Régénérer un pack ne laisse qu'UN seul Document par (session, participant, type) pour ATTESTATION_FIN/CERTIFICAT_REALISATION"
    - "Régénérer une convention/convocation/AGEFICE/attestation assiduité (sans force) remplace l'existant au lieu d'empiler"
    - "Le paramètre force reste accepté dans la signature des 4 generators (compat appelants) mais ne conditionne plus la suppression"
    - "Les PedagogicalAsset (QCM/grille/analyse/positionnement/satisfaction/déroulé/émargement) restent en upsert et ne sont pas touchés"
  artifacts:
    - path: "apps/web/src/lib/closure/worker.ts"
      provides: "deleteMany + create ATTESTATION/CERTIFICAT dans un $transaction atomique"
      contains: "prisma.$transaction"
    - path: "apps/web/src/server/actions/convention-generator.ts"
      provides: "deleteMany CONVENTION inconditionnelle"
    - path: "apps/web/src/server/actions/convocation-generator.ts"
      provides: "deleteMany CONVOCATION inconditionnelle"
    - path: "apps/web/src/server/actions/agefice-generator.ts"
      provides: "deleteMany AGEFICE inconditionnelle"
    - path: "apps/web/src/server/actions/agefice-attendance-generator.ts"
      provides: "deleteMany ASSIDUITE inconditionnelle"
  key_links:
    - from: "apps/web/src/lib/closure/worker.ts"
      to: "prisma.document (deleteMany + create)"
      via: "$transaction atomique sur where { tenantId, sessionId, participantId, type: docType }"
      pattern: "\\$transaction"
    - from: "4 generators"
      to: "prisma.document.deleteMany"
      via: "appel inconditionnel (hors if force)"
      pattern: "document\\.deleteMany"
---

<objective>
Rendre la génération des documents Qualiopi stockés dans la table `Document` idempotente : remplacer au lieu d'empiler. Aujourd'hui régénérer un pack crée des doublons (mesuré : jusqu'à 23 CONVENTION et 5 ATTESTATION_FIN pour un même apprenant/session).

Deux causes corrigées :
1. `worker.ts` (~ligne 220) crée ATTESTATION_FIN / CERTIFICAT_REALISATION via `prisma.document.create()` SANS suppression préalable.
2. `convention/convocation/agefice/agefice-attendance` generators ont déjà un `deleteMany`, mais GARDÉ derrière `if (options?.force)` → empilent en mode par défaut.

Purpose: garantir 1 seul `Document` par (session, participant, type) à chaque régénération, sans toucher aux PedagogicalAsset (déjà en upsert, corrects).
Output: worker + 4 generators corrigés, 2 fichiers de tests comportementaux, tsc vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Worker — bloc à modifier (~ligne 214-233) -->
@apps/web/src/lib/closure/worker.ts

<!-- 4 generators — le guard `if (options?.force)` à retirer autour du deleteMany -->
@apps/web/src/server/actions/convention-generator.ts
@apps/web/src/server/actions/convocation-generator.ts
@apps/web/src/server/actions/agefice-generator.ts
@apps/web/src/server/actions/agefice-attendance-generator.ts

<interfaces>
<!-- Contrats extraits du code — l'exécuteur les utilise tels quels, pas d'exploration. -->

worker.ts — bloc actuel à transformer (~ligne 218) :
```typescript
const docType = DOC_TYPE_BY_KIND[payload.kind]; // ATTESTATION→ATTESTATION_FIN, CERTIFICAT→CERTIFICAT_REALISATION
if (docType) {
  const doc = await prisma.document.create({
    data: {
      tenantId: payload.tenantId,
      type: docType,
      entityType: 'participant',
      entityId: payload.participantId,
      pdfUrl: key,
      hashSha256: hash,
      sessionId: payload.sessionId,
      participantId: payload.participantId,
    },
  });
  documentId = doc.id;
}
```
payload expose : `payload.tenantId`, `payload.sessionId`, `payload.participantId`, `payload.kind`.

Forme du deleteMany déjà utilisée dans les generators (NE PAS modifier les `where`) :
```typescript
await prisma.document.deleteMany({
  where: { tenantId: user.tenantId, type: 'CONVENTION', participantId },
});
```

Invocation tsc (pas de script `typecheck` dédié dans apps/web) :
`pnpm --filter @qualiof/web exec tsc --noEmit`

Invocation test (script `test` = `dotenv -e ../../.env -- vitest run`, accepte un chemin) :
`pnpm --filter @qualiof/web test <chemin-fichier-test>`

Pattern de mock prisma pour les tests (cf. single-participant.test.ts) :
`vi.mock('@qualiof/db', () => ({ prisma: { document: { create: vi.fn(), deleteMany: vi.fn(), findFirst: vi.fn() }, $transaction: vi.fn() }, ... }))`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: worker.ts — deleteMany+create ATTESTATION/CERTIFICAT en $transaction atomique</name>
  <files>apps/web/src/lib/closure/worker.ts, apps/web/src/lib/closure/__tests__/worker-idempotent-document.test.ts</files>
  <behavior>
    - Test 1 (RED→GREEN) : pour payload.kind='ATTESTATION', le code appelle `prisma.$transaction([...])` avec un tableau de 2 ops dont un `prisma.document.deleteMany` (où where = { tenantId, sessionId, participantId, type: 'ATTESTATION_FIN' }) PUIS un `prisma.document.create`. Assert que deleteMany est appelé avec ce where exact ET que create est appelé avec le même type.
    - Test 2 : idem pour kind='CERTIFICAT' → type 'CERTIFICAT_REALISATION'.
    - Test 3 (non-régression) : pour un kind PedagogicalAsset (ex. 'QCM'), `prisma.document.create` et `prisma.document.deleteMany` ne sont PAS appelés (la branche docType est skippée) — preuve que les PedagogicalAsset upsert ne sont pas touchés.
    - Test de puissance (consigne Laurent) : casser la branche deleteMany (la commenter mentalement) doit faire virer Test 1 ROUGE.
  </behavior>
  <action>
    Dans `worker.ts`, remplacer le bloc `if (docType) { const doc = await prisma.document.create({...}); documentId = doc.id; }` (~ligne 218-233) par une version atomique :

    ```typescript
    if (docType) {
      // Idempotence : 1 seul Document par (session, participant, type).
      // deleteMany + create dans la MÊME transaction → pas de fenêtre où
      // 0 doc existe si le process casse entre les deux (atomicité).
      const [, doc] = await prisma.$transaction([
        prisma.document.deleteMany({
          where: {
            tenantId: payload.tenantId,
            sessionId: payload.sessionId,
            participantId: payload.participantId,
            type: docType,
          },
        }),
        prisma.document.create({
          data: {
            tenantId: payload.tenantId,
            type: docType,
            entityType: 'participant',
            entityId: payload.participantId,
            pdfUrl: key,
            hashSha256: hash,
            sessionId: payload.sessionId,
            participantId: payload.participantId,
          },
        }),
      ]);
      documentId = doc.id;
    }
    ```

    NE PAS toucher au bloc PedagogicalAsset (`prisma.pedagogicalAsset.upsert`) juste en dessous — il est correct.

    Créer le test `worker-idempotent-document.test.ts` selon le bloc <behavior>. Mocker `@qualiof/db` (prisma avec `document.deleteMany`, `document.create`, `$transaction` qui exécute le tableau et renvoie `[{ count: 0 }, { id: 'doc-1' }]`), `@/lib/storage` (uploadFile no-op), `@/lib/mailer`, `./renderer` (renderClosureDoc → { pdfBuffer: Buffer.from('x'), rawJson: {}, usedStub: false }), `@/lib/of-config` (loadOfConfig), `./redis`, `./queue`. Mocker `prisma.sessionParticipant.findFirst` pour renvoyer un participant minimal valide (person + session + product) afin que `processClosureJobPayload` atteigne le bloc Document. Importer et appeler `processClosureJobPayload(payload, { attemptsMade: 0, maxAttempts: 1, markProcessing: false })`.

    Si l'assemblage du mock de findFirst est trop lourd, alternative acceptable : extraire/asserter via un mock de `$transaction` qui capte les arguments — l'essentiel est d'asserter que deleteMany(where type=docType) ET create sont passés ensemble à $transaction.

    Commit atomique : `fix(web): worker idempotent ATTESTATION/CERTIFICAT via $transaction deleteMany+create`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test src/lib/closure/__tests__/worker-idempotent-document.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <done>Le test worker-idempotent-document.test.ts passe (deleteMany+create dans $transaction pour ATTESTATION et CERTIFICAT, branche QCM intacte). tsc vert. 1 commit atomique.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 generators — rendre la deleteMany inconditionnelle (retrait du guard if force)</name>
  <files>apps/web/src/server/actions/convention-generator.ts, apps/web/src/server/actions/convocation-generator.ts, apps/web/src/server/actions/agefice-generator.ts, apps/web/src/server/actions/agefice-attendance-generator.ts, apps/web/src/server/actions/__tests__/generators-idempotent.test.ts</files>
  <behavior>
    - Test 1 : `generateConventionForParticipant(participantId)` SANS options (force unset) appelle `prisma.document.deleteMany` avec where { tenantId, type: 'CONVENTION', participantId } avant le create.
    - Test 2 : `generateConvocationForParticipant(participantId)` sans options → deleteMany type 'CONVOCATION'.
    - Test 3 : `generateAgeficeForParticipant(participantId)` sans options → deleteMany type 'AGEFICE'.
    - Test 4 : `generateAgeficeAttendanceForParticipant(participantId)` sans options → deleteMany type 'ASSIDUITE'.
    - Chaque test asserte que deleteMany est appelé EXACTEMENT une fois, sans avoir passé force.
    - Test de puissance : remettre le `if (options?.force)` autour d'un deleteMany doit faire virer le test correspondant ROUGE (deleteMany non appelé en mode par défaut).
  </behavior>
  <action>
    Dans CHACUN des 4 fichiers, retirer UNIQUEMENT le wrapper `if (options?.force) { ... }` autour du `await prisma.document.deleteMany({...})`, en gardant l'appel `deleteMany` intact et inconditionnel.

    - convention-generator.ts (~ligne 37) : `if (options?.force) {` → supprimer la condition, garder le `deleteMany` (where { tenantId, type: 'CONVENTION', participantId }).
    - convocation-generator.ts (~ligne 41) : idem, type 'CONVOCATION'.
    - agefice-generator.ts (~ligne 81) : idem, type 'AGEFICE' (le commentaire « Force regen … » au-dessus reste pertinent ; ajuster le wording si trivial, mais non requis).
    - agefice-attendance-generator.ts (~ligne 58) : idem, type 'ASSIDUITE'.

    CONTRAINTES STRICTES :
    - NE PAS modifier les `where` des deleteMany (ils scellent déjà tenantId + type + participantId correctement).
    - CONSERVER le paramètre `options?: { force?: boolean }` dans la signature des 4 fonctions (compat appelants). Il ne conditionne simplement plus la suppression. Ne pas le retirer même s'il devient inutilisé — si tsc/lint râle sur un paramètre non utilisé, le préfixer en `_options` n'est PAS souhaitable car des appelants nommés peuvent exister ; le laisser tel quel (TS n'erre pas sur un param non lu).
    - NE PAS toucher aux blocs d'idempotence par hash sha256 (`findFirst { hashSha256 }` puis reuse) déjà présents dans convocation/agefice/agefice-attendance : ils restent (le deleteMany inconditionnel s'exécute AVANT, donc l'ancien doc est supprimé puis un nouveau créé — comportement correct).
    - NE PAS toucher : programme-generator, deroule-product-generator, generate-checklist-formation, generate-grille-obs-session, prepare-training, ni les PedagogicalAsset upsert.

    Créer `generators-idempotent.test.ts` selon <behavior>. Mock minimal : `@qualiof/db` (prisma avec sessionParticipant.findFirst renvoyant un participant valide pour chaque generator, document.deleteMany/create/findFirst, ageficePointAccueil.findFirst si besoin), `@/lib/auth` (validateRequest → { user: { tenantId: 'tnt-1' } }), `@/lib/storage` (uploadFile no-op), `@/lib/pdf-render` (renderHtmlToPdfWeasy/renderHtmlToPdf → Buffer), templates render* no-op, `@/lib/of-config` (loadOfConfig), `@/lib/agefice-form-fill` (fillAgeficePdf → Buffer), `@/lib/pedagogy-templates`, `next/cache` (revalidatePath no-op). Si le câblage complet d'un generator est trop coûteux à mocker, il est acceptable d'asserter par fichier que `document.deleteMany` est invoqué sans `force` ; viser les 4 types couverts.

    Commit atomique : `fix(web): deleteMany Document inconditionnelle dans les 4 generators (anti-doublons)`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test src/server/actions/__tests__/generators-idempotent.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <done>Les 4 generators appellent deleteMany sans condition de force ; generators-idempotent.test.ts vert (4 types CONVENTION/CONVOCATION/AGEFICE/ASSIDUITE) ; signature `options.force` conservée ; tsc vert. 1 commit atomique.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web exec tsc --noEmit` → 0 erreur nouvelle (ignorer les 6 préexistantes redirect-308.test.ts documentées dans STATE.md si elles apparaissent).
- `pnpm --filter @qualiof/web test src/lib/closure/__tests__/worker-idempotent-document.test.ts src/server/actions/__tests__/generators-idempotent.test.ts` → tous verts.
- Optionnel (non bloquant, sandbox) : suite complète `pnpm --filter @qualiof/web test` reste verte.
- Grep de contrôle : `grep -n "if (options?.force)" apps/web/src/server/actions/{convention,convocation,agefice,agefice-attendance}-generator.ts` → 0 occurrence.
- Grep de contrôle : `grep -n "\$transaction" apps/web/src/lib/closure/worker.ts` → présent dans le bloc Document.
</verification>

<success_criteria>
- worker.ts crée ATTESTATION_FIN/CERTIFICAT_REALISATION via `$transaction([deleteMany, create])` (atomique, where sur tenantId+sessionId+participantId+type).
- Les 4 generators suppriment toujours l'ancien Document du même type avant de créer (force ne conditionne plus rien).
- PedagogicalAsset upsert intacts.
- Signatures `options.force` préservées.
- 2 fichiers de tests comportementaux verts, tsc vert.
- 2 commits atomiques (un par tâche).
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260618-a24-g-n-ration-docs-qualiopi-idempotente-rem/260618-a24-SUMMARY.md`
</output>
