---
phase: quick-260619-jy5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/closure/satisfaction-session-template.ts
  - apps/web/src/lib/closure/__tests__/satisfaction-session-agg.test.ts
  - apps/web/src/components/produits/product-satisfaction-panel.tsx
autonomous: true
requirements:
  - QUICK-260619-JY5
must_haves:
  truths:
    - "La fiche produit affiche une carte KPI « Note moyenne /5 » à côté des cartes % existantes"
    - "noteSur5 vaut 5.0 quand tous les ratings sont 'Très bien'"
    - "noteSur5 vaut 4.5 quand moitié 'Très bien' moitié 'Bien'"
    - "noteSur5 vaut 0 quand contents est vide"
    - "globalScore / recommandationRate / satisfactionFavorableRate sont inchangés (non-régression)"
  artifacts:
    - path: "apps/web/src/lib/closure/satisfaction-session-template.ts"
      provides: "SatisfactionSessionAgg.noteSur5 + barème dédié /5 dans aggregateSatisfactions"
      contains: "noteSur5"
    - path: "apps/web/src/lib/closure/__tests__/satisfaction-session-agg.test.ts"
      provides: "Tests déterministes noteSur5 (sans LLM ni DB)"
      contains: "aggregateSatisfactions"
    - path: "apps/web/src/components/produits/product-satisfaction-panel.tsx"
      provides: "Carte KPI Note moyenne /5 (format FR virgule décimale)"
      contains: "/5"
  key_links:
    - from: "apps/web/src/components/produits/product-satisfaction-panel.tsx"
      to: "agg.noteSur5"
      via: "lecture du champ agrégé"
      pattern: "agg\\.noteSur5"
---

<objective>
Ajouter la note de satisfaction /5 sur la fiche produit QualiOF.

La fiche produit affiche déjà la satisfaction agrégée en % (globalScore, % favorable, % recommandent) mais PAS en /5. Laurent suit la satisfaction en /5 avec le MÊME barème que la colonne K de son tableau Qualiopi (Très bien=5, Bien=4, Moyen=3, Mauvais=2). On calcule `noteSur5` dans `aggregateSatisfactions` et on l'affiche dans une nouvelle carte KPI.

Purpose: aligner l'UI fiche produit sur le suivi Qualiopi de Laurent (note /5 colonne K).
Output: champ `noteSur5` dans `SatisfactionSessionAgg`, test déterministe, carte KPI « Note moyenne /5 ».
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Contrats existants. L'exécuteur les utilise directement, pas d'exploration. -->

apps/web/src/lib/closure/satisfaction-chaud-template.ts :
```typescript
export type RatingValue = 'Très bien' | 'Bien' | 'Moyen' | 'Mauvais';
// benefice.utilite est un type SÉPARÉ (pas RatingValue) :
//   utilite: 'Très utile' | 'Utile' | 'Peu utile' | 'Pas utile';
```

apps/web/src/lib/closure/satisfaction-session-template.ts :
```typescript
// Barème % existant — NE PAS réutiliser pour noteSur5 :
const RATING_SCORE: Record<RatingValue, number> = {
  'Très bien': 100, 'Bien': 85, 'Moyen': 50, 'Mauvais': 0,
};

export interface SatisfactionSessionAgg {
  totalStagiaires: number;
  globalScore: number;             // 0-100  — NE PAS toucher
  recommandationRate: number;      // 0-100  — NE PAS toucher
  satisfactionFavorableRate: number; // 0-100 — NE PAS toucher
  byCriterion: Array<{ section: string; criterionLabel: string; score: number; distribution: Record<RatingValue, number>; }>;
  pointsForts: string[];
  pointsAAmeliorer: string[];
}

export function aggregateSatisfactions(contents: SatisfactionChaudContent[]): SatisfactionSessionAgg;
// total===0 → early return avec tous les champs à 0/[]
// `allRatings: RatingValue[]` est construit dans la fonction à partir des 4 sections
// SECTION_CRITERIA (organisation, moyens, pedagogie, groupe). globalScore = moyenne RATING_SCORE
// sur allRatings. noteSur5 DOIT utiliser la MÊME base allRatings.
// IMPORTANT : SECTION_CRITERIA EXCLUT benefice — donc allRatings ne contient QUE des RatingValue
// ('Très bien'|'Bien'|'Moyen'|'Mauvais'). L'entrée 'Très utile'=5 du barème est défensive
// (au cas où benefice.utilite serait un jour ajouté à allRatings) mais n'est pas exercée aujourd'hui.
```

apps/web/src/components/produits/product-satisfaction-panel.tsx :
```typescript
// grille KPI existante : <div className="grid grid-cols-3 gap-3 mb-5"> avec 3 cartes
// (Score global / % favorable / Recommandent) lisant agg.globalScore, agg.satisfactionFavorableRate,
// agg.recommandationRate. helper scoreColor(score:number) attend une valeur 0-100.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Ajouter noteSur5 (barème dédié /5) à aggregateSatisfactions + test déterministe</name>
  <files>apps/web/src/lib/closure/satisfaction-session-template.ts, apps/web/src/lib/closure/__tests__/satisfaction-session-agg.test.ts</files>
  <behavior>
    - Tous 'Très bien' (1 content, toutes sections) → noteSur5 === 5.0
    - Moitié 'Très bien' moitié 'Bien' (2 contents, l'un tout TB l'autre tout Bien) → noteSur5 === 4.5
    - Tous 'Moyen' → noteSur5 === 3.0
    - Tous 'Mauvais' → noteSur5 === 2.0
    - contents vide ([]) → noteSur5 === 0
    - Non-régression : sur le même jeu de données, globalScore / recommandationRate / satisfactionFavorableRate gardent leurs valeurs actuelles (asserter au moins globalScore = 100 sur tout 'Très bien', et = 0 sur contents vide).
  </behavior>
  <action>
    Dans `satisfaction-session-template.ts` :
    1. Ajouter un barème DÉDIÉ /5 (NE PAS réutiliser RATING_SCORE 0-100), au-dessus de l'interface :
       ```typescript
       // Barème /5 = colonne K du tableau de suivi Qualiopi de Laurent
       const RATING_SCORE_5: Record<RatingValue, number> = {
         'Très bien': 5, 'Bien': 4, 'Moyen': 3, 'Mauvais': 2,
       };
       ```
       NOTE défensive : si un jour `benefice.utilite` ('Très utile') entre dans allRatings, 'Très utile' vaut 5. Aujourd'hui allRatings ne contient QUE des RatingValue (SECTION_CRITERIA exclut benefice), donc RATING_SCORE_5 suffit. Ne PAS ajouter benefice à allRatings dans ce quick.
    2. Ajouter `noteSur5: number;` à l'interface `SatisfactionSessionAgg` (avec commentaire `// 0-5, 1 décimale, barème colonne K`).
    3. Dans le early-return `total === 0`, ajouter `noteSur5: 0,`.
    4. Calculer `noteSur5` sur la MÊME base `allRatings` que globalScore, arrondie à 1 décimale :
       ```typescript
       const noteSur5 = allRatings.length === 0
         ? 0
         : Math.round((allRatings.reduce((s, r) => s + RATING_SCORE_5[r], 0) / allRatings.length) * 10) / 10;
       ```
       (allRatings est non-vide quand total>0 car SECTION_CRITERIA fournit toujours des critères ; le garde length===0 est défensif.)
    5. Ajouter `noteSur5,` dans l'objet retourné final.
    NE PAS modifier globalScore, recommandationRate, satisfactionFavorableRate, byCriterion, pointsForts, pointsAAmeliorer, ni les fonctions de rendu HTML (renderSatisfactionSessionHtml).

    Créer le test `apps/web/src/lib/closure/__tests__/satisfaction-session-agg.test.ts` (Vitest, sans DB ni LLM). Construire un helper local qui fabrique un `SatisfactionChaudContent` complet en remplissant TOUTES les clés des 4 sections (organisation 4, moyens 4, pedagogie 8, groupe 4) avec une RatingValue donnée, plus `benefice` (adequation: même rating, utilite: 'Utile'), `recommandation: 'Oui'`, `remarques` si requis par le type. Asserter les cas du <behavior>. Vérifier la structure exacte du type via l'import de SatisfactionChaudContent ; remplir tous les champs obligatoires pour que `tsc` passe.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/closure/__tests__/satisfaction-session-agg.test.ts</automated>
  </verify>
  <done>Test vert : noteSur5 = 5.0 / 4.5 / 3.0 / 2.0 / 0 selon les cas ; globalScore non régressé. `tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Carte KPI « Note moyenne /5 » sur la fiche produit (format FR)</name>
  <files>apps/web/src/components/produits/product-satisfaction-panel.tsx</files>
  <action>
    Ajouter UNE carte KPI « Note moyenne /5 » lisant `agg.noteSur5`, à côté des 3 cartes existantes. Conserver les cartes % (Score global / % favorable / Recommandent) intactes.
    1. Passer la grille KPI de `grid-cols-3` à `grid-cols-2 md:grid-cols-4` (4 cartes : reste lisible mobile en 2×2, 4 colonnes en md+). Adapter UNIQUEMENT la className du conteneur `<div className="grid grid-cols-3 gap-3 mb-5">`.
    2. Insérer la nouvelle carte (en première position, c'est la métrique de suivi prioritaire de Laurent) :
       - Libellé court uppercase : « Note /5 ».
       - Valeur formatée FR avec virgule décimale : `agg.noteSur5.toLocaleString('fr-FR', { minimumFractionDigits: 1, maximumFractionDigits: 1 })` suivi de `/5` → ex. « 4,9/5 ».
       - Couleur : réutiliser `scoreColor(...)` qui attend du 0-100 → convertir via `scoreColor(agg.noteSur5 * 20)` (5→100, 4→80, 3→60, 2→40). Garder le même style de carte que les autres (`rounded-xl border-2 p-3 text-center`, label `text-[10px] uppercase tracking-wide opacity-70`, valeur `text-2xl font-bold tabular-nums`).
    Ne PAS toucher au bloc « Détail par critère », points forts/axes, ni à la logique de fetch Prisma.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit</automated>
  </verify>
  <done>La carte « Note /5 » s'affiche (ex. « 4,9/5 », virgule FR) avant les 3 cartes %. `tsc` clean.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lib/closure/__tests__/satisfaction-session-agg.test.ts` vert (5 cas noteSur5 + non-régression globalScore).
- Suite existante non régressée : `cd apps/web && pnpm vitest run` (aucun test cassé par l'ajout du champ).
- `cd apps/web && pnpm tsc --noEmit` clean.
</verification>

<success_criteria>
- `SatisfactionSessionAgg.noteSur5` existe, calculé barème dédié (TB=5/Bien=4/Moyen=3/Mauvais=2), moyenne sur allRatings, 1 décimale, 0 si vide.
- globalScore / recommandationRate / satisfactionFavorableRate INCHANGÉS.
- Fiche produit affiche la carte « Note /5 » (virgule décimale FR) à côté des cartes % existantes.
- 0 migration, 0 appel LLM, 0 accès DB dans les tests. tsc vert.
</success_criteria>

<output>
After completion, create `.planning/quick/260619-jy5-note-satisfaction-sur-5-fiche-produit-ba/260619-jy5-SUMMARY.md`
</output>
