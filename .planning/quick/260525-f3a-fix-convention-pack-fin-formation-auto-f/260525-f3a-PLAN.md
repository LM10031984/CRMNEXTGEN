---
phase: quick-260525-f3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/actions/convention-generator.ts
  - apps/web/src/server/actions/agefice-generator.ts
autonomous: false
requirements:
  - FIX-CONV-01
  - FIX-AGEFICE-01

must_haves:
  truths:
    - "Lancer 'Pack fin de formation' sur SES-0043 (9 participants à 3024€, product.priceHT=0) ne rejette plus aucun participant pour cause de Prix HT manquant"
    - "Le PDF Convention de Catherine ALENDA affiche 3024€ HT (depuis participant.priceHT)"
    - "Le PDF AGEFICE généré pour un apprenant avec participant.priceHT=3024 et product.priceHT=0 contient 3024 en prixHT"
    - "Si participant.priceHT=0 ET product.priceHT=0, l'erreur retournée explique précisément les deux endroits où renseigner le prix"
  artifacts:
    - path: apps/web/src/server/actions/convention-generator.ts
      provides: "Guard unifié avec fallback bidirectionnel participant.priceHT ↔ product.priceHT + payload utilise effectivePrice"
      contains: "effectivePrice"
    - path: apps/web/src/server/actions/agefice-generator.ts
      provides: "Guard early-return cohérent + payload utilise effectivePrice"
      contains: "effectivePrice"
  key_links:
    - from: "convention-generator.ts:effectivePrice"
      to: "ConventionData.produitPriceHTPerStagiaire"
      via: "data.produitPriceHTPerStagiaire = effectivePrice"
      pattern: "produitPriceHTPerStagiaire:\\s*effectivePrice"
    - from: "agefice-generator.ts:effectivePrice"
      to: "AgeficeFormData.formation.prixHT"
      via: "data.formation.prixHT = effectivePrice"
      pattern: "prixHT:\\s*effectivePrice"
---

<objective>
Fixer le faux positif du guard `product.priceHT` dans les générateurs Convention et AGEFICE qui bloque actuellement le "Pack fin de formation" sur SES-0043 (9 participants à 3024€ HT, mais PROD-0042 importé SmartOF avec priceHT=0).

Purpose: Le PDF Convention utilise déjà `Number(participant.priceHT)` ligne 126 — il n'a aucun besoin de `product.priceHT`. Le guard ligne 57 du convention-generator est donc un faux positif qui rejette des inscriptions tarifées correctement. Même logique côté AGEFICE où le fallback existe déjà côté payload (`Number(participant.priceHT) || Number(product.priceHT)` ligne 258) mais sans guard cohérent pour les cas "les deux à 0".

Output: 2 fichiers modifiés, build OK, test E2E manuel SES-0043 9 participants tous générés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/server/actions/convention-generator.ts
@apps/web/src/server/actions/agefice-generator.ts

<interfaces>
<!-- Contrats existants — l'exécuteur ne doit RIEN changer aux signatures publiques. -->

Convention generator (apps/web/src/server/actions/convention-generator.ts) :
- Signature : `generateConventionForParticipant(participantId: string): Promise<{ ok: boolean; documentId?: string; error?: string }>`
- Payload final ligne 126 actuel : `produitPriceHTPerStagiaire: Number(participant.priceHT)`
- Guards à remplacer : lignes 57-70 (2 guards séparés sur product.priceHT puis participant.priceHT)

AGEFICE generator (apps/web/src/server/actions/agefice-generator.ts) :
- Signature : `generateAgeficeForParticipant(participantId: string): Promise<{ ok: boolean; documentId?: string; error?: string; warnings?: string[] }>`
- Payload actuel ligne 258 : `prixHT: Number(participant.priceHT) || Number(product.priceHT)` (fallback OR déjà présent, mais SANS guard)
- Pas de guard priceHT actuellement → ajouter un guard early-return cohérent avec convention-generator

Pattern d'erreur projet (CONVENTIONS.md) :
- Server actions retournent `{ ok: false, error: '...' }` — pas de throw
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix convention-generator — guard unifié + effectivePrice</name>
  <files>apps/web/src/server/actions/convention-generator.ts</files>
  <action>
Remplacer les lignes 57-70 (les 2 guards séparés sur `product.priceHT` puis `participant.priceHT`) par UN seul bloc avec fallback bidirectionnel :

```ts
const participantPrice = Number(participant.priceHT);
const productPrice = Number(participant.session.product.priceHT);
const effectivePrice = participantPrice > 0 ? participantPrice : productPrice;
if (effectivePrice <= 0) {
  return {
    ok: false,
    error:
      "Prix HT non défini : ni sur l'inscription (fiche session, bouton Éditer) ni sur le produit (/app/produits). Renseignez l'un des deux avant de générer la convention.",
  };
}
```

Puis ligne 126 dans le payload `ConventionData`, remplacer :
```ts
produitPriceHTPerStagiaire: Number(participant.priceHT),
```
par :
```ts
produitPriceHTPerStagiaire: effectivePrice,
```

Garde-fous :
- NE PAS toucher au reste du fichier (logique sponsorOrg, isSelfEmployed, lieu, etc.)
- NE PAS ajouter de commentaires inutiles (cf. CLAUDE.md `// Comments`)
- Le pattern de retour `{ ok: false, error }` est conservé
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "convention-generator|error TS" | head -20</automated>
  </verify>
  <done>
- Plus aucun guard `Number(participant.session.product.priceHT) <= 0` isolé dans le fichier
- Variable `effectivePrice` calculée avant le guard et utilisée dans le payload (ligne ~126)
- Le seul guard restant rejette uniquement si `effectivePrice <= 0` (les deux sources à 0)
- Message d'erreur unique mentionne les 2 endroits (inscription + fiche produit)
- `tsc --noEmit` passe sans erreur nouvelle sur convention-generator.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix agefice-generator — guard cohérent + effectivePrice</name>
  <files>apps/web/src/server/actions/agefice-generator.ts</files>
  <action>
Le fichier a déjà un fallback OR ligne 258 (`Number(participant.priceHT) || Number(product.priceHT)`) mais aucun guard explicite. Ajouter un guard cohérent avec convention-generator + remplacer le calcul inline par `effectivePrice`.

Étape 1 — Localiser la déclaration `const product = ...` (vers la ligne 200, juste avant `const data: AgeficeFormData = {`). Juste APRÈS cette déclaration `product` ET la résolution `session`, insérer le bloc :

```ts
const participantPrice = Number(participant.priceHT);
const productPrice = Number(product.priceHT);
const effectivePrice = participantPrice > 0 ? participantPrice : productPrice;
if (effectivePrice <= 0) {
  return {
    ok: false,
    error:
      "Prix HT non défini : ni sur l'inscription (fiche session, bouton Éditer) ni sur le produit (/app/produits). Renseignez l'un des deux avant de générer le formulaire AGEFICE.",
    warnings,
  };
}
```

Étape 2 — Ligne 258, remplacer :
```ts
prixHT: Number(participant.priceHT) || Number(product.priceHT),
```
par :
```ts
prixHT: effectivePrice,
```

Garde-fous :
- Le `return` doit inclure `warnings` (le reste du fichier retourne `warnings` partout — préserver le contrat)
- NE PAS toucher au reste (eiOrg resolution, pointAccueil, splitDureeByModality, etc.)
- Si la variable `product` n'est pas déclarée explicitement (utilisation directe `participant.session.product`), créer la const locale juste avant le guard : `const product = participant.session.product;`
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "agefice-generator|error TS" | head -20 &amp;&amp; grep -n "effectivePrice\|prixHT:" apps/web/src/server/actions/agefice-generator.ts</automated>
  </verify>
  <done>
- Variable `effectivePrice` calculée avant le payload `AgeficeFormData`
- Guard early-return si `effectivePrice <= 0` retourne `{ ok: false, error, warnings }`
- Ligne payload `prixHT:` utilise `effectivePrice` (plus de `||` fallback inline)
- Message d'erreur cohérent avec convention-generator (mentionne les 2 endroits)
- `tsc --noEmit` passe sans erreur nouvelle
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Test E2E SES-0043 — Catherine ALENDA + 9 participants</name>
  <what-built>
2 fichiers modifiés :
- `apps/web/src/server/actions/convention-generator.ts` : guard unifié + `effectivePrice` utilisé dans le payload (ligne ~126)
- `apps/web/src/server/actions/agefice-generator.ts` : guard cohérent ajouté + `effectivePrice` utilisé dans le payload (ligne ~258)

Le faux positif "Prix HT manquant sur le produit de formation" sur SES-0043 (PROD-0042 priceHT=0, participants à 3024€) doit avoir disparu.
  </what-built>
  <how-to-verify>
1. **Build & dev** :
   ```bash
   cd "/Users/laurentmarx/Documents/CRM Next gen/files"
   pnpm dev:full
   ```

2. **Test Convention isolée — Catherine ALENDA (SES-0043)** :
   - Naviguer vers la fiche session SES-0043
   - Sur la ligne de Catherine ALENDA, cliquer "Générer convention"
   - Attendu : PDF généré sans erreur, ouvrir le PDF et vérifier que le prix HT par stagiaire = **3024,00 €**

3. **Test Pack fin de formation — SES-0043 (9 participants)** :
   - Sur la fiche SES-0043, bouton "Pack fin de formation"
   - Attendu : **les 9 participants** lancent leur génération sans message "Prix HT manquant sur le produit de formation"
   - Attendre la fin du batch (~12 min) ou vérifier les jobs `ClosureJob` en BDD : aucun n'a `errorMessage` contenant "Prix HT"

4. **Test AGEFICE** :
   - Sur la fiche d'un apprenant AGEFICE (Catherine ALENDA si elle a un AgeficeProfile, sinon n'importe quel apprenant AGEFICE de SES-0043)
   - Cliquer "Générer formulaire AGEFICE"
   - Attendu : PDF AGEFICE généré, ouvrir et vérifier que le champ prix HT = **3024**

5. **Test de non-régression — cas "les deux à 0"** :
   - Trouver un participant avec `priceHT=0` ET produit avec `priceHT=0` (cf. diagnostic : 9 vraiment bloquants)
   - Lancer "Générer convention" → attendu : erreur explicite mentionnant **inscription** + **fiche produit**
  </how-to-verify>
  <resume-signal>Tape "approved" si les 5 checks passent, sinon décris le comportement observé.</resume-signal>
</task>

</tasks>

<verification>
- `tsc --noEmit` passe sur apps/web
- SES-0043 : aucun rejet "Prix HT manquant sur le produit" sur les 9 participants
- Convention Catherine ALENDA affiche 3024€ HT
- AGEFICE Catherine ALENDA affiche 3024 en prixHT
- Cas "deux à 0" : message d'erreur unifié mentionnant les 2 endroits
</verification>

<success_criteria>
Le scénario rapporté par Laurent (SES-0043, 9 participants à 3024€, PROD-0042 priceHT=0) génère 9 conventions sans erreur "Prix HT manquant sur le produit de formation". Le PDF affiche 3024€ HT.
</success_criteria>

<output>
After completion, create `.planning/quick/260525-f3a-fix-convention-pack-fin-formation-auto-f/260525-f3a-SUMMARY.md`
</output>
