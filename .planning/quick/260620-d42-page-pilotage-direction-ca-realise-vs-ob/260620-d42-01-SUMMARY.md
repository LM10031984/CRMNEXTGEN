---
phase: quick-260620-d42
plan: 01
subsystem: database
tags: [prisma, vitest, pilotage, ca, tenant-scope, tdd]

# Dependency graph
requires:
  - phase: 09.1-05 (product-stats)
    provides: "pattern lib stats PURE + fetch scopé tenantId via session.tenantId (cloné ici)"
provides:
  - "Model Prisma RevenueTarget (objectif CA annuel par tenant/année, unique tenantId+year)"
  - "Lib pilotage-stats.ts : helpers PURS (distributeAnnualObjective, computeSeasonalityWeights, pctAtteint, computeKpis, aggregateMonthly)"
  - "Fetch scopés tenantId (getRealisedMonthly/getForecastMonthly/getAnnualObjective/getTotalRealisedForYear/getSeasonalityWeights) + orchestrateur getPilotageData"
  - "Suite de tests déterministes 16/16 (sans DB/LLM) + test de puissance (mutation résidu)"
affects: [quick-260620-d42-02 (server action + page pilotage + nav)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lib pilotage = 2 couches (helpers PURS testables + fetch I/O scopés tenantId), frontière nette"
    - "Répartition objectif annuel : Math.round par mois + résidu d'arrondi au DERNIER mois (somme exacte)"
    - "Bornes d'année en UTC (Date.UTC) + bucketise par getUTCMonth() pour cohérence stockage DateTime UTC"

key-files:
  created:
    - apps/web/src/lib/pilotage-stats.ts
    - apps/web/src/lib/__tests__/pilotage-stats.test.ts
  modified:
    - packages/db/prisma/schema.prisma

key-decisions:
  - "RevenueTarget sans relation inverse sur Tenant (tenantId = FK logique non contrainte) pour ne pas toucher le model Tenant"
  - "amountHT en euros HT entiers (pas de centimes pour un objectif annuel)"
  - "Saisonnalité = 3 années glissantes [year-3, year-1] ; années sans données neutralisées par la normalisation"
  - "Définition métier : session terminée = endDate < now ; prévisionnel = endDate >= now"

patterns-established:
  - "Pattern 1 : helpers purs + fetch scopés dans une même lib, helpers 100% testables sans DB"
  - "Pattern 2 : test de puissance (mutation du résidu d'arrondi -> 2 tests rouges -> restauré) pour prouver que la suite garde l'invariant somme exacte"

requirements-completed: [PILOT-01, PILOT-02]

# Metrics
duration: ~12min
completed: 2026-06-20
---

# Phase quick-260620-d42 Plan 01 : Fondations Pilotage Direction (RevenueTarget + pilotage-stats) Summary

**Model Prisma RevenueTarget (objectif CA annuel/tenant/année) + lib pilotage-stats avec répartition saisonnière à somme exacte, KPIs purs testés (16/16) et fetch CA réalisé/prévisionnel mensuel scopés tenantId via session.tenantId.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-20T09:31Z (approx)
- **Completed:** 2026-06-20T09:43Z
- **Tasks:** 3
- **Files modified:** 3 (1 schema, 1 lib, 1 test)

## Accomplishments
- Model `RevenueTarget { tenantId, year, amountHT }` (`@@unique([tenantId, year])` + `@@index([tenantId])`) appliqué en base locale via `prisma db push` ; client Prisma régénéré expose `prisma.revenueTarget`.
- Helpers PURS : `distributeAnnualObjective` (saisonnalité + fallback /12 + résidu au dernier mois => somme EXACTE), `computeSeasonalityWeights` (part de CA/mois, div/0 safe), `pctAtteint`, `computeKpis` (YTD, atterrissage, écart), `aggregateMonthly` (getUTCMonth).
- Fetch scopés tenantId : `getRealisedMonthly` / `getForecastMonthly` (SUM priceHT via `session: { tenantId }`, bornes année UTC, endDate `<`/`>=` now), `getAnnualObjective` (lit `revenueTarget`), `getTotalRealisedForYear` (N-1), `getSeasonalityWeights` (3 ans glissants), orchestrateur `getPilotageData`.
- 16/16 tests Vitest déterministes verts + test de puissance validé (la mutation du résidu casse exactement les 2 invariants somme-exacte).

## Task Commits

1. **Task 1: Model Prisma RevenueTarget + db push** - `4dc95eb` (feat)
2. **Task 2 (TDD RED): tests helpers purs** - `6e67c80` (test)
3. **Task 2 (TDD GREEN): helpers purs pilotage-stats** - `77d628e` (feat)
4. **Task 3: fetch scopés tenantId** - `fe76de7` (feat)

_Task 2 suit le cycle TDD (RED `6e67c80` puis GREEN `77d628e`). Pas de refactor nécessaire._

## Files Created/Modified
- `packages/db/prisma/schema.prisma` - ajout model `RevenueTarget` (zone nouveaux models, après les enums RegulatoryWatch). Model `Tenant` NON touché.
- `apps/web/src/lib/pilotage-stats.ts` - lib production : 5 helpers purs + 5 fetch scopés + orchestrateur `getPilotageData`.
- `apps/web/src/lib/__tests__/pilotage-stats.test.ts` - 16 tests déterministes (saisonnalité, fallback, somme exacte, pctAtteint, computeKpis, aggregateMonthly, div/0).

## Decisions Made
- **Pas de relation inverse Prisma sur Tenant** : `tenantId` reste une colonne FK logique non contrainte sur RevenueTarget, conformément au plan (évite d'éditer le model Tenant et de risquer le WIP).
- **Objectif en euros HT entiers** (`amountHT Int`) : un objectif annuel n'a pas de centimes.
- **Résidu d'arrondi au dernier mois** : garantit `sum(12 mois) === annual` exactement malgré les `Math.round` par mois.
- **getUTCMonth + Date.UTC** (suggestion plan-checker appliquée) : cohérence entre le bucketing mensuel et les bornes d'année UTC (stockage DateTime UTC).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- **Worktree en retard / sans node_modules** : le worktree était à 43 commits d'avance / 214 de retard vs `cloud-migration`. Résolu conformément aux contraintes : `git reset --hard cloud-migration` + symlinks `node_modules` (racine + apps/web + packages/db + packages/shared) depuis le checkout partagé + symlink `.env` racine. Le WIP du checkout principal (page.tsx produits AGEFICE, session-location-picker, crud-edits) n'a PAS été touché (travail isolé dans le worktree).
- **prisma db push exigeait DATABASE_URL/DIRECT_URL** : chargés via `dotenv -e ../../.env` (les deux pointent localhost:5432 = Docker `qualiof_postgres`, jamais le cloud). Push appliqué en 113ms, base locale en sync.
- **1 test pré-existant rouge hors scope** : `closure/__tests__/shared-template.test.ts` Test 6 (MIME logo `image/jpg` vs `image/jpeg`), dernière modif commit `5f2f75f` (Phase 7-03), aucun fichier `closure/` touché par ce plan. Loggé dans `deferred-items.md`. Suite globale : 1017/1018 verts (le seul rouge est ce pré-existant).

## Dette technique notée
- **`prisma migrate deploy` sur Postgres prod** : `prisma db push --skip-generate` a synchronisé la base locale mais n'a PAS créé de fichier de migration versionné (cf. feedback_prisma_db_push_sandbox + feedback_prisma_migrate_deploy). Avant tout déploiement cloud, créer/appliquer une migration versionnée pour la table `RevenueTarget`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Contrat prêt pour le **Plan 02** (server action + page `/app/pilotage` + nav) : `getPilotageData(tenantId, year)` retourne `{ realiseMonthly, previsionnelMonthly, objectifAnnuel, objectifMensuel, totalRealiseN1, kpis }`.
- Le Plan 02 devra ajouter la server action d'écriture de l'objectif (`revenueTarget.upsert` scope tenantId+year, RBAC ADMIN/MANAGER) — hors scope Plan 01.

## Self-Check: PASSED

- Fichiers vérifiés présents : pilotage-stats.ts, pilotage-stats.test.ts, schema.prisma (model RevenueTarget), 260620-d42-01-SUMMARY.md
- Commits vérifiés présents : 4dc95eb, 6e67c80, 77d628e, fe76de7
- tsc --noEmit clean sur apps/web ; 16/16 tests pilotage-stats verts ; revenueTarget délégué runtime OK

---
*Phase: quick-260620-d42*
*Completed: 2026-06-20*
