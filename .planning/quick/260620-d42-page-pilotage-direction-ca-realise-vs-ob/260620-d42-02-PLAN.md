---
quick_id: 260620-d42
plan: 02
type: execute
wave: 2
depends_on: ["260620-d42-01"]
files_modified:
  - apps/web/src/server/actions/revenue-target.ts
  - apps/web/src/app/app/pilotage/page.tsx
  - apps/web/src/app/app/pilotage/revenue-target-form.tsx
  - apps/web/src/app/app/pilotage/pilotage-bar-chart.tsx
  - apps/web/src/components/layout/nav-config.ts
  - apps/web/next.config.mjs
autonomous: true
requirements: [PILOT-03, PILOT-04, PILOT-05]
must_haves:
  truths:
    - "Un ADMIN ou MANAGER peut ouvrir /app/pilotage et voir les cartes KPI CA"
    - "Le selecteur d'annee (?year=) change l'annee affichee, defaut = annee courante"
    - "Le graphe mensuel montre realise + previsionnel empiles + cible mensuelle + cumul vs objectif cumule"
    - "La comparaison N-1 (total realise annee precedente) est affichee"
    - "Un ADMIN/MANAGER peut saisir l'objectif annuel via le formulaire -> setRevenueTarget upsert"
    - "L'entree Pilotage apparait dans la sidebar pour ADMIN et MANAGER uniquement"
    - "Un LECTEUR/COMMERCIAL/etc qui tape /app/pilotage est redirige (garde page.tsx)"
  artifacts:
    - path: "apps/web/src/server/actions/revenue-target.ts"
      provides: "setRevenueTarget — requireRole ADMIN/MANAGER, upsert scope tenantId, AuditLog, revalidatePath"
      contains: "setRevenueTarget"
    - path: "apps/web/src/app/app/pilotage/page.tsx"
      provides: "Page RSC pilotage : selecteur annee + KPI + graphe + N-1 + form"
      contains: "pilotage"
    - path: "apps/web/src/app/app/pilotage/revenue-target-form.tsx"
      provides: "Client component formulaire objectif annuel"
      contains: "setRevenueTarget"
  key_links:
    - from: "apps/web/src/app/app/pilotage/page.tsx"
      to: "apps/web/src/lib/pilotage-stats.ts"
      via: "getPilotageData(tenantId, year)"
      pattern: "getPilotageData|pilotage-stats"
    - from: "apps/web/src/server/actions/revenue-target.ts"
      to: "prisma.revenueTarget"
      via: "upsert scope tenantId_year"
      pattern: "revenueTarget\\.upsert"
    - from: "apps/web/src/app/app/pilotage/revenue-target-form.tsx"
      to: "apps/web/src/server/actions/revenue-target.ts"
      via: "appel setRevenueTarget"
      pattern: "setRevenueTarget"
---

<objective>
Construire la surface utilisateur du Pilotage Direction : la server action `setRevenueTarget` (saisie objectif annuel), la page RSC `/app/pilotage` (RBAC ADMIN/MANAGER) avec selecteur d'annee + cartes KPI + graphe a barres CSS/Tailwind (realise + previsionnel empiles, cible mensuelle, cumul vs objectif cumule) + comparaison N-1 + formulaire, l'entree sidebar filtree ADMIN+MANAGER, et le redirect 308.

Purpose: Donner a la direction une vue unique et lisible du CA realise vs objectif vs previsionnel, par mois/annee. Consomme integralement le contrat pose par le Plan 01 (lib pilotage-stats).
Output: 1 server action, 1 page RSC + 2 client components, 1 entree nav, 1 redirect 308.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Contrat lib pose par le Plan 01 (a consommer)
@apps/web/src/lib/pilotage-stats.ts

# Pattern server action : requireRole + AuditLog + revalidatePath (a cloner)
@apps/web/src/server/actions/invoice-settings.ts

# Garde RBAC cote page + pattern selecteur annee + KPI + buildHref
@apps/web/src/app/app/budget-agefice/page.tsx

# Garde auth du layout protege
@apps/web/src/app/app/layout.tsx

# Nav-config : pattern allowedRoles + filterNavForRole (entree a ajouter)
@apps/web/src/components/layout/nav-config.ts

<interfaces>
RBAC : `requireRole(['ADMIN','MANAGER'])` (apps/web/src/lib/rbac.ts) -> throw UnauthorizedError/ForbiddenError, a catch dans la server action -> { ok:false, error }.
Page RSC : `validateRequest()` + `hasRole(user, ['ADMIN','MANAGER'])` sinon `redirect('/app')` (pattern distribution-leads/page.tsx + budget-agefice).
AuditLog : `logTenantSettingsChange({ tenantId, userId, action, diff })` accepte une action LIBRE (string) -> passer 'revenueTarget.update' (nouvelle convention, coherente avec 'parameters.update'). computeDiff(before, after) dispo dans @/lib/audit-log.
Money UI : new Intl.NumberFormat('fr-FR', { style:'currency', currency:'EUR', maximumFractionDigits:0 }).
Lib Plan 01 : getPilotageData(tenantId, year) -> { realiseMonthly[12], previsionnelMonthly[12], objectifAnnuel, objectifMensuel[12], totalRealiseN1, kpis }. distributeAnnualObjective deja appliquee dans objectifMensuel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server action setRevenueTarget (upsert scope tenantId + AuditLog)</name>
  <files>apps/web/src/server/actions/revenue-target.ts</files>
  <action>
Creer apps/web/src/server/actions/revenue-target.ts ('use server'), en clonant le pattern de invoice-settings.ts.

export async function setRevenueTarget(year: number, amountHT: number): Promise<{ ok: true } | { ok: false; error: string }>
  - requireRole(['ADMIN','MANAGER']) (try/catch UnauthorizedError/ForbiddenError -> { ok:false, error }).
  - Valider : year entier dans [2020, 2100], amountHT entier >= 0. Si invalide -> { ok:false, error:'Valeurs invalides' }. (Zod facultatif inline ; un schema simple suffit, pas besoin de fichier shared dedie.)
  - Lire before : prisma.revenueTarget.findUnique({ where: { tenantId_year: { tenantId: user.tenantId, year } } }).
  - upsert scope tenantId :
      prisma.revenueTarget.upsert({
        where: { tenantId_year: { tenantId: user.tenantId, year } },
        create: { tenantId: user.tenantId, year, amountHT },
        update: { amountHT },
      })
  - logTenantSettingsChange({ tenantId: user.tenantId, userId: user.id, action: 'revenueTarget.update', diff: computeDiff({ amountHT: before?.amountHT ?? null }, { amountHT }) }).
  - revalidatePath('/app/pilotage').
  - return { ok: true }.

TS strict, scope tenantId PARTOUT (where contient tenantId via la cle composite tenantId_year). NE PAS editer audit-log.ts.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc --noEmit 2>&1 | head -5 && grep -q "requireRole(\['ADMIN', *'MANAGER'\]" src/server/actions/revenue-target.ts && grep -q "revenueTarget.upsert" src/server/actions/revenue-target.ts && grep -q "revenueTarget.update" src/server/actions/revenue-target.ts && echo OK</automated>
  </verify>
  <done>setRevenueTarget : requireRole ADMIN/MANAGER, upsert scope tenantId_year, AuditLog 'revenueTarget.update', revalidatePath('/app/pilotage'), retour { ok }. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Page RSC /app/pilotage (RBAC + selecteur annee + KPI + graphe + N-1 + form)</name>
  <files>apps/web/src/app/app/pilotage/page.tsx, apps/web/src/app/app/pilotage/revenue-target-form.tsx, apps/web/src/app/app/pilotage/pilotage-bar-chart.tsx</files>
  <action>
1) page.tsx (Server Component, `export const dynamic = 'force-dynamic'`) :
   - validateRequest() ; if (!user) return null ; if (!hasRole(user, ['ADMIN','MANAGER'])) redirect('/app').
   - searchParams: Promise<{ year?: string }> ; year = sp.year ? parseInt : new Date().getFullYear().
   - const data = await getPilotageData(user.tenantId, year) (Plan 01).
   - Header (PageHeader) + selecteur d'annee (liens Link ?year=N, ex. annee courante et 4 precedentes — buildHref pattern budget-agefice).
   - Cartes KPI (clone-local d'un PrioCard, ex. inspiration learner-prio-cards / invoices-prio-cards — PAS d'import cross-feature, composant inline ou local) affichant : CA realise YTD, Objectif annuel, % atteint, Previsionnel restant, Atterrissage estime, Ecart vs objectif. Money via Intl fr-FR EUR.
   - <PilotageBarChart> (Task 2 sous-composant) recevant realiseMonthly, previsionnelMonthly, objectifMensuel.
   - Bloc comparaison N-1 : data.kpis.totalRealiseN1 vs caRealiseYTD (afficher delta + libelle).
   - <RevenueTargetForm year={year} current={data.objectifAnnuel} /> (Task 2 client component).
   - Copywriting FR ("Pilotage Direction", "CA realise", "Objectif annuel", "Previsionnel", "Atterrissage estime", "Ecart vs objectif", "vs N-1").

2) pilotage-bar-chart.tsx (Server Component pur, barres CSS/Tailwind — PAS de chart lib, contrainte) :
   - props { realiseMonthly: number[]; previsionnelMonthly: number[]; objectifMensuel: number[] }.
   - 12 colonnes (mois FR via Intl.DateTimeFormat month:'short' ou tableau statique). Pour chaque mois : barre empilee realise (couleur pleine) + previsionnel (couleur claire/hachuree) ; hauteur en % du max (realise+previsionnel et objectif confondus) via style inline height. Trait/marqueur cible mensuelle (objectifMensuel[i]) en overlay (ex. petite ligne horizontale border-t). 
   - Sous le graphe : une 2e mini-vue cumul (realise cumule vs objectif cumule) — calculee inline (reduce). Peut etre une barre de progression ou 12 points. Garder simple et lisible.
   - a11y : aria-label par barre ("Mars : realise X EUR, previsionnel Y EUR, cible Z EUR"), role/img sur le conteneur si SVG-like, sinon table-like accessible. Pas de hex random (palette Tailwind existante : emerald/sky/slate).

3) revenue-target-form.tsx ('use client') :
   - RHF + useTransition + sonner (pattern distribution-leads-form / invoice-settings-form). Champ amountHT (number, euros HT entiers) prerempli avec `current`. onSubmit -> setRevenueTarget(year, amountHT) ; toast.success/error selon { ok }. router.refresh() ou s'appuyer sur revalidatePath.
   - Label FR "Objectif de CA {year} (EUR HT)".

⚠ NE PAS ecraser le WIP du checkout principal (produits AGEFICE, session-location-picker, crud-edits). On ne touche que des fichiers NOUVEAUX sous app/pilotage/.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc --noEmit 2>&1 | head -8 && pnpm exec next build 2>&1 | grep -E "/app/pilotage|Compiled|error" | head -10</automated>
  </verify>
  <done>/app/pilotage compile et apparait dans le build. RBAC ADMIN/MANAGER via hasRole+redirect. Selecteur annee, KPI, graphe barres CSS (realise+previsionnel+cible+cumul), N-1, form objectif. Aucune chart lib ajoutee.</done>
</task>

<task type="auto">
  <name>Task 3: Entree sidebar Pilotage (ADMIN+MANAGER) + redirect 308</name>
  <files>apps/web/src/components/layout/nav-config.ts, apps/web/next.config.mjs</files>
  <action>
1) nav-config.ts : ajouter dans la section "Suivi" (ou "Essentiel" si plus pertinent pour la direction — choisir "Suivi") un item :
   { label: 'Pilotage', href: '/app/pilotage', icon: <icone lucide adaptee, ex. TrendingUp deja importe, ou Target/BarChart3 a importer>, allowedRoles: ['ADMIN','MANAGER'] }.
   Si nouvelle icone -> l'ajouter a l'import lucide en tete de fichier. Reutiliser TrendingUp (deja importe) est acceptable mais BarChart3 differencie mieux de "Vue de charge". Importer BarChart3.

2) next.config.mjs : ajouter dans redirects() un 308 pour la variante naturelle (convention PROJECT.md BUG-03) :
   { source: '/app/pilotage-direction', destination: '/app/pilotage', permanent: true },
   { source: '/app/pilotage-direction/:path*', destination: '/app/pilotage/:path*', permanent: true },
   (Variante la plus probable tapee a la main. Pas d'accent dans les URLs.)
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc --noEmit 2>&1 | head -5 && grep -q "/app/pilotage'" src/components/layout/nav-config.ts && grep -q "pilotage-direction" next.config.mjs && pnpm exec vitest run src/components/layout/__tests__/nav-config.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>Entree Pilotage visible pour ADMIN+MANAGER (filterNavForRole), masquee pour les autres. Redirect 308 /app/pilotage-direction -> /app/pilotage en place. Tests nav-config existants toujours verts.</done>
</task>

</tasks>

<verification>
- tsc --noEmit clean sur apps/web.
- next build compile /app/pilotage.
- grep : setRevenueTarget upsert + AuditLog 'revenueTarget.update' + scope tenantId_year ; page consomme getPilotageData ; nav-config a l'entree allowedRoles ADMIN/MANAGER ; redirect 308 present.
- Tests nav-config existants verts (non regression).
</verification>

<success_criteria>
- /app/pilotage accessible ADMIN/MANAGER, redirige les autres roles.
- KPI (realise YTD, objectif, % atteint, previsionnel restant, atterrissage, ecart) + graphe barres CSS (realise+previsionnel empiles, cible mensuelle, cumul vs objectif cumule) + comparaison N-1 affiches.
- Saisie objectif annuel persiste via setRevenueTarget (upsert tenant-scope + AuditLog).
- Entree sidebar filtree + redirect 308. Aucune dependance npm ajoutee. WIP checkout principal intact.
</success_criteria>

<output>
After completion, create `.planning/quick/260620-d42-page-pilotage-direction-ca-realise-vs-ob/260620-d42-02-SUMMARY.md`
</output>
