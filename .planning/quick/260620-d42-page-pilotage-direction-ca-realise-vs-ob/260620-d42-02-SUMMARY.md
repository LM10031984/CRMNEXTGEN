---
quick_id: 260620-d42
plan: 02
subsystem: ui
tags: [pilotage, revenue-target, rsc, rbac, tailwind-chart, audit-log, nav-config]

# Dependency graph
requires:
  - phase: 260620-d42-01
    provides: "lib pilotage-stats.ts (getPilotageData) + model RevenueTarget"
provides:
  - "Server action setRevenueTarget (upsert tenant-scope + AuditLog revenueTarget.update)"
  - "Page RSC /app/pilotage (RBAC ADMIN/MANAGER) : sélecteur année + 6 KPI + graphe barres CSS + N-1 + form"
  - "Graphe à barres 100% CSS/Tailwind (réalisé+prévisionnel empilés + cible mensuelle + cumul) sans chart lib"
  - "Entrée sidebar Pilotage filtrée ADMIN+MANAGER + redirect 308 /app/pilotage-direction"
affects: [pilotage, dashboard-direction, futures vues CA prévisionnel]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Convention AuditLog 'revenueTarget.update' (entity=Tenant) — cohérente avec 'parameters.update'"
    - "Graphe à barres CSS pur (height en %, overlay cible en absolute bottom:%, role=img + aria-label par colonne)"

key-files:
  created:
    - apps/web/src/server/actions/revenue-target.ts
    - apps/web/src/app/app/pilotage/page.tsx
    - apps/web/src/app/app/pilotage/pilotage-bar-chart.tsx
    - apps/web/src/app/app/pilotage/revenue-target-form.tsx
  modified:
    - apps/web/src/components/layout/nav-config.ts
    - apps/web/next.config.mjs

key-decisions:
  - "setRevenueTarget : requireRole(['ADMIN','MANAGER']), validation inline (2 entiers bornés, pas de schéma shared dédié), upsert scope tenantId_year"
  - "Entrée nav placée en TÊTE de la section Suivi (vue direction d'ensemble), icône BarChart3 (distingue de Vue de charge / TrendingUp)"
  - "Comparaison N-1 : delta réalisé YTD vs total réalisé N-1, % seulement si N-1 > 0 (pas de div/0)"

patterns-established:
  - "Pattern graphe barres CSS empilées + marqueur cible overlay : échelle commune max(stack, cible), aucune dépendance"
  - "Pattern KPICard inline local (pas d'import cross-feature), accent emerald/primary/amber"

requirements-completed: [PILOT-03, PILOT-04, PILOT-05]

# Metrics
duration: ~20min
completed: 2026-06-20
---

# quick 260620-d42 Plan 02 : Page Pilotage Direction (surface utilisateur) Summary

**Surface utilisateur du Pilotage Direction livrée : page RSC `/app/pilotage` (RBAC ADMIN/MANAGER) avec sélecteur d'année, 6 cartes KPI (réalisé YTD, objectif, % atteint, prévisionnel restant, atterrissage, écart), graphe à barres 100% CSS (réalisé+prévisionnel empilés + cible mensuelle + cumul), comparaison N-1, et formulaire de saisie d'objectif persisté via `setRevenueTarget` (upsert tenant-scope + AuditLog). Aucune dépendance npm ajoutée.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3 / 3
- **Files created:** 4
- **Files modified:** 2

## Accomplishments

### Task 1 — Server action `setRevenueTarget` (commit `9640288`)
- `apps/web/src/server/actions/revenue-target.ts` (`'use server'`), cloné sur `invoice-settings.ts`.
- `requireRole(['ADMIN','MANAGER'])` + try/catch `UnauthorizedError`/`ForbiddenError` → `{ ok:false, error }`.
- Validation inline : `year` entier [2020,2100], `amountHT` entier ≥ 0 sinon `{ ok:false, error:'Valeurs invalides' }`.
- `findUnique` before → `upsert` scope `tenantId_year` (clé composite porte `user.tenantId` → mutation strictement tenant-scopée).
- `logTenantSettingsChange({ action:'revenueTarget.update', diff: computeDiff(...) })` (no-op si diff vide) ; `revalidatePath('/app/pilotage')` ; `return { ok:true }`.
- `audit-log.ts` NON modifié (action libre acceptée par la signature existante).

### Task 2 — Page RSC + graphe + form (commit `d070253`)
- `page.tsx` (`force-dynamic`) : `validateRequest()` → `hasRole(user, ['ADMIN','MANAGER'])` sinon `redirect('/app')`. `searchParams ?year=` (défaut = année courante). Consomme `getPilotageData(user.tenantId, year)` (Plan 01).
- Sélecteur d'année : année courante + 4 précédentes via `FilterChips`. 6 cartes KPI `Intl fr-FR EUR` (maximumFractionDigits:0). Bloc comparaison N-1 (delta + %).
- `pilotage-bar-chart.tsx` (Server Component pur) : 12 colonnes, barre empilée `emerald` (réalisé) + `sky-300` (prévisionnel), marqueur cible en overlay (`absolute bottom:%` + `border-dashed slate-500`), mini-vue cumul (progressbar réalisé/objectif). a11y : `role=img` conteneur, `aria-label` par colonne (mois : réalisé/prévisionnel/cible), `role=progressbar` sur le cumul. Aucune chart lib.
- `revenue-target-form.tsx` (`'use client'`) : RHF + `useTransition` + `sonner`, champ `amountHT` prérempli `current`, `onSubmit → setRevenueTarget(year, amount)`, toast success/error + `router.refresh()`.
- `next build` : `/app/pilotage` compile (ƒ dynamic, 1.25 kB).

### Task 3 — Sidebar + redirect 308 (commit `94f90ce`)
- `nav-config.ts` : import `BarChart3`, item `{ label:'Pilotage', href:'/app/pilotage', icon: BarChart3, allowedRoles:['ADMIN','MANAGER'] }` en tête de la section « Suivi ». Filtrage via `filterNavForRole` (existant).
- `next.config.mjs` : redirect 308 `/app/pilotage-direction(/:path*)` → `/app/pilotage(/:path*)` (convention BUG-03).
- 17 tests `nav-config.test.ts` verts (non-régression).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Régénération du client Prisma (RevenueTarget absent)**
- **Found during:** baseline avant Task 1.
- **Issue:** Le model `RevenueTarget` (ajouté en wave 1) n'était pas présent dans le client Prisma généré (`prisma.revenueTarget` introuvable) → `tsc` aurait échoué sur la lib `pilotage-stats.ts` et la nouvelle server action.
- **Fix:** `pnpm --filter @qualiof/db exec prisma generate` (node_modules partagé avec le checkout principal, même commit `ab750bc` → sûr). `revenueTarget` désormais présent (24 réfs dans le client `.d.ts`).
- **Files modified:** aucun fichier source (régénération client).
- **Commit:** n/a (artefact node_modules, hors index).

**2. [Rule 3 - Blocking] Worktree divergent / sans node_modules**
- **Found during:** load_project_state.
- **Issue:** HEAD du worktree à `065f184` (divergent, sans le contrat wave 1 `pilotage-stats.ts`) et sans `node_modules`.
- **Fix:** `git reset --hard cloud-migration` (→ `ab750bc`, wave 1 présente) + symlinks `node_modules` (root + apps/web) vers le checkout principal. WIP du checkout principal NON touché (lecture seule).
- **Commit:** n/a (mise en état du worktree).

## Deferred Issues (hors scope, pré-existants)

Documentés dans `deferred-items.md` :
- `closure/__tests__/shared-template.test.ts` Test 6 (MIME `image/jpg` vs `image/jpeg`) — pré-existant, aucun fichier `closure/` touché.
- `scripts/__tests__/dedupe.merge.test.ts` — 0 test collecté (dépendance BDD réelle absente du sandbox), pré-existant, aucun fichier `scripts/` touché.

Suite complète `apps/web` : **1014 passed / 1 failed** (le failed = `shared-template` Test 6 pré-existant). Aucune régression introduite par ce plan.

## Known Stubs

Aucun. Les KPI, le graphe et le N-1 sont câblés sur `getPilotageData` (données réelles SessionParticipant.priceHT scopées tenant). Le formulaire persiste réellement via `setRevenueTarget`.

## Verification

- `tsc --noEmit` sur `apps/web` : **clean** (exit 0) après chaque tâche.
- `next build` : `/app/pilotage` compile (route dynamique).
- greps : `setRevenueTarget` + `revenueTarget.upsert` + `revenueTarget.update` + scope `tenantId_year` ; page consomme `getPilotageData` ; nav-config `allowedRoles:['ADMIN','MANAGER']` ; redirect `pilotage-direction` présent.
- 17 tests `nav-config.test.ts` verts (non-régression du filtre RBAC sidebar).

## Self-Check: PASSED

- Fichiers créés vérifiés présents : `revenue-target.ts`, `page.tsx`, `pilotage-bar-chart.tsx`, `revenue-target-form.tsx`.
- Commits vérifiés présents : `9640288` (Task 1), `d070253` (Task 2), `94f90ce` (Task 3).
