---
phase: quick-260910-c1r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/<horodatage>_signature_regime_financement/migration.sql
  - packages/db/prisma/seed.ts
  - apps/web/src/lib/signature/regime.ts
  - apps/web/src/lib/signature/__tests__/regime.test.ts
  - apps/web/src/lib/signature/__tests__/seed-signataires.test.ts
  - apps/web/src/lib/derive-cell-state.ts
  - apps/web/src/lib/__tests__/derive-cell-state.test.ts
autonomous: true
requirements:
  - SIG-C1-01  # enum SignerRole + 3 colonnes nullables sur OpcoCatalog (D-10, forme « trois colonnes typées »)
  - SIG-C1-02  # seed des 6 financeurs + 1 test nommé par ligne du seed
  - SIG-C1-03  # module pur de résolution du régime (sans base, sans `if` sur un code financeur)
  - SIG-C1-04  # deriveCellState : NA hors régime, jamais MISSING

must_haves:
  truths:
    - "Le schéma Prisma porte `enum SignerRole { DIRIGEANT STAGIAIRE }` et trois colonnes NULLABLES `conventionSigner` / `ageficeSigner` / `assiduiteSigner` sur `OpcoCatalog` ; `requiredDocs` est inchangé."
    - "La migration est additive (CREATE TYPE + 3 ADD COLUMN nullables), créée par `prisma migrate dev`, sans aucun backfill ni DROP."
    - "Le seed pose les 6 lignes exactes du tableau D-10 (AGEFICE / OPCO_EP / ATLAS / OPCOMMERCE / CPF / FI-FPL) et reste idempotent (`upsert` create+update, comme aujourd'hui)."
    - "Six tests NOMMÉS — un par financeur — lisent le seed réel et échouent en désignant le financeur fautif si une seule ligne dévie."
    - "Une fonction PURE répond « quelles pièces sont en régime pour ce participant, et quel rôle les signe » à partir des 3 colonnes du financeur, sans accès base et sans aucun `if` sur un code financeur."
    - "Un financeur inconnu (règle absente) met les 3 pièces HORS régime — il n'ouvre pas la porte à trois signatures par défaut."
    - "Un rôle DIRIGEANT sans organisation payeuse produit un BLOCAGE nommé, jamais une pièce muette."
    - "`deriveCellState` rend `NA` (et non `MISSING`) pour un docType hors régime dont aucune trace n'existe, sans changer la priorité `MANUAL_OK` ni le cas dérogatoire D-01 `no_proof`."
    - "Une pièce hors régime qui EXISTE quand même (Document généré ou scan déposé) reste affichée telle quelle — `NA` ne masque jamais une preuve."
    - "Un appelant qui ne passe pas le nouveau paramètre retrouve exactement le comportement d'avant (les 8 call sites actuels restent verts)."
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "enum SignerRole + 3 colonnes signataires sur OpcoCatalog"
      contains: "enum SignerRole"
    - path: "packages/db/prisma/seed.ts"
      provides: "Les 6 lignes signataires du catalogue financeur"
      contains: "conventionSigner"
    - path: "apps/web/src/lib/signature/regime.ts"
      provides: "Résolution pure du régime de signature (pièces en régime + rôle + cible)"
      exports: ["resolveRegimeSignature", "docTypesHorsRegime", "DOC_TYPES_SIGNABLES"]
      min_lines: 60
    - path: "apps/web/src/lib/signature/__tests__/seed-signataires.test.ts"
      provides: "Recette 0-drift : 1 test nommé par ligne du seed (6) + garde anti-`if` sur code financeur"
    - path: "apps/web/src/lib/signature/__tests__/regime.test.ts"
      provides: "Tests du module pur + test de puissance (règle mutée → sortie qui change)"
    - path: "apps/web/src/lib/derive-cell-state.ts"
      provides: "Paramètre optionnel `docTypesHorsRegime` → NA au lieu de MISSING"
      contains: "docTypesHorsRegime"
  key_links:
    - from: "packages/db/prisma/seed.ts"
      to: "OpcoCatalog.conventionSigner / ageficeSigner / assiduiteSigner"
      via: "upsert idempotent des 6 financeurs"
      pattern: "assiduiteSigner"
    - from: "apps/web/src/lib/signature/regime.ts"
      to: "les 3 colonnes du financeur"
      via: "table COLONNE_PAR_DOCTYPE (donnée), jamais un test d'égalité sur un code"
      pattern: "COLONNE_PAR_DOCTYPE"
    - from: "apps/web/src/lib/derive-cell-state.ts"
      to: "docTypesHorsRegime"
      via: "dernier recours avant MISSING"
      pattern: "docTypesHorsRegime"
---

<objective>
Lot C.1 de la spec signature : **rendre « qui signe quoi » lisible dans la donnée**, pour que le lot C.2 (envoi) n'ait plus qu'à l'appliquer.

Aujourd'hui la règle n'existe nulle part. `OpcoCatalog.requiredDocs` est de la prose d'affichage (« Convention de formation signée », « Facture acquittée »), vide chez ATLAS et OPCOMMERCE, et ne dit jamais QUI signe. Le code, lui, tranche par `opcoCode === 'AGEFICE'` disséminé dans la page session, le pack de clôture et l'archive apprenant. Un septième financeur ferait un septième `if`.

Quatre livrables, dans cet ordre :

1. **Le schéma porte la règle** — `enum SignerRole { DIRIGEANT STAGIAIRE }` et trois colonnes nullables sur `OpcoCatalog` (**D-10, forme tranchée : trois colonnes typées, pas un Json**). `requiredDocs` n'est pas touché.
2. **Le seed pose les 6 lignes** du catalogue, et **six tests nommés** — un par financeur — les gardent.
3. **Un module pur** répond « quelles pièces, quel rôle, quelle cible » à partir de ces trois colonnes, sans base et sans un seul `if` sur un code financeur.
4. **`deriveCellState` sait dire NA** pour une pièce hors régime : `MISSING` appelle une action, `NA` dit qu'il n'y a rien à faire. Confondre les deux fait courir l'admin après des pièces qui n'existent pas.

Purpose : c'est cette donnée qui décidera, en C.2, si le bouton « Envoyer pour signature » **existe** — une session 100 % salariés OPCO n'a rien à envoyer côté APRÈS.
Output : une migration additive, 6 lignes de seed gardées par 6 tests, une fonction pure testée sans base, et un état `NA` disponible dans la matrice.

## Hors périmètre — explicitement

- `sendForSignature`, l'UI d'envoi, le webhook, les emails, le cron `signature-sync` : **lots C.2 et C.3**.
- **Le branchement de la page session** (`app/app/sessions/[id]/page.tsx`) sur ces nouvelles colonnes n'est PAS dans ce lot, et ce n'est pas un oubli. La page dérive aujourd'hui `isAgefice` par une règle ÉLARGIE (BUG-11, L. 425-436) : sponsor AGEFICE **ou** `LegalLink` `EI_SELF` **ou** une autre organisation rattachée en AGEFICE — le cas Florent HAUSSWIRTH, sponsor OPCO_EP mais auto-entrepreneur AGEFICE en parallèle. Piloter la matrice par le seul `sponsorOrg.opcoCode` ferait passer sa cellule AGEFICE en `NA` et supprimerait son dossier de l'écran. Réconcilier « régime du financeur » et « double casquette » est une décision à part entière : elle se prend en C.2, avec le bouton d'envoi sous les yeux. C.1 livre la **capacité** (`docTypesHorsRegime`), pas le câblage.
- `AGEFICE_ONLY` dans `lib/sessions/participant-phase-items.ts` : même raison, on n'y touche pas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/specs/2026-09-04-signature-electronique-docs-signes.md
@CLAUDE.md
@packages/db/prisma/schema.prisma
@packages/db/prisma/seed.ts
@apps/web/src/lib/derive-cell-state.ts
@apps/web/src/lib/docs/__tests__/seed-catalog.test.ts
@apps/web/src/lib/signature/__tests__/no-hardcoded-host.test.ts

<decisions_verrouillees>
<!-- Tranchées par Laurent le 10/09/2026. NE PAS les rouvrir, ne pas proposer d'alternative. -->

**D-10 — forme retenue : trois colonnes typées.** Pas un `Json`, pas une liste de `DocType`.

```prisma
enum SignerRole { DIRIGEANT STAGIAIRE }

model OpcoCatalog {
  // … existant, requiredDocs INTACT …
  conventionSigner SignerRole?
  ageficeSigner    SignerRole?
  assiduiteSigner  SignerRole?
}
```

**Les 6 lignes du seed — valeurs exactes, confirmées deux fois :**

| code | conventionSigner | ageficeSigner | assiduiteSigner |
|---|---|---|---|
| AGEFICE | DIRIGEANT | STAGIAIRE | STAGIAIRE |
| OPCO_EP | DIRIGEANT | null | null |
| ATLAS | DIRIGEANT | null | null |
| OPCOMMERCE | DIRIGEANT | null | null |
| CPF | STAGIAIRE | null | null |
| FI-FPL | STAGIAIRE | null | null |

**L'enum EST la règle :**
- `DIRIGEANT` → le **représentant légal de l'organisation payeuse** (`SessionParticipant.sponsorOrgId`). Un envoi par organisation (D-4).
- `STAGIAIRE` → **le participant** lui-même.
- `null` → **NA** dans la matrice. **Jamais `MISSING`.**

**Points de compréhension — à NE PAS « corriger » :**
- AGEFICE a bien `conventionSigner = DIRIGEANT` (et non `null`) : le TNS signe sa convention. Son EI étant l'organisation payeuse et lui-même son représentant légal, `DIRIGEANT` retombe sur lui. **Aucun cas particulier à coder.**
- CPF et FI-FPL sont à `STAGIAIRE` sur la convention : l'apprenant paie et signe lui-même, souvent sans organisation payeuse. `DIRIGEANT` y ferait échouer la résolution.
- FI-FPL est à `null` sur `agefice` et `assiduite` : `ageficeSigner` déclenche le **formulaire officiel** `src/assets/agefice-template.pdf`, propre à l'AGEFICE, et l'attestation d'assiduité est une pièce AGEFICE (spec §3). Un adhérent FI-FPL n'a rien à voir avec ces deux pièces.
- `requiredDocs` n'est pas touché : prose d'affichage, lue par la seule fiche financeur (`app/app/financeurs/[code]/page.tsx`), vide chez ATLAS et OPCOMMERCE. Elle ne dit jamais QUI signe — c'est précisément pourquoi ces colonnes existent.
</decisions_verrouillees>

<interfaces>
<!-- Contrats déjà en place. L'exécutant les utilise tels quels, sans exploration. -->

`packages/db/prisma/schema.prisma` L. 1256-1276 — état actuel :
```prisma
model OpcoCatalog {
  id                   String         @id @default(uuid())
  code                 String         @unique // "AGEFICE", "OPCO_EP", "ATLAS", "CPF", etc.
  name                 String
  type                 String // "FAF", "OPCO", "Autre"
  // … website, contactEmail, averageDelayDays, maxAmountPerTraining, yearlyCapPerPerson …
  conditions           String?
  requiredDocs         Json? // string[]  ← PROSE D'AFFICHAGE, on n'y touche pas
  adminContact         String?
  status               OpcoStatus     @default(ACTIVE)
  internalNotes        String?
  organizations        Organization[]
  createdAt            DateTime       @default(now())
  updatedAt            DateTime       @updatedAt
}
```

`packages/db/prisma/seed.ts` L. 62-164 — `seedOpcoCatalog()` : un tableau `const opcos = [ { code, name, type, … }, … ]` de 6 entrées, puis
```ts
for (const opco of opcos) {
  await prisma.opcoCatalog.upsert({ where: { code: opco.code }, create: opco, update: opco });
}
```
C'est **déjà** idempotent (`create` + `update` avec le même objet). Suivre ce schéma, ne pas le réécrire. Imports en tête du fichier : `import { UserRole, OpcoStatus, DocType } from '@prisma/client';`.

`apps/web/src/lib/derive-cell-state.ts` — `CellState` porte déjà `{ state: 'NA' }` (L. 48). La priorité, commentée L. 71-79, est : `docStatus` manuel → `participantDocs` → `pedagogicalAssets` → `sessionDocs` → `productDocs` → `MISSING` (L. 143). Signature actuelle, 7 paramètres, le 7ᵉ (`flags`) optionnel :
```ts
export function deriveCellState(
  docType: string,
  participant: { docStatus: DocStatusMap | null | undefined },
  participantDocs: Map<string, { id: string }>,
  productDocs: Map<string, { id: string }>,
  sessionDocs: Map<string, { id: string }>,
  pedagogicalAssets: Map<string, { id: string }>,
  flags?: CellFlagSets,
): CellState
```

`apps/web/src/lib/signature/` (lot B, livré) : `port.ts`, `docuseal.ts`, `dry-run.ts`, `provider.ts`, `signatory.ts`, `text-tags.ts`. Le nouveau module s'y range. Attention : `port.ts` expose déjà `SignatureSignerInput.role: string` — c'est le rôle d'ancre côté PDF, sans rapport avec `SignerRole`. Aucun renommage.

Recette « lecture du seed depuis apps/web » — le précédent à copier est `apps/web/src/lib/docs/__tests__/seed-catalog.test.ts` : `seed.ts` n'est PAS importable dans un test `apps/web` (top-level `loadEnv`, `main()` auto-invoqué, `@prisma/client`), donc on lit le FICHIER SOURCE et on parse par regex. Chemin résolu :
```ts
const SEED_PATH = path.resolve(
  path.dirname(fileURLToPath(import.meta.url)),
  '../../../../../../packages/db/prisma/seed.ts',
);
```
(depuis `apps/web/src/lib/signature/__tests__/` la profondeur est identique à `apps/web/src/lib/docs/__tests__/` — **vérifier le chemin, ne pas le recopier à l'aveugle**.)

Garde par lecture de source — le précédent est `apps/web/src/lib/signature/__tests__/no-hardcoded-host.test.ts` : il retire commentaires et littéraux de message avant d'assertir, « les mentions en commentaire sont tolérées, une valeur en position de code ne l'est pas ».
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : le schéma porte la règle, le seed pose les 6 lignes, 6 tests les gardent</name>
  <files>
    packages/db/prisma/schema.prisma,
    packages/db/prisma/migrations/&lt;horodatage&gt;_signature_regime_financement/migration.sql,
    packages/db/prisma/seed.ts,
    apps/web/src/lib/signature/__tests__/seed-signataires.test.ts
  </files>
  <behavior>
    Six tests NOMMÉS, un par ligne du seed — exigence explicite de Laurent : « le rouge doit
    désigner le financeur fautif ». Donc SIX `it(...)`, pas une boucle `it.each` sur un tableau,
    pas un `forEach` anonyme. Chaque test lit le seed réel et asserte les 3 valeurs du financeur :
      - `it('AGEFICE — DIRIGEANT / STAGIAIRE / STAGIAIRE')`
      - `it('OPCO_EP — DIRIGEANT / null / null')`
      - `it('ATLAS — DIRIGEANT / null / null')`
      - `it('OPCOMMERCE — DIRIGEANT / null / null')`
      - `it('CPF — STAGIAIRE / null / null')`
      - `it('FI-FPL — STAGIAIRE / null / null')`
    Plus deux gardes :
      - `it('les 6 financeurs sont parsés (sanity)')` — sinon un parseur cassé rendrait 6 tests
        verts sur un tableau vide, et le rouge n'arriverait jamais.
      - `it('requiredDocs n'a pas bougé : ATLAS et OPCOMMERCE restent sans prose')` — la garde
        que ce lot n'a pas transformé de l'affichage en règle.
    Les 6 valeurs attendues sont écrites EN DUR DANS LE TEST, pas exportées dans une constante
    d'application : une constante importable redeviendrait le moteur de règles en dur que ce lot
    supprime.
  </behavior>
  <action>
1. **Schéma** — `packages/db/prisma/schema.prisma` :
   - ajouter, à côté du modèle `OpcoCatalog`, l'enum :
     ```prisma
     /// Qui signe une pièce, pour un régime de financement donné (spec signature §3 bis, D-10).
     /// DIRIGEANT = le représentant légal de l'organisation payeuse (SessionParticipant.sponsorOrg).
     /// STAGIAIRE = le participant lui-même.
     enum SignerRole {
       DIRIGEANT
       STAGIAIRE
     }
     ```
   - ajouter les trois colonnes NULLABLES sur `OpcoCatalog`, avec un commentaire `///` qui dit
     pourquoi `null` ≠ « pas renseigné » mais « hors régime » :
     ```prisma
     /// null = la pièce est HORS RÉGIME pour ce financeur → NA dans la matrice, jamais MISSING.
     conventionSigner SignerRole?
     ageficeSigner    SignerRole?
     assiduiteSigner  SignerRole?
     ```
   - **`requiredDocs` reste strictement inchangé.**

2. **Migration** — `prisma migrate dev`, JAMAIS `db push` (règle projet, commit `8c33f8a`) :
   ```
   pnpm --filter @qualiof/db exec prisma migrate dev --name signature_regime_financement
   ```
   Si Prisma ne trouve pas `DATABASE_URL` depuis `packages/db` (le `.env` vit à la racine du
   monorepo), utiliser la variante du dépôt qui charge l'env — c'est le MÊME `prisma migrate dev` :
   ```
   pnpm --filter @qualiof/db run db:migrate:local --name signature_regime_financement
   ```
   Le SQL généré doit être additif et RIEN d'autre : un `CREATE TYPE "SignerRole"` et trois
   `ADD COLUMN` nullables. **Relire `migration.sql` avant de commiter** : tout `DROP`, tout
   `NOT NULL`, tout `UPDATE` de backfill est le signe que le schéma a dérivé — s'arrêter et le dire.
   Ajouter en tête du fichier un commentaire FR à la façon de
   `20260904170000_signature_document_signed_fields/migration.sql` (« migration ADDITIVE, aucun
   backfill », référence spec §3 bis / D-10).

3. **Seed** — `packages/db/prisma/seed.ts`, fonction `seedOpcoCatalog()` :
   - importer `SignerRole` depuis `@prisma/client` (ligne d'import existante L. 20).
   - ajouter les 3 champs aux 6 entrées du tableau `opcos`, valeurs du tableau D-10 ci-dessus,
     **explicitement `null` là où c'est null** (pas de champ omis : la lecture doit être frontale,
     et le parseur du test compte sur leur présence).
   - au-dessus du tableau, un commentaire FR de 4-6 lignes qui reprend les points de compréhension
     verrouillés : pourquoi AGEFICE est `DIRIGEANT` sur la convention (le TNS est le représentant
     légal de sa propre EI), pourquoi CPF/FI-FPL sont `STAGIAIRE`, pourquoi FI-FPL est `null` sur
     agefice/assiduité (ce sont deux pièces AGEFICE). Sans ce commentaire, la première relecture
     « corrigera » AGEFICE en `null`.
   - ne PAS toucher `requiredDocs`, ne PAS changer la boucle `upsert` : elle est déjà idempotente.

4. **Tests** — créer `apps/web/src/lib/signature/__tests__/seed-signataires.test.ts` sur le patron
   de `seed-catalog.test.ts` :
   - `readSeedBlock()` : `indexOf('function seedOpcoCatalog')` puis couper à la première accolade
     fermante en colonne 0 (`search(/\n}/)`).
   - `parseSignataires(block)` : découper le bloc aux occurrences de `code: '…'` (une tranche par
     financeur, jusqu'au `code:` suivant ou la fin), puis dans chaque tranche extraire
     `/(conventionSigner|ageficeSigner|assiduiteSigner):\s*(null|SignerRole\.(\w+))/g`.
     Rendre une `Map<code, { conventionSigner, ageficeSigner, assiduiteSigner }>` avec `null` ou
     la chaîne `'DIRIGEANT'` / `'STAGIAIRE'`. Attention `FI-FPL` : le tiret doit passer le regex de code.
   - les 8 `it(...)` décrits dans &lt;behavior&gt;.
   - en-tête de fichier : commentaire FR expliquant POURQUOI 6 tests nommés plutôt qu'une boucle
     (le rouge doit désigner le financeur fautif) et pourquoi on lit la source plutôt qu'importer
     le seed (même raison que `seed-catalog.test.ts`).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; pnpm --filter @qualiof/web exec vitest run src/lib/signature/__tests__/seed-signataires.test.ts</automated>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; grep -E "DROP|NOT NULL|UPDATE " packages/db/prisma/migrations/*_signature_regime_financement/migration.sql; test $? -eq 1</automated>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; pnpm --filter @qualiof/db run lint</automated>
  </verify>
  <done>
    `SignerRole` et les 3 colonnes nullables sont dans le schéma, `requiredDocs` est intact (`git diff`
    le montre), une migration additive existe dans `migrations/` (CREATE TYPE + 3 ADD COLUMN, rien
    d'autre), le seed porte les 6 lignes du tableau D-10, et 8 tests passent — dont 6 nommés par
    financeur. **Test de puissance à exécuter et à recopier dans le SUMMARY** : mettre
    temporairement `assiduiteSigner: SignerRole.STAGIAIRE` sur OPCO_EP dans le seed, relancer le
    fichier de test, vérifier qu'**un seul** test rouge apparaît et qu'il s'appelle
    « OPCO_EP — DIRIGEANT / null / null », puis remettre `null`. Un test qui ne peut pas échouer ne
    prouve rien.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : le module pur qui résout le régime — sans base, sans `if` sur un code financeur</name>
  <files>
    apps/web/src/lib/signature/regime.ts,
    apps/web/src/lib/signature/__tests__/regime.test.ts
  </files>
  <behavior>
    - Test 1 — régime AGEFICE (`DIRIGEANT` / `STAGIAIRE` / `STAGIAIRE`) → 3 pièces :
      CONVENTION signée par `DIRIGEANT`, cible `{ kind: 'ORGANISATION', organizationId }` ;
      AGEFICE et ASSIDUITE signées par `STAGIAIRE`, cible `{ kind: 'PARTICIPANT', participantId }`.
      Aucun blocage.
    - Test 2 — régime OPCO_EP (`DIRIGEANT` / `null` / `null`) → 1 seule pièce (CONVENTION) ;
      `docTypesHorsRegime` rend exactement `{ AGEFICE, ASSIDUITE }`. C'est le cas « session
      100 % salariés OPCO : rien à envoyer côté APRÈS ».
    - Test 3 — régime CPF (`STAGIAIRE` / `null` / `null`) → CONVENTION ciblée sur le PARTICIPANT,
      **pas** sur l'organisation. Le test l'asserte nommément : c'est la ligne qui casse si
      quelqu'un « simplifie » en supposant qu'une convention se signe toujours côté entreprise.
    - Test 4 — `regle: null` (financeur inconnu ou organisation sans `opcoCatalog`) → **zéro** pièce
      et les 3 docTypes hors régime. L'inconnu ne vaut pas trois signatures par défaut.
    - Test 5 — `DIRIGEANT` avec `sponsorOrgId: null` → **zéro** pièce et un blocage
      `{ docType: 'CONVENTION', raison: 'ORG_PAYEUSE_ABSENTE' }`. Règle métier n°4 de la commande
      `/signature` : les signataires se résolvent, ils ne se devinent pas — et un envoi manquant
      doit être bruyant.
    - Test 6 — **test de puissance** : la MÊME règle OPCO_EP mutée en `assiduiteSigner: 'STAGIAIRE'`
      fait apparaître ASSIDUITE en régime, ciblée PARTICIPANT. Si la sortie ne bouge pas, c'est que
      le module a une table en dur quelque part.
    - Test 7 — **garde anti-`if` financeur** : lecture du source de `regime.ts`, commentaires et
      littéraux de message retirés (patron `no-hardcoded-host.test.ts`), et absence de
      `/\bcode\s*===/` comme de `/OPCO_EP|OPCOMMERCE|FI-FPL|ATLAS/`.
      ⚠ Ne PAS chercher `'AGEFICE'` : c'est un `DocType` légitime du corpus documentaire, présent
      dans `DOC_TYPES_SIGNABLES`. Un test qui l'interdirait serait rouge dès la première ligne
      écrite — et se ferait désactiver.
  </behavior>
  <action>
Créer `apps/web/src/lib/signature/regime.ts`. **Fonction pure : aucun import Prisma, aucun accès
réseau, aucun `await`.** L'appelant (lot C.2) lira `OpcoCatalog` et passera les 3 colonnes.

```ts
/** Rôle qui signe. Miroir volontaire de l'enum Prisma `SignerRole` : le module ne connaît pas la base. */
export type SignerRole = 'DIRIGEANT' | 'STAGIAIRE';

/** Les 3 pièces qui partent en signature électronique (spec §3, décision O-2). */
export const DOC_TYPES_SIGNABLES = ['CONVENTION', 'AGEFICE', 'ASSIDUITE'] as const;
export type DocTypeSignable = (typeof DOC_TYPES_SIGNABLES)[number];

/** Les 3 colonnes d'`OpcoCatalog`, lues telles quelles. `null` = pièce hors régime. */
export interface RegleSignatureFinanceur {
  conventionSigner: SignerRole | null;
  ageficeSigner: SignerRole | null;
  assiduiteSigner: SignerRole | null;
}

/** Qui signe, désigné par l'entité — pas encore par son email : la résolution du Contact
 *  dirigeant (et son blocage nominatif) est le travail du lot C.2. */
export type CibleSignature =
  | { kind: 'ORGANISATION'; organizationId: string }
  | { kind: 'PARTICIPANT'; participantId: string };

export interface PieceASigner { docType: DocTypeSignable; role: SignerRole; cible: CibleSignature; }
export interface BlocageRegime { docType: DocTypeSignable; raison: 'ORG_PAYEUSE_ABSENTE'; }

export interface ContexteRegime {
  /** Les 3 colonnes du financeur du participant. `null` = financeur inconnu → rien en régime. */
  regle: RegleSignatureFinanceur | null;
  participantId: string;
  /** L'organisation payeuse : `SessionParticipant.sponsorOrgId`. */
  sponsorOrgId: string | null;
}

/** LA table qui remplace les `if` : docType → colonne du financeur. Ajouter une pièce = une ligne ici. */
const COLONNE_PAR_DOCTYPE: Record<DocTypeSignable, keyof RegleSignatureFinanceur> = {
  CONVENTION: 'conventionSigner',
  AGEFICE: 'ageficeSigner',
  ASSIDUITE: 'assiduiteSigner',
};

export function signataireDe(docType: DocTypeSignable, regle: RegleSignatureFinanceur | null): SignerRole | null
export function docTypesEnRegime(regle: RegleSignatureFinanceur | null): ReadonlySet<DocTypeSignable>
export function docTypesHorsRegime(regle: RegleSignatureFinanceur | null): ReadonlySet<DocTypeSignable>
export function resolveRegimeSignature(ctx: ContexteRegime): { pieces: PieceASigner[]; blocages: BlocageRegime[] }
```

Règles d'implémentation :
- `resolveRegimeSignature` itère `DOC_TYPES_SIGNABLES` **dans l'ordre**, lit `COLONNE_PAR_DOCTYPE`,
  saute les `null` (hors régime), et pour les autres construit la cible : `STAGIAIRE` →
  `{ kind: 'PARTICIPANT', participantId }` ; `DIRIGEANT` → `{ kind: 'ORGANISATION', organizationId: sponsorOrgId }`
  si `sponsorOrgId` est présent, sinon **pas de pièce** et un `BlocageRegime`.
- `docTypesHorsRegime` doit rendre les 3 docTypes quand `regle` est `null` — écrire ce cas
  explicitement, pas par accident de boucle.
- Un `switch` exhaustif sur `SignerRole` (avec branche `never`) plutôt qu'un `if/else` : si un
  troisième rôle apparaît un jour, `tsc` le dira au lieu de le laisser tomber en silence.
- En-tête de fichier : commentaire FR ~15 lignes citant spec §3 bis + D-10, expliquant que la règle
  vient de la donnée (`OpcoCatalog`) comme `ProductFundingType` porte le taux horaire, et pourquoi
  le module ignore et Prisma et DocuSeal.

Puis `apps/web/src/lib/signature/__tests__/regime.test.ts` avec les 7 tests de &lt;behavior&gt;.
Les règles de test sont écrites en dur dans le fichier de test (objets littéraux), **pas** importées
du seed : ce sont deux gardes indépendantes — la tâche 1 garde le seed, la tâche 2 garde le moteur.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; pnpm --filter @qualiof/web exec vitest run src/lib/signature/__tests__/regime.test.ts</automated>
  </verify>
  <done>
    `regime.ts` est pur (aucun `import { prisma }`, aucun `async`), les 7 tests passent, et la garde
    anti-`if` financeur est verte. **Test de puissance à recopier dans le SUMMARY** : dans
    `COLONNE_PAR_DOCTYPE`, échanger temporairement `ASSIDUITE: 'assiduiteSigner'` en
    `ASSIDUITE: 'ageficeSigner'` → les tests 1 et 2 tombent en rouge. Remettre. Un moteur qu'on peut
    câbler de travers sans conséquence visible n'est pas testé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : `deriveCellState` sait dire NA hors régime — sans toucher à la priorité</name>
  <files>
    apps/web/src/lib/derive-cell-state.ts,
    apps/web/src/lib/__tests__/derive-cell-state.test.ts
  </files>
  <behavior>
    - Test A — `ASSIDUITE` hors régime, aucune trace nulle part → `{ state: 'NA' }` (et non `MISSING`).
    - Test B — `ASSIDUITE` hors régime **mais** un `Document` participant existe → reste
      `GENERATED`. Une pièce qui EXISTE ne disparaît pas de l'écran parce qu'elle est hors régime :
      c'est exactement le raisonnement déjà tenu dans `lib/sessions/participant-phase-items.ts`
      (« "Sans objet" ne vaut que pour un document ABSENT ») — cas de la double casquette
      EI + enseigne.
    - Test C — `ASSIDUITE` hors régime **mais** `docStatus.ASSIDUITE = MANUAL_OK` (scan déposé) →
      reste `MANUAL_OK`. La priorité 1 (manuel) l'emporte toujours.
    - Test D — hors régime + `MANUAL_OK` + `markedOkWithoutUpload` → reste
      `{ state: 'MANUAL_OK', warning: 'no_proof' }`. Le cas dérogatoire D-01 est intact.
    - Test E — **non-régression** : sans le nouveau paramètre, une cellule vide rend toujours
      `MISSING`. Les 8 call sites actuels ne changent pas d'un caractère.
  </behavior>
  <action>
1. `apps/web/src/lib/derive-cell-state.ts` — ajouter un **8ᵉ paramètre OPTIONNEL**, après `flags` :
   ```ts
   /**
    * DocTypes SANS OBJET pour ce participant, au vu du régime de financement de son
    * financeur (spec §3 bis, D-10 — `docTypesHorsRegime` de `lib/signature/regime.ts`).
    *
    * Ne s'applique qu'en DERNIER RECOURS, juste avant `MISSING` : une pièce hors régime
    * qui existe malgré tout (Document généré, scan déposé) reste affichée telle quelle.
    * `MISSING` appelle une action, `NA` dit qu'il n'y a rien à faire — confondre les deux
    * fait courir l'admin après des pièces qui n'existent pas.
    *
    * Paramètre optionnel : un appelant qui ne le passe pas retrouve exactement le
    * comportement d'avant.
    */
   docTypesHorsRegime?: ReadonlySet<string>,
   ```
   Puis, **uniquement** à la fin de la fonction, remplacer `return { state: 'MISSING' };` par :
   ```ts
   if (docTypesHorsRegime?.has(docType)) return { state: 'NA' };
   return { state: 'MISSING' };
   ```
   **Ne rien déplacer d'autre.** Aucune ligne au-dessus de ce point ne change : c'est ce qui garantit
   que `MANUAL_OK`, le warning `no_proof` (D-01) et le bug P0 `productDocs` restent intouchés.
   Mettre à jour le bloc de commentaire « Priorité (D-07 + Bug P0…) » L. 71-79 pour y ajouter
   l'étape 6 bis « hors régime → NA », et ajouter le `@param`.

2. Ne PAS propager le paramètre dans `buildMatrixData` (L. 157) : ce helper n'a pas de source de
   régime par participant, lui en inventer une ferait un deuxième chemin. Le câblage réel se fait
   en C.2, sur `participant-doc-matrix.tsx` (cf. « Hors périmètre » de l'objectif).

3. Ajouter les 5 tests A-E dans `apps/web/src/lib/__tests__/derive-cell-state.test.ts`, à la suite
   des existants (numérotation continue, commentaire FR en tête du nouveau `describe` renvoyant à
   la spec §3 bis). **Ne pas modifier les tests existants** : leur passage inchangé EST la preuve de
   non-régression.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/derive-cell-state.test.ts src/lib/__tests__/learner-stats.test.ts src/lib/sessions src/lib/docs</automated>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; pnpm lint</automated>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files-signature" &amp;&amp; pnpm test</automated>
  </verify>
  <done>
    Les 5 nouveaux tests passent, TOUS les tests préexistants de `derive-cell-state.test.ts` passent
    sans avoir été touchés, et `pnpm lint` + `pnpm test` sont verts sur le dépôt.
    **Test de puissance à recopier dans le SUMMARY** : déplacer temporairement le `if
    (docTypesHorsRegime?.has(docType))` AVANT le bloc `MANUAL_OK` (en haut de la fonction) → les
    tests C et D doivent tomber en rouge. Si les deux restent verts, c'est que la priorité n'est pas
    gardée et il faut renforcer les tests avant de conclure. Remettre le `if` à sa place.
  </done>
</task>

</tasks>

<verification>
```bash
# Gates du dépôt — il n'y a PAS de script `typecheck` :
# le `lint` de @qualiof/db et @qualiof/shared EST un `tsc --noEmit`.
pnpm lint
pnpm test
```

Contrôles ciblés :
```bash
# 1. requiredDocs n'a pas bougé
git diff packages/db/prisma/seed.ts | grep -E "^[-+].*requiredDocs" ; test $? -eq 1

# 2. la migration est additive et rien d'autre
cat packages/db/prisma/migrations/*_signature_regime_financement/migration.sql

# 3. aucun `db push` n'a été lancé : le schéma et migrations/ racontent la même histoire
git status --short packages/db/prisma/

# 4. le module de régime est pur
grep -nE "prisma|@qualiof/db|fetch\(|async " apps/web/src/lib/signature/regime.ts ; test $? -eq 1
```
</verification>

<success_criteria>
- [ ] `enum SignerRole { DIRIGEANT STAGIAIRE }` + `conventionSigner` / `ageficeSigner` / `assiduiteSigner` nullables sur `OpcoCatalog` ; `requiredDocs` inchangé.
- [ ] Une migration `*_signature_regime_financement` créée par `prisma migrate dev` (jamais `db push`), additive : `CREATE TYPE` + 3 `ADD COLUMN`, aucun `DROP`, aucun `NOT NULL`, aucun backfill.
- [ ] Le seed porte les 6 lignes exactes du tableau D-10 et reste idempotent (`upsert` create+update).
- [ ] 6 tests NOMMÉS (+ 2 gardes) sur le seed : casser une ligne rend rouge **ce** financeur, mutation exécutée et consignée.
- [ ] `lib/signature/regime.ts` : pur, testé, aucun `if` sur un code financeur, garde de source verte.
- [ ] Financeur inconnu → 3 pièces hors régime ; `DIRIGEANT` sans organisation payeuse → blocage nommé, jamais de pièce muette.
- [ ] `deriveCellState` rend `NA` hors régime en dernier recours, priorité `MANUAL_OK` et cas D-01 intacts, 8 call sites inchangés.
- [ ] Les 3 tests de puissance exécutés, leur rouge observé et recopié dans le SUMMARY.
- [ ] `pnpm lint` et `pnpm test` verts.
- [ ] Commentaires et documentation en français.
</success_criteria>

<output>
Après exécution, créer `.planning/quick/260910-c1r-signature-lot-c1-regime-financement/260910-c1r-SUMMARY.md`.

Y consigner en plus du gabarit :
- le SQL réel de la migration (recopié, 5 lignes) ;
- le résultat des **3 tests de puissance** (quel test est passé au rouge, et lui seul) ;
- la ligne à reprendre en C.2 : réconcilier `docTypesHorsRegime` avec la règle élargie `isAgefice`
  (BUG-11, double casquette EI + enseigne) avant de câbler la matrice et le bouton d'envoi.

Commits attendus : `test(signature-C1): …` puis `feat(signature-C1): …` par tâche (boucle RED → GREEN
de la commande `/signature` §3).
</output>
