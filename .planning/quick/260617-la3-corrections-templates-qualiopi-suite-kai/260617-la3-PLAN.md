---
quick_id: 260617-la3
title: Corrections templates Qualiopi (suite Kaïna 16/06)
branch: cloud-migration
mode: interactif diff-par-diff (Laurent valide chaque diff, pas d'executor autonome)
date: 2026-06-17
---

# Quick Task 260617-la3 — Corrections templates Qualiopi (suite Kaïna 16/06)

Branche `cloud-migration`, LOCAL (aucun push). Commits atomiques. tsc vert à chaque commit.
Modèles runtime QualiOF inchangés : Sonnet (rédactionnel) / Haiku (mécanique) via AI_PROVIDER=openrouter.

## Ordre d'exécution

### A — Corrections pack témoin
- **A1** Dégénériciser le prompt analyse besoin (`qualiopi-prompts.ts` `SYSTEM_PROMPT_ANALYSE_BESOIN`) :
  ancrer dans le contexte réel + la demande du stagiaire, doc AMONT, ne PAS réciter le programme.
  Bump `PROMPT_VERSION`.
- **A2** Handicap : défaut = NON (« aucun besoin signalé ») sur sessions passées, champ conservé
  (`analyse-besoin-template.ts` section Situation de handicap).
- **A3** Attestation : retirer logo/bloc Qualiopi → `renderOfficialBadges({ qualiopi: false })`
  (`attestation-template.ts:44`). Certificat garde Ministère seul (déjà fait).
- **A4** Émargement : sous « Certifié exact », signature Jean-Guy (`loadTrainerSignatureDataUrl`)
  + tampon Start Academy (`loadStampDataUrl`), au lieu de la signature pédago Laurent
  (`emargement-template.ts`).

### C — Date analyse besoin = J-15 à J-22 jours OUVRÉS
- Réécrire `computeAnalyseDate` (`jours-feries-fr.ts`) pour reculer en jours ouvrés stricts
  (sam+dim+fériés exclus) via une fonction `subtractBusinessDaysISO` (miroir de
  `addBusinessDaysISO` dans `business-days.ts`). Variation déterministe 0-7 jours ouvrés en plus.
  NE PAS toucher positionnement (entrée = 1er jour).

### D — Deux checklists distinctes
- Déplacer « Conditions du lieu d'accueil » (salle adaptée au nb inscrits, coin pause, coin repas,
  internet) dans l'ANALYSE DE BESOIN, formulé AU PRÉSENT (process, pas checklist antidatée).
- Retirer de `checklist-formation-template.ts` les blocs Repas/Pause + la ligne Connexion internet
  (= conditions du lieu). La checklist garde Administratif + matériel OF (PC/projecteur/paperboard/
  écran/support).

### B — Chunking par jour pour déroulés longs (72h)
- Refactor `generateDerouleContent` : 1 appel LLM par jour (vagues parallèles) pour éviter le timeout
  sur les formations longues. Fusionner les jours en un seul DerouleContent.

## Décisions (validées par Laurent ce jour)
- C : J-15 à J-22 jours OUVRÉS (variation déterministe en jours ouvrés, minimum 15).
- D : checklist = Admin + matériel OF uniquement ; conditions-lieu → analyse besoin (présent).
