# Deferred items — 260618-czs

## Out-of-scope pre-existing issues (NOT fixed)

- **tsc error `apps/web/src/server/actions/sessions.ts:804`** — `Object literal may only
  specify known properties, and 'legalName' does not exist in type` (Location create input).
  Pre-existing, unrelated to this quick task. Belongs to Laurent's session-location WIP
  (`session-location-picker.tsx` / `crud-edits.ts`). Left untouched per scope boundary.
- **6 `redirect-308.test.ts` errors** — documented pre-existing in STATE.md, ignored as instructed.
