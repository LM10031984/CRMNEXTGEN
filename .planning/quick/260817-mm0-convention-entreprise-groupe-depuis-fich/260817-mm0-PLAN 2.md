---
phase: quick-260817-mm0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/closure/convention-core.ts
  - apps/web/src/server/actions/convention-generator.ts
  - apps/web/src/components/sessions/convention-entreprise-panel.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/lib/closure/__tests__/convention-entreprise.test.ts
autonomous: true
requirements:
  - QUALIOPI-IND-9
  - CONVENTION-GROUPE-COMMANDITAIRE
must_haves:
  truths:
    - "Depuis la fiche session, Laurent peut générer UNE convention unique au nom d'une entreprise commanditaire couvrant tous ses salariés inscrits"
    - "La convention groupe liste tous les stagiaires du commanditaire avec nom + prénom UNIQUEMENT (aucune CSP, aucun poste)"
    - "Générer la convention groupe supprime les conventions individuelles des participants concernés (règle figée : jamais une par stagiaire)"
    - "Un commanditaire personne physique (EI/AUTO_ENTREPRENEUR/PARTICULIER/EIRL) est REFUSÉ avec un message renvoyant vers le contrat individuel"
    - "Les participants couverts par une convention groupe n'affichent plus « convention manquante » dans la fiche session"
    - "La date de signature reste J-15 jours ouvrés avant le début de session (jamais hardcodée)"
  artifacts:
    - path: "apps/web/src/lib/closure/convention-core.ts"
      provides: "generateConventionEntrepriseCore — convention groupée par commanditaire, sans auth"
      contains: "generateConventionEntrepriseCore"
    - path: "apps/web/src/components/sessions/convention-entreprise-panel.tsx"
      provides: "Liste des commanditaires personnes morales + génération par commanditaire"
      contains: "generateConventionEntreprise"
  key_links:
    - from: "apps/web/src/lib/closure/convention-core.ts"
      to: "apps/web/src/lib/convention-template.ts"
      via: "renderConventionHtml avec stagiaires: N personnes"
      pattern: "renderConventionHtml"
    - from: "apps/web/src/app/app/sessions/[id]/page.tsx"
      to: "apps/web/src/lib/sessions/doc-dock-items.ts"
      via: "docsByParticipant enrichi de la convention groupe"
      pattern: "entityType === 'organization'"
---

<objective>
Combler le gap constaté en conditions réelles sur la session OPTIMMO (11 salariées, dossier OPCO EP) : depuis la fiche session, il est IMPOSSIBLE de produire la convention au nom de l'entreprise commanditaire. Laurent a dû générer le document hors app par script pour déposer sur le portail OPCO.

Purpose: appliquer dans l'app la règle métier figée le 12/08 — payeur personne morale ⇒ **1 convention unique signée par le chef d'entreprise pour tout le groupe**, jamais une par salarié (une convention par stagiaire là où le besoin est celui de l'entreprise est une non-conformité en audit).

Output: une section « Convention entreprise » sur la fiche session, listant les commanditaires personnes morales avec leur effectif, et un bouton de génération par commanditaire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<decisions>
- **D-1 — Le template n'évolue pas.** `ConventionData.stagiaires` est déjà un tableau (« 1 si AE, N si entreprise avec salariés »). Seule l'orchestration manque : `generateConventionCore` construit un tableau d'UN élément (« Pour le MVP, 1 seule personne », convention-core.ts ~ligne 98).
- **D-2 — Pivot de décision** : `SessionParticipant.sponsorOrgId` + `Organization.legalForm`. Personnes physiques = EI, EIRL, AUTO_ENTREPRENEUR, PARTICULIER → refus (relèvent du contrat individuel). Personnes morales = SAS, SARL, SASU, EURL, SA, ASSOCIATION, AUTRE → convention groupe.
- **D-3 — Rattachement du Document groupe** : `entityType='organization'`, `entityId=sponsorOrgId`, `sessionId` renseigné, `participantId=null`. `Document.entityType` est un String libre (le commentaire du schéma cite « session | participant | invoice », ce n'est pas une contrainte) et `participantId` est nullable. **Aucune migration Prisma.**
- **D-4 — La convention groupe remplace les individuelles** du même groupe (règle figée « jamais une par stagiaire »). Laisser les deux créerait un doublon contradictoire en audit.
- **D-5 — Annexe nominative : nom + prénom UNIQUEMENT.** Consigne explicite de Laurent : pas de CSP, pas de poste occupé sur les documents.
- **D-6 — Aucune modification de `generateConventionCore`** (le chemin individuel actuel) : il reste la voie des auto-payeurs. On ajoute à côté, on ne réécrit pas — anti-régression.
</decisions>

<tasks>

## Task 1 — Cœur : convention groupée par commanditaire

files:
  - apps/web/src/lib/closure/convention-core.ts

action:
  - Ajouter `generateConventionEntrepriseCore(tenantId, sessionId, sponsorOrgId, options?)` **en delta**, sans toucher à `generateConventionCore`.
  - ⚠ Ce module ne doit JAMAIS importer `@/lib/auth`, même transitivement (symptôme connu : « react does not provide an export named cache »). Réutiliser uniquement ce qui est déjà importé ici.
  - Charger l'`Organization` (scope tenantId) et REFUSER si `legalForm` ∈ {EI, EIRL, AUTO_ENTREPRENEUR, PARTICULIER} avec un message explicite renvoyant vers le contrat individuel.
  - Charger tous les `SessionParticipant` de la session dont `sponsorOrgId` correspond ; refuser si zéro.
  - `stagiaires` = tous ces participants → `{ prenom, nom, email }` (le type `ConventionStagiaire` ne porte volontairement pas de CSP — D-5).
  - Bénéficiaire = l'Organization : `legalName`, `siret`, `representative` (fallback explicite si absent), ville RCS depuis `address.city` — même heuristique que l'existant.
  - Réutiliser à l'identique : `subtractBusinessDaysISO(startIso, 15)` pour `conventionDate`, la composition du `lieu`, `loadOfConfig`, `renderConventionHtml`, `renderHtmlToPdfWeasy`.
  - Prix : `produitPriceHTPerStagiaire` — prix par stagiaire, comme le chemin individuel.
  - Clé objet : `conventions/{session.code}/entreprise-{slug(org.legalName)}-{hash8}.pdf` (slug normalisé comme l'existant).
  - Idempotence + D-4, dans une transaction : supprimer l'ancien Document groupe (type CONVENTION, entityType='organization', entityId=sponsorOrgId, sessionId) **et** les Documents CONVENTION individuels des participants du groupe, puis créer le Document groupe.

verify: `pnpm --filter @qualiof/web exec tsc --noEmit`
done: la fonction existe, type-check propre, `generateConventionCore` inchangée.

## Task 2 — Server action wrapper

files:
  - apps/web/src/server/actions/convention-generator.ts

action:
  - `generateConventionEntreprise({ sessionId, sponsorOrgId })` : `requireRole` cohérent avec le wrapper individuel existant du fichier, scope `user.tenantId`, délègue au cœur, `revalidatePath('/app/sessions/{sessionId}')`, retour `{ ok }` / `{ ok:false, error }`.
  - Journaliser via le même mécanisme d'audit que le wrapper individuel s'il en utilise un.

verify: `pnpm --filter @qualiof/web exec tsc --noEmit`
done: action exportée, scopée tenantId.

## Task 3 — Fiche session : la convention groupe devient visible

files:
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/components/sessions/convention-entreprise-panel.tsx

action:
  - **Point d'intégration critique** : `page.tsx` ligne ~189 fait `if (!d.participantId) continue;` en construisant `docsByParticipant`. Une convention groupe (participantId=null) serait donc INVISIBLE et les 11 salariées afficheraient « convention manquante » alors que le document existe. Ajouter une passe : pour chaque Document `type='CONVENTION'` et `entityType='organization'`, injecter son id dans `docsByParticipant` de CHAQUE participant dont `sponsorOrgId === d.entityId` (sans écraser une éventuelle convention individuelle déjà présente).
  - Nouveau composant client `convention-entreprise-panel.tsx` : dérive les commanditaires personnes morales de la session (regroupement des participants par `sponsorOrg`), affiche « {raison sociale} — N participants » + bouton de génération, état de génération via `useTransition`, toasts `sonner`. Ne rend RIEN si la session n'a aucun commanditaire personne morale.
  - Monter le panneau dans l'onglet approprié de la fiche session (conventions/documents amont — suivre l'organisation en 5 onglets de la Phase 15).

verify: `pnpm --filter @qualiof/web exec tsc --noEmit && pnpm --filter @qualiof/web lint`
done: panneau visible sur une session à commanditaire, absent sinon ; participants du groupe plus marqués « manquant ».

## Task 4 — Tests

files:
  - apps/web/src/lib/closure/__tests__/convention-entreprise.test.ts

action:
  - N participants d'un même commanditaire → **1 seul** Document créé (pas N).
  - Les N stagiaires figurent dans `stagiaires` passé au template.
  - Aucune CSP / poste dans les données transmises (D-5).
  - Commanditaire personne physique (EI, AUTO_ENTREPRENEUR, PARTICULIER, EIRL) → refus avec message.
  - Les conventions individuelles des participants du groupe sont supprimées (D-4).
  - `conventionDate` = J-15 jours ouvrés (jamais la date du jour).
  - Suivre le pattern hermétique des tests existants du dossier (Prisma et rendu PDF mockés — cf. `generators-idempotent.test.ts` qui mocke déjà `renderConventionHtml`).

verify: `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- vitest run src/lib/closure/__tests__/convention-entreprise.test.ts`
done: tests verts, suite complète non régressée.

</tasks>

<pitfalls>
- `convention-core.ts` ne doit jamais importer `@/lib/auth`, même transitivement (script tsx / worker l'importent).
- Ne PAS modifier `generateConventionCore` : c'est le chemin des auto-payeurs, et le test `generators-idempotent` dépend de son `deleteMany` inconditionnel.
- Toute requête Prisma scopée `tenantId`.
- Adresse OF et signataire viennent de `of-config` — jamais en dur (siège = 12 avenue des Camélias, Cagnes-sur-Mer ; signataire = Laurent Marx, pas Julien Lafitte).
- La suite de tests doit être lancée avec les env chargées (`dotenv -e ../../.env --`), sinon 2 fichiers tombent au chargement de `sharedEnv` sans rapport avec ce chantier.
- Branche `cloud-migration` ; ne toucher à aucun fichier de la Phase 22.
- HORS PÉRIMÈTRE : template Contrat de formation professionnelle, et analyse de besoin par commanditaire.
</pitfalls>
