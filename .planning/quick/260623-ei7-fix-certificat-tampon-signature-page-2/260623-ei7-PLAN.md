# Quick Task 260623-ei7: Fix certificat tampon + signature page 2

**Created:** 2026-06-23
**Mode:** quick
**Scope strict:** Certificat de réalisation uniquement (template statique, pas de LLM).

## Problème

Sur le certificat de réalisation généré :
1. **Tampon absent** — seul `signature-laurent.png` est rendu ; le cachet OF (`tampon-start-academy.png`, exposé via `loadStampDataUrl`) n'apparaît jamais.
2. **Signature en page 2** — le bloc `.signature-block` n'a aucune protection anti-saut de page, il bascule entier en page 2 dès que le contenu approche le bas de page.

## Tâches

### Task 1 — Afficher le tampon en superposition sur la signature
- **files:** `apps/web/src/lib/closure/certificat-template.ts`
- **action:** Importer `loadStampDataUrl`, charger le tampon, remplacer le `<img class="tampon">` unique par un conteneur `.sig-stamp` (position relative) contenant le tampon (`.stamp`, z-index 1, décalé, opacity 0.88) en fond et la signature (`.sig`, z-index 2) au premier plan. CSS local dans le `<style>` du template.
- **verify:** `tsc --noEmit` vert ; rendu test session montre signature + tampon superposés en page 1.
- **done:** Tampon visible derrière/sous la signature sur le certificat.

### Task 2 — Empêcher le bloc signature de tomber en page 2
- **files:** `apps/web/src/lib/closure/shared-template.ts`
- **action:** Ajouter `page-break-inside: avoid;` + `break-inside: avoid;` sur `.signature-block`.
- **verify:** `tsc --noEmit` vert ; rendu test : bloc signature reste solidaire du « Fait à … le … » en page 1.
- **done:** Plus de bascule du bloc signature en page 2.

## Hors scope
- Attestation d'assiduité / autres docs : non touchés.
- Aucune régénération de masse ici (étape séparée, validation Laurent requise).
