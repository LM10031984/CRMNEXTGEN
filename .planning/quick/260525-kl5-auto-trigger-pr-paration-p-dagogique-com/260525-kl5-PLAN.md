---
phase: 260525-kl5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/actions/prepare-training.ts
  - apps/web/src/server/actions/sessions-create.ts
  - apps/web/src/components/sessions/preparation-pedagogique-block.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/components/sessions/prepare-training-button.tsx
autonomous: true
requirements: [KL5-01, KL5-02, KL5-03, KL5-04]
must_haves:
  truths:
    - "Créer une session déclenche automatiquement la génération des 6 catégories de docs pré-formation sans clic supplémentaire."
    - "Le bloc 'Préparation pédagogique' affiche l'état agrégé des 6 docs (3 partagés + 3 par participant) en temps réel."
    - "Le bouton 'Préparer la formation' n'apparaît plus en haut de la fiche session."
    - "Cliquer 'Compléter (X manquants)' rejoue uniquement les docs manquants (idempotence)."
  artifacts:
    - path: apps/web/src/server/actions/prepare-training.ts
      provides: "Server action prepareSession(sessionId) idempotente orchestrant 6 générations"
      exports: ["prepareSession", "getSessionPreparationStatus"]
    - path: apps/web/src/server/actions/sessions-create.ts
      provides: "Hook fire-and-forget appelant prepareSession après createSessionFull"
    - path: apps/web/src/components/sessions/preparation-pedagogique-block.tsx
      provides: "Bloc UI agrégé 6 docs avec CTA Compléter + auto-refresh BullMQ"
    - path: apps/web/src/app/app/sessions/[id]/page.tsx
      provides: "Intégration du bloc + retrait du bouton 'Préparer la formation'"
  key_links:
    - from: apps/web/src/server/actions/sessions-create.ts
      to: prepareSession
      via: "fire-and-forget après transaction createSessionFull"
      pattern: "void prepareSession\\("
    - from: apps/web/src/server/actions/prepare-training.ts
      to: getClosureQueue + enqueueClosureJob
      via: "ClosureBatch mini-batch pour ANALYSE_BESOIN par participant"
      pattern: "enqueueClosureJob\\("
    - from: apps/web/src/components/sessions/preparation-pedagogique-block.tsx
      to: prepareSession
      via: "useTransition + onClick handler 'Compléter'"
      pattern: "prepareSession\\("
    - from: apps/web/src/app/app/sessions/[id]/page.tsx
      to: PreparationPedagogiqueBlock
      via: "rendu entre SessionOnlyDocsBlock et ParticipantDocMatrix"
      pattern: "<PreparationPedagogiqueBlock"
---

<objective>
Automatiser la production des 6 catégories de docs pré-formation (Programme, Déroulé, Checklist, Convention, Convocation, Analyse besoin) à la création de session, et remplacer le bouton manuel "Préparer la formation" par un bloc UI agrégé qui montre l'état en temps réel et permet de compléter les manquants en idempotence.

Purpose: Faire gagner un clic et une attente à Laurent — la session créée arrive déjà prête côté Qualiopi. Plus de friction "j'ai oublié de cliquer Préparer".

Output:
- Server action `prepareSession(sessionId)` idempotente (orchestre 5 generators sync + 1 enqueue BullMQ pour l'analyse besoin)
- Hook fire-and-forget dans `createSessionFull` qui remplace l'auto-gen convention actuel
- Bloc UI `PreparationPedagogiqueBlock` (2 colonnes partagés/par-stagiaire, 3 états, auto-refresh)
- Retrait `PrepareTrainingButton` de la fiche session
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/web/src/server/actions/sessions-create.ts
@apps/web/src/server/actions/prepare-training.ts
@apps/web/src/server/actions/closure-pack.ts
@apps/web/src/server/actions/programme-generator.ts
@apps/web/src/server/actions/deroule-product-generator.ts
@apps/web/src/server/actions/convention-generator.ts
@apps/web/src/server/actions/convocation-generator.ts
@apps/web/src/server/actions/generate-checklist-formation.ts
@apps/web/src/components/sessions/prepare-training-button.tsx
@apps/web/src/components/sessions/batch-progress-auto-refresh.tsx
@apps/web/src/app/app/sessions/[id]/page.tsx
@apps/web/src/lib/closure/queue.ts
@apps/web/src/lib/closure/types.ts

<interfaces>
Contrats existants à réutiliser tels quels (extraits du code lu) :

```typescript
// prepare-training.ts (existant - sera étendu)
export async function prepareTrainingForSession(sessionId: string): Promise<PrepareTrainingResult>;

// programme-generator.ts
export async function generateProgrammeForProduct(
  productId: string,
  opts?: { force?: boolean },
): Promise<{ ok: boolean; documentId?: string; pdfUrl?: string; error?: string }>;

// deroule-product-generator.ts
export async function generateDerouleForProduct(
  productId: string,
  opts?: { force?: boolean },
): Promise<{ ok: boolean; documentId?: string; pdfUrl?: string; error?: string; usedStub?: boolean }>;

// generate-checklist-formation.ts
export async function generateChecklistForSession(
  sessionId: string,
  opts?: { force?: boolean },
): Promise<{ ok: boolean; documentId?: string; pdfUrl?: string; error?: string }>;

// convention-generator.ts
export async function generateConventionForParticipant(
  participantId: string,
): Promise<{ ok: boolean; documentId?: string; error?: string }>;

// convocation-generator.ts
export async function generateConvocationForParticipant(
  participantId: string,
): Promise<{ ok: boolean; documentId?: string; error?: string }>;

// closure/queue.ts
export async function enqueueClosureJob(payload: ClosureJobPayload): Promise<void>;
// Payload : { jobId, batchId, tenantId, sessionId, participantId, kind: ClosureDocKind }
// Le worker (lib/closure/worker.ts) traite kind='ANALYSE_BESOIN' et crée un PedagogicalAsset.

// types.ts
type ClosureDocKind = 'ATTESTATION' | 'CERTIFICAT' | 'QCM' | 'GRILLE_OBS' |
  'ANALYSE_BESOIN' | 'POSITIONNEMENT' | 'SATISFACTION_CHAUD' |
  'SATISFACTION_FROID' | 'DEROULE_PEDA' | 'EMARGEMENT';
```

Modèles Prisma clés :
- `Document` : `type` + `entityType` ('product'|'session'|'participant') + `entityId` + `participantId?`
- `PedagogicalAsset` : `kind` (PedagogicalKind, ex 'ANALYSE_BESOIN') + `sessionId` + `participantId` + `pdfUrl`
- `ClosureBatch` + `ClosureJob` : tracking BullMQ (status PENDING/RUNNING/COMPLETED/PARTIAL/FAILED)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor prepareSession idempotente avec analyse besoin BullMQ</name>
  <files>apps/web/src/server/actions/prepare-training.ts</files>
  <behavior>
    - Garder `prepareTrainingForSession` (export pour backward-compat éventuel, mais le bouton sera supprimé Task 3).
    - Ajouter `prepareSession(sessionId)` qui :
      1. Lit la session (productId, participants[id, personId]) tenant-scopée.
      2. Garde-fou tarif identique à l'existant (productPriceHT==0 && sessionPrice==0 → error early).
      3. Propage sessionPrice → product.priceHT si product à 0 (logique existante).
      4. Lance en parallèle (Promise.allSettled) :
         - generateProgrammeForProduct(productId)
         - generateDerouleForProduct(productId)
         - generateChecklistForSession(sessionId)
         - Pour chaque participant : generateConventionForParticipant + generateConvocationForParticipant en parallèle
      5. Pour l'analyse besoin (IA-assisté), crée UN ClosureBatch + N ClosureJob (kind='ANALYSE_BESOIN') puis enqueue via enqueueClosureJob. Skip les participants qui ont déjà un PedagogicalAsset kind='ANALYSE_BESOIN' (find-or-skip idempotent).
      6. Retourne `{ ok, total, programmesGenerated, conventionsGenerated, convocationsGenerated, derouleGenerated, checklistGenerated, analyseBesoinEnqueued, analyseBesoinSkipped, batchId?, errors }`.
    - Ajouter `getSessionPreparationStatus(sessionId)` qui retourne `{ ok, programme: boolean, deroule: boolean, checklist: boolean, conventionsCount, convocationsCount, analyseBesoinDone, analyseBesoinInProgress, analyseBesoinPending, participantsCount, batchId? }`. Query bulk Document (type IN ('PROGRAMME','DEROULE_PEDAGOGIQUE','CHECKLIST_FORMATION','CONVENTION','CONVOCATION')) + PedagogicalAsset (kind='ANALYSE_BESOIN') + ClosureJob (batch dernier kind=ANALYSE_BESOIN). Tenant-scopée via validateRequest.
    - L'enqueue analyse besoin réutilise le pattern exact de `closure-pack.ts` lignes 257-282 (ClosureBatch.create avec jobs:{create:[...]} puis Promise.all sur enqueueClosureJob avec jobId=job.id). Important : `requireRole` n'est PAS appelé dans prepareSession (appelée en fire-and-forget post-création où l'auth context peut être perdu) — on lit `validateRequest()` et bail out silencieux si pas d'user. **CONTRE-EXEMPLE worker.no_react_imports** : on N'IMPORTE PAS prepareSession depuis un worker tsx ; elle reste server action.
  </behavior>
  <action>
    1. Ouvrir `apps/web/src/server/actions/prepare-training.ts`.
    2. Ajouter en haut les imports manquants : `import { enqueueClosureJob } from '@/lib/closure/queue';` et `import type { ClosureDocKind } from '@qualiof/db';`.
    3. Sous `prepareTrainingForSession`, ajouter `export async function prepareSession(sessionId: string): Promise<...>` (signature dans behavior). Réutiliser le bloc `validateRequest` + récupération session + garde-fou tarif + propagation prix existants (copier les lignes 42-124 puis adapter — pas de duplication, factoriser la lecture en helper local si plus lisible).
    4. Implémenter les 3 generators session/produit (Promise.allSettled) + map des participants (Promise.all sur convention+convocation).
    5. Implémenter le mini-batch ANALYSE_BESOIN :
       ```typescript
       const existingAB = await prisma.pedagogicalAsset.findMany({
         where: { tenantId: user.tenantId, sessionId, kind: 'ANALYSE_BESOIN', participantId: { in: participantIds }, pdfUrl: { not: null } },
         select: { participantId: true },
       });
       const doneIds = new Set(existingAB.map((a) => a.participantId).filter(Boolean));
       const toEnqueue = session.participants.filter((p) => !doneIds.has(p.id));
       let batchId: string | undefined;
       if (toEnqueue.length > 0) {
         const batch = await prisma.closureBatch.create({
           data: {
             tenantId: user.tenantId, sessionId, status: 'PENDING',
             totalDocs: toEnqueue.length, createdByUserId: user.id,
             jobs: { create: toEnqueue.map((p) => ({ participantId: p.id, kind: 'ANALYSE_BESOIN' as ClosureDocKind, status: 'QUEUED' as const })) },
           },
           include: { jobs: { select: { id: true, participantId: true, kind: true } } },
         });
         batchId = batch.id;
         await Promise.all(batch.jobs.map((job) => enqueueClosureJob({ jobId: job.id, batchId: batch.id, tenantId: user.tenantId, sessionId, participantId: job.participantId, kind: job.kind })));
       }
       ```
    6. AuditLog : `await prisma.auditLog.create({ data: { tenantId: user.tenantId, userId: user.id, action: 'session.prepare', entityType: 'TrainingSession', entityId: sessionId, metadata: { programme, deroule, checklist, conventionsGenerated, convocationsGenerated, analyseBesoinEnqueued, analyseBesoinSkipped } } });`. Vérifier le shape de AuditLog côté schema (action est camelcase ailleurs : 'parameters.update', 'documents.*' — utiliser 'session.prepare' cohérent avec memory). Si user absent (fire-and-forget post-session), skip l'AuditLog (pas d'userId).
    7. Ajouter `revalidatePath('/app/sessions/' + sessionId)`.
    8. Ajouter `export async function getSessionPreparationStatus(sessionId: string)` : `validateRequest`, 3 queries bulk en parallèle :
       - `Document.findMany` : `entityType in ('product','session','participant'), entityId in [productId, sessionId, ...participantIds], type in (...)`
       - `PedagogicalAsset.findMany` : `sessionId, kind='ANALYSE_BESOIN', participantId in [...], pdfUrl: { not: null }`
       - `ClosureJob.findMany` : derniers jobs kind='ANALYSE_BESOIN' pour `batch.sessionId=sessionId`, agréger status counts (QUEUED+PROCESSING=inProgress, DONE=done).
       Retour structuré pour piloter l'UI.
    9. Ne PAS toucher à `prepareTrainingForSession` existant (sera supprimé Task 3 si grep confirme zéro usage).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" &amp;&amp; pnpm --filter @qualiof/web exec tsc --noEmit 2>&amp;1 | grep -E "prepare-training\.ts" || echo "OK no errors in prepare-training.ts"</automated>
  </verify>
  <done>
    - `prepareSession` exportée, signature retournant `{ ok, programmesGenerated, conventionsGenerated, convocationsGenerated, derouleGenerated, checklistGenerated, analyseBesoinEnqueued, analyseBesoinSkipped, batchId?, errors }`.
    - `getSessionPreparationStatus` exportée, retourne un état lisible.
    - Idempotence : 2 appels successifs ne créent pas de doublons (les generators sont find-or-create, analyse besoin filtre les déjà-faits).
    - `tsc --noEmit` clean sur le fichier.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hook fire-and-forget dans createSessionFull</name>
  <files>apps/web/src/server/actions/sessions-create.ts</files>
  <action>
    1. Ouvrir `apps/web/src/server/actions/sessions-create.ts`.
    2. Remplacer l'import existant `import { generateConventionForParticipant } from './convention-generator';` par `import { prepareSession } from './prepare-training';`.
    3. Supprimer le bloc actuel lignes 218-225 (boucle `for (const pid of session.createdParticipantIds) { generateConventionForParticipant(pid).catch(...); }`).
    4. Le remplacer par un seul appel fire-and-forget après le `await prisma.$transaction(...)` :
       ```typescript
       // Auto-trigger préparation pédagogique complète (programme + déroulé +
       // checklist + convention/convocation/analyse besoin par participant).
       // Fire-and-forget : la création de session reste rapide côté UX, la
       // préparation tourne en arrière-plan (analyse besoin = jobs BullMQ).
       // Idempotente : les generators sont find-or-create.
       void prepareSession(session.created.id).catch((e) =>
         console.warn(`[createSessionFull] prepareSession failed for ${session.created.id}:`, e?.message ?? e),
       );
       ```
    5. Conserver les `revalidatePath` existants. Le retour `{ ok: true, sessionId }` reste inchangé (la session est créée instantanément, la préparation tourne après).
    6. **Pitfall auth context** : `prepareSession` lit `validateRequest()` ; appelée en fire-and-forget sous le même tick de la server action, les cookies Next.js sont encore dispo via `cookies()`. Mais si Next garbage-collect le contexte avant que la promesse résolve, `validateRequest()` peut retourner `{ user: null }`. Le code de `prepareSession` doit bail out silencieusement dans ce cas (déjà géré Task 1 — retourne early sans throw).
    7. Vérifier aucun autre import de `generateConventionForParticipant` à conserver dans ce fichier (sinon laisser l'import).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" &amp;&amp; grep -n "generateConventionForParticipant\|prepareSession" apps/web/src/server/actions/sessions-create.ts &amp;&amp; pnpm --filter @qualiof/web exec tsc --noEmit 2>&amp;1 | grep -E "sessions-create\.ts" || echo "OK no errors in sessions-create.ts"</automated>
  </verify>
  <done>
    - L'ancien import `generateConventionForParticipant` est supprimé de `sessions-create.ts`.
    - `prepareSession` est importée et appelée en fire-and-forget après le transaction.
    - `tsc --noEmit` clean sur le fichier.
    - `createSessionFull` retourne toujours `{ ok: true, sessionId }` sans bloquer sur la préparation.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Composant PreparationPedagogiqueBlock + intégration page session + retrait bouton</name>
  <files>apps/web/src/components/sessions/preparation-pedagogique-block.tsx, apps/web/src/app/app/sessions/[id]/page.tsx, apps/web/src/components/sessions/prepare-training-button.tsx</files>
  <action>
    1. **Créer** `apps/web/src/components/sessions/preparation-pedagogique-block.tsx` (Client Component) :
       - Props : `{ sessionId: string; initialStatus: SessionPreparationStatus; canWrite: boolean; }` où `SessionPreparationStatus` est typé identique au retour de `getSessionPreparationStatus`.
       - useState pour status local initialisé depuis `initialStatus`.
       - useEffect : si `status.analyseBesoinInProgress + status.analyseBesoinPending > 0`, poll toutes les 5s via `getSessionPreparationStatus(sessionId)` (même pattern que `batch-progress-auto-refresh.tsx` mais sans router.refresh — on set le state local). Cleanup interval au unmount.
       - useTransition + handler `handleCompleter` qui appelle `prepareSession(sessionId)` puis update le state + toast.success/warning.
       - Layout 2 colonnes (grid sm:grid-cols-2 gap-4) :
         - Col 1 "Documents partagés" : 3 lignes (Programme / Déroulé / Checklist) avec icône `Check` (emerald) ou `Minus` (slate).
         - Col 2 "Documents par stagiaire (N)" : 3 lignes (Convention / Analyse besoin / Convocation) avec compteur `X/N` ; Analyse besoin affiche `Loader2 spin` si in-progress.
       - 3 états globaux :
         - **vide** (0 docs) : CTA primaire "Lancer la préparation" (`<button>` bleu primary).
         - **partiel** (1+ missing) : CTA secondaire "Compléter (X manquants)" avec calcul `X = (programme?0:1) + (deroule?0:1) + (checklist?0:1) + (N-conventionsCount) + (N-convocationsCount) + (analyseBesoinPending+analyseBesoinInProgress)`.
         - **complet** : badge vert "✓ Préparation complète" + lien discret vers ancre `#section-participants`.
       - Disabled state si `!canWrite` (RBAC : seuls ADMIN/MANAGER/COMMERCIAL voient le CTA actif — cohérent avec `requireRole` dans createSessionFull).
       - Wrapper section identique au style des autres blocs : `<section className="rounded-2xl border border-border bg-white p-5">`.
       - Use `lucide-react` icons : `ClipboardList`, `Check`, `Minus`, `Loader2`, `AlertCircle`.
       - Use `sonner` toast (mounted globally).
    2. **Modifier** `apps/web/src/app/app/sessions/[id]/page.tsx` :
       - **Retirer** l'import `import { PrepareTrainingButton } from '@/components/sessions/prepare-training-button';` (ligne 14).
       - **Retirer** le bloc switch DRAFT/PLANNED/OPEN/VALIDATED qui rend `<PrepareTrainingButton ...>` (lignes ~427-437) — laisser uniquement les autres cases (IN_PROGRESS → MarkCompletedButton, COMPLETED → lien pack). Simplifier le switch : retourner `null` pour DRAFT/PLANNED/OPEN/VALIDATED.
       - **Ajouter** l'import `import { PreparationPedagogiqueBlock } from '@/components/sessions/preparation-pedagogique-block';` et `import { getSessionPreparationStatus } from '@/server/actions/prepare-training';`.
       - Après le chargement de la session, ajouter `const preparationStatus = await getSessionPreparationStatus(session.id);` (la server action est tenant-scopée via validateRequest et l'auth est déjà résolue côté layout — pas de soucis).
       - **Insérer** `<PreparationPedagogiqueBlock sessionId={session.id} initialStatus={preparationStatus} canWrite={['ADMIN','MANAGER','COMMERCIAL'].includes(user.role)} />` ENTRE `<SessionOnlyDocsBlock ...>` (ligne 578) et le bloc `<div id="section-participants" ... />` puis `<ParticipantDocMatrix>` (lignes 590-598). Important : insérer après `</SessionOnlyDocsBlock>` (autour ligne 586) avant le `<div id="section-participants">`.
    3. **Supprimer** `apps/web/src/components/sessions/prepare-training-button.tsx` (fichier entier — grep a confirmé 1 seul usage dans page.tsx, qui est retiré ci-dessus).
    4. Vérifier que `SessionActionsMenu` (kebab) ne référence pas PrepareTrainingButton dans ses children côté page.tsx (il ne le fait pas — il reçoit DuplicateSessionButton + DeleteSessionButton).
    5. **Pitfall RBAC** : `prepareSession` ne fait pas `requireRole` (Task 1) car appelée fire-and-forget post-création. Mais quand appelée depuis le bloc UI (bouton "Compléter"), elle DOIT toujours fonctionner — `canWrite` côté UI suffit (le code défensif côté server n'est pas demandé par Laurent : `feedback_aller_droit_au_but`). Si on veut sécuriser : la session-level RBAC côté layout app garantit que l'utilisateur est authentifié et a un tenant.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" &amp;&amp; (test ! -e apps/web/src/components/sessions/prepare-training-button.tsx &amp;&amp; echo "FILE_DELETED_OK" || echo "FILE_STILL_EXISTS") &amp;&amp; (grep -rn "PrepareTrainingButton\|prepare-training-button" apps/web/src 2>/dev/null | wc -l | xargs -I{} sh -c '[ "{}" = "0" ] &amp;&amp; echo "ZERO_REFS_OK" || echo "STILL_REFERENCED"') &amp;&amp; pnpm --filter @qualiof/web exec tsc --noEmit 2>&amp;1 | grep -E "preparation-pedagogique-block\.tsx|sessions/\[id\]/page\.tsx" || echo "TSC_OK"</automated>
  </verify>
  <done>
    - `preparation-pedagogique-block.tsx` créé, Client Component, useTransition + sonner + auto-refresh.
    - `prepare-training-button.tsx` supprimé.
    - `page.tsx` : import et bloc UI insérés entre `SessionOnlyDocsBlock` et `ParticipantDocMatrix` ; ancien import + switch case PrepareTrainingButton retirés.
    - `grep -rn "PrepareTrainingButton\|prepare-training-button" apps/web/src` retourne 0 résultat.
    - `tsc --noEmit` clean.
  </done>
</task>

</tasks>

<verification>
**Sanity checks après les 3 tasks** :
1. `pnpm --filter @qualiof/web exec tsc --noEmit` clean (aucune erreur TS).
2. `grep -rn "PrepareTrainingButton\|prepare-training-button" apps/web/src` retourne 0 résultat.
3. `grep -n "prepareSession\|prepareTrainingForSession" apps/web/src/server/actions/sessions-create.ts` : voit `prepareSession` (fire-and-forget) et plus de `generateConventionForParticipant`.
4. **Tests existants** : `pnpm --filter @qualiof/web test` doit rester vert (593+ tests). Pas de nouveau test forcé — Laurent valide en créant une session test après `pnpm dev:full`.

**Test manuel attendu par Laurent (validation finale)** :
1. `pnpm dev:full` (auto-clean .next inclus).
2. Créer une nouvelle session via wizard avec 2-3 participants.
3. Observer la fiche session :
   - Le bouton "Préparer la formation" n'apparaît plus en haut.
   - Le bloc "Préparation pédagogique" est présent entre les docs session et la matrice.
   - Programme/Déroulé/Checklist sont cochés ou en cours (selon vitesse Ollama).
   - Convention 2/2 et Convocation 2/2 cochés rapidement (~2s/participant).
   - Analyse besoin affiche "X/2 (en cours)" puis se met à jour toutes les 5s.
4. Recharger la page : `getSessionPreparationStatus` reflète l'état BDD réel.
5. Cliquer "Compléter" sur une session partielle : ne refait rien si tout existe (idempotent), génère les manquants sinon.
</verification>

<success_criteria>
- Créer une session déclenche automatiquement les 6 catégories de docs sans clic ; toute UX existante de création session reste identique côté rapidité.
- Bloc UI visible, lisible, affiche les 3 états (vide/partiel/complet) correctement.
- Auto-refresh 5s actif tant que des analyses besoin sont in-progress/pending.
- Idempotence vérifiée : 2 appels de prepareSession back-to-back ne génèrent pas de docs en double (les generators sont find-or-create, l'analyse besoin filtre via PedagogicalAsset existant).
- Bouton "Préparer la formation" entièrement retiré (composant + import + usage = 0 grep result).
- `tsc --noEmit` clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260525-kl5-auto-trigger-pr-paration-p-dagogique-com/260525-kl5-SUMMARY.md` documenting :
- Décisions techniques (pattern fire-and-forget, batch BullMQ uniquement pour analyse besoin, getSessionPreparationStatus dédié pour le polling)
- Pitfalls rencontrés (auth context fire-and-forget, RBAC côté UI)
- Validation manuelle Laurent (capture mentale du nouveau flux)
- Backlog identifié hors-scope (AGEFICE auto-trigger, bug G IMAGIMO Drive)
</output>
