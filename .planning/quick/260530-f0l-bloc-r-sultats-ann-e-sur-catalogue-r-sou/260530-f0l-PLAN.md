---
phase: quick-260530-f0l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/catalogue/page.tsx
autonomous: true
requirements:
  - QUALIOPI-IND-2
must_haves:
  truths:
    - "Un visiteur ouvrant /catalogue voit immédiatement un bloc 'Nos résultats {année}' avec 4 indicateurs chiffrés"
    - "Le bloc affiche l'année courante si des sessions existent pour cette année, sinon la dernière année disponible"
    - "Si noteMoyenneSatisfaction ou tauxRecommandation est null, la carte affiche un message de fallback explicite (jamais 'NaN', jamais 'null')"
    - "Aucune erreur TypeScript (tsc --noEmit clean)"
  artifacts:
    - path: "apps/web/src/app/catalogue/page.tsx"
      provides: "Page catalogue publique avec bloc résultats Qualiopi Ind 2"
      contains: "getQualiopiBilan"
  key_links:
    - from: "apps/web/src/app/catalogue/page.tsx"
      to: "apps/web/src/lib/qualiopi-bilan-stats.ts"
      via: "import { getQualiopiBilan }"
      pattern: "getQualiopiBilan\\(tenant\\.id"
---

<objective>
Compléter la page publique /catalogue (livrée hier par quick 260530-eoy) avec un bloc "Nos résultats {année}" pour résoudre **Qualiopi Indicateur 2** (indicateurs de résultats chiffrés). Audit Qualiopi prévu le 03/07/2026, c'est le top 2 risque.

Purpose: Rendre les indicateurs de résultats annuels (stagiaires formés, heures, satisfaction, recommandation) visibles publiquement sur le catalogue, sans interventionmanuelle — branchés directement sur la source unique `getQualiopiBilan` qui agrège déjà les sessions COMPLETED/IN_PROGRESS.

Output: Bloc 4 KPI cards inséré entre le header et la grille produits dans `apps/web/src/app/catalogue/page.tsx`, avec fallback automatique sur la dernière année disponible si l'année courante est vide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/app/catalogue/page.tsx
@apps/web/src/lib/qualiopi-bilan-stats.ts

<interfaces>
<!-- Interface utilisée par cette tâche. Pas besoin d'explorer le codebase. -->

From apps/web/src/lib/qualiopi-bilan-stats.ts:
```typescript
export async function getQualiopiBilan(tenantId: string, year: number): Promise<BilanData>;

export interface BilanData {
  year: number;
  rows: BilanRow[];
  total: {
    nbSessions: number;
    nbHeures: number;
    nbStagiairesPrevu: number;
    nbStagiairesPresents: number;
    nbStagiairesAbsents: number;
    nbStagiairesAbandonnes: number;
    noteMoyenneSatisfaction: number | null; // /5
    tauxRecommandation: number | null;      // %
    prixTTC: number;
  };
  availableYears: number[]; // trié décroissant
}
```

Notes contextuelles importantes :
- `noteMoyenneSatisfaction` et `tauxRecommandation` peuvent être `null` (pas assez de réponses satisfaction) → fallback explicite obligatoire.
- `availableYears` est déjà trié décroissant → `availableYears[0]` = dernière année avec sessions.
- La page est déjà en `export const dynamic = 'force-dynamic'` → pas de souci de cache stale.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Brancher le bloc "Nos résultats {année}" sur /catalogue</name>
  <files>apps/web/src/app/catalogue/page.tsx</files>
  <behavior>
    Comportements attendus à la fin de la tâche :
    - À l'ouverture de /catalogue, un bloc visible immédiatement (entre le header et la grille produits) affiche "Nos résultats {année}" avec 4 KPI.
    - L'année affichée = année courante si `bilan.total.nbSessions > 0`, sinon `bilan.availableYears[0]`, sinon on n'affiche pas le bloc (cas BDD vide).
    - KPI 1 "Stagiaires formés" : chiffre = `bilan.total.nbStagiairesPresents`, caption = "ont suivi nos formations".
    - KPI 2 "Heures de formation" : chiffre = `${bilan.total.nbHeures} h`, caption = "dispensées cette année".
    - KPI 3 "Satisfaction moyenne" : si `noteMoyenneSatisfaction != null` → `${X.toFixed(1)} / 5`, sinon "Données en cours de consolidation" ; caption = "satisfaction stagiaires à chaud".
    - KPI 4 "Taux de recommandation" : si `tauxRecommandation != null` → `${Math.round(X)}%`, sinon "Données en cours de consolidation" ; caption = "stagiaires nous recommandent".
    - Aucun affichage de "NaN", "null", "undefined" peu importe l'état BDD.
    - Mention sous le bloc : "Données mises à jour automatiquement depuis notre système de gestion Qualiopi. Période : année {bilan.year}."
    - Grid responsive : `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`.
    - `pnpm tsc --noEmit` clean (pas de nouvelle erreur introduite).
  </behavior>
  <action>
    Modifier UNIQUEMENT `apps/web/src/app/catalogue/page.tsx`. Aucun autre fichier.

    1. **Ajouter l'import en haut du fichier** (à côté des autres imports `@/lib/...`) :
       ```ts
       import { getQualiopiBilan } from '@/lib/qualiopi-bilan-stats';
       ```

    2. **Dans `CataloguePage()`, après `const of = await loadOfConfig(tenant.id);` et avant le calcul de `dateLong`**, ajouter la résolution de l'année à afficher :
       ```ts
       const currentYear = new Date().getFullYear();
       let bilan = await getQualiopiBilan(tenant.id, currentYear);
       if (bilan.total.nbSessions === 0 && bilan.availableYears.length > 0) {
         bilan = await getQualiopiBilan(tenant.id, bilan.availableYears[0]);
       }
       const showBilan = bilan.total.nbSessions > 0;
       ```
       Le flag `showBilan` permet de masquer le bloc si la BDD est totalement vide (cas démo).

    3. **Insérer le bloc JSX entre `</section>` du hero (ligne ~107) et le bloc `{products.length === 0 ? (...)}`** :
       ```tsx
       {showBilan ? (
         <section className="mb-10" aria-labelledby="resultats-title">
           <h2
             id="resultats-title"
             className="text-2xl font-bold tracking-tight text-slate-900"
           >
             Nos résultats {bilan.year}
           </h2>
           <p className="mt-2 text-sm text-slate-600">
             Indicateurs de résultats annuels — Qualiopi indicateur 2.
           </p>

           <div className="mt-5 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
             {/* KPI 1 — Stagiaires formés */}
             <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
               <div className="text-3xl font-bold text-slate-900">
                 {bilan.total.nbStagiairesPresents}
               </div>
               <div className="mt-1 text-sm font-medium text-slate-800">
                 Stagiaires formés
               </div>
               <div className="text-xs text-slate-500">
                 ont suivi nos formations
               </div>
             </div>

             {/* KPI 2 — Heures de formation */}
             <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
               <div className="text-3xl font-bold text-slate-900">
                 {bilan.total.nbHeures} h
               </div>
               <div className="mt-1 text-sm font-medium text-slate-800">
                 Heures de formation
               </div>
               <div className="text-xs text-slate-500">
                 dispensées cette année
               </div>
             </div>

             {/* KPI 3 — Satisfaction moyenne */}
             <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
               <div className="text-3xl font-bold text-slate-900">
                 {bilan.total.noteMoyenneSatisfaction != null
                   ? `${bilan.total.noteMoyenneSatisfaction.toFixed(1)} / 5`
                   : (
                     <span className="text-base font-medium text-slate-500">
                       Données en cours de consolidation
                     </span>
                   )}
               </div>
               <div className="mt-1 text-sm font-medium text-slate-800">
                 Satisfaction moyenne
               </div>
               <div className="text-xs text-slate-500">
                 satisfaction stagiaires à chaud
               </div>
             </div>

             {/* KPI 4 — Taux de recommandation */}
             <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
               <div className="text-3xl font-bold text-slate-900">
                 {bilan.total.tauxRecommandation != null
                   ? `${Math.round(bilan.total.tauxRecommandation)}%`
                   : (
                     <span className="text-base font-medium text-slate-500">
                       Données en cours de consolidation
                     </span>
                   )}
               </div>
               <div className="mt-1 text-sm font-medium text-slate-800">
                 Taux de recommandation
               </div>
               <div className="text-xs text-slate-500">
                 stagiaires nous recommandent
               </div>
             </div>
           </div>

           <p className="mt-3 text-xs text-slate-500">
             Données mises à jour automatiquement depuis notre système de gestion
             Qualiopi. Période : année {bilan.year}.
           </p>
         </section>
       ) : null}
       ```

    4. **Points à ne PAS faire** :
       - Ne PAS créer un nouveau composant (un seul fichier modifié).
       - Ne PAS modifier `qualiopi-bilan-stats.ts` (la fonction est déjà parfaite).
       - Ne PAS ajouter de migration BDD, ni de nouveau lib.
       - Ne PAS toucher au style des cartes produits existantes.
       - Ne PAS utiliser de chiffre formaté avec `Intl.NumberFormat` pour les KPI (les nombres restent bruts pour la lisibilité — sauf si Laurent demande après).

    5. **Pourquoi ces choix** :
       - Fallback double (année courante → dernière année dispo → null) : début 2026 il n'y a quasiment pas encore de sessions COMPLETED, on bascule sur 2025 automatiquement.
       - Wrapper `<span>` avec `text-base font-medium text-slate-500` autour du fallback "Données en cours de consolidation" : sinon le `text-3xl font-bold` rend le message trop agressif visuellement.
       - `showBilan` masque le bloc entier si zéro session toute année confondue (cas démo BDD vide) — évite un bloc moche avec "0 stagiaires formés / 0 h".
       - Pas de `Suspense` ni `loading.tsx` : la page est déjà `force-dynamic`, l'`await` séquentiel est OK (requête bilan rapide, max 200ms en local).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit</automated>

    Vérifications manuelles complémentaires (Laurent ouvre /catalogue) :
    1. Bloc "Nos résultats 2026" (ou 2025 selon données) visible entre le hero et la grille produits.
    2. 4 cartes alignées : Stagiaires formés / Heures / Satisfaction / Recommandation.
    3. Si satisfaction ou recommandation est null → "Données en cours de consolidation" affiché (pas "NaN", pas "null").
    4. Responsive : 1 col mobile, 2 cols tablette (sm), 4 cols desktop (lg).
    5. Mention "Données mises à jour automatiquement..." en gris discret sous le bloc.
  </verify>
  <done>
    - `pnpm --filter @qualiof/web exec tsc --noEmit` retourne 0 erreur sur `apps/web/src/app/catalogue/page.tsx`.
    - Le fichier importe `getQualiopiBilan` depuis `@/lib/qualiopi-bilan-stats`.
    - Le fichier contient le bloc `<section aria-labelledby="resultats-title">` avec les 4 KPI.
    - Le bloc gère le fallback null pour `noteMoyenneSatisfaction` et `tauxRecommandation` (string explicite, pas de NaN).
    - Le bloc gère le fallback année courante → dernière année disponible.
    - Aucun autre fichier modifié.
    - À l'ouverture manuelle de /catalogue par Laurent, les indicateurs sont visibles immédiatement (au-dessus de la fold sur desktop).
  </done>
</task>

</tasks>

<verification>
**Vérifications globales du plan :**

1. **TypeScript** : `pnpm --filter @qualiof/web exec tsc --noEmit` clean.
2. **Pas de régression visuelle** : grille produits, header, footer inchangés.
3. **Qualiopi Indicateur 2 satisfait** : indicateurs chiffrés de résultats annuels publiés publiquement, mis à jour automatiquement depuis le CRM (preuve d'audit auto-actualisée — aucune ressaisie manuelle).
4. **Robustesse fallback** : si BDD vide totalement → bloc masqué (showBilan=false), pas de "0 stagiaires / 0h" affichés. Si satisfaction/reco null → message "en cours de consolidation".
</verification>

<success_criteria>
**Mesurables :**
- [ ] `apps/web/src/app/catalogue/page.tsx` importe `getQualiopiBilan`.
- [ ] La page contient `<h2>Nos résultats {bilan.year}</h2>` (ou son équivalent JSX).
- [ ] 4 KPI rendus dans une grille `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`.
- [ ] Fallback explicite "Données en cours de consolidation" pour les valeurs null.
- [ ] `pnpm --filter @qualiof/web exec tsc --noEmit` retourne 0 erreur.
- [ ] Laurent ouvre `/catalogue` et confirme visuellement l'affichage des 4 indicateurs.
</success_criteria>

<output>
After completion, create `.planning/quick/260530-f0l-bloc-r-sultats-ann-e-sur-catalogue-r-sou/260530-f0l-SUMMARY.md` documenting :
- Modifications exactes apportées à `apps/web/src/app/catalogue/page.tsx` (lignes ajoutées).
- Année effectivement affichée (currentYear ou fallback).
- Comportement observé pour les valeurs null satisfaction/reco.
- Confirmation tsc clean.
- Lien vers preuve Qualiopi Ind 2 (capture /catalogue à archiver dans dossier audit 03/07/2026).
</output>
