---
phase: 09-distribution-leads-automatique
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/server/actions/leads.ts
  - apps/web/src/server/actions/distribution-leads-config.ts
  - apps/web/src/server/actions/__tests__/leads.test.ts
  - apps/web/src/server/actions/__tests__/distribution-leads-config.test.ts
  - apps/web/src/lib/lead-notifications.ts
  - apps/web/src/lib/__tests__/lead-notifications.test.ts
  - apps/web/src/lib/audit-log.ts
  - apps/web/src/lib/__tests__/audit-log.test.ts
  - .env.example
autonomous: true
requirements: [LEAD-01]

must_haves:
  truths:
    - "`createLead(input)` valide via CreateLeadSchema, scope tenantId via requireRole, déclenche autoAssignLead si tenant.autoAssignLeads=true, puis notifyLeadAssigned"
    - "`reassignLead(leadId)` appelle autoAssignLead(force: true) puis notifyLeadAssigned(assignedBy=user.id) — IGNORE le toggle autoAssignLeads (Pitfall 4 décision : oui)"
    - "`updateLeadStatus(leadId, newStatus)` set wonAt=now() si transition vers WON, wonAt=null sinon"
    - "`updateLeadDistributionConfig(input)` ADMIN-only, écrit les 3 toggles Tenant + AuditLog `leads.distribution_config`"
    - "`notifyLeadAssigned(opts)` orchestre (1) Notification cloche si notifyOnLeadAssignBell, (2) email si notifyOnLeadAssignEmail, (3) AuditLog `leads.auto_assigned` OU `leads.reassigned`"
    - "`logUserAction` accepte targetEntityId pour entity='Lead' (extension Phase 9 : action=`leads.*`, actorUserId nullable pour system)"
  artifacts:
    - path: "apps/web/src/server/actions/leads.ts"
      provides: "createLead, reassignLead, updateLeadStatus (server actions LEAD-01)"
      exports: ["createLead", "reassignLead", "updateLeadStatus"]
    - path: "apps/web/src/server/actions/distribution-leads-config.ts"
      provides: "updateLeadDistributionConfig (D-06 toggles)"
      exports: ["updateLeadDistributionConfig"]
    - path: "apps/web/src/lib/lead-notifications.ts"
      provides: "Helper centralisé side-effects post-assignation"
      exports: ["notifyLeadAssigned"]
    - path: "apps/web/src/lib/audit-log.ts"
      provides: "Phase 9 — extension logUserAction pour entity générique (Lead)"
      exports: ["logUserAction", "logTenantSettingsChange", "computeDiff", "logLeadEvent (nouveau)"]
  key_links:
    - from: "server/actions/leads.ts createLead"
      to: "autoAssignLead + notifyLeadAssigned"
      via: "appel séquentiel après lead.create, conditionné par tenant.autoAssignLeads"
      pattern: "autoAssignLead\\(\\s*lead\\.id"
    - from: "lib/lead-notifications.ts notifyLeadAssigned"
      to: "prisma.notification.create + sendMail + prisma.auditLog.create"
      via: "3 side-effects en parallèle conditionnés par 2 toggles tenant"
      pattern: "(prisma\\.notification\\.create|sendMail|prisma\\.auditLog\\.create)"
    - from: "server/actions/leads.ts reassignLead"
      to: "AuditLog action='leads.reassigned' avec userId=user.id (manuel)"
      via: "via notifyLeadAssigned avec assignedBy=user.id"
      pattern: "leads\\.reassigned"
---

<objective>
Plan 09-02 — Server actions + wiring : implémenter LEAD-01 trigger automatique.

Cette plan branche l'algo existant `autoAssignLead` (Phase 9 read-only) sur les call-sites métier qu'il fallait créer :
1. `createLead` — server action de création + auto-assign + notification + audit
2. `reassignLead` — bouton manuel "Réassigner" (force=true, audit user.id)
3. `updateLeadStatus` — set `wonAt` pour faire vivre les KPI 2/3/4 (Pitfall 3)
4. `updateLeadDistributionConfig` — 3 toggles tenant (D-06)
5. `notifyLeadAssigned` — helper centralisé (Notification + Email + AuditLog) conditionné par les 2 toggles notif
6. Extension `logUserAction` (ou nouveau `logLeadEvent`) pour entity='Lead' avec actorUserId nullable (system)

Purpose: Sans ce plan, l'algo `autoAssignLead` n'a aucun consumer dans le flow normal (juste le bouton bulk).
Output: 5 nouveaux fichiers (3 actions + 1 helper + 1 audit extension), 4 fichiers tests, 1 mise à jour `.env.example` (APP_URL si manquant), 0 modif UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-distribution-leads-automatique/09-CONTEXT.md
@.planning/phases/09-distribution-leads-automatique/09-RESEARCH.md
@.planning/phases/09-distribution-leads-automatique/09-01-SUMMARY.md

@apps/web/src/server/actions/auto-assign-leads.ts
@apps/web/src/server/actions/tenant-users.ts
@apps/web/src/lib/rbac.ts
@apps/web/src/lib/audit-log.ts
@apps/web/src/lib/mailer.ts
@apps/web/src/lib/of-config.ts

<interfaces>
<!-- Contrats EXISTANTS à consommer (Phases 7/8). -->

From apps/web/src/lib/rbac.ts:
```typescript
export class UnauthorizedError extends Error {}
export class ForbiddenError extends Error {}
export async function requireRole(allowed: UserRole[]): Promise<LuciaUser>;
// LuciaUser a : id, email, role, tenantId, firstName, lastName
```

From apps/web/src/lib/audit-log.ts:
```typescript
export async function logUserAction(opts: {
  tenantId: string;
  actorUserId: string | null;          // null = system action
  targetUserId: string;
  action: string;
  diff?: Diff | Record<string, unknown>;
  ip?: string | null;
  userAgent?: string | null;
}): Promise<void>;
// ⚠ targetUserId est tied à entity='User' — pour Lead on a 2 options :
//   (a) ajouter une fn jumelle logLeadEvent (recommandé — sémantique claire)
//   (b) generaliser logUserAction → entity générique (refacto plus large)
// Choisir (a) pour cette phase.
```

From apps/web/src/server/actions/auto-assign-leads.ts:
```typescript
export async function autoAssignLead(
  leadId: string,
  opts?: { force?: boolean },
): Promise<{
  ok: boolean;
  leadId: string;
  ownerUserId?: string;
  ownerName?: string;
  skippedReason?: string;
  error?: string;
}>;
```

From apps/web/src/lib/mailer.ts:
```typescript
export async function sendMail(opts: {
  to: string;
  subject: string;
  html: string;
  text: string;
}): Promise<{ ok: boolean; messageId?: string; dryRun?: boolean; error?: string }>;
// Dry-run automatique si SMTP_HOST absent → logs console, retourne ok=true dryRun=true.
```

From packages/shared/src/schemas/lead.ts (Plan 09-01) :
```typescript
export const CreateLeadSchema: z.ZodType<{...}>;
export const DistributionConfigSchema: z.ZodType<{...}>;
export type CreateLeadInput, DistributionConfigInput, LeadAssignedPayload;
```

From apps/web/src/lib/mailer-templates/lead-assigned.ts (Plan 09-01) :
```typescript
export interface LeadAssignedEmailInput {
  commercialFirstName: string;
  prospectName: string;
  leadSource: string | null;
  productTitle: string | null;
  leadUrl: string;
}
export function renderLeadAssignedEmail(input, of): { subject, html, text };
```

Pattern Server Action discriminé Phase 7/8 :
```typescript
type ActionResult<T = void> =
  | { ok: true; data?: T }
  | { ok: false; error: string; fieldErrors?: Record<string, string[]> };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extension audit-log.ts pour logLeadEvent + helper lead-notifications.ts</name>
  <files>apps/web/src/lib/audit-log.ts, apps/web/src/lib/__tests__/audit-log.test.ts, apps/web/src/lib/lead-notifications.ts, apps/web/src/lib/__tests__/lead-notifications.test.ts</files>
  <read_first>
    - apps/web/src/lib/audit-log.ts (logUserAction lignes 91-112 — pattern à cloner)
    - apps/web/src/lib/mailer.ts (sendMail signature)
    - apps/web/src/lib/of-config.ts (loadOfConfig)
    - apps/web/src/lib/mailer-templates/lead-assigned.ts (Plan 09-01 — renderLeadAssignedEmail)
    - .planning/phases/09-distribution-leads-automatique/09-RESEARCH.md (Pattern 2)
  </read_first>
  <behavior>
    `logLeadEvent` :
    - Crée 1 row AuditLog avec entity='Lead', entityId=leadId
    - actorUserId nullable (null = system → action='leads.auto_assigned' ; non-null = action='leads.reassigned' ou 'leads.distribution_config')
    - Pas de no-op sur diff vide (différent de logTenantSettingsChange)

    `notifyLeadAssigned` :
    - Lit lead + owner + tenant (3 toggles)
    - Si lead/owner/tenant null → return silencieux
    - Si `notifyOnLeadAssignBell=true` → crée Notification {type:'lead.assigned', payload typé}
    - Si `notifyOnLeadAssignEmail=true` ET `owner.email` → loadOfConfig + renderLeadAssignedEmail + sendMail
    - TOUJOURS écrit AuditLog (action='leads.auto_assigned' si assignedBy=null, sinon 'leads.reassigned')
    - Ne renvoie PAS le statut `dryRun` de sendMail dans le AuditLog (Pitfall 5 : option b)
    - `prospectName` calculé : `person.firstName + person.lastName` SINON `lead.firstName + lead.lastName` SINON 'Prospect'
  </behavior>
  <action>
    **1. Étendre `apps/web/src/lib/audit-log.ts`** — AJOUTER en fin de fichier :

    ```typescript
    /**
     * Helper AuditLog pour entité 'Lead' (Phase 9 — D-07 CONTEXT.md).
     *
     * Convention `action` :
     *  - 'leads.auto_assigned'      → actorUserId=null (system, à la création)
     *  - 'leads.reassigned'         → actorUserId=user.id (clic manuel "Réassigner")
     *  - 'leads.distribution_config' → actorUserId=admin.id (modif des 3 toggles)
     *
     * Distinction par rapport à `logUserAction` :
     *  - `targetLeadId` = lead concerné (AuditLog.entityId).
     *  - `actorUserId`  = qui déclenche (null = system).
     *  - Pas de no-op sur diff vide : certains événements n'ont qu'un payload (ex. auto_assigned).
     */
    export async function logLeadEvent(opts: {
      tenantId: string;
      actorUserId: string | null;
      targetLeadId: string;
      action: string;
      diff?: Diff | Record<string, unknown>;
    }): Promise<void> {
      await prisma.auditLog.create({
        data: {
          tenantId: opts.tenantId,
          userId: opts.actorUserId,
          entity: 'Lead',
          entityId: opts.targetLeadId,
          action: opts.action,
          diff: (opts.diff ?? {}) as never,
        },
      });
    }
    ```

    **2. Créer `apps/web/src/lib/__tests__/audit-log.test.ts`** (~50 lignes) — 3 cas :
    - `logLeadEvent` avec actorUserId=null écrit row avec userId=null + action='leads.auto_assigned'
    - `logLeadEvent` avec actorUserId='abc' écrit row avec userId='abc' + action='leads.reassigned'
    - `logLeadEvent` avec diff vide écrit quand même la row (pas de no-op)
    - Mock prisma.auditLog.create avec `vi.mock('@qualiof/db')`

    **3. Créer `apps/web/src/lib/lead-notifications.ts`** (~95 lignes) — pattern 1:1 avec RESEARCH.md Pattern 2 :

    ```typescript
    /**
     * LEAD-01 — Side-effects post-assignation (Notification + Email + AuditLog).
     *
     * Centralisé ici (vs inline dans server actions) pour :
     *  - Tester en isolation avec mocks Prisma + mailer
     *  - Garder createLead/reassignLead lisibles
     *  - Respecter les 3 toggles Tenant (D-06)
     */

    import { prisma } from '@qualiof/db';
    import { sendMail } from './mailer';
    import { renderLeadAssignedEmail } from './mailer-templates/lead-assigned';
    import { loadOfConfig } from './of-config';
    import { logLeadEvent } from './audit-log';

    export async function notifyLeadAssigned(opts: {
      tenantId: string;
      leadId: string;
      ownerUserId: string;
      assignedBy: string | null;       // null = system (auto à la création)
    }): Promise<void> {
      const [lead, owner, tenant] = await Promise.all([
        prisma.lead.findUnique({
          where: { id: opts.leadId },
          include: { person: true, interestedProduct: true },
        }),
        prisma.user.findUnique({
          where: { id: opts.ownerUserId },
          select: { id: true, email: true, firstName: true, lastName: true },
        }),
        prisma.tenant.findUnique({
          where: { id: opts.tenantId },
          select: { notifyOnLeadAssignEmail: true, notifyOnLeadAssignBell: true },
        }),
      ]);
      if (!lead || !owner || !tenant) return;

      // Compute prospectName (D-03 dans CONTEXT.md)
      const prospectName =
        (lead.person ? `${lead.person.firstName} ${lead.person.lastName}`.trim() : '') ||
        `${lead.firstName ?? ''} ${lead.lastName ?? ''}`.trim() ||
        'Prospect';

      // 1. Notification cloche (si toggle bell ON)
      if (tenant.notifyOnLeadAssignBell !== false) {
        await prisma.notification.create({
          data: {
            tenantId: opts.tenantId,
            userId: opts.ownerUserId,
            type: 'lead.assigned',
            payload: {
              leadId: opts.leadId,
              prospectName,
              source: lead.source ?? null,
            },
          },
        });
      }

      // 2. Email (si toggle email ON ET owner a un email)
      if (tenant.notifyOnLeadAssignEmail !== false && owner.email) {
        const of = await loadOfConfig(opts.tenantId);
        const appUrl = process.env.APP_URL ?? '';
        const { subject, html, text } = renderLeadAssignedEmail(
          {
            commercialFirstName: owner.firstName,
            prospectName,
            leadSource: lead.source ?? null,
            productTitle: lead.interestedProduct?.title ?? null,
            leadUrl: `${appUrl}/app/leads/${opts.leadId}`,
          },
          of,
        );
        await sendMail({ to: owner.email, subject, html, text });
        // ⚠ On ignore volontairement le dryRun flag (Pitfall 5 option b) :
        // l'AuditLog `leads.auto_assigned` n'atteste pas l'envoi physique.
      }

      // 3. AuditLog (toujours — convention D-07)
      await logLeadEvent({
        tenantId: opts.tenantId,
        actorUserId: opts.assignedBy,
        targetLeadId: opts.leadId,
        action: opts.assignedBy ? 'leads.reassigned' : 'leads.auto_assigned',
        diff: {
          assignedTo: opts.ownerUserId,
          assignedBy: opts.assignedBy ?? 'system',
        },
      });
    }
    ```

    **4. Créer `apps/web/src/lib/__tests__/lead-notifications.test.ts`** (~120 lignes) — 5 cas :
    - Toggle bell=true → crée 1 Notification ; toggle bell=false → ne crée pas
    - Toggle email=true + owner.email → appel sendMail ; toggle email=false → pas d'appel
    - Owner sans email → pas d'appel sendMail même si toggle ON
    - assignedBy=null → AuditLog action='leads.auto_assigned' userId=null
    - assignedBy='admin-id' → AuditLog action='leads.reassigned' userId='admin-id'
    - lead introuvable → return silencieux (pas d'erreur)
    - Mock prisma.lead/user/tenant.findUnique + prisma.notification.create + prisma.auditLog.create + sendMail + loadOfConfig
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/lib/__tests__/audit-log.test.ts src/lib/__tests__/lead-notifications.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function logLeadEvent" apps/web/src/lib/audit-log.ts` retourne 1 match.
    - `grep -n "export async function notifyLeadAssigned" apps/web/src/lib/lead-notifications.ts` retourne 1 match.
    - `grep -n "leads.auto_assigned" apps/web/src/lib/lead-notifications.ts` retourne 1 match.
    - `grep -n "leads.reassigned" apps/web/src/lib/lead-notifications.ts` retourne 1 match.
    - `grep -n "notifyOnLeadAssignBell" apps/web/src/lib/lead-notifications.ts` retourne 1 match.
    - `grep -n "notifyOnLeadAssignEmail" apps/web/src/lib/lead-notifications.ts` retourne 1 match.
    - `pnpm --filter @qualiof/web test --run src/lib/__tests__/audit-log.test.ts` → ≥ 3 tests verts.
    - `pnpm --filter @qualiof/web test --run src/lib/__tests__/lead-notifications.test.ts` → ≥ 5 tests verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    `logLeadEvent` exporté depuis audit-log.ts (convention D-07 actionnable). `notifyLeadAssigned` orchestre les 3 side-effects en respectant les 2 toggles. Tests Vitest verts. Pas de couplage circulaire (lead-notifications importe mailer + audit-log + of-config, jamais l'inverse).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server actions leads.ts (createLead + reassignLead + updateLeadStatus)</name>
  <files>apps/web/src/server/actions/leads.ts, apps/web/src/server/actions/__tests__/leads.test.ts, .env.example</files>
  <read_first>
    - apps/web/src/server/actions/auto-assign-leads.ts (autoAssignLead signature)
    - apps/web/src/server/actions/tenant-users.ts (pattern requireRole + try/catch UnauthorizedError/ForbiddenError + revalidatePath)
    - apps/web/src/lib/lead-notifications.ts (notifyLeadAssigned — depuis Task 1)
    - packages/shared/src/schemas/lead.ts (CreateLeadSchema — Plan 09-01)
    - .env.example (vérifier APP_URL ; ajouter si absent)
  </read_first>
  <behavior>
    `createLead(input)` :
    - requireRole ADMIN/MANAGER/COMMERCIAL
    - safeParse(CreateLeadSchema) ; si échec → `{ ok: false, error: 'Validation', fieldErrors }`
    - Lit `tenant.autoAssignLeads` ; create lead (ownerUserId=null) ; si toggle ON appelle autoAssignLead ; si succès → notifyLeadAssigned(assignedBy=null)
    - revalidatePath('/app/leads')
    - return `{ ok: true, leadId, ownerUserId? }`

    `reassignLead(leadId)` :
    - requireRole ADMIN/MANAGER/COMMERCIAL
    - autoAssignLead(leadId, { force: true })
    - Si ok ET ownerUserId → notifyLeadAssigned(assignedBy=user.id) // Pitfall 4 : IGNORE autoAssignLeads toggle
    - revalidatePath('/app/leads', '/app/leads/[id]')
    - return `{ ok: true, ownerUserId, ownerName }` ou `{ ok: false, error }`

    `updateLeadStatus(leadId, newStatus)` :
    - requireRole ADMIN/MANAGER/COMMERCIAL
    - Valide newStatus dans LeadStatus enum
    - Lit lead actuel ; update status + (wonAt=now si newStatus==='WON' ET ancien !== 'WON' ; wonAt=null si newStatus !== 'WON' ET ancien==='WON')
    - AuditLog `leads.status.change` action (diff before/after)
    - revalidatePath('/app/leads', '/app/leads/[id]', '/app/leads/charge')
    - return `{ ok: true }` ou erreurs
  </behavior>
  <action>
    **1. Vérifier `.env.example`** — Si `APP_URL=` est absent, AJOUTER :
    ```
    # URL absolue de l'application (utilisée par les emails — ex. liens "Voir le lead").
    # Sans valeur, les liens dans les emails seront relatifs (utilisable mais moche).
    APP_URL=http://localhost:3002
    ```

    **2. Créer `apps/web/src/server/actions/leads.ts`** (~165 lignes) :

    ```typescript
    'use server';

    import { revalidatePath } from 'next/cache';
    import { prisma } from '@qualiof/db';
    import type { LeadStatus } from '@qualiof/db';
    import { CreateLeadSchema } from '@qualiof/shared';
    import { requireRole, UnauthorizedError, ForbiddenError } from '@/lib/rbac';
    import { autoAssignLead } from './auto-assign-leads';
    import { notifyLeadAssigned } from '@/lib/lead-notifications';
    import { logLeadEvent } from '@/lib/audit-log';

    type ActionResult<T = void> =
      | { ok: true; data?: T }
      | { ok: false; error: string; fieldErrors?: Record<string, string[]> };

    /**
     * LEAD-01 — Création d'un Lead avec auto-assignation conditionnelle.
     *
     * Flow :
     *  1. RBAC ADMIN/MANAGER/COMMERCIAL
     *  2. Validation Zod CreateLeadSchema
     *  3. Lecture toggle tenant.autoAssignLeads
     *  4. prisma.lead.create (ownerUserId=null)
     *  5. Si toggle ON → autoAssignLead(lead.id) puis notifyLeadAssigned(assignedBy=null)
     *
     * Pitfall 1 (race condition) : documenté JSDoc — pas de fix urgent (volume 1-5 leads/jour).
     */
    export async function createLead(input: unknown): Promise<ActionResult<{ leadId: string; ownerUserId: string | null }>> {
      let user;
      try {
        user = await requireRole(['ADMIN', 'MANAGER', 'COMMERCIAL']);
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }

      const parsed = CreateLeadSchema.safeParse(input);
      if (!parsed.success) {
        return { ok: false, error: 'Validation', fieldErrors: parsed.error.flatten().fieldErrors };
      }

      const tenant = await prisma.tenant.findUnique({
        where: { id: user.tenantId },
        select: { autoAssignLeads: true },
      });

      const lead = await prisma.lead.create({
        data: {
          tenantId: user.tenantId,
          ...parsed.data,
        },
        select: { id: true, ownerUserId: true },
      });

      let ownerUserId: string | null = lead.ownerUserId;
      if (tenant?.autoAssignLeads !== false) {
        const r = await autoAssignLead(lead.id);
        if (r.ok && r.ownerUserId) {
          ownerUserId = r.ownerUserId;
          await notifyLeadAssigned({
            tenantId: user.tenantId,
            leadId: lead.id,
            ownerUserId: r.ownerUserId,
            assignedBy: null,   // system
          });
        }
      }

      revalidatePath('/app/leads');
      return { ok: true, data: { leadId: lead.id, ownerUserId } };
    }

    /**
     * LEAD-01 — Réassignation manuelle (bouton "Réassigner" sur fiche Lead).
     *
     * Pitfall 4 : `force: true` IGNORE volontairement le toggle Tenant.autoAssignLeads
     * (l'admin/manager qui clique sait ce qu'il fait — sinon le bouton serait inutile).
     */
    export async function reassignLead(leadId: string): Promise<ActionResult<{ ownerUserId: string; ownerName: string }>> {
      let user;
      try {
        user = await requireRole(['ADMIN', 'MANAGER', 'COMMERCIAL']);
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }

      const r = await autoAssignLead(leadId, { force: true });
      if (!r.ok) {
        return { ok: false, error: r.error ?? r.skippedReason ?? 'Échec réassignation' };
      }
      if (r.ownerUserId) {
        await notifyLeadAssigned({
          tenantId: user.tenantId,
          leadId,
          ownerUserId: r.ownerUserId,
          assignedBy: user.id,  // manuel
        });
      }
      revalidatePath('/app/leads');
      revalidatePath(`/app/leads/${leadId}`);
      return { ok: true, data: { ownerUserId: r.ownerUserId!, ownerName: r.ownerName ?? '' } };
    }

    /**
     * LEAD-02 — Transition de statut, set wonAt automatiquement (Pitfall 3).
     */
    export async function updateLeadStatus(
      leadId: string,
      newStatus: LeadStatus,
    ): Promise<ActionResult> {
      let user;
      try {
        user = await requireRole(['ADMIN', 'MANAGER', 'COMMERCIAL']);
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }

      const existing = await prisma.lead.findFirst({
        where: { id: leadId, tenantId: user.tenantId },
        select: { id: true, status: true, wonAt: true },
      });
      if (!existing) return { ok: false, error: 'Lead introuvable' };

      const wasWon = existing.status === 'WON';
      const becomesWon = newStatus === 'WON';

      const wonAt = becomesWon && !wasWon ? new Date() : !becomesWon && wasWon ? null : existing.wonAt;

      await prisma.lead.update({
        where: { id: leadId },
        data: { status: newStatus, wonAt },
      });

      await logLeadEvent({
        tenantId: user.tenantId,
        actorUserId: user.id,
        targetLeadId: leadId,
        action: 'leads.status.change',
        diff: {
          status: { before: existing.status, after: newStatus },
          wonAt: { before: existing.wonAt, after: wonAt },
        },
      });

      revalidatePath('/app/leads');
      revalidatePath(`/app/leads/${leadId}`);
      revalidatePath('/app/leads/charge');
      return { ok: true };
    }
    ```

    **3. Créer `apps/web/src/server/actions/__tests__/leads.test.ts`** (~150 lignes) — 8 cas :
    - createLead validation : payload sans personId ni firstName → fieldErrors retournés
    - createLead avec tenant.autoAssignLeads=true → autoAssignLead appelé + notifyLeadAssigned avec assignedBy=null
    - createLead avec tenant.autoAssignLeads=false → autoAssignLead PAS appelé + notifyLeadAssigned PAS appelé
    - reassignLead avec lead inexistant → return ok:false
    - reassignLead avec lead OK → autoAssignLead(force:true) + notifyLeadAssigned(assignedBy=user.id)
    - updateLeadStatus NEW → WON : set wonAt = new Date()
    - updateLeadStatus WON → LOST : set wonAt = null
    - updateLeadStatus CONTACTED → QUALIFIED : laisse wonAt inchangé
    - Mock requireRole, prisma.tenant.findUnique, prisma.lead.create/findFirst/update, autoAssignLead, notifyLeadAssigned, logLeadEvent
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/server/actions/__tests__/leads.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function createLead" apps/web/src/server/actions/leads.ts` retourne 1 match.
    - `grep -n "export async function reassignLead" apps/web/src/server/actions/leads.ts` retourne 1 match.
    - `grep -n "export async function updateLeadStatus" apps/web/src/server/actions/leads.ts` retourne 1 match.
    - `grep -n "autoAssignLead(" apps/web/src/server/actions/leads.ts` retourne ≥ 2 matches (createLead + reassignLead).
    - `grep -n "notifyLeadAssigned" apps/web/src/server/actions/leads.ts` retourne ≥ 2 matches.
    - `grep -n "requireRole" apps/web/src/server/actions/leads.ts` retourne ≥ 3 matches (1 par action).
    - `grep -n "'use server'" apps/web/src/server/actions/leads.ts` retourne 1 match.
    - `grep -n "APP_URL" .env.example` retourne ≥ 1 match.
    - `pnpm --filter @qualiof/web test --run src/server/actions/__tests__/leads.test.ts` → 8 tests verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    3 server actions exportées, RBAC appliqué, wiring complet `createLead → autoAssignLead → notifyLeadAssigned`, `reassignLead → autoAssignLead(force) → notifyLeadAssigned(user.id)`, `updateLeadStatus → wonAt logic + AuditLog`. Tests Vitest verts (8). `.env.example` documente `APP_URL`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Server action distribution-leads-config.ts (3 toggles tenant ADMIN-only)</name>
  <files>apps/web/src/server/actions/distribution-leads-config.ts, apps/web/src/server/actions/__tests__/distribution-leads-config.test.ts</files>
  <read_first>
    - apps/web/src/server/actions/tenant-settings.ts (pattern updateTenant* Phase 7 — computeDiff + logTenantSettingsChange)
    - apps/web/src/lib/audit-log.ts (computeDiff + logTenantSettingsChange — convention 'parameters.*')
    - packages/shared/src/schemas/lead.ts (DistributionConfigSchema — Plan 09-01)
  </read_first>
  <behavior>
    `updateLeadDistributionConfig(input)` :
    - requireRole ADMIN-only
    - safeParse DistributionConfigSchema
    - Lit tenant actuel (3 toggles) ; compute diff ; update les 3 toggles
    - AuditLog `leads.distribution_config` (entity='Tenant', entityId=tenantId, userId=admin.id) — pas no-op si diff vide
    - revalidatePath('/app/parametres/distribution-leads', '/app/leads', '/app/leads/charge')
    - return ok/error
  </behavior>
  <action>
    **1. Créer `apps/web/src/server/actions/distribution-leads-config.ts`** (~75 lignes) :

    ```typescript
    'use server';

    import { revalidatePath } from 'next/cache';
    import { prisma } from '@qualiof/db';
    import { DistributionConfigSchema } from '@qualiof/shared';
    import { requireRole, UnauthorizedError, ForbiddenError } from '@/lib/rbac';
    import { computeDiff } from '@/lib/audit-log';

    type ActionResult =
      | { ok: true }
      | { ok: false; error: string; fieldErrors?: Record<string, string[]> };

    /**
     * LEAD-01 / D-06 — Modifie les 3 toggles Tenant gérant la distribution leads.
     * Réservé ADMIN. AuditLog action='leads.distribution_config' (entity='Tenant').
     */
    export async function updateLeadDistributionConfig(input: unknown): Promise<ActionResult> {
      let admin;
      try {
        admin = await requireRole(['ADMIN']);
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }

      const parsed = DistributionConfigSchema.safeParse(input);
      if (!parsed.success) {
        return { ok: false, error: 'Validation', fieldErrors: parsed.error.flatten().fieldErrors };
      }

      const before = await prisma.tenant.findUnique({
        where: { id: admin.tenantId },
        select: {
          autoAssignLeads: true,
          notifyOnLeadAssignEmail: true,
          notifyOnLeadAssignBell: true,
        },
      });
      if (!before) return { ok: false, error: 'Tenant introuvable' };

      const after = parsed.data;
      const diff = computeDiff(before as Record<string, unknown>, after as Record<string, unknown>);

      await prisma.tenant.update({
        where: { id: admin.tenantId },
        data: {
          autoAssignLeads: after.autoAssignLeads,
          notifyOnLeadAssignEmail: after.notifyOnLeadAssignEmail,
          notifyOnLeadAssignBell: after.notifyOnLeadAssignBell,
        },
      });

      // Pas de no-op : on logge même si diff vide (utile pour traçer les "vérifications" admin).
      // Si on veut no-op : remplacer par logTenantSettingsChange (qui no-op sur diff vide).
      await prisma.auditLog.create({
        data: {
          tenantId: admin.tenantId,
          userId: admin.id,
          entity: 'Tenant',
          entityId: admin.tenantId,
          action: 'leads.distribution_config',
          diff: diff as never,
        },
      });

      revalidatePath('/app/parametres/distribution-leads');
      revalidatePath('/app/leads');
      revalidatePath('/app/leads/charge');
      return { ok: true };
    }
    ```

    **2. Créer `apps/web/src/server/actions/__tests__/distribution-leads-config.test.ts`** (~80 lignes) — 4 cas :
    - non-admin (MANAGER) → return ok:false avec error ForbiddenError msg
    - admin avec input invalide → fieldErrors
    - admin avec diff non vide → update tenant + AuditLog row écrit avec action='leads.distribution_config'
    - admin avec diff vide (toggles identiques) → AuditLog quand même écrit (pas no-op) — comportement explicite
    - Mock requireRole, prisma.tenant.findUnique/update, prisma.auditLog.create
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/server/actions/__tests__/distribution-leads-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function updateLeadDistributionConfig" apps/web/src/server/actions/distribution-leads-config.ts` retourne 1 match.
    - `grep -n "requireRole(\['ADMIN'\])" apps/web/src/server/actions/distribution-leads-config.ts` retourne 1 match.
    - `grep -n "leads.distribution_config" apps/web/src/server/actions/distribution-leads-config.ts` retourne 1 match.
    - `grep -n "DistributionConfigSchema" apps/web/src/server/actions/distribution-leads-config.ts` retourne 1 match.
    - `pnpm --filter @qualiof/web test --run src/server/actions/__tests__/distribution-leads-config.test.ts` → 4 tests verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    `updateLeadDistributionConfig` ADMIN-only opérationnel + audit + revalidatePath. Tests verts. Convention `leads.distribution_config` instaurée (consommable par la page Historique Phase 8 — aucun hardcoded enum côté UI Historique d'après STATE.md Plan 08-05).
  </done>
</task>

</tasks>

<verification>
- Tous les tests Vitest (tasks 1-3) verts.
- `tsc --noEmit` clean sur `apps/web`.
- `grep -rn "autoAssignLead(" apps/web/src/server/actions/ | grep -v auto-assign-leads.ts` → 2 matches (createLead, reassignLead).
- `grep -rn "notifyLeadAssigned" apps/web/src/` → ≥ 3 matches (helper + 2 call sites).
- `.env.example` documente APP_URL.
- Aucun import circulaire : `lead-notifications.ts` → mailer/of-config/audit-log ; `leads.ts` → auto-assign-leads/lead-notifications/audit-log/rbac/shared. Aucun de ces fichiers n'importe `leads.ts`.
</verification>

<success_criteria>
- 3 server actions exportées : `createLead`, `reassignLead`, `updateLeadStatus`, `updateLeadDistributionConfig` (dans 2 fichiers).
- Helper `notifyLeadAssigned` consomme les 2 toggles (bell + email) et écrit toujours l'AuditLog.
- Helper `logLeadEvent` ajouté à audit-log.ts pour entity='Lead' avec actorUserId nullable.
- `wonAt` set automatiquement par `updateLeadStatus` à la transition vers WON (Pitfall 3 fix).
- Bouton Réassigner ignore `tenant.autoAssignLeads` (Pitfall 4 décision : assumée).
- `.env.example` documente APP_URL.
- Tests Vitest pour Tasks 1-3 : ≥ 17 tests verts au total.
- tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution-leads-automatique/09-02-SUMMARY.md` with:
- 4 server actions créées (signatures + RBAC roles)
- Helper notifyLeadAssigned + ordre des side-effects + comportement des 2 toggles
- Helper logLeadEvent (extension audit-log.ts)
- APP_URL ajouté/présent dans .env.example
- Tests créés + count (cible ≥ 17)
- Décisions appliquées (Pitfall 3, 4, 5 = option b)
</output>
