---
phase: 09-distribution-leads-automatique
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - apps/web/src/app/app/parametres/distribution-leads/page.tsx
  - apps/web/src/app/app/parametres/distribution-leads/__tests__/page.smoke.test.ts
  - apps/web/src/components/parametres/distribution-leads-form.tsx
  - apps/web/src/server/actions/notifications.ts
  - apps/web/src/server/actions/__tests__/notifications.test.ts
  - apps/web/src/server/actions/notification-mark-read.ts
  - apps/web/src/components/layout/notifications-bell.tsx
  - apps/web/src/components/layout/nav-config.ts
  - apps/web/src/components/layout/__tests__/nav-config.test.ts
  - apps/web/src/app/app/leads/page.tsx
autonomous: true
requirements: [LEAD-01]

must_haves:
  truths:
    - "Page `/app/parametres/distribution-leads` ADMIN-only avec 3 toggles (auto-assign, email, bell) qui appellent updateLeadDistributionConfig"
    - "Sidebar contient une entrée 'Vue de charge' sous Leads (allowedRoles=ADMIN+MANAGER) et 'Distribution leads' sous Configuration (allowedRoles=ADMIN)"
    - "getNotifications() lit aussi les rows Notification non-lues type='lead.assigned' pour user-scoped (parsed via LeadAssignedPayloadSchema)"
    - "NotificationsBell rend les items 'lead.assigned' avec icône UserPlus et marque la notif comme lue au clic (server action markNotificationRead)"
    - "Page list /app/leads contient un bouton 'Nouveau lead' linké vers /app/leads/new"
  artifacts:
    - path: "apps/web/src/app/app/parametres/distribution-leads/page.tsx"
      provides: "Page config ADMIN 3 toggles tenant"
      min_lines: 40
    - path: "apps/web/src/components/parametres/distribution-leads-form.tsx"
      provides: "Form RHF + 3 toggles + submit updateLeadDistributionConfig"
      exports: ["DistributionLeadsForm"]
    - path: "apps/web/src/server/actions/notifications.ts"
      provides: "Extension getNotifications avec union dérivés + persistées 'lead.assigned'"
      exports: ["getNotifications", "NotificationKind", "NotificationItem"]
    - path: "apps/web/src/server/actions/notification-mark-read.ts"
      provides: "Server action markNotificationRead (clic item cloche)"
      exports: ["markNotificationRead"]
    - path: "apps/web/src/components/layout/nav-config.ts"
      provides: "NAV étendu avec Vue de charge + Distribution leads"
      contains: "/app/leads/charge"
  key_links:
    - from: "app/app/parametres/distribution-leads/page.tsx"
      to: "updateLeadDistributionConfig (Plan 09-02)"
      via: "DistributionLeadsForm client → useTransition"
      pattern: "updateLeadDistributionConfig\\("
    - from: "components/layout/notifications-bell.tsx"
      to: "markNotificationRead server action"
      via: "onClick handler avant Link navigation"
      pattern: "markNotificationRead\\("
    - from: "server/actions/notifications.ts"
      to: "prisma.notification.findMany scoped user.id + user.tenantId + readAt: null"
      via: "Promise.all parallel avec les 4 derived counts"
      pattern: "prisma\\.notification\\.findMany"
---

<objective>
Plan 09-04 — Cloche + page config tenant. Branche les 3 toggles ADMIN + étend la cloche TopBar Phase 4 pour rendre les notifications événementielles 'lead.assigned' (NEW model Phase 9-01), avec marquage comme lue au clic. Ajoute aussi les 2 entrées sidebar manquantes.

Purpose: Fermer la boucle UX :
1. ADMIN peut activer/désactiver l'auto-distribution et les 2 notifications via une page dédiée.
2. Quand un lead est assigné à un commercial → le commercial voit l'event en cloche, clique → la notif est marquée lue → navigation vers /app/leads/[id].
3. Les nouvelles routes sont accessibles via la sidebar.

Output: 1 page paramètres + 1 form + 1 server action `markNotificationRead` + extension `getNotifications` + extension `notifications-bell.tsx` + 2 entrées nav-config + bouton "Nouveau lead" sur la page list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-distribution-leads-automatique/09-CONTEXT.md
@.planning/phases/09-distribution-leads-automatique/09-RESEARCH.md
@.planning/phases/09-distribution-leads-automatique/09-01-SUMMARY.md
@.planning/phases/09-distribution-leads-automatique/09-02-SUMMARY.md
@.planning/phases/09-distribution-leads-automatique/09-03-SUMMARY.md

@apps/web/src/server/actions/notifications.ts
@apps/web/src/components/layout/notifications-bell.tsx
@apps/web/src/components/layout/nav-config.ts
@apps/web/src/app/app/leads/page.tsx
@apps/web/src/components/layout/__tests__/nav-config.test.ts
@apps/web/src/app/app/parametres/utilisateurs/page.tsx

<interfaces>
<!-- Contrats existants (Phase 4/8) + Plans 09-01/02/03. -->

From apps/web/src/server/actions/notifications.ts (Phase 4, lignes 15-23):
```typescript
export type NotificationKind = 'preinscription' | 'session_no_attendee' | 'session_to_close' | 'cleanup';
// ⚠ DOIT être étendu : ajouter 'lead.assigned'

export interface NotificationItem {
  kind: NotificationKind;
  label: string;
  href: string;
  count: number;
  severity: 'info' | 'warning' | 'danger';
}
// ⚠ Étendre avec id?: string (Notification.id) pour markNotificationRead
```

From apps/web/src/components/layout/notifications-bell.tsx (Phase 4 lignes 10-15):
```typescript
const ICONS: Record<NotificationKind, React.ComponentType<{ className?: string }>> = {
  preinscription: Inbox,
  session_no_attendee: AlertTriangle,
  session_to_close: AlertCircle,
  cleanup: Users,
};
// ⚠ DOIT être étendu : ajouter `'lead.assigned': UserPlus` ou icône similaire
```

From apps/web/src/components/layout/nav-config.ts (Phase 8):
```typescript
// Structure 3 sections : 'Essentiel', 'Suivi', 'Configuration'.
// 'Suivi' contient déjà Leads (allowedRoles=['ADMIN','MANAGER','COMMERCIAL'])
// 'Configuration' contient déjà Paramètres + Utilisateurs + Historique (all ADMIN)
// Icônes déjà importées : Users, Megaphone, Settings, etc.
// Icônes à AJOUTER à l'import : TrendingUp (charge), Sliders (distribution leads)
```

From packages/shared/src/schemas/lead.ts (Plan 09-01):
```typescript
export const LeadAssignedPayloadSchema: ZodSchema; // { leadId, prospectName, source }
export type LeadAssignedPayload;
```

From apps/web/src/server/actions/distribution-leads-config.ts (Plan 09-02):
```typescript
export async function updateLeadDistributionConfig(input: unknown): Promise<ActionResult>;
```

From apps/web/src/lib/rbac.ts:
```typescript
export async function requireRole(allowed: UserRole[]): Promise<LuciaUser>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extension notifications.ts (lecture Notification rows persistées) + markNotificationRead</name>
  <files>apps/web/src/server/actions/notifications.ts, apps/web/src/server/actions/__tests__/notifications.test.ts, apps/web/src/server/actions/notification-mark-read.ts</files>
  <read_first>
    - apps/web/src/server/actions/notifications.ts (intégral — 102 lignes — extension non-breaking)
    - packages/shared/src/schemas/lead.ts (LeadAssignedPayloadSchema)
    - apps/web/src/lib/audit-log.ts (pattern logUserAction si besoin marquer "user a lu")
  </read_first>
  <behavior>
    Extension `getNotifications` :
    - `NotificationKind` étendu : `'preinscription' | 'session_no_attendee' | 'session_to_close' | 'cleanup' | 'lead.assigned'`
    - `NotificationItem` étendu avec `id?: string` (id de Notification row si persistée, undefined sinon)
    - Ajoute une 5e source : `prisma.notification.findMany({ where: { tenantId, userId, readAt: null, type: 'lead.assigned' }, orderBy: createdAt desc, take: 10 })`
    - Parse payload via `LeadAssignedPayloadSchema.safeParse` ; skip si parse échoue (Pitfall 6)
    - Pour chaque notif persistée : push 1 NotificationItem par row avec `id: notif.id`, `kind: 'lead.assigned'`, `label: "Nouveau lead à traiter : {prospectName}"`, `href: /app/leads/{leadId}`, `count: 1`, `severity: 'info'`
    - total = somme des counts (inchangé pour les 4 kinds dérivés + 1 par notif persistée)

    `markNotificationRead(notifId)` :
    - validateRequest (pas de requireRole — n'importe quel user authentifié peut marquer SES notifs)
    - prisma.notification.updateMany({ where: { id, userId: user.id, readAt: null }, data: { readAt: new Date() } }) — single-use atomique
    - return `{ ok: true }`
    - Pas de revalidatePath ici (la cloche refetch toutes les 60s via polling, et le clic redirige immédiatement)
  </behavior>
  <action>
    **1. Étendre `apps/web/src/server/actions/notifications.ts`** :

    - Ligne 15 : étendre l'union :
    ```typescript
    export type NotificationKind = 'preinscription' | 'session_no_attendee' | 'session_to_close' | 'cleanup' | 'lead.assigned';
    ```

    - Ligne 17-23 : étendre l'interface :
    ```typescript
    export interface NotificationItem {
      kind: NotificationKind;
      label: string;
      href: string;
      count: number;
      severity: 'info' | 'warning' | 'danger';
      /** Présent uniquement pour les notifs persistées (table Notification). Permet markNotificationRead. */
      id?: string;
    }
    ```

    - Étendre `getNotifications` : ajouter dans le `Promise.all` un 5e fetch :
    ```typescript
    const [preinscriptionsToValidate, sessionsNoAttendee, sessionsToClose, cleanupCount, leadAssignedNotifs] = await Promise.all([
      // ... 4 existants ...
      prisma.notification.findMany({
        where: { tenantId: user.tenantId, userId: user.id, readAt: null, type: 'lead.assigned' },
        select: { id: true, payload: true, createdAt: true },
        orderBy: { createdAt: 'desc' },
        take: 10,
      }),
    ]);
    ```

    - Après les 4 push existants (avant `const total = items.reduce(...)`), AJOUTER :
    ```typescript
    // Phase 9 — Notification rows persistées (type 'lead.assigned')
    for (const notif of leadAssignedNotifs) {
      const parsed = LeadAssignedPayloadSchema.safeParse(notif.payload);
      if (!parsed.success) continue;  // Pitfall 6 — skip silencieux si payload corrompu
      items.push({
        kind: 'lead.assigned',
        id: notif.id,
        label: `Nouveau lead à traiter : ${parsed.data.prospectName}`,
        href: `/app/leads/${parsed.data.leadId}`,
        count: 1,
        severity: 'info',
      });
    }
    ```

    - AJOUTER l'import en haut :
    ```typescript
    import { LeadAssignedPayloadSchema } from '@qualiof/shared';
    ```

    **2. Créer `apps/web/src/server/actions/notification-mark-read.ts`** (~35 lignes) :

    ```typescript
    'use server';

    import { prisma } from '@qualiof/db';
    import { validateRequest } from '@/lib/auth';

    type ActionResult = { ok: true } | { ok: false; error: string };

    /**
     * Phase 9 — Marque une Notification row comme lue.
     * Appelé par notifications-bell.tsx au clic sur un item persistant (kind='lead.assigned').
     *
     * Sécurité : single-use atomique scope userId — un user ne peut marquer que SES notifs.
     */
    export async function markNotificationRead(notifId: string): Promise<ActionResult> {
      const { user } = await validateRequest();
      if (!user) return { ok: false, error: 'Non authentifié' };

      const r = await prisma.notification.updateMany({
        where: { id: notifId, userId: user.id, readAt: null },
        data: { readAt: new Date() },
      });

      if (r.count === 0) {
        // Soit notif inexistante, soit déjà lue, soit pas la sienne — silencieux côté UI.
        return { ok: true };
      }
      return { ok: true };
    }
    ```

    **3. Créer `apps/web/src/server/actions/__tests__/notifications.test.ts`** (~120 lignes) — 6 cas :
    - Tenant sans notifs persistées → items contient uniquement les 4 kinds dérivés (existant non régressé)
    - User avec 2 notifs `lead.assigned` non-lues → items contient 2 entrées kind='lead.assigned' + id défini
    - Notif payload corrompu (manque leadId) → skip silencieux (pas d'erreur, pas d'item)
    - Notif déjà lue (readAt != null) → exclue du résultat (filter readAt: null)
    - Notif d'un autre user → exclue (filter userId)
    - markNotificationRead : user marque sa notif → updateMany retourne count=1, ok:true
    - markNotificationRead : user essaie de marquer notif d'un autre user → updateMany retourne count=0, ok:true (silencieux)
    - Mock prisma.notification.findMany/updateMany + prisma.preEnrollment.count + prisma.trainingSession.count + prisma.person.count + validateRequest
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/server/actions/__tests__/notifications.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lead.assigned" apps/web/src/server/actions/notifications.ts` retourne ≥ 2 matches (type union + push).
    - `grep -n "LeadAssignedPayloadSchema" apps/web/src/server/actions/notifications.ts` retourne ≥ 1 match.
    - `grep -n "prisma.notification.findMany" apps/web/src/server/actions/notifications.ts` retourne 1 match.
    - `grep -n "id\?:" apps/web/src/server/actions/notifications.ts` retourne ≥ 1 match (interface étendue).
    - `grep -n "export async function markNotificationRead" apps/web/src/server/actions/notification-mark-read.ts` retourne 1 match.
    - `grep -n "userId: user.id, readAt: null" apps/web/src/server/actions/notification-mark-read.ts` retourne 1 match.
    - `pnpm --filter @qualiof/web test --run src/server/actions/__tests__/notifications.test.ts` → ≥ 6 tests verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    `getNotifications` hybride dérivés (4 kinds tenant-wide) + persisté (1 kind 'lead.assigned' user-scoped) — Pitfall 2 résolu (filter userId par user.id pour les persistées). `markNotificationRead` opérationnelle. Le pattern reste compatible avec Phase 4 (interface NotificationItem rétro-compatible — `id?: string` optionnel).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extension notifications-bell.tsx + nav-config.ts + page list bouton "Nouveau lead"</name>
  <files>apps/web/src/components/layout/notifications-bell.tsx, apps/web/src/components/layout/nav-config.ts, apps/web/src/components/layout/__tests__/nav-config.test.ts, apps/web/src/app/app/leads/page.tsx</files>
  <read_first>
    - apps/web/src/components/layout/notifications-bell.tsx (intégral — 113 lignes)
    - apps/web/src/components/layout/nav-config.ts (intégral — 188 lignes)
    - apps/web/src/components/layout/__tests__/nav-config.test.ts (étendre les assertions)
    - apps/web/src/app/app/leads/page.tsx (chercher header bouton "Auto-assigner" → ajouter à côté)
  </read_first>
  <action>
    **1. Étendre `apps/web/src/components/layout/notifications-bell.tsx`** :

    - Import : ajouter `UserPlus` à l'import lucide-react existant ligne 6 :
    ```typescript
    import { Bell, Inbox, AlertTriangle, AlertCircle, Users, ChevronRight, UserPlus } from 'lucide-react';
    ```

    - Import en plus :
    ```typescript
    import { markNotificationRead } from '@/server/actions/notification-mark-read';
    ```

    - Ligne 10-15 : étendre le mapping ICONS :
    ```typescript
    const ICONS: Record<NotificationKind, React.ComponentType<{ className?: string }>> = {
      preinscription: Inbox,
      session_no_attendee: AlertTriangle,
      session_to_close: AlertCircle,
      cleanup: Users,
      'lead.assigned': UserPlus,
    };
    ```

    - Dans le rendu (ligne ~85-105) du `<Link>` : ajouter l'appel `markNotificationRead` AVANT `setOpen(false)` pour les items avec `item.id` :
    ```tsx
    <Link
      href={item.href as any}
      onClick={() => {
        if (item.id) {
          // Fire-and-forget : on ne bloque pas la navigation sur la mise à jour DB.
          void markNotificationRead(item.id);
        }
        setOpen(false);
      }}
      className="..."
    >
    ```

    **2. Étendre `apps/web/src/components/layout/nav-config.ts`** :

    - Ajouter à l'import lucide-react ligne 1-19 : `TrendingUp` et `Sliders` :
    ```typescript
    import {
      Users, Building2, GraduationCap, BookOpen, FileText, Calendar, Receipt, Megaphone,
      Settings, LayoutDashboard, ListChecks, Landmark, ClipboardCheck, Inbox, Wallet, History, UserCog,
      TrendingUp, Sliders,
    } from 'lucide-react';
    ```

    - Dans la section 'Suivi' (lignes 78-109), JUSTE APRÈS l'item Leads existant (ligne 102-107), AJOUTER :
    ```typescript
      {
        label: 'Vue de charge',
        href: '/app/leads/charge',
        icon: TrendingUp,
        allowedRoles: ['ADMIN', 'MANAGER'],
      },
    ```

    - Dans la section 'Configuration' (lignes 110-157), JUSTE APRÈS l'item Paramètres (ligne 136-141) et AVANT Utilisateurs, AJOUTER :
    ```typescript
      {
        label: 'Distribution leads',
        href: '/app/parametres/distribution-leads',
        icon: Sliders,
        allowedRoles: ['ADMIN'],
      },
    ```

    **3. Étendre `apps/web/src/components/layout/__tests__/nav-config.test.ts`** — Ajouter ces assertions à la suite existante :

    ```typescript
    describe('filterNavForRole — Phase 9 ajouts', () => {
      it('Vue de charge visible pour ADMIN', () => {
        const nav = filterNavForRole(NAV, 'ADMIN');
        const allItems = nav.flatMap((s) => s.items);
        expect(allItems.some((i) => i.href === '/app/leads/charge')).toBe(true);
      });
      it('Vue de charge visible pour MANAGER', () => {
        const nav = filterNavForRole(NAV, 'MANAGER');
        const allItems = nav.flatMap((s) => s.items);
        expect(allItems.some((i) => i.href === '/app/leads/charge')).toBe(true);
      });
      it('Vue de charge MASQUÉE pour COMMERCIAL', () => {
        const nav = filterNavForRole(NAV, 'COMMERCIAL');
        const allItems = nav.flatMap((s) => s.items);
        expect(allItems.some((i) => i.href === '/app/leads/charge')).toBe(false);
      });
      it('Distribution leads visible UNIQUEMENT pour ADMIN', () => {
        const adminItems = filterNavForRole(NAV, 'ADMIN').flatMap((s) => s.items);
        const managerItems = filterNavForRole(NAV, 'MANAGER').flatMap((s) => s.items);
        expect(adminItems.some((i) => i.href === '/app/parametres/distribution-leads')).toBe(true);
        expect(managerItems.some((i) => i.href === '/app/parametres/distribution-leads')).toBe(false);
      });
    });
    ```

    **4. Étendre `apps/web/src/app/app/leads/page.tsx`** :

    Chercher la section header (à proximité du bouton "Auto-assigner" si présent) et AJOUTER un bouton "Nouveau lead" qui linke vers `/app/leads/new` :

    ```tsx
    import Link from 'next/link';
    import { Plus } from 'lucide-react';

    // ... dans le rendu existant, à côté du bouton AutoAssignButton :
    <Link
      href="/app/leads/new"
      className="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-primary text-primary-foreground hover:bg-primary/90 text-sm"
    >
      <Plus className="h-4 w-4" />
      Nouveau lead
    </Link>
    ```

    Lire le fichier existant et placer le bouton dans le bloc des actions de header (probablement à côté de `<AutoAssignButton />`).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/components/layout/__tests__/nav-config.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "UserPlus" apps/web/src/components/layout/notifications-bell.tsx` retourne ≥ 2 matches (import + mapping ICONS).
    - `grep -n "'lead.assigned': UserPlus" apps/web/src/components/layout/notifications-bell.tsx` retourne 1 match.
    - `grep -n "markNotificationRead" apps/web/src/components/layout/notifications-bell.tsx` retourne ≥ 2 matches (import + call).
    - `grep -n "Vue de charge" apps/web/src/components/layout/nav-config.ts` retourne 1 match.
    - `grep -n "Distribution leads" apps/web/src/components/layout/nav-config.ts` retourne 1 match.
    - `grep -n "/app/leads/charge" apps/web/src/components/layout/nav-config.ts` retourne 1 match.
    - `grep -n "/app/parametres/distribution-leads" apps/web/src/components/layout/nav-config.ts` retourne 1 match.
    - `grep -n "TrendingUp\|Sliders" apps/web/src/components/layout/nav-config.ts` retourne ≥ 2 matches.
    - `grep -n "Nouveau lead\|/app/leads/new" apps/web/src/app/app/leads/page.tsx` retourne ≥ 1 match.
    - `pnpm --filter @qualiof/web test --run src/components/layout/__tests__/nav-config.test.ts` → tests Phase 9 verts (≥ 4 nouveaux tests verts) + tests existants Phase 8 toujours verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    Cloche TopBar étendue avec `lead.assigned` (icône UserPlus + markNotificationRead au clic, Open Question #3 résolue par "lue au clic"). Sidebar contient 'Vue de charge' (ADMIN+MANAGER) + 'Distribution leads' (ADMIN). Page liste leads accède à `/app/leads/new`. tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Page /app/parametres/distribution-leads (ADMIN, 3 toggles)</name>
  <files>apps/web/src/app/app/parametres/distribution-leads/page.tsx, apps/web/src/app/app/parametres/distribution-leads/__tests__/page.smoke.test.ts, apps/web/src/components/parametres/distribution-leads-form.tsx</files>
  <read_first>
    - apps/web/src/app/app/parametres/utilisateurs/page.tsx (pattern Server Component ADMIN-only Phase 8)
    - apps/web/src/app/app/parametres/page.tsx (page racine paramètres — pour potentiellement linker vers distribution-leads)
    - apps/web/src/components/parametres/ (dossier existant ou à créer — vérifier)
    - apps/web/src/server/actions/distribution-leads-config.ts (Plan 09-02 — updateLeadDistributionConfig)
  </read_first>
  <behavior>
    Page Server Component :
    - validateRequest + hasRole(['ADMIN']) → sinon redirect('/app')
    - Fetch tenant 3 toggles (autoAssignLeads, notifyOnLeadAssignEmail, notifyOnLeadAssignBell)
    - Header + breadcrumb
    - Rend `<DistributionLeadsForm initial={...} />`

    DistributionLeadsForm (client) :
    - 3 toggles (Switch ou checkbox stylé)
    - Bouton "Enregistrer" → useTransition → updateLeadDistributionConfig(values) → toast
    - Toggles affichent label + description courte (3 lignes max)

    Smoke test :
    - Render sans crash avec user ADMIN mocké
    - Vérifie présence DistributionLeadsForm dans le fichier page
  </behavior>
  <action>
    **1. Créer `apps/web/src/app/app/parametres/distribution-leads/page.tsx`** (~70 lignes) :

    ```tsx
    import { redirect } from 'next/navigation';
    import { validateRequest } from '@/lib/auth';
    import { hasRole } from '@/lib/rbac';
    import { prisma } from '@qualiof/db';
    import { Breadcrumb } from '@/components/ui/breadcrumb';
    import { DistributionLeadsForm } from '@/components/parametres/distribution-leads-form';

    export const dynamic = 'force-dynamic';

    export default async function DistributionLeadsConfigPage() {
      const { user } = await validateRequest();
      if (!user) redirect('/login');
      if (!hasRole(user, ['ADMIN'])) redirect('/app');

      const tenant = await prisma.tenant.findUnique({
        where: { id: user.tenantId },
        select: {
          autoAssignLeads: true,
          notifyOnLeadAssignEmail: true,
          notifyOnLeadAssignBell: true,
        },
      });

      if (!tenant) redirect('/app');

      return (
        <div className="space-y-6">
          <Breadcrumb
            items={[
              { label: 'Paramètres', href: '/app/parametres' },
              { label: 'Distribution leads' },
            ]}
          />
          <header>
            <h1 className="text-2xl font-semibold">Distribution des leads</h1>
            <p className="text-sm text-muted-foreground mt-1">
              Paramètres de l'auto-assignation Lead → Commercial et des notifications associées.
            </p>
          </header>
          <DistributionLeadsForm
            initial={{
              autoAssignLeads: tenant.autoAssignLeads,
              notifyOnLeadAssignEmail: tenant.notifyOnLeadAssignEmail,
              notifyOnLeadAssignBell: tenant.notifyOnLeadAssignBell,
            }}
          />
        </div>
      );
    }
    ```

    **2. Créer `apps/web/src/components/parametres/distribution-leads-form.tsx`** (~110 lignes) — client RHF :

    ```tsx
    'use client';

    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { useTransition } from 'react';
    import { toast } from 'sonner';
    import { DistributionConfigSchema, type DistributionConfigInput } from '@qualiof/shared';
    import { updateLeadDistributionConfig } from '@/server/actions/distribution-leads-config';

    interface Props {
      initial: DistributionConfigInput;
    }

    export function DistributionLeadsForm({ initial }: Props) {
      const [pending, startTransition] = useTransition();
      const { register, handleSubmit, watch } = useForm<DistributionConfigInput>({
        resolver: zodResolver(DistributionConfigSchema),
        defaultValues: initial,
      });

      function onSubmit(data: DistributionConfigInput) {
        startTransition(async () => {
          const r = await updateLeadDistributionConfig(data);
          if (r.ok) {
            toast.success('Paramètres mis à jour');
          } else {
            toast.error(r.error);
          }
        });
      }

      const auto = watch('autoAssignLeads');

      return (
        <form onSubmit={handleSubmit(onSubmit)} className="space-y-6 max-w-2xl">
          <ToggleRow
            label="Auto-assignation des leads"
            description="Quand activée, chaque nouveau lead est automatiquement attribué au commercial le moins chargé (algorithme round-robin équilibré)."
            register={register('autoAssignLeads')}
          />

          <div className={`pl-4 border-l-2 ${auto ? 'border-primary/40' : 'border-border'} space-y-6`}>
            <ToggleRow
              label="Notification email à l'assignation"
              description="Envoie un email au commercial à chaque attribution (utilise le SMTP configuré dans Paramètres)."
              register={register('notifyOnLeadAssignEmail')}
              disabled={!auto}
            />
            <ToggleRow
              label="Notification cloche à l'assignation"
              description="Affiche une notification dans la cloche TopBar du commercial dès l'attribution."
              register={register('notifyOnLeadAssignBell')}
              disabled={!auto}
            />
          </div>

          <div className="flex justify-end pt-4 border-t border-border">
            <button
              type="submit"
              disabled={pending}
              className="px-4 py-2 rounded-md bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50"
            >
              {pending ? 'Enregistrement...' : 'Enregistrer'}
            </button>
          </div>
        </form>
      );
    }

    function ToggleRow({
      label, description, register, disabled,
    }: {
      label: string;
      description: string;
      register: ReturnType<ReturnType<typeof useForm<DistributionConfigInput>>['register']>;
      disabled?: boolean;
    }) {
      return (
        <label className={`flex items-start gap-3 ${disabled ? 'opacity-60' : ''}`}>
          <input
            type="checkbox"
            {...register}
            disabled={disabled}
            className="mt-1 h-4 w-4 rounded border-border text-primary"
          />
          <div className="flex-1">
            <div className="text-sm font-medium">{label}</div>
            <div className="text-xs text-muted-foreground mt-0.5">{description}</div>
          </div>
        </label>
      );
    }
    ```

    Note : si `apps/web/src/components/parametres/` n'existe pas, créer le dossier (déjà utilisé Phase 7 pour les forms tenant settings → vérifier `ls apps/web/src/components/parametres/`).

    **3. Créer `apps/web/src/app/app/parametres/distribution-leads/__tests__/page.smoke.test.ts`** (~50 lignes) :
    - Import du module page sans erreur runtime (smoke pattern Phase 8 `utilisateurs/__tests__/`)
    - Vérifie présence des helpers requireRole/hasRole et import DistributionLeadsForm
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/app/app/parametres/distribution-leads/__tests__/page.smoke.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/app/app/parametres/distribution-leads/page.tsx` retourne 0.
    - `test -f apps/web/src/components/parametres/distribution-leads-form.tsx` retourne 0.
    - `grep -n "hasRole(user, \['ADMIN'\])" apps/web/src/app/app/parametres/distribution-leads/page.tsx` retourne 1 match.
    - `grep -n "DistributionLeadsForm" apps/web/src/app/app/parametres/distribution-leads/page.tsx` retourne ≥ 2 matches (import + JSX).
    - `grep -n "updateLeadDistributionConfig" apps/web/src/components/parametres/distribution-leads-form.tsx` retourne 1 match.
    - `grep -n "autoAssignLeads\|notifyOnLeadAssignEmail\|notifyOnLeadAssignBell" apps/web/src/components/parametres/distribution-leads-form.tsx` retourne ≥ 3 matches.
    - `pnpm --filter @qualiof/web test --run src/app/app/parametres/distribution-leads/__tests__/page.smoke.test.ts` → ≥ 2 tests verts.
    - `pnpm --filter @qualiof/web exec next build` → page `/app/parametres/distribution-leads` compile.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    Page paramètres ADMIN-only avec 3 toggles fonctionnels (RHF + zodResolver + useTransition). Wiring complet : form → updateLeadDistributionConfig → AuditLog 'leads.distribution_config' → revalidate /app/leads/charge.
  </done>
</task>

</tasks>

<verification>
- Tous les tests Vitest (tasks 1-3) verts.
- `tsc --noEmit` clean.
- `pnpm --filter @qualiof/web build` → 4 nouvelles routes Phase 9 compilent (`/app/leads/charge`, `/app/leads/[id]`, `/app/leads/new`, `/app/parametres/distribution-leads`).
- Cloche TopBar gère 5 kinds (4 dérivés + 1 persisté).
- Sidebar à 3 nouvelles entrées (Vue de charge / Distribution leads) visibles selon allowedRoles.
- `getNotifications` retourne hybride dérivé + persisté sans double comptage (Pitfall 2 résolu).
</verification>

<success_criteria>
- 1 page paramètres + 1 form + 1 server action `markNotificationRead` + extension `getNotifications` + extension `notifications-bell` + 2 entrées sidebar + 1 bouton "Nouveau lead" sur page list.
- 3 toggles tenant déclenchent updateLeadDistributionConfig → AuditLog 'leads.distribution_config'.
- Click sur item cloche `lead.assigned` → markNotificationRead + redirect → notif disparaît au prochain poll 60s.
- Sidebar filtrée correctement par filterNavForRole (4 tests Phase 9 verts).
- Build clean, tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution-leads-automatique/09-04-SUMMARY.md` with:
- Extensions notifications.ts (interface + 5e fetch + parsing payload)
- Décision Open Question #3 : "lue au clic" via markNotificationRead fire-and-forget
- Page distribution-leads + form (3 toggles + UX disabled si auto OFF)
- Sidebar entries ajoutées + nav-config.test.ts étendu
- Tests créés + count
</output>
