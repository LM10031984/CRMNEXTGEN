---
phase: 09-distribution-leads-automatique
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - apps/web/src/app/app/leads/charge/page.tsx
  - apps/web/src/app/app/leads/charge/__tests__/page.smoke.test.ts
  - apps/web/src/app/app/leads/[id]/page.tsx
  - apps/web/src/app/app/leads/[id]/__tests__/page.smoke.test.ts
  - apps/web/src/app/app/leads/new/page.tsx
  - apps/web/src/components/leads/lead-distribution-pie.tsx
  - apps/web/src/components/leads/__tests__/lead-distribution-pie.test.tsx
  - apps/web/src/components/leads/lead-load-table.tsx
  - apps/web/src/components/leads/reassign-lead-button.tsx
  - apps/web/src/components/leads/lead-create-form.tsx
  - apps/web/src/components/leads/lead-status-select.tsx
autonomous: true
requirements: [LEAD-01, LEAD-02]

must_haves:
  truths:
    - "Page `/app/leads/charge` est accessible ADMIN+MANAGER, affiche 4 PrioCard KPI globaux + tableau commercial × KPI + camembert SVG inline (répartition leads actifs)"
    - "Page `/app/leads/[id]` affiche détails Lead, badge owner, bouton Réassigner (AlertDialog → reassignLead), select statut (→ updateLeadStatus avec set wonAt si WON)"
    - "Page `/app/leads/new` affiche form RHF zodResolver(CreateLeadSchema) → submit appelle createLead → redirect vers /app/leads"
    - "Composant `LeadDistributionPie` rend `<svg>` avec N paths colorés + légende textuelle ; `total === 0` rend message 'Aucun lead actif'"
    - "Aucune dépendance chart externe ajoutée (pas de recharts/chart.js)"
  artifacts:
    - path: "apps/web/src/app/app/leads/charge/page.tsx"
      provides: "Vue de charge ADMIN+MANAGER avec 4 KPI globaux + table + camembert"
      min_lines: 80
    - path: "apps/web/src/app/app/leads/[id]/page.tsx"
      provides: "Fiche Lead avec actions (Réassigner + transition statut)"
      min_lines: 90
    - path: "apps/web/src/app/app/leads/new/page.tsx"
      provides: "Form création Lead (RHF + zodResolver)"
      min_lines: 50
    - path: "apps/web/src/components/leads/lead-distribution-pie.tsx"
      provides: "Camembert SVG inline pur"
      exports: ["LeadDistributionPie", "Slice"]
    - path: "apps/web/src/components/leads/lead-load-table.tsx"
      provides: "Tableau commercial × 4 KPI"
      exports: ["LeadLoadTable"]
    - path: "apps/web/src/components/leads/reassign-lead-button.tsx"
      provides: "Bouton client Réassigner avec AlertDialog Radix"
      exports: ["ReassignLeadButton"]
    - path: "apps/web/src/components/leads/lead-create-form.tsx"
      provides: "Form création client (RHF)"
      exports: ["LeadCreateForm"]
    - path: "apps/web/src/components/leads/lead-status-select.tsx"
      provides: "Select inline transition statut → updateLeadStatus"
      exports: ["LeadStatusSelect"]
  key_links:
    - from: "app/app/leads/charge/page.tsx"
      to: "getCommercialsWithKpis(tenantId) (Plan 09-01)"
      via: "Server Component fetch"
      pattern: "getCommercialsWithKpis\\("
    - from: "app/app/leads/[id]/page.tsx"
      to: "ReassignLeadButton (client) → reassignLead server action"
      via: "form action transition"
      pattern: "reassignLead\\("
    - from: "components/leads/lead-create-form.tsx"
      to: "createLead server action + CreateLeadSchema"
      via: "useForm + zodResolver"
      pattern: "createLead\\("
    - from: "components/leads/lead-status-select.tsx"
      to: "updateLeadStatus server action"
      via: "onChange → useTransition"
      pattern: "updateLeadStatus\\("
---

<objective>
Plan 09-03 — UI pages métier. Implémente les 3 routes Lead manquantes + 5 composants client/serveur :
1. `/app/leads/charge` (LEAD-02) — vue de charge 4 KPI globaux + table + camembert
2. `/app/leads/[id]` (LEAD-01) — fiche détail + bouton Réassigner + select statut
3. `/app/leads/new` (LEAD-01) — form création RHF + zodResolver

Purpose: Sans ce plan, les server actions de plan 09-02 n'ont aucun consumer UI et les KPI 2/3/4 de la page charge restent vides faute de pouvoir transitionner un statut.
Output: 3 pages + 5 composants (3 client + 2 server), 3 fichiers tests smoke/unit, 0 dépendance externe ajoutée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-distribution-leads-automatique/09-CONTEXT.md
@.planning/phases/09-distribution-leads-automatique/09-RESEARCH.md
@.planning/phases/09-distribution-leads-automatique/09-01-SUMMARY.md
@.planning/phases/09-distribution-leads-automatique/09-02-SUMMARY.md

@apps/web/src/app/app/leads/page.tsx
@apps/web/src/app/page.tsx
@apps/web/src/lib/lead-load-stats.ts
@apps/web/src/server/actions/leads.ts
@apps/web/src/components/ui/breadcrumb.tsx
@apps/web/src/components/forms/delete-entity-button.tsx
@apps/web/src/components/leads/auto-assign-button.tsx

<interfaces>
<!-- Contrats Plans 09-01 + 09-02 disponibles. -->

From apps/web/src/lib/lead-load-stats.ts (Plan 09-01):
```typescript
export interface CommercialKpis {
  userId: string;
  name: string;
  kpis: {
    leadsActifs: number;
    leadsWonThisMonth: number;
    conversionPct: number;
    avgDaysToWin: number | null;
  };
}
export async function getCommercialsWithKpis(tenantId: string): Promise<CommercialKpis[]>;
```

From apps/web/src/server/actions/leads.ts (Plan 09-02):
```typescript
export async function createLead(input: unknown): Promise<
  | { ok: true; data?: { leadId: string; ownerUserId: string | null } }
  | { ok: false; error: string; fieldErrors?: Record<string, string[]> }
>;
export async function reassignLead(leadId: string): Promise<
  | { ok: true; data?: { ownerUserId: string; ownerName: string } }
  | { ok: false; error: string }
>;
export async function updateLeadStatus(leadId: string, newStatus: LeadStatus): Promise<{ ok: boolean; error?: string }>;
```

From packages/shared/src/schemas/lead.ts (Plan 09-01):
```typescript
export const CreateLeadSchema: ZodSchema; // détails Plan 09-01
```

From apps/web/src/lib/rbac.ts (Phase 8):
```typescript
export async function requireRole(allowed: UserRole[]): Promise<LuciaUser>;
// Pour pages Server : appeler dans try/catch, sur ForbiddenError → notFound() ou redirect('/app')
```

From dashboard pattern (PrioCard — Phase 6) :
```tsx
// apps/web/src/app/page.tsx ligne ~XX
<PrioCard label="..." value="..." subtitle="..." />
// Composant à réutiliser tel quel pour les 4 KPI globaux haut de /app/leads/charge.
```

LeadStatus enum (9 valeurs):
```typescript
NEW | CONTACTED | QUALIFIED | PROPOSAL_SENT | NEGOTIATION | WON | LOST | ON_HOLD | TO_FOLLOWUP
```

Pattern AlertDialog Radix Phase 4 :
```tsx
// apps/web/src/components/layout/user-menu-button.tsx (référence)
<AlertDialog.Root> + Trigger + Portal + Overlay + Content + Title + Description + Cancel + Action
```

Pattern Server Component avec requireRole :
```tsx
// apps/web/src/app/app/parametres/utilisateurs/page.tsx (Phase 8 référence)
import { redirect } from 'next/navigation';
import { hasRole } from '@/lib/rbac';
import { validateRequest } from '@/lib/auth';

export default async function Page() {
  const { user } = await validateRequest();
  if (!user) redirect('/login');
  if (!hasRole(user, ['ADMIN', 'MANAGER'])) redirect('/app');
  // ... fetch + render
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Composants leads/* (pie + table + create-form + status-select + reassign-button)</name>
  <files>apps/web/src/components/leads/lead-distribution-pie.tsx, apps/web/src/components/leads/__tests__/lead-distribution-pie.test.tsx, apps/web/src/components/leads/lead-load-table.tsx, apps/web/src/components/leads/lead-create-form.tsx, apps/web/src/components/leads/lead-status-select.tsx, apps/web/src/components/leads/reassign-lead-button.tsx</files>
  <read_first>
    - apps/web/src/components/leads/auto-assign-button.tsx (pattern client transition + sonner + router.refresh)
    - apps/web/src/components/layout/user-menu-button.tsx (pattern AlertDialog Radix Phase 4)
    - apps/web/src/components/forms/delete-entity-button.tsx (pattern confirmation Dialog)
    - .planning/phases/09-distribution-leads-automatique/09-RESEARCH.md (Pattern 4 SVG camembert)
    - apps/web/src/app/page.tsx (PrioCard component — chercher l'import + signature)
  </read_first>
  <behavior>
    - `LeadDistributionPie` : 
      - input `slices: { label, value, color }[]`
      - total > 0 : rend SVG 160×160 avec arcs `<path d="M cx cy L x1 y1 A r r 0 large 1 x2 y2 Z" fill={color}>` + légende
      - total === 0 : rend texte "Aucun lead actif"
      - aria-label sur svg + `<title>` par arc
    - `LeadLoadTable` : table `<table>` avec colonnes Commercial | Leads en cours | Gagnés ce mois | Taux conversion | Temps moyen (j)
    - `LeadCreateForm` : RHF + zodResolver(CreateLeadSchema), submit createLead, success → router.push('/app/leads/' + leadId), sonner toast
    - `LeadStatusSelect` : `<select>` natif liant les 9 statuts, onChange → useTransition → updateLeadStatus(leadId, value), toast confirmation
    - `ReassignLeadButton` : bouton + AlertDialog Radix → "Réassigner ce lead ?" → confirm = appelle reassignLead(leadId), toast + router.refresh()
  </behavior>
  <action>
    **1. Créer `apps/web/src/components/leads/lead-distribution-pie.tsx`** (~50 lignes) — copie 1:1 du Pattern 4 RESEARCH.md :

    ```tsx
    'use client';

    export interface Slice {
      label: string;
      value: number;
      color: string;
    }

    export function LeadDistributionPie({ slices }: { slices: Slice[] }) {
      const total = slices.reduce((s, x) => s + x.value, 0);
      if (total === 0) {
        return <div className="text-sm text-muted-foreground italic">Aucun lead actif</div>;
      }

      let cumulative = 0;
      const cx = 80, cy = 80, r = 70;
      const arcs = slices.map((s) => {
        const startAngle = (cumulative / total) * 2 * Math.PI - Math.PI / 2;
        cumulative += s.value;
        const endAngle = (cumulative / total) * 2 * Math.PI - Math.PI / 2;
        const x1 = cx + r * Math.cos(startAngle);
        const y1 = cy + r * Math.sin(startAngle);
        const x2 = cx + r * Math.cos(endAngle);
        const y2 = cy + r * Math.sin(endAngle);
        const large = s.value / total > 0.5 ? 1 : 0;
        return {
          d: `M ${cx} ${cy} L ${x1} ${y1} A ${r} ${r} 0 ${large} 1 ${x2} ${y2} Z`,
          color: s.color,
          label: `${s.label} : ${s.value}`,
        };
      });

      return (
        <div className="flex items-center gap-4">
          <svg
            viewBox="0 0 160 160"
            width={160}
            height={160}
            role="img"
            aria-label="Répartition des leads actifs par commercial"
          >
            {arcs.map((a, i) => (
              <path key={i} d={a.d} fill={a.color} stroke="white" strokeWidth={1}>
                <title>{a.label}</title>
              </path>
            ))}
          </svg>
          <ul className="text-xs space-y-1">
            {slices.map((s) => (
              <li key={s.label} className="flex items-center gap-2">
                <span className="inline-block w-3 h-3 rounded-sm" style={{ background: s.color }} aria-hidden />
                <span>
                  {s.label}{' '}
                  <span className="text-muted-foreground">({s.value})</span>
                </span>
              </li>
            ))}
          </ul>
        </div>
      );
    }
    ```

    **2. Créer test `apps/web/src/components/leads/__tests__/lead-distribution-pie.test.tsx`** (~50 lignes) — @testing-library/react :
    - slices vides → rend "Aucun lead actif"
    - 3 slices (10, 20, 30) → SVG contient 3 `<path>` + 3 `<title>` + 3 `<li>` légende
    - aria-label "Répartition des leads actifs par commercial" présent

    **3. Créer `apps/web/src/components/leads/lead-load-table.tsx`** (~50 lignes, Server Component) :

    ```tsx
    import type { CommercialKpis } from '@/lib/lead-load-stats';

    export function LeadLoadTable({ rows }: { rows: CommercialKpis[] }) {
      if (rows.length === 0) {
        return (
          <div className="text-sm text-muted-foreground italic">
            Aucun commercial actif dans ce tenant.
          </div>
        );
      }
      return (
        <div className="overflow-x-auto -mx-4 sm:mx-0">
          <table className="w-full border-collapse text-sm">
            <thead>
              <tr className="border-b border-border">
                <th className="text-left py-2 px-3 font-medium">Commercial</th>
                <th className="text-right py-2 px-3 font-medium">Leads en cours</th>
                <th className="text-right py-2 px-3 font-medium">Gagnés ce mois</th>
                <th className="text-right py-2 px-3 font-medium">Taux conv.</th>
                <th className="text-right py-2 px-3 font-medium">Temps moyen (j)</th>
              </tr>
            </thead>
            <tbody>
              {rows.map((r) => (
                <tr key={r.userId} className="border-b border-border/50 hover:bg-muted/30">
                  <td className="py-2 px-3">{r.name || '—'}</td>
                  <td className="py-2 px-3 text-right tabular-nums">{r.kpis.leadsActifs}</td>
                  <td className="py-2 px-3 text-right tabular-nums">{r.kpis.leadsWonThisMonth}</td>
                  <td className="py-2 px-3 text-right tabular-nums">{r.kpis.conversionPct}%</td>
                  <td className="py-2 px-3 text-right tabular-nums">
                    {r.kpis.avgDaysToWin !== null ? r.kpis.avgDaysToWin : '—'}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      );
    }
    ```

    **4. Créer `apps/web/src/components/leads/reassign-lead-button.tsx`** (~70 lignes) — client + AlertDialog Radix :

    ```tsx
    'use client';

    import { useState, useTransition } from 'react';
    import * as AlertDialog from '@radix-ui/react-alert-dialog';
    import { useRouter } from 'next/navigation';
    import { toast } from 'sonner';
    import { RefreshCw } from 'lucide-react';
    import { reassignLead } from '@/server/actions/leads';

    export function ReassignLeadButton({ leadId }: { leadId: string }) {
      const [open, setOpen] = useState(false);
      const [pending, startTransition] = useTransition();
      const router = useRouter();

      function handleConfirm() {
        startTransition(async () => {
          const r = await reassignLead(leadId);
          if (r.ok) {
            toast.success(`Lead réassigné à ${r.data?.ownerName ?? 'un commercial'}`);
            setOpen(false);
            router.refresh();
          } else {
            toast.error(r.error);
          }
        });
      }

      return (
        <AlertDialog.Root open={open} onOpenChange={setOpen}>
          <AlertDialog.Trigger asChild>
            <button
              type="button"
              className="inline-flex items-center gap-2 px-3 py-1.5 rounded-md border border-border hover:bg-muted text-sm"
            >
              <RefreshCw className="h-4 w-4" />
              Réassigner
            </button>
          </AlertDialog.Trigger>
          <AlertDialog.Portal>
            <AlertDialog.Overlay className="fixed inset-0 bg-black/40 z-40" />
            <AlertDialog.Content className="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-50 bg-white rounded-lg shadow-xl p-6 max-w-md w-[90vw]">
              <AlertDialog.Title className="text-lg font-semibold mb-2">
                Réassigner ce lead ?
              </AlertDialog.Title>
              <AlertDialog.Description className="text-sm text-muted-foreground mb-4">
                Le lead sera réattribué au commercial le moins chargé selon l'algorithme round-robin équilibré.
                Le commercial recevra une notification cloche + email (selon vos préférences).
              </AlertDialog.Description>
              <div className="flex justify-end gap-2">
                <AlertDialog.Cancel asChild>
                  <button type="button" className="px-3 py-1.5 rounded-md border border-border hover:bg-muted text-sm">
                    Annuler
                  </button>
                </AlertDialog.Cancel>
                <button
                  type="button"
                  onClick={handleConfirm}
                  disabled={pending}
                  className="px-3 py-1.5 rounded-md bg-primary text-primary-foreground hover:bg-primary/90 text-sm disabled:opacity-50"
                >
                  {pending ? 'Réassignation...' : 'Réassigner'}
                </button>
              </div>
            </AlertDialog.Content>
          </AlertDialog.Portal>
        </AlertDialog.Root>
      );
    }
    ```

    **5. Créer `apps/web/src/components/leads/lead-status-select.tsx`** (~50 lignes) :

    ```tsx
    'use client';

    import { useTransition } from 'react';
    import { useRouter } from 'next/navigation';
    import { toast } from 'sonner';
    import type { LeadStatus } from '@qualiof/db';
    import { updateLeadStatus } from '@/server/actions/leads';

    const STATUS_LABELS: Record<LeadStatus, string> = {
      NEW: 'Nouveau',
      CONTACTED: 'Contacté',
      QUALIFIED: 'Qualifié',
      PROPOSAL_SENT: 'Proposition envoyée',
      NEGOTIATION: 'Négociation',
      WON: 'Gagné',
      LOST: 'Perdu',
      ON_HOLD: 'En attente',
      TO_FOLLOWUP: 'À relancer',
    };

    export function LeadStatusSelect({ leadId, current }: { leadId: string; current: LeadStatus }) {
      const [pending, startTransition] = useTransition();
      const router = useRouter();
      return (
        <select
          defaultValue={current}
          disabled={pending}
          onChange={(e) => {
            const v = e.target.value as LeadStatus;
            startTransition(async () => {
              const r = await updateLeadStatus(leadId, v);
              if (r.ok) {
                toast.success(`Statut : ${STATUS_LABELS[v]}`);
                router.refresh();
              } else {
                toast.error(r.error ?? 'Erreur');
              }
            });
          }}
          className="px-2 py-1 rounded-md border border-border bg-white text-sm disabled:opacity-50"
        >
          {(Object.keys(STATUS_LABELS) as LeadStatus[]).map((s) => (
            <option key={s} value={s}>{STATUS_LABELS[s]}</option>
          ))}
        </select>
      );
    }
    ```

    **6. Créer `apps/web/src/components/leads/lead-create-form.tsx`** (~110 lignes) — RHF + zodResolver :

    ```tsx
    'use client';

    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { useRouter } from 'next/navigation';
    import { useTransition } from 'react';
    import { toast } from 'sonner';
    import { CreateLeadSchema, type CreateLeadInput } from '@qualiof/shared';
    import { createLead } from '@/server/actions/leads';

    export function LeadCreateForm() {
      const router = useRouter();
      const [pending, startTransition] = useTransition();
      const { register, handleSubmit, formState: { errors } } = useForm<CreateLeadInput>({
        resolver: zodResolver(CreateLeadSchema),
        defaultValues: { status: 'NEW', priority: 'MEDIUM' },
      });

      function onSubmit(data: CreateLeadInput) {
        startTransition(async () => {
          const r = await createLead(data);
          if (r.ok && r.data) {
            toast.success('Lead créé');
            router.push(`/app/leads/${r.data.leadId}`);
          } else if (!r.ok) {
            toast.error(r.error);
          }
        });
      }

      return (
        <form onSubmit={handleSubmit(onSubmit)} className="space-y-4 max-w-2xl">
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label className="block text-sm font-medium mb-1">Prénom</label>
              <input {...register('firstName')} className="w-full px-3 py-2 rounded-md border border-border" />
              {errors.firstName && <p className="text-xs text-red-600 mt-1">{errors.firstName.message}</p>}
            </div>
            <div>
              <label className="block text-sm font-medium mb-1">Nom *</label>
              <input {...register('lastName')} className="w-full px-3 py-2 rounded-md border border-border" />
              {errors.lastName && <p className="text-xs text-red-600 mt-1">{errors.lastName.message}</p>}
            </div>
            <div>
              <label className="block text-sm font-medium mb-1">Email</label>
              <input type="email" {...register('email')} className="w-full px-3 py-2 rounded-md border border-border" />
              {errors.email && <p className="text-xs text-red-600 mt-1">{errors.email.message}</p>}
            </div>
            <div>
              <label className="block text-sm font-medium mb-1">Téléphone</label>
              <input {...register('phone')} className="w-full px-3 py-2 rounded-md border border-border" />
            </div>
            <div className="sm:col-span-2">
              <label className="block text-sm font-medium mb-1">Source</label>
              <input {...register('source')} placeholder="Linkedin, Salon, Recommandation..." className="w-full px-3 py-2 rounded-md border border-border" />
            </div>
            <div className="sm:col-span-2">
              <label className="block text-sm font-medium mb-1">Notes</label>
              <textarea {...register('notes')} rows={3} className="w-full px-3 py-2 rounded-md border border-border" />
            </div>
          </div>
          <div className="flex justify-end gap-2">
            <button
              type="submit"
              disabled={pending}
              className="px-4 py-2 rounded-md bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50"
            >
              {pending ? 'Création...' : 'Créer le lead'}
            </button>
          </div>
        </form>
      );
    }
    ```
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/components/leads/__tests__/lead-distribution-pie.test.tsx && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function LeadDistributionPie" apps/web/src/components/leads/lead-distribution-pie.tsx` retourne 1 match.
    - `grep -n "export function LeadLoadTable" apps/web/src/components/leads/lead-load-table.tsx` retourne 1 match.
    - `grep -n "export function ReassignLeadButton" apps/web/src/components/leads/reassign-lead-button.tsx` retourne 1 match.
    - `grep -n "export function LeadStatusSelect" apps/web/src/components/leads/lead-status-select.tsx` retourne 1 match.
    - `grep -n "export function LeadCreateForm" apps/web/src/components/leads/lead-create-form.tsx` retourne 1 match.
    - `grep -n "reassignLead(" apps/web/src/components/leads/reassign-lead-button.tsx` retourne 1 match.
    - `grep -n "updateLeadStatus(" apps/web/src/components/leads/lead-status-select.tsx` retourne 1 match.
    - `grep -n "createLead(" apps/web/src/components/leads/lead-create-form.tsx` retourne 1 match.
    - `grep -rn "from 'recharts'\|from \"recharts\"" apps/web/src/components/leads/` retourne 0 matches (anti-pattern : pas de recharts).
    - `pnpm --filter @qualiof/web test --run src/components/leads/__tests__/lead-distribution-pie.test.tsx` → 3 tests verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    5 composants leads/* créés et typés. Camembert SVG sans dépendance externe (Finding #7 RESEARCH respecté). Pattern AlertDialog Radix conservé (cohérent Phase 4 user-menu-button.tsx). Pattern RHF + zodResolver conservé (cohérent Phase 8 InviteUserButton).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Page /app/leads/charge (LEAD-02 vue de charge ADMIN+MANAGER)</name>
  <files>apps/web/src/app/app/leads/charge/page.tsx, apps/web/src/app/app/leads/charge/__tests__/page.smoke.test.ts</files>
  <read_first>
    - apps/web/src/app/page.tsx (PrioCard component + import path)
    - apps/web/src/lib/lead-load-stats.ts (Plan 09-01 — getCommercialsWithKpis)
    - apps/web/src/components/leads/lead-load-table.tsx (Task 1)
    - apps/web/src/components/leads/lead-distribution-pie.tsx (Task 1)
    - apps/web/src/components/ui/breadcrumb.tsx (Phase 5)
    - apps/web/src/app/app/parametres/utilisateurs/page.tsx (pattern Server Component RBAC Phase 8)
  </read_first>
  <behavior>
    - Server Component avec `validateRequest` + `hasRole(['ADMIN','MANAGER'])` → sinon `redirect('/app')`
    - Fetch `getCommercialsWithKpis(user.tenantId)`
    - Calcule 4 KPI globaux : sommes/moyennes sur rows
      - totalLeadsActifs = sum(leadsActifs)
      - totalWonThisMonth = sum(leadsWonThisMonth)
      - conversionGlobale = moyenne pondérée (totalWon / totalAttribués total) — réutiliser une nouvelle requête simple
      - avgDaysGlobal = moyenne des avgDaysToWin non-null
    - Rend 4 PrioCard en haut + LeadLoadTable + LeadDistributionPie à droite (slices = rows mappées avec couleurs HSL distinctes)
    - Smoke test : rend la page avec auth mock ADMIN sans crash
  </behavior>
  <action>
    **1. Créer `apps/web/src/app/app/leads/charge/page.tsx`** (~110 lignes) — Server Component RBAC :

    ```tsx
    import { redirect } from 'next/navigation';
    import { validateRequest } from '@/lib/auth';
    import { hasRole } from '@/lib/rbac';
    import { prisma } from '@qualiof/db';
    import { getCommercialsWithKpis } from '@/lib/lead-load-stats';
    import { LeadLoadTable } from '@/components/leads/lead-load-table';
    import { LeadDistributionPie } from '@/components/leads/lead-distribution-pie';
    import { Breadcrumb } from '@/components/ui/breadcrumb';

    export const dynamic = 'force-dynamic';

    // Palette accessible — réutilise les tons primary/emerald/amber/sky/red attestés Phase 6 a11y audit.
    const PIE_COLORS = ['#00527A', '#10B981', '#F59E0B', '#0EA5E9', '#EF4444', '#8B5CF6', '#EC4899', '#14B8A6'];

    export default async function LeadsChargePage() {
      const { user } = await validateRequest();
      if (!user) redirect('/login');
      if (!hasRole(user, ['ADMIN', 'MANAGER'])) redirect('/app');

      const rows = await getCommercialsWithKpis(user.tenantId);

      // KPI globaux
      const totalLeadsActifs = rows.reduce((s, r) => s + r.kpis.leadsActifs, 0);
      const totalWonThisMonth = rows.reduce((s, r) => s + r.kpis.leadsWonThisMonth, 0);

      // Conversion globale = sum(WON tous owners) / sum(total attribués tous owners)
      const [globalWon, globalAttributed] = await Promise.all([
        prisma.lead.count({ where: { tenantId: user.tenantId, status: 'WON', ownerUserId: { not: null } } }),
        prisma.lead.count({ where: { tenantId: user.tenantId, ownerUserId: { not: null } } }),
      ]);
      const conversionGlobale = globalAttributed > 0 ? Math.round((globalWon / globalAttributed) * 100) : 0;

      const avgDaysVals = rows.map((r) => r.kpis.avgDaysToWin).filter((v): v is number => v !== null);
      const avgDaysGlobal = avgDaysVals.length > 0
        ? Math.round(avgDaysVals.reduce((s, v) => s + v, 0) / avgDaysVals.length)
        : null;

      const slices = rows
        .filter((r) => r.kpis.leadsActifs > 0)
        .map((r, i) => ({
          label: r.name || `Commercial ${i + 1}`,
          value: r.kpis.leadsActifs,
          color: PIE_COLORS[i % PIE_COLORS.length]!,
        }));

      return (
        <div className="space-y-6">
          <Breadcrumb items={[{ label: 'Leads', href: '/app/leads' }, { label: 'Vue de charge' }]} />
          <header>
            <h1 className="text-2xl font-semibold">Vue de charge — Distribution leads</h1>
            <p className="text-sm text-muted-foreground mt-1">
              Charge active et performance commerciale (mise à jour temps réel).
            </p>
          </header>

          <section aria-label="Indicateurs globaux" className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
            <PrioCardLocal label="Leads en cours" value={totalLeadsActifs.toString()} subtitle="tous commerciaux" />
            <PrioCardLocal label="Gagnés ce mois" value={totalWonThisMonth.toString()} subtitle="status = WON" />
            <PrioCardLocal label="Taux conversion" value={`${conversionGlobale}%`} subtitle="historique global" />
            <PrioCardLocal
              label="Temps moyen lead → signature"
              value={avgDaysGlobal !== null ? `${avgDaysGlobal} j` : '—'}
              subtitle="moyenne des commerciaux avec WON"
            />
          </section>

          <section className="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <div className="lg:col-span-2">
              <h2 className="text-lg font-semibold mb-3">Détail par commercial</h2>
              <LeadLoadTable rows={rows} />
            </div>
            <div>
              <h2 className="text-lg font-semibold mb-3">Répartition leads actifs</h2>
              <LeadDistributionPie slices={slices} />
            </div>
          </section>
        </div>
      );
    }

    /**
     * Mini-clone local de PrioCard (Phase 6) — évite import croisé app/page.tsx.
     * Si PrioCard devient exporté depuis components/ui, basculer dessus.
     */
    function PrioCardLocal({ label, value, subtitle }: { label: string; value: string; subtitle?: string }) {
      return (
        <div className="rounded-lg border border-border bg-white p-5">
          <div className="text-xs uppercase tracking-wide text-muted-foreground font-medium">{label}</div>
          <div className="text-3xl font-semibold mt-2 tabular-nums">{value}</div>
          {subtitle && <div className="text-xs text-muted-foreground mt-1">{subtitle}</div>}
        </div>
      );
    }
    ```

    **2. Créer `apps/web/src/app/app/leads/charge/__tests__/page.smoke.test.ts`** (~50 lignes) :
    - Test imports + RSC default export (sans render runtime — pattern Phase 1 BUG-01 smoke)
    - Vérifie qu'aucune dépendance manquante (lead-load-stats, components/leads/*, ui/breadcrumb)
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/app/app/leads/charge/__tests__/page.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/app/app/leads/charge/page.tsx` retourne 0.
    - `grep -n "validateRequest" apps/web/src/app/app/leads/charge/page.tsx` retourne 1 match.
    - `grep -n "hasRole(user, \['ADMIN', 'MANAGER'\])" apps/web/src/app/app/leads/charge/page.tsx` retourne 1 match.
    - `grep -n "getCommercialsWithKpis" apps/web/src/app/app/leads/charge/page.tsx` retourne 1 match.
    - `grep -n "LeadLoadTable\|LeadDistributionPie" apps/web/src/app/app/leads/charge/page.tsx` retourne ≥ 2 matches.
    - `grep -c "PrioCardLocal" apps/web/src/app/app/leads/charge/page.tsx` ≥ 5 (composant + 4 instances).
    - `pnpm --filter @qualiof/web test --run src/app/app/leads/charge/__tests__/page.smoke.test.ts` → 2 tests verts.
    - `pnpm --filter @qualiof/web exec next build` → page `/app/leads/charge` compile (vérifier output Next).
  </acceptance_criteria>
  <done>
    Page `/app/leads/charge` ADMIN+MANAGER opérationnelle. 4 KPI globaux + table + camembert. Build Next OK.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pages /app/leads/[id] + /app/leads/new + smoke tests</name>
  <files>apps/web/src/app/app/leads/[id]/page.tsx, apps/web/src/app/app/leads/[id]/__tests__/page.smoke.test.ts, apps/web/src/app/app/leads/new/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/leads/page.tsx (page list — pour réutiliser le fetch lead + lien "Nouveau lead")
    - apps/web/src/components/leads/reassign-lead-button.tsx (Task 1)
    - apps/web/src/components/leads/lead-status-select.tsx (Task 1)
    - apps/web/src/components/leads/lead-create-form.tsx (Task 1)
    - apps/web/src/components/ui/breadcrumb.tsx (Phase 5)
    - packages/db/prisma/schema.prisma (Lead relations — person, organization, interestedProduct, owner — lignes 648-687)
  </read_first>
  <behavior>
    `/app/leads/[id]/page.tsx` :
    - Server Component, validateRequest + redirect si !user
    - Fetch lead avec includes person/organization/interestedProduct/owner (scope tenantId)
    - Si null → notFound()
    - Header avec breadcrumb + prospectName
    - Section "Owner" : badge nom owner ou "Non assigné" + ReassignLeadButton
    - Section "Statut" : LeadStatusSelect(leadId, lead.status)
    - Section "Détails" : source, priority, email, phone, interestedProduct, notes

    `/app/leads/new/page.tsx` :
    - Server Component, validateRequest + hasRole(['ADMIN','MANAGER','COMMERCIAL']) → sinon redirect('/app/leads')
    - Header + Breadcrumb
    - <LeadCreateForm /> client component (RHF + createLead)
  </behavior>
  <action>
    **1. Créer `apps/web/src/app/app/leads/[id]/page.tsx`** (~115 lignes) :

    ```tsx
    import { notFound, redirect } from 'next/navigation';
    import Link from 'next/link';
    import { validateRequest } from '@/lib/auth';
    import { prisma } from '@qualiof/db';
    import { Breadcrumb } from '@/components/ui/breadcrumb';
    import { ReassignLeadButton } from '@/components/leads/reassign-lead-button';
    import { LeadStatusSelect } from '@/components/leads/lead-status-select';

    export const dynamic = 'force-dynamic';

    export default async function LeadDetailPage({ params }: { params: { id: string } }) {
      const { user } = await validateRequest();
      if (!user) redirect('/login');

      const lead = await prisma.lead.findFirst({
        where: { id: params.id, tenantId: user.tenantId },
        include: {
          person: { select: { id: true, firstName: true, lastName: true } },
          organization: { select: { id: true, displayName: true } },
          interestedProduct: { select: { id: true, title: true } },
          owner: { select: { id: true, firstName: true, lastName: true } },
        },
      });
      if (!lead) notFound();

      const prospectName =
        (lead.person ? `${lead.person.firstName} ${lead.person.lastName}`.trim() : '') ||
        `${lead.firstName ?? ''} ${lead.lastName ?? ''}`.trim() ||
        'Prospect';
      const ownerName = lead.owner ? `${lead.owner.firstName} ${lead.owner.lastName}`.trim() : null;

      return (
        <div className="space-y-6">
          <Breadcrumb items={[{ label: 'Leads', href: '/app/leads' }, { label: prospectName }]} />
          <header className="flex items-start justify-between gap-4">
            <div>
              <h1 className="text-2xl font-semibold">{prospectName}</h1>
              <p className="text-sm text-muted-foreground mt-1">
                Créé le {new Intl.DateTimeFormat('fr-FR', { dateStyle: 'long' }).format(lead.createdAt)}
              </p>
            </div>
            <ReassignLeadButton leadId={lead.id} />
          </header>

          <section className="grid grid-cols-1 sm:grid-cols-2 gap-6">
            <div className="space-y-1">
              <div className="text-xs uppercase tracking-wide text-muted-foreground font-medium">Commercial assigné</div>
              <div className="text-base font-medium">
                {ownerName ? (
                  <span className="inline-flex items-center px-2 py-1 rounded-md bg-primary/10 text-primary">{ownerName}</span>
                ) : (
                  <span className="text-muted-foreground italic">Non assigné</span>
                )}
              </div>
            </div>

            <div className="space-y-1">
              <div className="text-xs uppercase tracking-wide text-muted-foreground font-medium">Statut</div>
              <LeadStatusSelect leadId={lead.id} current={lead.status} />
              {lead.wonAt && (
                <div className="text-xs text-emerald-700 mt-1">
                  Gagné le {new Intl.DateTimeFormat('fr-FR', { dateStyle: 'long' }).format(lead.wonAt)}
                </div>
              )}
            </div>
          </section>

          <section className="grid grid-cols-1 sm:grid-cols-2 gap-6 border-t border-border pt-6">
            <DetailRow label="Source" value={lead.source ?? '—'} />
            <DetailRow label="Priorité" value={lead.priority} />
            <DetailRow label="Email" value={lead.email ?? lead.person?.firstName ? lead.email ?? '—' : '—'} />
            <DetailRow label="Téléphone" value={lead.phone ?? '—'} />
            {lead.organization && (
              <DetailRow
                label="Organisation"
                value={
                  <Link href={`/app/organisations/${lead.organization.id}`} className="text-primary hover:underline">
                    {lead.organization.displayName}
                  </Link>
                }
              />
            )}
            {lead.interestedProduct && (
              <DetailRow label="Formation d'intérêt" value={lead.interestedProduct.title} />
            )}
          </section>

          {lead.notes && (
            <section className="border-t border-border pt-6">
              <div className="text-xs uppercase tracking-wide text-muted-foreground font-medium mb-2">Notes</div>
              <div className="text-sm whitespace-pre-wrap">{lead.notes}</div>
            </section>
          )}
        </div>
      );
    }

    function DetailRow({ label, value }: { label: string; value: React.ReactNode }) {
      return (
        <div className="space-y-1">
          <div className="text-xs uppercase tracking-wide text-muted-foreground font-medium">{label}</div>
          <div className="text-sm">{value}</div>
        </div>
      );
    }
    ```

    **2. Créer `apps/web/src/app/app/leads/[id]/__tests__/page.smoke.test.ts`** (~40 lignes) :
    - Test imports + RSC default export sans crash
    - Vérifie présence ReassignLeadButton + LeadStatusSelect + Breadcrumb dans le fichier

    **3. Créer `apps/web/src/app/app/leads/new/page.tsx`** (~50 lignes) :

    ```tsx
    import { redirect } from 'next/navigation';
    import { validateRequest } from '@/lib/auth';
    import { hasRole } from '@/lib/rbac';
    import { Breadcrumb } from '@/components/ui/breadcrumb';
    import { LeadCreateForm } from '@/components/leads/lead-create-form';

    export const dynamic = 'force-dynamic';

    export default async function NewLeadPage() {
      const { user } = await validateRequest();
      if (!user) redirect('/login');
      if (!hasRole(user, ['ADMIN', 'MANAGER', 'COMMERCIAL'])) redirect('/app/leads');

      return (
        <div className="space-y-6">
          <Breadcrumb items={[{ label: 'Leads', href: '/app/leads' }, { label: 'Nouveau' }]} />
          <header>
            <h1 className="text-2xl font-semibold">Nouveau lead</h1>
            <p className="text-sm text-muted-foreground mt-1">
              Le commercial le moins chargé sera automatiquement assigné (si l'auto-distribution est active).
            </p>
          </header>
          <LeadCreateForm />
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run src/app/app/leads/[id]/__tests__/page.smoke.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/app/app/leads/[id]/page.tsx` retourne 0.
    - `test -f apps/web/src/app/app/leads/new/page.tsx` retourne 0.
    - `grep -n "ReassignLeadButton" apps/web/src/app/app/leads/[id]/page.tsx` retourne 1 match.
    - `grep -n "LeadStatusSelect" apps/web/src/app/app/leads/[id]/page.tsx` retourne 1 match.
    - `grep -n "notFound()" apps/web/src/app/app/leads/[id]/page.tsx` retourne 1 match.
    - `grep -n "tenantId: user.tenantId" apps/web/src/app/app/leads/[id]/page.tsx` retourne 1 match.
    - `grep -n "LeadCreateForm" apps/web/src/app/app/leads/new/page.tsx` retourne 1 match.
    - `grep -n "hasRole(user, \['ADMIN', 'MANAGER', 'COMMERCIAL'\])" apps/web/src/app/app/leads/new/page.tsx` retourne 1 match.
    - `pnpm --filter @qualiof/web test --run src/app/app/leads/[id]/__tests__/page.smoke.test.ts` → 2 tests verts.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>
    2 nouvelles routes (`/app/leads/[id]` + `/app/leads/new`) opérationnelles. Le bouton Réassigner et le select statut consomment les server actions de Plan 09-02. La création de lead respecte le wiring auto-assign+notif via createLead.
  </done>
</task>

</tasks>

<verification>
- 5 composants leads/* créés (pie, table, reassign-button, status-select, create-form) + 3 pages app (charge, [id], new).
- Tous les smoke tests + unit test pie verts.
- `tsc --noEmit` clean.
- `pnpm --filter @qualiof/web build` → 3 nouvelles routes compilent.
- `grep -rn "recharts\|chart\.js" apps/web/src/` → 0 match (Finding #7 respecté).
- Pattern AlertDialog Radix Phase 4 (user-menu-button.tsx) cloné dans ReassignLeadButton.
- Pattern RHF + zodResolver Phase 8 (InviteUserButton) cloné dans LeadCreateForm.
</verification>

<success_criteria>
- 3 routes Lead disponibles : `/app/leads/charge`, `/app/leads/[id]`, `/app/leads/new`.
- RBAC appliqué côté pages : charge ADMIN+MANAGER, new ADMIN+MANAGER+COMMERCIAL, [id] tous rôles (lecture).
- Camembert SVG sans deps externes.
- KPI 1 (leadsActifs), 2 (won this month), 3 (conversion), 4 (avg days) tous calculés et affichés.
- Build Next.js clean, tsc clean.
- ≥ 7 tests verts (3 pie + 2 charge smoke + 2 [id] smoke).
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution-leads-automatique/09-03-SUMMARY.md` with:
- 5 composants créés (+ paths + line counts)
- 3 pages créées + RBAC appliqué
- Patterns réutilisés (PrioCardLocal vs PrioCard, AlertDialog Phase 4, RHF Phase 8)
- Décisions techniques (pas de recharts, palette PIE_COLORS HSL distinctes)
- Tests créés + count
</output>
