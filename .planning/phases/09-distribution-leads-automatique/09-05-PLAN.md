---
phase: 09-distribution-leads-automatique
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/09-distribution-leads-automatique/09-SMOKE.md
  - .planning/phases/09-distribution-leads-automatique/09-SUMMARY.md
autonomous: false
requirements: [LEAD-01, LEAD-02]

must_haves:
  truths:
    - "LEAD-01 et LEAD-02 marqués [x] DONE dans REQUIREMENTS.md avec preuve (paths + grep)"
    - "ROADMAP.md Phase 9 marquée Complete avec date du jour"
    - "STATE.md mis à jour : completed_phases +1, completed_plans +5, current_focus = Phase 10"
    - "09-SMOKE.md décrit le script manuel DevTools (4 flows) que Laurent peut suivre"
    - "09-SUMMARY.md consolide les 4 plans (commits + fichiers + décisions clés)"
    - "Build Next.js OK + suite Vitest globale verte"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "LEAD-01 / LEAD-02 marqués DONE 2026-05-NN"
      contains: "LEAD-01.*DONE"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 listée Complete + 5/5 plans"
    - path: ".planning/STATE.md"
      provides: "Position post-Phase 9 + accumulated context"
    - path: ".planning/phases/09-distribution-leads-automatique/09-SMOKE.md"
      provides: "Script smoke DevTools manuel (4 flows clés)"
    - path: ".planning/phases/09-distribution-leads-automatique/09-SUMMARY.md"
      provides: "Récap consolidé Phase 9"
  key_links:
    - from: "REQUIREMENTS.md LEAD-01/LEAD-02"
      to: "Plans 09-01..09-04 et commits associés"
      via: "Refs paths + decisions"
      pattern: "LEAD-0[12].*DONE"
---

<objective>
Plan 09-05 — Bookkeeping fin de phase. Clôt formellement la Phase 9 :
1. Smoke build + vitest global vert (preuve agrégée)
2. Script manuel DevTools (4 flows) que Laurent peut suivre
3. REQUIREMENTS.md : LEAD-01 / LEAD-02 marqués DONE avec preuves
4. ROADMAP.md : Phase 9 marquée Complete + count plans 5/5
5. STATE.md : completed_phases +1, completed_plans +5, position avance vers Phase 10
6. 09-SUMMARY.md consolide les 4 plans précédents

Purpose: Sans ce bookkeeping, le projet ne peut pas démarrer la Phase 10 (STATE.md cohérent + REQUIREMENTS à jour = pré-requis `/gsd:plan-phase 10`).
Output: 5 fichiers mis à jour, 1 checkpoint humain final pour validation Laurent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-distribution-leads-automatique/09-CONTEXT.md
@.planning/phases/09-distribution-leads-automatique/09-RESEARCH.md
@.planning/phases/09-distribution-leads-automatique/09-VALIDATION.md
@.planning/phases/09-distribution-leads-automatique/09-01-SUMMARY.md
@.planning/phases/09-distribution-leads-automatique/09-02-SUMMARY.md
@.planning/phases/09-distribution-leads-automatique/09-03-SUMMARY.md
@.planning/phases/09-distribution-leads-automatique/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke build + suite Vitest globale + écriture 09-SMOKE.md</name>
  <files>.planning/phases/09-distribution-leads-automatique/09-SMOKE.md</files>
  <read_first>
    - .planning/phases/09-distribution-leads-automatique/09-VALIDATION.md (4 flows manuels listés)
    - .planning/phases/09-distribution-leads-automatique/09-RESEARCH.md (section "Validation Architecture > Manuel")
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-SMOKE.md si existe (pattern référence)
  </read_first>
  <action>
    **1. Exécuter la suite Vitest complète** :

    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test --run
    ```

    Vérifier 0 failure. Si erreur → STOP, retourner à plan correspondant.

    **2. Build Next.js** :

    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files/apps/web" && rm -rf .next && pnpm exec next build
    ```

    Vérifier les 4 nouvelles routes compilent (cherche dans l'output) :
    - `/app/leads/charge`
    - `/app/leads/[id]`
    - `/app/leads/new`
    - `/app/parametres/distribution-leads`

    **3. tsc --noEmit final** :

    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files/apps/web" && pnpm exec tsc --noEmit
    ```

    **4. Créer `.planning/phases/09-distribution-leads-automatique/09-SMOKE.md`** (~70 lignes) — Script manuel DevTools 4 flows :

    ```markdown
    # Phase 9 — Smoke manuel DevTools

    **Pré-requis :**
    - `pnpm dev:full` lancé (.next clean auto-clean inclus)
    - Connecté en tant qu'ADMIN dans le tenant Start Academy
    - Au moins 2 comptes COMMERCIAL actifs dans le tenant
    - SMTP_HOST vide (mode dry-run) — vérification email = console log

    ## Flow 1 — Création de lead avec auto-assignation

    1. Naviguer vers `/app/leads`
    2. Cliquer "Nouveau lead" (en haut à droite)
    3. Remplir : Prénom "Test", Nom "Smoke9", Source "DevTools"
    4. Cliquer "Créer le lead"

    **Attendu :**
    - Toast vert "Lead créé"
    - Redirect vers `/app/leads/{leadId}`
    - Section "Commercial assigné" : badge avec nom d'un commercial
    - En console serveur : log `mailer dry-run` avec subject "Nouveau lead à traiter — Test Smoke9"

    ## Flow 2 — Réassignation manuelle

    1. Sur la fiche lead de Flow 1, cliquer "Réassigner"
    2. Confirmer dans la dialog

    **Attendu :**
    - Toast vert "Lead réassigné à {ownerName}"
    - Section "Commercial assigné" : badge mis à jour (potentiellement le même si charge équilibrée → vérifier que l'AuditLog enregistre bien la transaction quoi qu'il en soit)
    - Page `/app/parametres/historique` (ADMIN) : nouvelle row `leads.reassigned` avec userId=admin actuel

    ## Flow 3 — Toggles distribution leads

    1. Naviguer vers `/app/parametres/distribution-leads`
    2. Décocher "Auto-assignation des leads"
    3. Cliquer "Enregistrer"
    4. Toast vert "Paramètres mis à jour"
    5. Retourner `/app/leads/new` → créer un nouveau lead "Test SmokeNoAuto"

    **Attendu :**
    - Lead créé mais "Commercial assigné" = "Non assigné"
    - Aucun email dry-run en console (toggle email vu désactivé via le toggle parent)
    - Page Historique : row `leads.distribution_config` avec diff `autoAssignLeads: true → false`

    Réactiver "Auto-assignation des leads" + sauver après Flow 3.

    ## Flow 4 — Vue de charge

    1. Naviguer vers `/app/leads/charge` (ADMIN ou MANAGER uniquement)
    2. Vérifier 4 PrioCard en haut : Leads en cours / Gagnés ce mois / Conversion / Temps moyen
    3. Vérifier tableau commercial × 4 KPI
    4. Vérifier camembert SVG (1 part par commercial avec leads actifs)
    5. Se déconnecter, se reconnecter en COMMERCIAL
    6. Naviguer vers `/app/leads/charge`

    **Attendu :**
    - En ADMIN/MANAGER : page rendue avec données réelles
    - En COMMERCIAL : redirect immédiat vers `/app` (RBAC bloque)
    - Sidebar COMMERCIAL : pas d'entrée "Vue de charge"

    ## Flow 5 (bonus) — Transition statut + KPI Gagnés ce mois

    1. Sur la fiche lead Flow 1, changer le select statut vers "Gagné"
    2. Toast vert "Statut : Gagné"
    3. Vérifier "Gagné le {date du jour}" sous le select
    4. Retourner `/app/leads/charge` → "Gagnés ce mois" devrait avoir incrémenté

    **Attendu :**
    - Database : `Lead.wonAt` set à `now()`
    - KPI 2 (Leads gagnés ce mois) sur la vue de charge a incrémenté de 1

    ---

    **Validation finale :** Laurent confirme que les 5 flows passent → Phase 9 OK.
    ```

    Sauver ce fichier.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test --run 2>&1 | tail -10 && pnpm --filter @qualiof/web exec tsc --noEmit && test -f .planning/phases/09-distribution-leads-automatique/09-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @qualiof/web test --run` → 0 test échoue, le nombre total de tests Vitest a augmenté d'au moins 25 vs avant la Phase 9 (plans 09-01..04 ajoutent ~30 tests).
    - `pnpm --filter @qualiof/web exec tsc --noEmit` → 0 erreur.
    - `pnpm --filter @qualiof/web exec next build` → output mentionne `/app/leads/charge`, `/app/leads/new`, `/app/leads/[id]`, `/app/parametres/distribution-leads`.
    - `test -f .planning/phases/09-distribution-leads-automatique/09-SMOKE.md && wc -l .planning/phases/09-distribution-leads-automatique/09-SMOKE.md` ≥ 60 lignes.
    - `grep -c "## Flow" .planning/phases/09-distribution-leads-automatique/09-SMOKE.md` ≥ 4.
  </acceptance_criteria>
  <done>
    Build clean, tsc clean, suite Vitest verte (≥ 25 tests Phase 9 ajoutés). 09-SMOKE.md écrit avec 5 flows DevTools que Laurent peut exécuter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mise à jour REQUIREMENTS.md + ROADMAP.md + STATE.md + écriture 09-SUMMARY.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/09-distribution-leads-automatique/09-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entrées LEAD-01 / LEAD-02 lignes 59-60 actuellement [ ])
    - .planning/ROADMAP.md (Phase 9 lignes 136-145 + tableau Progress lignes 184-198)
    - .planning/STATE.md (frontmatter completed_phases/completed_plans, "Current Position", "Roadmap Evolution", "Last session")
    - .planning/phases/09-distribution-leads-automatique/09-01-SUMMARY.md
    - .planning/phases/09-distribution-leads-automatique/09-02-SUMMARY.md
    - .planning/phases/09-distribution-leads-automatique/09-03-SUMMARY.md
    - .planning/phases/09-distribution-leads-automatique/09-04-SUMMARY.md
  </read_first>
  <action>
    **1. Mettre à jour `.planning/REQUIREMENTS.md`** :

    - Ligne 59 (LEAD-01) : remplacer `- [ ] **LEAD-01** : Distribution automatique...` par :
    ```markdown
    - [x] **LEAD-01** : Distribution automatique Lead → Commercial. **DONE 2026-05-NN** — algo `autoAssignLead` Phase 9 read-only WIRÉ dans `server/actions/leads.ts` (createLead + reassignLead). Trigger automatique à la création (conditionné par `Tenant.autoAssignLeads`), trigger manuel via bouton "Réassigner" (AlertDialog Radix). Notification cloche persistée (`Notification` model NEW + type 'lead.assigned' + payload typé Zod `LeadAssignedPayloadSchema`) + email dry-run/SMTP (template `lead-assigned.ts` clone strict de `user-invitation.ts` Phase 8). AuditLog conventions `leads.auto_assigned` (system actorUserId=null), `leads.reassigned` (manuel actorUserId=user.id), `leads.distribution_config` (toggles ADMIN). 3 toggles Tenant `autoAssignLeads/notifyOnLeadAssignEmail/notifyOnLeadAssignBell` éditables via `/app/parametres/distribution-leads` (ADMIN). Plans 09-01..04.
    ```

    - Ligne 60 (LEAD-02) : remplacer `- [ ] **LEAD-02** : Vue de charge...` par :
    ```markdown
    - [x] **LEAD-02** : Vue de charge par commercial. **DONE 2026-05-NN** — page `/app/leads/charge` ADMIN+MANAGER, 4 PrioCard globaux (leads en cours / gagnés ce mois / taux conversion / temps moyen) + tableau commercial × 4 KPI + camembert SVG inline (40 lignes, pas de Recharts). Helper pur `getCommercialsWithKpis(tenantId)` (`apps/web/src/lib/lead-load-stats.ts`) avec 3 `prisma.lead.groupBy` + 1 `$queryRaw` AVG en parallèle. Champ `Lead.wonAt` ajouté (set automatiquement par `updateLeadStatus` à la transition vers WON, Pitfall 3 résolu). Sidebar entrée "Vue de charge" sous "Leads" (allowedRoles=ADMIN+MANAGER). Plans 09-01 + 09-03.
    ```

    (Remplacer "2026-05-NN" par la date du jour `date +%Y-%m-%d`.)

    **2. Mettre à jour `.planning/ROADMAP.md`** :

    - Lignes 136-145 (section Phase 9) : remplacer `**Plans**: TBD` par :
    ```markdown
    **Plans:** 5/5 plans complete
    **Plans**:
    - [x] 09-01-PLAN.md — Foundation : migration Prisma (Notification + Lead.wonAt + 3 toggles Tenant) + Zod schemas + getCommercialsWithKpis + renderLeadAssignedEmail
    - [x] 09-02-PLAN.md — Server actions leads.ts (createLead/reassignLead/updateLeadStatus) + distribution-leads-config.ts + helper notifyLeadAssigned + extension audit-log.ts (logLeadEvent)
    - [x] 09-03-PLAN.md — UI pages : /app/leads/charge + /app/leads/[id] + /app/leads/new + 5 composants leads/* (pie SVG + table + reassign-button + status-select + create-form)
    - [x] 09-04-PLAN.md — Page paramètres /app/parametres/distribution-leads (ADMIN 3 toggles) + extension cloche TopBar (kind 'lead.assigned' + markNotificationRead) + sidebar (Vue de charge + Distribution leads) + bouton "Nouveau lead"
    - [x] 09-05-PLAN.md — Bookkeeping (REQUIREMENTS/ROADMAP/STATE + smoke build/tests + 09-SMOKE.md + 09-SUMMARY.md)
    ```

    - Ligne 194 (tableau Progress) : remplacer la ligne Phase 9 par :
    ```markdown
    | 9. Distribution leads automatique | 5/5 | Complete    | 2026-05-NN |
    ```

    - Ligne 23 : remplacer `- [ ] **Phase 9: Distribution leads automatique**` par `- [x] **Phase 9: Distribution leads automatique**`.

    **3. Mettre à jour `.planning/STATE.md`** :

    - Frontmatter ligne 8-12 (`progress`) :
    ```yaml
    progress:
      total_phases: 12
      completed_phases: 5
      total_plans: 36
      completed_plans: 24
    ```

    - Frontmatter ligne 6 (`stopped_at`) :
    ```yaml
    stopped_at: "Phase 9 closed. Prochaine étape : /gsd:plan-phase 10."
    ```

    - Frontmatter ligne 7 (`last_updated`) → mettre la date ISO actuelle.

    - Ligne 23 ("Current focus") :
    ```markdown
    **Current focus:** Phase 10 — Audit Qualiopi blanc (à planifier)
    ```

    - Lignes 29-30 ("Current Position") :
    ```markdown
    Phase: 10
    Plan: Not started
    ```

    - "Roadmap Evolution" — APPENDER (après l'entrée 2026-05-15 Phase 8) :
    ```markdown
    - 2026-05-NN — Phase 9 closed : LEAD-01/02 livrés. 5 plans exécutés. (1) Plan 09-01 fondations : migration Prisma `phase09_distribution` (Notification model + Lead.wonAt + 3 toggles Tenant.autoAssignLeads/notifyOnLeadAssignEmail/notifyOnLeadAssignBell @default(true)) + packages/shared/src/schemas/lead.ts (CreateLeadSchema/DistributionConfigSchema/LeadAssignedPayloadSchema) + lib/lead-load-stats.ts (getCommercialsWithKpis avec 3 groupBy + $queryRaw AVG) + lib/mailer-templates/lead-assigned.ts (clone strict user-invitation.ts Phase 8). (2) Plan 09-02 server actions : server/actions/leads.ts (createLead/reassignLead/updateLeadStatus avec set wonAt sur transition WON), server/actions/distribution-leads-config.ts (ADMIN 3 toggles), lib/lead-notifications.ts (notifyLeadAssigned orchestre Notification + Email + AuditLog avec 2 toggles), extension lib/audit-log.ts logLeadEvent (entity='Lead' + actorUserId nullable system). APP_URL documenté .env.example. (3) Plan 09-03 UI : 3 pages (/app/leads/charge ADMIN+MANAGER avec 4 PrioCard + table + camembert SVG inline 40 lignes / /app/leads/[id] fiche + Réassigner + statut select / /app/leads/new RHF zodResolver) + 5 composants leads/* (pie/table/reassign-button/status-select/create-form). Pas de Recharts (Finding #7). (4) Plan 09-04 cloche + config tenant : /app/parametres/distribution-leads ADMIN avec DistributionLeadsForm + extension notifications.ts (hybride 4 kinds dérivés + N persistés 'lead.assigned' user-scoped + parsing Zod payload Pitfall 6) + notification-mark-read.ts (single-use atomique) + extension notifications-bell.tsx (icône UserPlus + markNotificationRead fire-and-forget Open Question #3 résolue) + 2 entrées nav-config (Vue de charge ADMIN+MANAGER / Distribution leads ADMIN). (5) Plan 09-05 bookkeeping + smoke build + 5 flows DevTools 09-SMOKE.md. Conventions AuditLog complétées : `leads.*` (3 actions) en plus de `parameters.*` Phase 7 + `users.*` + `auth.*` Phase 8. Total nouveaux fichiers : ~22 production + 9 tests. Tests Vitest verts : ≥ 25 nouveaux ajoutés. Phase 10 (Audit Qualiopi blanc) débloquée.
    ```

    - "Key Decisions Recorded" — APPENDER :
    ```markdown
    - 2026-05-NN — **D-11 Hybride cloche (Pitfall 2 résolu)** : `getNotifications` retourne union `[dérivé tenant-wide, persisté user-scoped]`. Les 4 kinds dérivés (preinscription/session_no_attendee/session_to_close/cleanup) sont tenant-wide. Les rows `Notification` du nouveau model (Phase 9) sont user-scoped (filter `userId: user.id`) — un lead assigné apparaît uniquement dans la cloche du commercial concerné, pas tenant-wide.
    - 2026-05-NN — **D-12 Convention AuditLog Phase 9** : entity='Lead' (3 actions) + entity='Tenant' (1 action). Actions : `leads.auto_assigned` (actorUserId=null system, à la création conditionnelle), `leads.reassigned` (actorUserId=user.id, bouton manuel — IGNORE le toggle `autoAssignLeads` car force=true), `leads.status.change` (actorUserId=user.id, transition statut), `leads.distribution_config` (actorUserId=admin.id, modification des 3 toggles).
    - 2026-05-NN — **D-13 Marquage notification lue (Open Question #3 RESEARCH)** : "lue au clic" via `markNotificationRead(notifId)` fire-and-forget dans `notifications-bell.tsx`. Pas de bouton "Marquer tout comme lu" (deferred). Le `useEffect` polling 60s rafraîchit naturellement la cloche après le clic.
    ```

    - Section "Last session" : remplacer par `Stopped at: Phase 9 closed. Prochaine étape : /gsd:plan-phase 10.`

    **4. Créer `.planning/phases/09-distribution-leads-automatique/09-SUMMARY.md`** (~80 lignes) — récap consolidé en agrégant les 4 SUMMARY précédents.

    Structure :
    ```markdown
    # Phase 9 — Distribution leads automatique — SUMMARY

    **Status:** Complete · **Closed:** 2026-05-NN

    ## Requirements Delivered
    - LEAD-01 ✓ (algo wiré + 4 server actions + 3 toggles + notifs + audit)
    - LEAD-02 ✓ (page charge + 4 KPI + camembert + sidebar)

    ## Plans Executed
    | Plan | Theme | Files (prod) | Tests | Key decisions |
    |---|---|---|---|---|
    | 09-01 | Foundation BDD + types | 4 | 3 fichiers tests | Notification model + wonAt + 3 toggles, schemas Zod |
    | 09-02 | Server actions + wiring | 5 | 4 fichiers tests | notifyLeadAssigned orchestre 3 side-effects, Pitfall 3/4/5 décisions |
    | 09-03 | UI pages métier | 8 | 3 fichiers tests | PrioCardLocal + SVG inline, pas de Recharts |
    | 09-04 | Cloche + config tenant | 6 | 3 fichiers tests | Hybride dérivé/persisté, markNotificationRead fire-and-forget |
    | 09-05 | Bookkeeping | 5 docs | — | (ce plan) |

    ## Architecture Drift / Discovered
    - `getCommercialsWithLoad` reste privé dans `auto-assign-leads.ts` ; `lead-load-stats.ts` duplique `ACTIVE_STATUSES`. Refactor possible v6 : exporter une seule fois.
    - `Notification` payload est `Json` ; Zod parse côté reader (Pitfall 6 fix). Si futurs types ajoutés, créer un union schema dans `packages/shared/src/schemas/notification.ts`.

    ## Anti-patterns avoided
    - Aucune dépendance chart externe (camembert SVG pur).
    - Pas d'email/notification dans `autoAssignLead` (algo reste pur, side-effects côté caller).
    - Pas de `entity='AutoAssignment'` — convention `entity='Lead'` cohérente Phase 7/8.

    ## Validation
    - Build Next.js OK (4 nouvelles routes Phase 9 + 4 routes Phase 8 + ...).
    - Suite Vitest : ≥ 25 tests Phase 9 verts.
    - tsc --noEmit clean.
    - Smoke DevTools 5 flows documenté `09-SMOKE.md` — à exécuter manuellement par Laurent.

    ## Next: Phase 10 — Audit Qualiopi blanc (QBLANC-01..03)
    ```

    Sauver tous les fichiers.
  </action>
  <verify>
    <automated>grep -c "LEAD-01.*DONE\|LEAD-02.*DONE" .planning/REQUIREMENTS.md && grep -c "Phase 9.*Complete" .planning/ROADMAP.md && grep -c "completed_phases: 5" .planning/STATE.md && test -f .planning/phases/09-distribution-leads-automatique/09-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LEAD-01.*DONE 2026" .planning/REQUIREMENTS.md` retourne 1 match.
    - `grep -n "LEAD-02.*DONE 2026" .planning/REQUIREMENTS.md` retourne 1 match.
    - `grep -n "- \[x\] \*\*Phase 9: Distribution leads automatique\*\*" .planning/ROADMAP.md` retourne 1 match.
    - `grep -n "09-01-PLAN.md\|09-02-PLAN.md\|09-03-PLAN.md\|09-04-PLAN.md\|09-05-PLAN.md" .planning/ROADMAP.md` retourne 5 matches.
    - `grep -n "9. Distribution leads automatique | 5/5 | Complete" .planning/ROADMAP.md` retourne 1 match.
    - `grep -n "completed_phases: 5" .planning/STATE.md` retourne 1 match.
    - `grep -n "completed_plans: 24" .planning/STATE.md` retourne 1 match.
    - `grep -n "Phase 9 closed. Prochaine étape : /gsd:plan-phase 10" .planning/STATE.md` retourne ≥ 1 match.
    - `grep -n "D-11\|D-12\|D-13" .planning/STATE.md` retourne ≥ 3 matches.
    - `test -f .planning/phases/09-distribution-leads-automatique/09-SUMMARY.md && wc -l .planning/phases/09-distribution-leads-automatique/09-SUMMARY.md` ≥ 60 lignes.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md, ROADMAP.md, STATE.md cohérents. 09-SUMMARY.md écrit avec 3 décisions clés D-11/D-12/D-13. Phase 10 débloquée.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint Laurent — Validation finale Phase 9</name>
  <what-built>
    Phase 9 — Distribution leads automatique. 4 plans d'exécution + 1 plan de bookkeeping.

    Livré :
    - Migration Prisma `phase09_distribution` (Notification model + Lead.wonAt + 3 toggles Tenant)
    - 4 server actions Lead (createLead / reassignLead / updateLeadStatus / updateLeadDistributionConfig)
    - Helper `notifyLeadAssigned` (Notification + Email + AuditLog conditionnés par 2 toggles)
    - 3 pages UI (`/app/leads/charge` ADMIN+MANAGER, `/app/leads/[id]`, `/app/leads/new`)
    - Page paramètres ADMIN `/app/parametres/distribution-leads` (3 toggles)
    - Cloche TopBar étendue : kind `lead.assigned` user-scoped + `markNotificationRead` au clic
    - Sidebar : 2 nouvelles entrées (Vue de charge ADMIN+MANAGER / Distribution leads ADMIN)
    - 9 fichiers tests Vitest (≥ 25 nouveaux tests verts)
  </what-built>
  <how-to-verify>
    **Étape 1 — Smoke build :**
    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files"
    pnpm dev:full
    # Attendre le boot complet (auto-clean .next inclus)
    ```

    **Étape 2 — Smoke DevTools (5 flows) :** suivre `.planning/phases/09-distribution-leads-automatique/09-SMOKE.md` étape par étape :
    1. Création de lead avec auto-assignation
    2. Réassignation manuelle (bouton sur fiche lead)
    3. Toggles distribution leads (désactiver auto, créer lead → owner null)
    4. Vue de charge (ADMIN/MANAGER seulement, COMMERCIAL bloqué)
    5. Transition statut → WON (vérifier wonAt + KPI charge incrémenté)

    **Étape 3 — Bonus visuels :**
    - Sidebar visible en ADMIN : présence "Vue de charge" sous Leads + "Distribution leads" sous Configuration
    - Sidebar visible en COMMERCIAL : "Vue de charge" absente, "Distribution leads" absente
    - Page Historique `/app/parametres/historique` : nouvelles rows `leads.auto_assigned` / `leads.reassigned` / `leads.distribution_config` filtrables

    **Étape 4 — Email dry-run :** vérifier en console serveur (`pnpm dev:full` terminal) que les emails de notification ont bien été loggués lors de Flow 1 + 2 avec subject "Nouveau lead à traiter — {prospectName}".

    Si tout OK → typer "approuvé Phase 9" pour fermer.
    Si des frictions → décrire (ex. "le camembert ne s'affiche pas sur Safari", "le toggle email reste actif quand auto est OFF") → orchestrator créera des gap closure plans (`/gsd:plan-phase 9 --gaps`).
  </how-to-verify>
  <resume-signal>Type "approuvé Phase 9" ou décrire les frictions à fixer.</resume-signal>
</task>

</tasks>

<verification>
- Suite Vitest verte (`pnpm --filter @qualiof/web test --run`).
- Build Next.js clean (`pnpm --filter @qualiof/web build`).
- tsc clean (`pnpm --filter @qualiof/web exec tsc --noEmit`).
- REQUIREMENTS.md / ROADMAP.md / STATE.md cohérents et synchronisés.
- 09-SMOKE.md + 09-SUMMARY.md écrits.
- Validation Laurent positive (checkpoint Task 3).
</verification>

<success_criteria>
- LEAD-01 et LEAD-02 marqués DONE dans REQUIREMENTS.md avec preuves.
- ROADMAP.md Phase 9 marquée Complete + 5/5 plans listés.
- STATE.md : progress completed_phases=5 / completed_plans=24 ; current focus = Phase 10.
- 09-SMOKE.md décrit 5 flows DevTools manuels.
- 09-SUMMARY.md consolide les 4 plans précédents avec décisions D-11/D-12/D-13.
- Checkpoint Laurent passé.
</success_criteria>

<output>
After completion, the phase 9 is closed. `/gsd:plan-phase 10` peut démarrer.
Pas de nouveau SUMMARY pour ce plan (09-05 EST le bookkeeping qui écrit 09-SUMMARY.md global).
</output>
