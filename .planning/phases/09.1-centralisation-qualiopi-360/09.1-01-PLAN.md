---
phase: 09.1-centralisation-qualiopi-360
plan: 01
type: execute
wave: 1
depends_on: []
parallelization: false
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/<TIMESTAMP>_phase091_participant_doc_status/migration.sql
  - packages/shared/src/schemas/qualiopi-matrix.ts
  - packages/shared/src/index.ts
  - apps/web/src/lib/doc-scope.ts
  - apps/web/src/lib/derive-cell-state.ts
  - apps/web/src/lib/document-audit.ts
  - apps/web/src/lib/__tests__/doc-scope.test.ts
  - apps/web/src/lib/__tests__/derive-cell-state.test.ts
  - apps/web/src/lib/__tests__/document-audit.test.ts
autonomous: true
requirements:
  - CENTRAL-01
  - CENTRAL-05
must_haves:
  truths:
    - "`SessionParticipant.docStatus` Json? exists in DB (additive migration applied)"
    - "`deriveCellState` returns GENERATED for PROGRAMME when productDoc exists (Bug P0 anti-régression)"
    - "`deriveCellState` returns MANUAL_OK + warning='no_proof' when markedOkWithoutUpload=true (D-01)"
    - "`DOC_SCOPE` (16 DocTypes table, D-04) exposes per-doc PDF scope (session|participant|product) and matrix lane (matrix|session-only)"
    - "`logDocumentEvent` writes AuditLog row with entity='Document', actions namespaced documents.*"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "SessionParticipant.docStatus Json? column"
      contains: "docStatus"
    - path: "packages/shared/src/schemas/qualiopi-matrix.ts"
      provides: "DocStatusState enum + DocStatusEntrySchema + DocStatusMapSchema (Zod)"
      exports:
        - "DocStatusState"
        - "DocStatusEntrySchema"
        - "DocStatusMapSchema"
    - path: "apps/web/src/lib/doc-scope.ts"
      provides: "Locked D-04 table (16 DocTypes) + DOC_TYPE_TO_CLOSURE_KIND + DOC_TYPE_TO_PED_KIND + DOC_TYPE_LABELS"
      exports:
        - "MATRIX_DOC_TYPES"
        - "SESSION_ONLY_DOC_TYPES"
        - "DOC_TYPE_LABELS"
    - path: "apps/web/src/lib/derive-cell-state.ts"
      provides: "Pure function deriveCellState (priority: participant > pedagogical > session > product)"
      exports:
        - "deriveCellState"
        - "buildMatrixData"
    - path: "apps/web/src/lib/document-audit.ts"
      provides: "logDocumentEvent helper (entity='Document', clone strict logLeadEvent)"
      exports:
        - "logDocumentEvent"
  key_links:
    - from: "deriveCellState"
      to: "MATRIX_DOC_TYPES / DOC_TYPE_TO_CLOSURE_KIND"
      via: "doc-scope import"
      pattern: "import.*doc-scope"
    - from: "logDocumentEvent"
      to: "prisma.auditLog.create"
      via: "@qualiof/db import"
      pattern: "auditLog\\.create"
---

<objective>
Foundation Wave 0 : poser les briques BDD, schémas Zod, helpers purs et stubs de test qui supportent toutes les vagues suivantes. Aucun composant UI, aucune server action mutante exportée — pure plomberie additive.

**Purpose:** Eviter les patches itératifs sur le modèle de données pendant les Waves 1/2. Tout ce qui suit lit `SessionParticipant.docStatus` ; `deriveCellState` est le pivot du Bug P0 résolu structurellement.
**Output:** 1 migration Prisma additive + 1 schema Zod + 3 helpers purs + 3 fichiers de tests Vitest (≥ 8 tests sur deriveCellState seul).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-centralisation-qualiopi-360/09.1-CONTEXT.md
@.planning/phases/09.1-centralisation-qualiopi-360/09.1-RESEARCH.md
@.planning/phases/09.1-centralisation-qualiopi-360/09.1-VALIDATION.md
@.planning/phases/09.1-centralisation-qualiopi-360/09.1-UI-SPEC.md
@packages/db/prisma/schema.prisma
@apps/web/src/lib/audit-log.ts
@apps/web/src/lib/closure/types.ts

<interfaces>
<!-- Contracts extraits du codebase pour éviter exploration -->

From `apps/web/src/lib/audit-log.ts` (pattern à cloner strictement) :
```typescript
export async function logLeadEvent(opts: {
  tenantId: string;
  actorUserId: string | null;
  targetLeadId: string;
  action: string;
  diff?: Diff | Record<string, unknown>;
}): Promise<void> {
  await prisma.auditLog.create({
    data: {
      tenantId: opts.tenantId,
      userId: opts.actorUserId,
      entity: 'Lead',
      entityId: opts.targetLeadId,
      action: opts.action,
      diff: (opts.diff ?? {}) as never,
    },
  });
}
```

From `packages/db/prisma/schema.prisma` lines 536-616 (SessionParticipant model) — patch additif uniquement, **ne pas toucher** les booléens legacy (`conventionSigned`, `programmeDelivered`, etc. — shadowed quand `docStatus[type]` présent).

From `apps/web/src/lib/closure/types.ts` :
```typescript
export const CLOSURE_DOC_KINDS = [...]; // ATTESTATION, CERTIFICAT, QCM, GRILLE_OBS, ANALYSE_BESOIN, POSITIONNEMENT, SATISFACTION_CHAUD, SATISFACTION_FROID, EMARGEMENT (DEROULE_PEDA exclu — niveau produit depuis 05/05/2026)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration Prisma additive + schémas Zod qualiopi-matrix</name>
  <read_first>
    - packages/db/prisma/schema.prisma (focus L536-616 — SessionParticipant)
    - packages/db/prisma/migrations/20260516160839_phase09_distribution/migration.sql (référence convention nommage)
    - packages/shared/src/schemas/lead.ts (référence structure Zod Phase 9)
    - packages/shared/src/index.ts (vérifier exports actuels)
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-RESEARCH.md §"Decision A — Data Model JSON Shape"
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-CONTEXT.md §D-07, D-03, D-01
  </read_first>
  <behavior>
    - Test 1: `DocStatusState.parse('GENERATED')` retourne 'GENERATED'
    - Test 2: `DocStatusState.parse('INVALID')` lève ZodError
    - Test 3: `DocStatusEntrySchema.parse({ state: 'MANUAL_OK', uploadedSignedPdfKey: 'signed/tenant1/SES-001/abc.pdf', uploadedSignedAt: '2026-05-18T10:00:00.000Z', updatedAt: '2026-05-18T10:00:00.000Z' })` passe
    - Test 4: `DocStatusEntrySchema.parse({ state: 'MANUAL_OK', markedOkWithoutUpload: true, note: 'Justif dérog', updatedAt: '...' })` passe
    - Test 5: `DocStatusMapSchema.parse({ PROGRAMME: { state: 'GENERATED', updatedAt: '...' }, CONVENTION: { state: 'MANUAL_OK', ... } })` passe
    - Test 6: `DocStatusEntrySchema.parse({ state: 'MANUAL_OK', note: 'a'.repeat(501) })` lève (max 500 chars)
  </behavior>
  <action>
    1. **Migration Prisma** : éditer `packages/db/prisma/schema.prisma` model `SessionParticipant` (autour ligne 600 avant les `@@index` finaux). Ajouter **uniquement** :

```prisma
  // Phase 9.1 — Centralisation Qualiopi 360°.
  // Map: { [docType: DocType string]: { state, uploadedSignedPdfKey?, ... } }
  // Voir packages/shared/src/schemas/qualiopi-matrix.ts pour la shape exacte.
  // Source de vérité des statuts manuels par-participant (Bug P0 résolu :
  // 1 PDF Programme stocké session-wide via Document.entityType='product',
  // N statuts par-participant dérivés ici).
  // Les booléens legacy (conventionSigned, programmeDelivered, ...) restent
  // pour rétrocompat — shadowed quand docStatus[type] est présent.
  docStatus       Json?
```

   Ne PAS ajouter d'index (D-07 + RESEARCH Decision A : cardinality faible, pas de scan filtré).

   Générer la migration : `pnpm --filter @qualiof/db exec prisma migrate dev --name phase091_participant_doc_status`. Vérifier que le SQL généré est exactement `ALTER TABLE "SessionParticipant" ADD COLUMN "docStatus" JSONB;` (additive pure, sans backfill).

   Si la commande prompt pour reset DB, **refuser** et vérifier l'état de la base (ce doit être un add column trivial). Si conflit de timestamp avec une migration existante, renommer en suivant le pattern Phase 9 : `<YYYYMMDDhhmmss>_phase091_participant_doc_status`.

  2. **Schémas Zod** : créer `packages/shared/src/schemas/qualiopi-matrix.ts` (clone strict shape RESEARCH Decision A) :

```typescript
import { z } from 'zod';

/** 3 états figés par D-03 CONTEXT.md. */
export const DocStatusState = z.enum(['GENERATED', 'MANUAL_OK', 'MISSING']);
export type DocStatusState = z.infer<typeof DocStatusState>;

/**
 * Entrée pour un DocType donné dans `SessionParticipant.docStatus`.
 *
 * - `uploadedSignedPdfKey` : clé MinIO (pas FK Document, cohérent ribKey).
 * - `markedOkWithoutUpload` : cas dérogatoire D-01 (pastille orange ⚠).
 * - `note` : justification libre admin (raison dérogation, ≤ 500 chars).
 */
export const DocStatusEntrySchema = z.object({
  state: DocStatusState,
  uploadedSignedPdfKey: z.string().optional(),
  uploadedSignedAt: z.string().datetime().optional(),
  uploadedByUserId: z.string().uuid().optional(),
  markedOkWithoutUpload: z.boolean().optional(),
  note: z.string().max(500).optional(),
  updatedAt: z.string().datetime(),
});
export type DocStatusEntry = z.infer<typeof DocStatusEntrySchema>;

export const DocStatusMapSchema = z.record(z.string(), DocStatusEntrySchema);
export type DocStatusMap = z.infer<typeof DocStatusMapSchema>;
```

   Exporter depuis `packages/shared/src/index.ts` (ajouter `export * from './schemas/qualiopi-matrix';`).

  3. **Tests Vitest** : créer `packages/shared/src/schemas/__tests__/qualiopi-matrix.test.ts` (suivre pattern Phase 9 `lead.test.ts`) avec 6 tests minimum couvrant le `<behavior>` ci-dessus. Format `*.test.ts`, pas de `describe.skip`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/db exec prisma format && pnpm --filter @qualiof/shared test --run schemas/__tests__/qualiopi-matrix.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "docStatus.*Json" packages/db/prisma/schema.prisma` matche 1 ligne dans le model SessionParticipant
    - `ls packages/db/prisma/migrations/*phase091_participant_doc_status*/migration.sql` existe
    - `grep "ADD COLUMN.*docStatus.*JSONB" packages/db/prisma/migrations/*phase091*/migration.sql` matche
    - `grep -c "DocStatusState\|DocStatusEntrySchema\|DocStatusMapSchema" packages/shared/src/schemas/qualiopi-matrix.ts` ≥ 3
    - `grep "qualiopi-matrix" packages/shared/src/index.ts` matche (export ajouté)
    - Suite Vitest `qualiopi-matrix.test.ts` : ≥ 6 tests verts
    - `tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    Migration Prisma additive `docStatus JSONB` appliquée (status migrate dev OK). Schémas Zod `DocStatusState`/`DocStatusEntrySchema`/`DocStatusMapSchema` exportés depuis `@qualiof/shared`. ≥ 6 tests unitaires verts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helpers purs doc-scope + derive-cell-state + tests anti-régression Bug P0</name>
  <read_first>
    - apps/web/src/lib/closure/types.ts (CLOSURE_DOC_KINDS — savoir ce qu'on map)
    - packages/db/prisma/schema.prisma §enum DocType (L781-804) + enum PedagogicalKind
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-RESEARCH.md §"Finding 1 — Mapping..." + §"Decision A — deriveCellState"
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-CONTEXT.md §D-04 (table figée 16 DocTypes)
    - apps/web/src/lib/funder-codes.ts (référence helper pur Phase 6 — pattern lookup map)
  </read_first>
  <behavior>
    - Test 1: `deriveCellState('PROGRAMME', { docStatus: null }, new Map(), new Map([['PROGRAMME', { id: 'doc1', pdfUrl: '...' }]]), ...)` → `{ state: 'GENERATED', pdfRef: { kind: 'productDoc', id: 'doc1' } }` **(Bug P0 anti-régression)**
    - Test 2: `deriveCellState('CONVENTION', { docStatus: { CONVENTION: { state: 'MANUAL_OK', uploadedSignedPdfKey: 'k', updatedAt: '...' } } }, ...)` → `{ state: 'MANUAL_OK' }` (priorité au manuel)
    - Test 3: `deriveCellState('CONVENTION', { docStatus: { CONVENTION: { state: 'MANUAL_OK', markedOkWithoutUpload: true, updatedAt: '...' } } }, ...)` → `{ state: 'MANUAL_OK', warning: 'no_proof' }` (dérogation D-01)
    - Test 4: `deriveCellState('CERTIFICAT_REALISATION', { docStatus: null }, mapWithParticipantDoc, ...)` → `{ state: 'GENERATED', pdfRef: { kind: 'document', id: ... } }`
    - Test 5: `deriveCellState('EVALUATION_ACQUIS', { docStatus: null }, new Map(), new Map(), new Map(), mapWithAsset)` → `{ state: 'GENERATED', pdfRef: { kind: 'asset', id: ... } }`
    - Test 6: `deriveCellState('CONVENTION', { docStatus: null }, new Map(), new Map(), new Map(), new Map())` → `{ state: 'MISSING' }`
    - Test 7: priorité strict participant > asset > session > product (test isoler avec 4 maps non vides)
    - Test 8: `MATRIX_DOC_TYPES.length === 14` (D-04 + 2 lignes satisfaction chaud/froid = 14 selon UI-SPEC Open Question 1 résolue)
    - Test 9: `SESSION_ONLY_DOC_TYPES.length === 3` (DEROULE_PEDAGOGIQUE, GRILLE_OBS_SESSION, CHECKLIST_FORMATION)
    - Test 10: `DOC_TYPE_TO_CLOSURE_KIND['CERTIFICAT_REALISATION'] === 'CERTIFICAT'` (mapping cohérent)
  </behavior>
  <action>
    1. **Créer `apps/web/src/lib/doc-scope.ts`** :

```typescript
/**
 * Table figée D-04 (CONTEXT.md) : 14 DocTypes affichés dans la matrice
 * participants + 3 DocTypes session-only. Ordre = ordre d'affichage colonnes
 * matrice (Phase 9.1 UI-SPEC §Surface 1 column header glyph legend).
 *
 * AGEFICE est inclus mais affiché conditionnellement : NA si sponsorOrg.opcoCode
 * !== 'AGEFICE' (voir UI-SPEC §Edge Cases).
 *
 * Satisfaction chaud + froid = 2 lignes distinctes (UI-SPEC Open Question 1
 * résolue : 2 docs Qualiopi distincts J+1 vs J+90).
 */
export const MATRIX_DOC_TYPES = [
  'PROGRAMME',
  'CONVENTION',
  'CONVOCATION',
  'PRE_ACCORD_OPCO',
  'SUPPORT_PEDAGOGIQUE',
  'EMARGEMENT',
  'CERTIFICAT_REALISATION',
  'ATTESTATION_FIN',
  'EVALUATION_ACQUIS',
  'ASSIDUITE',
  'SATISFACTION_CHAUD',
  'SATISFACTION_FROID',
  'POSITIONNEMENT',
  'ANALYSE_BESOIN',
  // AGEFICE conditionnel — ajouté côté UI si applicable, hors core list
] as const;
export type MatrixDocType = (typeof MATRIX_DOC_TYPES)[number];

export const SESSION_ONLY_DOC_TYPES = [
  'DEROULE_PEDAGOGIQUE',
  'GRILLE_OBS_SESSION',
  'CHECKLIST_FORMATION',
] as const;
export type SessionOnlyDocType = (typeof SESSION_ONLY_DOC_TYPES)[number];

/** Labels FR — short = 2 chars header (UI-SPEC glyph legend), long = aria-label/tooltip. */
export const DOC_TYPE_LABELS: Record<string, { short: string; long: string }> = {
  PROGRAMME: { short: 'Pg', long: 'Programme de formation' },
  CONVENTION: { short: 'Cv', long: 'Convention de formation' },
  CONVOCATION: { short: 'Cn', long: 'Convocation' },
  PRE_ACCORD_OPCO: { short: 'Op', long: 'Pré-accord OPCO' },
  SUPPORT_PEDAGOGIQUE: { short: 'Sp', long: 'Supports pédagogiques' },
  EMARGEMENT: { short: 'Em', long: 'Émargement' },
  CERTIFICAT_REALISATION: { short: 'Ce', long: 'Certificat de réalisation' },
  ATTESTATION_FIN: { short: 'At', long: 'Attestation' },
  EVALUATION_ACQUIS: { short: 'QC', long: 'Évaluation des acquis (QCM)' },
  ASSIDUITE: { short: 'As', long: 'Assiduité' },
  SATISFACTION_CHAUD: { short: 'Sc', long: 'Satisfaction (à chaud)' },
  SATISFACTION_FROID: { short: 'Sf', long: 'Satisfaction (à froid)' },
  POSITIONNEMENT: { short: 'Po', long: 'Positionnement' },
  ANALYSE_BESOIN: { short: 'An', long: 'Analyse des besoins' },
  AGEFICE: { short: 'Ag', long: 'Fiche AGEFICE' },
  DEROULE_PEDAGOGIQUE: { short: 'Dp', long: 'Déroulé pédagogique' },
  GRILLE_OBS_SESSION: { short: 'Go', long: "Grille d'observation formateur" },
  CHECKLIST_FORMATION: { short: 'Ck', long: 'Checklist formation' },
};

/** Map MatrixDocType → ClosureDocKind (worker BullMQ). null = pas de generator BullMQ. */
export const DOC_TYPE_TO_CLOSURE_KIND: Record<string, string | null> = {
  CERTIFICAT_REALISATION: 'CERTIFICAT',
  ATTESTATION_FIN: 'ATTESTATION',
  EVALUATION_ACQUIS: 'QCM',
  EMARGEMENT: 'EMARGEMENT',
  ANALYSE_BESOIN: 'ANALYSE_BESOIN',
  POSITIONNEMENT: 'POSITIONNEMENT',
  SATISFACTION_CHAUD: 'SATISFACTION_CHAUD',
  SATISFACTION_FROID: 'SATISFACTION_FROID',
  ASSIDUITE: null, // upload manuel only (UI-SPEC Open Question 2 résolue)
  PROGRAMME: null, // generator dédié produit-level
  CONVENTION: null, // generator dédié synchrone
  CONVOCATION: null, // generator dédié synchrone
  PRE_ACCORD_OPCO: null,
  SUPPORT_PEDAGOGIQUE: null,
  AGEFICE: null, // generator dédié synchrone
};

/** Map MatrixDocType → PedagogicalKind (lecture asset). */
export const DOC_TYPE_TO_PED_KIND: Record<string, string | null> = {
  EVALUATION_ACQUIS: 'QCM',
  EMARGEMENT: 'EMARGEMENT',
  ANALYSE_BESOIN: 'ANALYSE_BESOIN',
  POSITIONNEMENT: 'POSITIONNEMENT',
  SATISFACTION_CHAUD: 'SATISFACTION_CHAUD',
  SATISFACTION_FROID: 'SATISFACTION_FROID',
  GRILLE_OBS_SESSION: 'GRILLE_OBS',
  DEROULE_PEDAGOGIQUE: 'DEROULE',
};
```

  2. **Créer `apps/web/src/lib/derive-cell-state.ts`** (clone exact RESEARCH Decision A) :

```typescript
import type { DocStatusMap, DocStatusState } from '@qualiof/shared';

export type CellPdfRef = {
  kind: 'document' | 'asset' | 'productDoc' | 'sessionDoc';
  id: string;
};

export type CellState =
  | { state: 'GENERATED'; pdfRef: CellPdfRef }
  | { state: 'MANUAL_OK'; pdfRef?: CellPdfRef; warning?: 'no_proof' }
  | { state: 'MISSING' }
  | { state: 'NA' };

/**
 * Dérive l'état d'une cellule matrice.
 *
 * Priorité (D-07 + Bug P0 anti-régression) :
 *   1. participant.docStatus[docType] (manuel — wins toujours)
 *   2. participantDocs (Document.entityType='participant', personId match)
 *   3. pedagogicalAssets (PedagogicalAsset participant + kind)
 *   4. sessionDocs (Document.entityType='session')
 *   5. productDocs (Document.entityType='product' — PROGRAMME, etc.) **← Bug P0**
 *   6. MISSING
 *
 * Cas dérogatoire D-01 : `MANUAL_OK + markedOkWithoutUpload=true` → warning='no_proof'.
 */
export function deriveCellState(
  docType: string,
  participant: { docStatus: DocStatusMap | null | undefined },
  participantDocs: Map<string, { id: string }>,
  productDocs: Map<string, { id: string }>,
  sessionDocs: Map<string, { id: string }>,
  pedagogicalAssets: Map<string, { id: string }>,
): CellState {
  const manual = participant.docStatus?.[docType];

  // Cas dérogatoire D-01 : coché OK sans upload → pastille orange
  if (manual?.state === 'MANUAL_OK' && manual.markedOkWithoutUpload) {
    return { state: 'MANUAL_OK', warning: 'no_proof' };
  }
  if (manual?.state === 'MANUAL_OK') {
    return { state: 'MANUAL_OK' };
  }

  const partDoc = participantDocs.get(docType);
  if (partDoc) return { state: 'GENERATED', pdfRef: { kind: 'document', id: partDoc.id } };

  const asset = pedagogicalAssets.get(docType);
  if (asset) return { state: 'GENERATED', pdfRef: { kind: 'asset', id: asset.id } };

  const sessDoc = sessionDocs.get(docType);
  if (sessDoc) return { state: 'GENERATED', pdfRef: { kind: 'sessionDoc', id: sessDoc.id } };

  // **Bug P0 anti-régression** : PROGRAMME a Document.entityType='product'
  // partagé entre tous les participants. La cellule lit ce ref partagé.
  const prodDoc = productDocs.get(docType);
  if (prodDoc) return { state: 'GENERATED', pdfRef: { kind: 'productDoc', id: prodDoc.id } };

  return { state: 'MISSING' };
}
```

  3. **Créer les fichiers de tests** :
     - `apps/web/src/lib/__tests__/doc-scope.test.ts` : tests 8/9/10 du `<behavior>` + sanity sur DOC_TYPE_LABELS keys (chaque MATRIX_DOC_TYPES doit avoir un label).
     - `apps/web/src/lib/__tests__/derive-cell-state.test.ts` : tests 1-7 du `<behavior>`. **Test 1 doit avoir `// Bug P0 anti-régression` en commentaire** (audit grep-able). Suivre pattern Phase 9 `lead-load-stats.test.ts` (vitest node, pas de testing-library).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run lib/__tests__/doc-scope.test.ts lib/__tests__/derive-cell-state.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MATRIX_DOC_TYPES\|SESSION_ONLY_DOC_TYPES" apps/web/src/lib/doc-scope.ts` ≥ 2
    - `grep -c "^export" apps/web/src/lib/doc-scope.ts` ≥ 5 (5 exports nommés)
    - `grep "Bug P0 anti-régression" apps/web/src/lib/__tests__/derive-cell-state.test.ts` matche (commentaire requis)
    - `grep "kind: 'productDoc'" apps/web/src/lib/derive-cell-state.ts` matche
    - Suite Vitest `doc-scope.test.ts` + `derive-cell-state.test.ts` : ≥ 10 tests verts au total
    - `tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    `doc-scope.ts` expose 5 constantes/maps + labels FR alignés UI-SPEC. `derive-cell-state.ts` retourne la priorité documentée (test 1 = Bug P0 anti-régression). ≥ 10 tests Vitest verts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Helper logDocumentEvent + tests + extension audit-log.ts</name>
  <read_first>
    - apps/web/src/lib/audit-log.ts (toute la file — pour cloner pattern logLeadEvent)
    - apps/web/src/lib/__tests__/audit-log.test.ts (référence test pattern)
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-RESEARCH.md §"Decision B — Server Actions List" + §"Open Question 5 — AuditLog entity value"
    - .planning/STATE.md §"D-12 Convention AuditLog Phase 9" (référence convention namespaced)
  </read_first>
  <behavior>
    - Test 1: `logDocumentEvent({ tenantId, actorUserId: 'u1', targetEntityId: 'p1', action: 'documents.status_change', diff: { PROGRAMME: { before: null, after: { state: 'MANUAL_OK', updatedAt: '...' } } } })` appelle `prisma.auditLog.create` avec `entity: 'Document'`, `entityId: 'p1'`, `action: 'documents.status_change'`
    - Test 2: `logDocumentEvent` sans `diff` passe `{}` à prisma (pas d'erreur null)
    - Test 3: `actorUserId: null` est accepté (cas system, ex. worker BullMQ async)
    - Test 4: pas de no-op sur diff vide (cohérent logLeadEvent — chaque doc event est tracé)
  </behavior>
  <action>
    Créer **un nouveau module** `apps/web/src/lib/document-audit.ts` (NE PAS éditer `audit-log.ts` pour rester isolé en cas de rollback). Clone strict de `logLeadEvent` :

```typescript
/**
 * Helper AuditLog Phase 9.1 — entity='Document', actions namespacées documents.*.
 *
 * Conventions (RESEARCH Decision B + Open Question 5 résolue) :
 *  - 'documents.regenerate'       → re-gen ciblée (ou batch via closure-pack)
 *  - 'documents.upload_signed'    → upload PDF signé scanné
 *  - 'documents.status_change'    → mark MANUAL_OK / markedOkWithoutUpload
 *  - 'documents.delete'           → suppression Document / asset
 *
 * `targetEntityId` = participantId (le participant porte le statut, le doc
 * est l'objet de l'action). entity='Document' (générique, pas 'SessionParticipant'
 * pour rester sémantique côté UI AuditLog Phase 8).
 *
 * Pattern clone-strict `logLeadEvent` (Phase 9 D-Phase9-H) — pas de no-op sur
 * diff vide, actorUserId nullable pour cas system (worker async).
 */
import { prisma } from '@qualiof/db';

export type Diff = Record<string, { before: unknown; after: unknown } | unknown>;

export async function logDocumentEvent(opts: {
  tenantId: string;
  actorUserId: string | null;
  targetEntityId: string;
  action: string;
  diff?: Diff | Record<string, unknown>;
}): Promise<void> {
  await prisma.auditLog.create({
    data: {
      tenantId: opts.tenantId,
      userId: opts.actorUserId,
      entity: 'Document',
      entityId: opts.targetEntityId,
      action: opts.action,
      diff: (opts.diff ?? {}) as never,
    },
  });
}
```

  Créer `apps/web/src/lib/__tests__/document-audit.test.ts` (Vitest + `vi.mock('@qualiof/db', ...)` pattern Phase 9 `audit-log.test.ts`). 4 tests minimum (un par item `<behavior>`).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test --run lib/__tests__/document-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "entity: 'Document'" apps/web/src/lib/document-audit.ts` matche
    - `grep "actorUserId: string | null" apps/web/src/lib/document-audit.ts` matche
    - `grep -c "documents\\." apps/web/src/lib/document-audit.ts` ≥ 1 (commentaire JSDoc namespacing)
    - Suite Vitest `document-audit.test.ts` : ≥ 4 tests verts
    - Aucune modification à `apps/web/src/lib/audit-log.ts` (isolation D-Phase9-H — `git diff apps/web/src/lib/audit-log.ts` vide)
  </acceptance_criteria>
  <done>
    `logDocumentEvent` exporté avec signature `{ tenantId, actorUserId: string | null, targetEntityId, action, diff? }`. ≥ 4 tests Vitest verts. `audit-log.ts` (Phase 9) inchangé.
  </done>
</task>

</tasks>

<verification>
- Migration Prisma additive appliquée + visible dans `packages/db/prisma/migrations/`
- 5 fichiers production créés : `qualiopi-matrix.ts` (shared), `doc-scope.ts`, `derive-cell-state.ts`, `document-audit.ts`
- 3 fichiers tests créés : `qualiopi-matrix.test.ts` (shared), `doc-scope.test.ts`, `derive-cell-state.test.ts`, `document-audit.test.ts`
- `pnpm --filter @qualiof/web test --run` : Phase 9 baseline 239/239 + nouveaux tests ≥ 20 supplémentaires
- `pnpm --filter @qualiof/shared test --run` : Phase 9 baseline + ≥ 6 nouveaux tests
- `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
- 0 nouvelle dépendance installée (RESEARCH §Standard Stack)
</verification>

<success_criteria>
- Wave 0 terminée — Wave 1 (Plan 02) débloquée
- Bug P0 anti-régression couvert par test unitaire ciblé (`derive-cell-state.test.ts` Test 1)
- 0 server action mutante exportée (frontière respectée — Plan 02 prend le relais)
- Convention AuditLog `documents.*` posée (action namespacée prête pour Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-centralisation-qualiopi-360/09.1-01-SUMMARY.md` avec:
- migration timestamp exact (`grep -l phase091 packages/db/prisma/migrations/*`)
- liste des exports `@qualiof/shared` ajoutés
- nombre exact de tests verts par fichier
- diff lignes schema.prisma (avant/après)
- mention explicite "Bug P0 anti-régression couvert" + ref test
</output>
