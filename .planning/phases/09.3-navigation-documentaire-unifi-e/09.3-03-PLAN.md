---
phase: 09.3-navigation-documentaire-unifi-e
plan: 03
type: execute
wave: 2
depends_on: ["09.3-01"]
files_modified:
  - apps/web/src/components/docs/unified-docs-list.tsx
  - apps/web/src/app/app/apprenants/[id]/page.tsx
  - apps/web/src/components/produits/tabs/product-docs-tab.tsx
  - apps/web/src/app/app/produits/[id]/page.tsx
  - apps/web/src/components/docs/__tests__/unified-docs-list.smoke.test.ts
autonomous: false
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "Onglet Docs apprenant affiche docs session + produit + participant + liens tenant (≤2 clics)"
    - "Bloc Docs produit agrège programme/déroulé + docs apprenants du produit"
    - "Liens docs tenant (CGV/RI) croisés depuis les fiches apprenant et produit"
    - "Un doc usedStub=true affiche un état distinct (jamais conforme) vs present vs missing"
  artifacts:
    - path: "apps/web/src/components/docs/unified-docs-list.tsx"
      provides: "Rendu réutilisable d'une liste UnifiedDoc[] avec badge present/stub/missing + liens cross-nav"
      exports: ["UnifiedDocsList"]
      min_lines: 60
    - path: "apps/web/src/components/produits/tabs/product-docs-tab.tsx"
      provides: "Onglet Docs produit (programme/déroulé + docs apprenants agrégés)"
      exports: ["ProductDocsTab"]
  key_links:
    - from: "apps/web/src/components/docs/unified-docs-list.tsx"
      to: "apps/web/src/lib/docs/resolve-docs.ts"
      via: "type UnifiedDoc + champ usedStub"
      pattern: "usedStub"
    - from: "apps/web/src/app/app/apprenants/[id]/page.tsx"
      to: "rawSessionDocs/rawProductDocs déjà chargés"
      via: "rendu de l'onglet documents"
      pattern: "UnifiedDocsList"
---

<objective>
Brancher le résolveur sur les 3 surfaces UI (NAV-02, règle ≤2 clics D-09.3-06) et rendre `usedStub` visible (NAV-03, blocker T4/ind.11) : (a) onglet Docs fiche apprenant — AFFICHER les docs session+produit DÉJÀ chargés (rawSessionDocs/rawProductDocs) + liens tenant ; (b) bloc Docs fiche produit agrégeant programme/déroulé + docs apprenants ; (c) liens docs tenant (CGV/RI) croisés.

Purpose: La donnée est déjà au bon endroit (la fiche apprenant charge déjà rawSessionDocs/rawProductDocs sans les afficher → surtout du RENDU, Phase 3 moins chère que prévu). Un doc stub ne doit JAMAIS passer pour conforme devant l'auditeur.
Output: composant UnifiedDocsList réutilisable (badge present/stub/missing + cross-nav) + onglet Docs apprenant enrichi + onglet Docs produit. Checkpoint visuel humain à la fin (règle ≤2 clics vérifiable seulement à l'écran).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.3-navigation-documentaire-unifi-e/09.3-CONTEXT.md
@.planning/audit/MATRICE-NAVIGATION-DOCS.md
@.planning/phases/09.3-navigation-documentaire-unifi-e/09.3-01-SUMMARY.md

<interfaces>
<!-- Produit du Plan 09.3-01 (consommé ici). -->
resolve-docs.ts exporte : `resolveDocs`, `UnifiedDoc`, `DocSourceTable`.
get-docs-for.ts exporte : `getDocsForPerson(personId, tenantId)`, `getDocsForSession(sessionId, tenantId)`, `getDocsForProduct(productId, tenantId)` → Promise<UnifiedDoc[]>.
UnifiedDoc porte : sourceTable, sourceId, docType, qualiopiIndicator, anchor (level participant|session|product|tenant), generatedAt, href (null=preuve à produire), status ('present'|'stub'|'missing'), usedStub.

<!-- Composant existant à RÉUTILISER (ne pas réécrire la pastille). CONTRAT RÉEL vérifié 2026-06-10. -->
DocStatusBadge (apps/web/src/components/sessions/qualiopi-matrix/doc-status-badge.tsx). Contrat EXACT :
```ts
export interface DocStatusBadgeProps {
  state: DocStatusState | 'NA' | 'RUNNING';   // DocStatusState (@qualiof/shared) = 'GENERATED' | 'MANUAL_OK' | 'MISSING'
  warning?: 'no_proof';                         // LITTÉRAL, pas un booléen. PAS de prop `title`.
  label: string;                                // sert aria-label + title interne du badge
  hasUploadedPdf?: boolean;
}
```
ATTENTION : `warning` est le littéral `'no_proof'`, PAS `true`. Il n'existe AUCUNE prop `title` (le title est calculé en interne par le badge à partir de `label`).
Mapping UnifiedDoc.status → props :
  - present → `state='GENERATED'` (emerald)
  - stub    → `state='MANUAL_OK'` + `warning='no_proof'` (amber, AlertTriangle). Le texte « non conforme » s'affiche via un `<span>` adjacent OU via `label`, JAMAIS via `title`.
  - missing → `state='MISSING'` (red)

<!-- Points d'ancrage UI RÉELS (vérifiés branche cloud-migration 2026-06-10). -->
apprenants/[id]/page.tsx : `import { LearnerTabs }` ligne 15 ; rawProductDocs/rawSessionDocs chargés dans le Promise.all ligne 113 ; onglet rendu via `{tab === 'documents' && (() => {` ligne 769 (bloc « Documents générés ({documents.length}) » à ENRICHIR, pas réécrire).
produits/[id]/page.tsx + components/produits/tabs/product-tabs.tsx : onglets URL-state ?tab=stats|sessions|apprenants|programme. Ajouter un onglet/bloc Docs.
DOC_TYPE_LABELS (apps/web/src/lib/doc-scope.ts:43) : libellés FR { short, long } par DocType — réutiliser pour l'affichage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NAV-03 — composant UnifiedDocsList (rendu + badge present/stub/missing + cross-nav)</name>
  <files>apps/web/src/components/docs/unified-docs-list.tsx, apps/web/src/components/docs/__tests__/unified-docs-list.smoke.test.ts</files>
  <read_first>
    - apps/web/src/lib/docs/resolve-docs.ts (type UnifiedDoc — source de vérité des props)
    - apps/web/src/components/sessions/qualiopi-matrix/doc-status-badge.tsx (DocStatusBadge props RÉELS + variants à réutiliser)
    - CONFIRMER LE CONTRAT (avant d'écrire le mapping) : `grep -n "warning?\|state:\|export interface DocStatusBadgeProps\|title" apps/web/src/components/sessions/qualiopi-matrix/doc-status-badge.tsx` → confirmer (a) `warning?: 'no_proof'` (littéral, pas booléen) et (b) AUCUNE prop `title`. Si le contrat a changé, adapter le mapping en conséquence et le noter dans le SUMMARY.
    - CONFIRMER LES VALEURS state : `grep -n "DocStatusState = z.enum\|export type DocStatusState" packages/shared/src/schemas/qualiopi-matrix.ts` → doit montrer `['GENERATED', 'MANUAL_OK', 'MISSING']`. Ce sont les seules valeurs `state` autorisées (hors 'NA'|'RUNNING' littéraux). Si une valeur attendue manque, ne PAS l'inventer.
    - apps/web/src/lib/doc-scope.ts (DOC_TYPE_LABELS ligne 43 pour libellés FR)
    - apps/web/src/components/sessions/qualiopi-matrix/matrix-row.tsx (pattern Link cross-nav Phase 9.1 — clone pour les ancres)
  </read_first>
  <action>
Créer `UnifiedDocsList({ docs }: { docs: UnifiedDoc[] })` (Server Component, lecture seule). Pour chaque UnifiedDoc :
  - Libellé : `DOC_TYPE_LABELS[doc.docType]?.short ?? doc.docType`.
  - Badge d'état (NAV-03) via DocStatusBadge — utiliser le CONTRAT RÉEL (cf <interfaces>), PAS un booléen `warning` ni une prop `title` :
      - `present` → `<DocStatusBadge state="GENERATED" label={libellé} />`
      - `stub`    → `<DocStatusBadge state="MANUAL_OK" warning="no_proof" label={libellé} />` + un `<span className="text-amber-700 text-xs">Mode stub — NON conforme, à régénérer</span>` adjacent (le texte d'avertissement passe par ce span, JAMAIS par une prop `title` inexistante).
      - `missing` → `<DocStatusBadge state="MISSING" label={libellé} />`
    usedStub=true (status==='stub') DOIT produire un signal visuel distinct (amber + texte) — un stub ne passe JAMAIS pour conforme (blocker T4 ind.11).
  - qualiopiIndicator affiché en petit tag à côté du libellé si non null.
  - Lien : si doc.href non null → `<a href={doc.href} target="_blank" rel="noopener">Télécharger</a>` ; cross-nav vers l'ancre selon anchor.level via `<Link>` (participant → `/app/sessions/{anchor.sessionId}` ; session → `/app/sessions/{anchor.sessionId}` ; product → `/app/produits/{anchor.productId}` ; tenant → `/app/parametres`). Pattern Link Phase 9.1.
  - État vide : early return d'un placeholder « Aucun document » si docs.length===0 (pattern Phase 5 UX-09).
Smoke test (source-regex pattern Phase 9, clone de doc-status-badge.smoke.test.ts) : fichier existe + exporte UnifiedDocsList + référence DocStatusBadge + gère les 3 status + le lien /app/parametres pour tenant. NE PAS importer @testing-library (convention projet = smoke source-regex).
  </action>
  <acceptance_criteria>
- `grep -n "export function UnifiedDocsList" apps/web/src/components/docs/unified-docs-list.tsx` → matche.
- `grep -n "usedStub\|'stub'\|MANUAL_OK\|MISSING\|GENERATED" apps/web/src/components/docs/unified-docs-list.tsx` → matches (les 3 états mappés).
- `grep -n "warning=\"no_proof\"\|warning={'no_proof'}" apps/web/src/components/docs/unified-docs-list.tsx` → matche (warning passé comme littéral, PAS un booléen).
- `grep -c "title=" apps/web/src/components/docs/unified-docs-list.tsx` → 0 sur les usages de DocStatusBadge (aucune prop `title` passée au badge — elle n'existe pas).
- `grep -n "DocStatusBadge" apps/web/src/components/docs/unified-docs-list.tsx` → matche (réutilisation, pas de nouvelle pastille).
- `grep -n "/app/parametres\|/app/produits\|/app/sessions" apps/web/src/components/docs/unified-docs-list.tsx` → ≥3 (cross-nav par anchor).
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- unified-docs-list` → vert.
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit` → exit 0 (le mapping DocStatusBadge compile contre le contrat réel : state ∈ DocStatusState, warning='no_proof', pas de title).
  </acceptance_criteria>
  <verify><automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- unified-docs-list && pnpm --filter @qualiof/web exec tsc --noEmit</automated></verify>
  <done>UnifiedDocsList rend les 3 états (stub distinct/non conforme via warning='no_proof' + span amber, jamais via title), réutilise DocStatusBadge avec le contrat réel, cross-nav par anchor, smoke vert, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: NAV-02(a) — enrichir l'onglet Docs apprenant (afficher session+produit+tenant)</name>
  <files>apps/web/src/app/app/apprenants/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/apprenants/[id]/page.tsx (lignes 105-160 : Promise.all qui charge DÉJÀ rawProductDocs/rawSessionDocs à la 113 ; lignes 769-810 : bloc onglet 'documents' actuel à enrichir)
    - CONFIRMER LE NOM RÉEL DE LA VARIABLE TENANT (avant d'écrire l'appel) : `grep -n "tenantId\|user\.tenant\|tenant\.id\|validateRequest\|const { user" "apps/web/src/app/app/apprenants/[id]/page.tsx" | head` → identifier la variable RÉELLE portant le tenantId dans cette page (peut être `user.tenantId`, `tenant.id`, etc.). NE PAS supposer `user.tenantId` à l'aveugle — utiliser le nom trouvé.
    - apps/web/src/lib/docs/get-docs-for.ts (getDocsForPerson — wrapper à appeler côté Server Component)
    - apps/web/src/components/docs/unified-docs-list.tsx (Task 1 — composant à monter)
  </read_first>
  <action>
Dans la fiche apprenant (Server Component) :
  - Appeler `const unifiedDocs = await getDocsForPerson(person.id, <VAR_TENANT_RÉELLE>)` en utilisant le nom EXACT de la variable tenant trouvé au read_first (NE PAS hardcoder `user.tenantId` si la page utilise un autre nom). Le wrapper réutilise les sources déjà fetchées — ou refetch proprement, scope tenantId garanti.
  - Dans le bloc `{tab === 'documents' && ...}` (ligne 769), SOUS la liste « Documents générés » existante (NE PAS la supprimer — anti-régression CENTRAL-03), ajouter une section `<UnifiedDocsList docs={unifiedDocs} />` qui affiche les docs session + produit + participant + liens tenant (CGV/RI) — c'est exactement ce que rawSessionDocs/rawProductDocs chargeaient sans afficher.
  - But mesurable D-09.3-06 : depuis la fiche apprenant, un doc produit (PROGRAMME), un doc session (DEROULE) et un doc tenant (CGV) sont atteignables en ≤2 clics (le lien dans UnifiedDocsList = 1 clic).
Conserver le titre existant « Documents générés ({documents.length}) » ; ajouter un sous-titre « Tous les documents (toutes sources) » pour la nouvelle section.
  </action>
  <acceptance_criteria>
- `grep -n "getDocsForPerson" "apps/web/src/app/app/apprenants/[id]/page.tsx"` → matche.
- `grep -n "UnifiedDocsList" "apps/web/src/app/app/apprenants/[id]/page.tsx"` → matche.
- `grep -n "Documents générés" "apps/web/src/app/app/apprenants/[id]/page.tsx"` → matche TOUJOURS (bloc existant préservé, anti-régression).
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit` → clean (l'appel getDocsForPerson compile avec la variable tenant réelle — preuve que le nom n'a pas été inventé).
  </acceptance_criteria>
  <verify><automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit</automated></verify>
  <done>Onglet Docs apprenant affiche la liste unifiée (session+produit+participant+tenant) sous le bloc existant ; getDocsForPerson appelé avec la variable tenant RÉELLE de la page ; ≤2 clics vers tout doc ; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: NAV-02(b)(c) — onglet/bloc Docs produit (programme/déroulé + docs apprenants + tenant)</name>
  <files>apps/web/src/components/produits/tabs/product-docs-tab.tsx, apps/web/src/app/app/produits/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/components/produits/tabs/product-tabs.tsx (pattern URL-state ?tab=, à étendre avec un onglet 'docs')
    - apps/web/src/components/produits/tabs/product-programme-tab.tsx (pattern panel Server Component existant à cloner)
    - CONFIRMER LE NOM RÉEL DE LA VARIABLE TENANT (avant de câbler) : `grep -n "tenantId\|user\.tenant\|tenant\.id\|validateRequest\|const { user" "apps/web/src/app/app/produits/[id]/page.tsx" | head` → utiliser le nom EXACT trouvé pour passer le tenantId au tab. NE PAS supposer `user.tenantId`.
    - apps/web/src/lib/docs/get-docs-for.ts (getDocsForProduct)
    - apps/web/src/components/docs/unified-docs-list.tsx (Task 1)
  </read_first>
  <action>
  - Créer `ProductDocsTab({ productId, tenantId }: { productId: string; tenantId: string })` (Server Component) : `const docs = await getDocsForProduct(productId, tenantId)` puis `<UnifiedDocsList docs={docs} />`. Affiche programme/déroulé (entityType=product) + docs apprenants agrégés du produit + liens tenant (CGV/RI) croisés (NAV-02c).
  - Étendre product-tabs.tsx : ajouter l'onglet `docs` à la liste URL-state (?tab=docs) avec libellé « Documents » et lazy-load `<ProductDocsTab .../>` côté Server (pattern lazy-par-tab existant Phase 9.1 CENTRAL-04).
  - Câbler dans produits/[id]/page.tsx : passer productId + la variable tenant RÉELLE (nom trouvé au read_first) au tab.
But mesurable D-09.3-06 : depuis fiche produit, un doc apprenant et un doc tenant atteignables en ≤2 clics.
  </action>
  <acceptance_criteria>
- `grep -n "export function ProductDocsTab\|export async function ProductDocsTab" apps/web/src/components/produits/tabs/product-docs-tab.tsx` → matche.
- `grep -n "getDocsForProduct" apps/web/src/components/produits/tabs/product-docs-tab.tsx` → matche.
- `grep -n "UnifiedDocsList" apps/web/src/components/produits/tabs/product-docs-tab.tsx` → matche.
- `grep -n "docs\|Documents" apps/web/src/components/produits/tabs/product-tabs.tsx` → l'onglet 'docs' ajouté à la liste des tabs.
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit` → clean (câblage tenantId compile avec la variable réelle).
  </acceptance_criteria>
  <verify><automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- product-tabs</automated></verify>
  <done>Onglet Docs produit livré (programme/déroulé + apprenants agrégés + tenant), URL-state ?tab=docs, tenantId passé via la variable réelle de la page, ≤2 clics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — vérification visuelle des 3 surfaces (≤2 clics + stub non conforme)</name>
  <files>N/A — vérification visuelle, aucun fichier modifié</files>
  <what-built>3 surfaces docs unifiées (NAV-02) + signal usedStub (NAV-03) : onglet Docs apprenant enrichi, onglet Docs produit, liens tenant croisés, badge present/stub/missing.</what-built>
  <action>
Vérification humaine à l'écran (la règle ≤2 clics et le rendu stub/conforme ne sont vérifiables qu'en runtime) :
1. `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && curl -sI localhost:3010/login` (instance dev:full de Laurent tourne sur :3010). Si down, démarrer dev:full.
2. Ouvrir une fiche apprenant ayant des sessions (ex : un apprenant SmartOF AGEFICE) → onglet « Documents ». Vérifier : la nouvelle section « Tous les documents » liste des docs de session (Déroulé), de produit (Programme), participant (Convention/Émargement) ET un lien CGV/RI tenant. Cliquer un doc produit → arrive en ≤2 clics.
3. Ouvrir une fiche produit → onglet « Documents ». Vérifier : programme/déroulé + docs des apprenants formés + lien CGV/RI.
4. Vérifier qu'un doc en mode stub (s'il en existe : émargement/QCM avec rawJson.source='stub') affiche un badge AMBER distinct « non conforme », PAS un vert « conforme ». Un doc missing = rouge.
  </action>
  <how-to-verify>
1. `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && curl -sI localhost:3010/login` (instance dev:full de Laurent tourne sur :3010). Si down, démarrer dev:full.
2. Fiche apprenant → onglet « Documents » → section « Tous les documents » liste session+produit+participant+tenant ; clic doc produit ≤2 clics.
3. Fiche produit → onglet « Documents » → programme/déroulé + apprenants + lien CGV/RI.
4. Doc stub = badge amber « non conforme » (pas vert) ; doc missing = rouge.
  </how-to-verify>
  <verify>Validation humaine visuelle (pas d'automated — règle ≤2 clics + rendu stub vérifiables seulement à l'écran).</verify>
  <done>Laurent approuve : 3 surfaces affichent les docs unifiés en ≤2 clics, stub rendu en amber non conforme.</done>
  <resume-signal>Taper « approved » ou décrire les écarts (clics > 2, stub vert au lieu d'amber, source manquante).</resume-signal>
</task>

</tasks>

<verification>
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test` → suite verte (unified-docs-list + smokes pages).
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit` → clean.
- Checkpoint humain approuvé (règle ≤2 clics + stub non conforme).
</verification>

<success_criteria>
- Onglet Docs apprenant affiche docs session+produit+participant+tenant (≤2 clics).
- Onglet Docs produit agrège programme/déroulé + docs apprenants + tenant.
- usedStub visible : stub=amber non conforme (warning='no_proof'), present=emerald, missing=red.
- Mapping DocStatusBadge conforme au contrat réel (state ∈ DocStatusState, warning='no_proof', pas de title) → tsc clean.
- Bloc « Documents générés » existant préservé (anti-régression CENTRAL-03).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/09.3-navigation-documentaire-unifi-e/09.3-03-SUMMARY.md`
</output>
</output>
