---
phase: 09.3-navigation-documentaire-unifi-e
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/docs/resolve-docs.ts
  - apps/web/src/lib/docs/get-docs-for.ts
  - apps/web/src/lib/docs/__tests__/resolve-docs.test.ts
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "resolveDocs prend les 6 sources et renvoie une liste UnifiedDoc[] normalisée"
    - "Une preuve présente dans CHACUNE des 6 sources pour un même apprenant produit 6 UnifiedDoc"
    - "usedStub=true uniquement quand PedagogicalAsset.rawJson.source==='stub', false pour les docs Document"
    - "Les wrappers Prisma scopent toutes les requêtes par tenantId (racine Person/Document/Tenant/PedagogicalAsset)"
  artifacts:
    - path: "apps/web/src/lib/docs/resolve-docs.ts"
      provides: "Fonction pure resolveDocs + type UnifiedDoc + type DocSourceTable"
      exports: ["resolveDocs", "UnifiedDoc", "DocSourceTable"]
      min_lines: 120
    - path: "apps/web/src/lib/docs/get-docs-for.ts"
      provides: "Wrappers Prisma getDocsForPerson/getDocsForSession/getDocsForProduct scoped tenantId"
      exports: ["getDocsForPerson", "getDocsForSession", "getDocsForProduct"]
    - path: "apps/web/src/lib/docs/__tests__/resolve-docs.test.ts"
      provides: "Test comportemental 6-sources avec fixtures (D-09.3-04, AgeficeProfile/CFP inclus)"
      contains: "toHaveLength(6)"
  key_links:
    - from: "apps/web/src/lib/docs/get-docs-for.ts"
      to: "apps/web/src/lib/docs/resolve-docs.ts"
      via: "import resolveDocs"
      pattern: "resolveDocs\\("
    - from: "apps/web/src/lib/docs/resolve-docs.ts"
      to: "PedagogicalAsset.rawJson.source"
      via: "détection stub"
      pattern: "source.*===.*'stub'"
---

<objective>
Livrer le résolveur LECTURE pur `resolveDocs` qui UNIONise les 6 sources de preuves Qualiopi éclatées (Document, PedagogicalAsset, SensitiveData/CNI, Person/RIB, AgeficeProfile/CFP, Tenant/CGV-RI) en une liste `UnifiedDoc[]` normalisée, plus les wrappers Prisma minces scopés tenantId. Inclut le garde-fou de re-vérification des chemins de la matrice AVANT toute écriture (les chemins de l'audit 2026-06-10 ont déjà divergé : `rawSessionDocs/rawProductDocs` sont à la ligne 113, pas 147).

Purpose: Fondation de toute la phase. Sans ce résolveur, « tous les docs de l'apprenant X » reste impossible en une requête (preuves sur 6 tables). Porte la conformité (qualiopiIndicator + usedStub) et expose une référence polymorphe sourceTable+sourceId réutilisable par T7 check-list. ZÉRO migration, ZÉRO backfill, réversible (D-09.3-01).
Output: `resolve-docs.ts` (pure) + `get-docs-for.ts` (wrappers) + test comportemental 6-sources vert (critère de recette NON-NÉGOCIABLE D-09.3-04 — chaque source, AgeficeProfile/CFP incluse, doit contribuer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09.3-navigation-documentaire-unifi-e/09.3-CONTEXT.md
@.planning/audit/MATRICE-NAVIGATION-DOCS.md

<interfaces>
<!-- Contrat figé UnifiedDoc (CONTEXT.md). À recopier verbatim dans resolve-docs.ts. -->
```ts
export type DocSourceTable =
  | 'Document' | 'PedagogicalAsset'
  | 'SensitiveData' /*CNI*/ | 'Person' /*RIB*/ | 'AgeficeProfile' /*CFP*/
  | 'Tenant' /*CGV/RI*/;

export interface UnifiedDoc {
  sourceTable: DocSourceTable;   // D-#2 : réf polymorphe (T7-ready)
  sourceId: string;
  docType: string;
  qualiopiIndicator: string | null;  // D-#1 : conformité, pas juste existence
  anchor:
    | { level: 'participant'; participantId: string; personId: string; sessionId: string }
    | { level: 'session'; sessionId: string }
    | { level: 'product'; productId: string }
    | { level: 'tenant'; tenantId: string };
  generatedAt: Date | null;
  href: string | null;           // null = preuve à produire
  status: 'present' | 'stub' | 'missing';
  usedStub: boolean;             // D-#1 : stub = pire qu'absent (blocker T4)
}
```

<!-- Schéma Prisma RÉEL des 6 sources (vérifié branche cloud-migration 2026-06-10) -->
Document (schema.prisma:994) : id, tenantId, type DocType, entityType String ("session"|"participant"|"product"|"tenant"|"invoice"), entityId String, pdfUrl, status, sessionId String?, participantId String?, createdAt. SEULES sessionId/participantId sont de vraies FK ; product/tenant = chaînes entityType/entityId.
PedagogicalAsset (schema.prisma model, kind enum : ANALYSE_BESOIN/QCM/GRILLE_OBS/COMPETENCES/DEROULE/POSITIONNEMENT/SATISFACTION_CHAUD/SATISFACTION_FROID/EMARGEMENT) : id, tenantId, kind, sessionId, participantId String?, rawJson Json, pdfUrl String?, generatedAt. Règle stub : rawJson.source === 'stub' (cf renderer.ts:155).
SensitiveData (schema.prisma:180) : idDocumentUrl String? (CNI, clé MinIO ligne 185). Relation via Person (pas de tenantId direct → scope par la racine Person).
Person (schema.prisma:139) : ribKey String? (RIB, clé MinIO ligne 155). Porte tenantId.
AgeficeProfile (schema.prisma:1245) : cfpAttestationKey String? (CFP, clé MinIO ligne 1267). Relation via Person (scope par la racine Person).
Tenant (schema.prisma:30) : cgvMarkdown String? (ligne 51), reglementInterieurMarkdown String? (ligne 52).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveDocs (fonction pure) + wrappers Prisma + test 6-sources</name>
  <files>apps/web/src/lib/docs/resolve-docs.ts, apps/web/src/lib/docs/get-docs-for.ts, apps/web/src/lib/docs/__tests__/resolve-docs.test.ts</files>
  <behavior>
Test comportemental 6-sources (D-09.3-04, NON-NÉGOCIABLE — c'est l'acceptance_criteria central de NAV-01 ; les 6 sources DOIVENT chacune être exercée, y compris AgeficeProfile/CFP pour ne pas reproduire l'anti-pattern « source oubliée ») :
  - Fixtures en mémoire : pour UN MÊME apprenant (personId='p1', participantId='pp1', sessionId='s1'), construire 1 preuve dans CHACUNE des 6 sources :
    1. Document { id:'d1', type:'CONVENTION', entityType:'participant', participantId:'pp1', sessionId:'s1', pdfUrl:'http://minio/d1.pdf', createdAt }
    2. PedagogicalAsset { id:'pa1', kind:'EMARGEMENT', sessionId:'s1', participantId:'pp1', rawJson:{source:'stub'}, pdfUrl:'http://minio/pa1.pdf', generatedAt }
    3. SensitiveData { idDocumentUrl:'cni-key' } (CNI)
    4. Person { ribKey:'rib-key' } (RIB)
    5. AgeficeProfile { cfpAttestationKey:'cfp-key' } (CFP)  ← 6e source, NE PAS l'oublier
    6. Tenant { cgvMarkdown:'...' } (CGV)
  - Test 1: expect(resolveDocs(input)).toHaveLength(6)  ← assertion centrale, NE PAS remplacer par un grep d'imports
  - Test 2: le PedagogicalAsset stub → status:'stub', usedStub:true
  - Test 3: le Document CONVENTION → usedStub:false (template déterministe), status:'present'
  - Test 4: chaque UnifiedDoc porte sourceTable correct ('Document'|'PedagogicalAsset'|'SensitiveData'|'Person'|'AgeficeProfile'|'Tenant') et sourceId non vide
  - Test 4b (CFP explicite) : le doc issu d'AgeficeProfile a sourceTable:'AgeficeProfile' ET docType:'CFP' (assertion dédiée — la 6e source ne doit jamais disparaître silencieusement de l'union)
  - Test 5: anchor.level correct par source (Document participant → participant ; PedagogicalAsset → participant ; CNI/RIB/CFP → participant via personId ; CGV → tenant)
  - Test 6 (régression union complète) : retirer UNE source des fixtures (au choix, ET un cas où l'on retire AgeficeProfile) → length passe à 5 (prouve que chaque source contribue indépendamment ; une source oubliée = dossier incomplet silencieux)
  </behavior>
  <read_first>
    - apps/web/src/app/app/apprenants/[id]/page.tsx (lignes 100-160 : voir comment rawDocs/rawAssets/rawProductDocs/rawSessionDocs sont déjà fetchés — réutiliser EXACTEMENT ces shapes pour les wrappers ; rawSessionDocs/rawProductDocs sont à la ligne 113, PAS 147 comme l'audit le disait)
    - apps/web/src/lib/doc-scope.ts (MATRIX_DOC_TYPES, SESSION_ONLY_DOC_TYPES, DOC_TYPE_LABELS, DOC_TYPE_TO_PED_KIND — ne PAS les redéfinir, les importer si besoin du mapping DocType↔PedagogicalKind)
    - apps/web/src/lib/closure/renderer.ts (lignes 130-250 : confirmer que rawJson.source vaut 'ollama'|'stub'|'shared' et que usedStub = source === 'stub')
    - packages/db/prisma/schema.prisma (lignes 30-52 Tenant, 139-160 Person, 180-190 SensitiveData, 994-1014 Document, 1245-1270 AgeficeProfile, modèle PedagogicalAsset)
    - .planning/phases/09.3-navigation-documentaire-unifi-e/09.3-CONTEXT.md (contrat UnifiedDoc figé + règle usedStub)
  </read_first>
  <action>
GARDE-FOU RE-VÉRIFICATION (À FAIRE EN PREMIER, AVANT TOUTE ÉCRITURE — garde-fou utilisateur) :
Exécuter ces grep et CONFIRMER qu'ils retournent une ligne. Si un chemin a bougé, le documenter dans le SUMMARY et adapter — ne PAS écrire à l'aveugle :
  - `grep -n "rawSessionDocs" "apps/web/src/app/app/apprenants/[id]/page.tsx"` → doit matcher (réel : lignes 113/314)
  - `grep -n "rawProductDocs" "apps/web/src/app/app/apprenants/[id]/page.tsx"` → doit matcher (réel : lignes 113/326)
  - `grep -n "model Document " packages/db/prisma/schema.prisma` → doit retourner 994
  - `grep -n "model PedagogicalAsset " packages/db/prisma/schema.prisma` → doit matcher
  - `grep -n "idDocumentUrl" packages/db/prisma/schema.prisma` → 185 ; `grep -n "ribKey" packages/db/prisma/schema.prisma` → 155 ; `grep -n "cfpAttestationKey" packages/db/prisma/schema.prisma` → 1267
  - `grep -n "cgvMarkdown\|reglementInterieurMarkdown" packages/db/prisma/schema.prisma` → 51/52
  - `grep -rn "source === 'stub'" apps/web/src/lib/closure/renderer.ts` → doit matcher (règle usedStub)
  - CONFIRMER LE SCOPE TENANT DES SOURCES PII : `grep -n "tenantId" packages/db/prisma/schema.prisma | grep -iE "SensitiveData|AgeficeProfile|^.*Person"` → vérifier si SensitiveData/AgeficeProfile portent tenantId direct. Constat attendu : Person porte tenantId, SensitiveData/AgeficeProfile sont des relations 1-1 de Person SANS tenantId direct → le scope DOIT passer par la racine Person (cf action wrappers).

resolve-docs.ts (fonction PURE, ZÉRO appel Prisma) :
  1. Copier verbatim DocSourceTable + UnifiedDoc + signature resolveDocs depuis le bloc <interfaces>.
  2. Signature : `resolveDocs(input: { documents: DocumentRow[]; pedagogicalAssets: PedAssetRow[]; identity: { participantId; personId; sessionId; cniUrl?; ribKey?; cfpKey? }[]; legal: { tenantId; cgvMarkdown?; reglementInterieurMarkdown? }; opcoSubmissions?: never }): UnifiedDoc[]` — définir des row-types minces locaux (juste les champs lus, pas le type Prisma complet, pour rester testable sans DB).
  3. Map Document → UnifiedDoc : sourceTable='Document', sourceId=doc.id, docType=doc.type, anchor dérivé de entityType (participant → {level:'participant', participantId, personId, sessionId} ; session → {level:'session'} ; product → {level:'product', productId:entityId} ; tenant → {level:'tenant'}), generatedAt=doc.createdAt, href=doc.pdfUrl, status='present', usedStub=false (templates déterministes — règle CONTEXT).
  4. Map PedagogicalAsset → UnifiedDoc : sourceTable='PedagogicalAsset', sourceId=pa.id, docType dérivé de kind (réutiliser le mapping de doc-scope si dispo, sinon kind tel quel), anchor {level:'participant'} si participantId sinon {level:'session'}, generatedAt=pa.generatedAt, href=pa.pdfUrl, usedStub = (pa.rawJson?.source === 'stub'), status = usedStub ? 'stub' : (pa.pdfUrl ? 'present' : 'missing').
  5. Map identité : pour chaque entrée identity avec cniUrl → UnifiedDoc {sourceTable:'SensitiveData', docType:'CNI', anchor participant, href:cniUrl, status:'present', usedStub:false} ; ribKey → {sourceTable:'Person', docType:'RIB', href:ribKey} ; cfpKey → {sourceTable:'AgeficeProfile', docType:'CFP', href:cfpKey}. sourceId = la clé MinIO elle-même (pas de row id distinct disponible). LES 3 (CNI/RIB/CFP) DOIVENT être émis — ne pas oublier le CFP (6e source).
  6. Map legal : cgvMarkdown présent → UnifiedDoc {sourceTable:'Tenant', sourceId:tenantId, docType:'CGV', anchor:{level:'tenant',tenantId}, href:null, status:'present', usedStub:false} ; idem reglementInterieurMarkdown → docType:'REGLEMENT_INTERIEUR'.
  7. qualiopiIndicator : pour V1, dériver via lookup sur le catalog (importer un map docType→indicator depuis doc-scope OU laisser null si non mappé — le câblage fin du catalog seed est dans le Plan 09.3-02 ; ici accepter null et documenter). NE PAS bloquer sur ce point.

get-docs-for.ts (wrappers Prisma minces, scope tenantId OBLIGATOIRE — par requête racine quand la table porte tenantId, par la racine Person quand elle ne le porte pas) :
  - `getDocsForPerson(personId: string, tenantId: string): Promise<UnifiedDoc[]>` :
      a. ACCÈS IDENTITÉ SCOPÉ PAR LA RACINE : `const person = await prisma.person.findFirst({ where: { id: personId, tenantId }, include: { sensitiveData: true, ageficeProfile: true } })` — le `where` porte tenantId sur Person ; SensitiveData (CNI) et AgeficeProfile (CFP) sont lus en RELATIONS de cette Person scopée (pas de findMany cross-tenant possible → pas de fuite RGPD). Lire person.ribKey (RIB), person.sensitiveData?.idDocumentUrl (CNI), person.ageficeProfile?.cfpAttestationKey (CFP).
      b. Récupérer les participants de la personne → sessionIds/participantIds/productIds.
      c. `prisma.document.findMany({ where: { tenantId, OR: [{ participantId: { in } }, { entityType: 'product', entityId: { in: productIds } }] } })` — tenantId dans le where.
      d. `prisma.pedagogicalAsset.findMany({ where: { tenantId, participantId: { in } } })` — tenantId dans le where.
      e. `prisma.tenant.findUnique({ where: { id: tenantId } })` pour cgv/ri.
      f. `resolveDocs({ documents, pedagogicalAssets, identity:[{ participantId, personId, sessionId, cniUrl, ribKey, cfpKey }], legal:{ tenantId, cgvMarkdown, reglementInterieurMarkdown } })`.
  - `getDocsForSession(sessionId, tenantId)` : `prisma.document.findMany({ where:{ tenantId, OR:[{ sessionId }, { entityType:'session', entityId: sessionId }] } })` + `prisma.pedagogicalAsset.findMany({ where:{ tenantId, sessionId } })` + tenant → resolveDocs.
  - `getDocsForProduct(productId, tenantId)` : `prisma.document.findMany({ where:{ tenantId, entityType:'product', entityId: productId } })` + agrégation via sessions du produit (sessions scopées tenantId) + tenant → resolveDocs.
  - RÈGLE NON-NÉGOCIABLE : aucune lecture de PII (CNI/RIB/CFP) hors d'une racine Person déjà scopée tenantId. Chaque source racine portant tenantId (person, document, pedagogicalAsset, tenant) inclut tenantId dans son where (defense-in-depth, convention CLAUDE.md « Prisma queries always scoped to user.tenantId »).

Conventions : kebab-case fichiers ✅, fonction pure côté resolve-docs ✅, Vitest. PAS de server action ici (pas de {ok,...}), ce sont des helpers de lecture appelés par les Server Components.
  </action>
  <acceptance_criteria>
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- resolve-docs` → tous verts, dont le test `toHaveLength(6)`.
- `grep -n "toHaveLength(6)" apps/web/src/lib/docs/__tests__/resolve-docs.test.ts` → matche (preuve que le test 6-sources existe, pas un smoke ; les 5 sources ne suffisent pas — AgeficeProfile/CFP exercée).
- `grep -n "AgeficeProfile\|cfpKey\|cfpAttestationKey\|'CFP'" apps/web/src/lib/docs/__tests__/resolve-docs.test.ts` → matche (la 6e source est bien exercée par le test).
- `grep -n "toHaveLength(5)" apps/web/src/lib/docs/__tests__/resolve-docs.test.ts` → matche (test de régression d'union : retrait d'une source → 5).
- `grep -n "export function resolveDocs\|export interface UnifiedDoc\|export type DocSourceTable" apps/web/src/lib/docs/resolve-docs.ts` → 3 matches.
- `grep -n "export async function getDocsForPerson\|getDocsForSession\|getDocsForProduct" apps/web/src/lib/docs/get-docs-for.ts` → 3 matches.
- `grep -n "person.findFirst\|findFirst({ where: { id: personId, tenantId" apps/web/src/lib/docs/get-docs-for.ts` → matche (accès identité PII scopé par la racine Person — pas de findMany SensitiveData/AgeficeProfile cross-tenant).
- `grep -c "tenantId" apps/web/src/lib/docs/get-docs-for.ts` → ≥ 4 (person + document + pedagogicalAsset + tenant ; chaque source racine portant tenantId le scope).
- `grep -c "@qualiof/db" apps/web/src/lib/docs/resolve-docs.ts` → 0 (fonction pure, aucun import Prisma).
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit` → clean.
- **TEST DE PUISSANCE (mutation, OBLIGATOIRE au gate — vérifie que le test GARDE quelque chose, pas seulement qu'il existe)** : commenter temporairement UNE des 6 branches de source dans `resolveDocs` (ex : la branche AgeficeProfile/CFP), relancer `pnpm --filter @qualiof/web test -- resolve-docs`, CONFIRMER que le test passe au ROUGE (`toHaveLength(6)` échoue → reçoit 5), PUIS restaurer la branche et reconfirmer le VERT. Prouve que `toHaveLength(6)` teste l'union réelle et pas un mock qui renvoie 6 quoi qu'il arrive. Consigner le résultat (rouge puis vert) dans le rapport d'exécution de la tâche. 30 secondes, non négociable.
  </acceptance_criteria>
  <verify><automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- resolve-docs</automated></verify>
  <done>resolveDocs pur (0 import @qualiof/db), test 6-sources vert (1 preuve/source → 6 UnifiedDoc, AgeficeProfile/CFP incluse) + régression retrait→5, usedStub correct (stub PedagogicalAsset=true, Document=false), wrappers scopés tenantId (PII via racine Person scopée), tsc clean.</done>
</task>

</tasks>

<verification>
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- resolve-docs` → tous verts, dont `toHaveLength(6)`.
- `grep -c "@qualiof/db" apps/web/src/lib/docs/resolve-docs.ts` → 0.
- `grep -c "tenantId" apps/web/src/lib/docs/get-docs-for.ts` → ≥4.
- `grep -n "person.findFirst" apps/web/src/lib/docs/get-docs-for.ts` → matche (PII scopée par racine Person).
- `cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit` → clean.
</verification>

<success_criteria>
- resolveDocs est une fonction pure (aucun import @qualiof/db dans resolve-docs.ts).
- Test comportemental 6-sources vert : 1 preuve par source pour 1 apprenant → 6 UnifiedDoc (AgeficeProfile/CFP exercée, plus de source oubliée).
- usedStub correct : true pour PedagogicalAsset stub, false pour Document.
- Wrappers scopent tenantId sur chaque requête racine ; les PII (CNI/RIB/CFP) sont lues via une racine Person déjà scopée tenantId (pas de fuite cross-tenant).
- tsc clean, suite apps/web verte (pas de régression).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/09.3-navigation-documentaire-unifi-e/09.3-01-SUMMARY.md`
</output>
</output>
