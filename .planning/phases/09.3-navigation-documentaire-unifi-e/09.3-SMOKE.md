# Phase 09.3 — SMOKE (vérifications réelles de fin de phase)

> Exécuté 2026-06-10, branche `cloud-migration`. Bookkeeping de clôture (Plan 09.3-04, Task 1).
> Garde-fou Laurent : le « smoke » de cette tâche reste cantonné au bookkeeping. Les gates
> comportementaux des vagues précédentes (test 6-sources de 09.3-01, checkpoint visuel ≤2 clics
> de 09.3-03 approuvé par Laurent) NE sont PAS dégradés en grep ici — ils restent les preuves de fond.

## Résultats numériques

| Vérification | Commande | Résultat |
|---|---|---|
| Suite complète web | `pnpm --filter @qualiof/web test` | ✅ **897 passed (897)** — 108 fichiers, 0 fail |
| Typecheck web | `pnpm --filter @qualiof/web exec tsc --noEmit` | ✅ **0 erreur hors scope** — seules les 6 erreurs PRÉ-EXISTANTES de `redirect-308.test.ts` (voir note) |
| Typecheck db (seed.ts) | `pnpm --filter @qualiof/db exec tsc --noEmit` | ✅ **clean (0 erreur)** |
| Build web | `pnpm --filter @qualiof/web build` | ✅ OK — `/app/apprenants/[id]`, `/app/produits/[id]` (4.38 kB), `/app/sessions/[id]` (30.6 kB) compilent |
| Fichiers orphelins | `find apps packages -name "*.bak" -o -name "*.orig" \| wc -l` | ✅ **0** (aucun résidu sed/backup sur la branche) |

### Note — 6 erreurs tsc PRÉ-EXISTANTES (exclues du verdict, non corrigées)

`src/server/actions/__tests__/redirect-308.test.ts` lignes 16/27/38 → TS2722 + TS18048
(`nextConfig.redirects` possibly undefined), soit 3 lignes × 2 erreurs = **6 erreurs**.
Cause : édition **WIP non commitée** de `apps/web/next.config.mjs` (présente sur la branche avant
la Phase 9.3, étrangère au scope NAV). Vérifié : **0 erreur tsc hors ce fichier**. Hotfix séparé
(hors 9.3) — ne pas corriger ici. Consigné `deferred-items.md` (déjà documenté Plans 01 et 02).

## Les 6 critères de succès du CONTEXT.md

1. ✅ **`resolveDocs` pur livré + wrappers Prisma scopés tenantId.**
   `apps/web/src/lib/docs/resolve-docs.ts` (fonction pure, 0 import `@qualiof/db`) + wrappers
   `getDocsForPerson/Session/Product` (`get-docs-for.ts`, 26 occurrences `tenantId`, PII lues via
   racine `Person` scopée `findFirst({ id, tenantId })` — 0 fuite RGPD cross-tenant). Plan 09.3-01.

2. ✅ **Test comportemental 6-sources vert (D-09.3-04).**
   `resolve-docs.test.ts` — 15 tests dont le central `toHaveLength(6)` (UNION RÉELLE des 6 sources :
   Document, PedagogicalAsset, SensitiveData/CNI, Person/RIB, AgeficeProfile/CFP, Tenant/CGV-RI).
   Test de puissance (mutation) prouvé ROUGE→VERT au gate 09.3-01 (branche CFP commentée → 7 fails).
   **Gate comportemental NON dégradé en grep.** Plan 09.3-01.

3. ✅ **Onglet Docs apprenant affiche docs session + produit + participant + liens tenant (≤2 clics).**
   `getDocsForPerson(person.id, user.tenantId)` câblé dans `apprenants/[id]/page.tsx` (variable tenant
   réelle confirmée), section « Tous les documents (toutes sources) » rendue via `UnifiedDocsList`
   (cross-nav par `anchor.level`). Bloc « Documents générés » CENTRAL-03 préservé (anti-régression).
   Checkpoint visuel ≤2 clics **APPROUVÉ par Laurent** (avec 3 corrections d'indicateurs appliquées). Plan 09.3-03.

4. ✅ **Bloc Docs produit affiche programme/déroulé + agrège docs apprenants du produit.**
   `ProductDocsTab` (Server Component) fetche `getDocsForProduct(productId, tenantId)`, onglet `?tab=docs`
   ajouté à `PRODUCT_TABS`/`VALID_TABS`. **Groupage hiérarchique produit → sessions → apprenant**
   (correction post-checkpoint `95625bf`). Plan 09.3-03.

5. ✅ **5 types fantômes triés selon D-09.3-07 (plus de MISSING permanent injustifié).**
   SATISFACTION nu retiré du seed (remplacé CHAUD/FROID), PRE_ACCORD_OPCO retiré de MATRIX_DOC_TYPES
   (14→13, jalon OpcoSubmission), VALIDATION_OPCO jalon, SUPPORT_PEDAGOGIQUE gardé (ind. 19), CUSTOM
   upload libre. Gardé par `seed-catalog.test.ts` (0-drift, lecture seed réel). Plan 09.3-02.

6. ✅ **`usedStub` visible dans l'UI (un doc stub ne passe pas pour conforme).**
   `UnifiedDocsList` mappe `status='stub'` → `DocStatusBadge state="MANUAL_OK" warning="no_proof"` (amber)
   + libellé adjacent « Mode stub — NON conforme, à régénérer » (jamais via `title`). `usedStub` =
   `PedagogicalAsset.rawJson.source === 'stub'`, false pour les Document (templates déterministes). Plan 09.3-03.

## Corrections d'indicateurs V9 post-checkpoint (tranchées par Laurent, déjà commitées)

Au-delà des 6 critères, le checkpoint visuel a généré 3 corrections de source-de-vérité validées par Laurent :
- **Source unique indicateurs + libellés** : retrait de tous les littéraux « Ind 27 » dans 4 fichiers session,
  le résolveur lit le catalogue (`7271fed`) ; indicateur séparé du libellé (`e394271`).
- **Dédup version courante** : champs `version`/`isCurrent` additifs (`e394271`).
- **3 corrections d'indicateurs RNQ V9** : ASSIDUITE→null (badge financeur, hors rollup ind.12, `9a10cf4`),
  CERTIFICAT→Ind 11 + mention légale (`4a4ee11`), ATTESTATION_FIN→Légal pur (`5ddba13`),
  captions Préparation=1·4·5·6·9·17 / Pack=8·11·12·30 (`f127535`). Verrou de mapping `def2b46`.

## Verdict

**PASS.** Suite 897/897 verte, tsc web+db clean (hors 6 erreurs pré-existantes documentées),
build web OK, 0 fichier orphelin. Les 6 critères CONTEXT sont satisfaits avec preuve comportementale.
