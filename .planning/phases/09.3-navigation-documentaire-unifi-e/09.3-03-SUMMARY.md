---
phase: 09.3-navigation-documentaire-unifi-e
plan: 03
subsystem: docs-navigation
tags: [NAV-02, NAV-03, ui, qualiopi, cross-nav]
status: complete (checkpoint APPROVED by Laurent 2026-06-10, + 3 V9 indicator corrections applied)
requires:
  - "09.3-01 (resolveDocs + UnifiedDoc + getDocsForPerson/Product)"
provides:
  - "UnifiedDocsList (rendu réutilisable UnifiedDoc[] + badge present/stub/missing + cross-nav)"
  - "Onglet Docs apprenant enrichi (session+produit+participant+tenant)"
  - "Onglet Docs produit (programme/déroulé + apprenants agrégés + tenant)"
affects:
  - "apps/web/src/app/app/apprenants/[id]/page.tsx"
  - "apps/web/src/app/app/produits/[id]/page.tsx"
tech-stack:
  patterns:
    - "Server Component lecture seule rend UnifiedDoc[]"
    - "DocStatusBadge réutilisé (contrat réel : state ∈ DocStatusState, warning='no_proof' littéral, pas de title)"
    - "Onglet produit URL-state ?tab=docs lazy (fetch dans le tab)"
key-files:
  created:
    - "apps/web/src/components/docs/unified-docs-list.tsx"
    - "apps/web/src/components/docs/__tests__/unified-docs-list.smoke.test.ts"
    - "apps/web/src/components/produits/tabs/product-docs-tab.tsx"
  modified:
    - "apps/web/src/app/app/apprenants/[id]/page.tsx"
    - "apps/web/src/app/app/produits/[id]/page.tsx"
    - "apps/web/src/components/produits/tabs/product-tabs.tsx"
    - "apps/web/src/components/produits/tabs/__tests__/product-tabs.smoke.test.ts"
decisions:
  - "Cross-nav par anchor.level : participant/session → /app/sessions/{id}, product → /app/produits/{id}, tenant → /app/parametres"
  - "Onglet produit Docs lazy : ProductDocsTab fetche lui-même via getDocsForProduct (pas d'over-fetch côté page)"
metrics:
  duration: "~10 min (3 tâches AUTO)"
  completed-date: "2026-06-10"
  tasks-auto: 3
  tasks-checkpoint-pending: 1
---

# Phase 9.3 Plan 03 : Branchement résolveur sur 3 surfaces UI (NAV-02 + NAV-03) — Summary

Navigation documentaire unifiée branchée sur les fiches apprenant et produit : composant `UnifiedDocsList` réutilisable rendant `UnifiedDoc[]` avec badge `present`/`stub`/`missing` et cross-nav ≤2 clics, onglet Docs apprenant enrichi (session+produit+participant+tenant) et onglet Docs produit (programme/déroulé + apprenants agrégés + CGV/RI). **Checkpoint visuel humain EN ATTENTE** (Task 4 non simulé).

## Tâches exécutées (AUTO)

| Task | Nom | Commit | Fichiers |
| ---- | --- | ------ | -------- |
| 1 | NAV-03 — composant UnifiedDocsList | `351ec88` | unified-docs-list.tsx + smoke |
| 2 | NAV-02(a) — onglet Docs apprenant | `5768379` | apprenants/[id]/page.tsx |
| 3 | NAV-02(b)(c) — onglet Docs produit | `7fca330` | product-docs-tab.tsx + product-tabs.tsx + produits/[id]/page.tsx + smoke |

## Détails

- **Task 1 — UnifiedDocsList** : Server Component lecture seule. Mapping `UnifiedDoc.status` → `DocStatusBadge` contre le contrat RÉEL vérifié (`state ∈ ['GENERATED','MANUAL_OK','MISSING']`, `warning?: 'no_proof'` littéral, AUCUNE prop `title`). NAV-03 : `stub` rendu amber (`state="MANUAL_OK" warning="no_proof"`) + `<span>` adjacent « Mode stub — NON conforme, à régénérer » (jamais via `title`). `missing` = rouge + « Preuve à produire ». Cross-nav par `anchor.level`. État vide « Aucun document ». Smoke source-regex 6 tests.
- **Task 2 — Docs apprenant** : `getDocsForPerson(person.id, user.tenantId)` (variable tenant RÉELLE confirmée ligne 63 de la page, PAS supposée). Nouvelle section « Tous les documents (toutes sources) » ajoutée SOUS le bloc « Documents générés » existant (anti-régression CENTRAL-03 préservée — grep confirme le bloc toujours présent).
- **Task 3 — Docs produit** : `ProductDocsTab` Server Component fetche `getDocsForProduct(productId, tenantId)`. Onglet `docs` (libellé « Documents ») ajouté à `PRODUCT_TABS` + `VALID_TABS` + tabpanel, URL-state `?tab=docs`, `tenantId={user.tenantId}` passé par la page (variable réelle). Smoke product-tabs étendu (5 tabs + ProductDocsTab, 25 tests verts).

## Garde-fous respectés

- **Mapping DocStatusBadge compilant** : `tsc --noEmit` n'ajoute AUCUNE erreur sur mes fichiers (`warning="no_proof"` littéral, `state` typé, pas de `title`).
- **Variable tenant réelle** : `user.tenantId` confirmé par grep dans les deux pages avant câblage — pas supposé à l'aveugle.
- **tsc** : seules subsistent les 6 erreurs PRÉ-EXISTANTES dans `redirect-308.test.ts` (WIP next.config.mjs non commité, hors scope — non corrigées).
- **Tests** : suite complète verte 867/867 (+3 depuis mes smokes).

## Deviations from Plan

None — plan exécuté tel qu'écrit pour les 3 tâches AUTO.

## Known Stubs

Aucun stub introduit. `UnifiedDocsList` rend des données réelles issues de `resolveDocs` (pas de mock/placeholder). Le concept « stub » manipulé ici est la donnée métier `usedStub` (PedagogicalAsset.rawJson.source==='stub') délibérément rendue visible (NAV-03), pas un stub de code.

## Checkpoint — Task 4 APPROUVÉ par Laurent (2026-06-10)

Vérification visuelle humaine effectuée sur localhost:3010 (règle ≤2 clics + rendu stub/conforme).
**Checkpoint APPROUVÉ.** L'approbation a généré 3 corrections additionnelles, appliquées et commitées :
- **Source unique indicateurs + libellés** : retrait de tous les littéraux « Ind 27 » dans 4 fichiers session, le résolveur lit le catalogue (`7271fed`) ; indicateur séparé du libellé (`e394271`).
- **Dédup version courante** : champs `version`/`isCurrent` additifs (`e394271`) ; groupage hiérarchique produit → sessions → apprenant onglet Docs produit (`95625bf`).
- **3 corrections d'indicateurs RNQ V9** : ASSIDUITE → null (badge financeur, hors rollup ind.12, `9a10cf4`), CERTIFICAT → Ind 11 + mention légale (`4a4ee11`), ATTESTATION_FIN → Légal pur (`5ddba13`), captions Préparation = 1·4·5·6·9·17 / Pack = 8·11·12·30 (`f127535`), verrou de mapping (`def2b46`).

Plan 09.3-03 **complet** (NAV-02 + NAV-03 livrés et validés).

## Self-Check: PASSED
