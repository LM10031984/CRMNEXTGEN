---
phase: 12-modules-stub-inscriptions-et-modeles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/app/inscriptions/page.tsx
  - apps/web/src/app/app/inscriptions/[id]/page.tsx
  - apps/web/src/app/app/page.tsx
  - apps/web/src/components/layout/nav-config.ts
  - apps/web/src/components/layout/__tests__/nav-config.test.ts
  - apps/web/src/components/preinscriptions/new-link-button.tsx
  - apps/web/src/components/preinscriptions/detail-actions.tsx
  - apps/web/src/components/command-palette/command-palette.tsx
  - apps/web/src/server/actions/notifications.ts
  - apps/web/src/server/actions/preinscriptions.ts
  - apps/web/src/server/actions/preinscription-public.ts
  - apps/web/src/server/actions/preinscription-convert.ts
  - apps/web/src/server/actions/preinscription-reminders.ts
  - apps/web/src/server/actions/__tests__/redirect-308.test.ts
  - apps/web/next.config.mjs
autonomous: true
requirements:
  - MOD-01
must_haves:
  truths:
    - "La page admin `/app/inscriptions` affiche la liste des pré-inscriptions (ex-/app/preinscriptions) et plus le stub Placeholder"
    - "Toute URL `/app/preinscriptions` ou `/app/preinscriptions/{id}` retourne un 308 vers `/app/inscriptions(...)` (CLAUDE.md convention)"
    - "La sidebar contient 1 entrée 'Inscriptions' (icône Inbox, ADMIN/MANAGER/COMMERCIAL) et 0 entrée 'Pré-inscriptions' ni doublon 'Inscriptions' générique"
    - "Aucun `href`, `redirect()`, `revalidatePath()` ou `router.push()` ne pointe encore vers `/app/preinscriptions` dans le code applicatif (seules les conservations valides : redirect 308 next.config.mjs, route publique `/preinscription/[token]`, constante MinIO bucket, commentaires/doc)"
    - "Le formulaire public `/preinscription/[token]` reste inchangé et accessible aux apprenants"
    - "Le build Next.js passe : `pnpm --filter @qualiof/web build` exit 0"
  artifacts:
    - path: "apps/web/src/app/app/inscriptions/page.tsx"
      provides: "Page listing inscriptions (ex contenu de preinscriptions/page.tsx, server component)"
      contains: "PreEnrollment"
    - path: "apps/web/src/app/app/inscriptions/[id]/page.tsx"
      provides: "Page détail d'une inscription (ex contenu de preinscriptions/[id]/page.tsx)"
      contains: "PreEnrollmentActions"
    - path: "apps/web/next.config.mjs"
      provides: "Redirects 308 reverse `/app/preinscriptions(...)` → `/app/inscriptions(...)`"
      contains: "/app/preinscriptions"
    - path: "apps/web/src/components/layout/nav-config.ts"
      provides: "1 entrée sidebar 'Inscriptions' avec allowedRoles ADMIN/MANAGER/COMMERCIAL, plus de doublon"
      contains: "label: 'Inscriptions'"
  key_links:
    - from: "next.config.mjs redirects"
      to: "/app/inscriptions"
      via: "permanent: true 308"
      pattern: "source: '/app/preinscriptions"
    - from: "apps/web/src/components/layout/nav-config.ts"
      to: "/app/inscriptions"
      via: "NavItem.href"
      pattern: "href: '/app/inscriptions'"
    - from: "apps/web/src/app/app/page.tsx (dashboard)"
      to: "/app/inscriptions"
      via: "Link/href + PipelineRow"
      pattern: "href=\"/app/inscriptions\""
---

<objective>
Renommer la route admin `/app/preinscriptions` en `/app/inscriptions` pour faire disparaître le stub Placeholder existant et terminer le succès 3 du roadmap (« plus aucun item de sidebar ne renvoie sur une page placeholder » côté Inscriptions). Respect strict des décisions D-01..D-05 verrouillées en `12-CONTEXT.md`.

Purpose: MOD-01 résolu, sidebar nettoyée, formulaire public préservé, pattern « rename route + redirect 308 + grep update » documenté (1ère application projet — STATE.md à mettre à jour côté Plan 03).
Output:
- `apps/web/src/app/app/inscriptions/` contient le code admin (page liste + détail) ex-`/app/preinscriptions/`
- `apps/web/src/app/app/preinscriptions/` n'existe plus
- Redirect 308 reverse `/app/preinscriptions(/:path*)` → `/app/inscriptions(/:path*)` dans `next.config.mjs`
- Sidebar : 1 seule entrée « Inscriptions » (anciennement « Pré-inscriptions ») icône Inbox + RBAC ADMIN/MANAGER/COMMERCIAL ; doublon stub supprimé
- 30+ hrefs/redirects/revalidatePath migrés via grep
- Build Next clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md
@./CLAUDE.md

# Fichiers source à manipuler
@apps/web/next.config.mjs
@apps/web/src/components/layout/nav-config.ts
@apps/web/src/app/app/inscriptions/page.tsx
@apps/web/src/app/app/preinscriptions/page.tsx
@apps/web/src/app/app/preinscriptions/[id]/page.tsx
@apps/web/src/app/app/page.tsx
@apps/web/src/components/command-palette/command-palette.tsx
@apps/web/src/server/actions/notifications.ts

<interfaces>
<!-- Décisions verrouillées CONTEXT.md à respecter verbatim -->

D-01 : `/app/preinscriptions` admin → `/app/inscriptions`. Le stub `apps/web/src/app/app/inscriptions/page.tsx` (20 LOC `<Placeholder palier="Palier 3" ...>`) est remplacé par le contenu actuel de `apps/web/src/app/app/preinscriptions/page.tsx`.

D-02 : Redirect 308 reverse dans `apps/web/next.config.mjs` async redirects() :
```js
{ source: '/app/preinscriptions', destination: '/app/inscriptions', permanent: true },
{ source: '/app/preinscriptions/:path*', destination: '/app/inscriptions/:path*', permanent: true },
```
(les redirects existants `/app/pre-inscriptions → /app/preinscriptions` et `/app/modeles → /app/templates` restent en place : la chaîne `pre-inscriptions → preinscriptions → inscriptions` est OK pour le browser).

D-03 : `apps/web/src/app/preinscription/[token]/page.tsx` (formulaire public tokenisé, sans `/app/` prefix) — **NE PAS TOUCHER**. La sous-arbre `apps/web/src/app/preinscription/` reste hors scope du rename.

D-04 : Sidebar `apps/web/src/components/layout/nav-config.ts` :
- Section « Essentiel », item ligne ~73 : `label: 'Pré-inscriptions', href: '/app/preinscriptions', icon: Inbox, allowedRoles: ['ADMIN','MANAGER','COMMERCIAL']` devient `label: 'Inscriptions', href: '/app/inscriptions', icon: Inbox, allowedRoles: ['ADMIN','MANAGER','COMMERCIAL']`.
- Section « Configuration », item ligne ~158 : `{ label: 'Inscriptions', href: '/app/inscriptions', icon: ListChecks }` est **supprimé** (c'est le doublon stub).
- L'import `ListChecks` peut être supprimé s'il n'est plus utilisé ailleurs dans ce fichier (vérifier avec grep avant retrait).

D-05 : 36 refs à migrer. Liste exhaustive (grep `preinscriptions` apps/web/src/) — distinguer :
* **À migrer (URLs publiques admin)** : hrefs `/app/preinscriptions(/:id)?`, `redirect()`, `router.push()`, `revalidatePath()`, label sidebar/command-palette
* **À garder (PAS DE rename)** :
  - `apps/web/src/app/preinscription/[token]/page.tsx` (D-03)
  - Imports de modules `@/components/preinscriptions/...` (nom de dossier interne, OK)
  - Imports de modules `@/server/actions/preinscriptions` / `preinscription-public` / `preinscription-convert` / `preinscription-reminders` (D-05 : noms internes OK)
  - `apps/web/src/lib/storage.ts:13 export const PREENROLLMENT_BUCKET = 'preinscriptions'` (constante MinIO, ne pas renommer)
  - Commentaires (`* Pattern repris de preinscriptions.ts`) — peuvent rester
  - Tests Wave 0 du Plan 01 (this plan) — référencent les redirects à valider

Sites concrets à éditer :
1. `apps/web/src/app/app/page.tsx:77` → `href="/app/inscriptions"`
2. `apps/web/src/app/app/page.tsx:239` → `<PipelineRow ... href="/app/inscriptions" />` (label `"Pré-inscriptions à valider"` conservé : c'est un libellé métier, pas le label sidebar)
3. `apps/web/src/components/command-palette/command-palette.tsx:51` → `href: '/app/inscriptions'` (le `label: 'Pré-inscriptions à valider'` peut être conservé tel quel pour la recherche par mot-clé)
4. `apps/web/src/components/preinscriptions/new-link-button.tsx:171` → texte d'aide mis à jour `<strong>/app/inscriptions</strong>`
5. `apps/web/src/components/preinscriptions/detail-actions.tsx:132` → `router.push('/app/inscriptions')`
6. `apps/web/src/server/actions/notifications.ts:95` → `href: '/app/inscriptions'` (label de l'alerte `"X pré-inscription(s) à valider"` conservé : terme métier)
7. `apps/web/src/server/actions/preinscriptions.ts:51,65` → `revalidatePath('/app/inscriptions')`
8. `apps/web/src/server/actions/preinscription-public.ts:106,136` → `revalidatePath('/app/inscriptions')`
9. `apps/web/src/server/actions/preinscription-public.ts:137` → `revalidatePath('/app/inscriptions/${preEnrollmentId}')`
10. `apps/web/src/server/actions/preinscription-convert.ts:211,239` → `revalidatePath('/app/inscriptions')`
11. `apps/web/src/server/actions/preinscription-convert.ts:212` → `revalidatePath('/app/inscriptions/${input.preEnrollmentId}')`
12. `apps/web/src/server/actions/preinscription-reminders.ts:119` → `revalidatePath('/app/inscriptions')`
13. `apps/web/src/server/actions/preinscription-reminders.ts:120` → `revalidatePath('/app/inscriptions/${preEnrollmentId}')`
14. `apps/web/src/server/actions/preinscription-reminders.ts:144` → commentaire `* sur /app/inscriptions` (cosmétique)
15. `apps/web/src/app/app/preinscriptions/[id]/page.tsx:60` (avant rename) → `href="/app/inscriptions"` (bouton retour)
16. `apps/web/src/app/app/preinscriptions/page.tsx:142` (avant rename) → `href={\`/app/inscriptions/${r.id}\`}`
17. `apps/web/src/components/layout/__tests__/nav-config.test.ts:56` → snapshot test : remplacer `'/app/preinscriptions'` par `'/app/inscriptions'` ET vérifier qu'il n'y a plus de duplicate

NE PAS RENOMMER les noms de fichiers/dossiers `components/preinscriptions/` ni les imports `@/components/preinscriptions/...` — c'est de la convention interne (D-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 — Test redirect 308 + test sidebar nav-config snapshot</name>
  <files>
    - apps/web/src/server/actions/__tests__/redirect-308.test.ts (NEW)
    - apps/web/src/components/layout/__tests__/nav-config.test.ts (UPDATE)
  </files>
  <read_first>
    - apps/web/src/components/layout/__tests__/nav-config.test.ts (lire l'existant pour comprendre le snapshot pattern)
    - apps/web/next.config.mjs (voir les redirects() existants — c'est une fonction async)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-04 + D-05)
    - apps/web/src/components/layout/nav-config.ts (voir les 2 entrées Inscriptions/Pré-inscriptions actuelles)
  </read_first>
  <behavior>
    - Test 1 (redirect-308.test.ts) : importer `nextConfig` depuis `apps/web/next.config.mjs` ; appeler `await nextConfig.redirects()` ; assert que le tableau retourné contient un objet avec `source === '/app/preinscriptions' && destination === '/app/inscriptions' && permanent === true`.
    - Test 2 (redirect-308.test.ts) : même import ; assert qu'un objet a `source === '/app/preinscriptions/:path*' && destination === '/app/inscriptions/:path*' && permanent === true`.
    - Test 3 (redirect-308.test.ts) : assert que les redirects existants D-01 BUG-03 sont préservés : `/app/pre-inscriptions` → `/app/preinscriptions` (chaîne valide browser) ET `/app/modeles` → `/app/templates`.
    - Test 4 (nav-config.test.ts UPDATE) : assert que `NAV` contient exactement 1 item avec `label === 'Inscriptions'` (et que cet item a `href === '/app/inscriptions'`, `allowedRoles` contient ADMIN/MANAGER/COMMERCIAL).
    - Test 5 (nav-config.test.ts UPDATE) : assert que `NAV` ne contient AUCUN item avec `label === 'Pré-inscriptions'` ni avec `href === '/app/preinscriptions'`.
  </behavior>
  <action>
    1. Créer `apps/web/src/server/actions/__tests__/redirect-308.test.ts` :
       ```ts
       import { describe, it, expect } from 'vitest';
       import nextConfig from '../../../../next.config.mjs';

       describe('next.config.mjs redirects — Phase 12 D-02 reverse alias', () => {
         it('redirige /app/preinscriptions → /app/inscriptions (308)', async () => {
           const list = await nextConfig.redirects();
           expect(list).toContainEqual(
             expect.objectContaining({
               source: '/app/preinscriptions',
               destination: '/app/inscriptions',
               permanent: true,
             }),
           );
         });

         it('redirige /app/preinscriptions/:path* → /app/inscriptions/:path*', async () => {
           const list = await nextConfig.redirects();
           expect(list).toContainEqual(
             expect.objectContaining({
               source: '/app/preinscriptions/:path*',
               destination: '/app/inscriptions/:path*',
               permanent: true,
             }),
           );
         });

         it('préserve les redirects historiques BUG-03 (chaîne pre-inscriptions → preinscriptions → inscriptions OK pour browser)', async () => {
           const list = await nextConfig.redirects();
           expect(list).toContainEqual(
             expect.objectContaining({ source: '/app/pre-inscriptions', destination: '/app/preinscriptions' }),
           );
           expect(list).toContainEqual(
             expect.objectContaining({ source: '/app/modeles', destination: '/app/templates' }),
           );
         });
       });
       ```
       Note : si import depuis `.mjs` pose problème en Vitest, utiliser `import('../../../../next.config.mjs').then(m => m.default)`.
    2. Mettre à jour `apps/web/src/components/layout/__tests__/nav-config.test.ts` :
       - Le test existant à la ligne 56 référence `'/app/preinscriptions'` — le remplacer par `'/app/inscriptions'`.
       - Ajouter ces 2 nouveaux tests dans le describe approprié :
         ```ts
         it('contient exactement 1 entrée Inscriptions (post Phase 12 rename)', () => {
           const all = NAV.flatMap(s => s.items);
           const inscriptions = all.filter(i => i.label === 'Inscriptions');
           expect(inscriptions).toHaveLength(1);
           expect(inscriptions[0].href).toBe('/app/inscriptions');
           expect(inscriptions[0].allowedRoles).toEqual(expect.arrayContaining(['ADMIN', 'MANAGER', 'COMMERCIAL']));
         });

         it('ne contient plus aucune trace de Pré-inscriptions ou /app/preinscriptions', () => {
           const all = NAV.flatMap(s => s.items);
           expect(all.find(i => i.label === 'Pré-inscriptions')).toBeUndefined();
           expect(all.find(i => i.href === '/app/preinscriptions')).toBeUndefined();
         });
         ```
    3. Lancer `pnpm --filter @qualiof/web test -- redirect-308 nav-config` — les 5 nouveaux tests doivent ÉCHOUER (RED) car D-02 et D-04 pas encore appliquées.
    4. Commit RED : `test(12-01): add Wave 0 tests for D-02 redirect + D-04 sidebar rename`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- redirect-308 nav-config 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/server/actions/__tests__/redirect-308.test.ts` returns 0
    - `grep -c "describe('next.config.mjs redirects" apps/web/src/server/actions/__tests__/redirect-308.test.ts` returns 1
    - `grep -c "ne contient plus aucune trace de Pré-inscriptions" apps/web/src/components/layout/__tests__/nav-config.test.ts` returns 1
    - Les tests Wave 0 sont RED (≥3 failures attendues) avant Tasks 1+2
  </acceptance_criteria>
  <done>5 nouveaux tests ajoutés (3 redirect + 2 nav-config). Ils échouent (RED). Commit avec préfixe `test(12-01):`.</done>
</task>

<task type="auto">
  <name>Task 1: Déplacement physique fichiers preinscriptions → inscriptions + suppression stub</name>
  <files>
    - apps/web/src/app/app/inscriptions/page.tsx (OVERWRITE — remplace le stub)
    - apps/web/src/app/app/inscriptions/[id]/page.tsx (NEW — depuis preinscriptions/[id])
    - apps/web/src/app/app/preinscriptions/page.tsx (DELETE)
    - apps/web/src/app/app/preinscriptions/[id]/page.tsx (DELETE)
  </files>
  <read_first>
    - apps/web/src/app/app/preinscriptions/page.tsx (lire intégralement — c'est ce code qui doit aller dans inscriptions/page.tsx)
    - apps/web/src/app/app/preinscriptions/[id]/page.tsx (lire intégralement — c'est ce code qui doit aller dans inscriptions/[id]/page.tsx)
    - apps/web/src/app/app/inscriptions/page.tsx (stub 20 LOC actuel — il sera ÉCRASÉ)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-01)
  </read_first>
  <action>
    Stratégie : `git mv` pour préserver l'historique, puis écraser le stub.

    1. Supprimer le stub actuel (Placeholder) :
       ```bash
       rm apps/web/src/app/app/inscriptions/page.tsx
       ```
    2. Move physique avec git pour préserver le blame :
       ```bash
       git mv apps/web/src/app/app/preinscriptions/page.tsx apps/web/src/app/app/inscriptions/page.tsx
       mkdir -p apps/web/src/app/app/inscriptions
       git mv apps/web/src/app/app/preinscriptions/[id]/page.tsx apps/web/src/app/app/inscriptions/[id]/page.tsx
       ```
       (Si pas de repo git ou git mv échoue, fallback : `mv` natifs ; le blame est secondaire.)
    3. Supprimer le dossier `apps/web/src/app/app/preinscriptions/` (devenu vide après les mv) :
       ```bash
       rmdir apps/web/src/app/app/preinscriptions/[id]
       rmdir apps/web/src/app/app/preinscriptions
       ```
    4. Mettre à jour les hrefs internes des 2 fichiers déplacés (D-05) :
       - Dans `apps/web/src/app/app/inscriptions/page.tsx` ligne ~142 (anciennement preinscriptions/page.tsx:142) : `href={\`/app/inscriptions/${r.id}\`}` (au lieu de `/app/preinscriptions/${r.id}`).
       - Dans `apps/web/src/app/app/inscriptions/[id]/page.tsx` ligne ~60 (anciennement preinscriptions/[id]/page.tsx:60) : `href="/app/inscriptions"` (au lieu de `/app/preinscriptions`).
    5. Vérifier les imports relatifs des 2 fichiers : tous les imports en `@/components/...` ou `@/lib/...` ou `@/server/...` sont OK (alias absolu, pas affecté par le move). Pas d'imports relatifs `./` ni `../../` à craindre car ces 2 pages n'avaient pas de helpers locaux dans `preinscriptions/`.
    6. Commit GREEN partiel : `feat(12-01): move admin preinscriptions pages to /app/inscriptions (D-01)`.
  </action>
  <verify>
    <automated>test -f apps/web/src/app/app/inscriptions/page.tsx && test -f apps/web/src/app/app/inscriptions/\[id\]/page.tsx && test ! -d apps/web/src/app/app/preinscriptions && grep -c "Placeholder" apps/web/src/app/app/inscriptions/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/app/app/inscriptions/page.tsx` returns 0
    - `test -f "apps/web/src/app/app/inscriptions/[id]/page.tsx"` returns 0
    - `test ! -e apps/web/src/app/app/preinscriptions` returns 0 (dossier supprimé)
    - `grep -c "Placeholder" apps/web/src/app/app/inscriptions/page.tsx` returns 0 (le stub a disparu)
    - `grep -c "import.*PreEnrollment\|preEnrollment\|PreEnrollmentActions" apps/web/src/app/app/inscriptions/page.tsx apps/web/src/app/app/inscriptions/\[id\]/page.tsx` returns ≥2 (le vrai contenu admin a bien migré)
    - `grep -rn "/app/preinscriptions" apps/web/src/app/app/inscriptions/` returns 0 (les hrefs internes des 2 pages ont été mis à jour)
  </acceptance_criteria>
  <done>Pages admin déplacées vers `/app/inscriptions/{page,[id]/page}.tsx`. Stub Placeholder supprimé. Dossier `apps/web/src/app/app/preinscriptions/` n'existe plus. Hrefs internes des 2 pages migrées vers `/app/inscriptions`. Préservation D-03 : `apps/web/src/app/preinscription/[token]/` non touché (hors `/app/`).</done>
</task>

<task type="auto">
  <name>Task 2: Migration des refs hardcodées + redirect 308 + sidebar nav-config</name>
  <files>
    - apps/web/next.config.mjs
    - apps/web/src/components/layout/nav-config.ts
    - apps/web/src/app/app/page.tsx
    - apps/web/src/components/preinscriptions/new-link-button.tsx
    - apps/web/src/components/preinscriptions/detail-actions.tsx
    - apps/web/src/components/command-palette/command-palette.tsx
    - apps/web/src/server/actions/notifications.ts
    - apps/web/src/server/actions/preinscriptions.ts
    - apps/web/src/server/actions/preinscription-public.ts
    - apps/web/src/server/actions/preinscription-convert.ts
    - apps/web/src/server/actions/preinscription-reminders.ts
  </files>
  <read_first>
    - apps/web/next.config.mjs (voir bloc redirects())
    - apps/web/src/components/layout/nav-config.ts (lire intégralement — 2 entrées à modifier section Essentiel ligne ~73 + section Configuration ligne ~158)
    - apps/web/src/app/app/page.tsx (lignes 77 + 239 — `href` + `PipelineRow`)
    - apps/web/src/components/command-palette/command-palette.tsx (ligne ~51)
    - apps/web/src/server/actions/notifications.ts (lignes ~91-96)
    - apps/web/src/server/actions/preinscriptions.ts (lignes 51, 65)
    - apps/web/src/server/actions/preinscription-public.ts (lignes 106, 136, 137)
    - apps/web/src/server/actions/preinscription-convert.ts (lignes 211, 212, 239)
    - apps/web/src/server/actions/preinscription-reminders.ts (lignes 119, 120, 144)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-02 + D-04 + D-05)
  </read_first>
  <action>
    1. **`apps/web/next.config.mjs`** — Ajouter 2 redirects D-02 DANS le tableau retourné par `async redirects()`. Bloc final attendu (les 4 existants + 2 nouveaux) :
       ```js
       async redirects() {
         return [
           { source: '/app/pre-inscriptions', destination: '/app/preinscriptions', permanent: true },
           { source: '/app/pre-inscriptions/:path*', destination: '/app/preinscriptions/:path*', permanent: true },
           // Phase 12 D-02 reverse alias : route admin renommée
           { source: '/app/preinscriptions', destination: '/app/inscriptions', permanent: true },
           { source: '/app/preinscriptions/:path*', destination: '/app/inscriptions/:path*', permanent: true },
           { source: '/app/modeles', destination: '/app/templates', permanent: true },
           { source: '/app/modeles/:path*', destination: '/app/templates/:path*', permanent: true },
         ];
       },
       ```
       Note importante : la chaîne `pre-inscriptions → preinscriptions → inscriptions` est OK pour le browser (Next.js redirect chains résolvent en double-hop ; les bookmarks utilisateurs aboutissent à `/app/inscriptions` au final).

    2. **`apps/web/src/components/layout/nav-config.ts`** — 2 modifications :
       a) Section « Essentiel » (ligne ~73) — remplacer :
          ```ts
          {
            label: 'Pré-inscriptions',
            href: '/app/preinscriptions',
            icon: Inbox,
            allowedRoles: ['ADMIN', 'MANAGER', 'COMMERCIAL'],
          },
          ```
          par :
          ```ts
          {
            label: 'Inscriptions',
            href: '/app/inscriptions',
            icon: Inbox,
            allowedRoles: ['ADMIN', 'MANAGER', 'COMMERCIAL'],
          },
          ```
       b) Section « Configuration » (ligne ~158) — **SUPPRIMER** la ligne complète :
          ```ts
          { label: 'Inscriptions', href: '/app/inscriptions', icon: ListChecks },
          ```
       c) Si `ListChecks` n'est plus référencé ailleurs dans le fichier après suppression : `grep -c "ListChecks" apps/web/src/components/layout/nav-config.ts` doit retourner 1 (seulement l'import). Dans ce cas, retirer aussi `ListChecks,` de la liste d'imports `import { ... } from 'lucide-react'` en haut du fichier.

    3. **`apps/web/src/app/app/page.tsx`** :
       - Ligne 77 : `href="/app/preinscriptions"` → `href="/app/inscriptions"`
       - Ligne 239 : `<PipelineRow icon={Inbox} label="Pré-inscriptions à valider" value={...} href="/app/preinscriptions" />` → remplacer SEULEMENT le href : `href="/app/inscriptions"`. Le label « Pré-inscriptions à valider » est conservé : c'est un libellé métier (compteur de pipeline), pas un libellé de navigation.

    4. **`apps/web/src/components/command-palette/command-palette.tsx`** ligne 51 :
       - `href: '/app/preinscriptions'` → `href: '/app/inscriptions'`
       - Label conservé : `'Pré-inscriptions à valider'` (terme métier de recherche). Garder aussi `keywords: 'préinscription valider apprenant'` (ces mots-clés permettent à l'utilisateur de trouver l'item via Cmd+K en tapant l'ancien nom).

    5. **`apps/web/src/components/preinscriptions/new-link-button.tsx`** ligne 171 :
       - `<strong>/app/preinscriptions</strong>` → `<strong>/app/inscriptions</strong>` (texte d'aide affiché à l'utilisateur).

    6. **`apps/web/src/components/preinscriptions/detail-actions.tsx`** ligne 132 :
       - `router.push('/app/preinscriptions')` → `router.push('/app/inscriptions')`.

    7. **`apps/web/src/server/actions/notifications.ts`** ligne 95 :
       - `href: '/app/preinscriptions'` → `href: '/app/inscriptions'`.
       - Le label de l'alerte ligne 94 (`${preinscriptionsToValidate} pré-inscription${...} à valider`) est CONSERVÉ : c'est un terme métier.

    8. **`apps/web/src/server/actions/preinscriptions.ts`** :
       - Ligne 51 : `revalidatePath('/app/preinscriptions')` → `revalidatePath('/app/inscriptions')`
       - Ligne 65 : idem

    9. **`apps/web/src/server/actions/preinscription-public.ts`** :
       - Ligne 106 : `revalidatePath('/app/preinscriptions')` → `revalidatePath('/app/inscriptions')`
       - Ligne 136 : idem
       - Ligne 137 : `revalidatePath(\`/app/preinscriptions/${preEnrollmentId}\`)` → `revalidatePath(\`/app/inscriptions/${preEnrollmentId}\`)`

    10. **`apps/web/src/server/actions/preinscription-convert.ts`** :
        - Ligne 211 : `revalidatePath('/app/preinscriptions')` → `revalidatePath('/app/inscriptions')`
        - Ligne 212 : `revalidatePath(\`/app/preinscriptions/${input.preEnrollmentId}\`)` → `revalidatePath(\`/app/inscriptions/${input.preEnrollmentId}\`)`
        - Ligne 239 : `revalidatePath('/app/preinscriptions')` → `revalidatePath('/app/inscriptions')`

    11. **`apps/web/src/server/actions/preinscription-reminders.ts`** :
        - Ligne 119 : `revalidatePath('/app/preinscriptions')` → `revalidatePath('/app/inscriptions')`
        - Ligne 120 : `revalidatePath(\`/app/preinscriptions/${preEnrollmentId}\`)` → `revalidatePath(\`/app/inscriptions/${preEnrollmentId}\`)`
        - Ligne 144 : commentaire `* sur /app/preinscriptions et par le scheduler` → `* sur /app/inscriptions et par le scheduler` (cosmétique mais évite confusion future).

    12. **NE PAS MODIFIER** :
        - `apps/web/src/app/preinscription/[token]/page.tsx` (D-03)
        - `apps/web/src/lib/storage.ts:13 PREENROLLMENT_BUCKET = 'preinscriptions'` (constante MinIO, nom du bucket S3 — ne pas casser le stockage existant)
        - `apps/web/src/components/preinscriptions/` (nom de dossier interne, D-05)
        - Imports `@/server/actions/preinscriptions` ou `@/components/preinscriptions/*` (idem D-05)
        - Les références à `preinscriptions` dans les commentaires `// Pattern repris de preinscriptions.ts` (D-05 noms internes)

    13. Lancer `pnpm --filter @qualiof/web test -- redirect-308 nav-config` — les 5 tests Wave 0 doivent maintenant PASSER (GREEN).

    14. Commit : `feat(12-01): rename /app/preinscriptions → /app/inscriptions + redirect 308 + sidebar (D-02..D-05)`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- redirect-308 nav-config 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "source: '/app/preinscriptions'" apps/web/next.config.mjs` returns 1 (redirect ajouté D-02)
    - `grep -c "source: '/app/preinscriptions/:path\\*'" apps/web/next.config.mjs` returns 1 (variante :path*)
    - `grep -c "label: 'Inscriptions'" apps/web/src/components/layout/nav-config.ts` returns 1 (D-04 — 1 seule entrée, pas le doublon)
    - `grep -c "label: 'Pré-inscriptions'" apps/web/src/components/layout/nav-config.ts` returns 0
    - `grep -rn "href=\"/app/preinscriptions\"" apps/web/src/` returns 0
    - `grep -rn "href: '/app/preinscriptions'" apps/web/src/` returns 0
    - `grep -rn "router.push('/app/preinscriptions')" apps/web/src/` returns 0
    - `grep -rn "revalidatePath('/app/preinscriptions" apps/web/src/` returns 0
    - `grep -rn "revalidatePath(\`/app/preinscriptions" apps/web/src/` returns 0
    - `test -f apps/web/src/app/preinscription/\[token\]/page.tsx` returns 0 (D-03 préservé)
    - `grep -c "PREENROLLMENT_BUCKET = 'preinscriptions'" apps/web/src/lib/storage.ts` returns 1 (constante MinIO préservée)
    - Les 5 tests Wave 0 Task 0 sont GREEN
  </acceptance_criteria>
  <done>Redirect 308 D-02 actif (next.config.mjs). Sidebar nav-config nettoyée (D-04 : 1 entrée 'Inscriptions' + 0 doublon). 30+ refs hardcodées migrées (D-05). Formulaire public `/preinscription/[token]` intact (D-03). Constante MinIO bucket préservée. Wave 0 GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke build + grep final defense-in-depth + commit</name>
  <files>
    - (aucun nouveau fichier ; smoke + commit)
  </files>
  <read_first>
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (final review : all D-01..D-05 respectées)
  </read_first>
  <action>
    1. Smoke build complet :
       ```bash
       pnpm --filter @qualiof/web build 2>&1 | tail -50
       ```
       Si erreur de build (typedRoutes pourrait gueuler sur d'éventuelles URL hardcodées résiduelles `/app/preinscriptions` non détectées par grep) → identifier et corriger.

    2. Grep final defense-in-depth :
       ```bash
       grep -rn "preinscriptions" apps/web/src/ | grep -v -E "(import|from|components/preinscriptions|server/actions/preinscription|PREENROLLMENT_BUCKET|preinscription-|Pré-inscription|preinscriptionsToValidate|PreEnrollment|preEnrollment|@/server/actions/preinscription|// |/\* |preinscription/\[token\]|reference_qualiopi|test-)" 2>&1
       ```
       Idéalement 0 lignes. Sinon, vérifier chaque match restant :
       - Si c'est un commentaire ou une référence à un nom interne → OK (D-05)
       - Si c'est une URL `/app/preinscriptions` → CORRIGER

    3. Smoke navigation manuel (Laurent dans Plan 03) : indiquer dans le commit message les flows à valider :
       - `curl -sI http://localhost:3010/app/preinscriptions` → `HTTP/1.1 308` + `location: /app/inscriptions`
       - `curl -sI http://localhost:3010/app/preinscriptions/abc-123` → `308` + `location: /app/inscriptions/abc-123`
       - `curl -sI http://localhost:3010/app/pre-inscriptions` → `308` + `location: /app/preinscriptions` (qui redirigera à son tour)

    4. Commit final : `chore(12-01): smoke build OK + grep validation MOD-01 complete`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web build 2>&1 | grep -E "(Compiled successfully|error|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @qualiof/web build` exit code 0 (build clean)
    - `grep -rn "href=\"/app/preinscriptions\"\\|href: '/app/preinscriptions'\\|router.push('/app/preinscriptions" apps/web/src/` returns 0
    - `grep -rn "revalidatePath.*'/app/preinscriptions\\|revalidatePath.*\`/app/preinscriptions" apps/web/src/` returns 0
    - **Defense-in-depth grep D-05** : `grep -rn "/app/preinscriptions" apps/web/src/ | grep -v "next.config\\|preinscription/\\[token\\]\\|PREENROLLMENT_BUCKET\\|^[[:space:]]*#\\|^[[:space:]]*//" | wc -l` returns 0 (zéro URL admin résiduelle hors exclusions explicites D-03/D-05)
    - `test -d apps/web/src/app/preinscription/\[token\]` returns 0 (route publique préservée)
    - `pnpm --filter @qualiof/web test -- redirect-308 nav-config` exit code 0 (tests GREEN)
  </acceptance_criteria>
  <done>Build Next clean. Grep final validation : aucune URL admin `/app/preinscriptions` résiduelle (hors redirect lui-même, route publique préservée D-03, constante MinIO préservée). MOD-01 résolu côté code. Validation visuelle 3 rôles renvoyée au Plan 03 (checkpoint Laurent).</done>
</task>

</tasks>

<verification>
**Phase requirement MOD-01 vérifiable :**
- Sidebar : `grep -c "label: 'Inscriptions'" apps/web/src/components/layout/nav-config.ts` → 1 ; `grep -c "label: 'Pré-inscriptions'" ...` → 0
- Plus de stub : `grep -c "Placeholder" apps/web/src/app/app/inscriptions/page.tsx` → 0
- Redirect 308 : test `redirect-308.test.ts` green
- Build : `pnpm --filter @qualiof/web build` exit 0
- Route publique préservée (D-03) : `test -f apps/web/src/app/preinscription/[token]/page.tsx` exit 0
</verification>

<success_criteria>
1. `/app/inscriptions` affiche la liste admin (server component avec PreEnrollment, plus le Placeholder)
2. `/app/preinscriptions` retourne un 308 (testable au runtime via curl ou Vitest sur next.config)
3. Sidebar = 1 seule entrée « Inscriptions » dans la section Essentiel (icône Inbox, allowedRoles ADMIN/MANAGER/COMMERCIAL) ; le doublon stub de la section Configuration est supprimé
4. Build Next.js clean
5. Le formulaire public `/preinscription/[token]` reste intouché (D-03)
6. Constante MinIO `PREENROLLMENT_BUCKET = 'preinscriptions'` préservée (pas de breaking change stockage)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/12-modules-stub-inscriptions-et-modeles/12-01-SUMMARY.md` documentant :
- Décisions D-01..D-05 appliquées (avec preuves grep)
- Liste exacte des 11 fichiers modifiés + 2 supprimés + 2 déplacés
- Pattern « rename route + redirect 308 + grep update » établi (1ère application projet — convention à documenter par Plan 03 dans STATE.md)
- Notes : route publique `/preinscription/[token]` préservée ; constante MinIO `PREENROLLMENT_BUCKET` préservée ; libellés métier (`'Pré-inscriptions à valider'` dans command-palette et notifications) conservés volontairement
</output>
