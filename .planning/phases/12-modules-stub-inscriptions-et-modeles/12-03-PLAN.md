---
phase: 12-modules-stub-inscriptions-et-modeles
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md
  - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md
autonomous: false
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "MOD-01 et MOD-02 sont marqués [x] dans REQUIREMENTS.md avec preuves (paths fichiers + counts grep)"
    - "ROADMAP.md Phase 12 status = Complete avec date 2026-05-25 (ou date d'exécution effective) + liste des 3 plans cochés"
    - "STATE.md `Current Position` mis à jour : Phase 12 close, prochaine phase = Phase 10 (Audit Qualiopi blanc, seule restante v5)"
    - "STATE.md documente la convention `renommage de route` (1ère application projet : move + redirect 308 + grep update, base pour futurs renommages)"
    - "12-SUMMARY.md récapitule les 3 plans + preuves grep des décisions D-01..D-11 appliquées"
    - "12-SMOKE.md liste les flows manuels à valider par Laurent (login 3 rôles + URLs /app/inscriptions + /app/templates + redirects 308)"
    - "Checkpoint Laurent : validation que la sidebar est propre (0 placeholder restant côté Inscriptions ET Modèles)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Position projet actualisée + convention renommage de route documentée"
      contains: "Phase 12"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MOD-01 et MOD-02 cochés [x] avec preuves"
      contains: "MOD-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 Complete + ligne tableau Progress mise à jour"
      contains: "12. Modules stub"
    - path: ".planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md"
      provides: "Récap consolidé phase + traçabilité D-01..D-11"
      contains: "Phase 12"
    - path: ".planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md"
      provides: "Flows manuels à valider par Laurent (avant clôture définitive)"
      contains: "Flow 1"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Plans 12-01 + 12-02"
      via: "preuves grep"
      pattern: "Plan 12-01\\|Plan 12-02"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/12-.../12-0[1-3]-PLAN.md"
      via: "liste plans cochés"
      pattern: "12-01-PLAN.md"
---

<objective>
Bookkeeping de fin de Phase 12 : documenter MOD-01 + MOD-02 livrés dans REQUIREMENTS.md, marquer Phase 12 Complete dans ROADMAP.md, actualiser STATE.md avec la convention « renommage de route » (1ère application projet), produire 12-SUMMARY.md + 12-SMOKE.md, et obtenir validation Laurent (checkpoint human-verify) avant de clôturer.

Purpose: Trace audit-compliant des décisions D-01..D-11 et des preuves grep. Capitalisation de la convention de renommage pour futurs renames (FR kebab-case, redirect 308 obligatoire, grep update systématique).
Output:
- REQUIREMENTS.md : MOD-01 + MOD-02 cochés [x] avec preuves
- ROADMAP.md : Phase 12 Complete + status table mis à jour
- STATE.md : position actualisée + convention renommage documentée
- 12-SUMMARY.md : récap phase
- 12-SMOKE.md : 5+ flows manuels pour validation Laurent
- Checkpoint Laurent : valide sidebar propre (0 placeholder)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md
@.planning/phases/12-modules-stub-inscriptions-et-modeles/12-01-PLAN.md
@.planning/phases/12-modules-stub-inscriptions-et-modeles/12-02-PLAN.md
@./CLAUDE.md

<!-- Si livrés par exécution séquentielle, charger aussi : -->
<!-- @.planning/phases/12-modules-stub-inscriptions-et-modeles/12-01-SUMMARY.md -->
<!-- @.planning/phases/12-modules-stub-inscriptions-et-modeles/12-02-SUMMARY.md -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mettre à jour REQUIREMENTS.md (MOD-01 + MOD-02 cochés)</name>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (section "📦 Modules stub à clarifier" lignes 77-78)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-01-PLAN.md (pour récap des changements)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-02-PLAN.md (pour récap des changements)
  </read_first>
  <action>
    Dans `.planning/REQUIREMENTS.md`, section `### 📦 Modules stub à clarifier`, remplacer les 2 lignes :

    AVANT :
    ```
    - [ ] **MOD-01** : Module "Inscriptions" — décider du périmètre (vs Sessions+Participants existant). Soit vue agrégée par produit, soit module à supprimer.
    - [ ] **MOD-02** : Module "Modèles de documents" — décider entre éditeur de templates customisables vs simple liste lecture seule des templates existants.
    ```

    APRÈS :
    ```
    - [x] **MOD-01** : Module "Inscriptions" — décision : **renommer la route admin** `/app/preinscriptions` → `/app/inscriptions`. **DONE 2026-05-25** — Plan 12-01 : (1) move physique `apps/web/src/app/app/preinscriptions/{page,[id]/page}.tsx` → `apps/web/src/app/app/inscriptions/{page,[id]/page}.tsx` (git mv + stub Placeholder supprimé) ; (2) redirect 308 reverse `/app/preinscriptions(/:path*)` → `/app/inscriptions(/:path*)` dans `apps/web/next.config.mjs` (D-02) ; (3) sidebar nav-config : entrée 'Pré-inscriptions' renommée 'Inscriptions' (section Essentiel, icône Inbox, allowedRoles ADMIN/MANAGER/COMMERCIAL préservés) + doublon stub 'Inscriptions' section Configuration supprimé (D-04) ; (4) 30+ refs hardcodées migrées (hrefs, redirects, router.push, revalidatePath) selon liste exhaustive grep (D-05) ; (5) route publique `/preinscription/[token]` PRÉSERVÉE (D-03) + constante MinIO `PREENROLLMENT_BUCKET = 'preinscriptions'` préservée. Tests Wave 0 verts (5 tests : 3 redirect-308 + 2 nav-config snapshot). Build Next.js clean. Pattern « renommage de route » documenté dans STATE.md (1ère application projet).
    - [x] **MOD-02** : Module "Modèles de documents" — décision : **liste read-only des templates existants** (pas d'éditeur, pas de versioning, pas de BDD). **DONE 2026-05-25** — Plan 12-02 : (1) catalogue centralisé `apps/web/src/lib/templates-catalog.ts` (D-10) avec 25+ entrées typées (TemplateCatalogEntry) réparties dans 3 catégories : Qualiopi (≥10 closure + convention + programme + convocation + legal-docs + invoice + veille-audit), AGEFICE (3), Email MJML (5) ; helpers `getTemplatesByCategory`, `getTemplateById`, `countByCategory`, `CATEGORY_LABELS` ; (2) `apps/web/src/app/app/templates/page.tsx` réécrit en Server Component listing par catégorie (tableau Template/Source/Variables) ; (3) RBAC `requireRole(['ADMIN','MANAGER','LECTEUR'])` côté page + `allowedRoles: ['ADMIN','MANAGER','LECTEUR']` côté sidebar nav-config (D-09) — FORMATEUR/COMMERCIAL/COMPTABLE exclus ; (4) D-11 V1 sans preview Gotenberg (décision planner — coût ROI insuffisant, screenshots statiques v2 possible) — note V1 affichée dans la page pour transparence utilisateur. Tests Wave 0 verts (10 tests : 6 catalogue + 4 page smoke). Build Next.js clean.
    ```

    Note : la date « 2026-05-25 » est à remplacer par la date effective d'exécution si différente (commande `date +%Y-%m-%d`).
  </action>
  <verify>
    <automated>grep -c "^\- \[x\] \*\*MOD-0[12]\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\- \\[x\\] \\*\\*MOD-01\\*\\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^\\- \\[x\\] \\*\\*MOD-02\\*\\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^\\- \\[ \\] \\*\\*MOD-0[12]\\*\\*" .planning/REQUIREMENTS.md` returns 0
    - `grep -c "Plan 12-01" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "Plan 12-02" .planning/REQUIREMENTS.md` returns 1
  </acceptance_criteria>
  <done>MOD-01 et MOD-02 cochés [x] avec preuves complètes (paths fichiers, counts grep, références Plans 12-01/12-02). Date effective utilisée.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre à jour ROADMAP.md (Phase 12 Complete) + STATE.md (position + convention renommage)</name>
  <files>
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (ligne 24 Phase 12 + section "### Phase 12: Modules stub Inscriptions et Modèles" lignes 208-216 + tableau Progress ligne 237)
    - .planning/STATE.md (lignes 1-40 : frontmatter + Current Position + Accumulated Context)
  </read_first>
  <action>
    ### A. ROADMAP.md — 3 modifications :

    1. Ligne 24 : `- [ ] **Phase 12: Modules stub Inscriptions et Modèles** - ...` → `- [x] **Phase 12: Modules stub Inscriptions et Modèles** - Trancher périmètre, livrer ou retirer`

    2. Section `### Phase 12: Modules stub Inscriptions et Modèles` (lignes 208-216) — remplacer `**Plans**: TBD` par :
       ```
       **Plans:** 3/3 plans complete
       **Plans**:
       - [x] 12-01-PLAN.md — MOD-01 : rename /app/preinscriptions → /app/inscriptions + redirect 308 + sidebar D-04 + 30+ refs migrées D-05 (route publique D-03 préservée)
       - [x] 12-02-PLAN.md — MOD-02 : catalogue centralisé `lib/templates-catalog.ts` (25+ entries D-07/D-10) + Server Component listing /app/templates + RBAC ADMIN+MANAGER+LECTEUR D-09 (V1 sans preview D-11)
       - [x] 12-03-PLAN.md — Bookkeeping : REQUIREMENTS/STATE/ROADMAP + SUMMARY/SMOKE + checkpoint Laurent
       ```

    3. Tableau "Progress" ligne 237 : `| 12. Modules stub Inscriptions et Modèles | 0/TBD | Not started | - |` → `| 12. Modules stub Inscriptions et Modèles | 3/3 | Complete    | 2026-05-25 |`

    ### B. STATE.md — 3 modifications :

    1. **Frontmatter** lignes 5-7 : mettre à jour `stopped_at` et `last_updated`/`last_activity` :
       ```yaml
       stopped_at: Phase 12 complete (MOD-01 rename preinscriptions→inscriptions + MOD-02 catalogue templates read-only)
       last_updated: "2026-05-25T<HH:MM:SS>Z"
       last_activity: "2026-05-25 — Phase 12 close : MOD-01 rename route admin /app/preinscriptions → /app/inscriptions (move git mv + redirect 308 + sidebar 1 entrée + 30+ refs migrées + route publique /preinscription/[token] préservée D-03 + constante MinIO bucket préservée) + MOD-02 catalogue lib/templates-catalog.ts (25+ entries 3 catégories qualiopi/agefice/email + Server Component listing /app/templates + RBAC ADMIN+MANAGER+LECTEUR D-09). V1 sans preview Gotenberg (D-11 décision planner). Convention 'renommage de route' établie (1ère application projet : move + redirect 308 + grep update systématique). 15 tests Wave 0 verts (5 redirect-308/nav-config + 6 catalogue + 4 page smoke). Build Next.js clean. Plus aucun placeholder dans la sidebar."
       progress:
         total_phases: 14
         completed_phases: 9
         total_plans: 61
         completed_plans: 49
       ```
       (Ajuster `last_updated` à la date/heure effective d'exécution.)

    2. **Current Position** lignes 28-31 : remplacer par :
       ```
       Phase: 12 (Modules stub Inscriptions et Modèles) — COMPLETE (closed 2026-05-25, 3/3 plans, 2/2 MOD-* requirements, 15 tests Wave 0 verts, 0 placeholder restant dans sidebar)
       Next: /gsd:plan-phase 10 (Audit Qualiopi blanc — seule phase v5 restante : QBLANC-01/02/03)
       ```

    3. **Accumulated Context > Roadmap Evolution** : ajouter une ligne en queue (juste après la dernière ligne `2026-05-25 ...` actuelle) :
       ```
       - 2026-05-25 — Phase 12 closed : MOD-01 + MOD-02 livrés. Plan 12-01 rename `/app/preinscriptions` → `/app/inscriptions` (move git mv 2 fichiers + stub Placeholder supprimé + redirect 308 reverse next.config.mjs + sidebar 1 entrée Inscriptions section Essentiel D-04 + doublon stub Configuration supprimé + 30+ refs hardcodées migrées (hrefs/redirects/router.push/revalidatePath) D-05 + route publique /preinscription/[token] PRÉSERVÉE D-03 + constante MinIO PREENROLLMENT_BUCKET préservée). Plan 12-02 catalogue read-only : lib/templates-catalog.ts source unique D-10 (25+ entries TemplateCatalogEntry typed) + Server Component /app/templates listing par catégorie qualiopi/agefice/email + helpers getTemplatesByCategory/getTemplateById/countByCategory + RBAC requireRole(['ADMIN','MANAGER','LECTEUR']) D-09 + sidebar allowedRoles miroir + V1 sans preview Gotenberg (D-11 décision planner, ROI insuffisant, screenshots statiques v2 possible) + note V1 affichée dans page pour transparence. Convention 'renommage de route' établie (1ère application projet) : pattern [move git mv + redirect 308 dans next.config + grep update systématique apps/web/src/]. Réutilisable pour futurs renames FR kebab-case. 15 tests Wave 0 verts. Build Next clean. Phase v5 restante : Phase 10 (Audit Qualiopi blanc QBLANC-01/02/03).
       ```

    4. **Nouvelle convention** — ajouter (vers la fin de la section "Accumulated Context" — chercher une sous-section "Conventions" ou créer si absente) :
       ```markdown
       ### Renommage de route (convention — 1ère application Phase 12)

       Quand on renomme une route Next.js (ex: `/app/foo` → `/app/bar`) :
       1. **Move physique** : `git mv apps/web/src/app/app/foo/page.tsx apps/web/src/app/app/bar/page.tsx` (et tous les fichiers du dossier). Préserve le blame.
       2. **Stub remplacement** : si la nouvelle route avait un stub Placeholder, le supprimer AVANT le `git mv`.
       3. **Redirect 308 reverse** : ajouter dans `apps/web/next.config.mjs` `async redirects()` :
          ```js
          { source: '/app/foo', destination: '/app/bar', permanent: true },
          { source: '/app/foo/:path*', destination: '/app/bar/:path*', permanent: true },
          ```
          (Convention CLAUDE.md > Routes — toujours `:path*` pour les sous-routes.)
       4. **Grep update systématique** : `grep -rn "/app/foo" apps/web/src/` puis migrer tous les `href`, `redirect()`, `router.push()`, `revalidatePath()`. Les noms de dossiers internes (`components/foo/`, `server/actions/foo*.ts`) et les constantes MinIO peuvent garder leur nom (D-05 Phase 12).
       5. **Sidebar nav-config** : si l'item existait, renommer `label` + `href`. Si doublon stub, supprimer.
       6. **Tests Wave 0** : test redirect-308 (vérifie le tableau retourné par `nextConfig.redirects()`) + test snapshot sidebar (vérifie 1 seule entrée du nouveau label, 0 entrée de l'ancien).
       7. **Préservations explicites** : lister dans le plan les fichiers à NE PAS toucher (ex Phase 12 : route publique `/preinscription/[token]`, constante MinIO `PREENROLLMENT_BUCKET = 'preinscriptions'`).
       8. **Smoke build** : `pnpm --filter @qualiof/web build` doit passer.

       Cf. Phase 12 (`/app/preinscriptions` → `/app/inscriptions`) pour exemple complet.
       ```
       Si une telle section n'existe pas, la créer en `## Workflow Conventions` ou `## Patterns établis`.
  </action>
  <verify>
    <automated>grep -c "^| 12\\. Modules stub.*Complete" .planning/ROADMAP.md && grep -c "Renommage de route" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\- \\[x\\] \\*\\*Phase 12:" .planning/ROADMAP.md` returns 1
    - `grep -c "12-01-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "12-02-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "12-03-PLAN.md" .planning/ROADMAP.md` returns 1
    - `grep -c "| 12\\. Modules stub.*3/3.*Complete" .planning/ROADMAP.md` returns 1
    - `grep -c "Phase 12.*COMPLETE" .planning/STATE.md` returns ≥1
    - `grep -c "/gsd:plan-phase 10" .planning/STATE.md` returns ≥1
    - `grep -c "Renommage de route" .planning/STATE.md` returns ≥1 (convention documentée)
    - `grep -c "git mv" .planning/STATE.md` returns ≥1 (pattern explicite)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 12 Complete avec 3 plans listés. STATE.md actualisé (frontmatter + Current Position + nouvelle ligne Roadmap Evolution + convention « Renommage de route » documentée pour réutilisation future). Prochaine étape claire : /gsd:plan-phase 10.</done>
</task>

<task type="auto">
  <name>Task 3: Créer 12-SUMMARY.md (récap phase) + 12-SMOKE.md (flows manuels Laurent)</name>
  <files>
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md (NEW)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md (NEW)
  </files>
  <read_first>
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (récap décisions D-01..D-11)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-01-PLAN.md
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-02-PLAN.md
    - <!-- Si livrés : 12-01-SUMMARY.md + 12-02-SUMMARY.md -->
  </read_first>
  <action>
    ### A. Créer `12-SUMMARY.md` :

    ```markdown
    # Phase 12: Modules stub Inscriptions et Modèles — SUMMARY

    **Status:** Complete
    **Closed:** 2026-05-25
    **Plans:** 3/3
    **Requirements:** MOD-01 ✅, MOD-02 ✅
    **Tests Wave 0:** 15/15 verts (5 + 10)

    ---

    ## Goal

    Trancher le périmètre des 2 modules placeholder restants dans la sidebar QualiOF et livrer la décision. Success criterion roadmap : **zéro placeholder dans la sidebar**.

    ---

    ## Decisions appliquées (verrouillées CONTEXT.md)

    ### MOD-01 — Renommage `/app/preinscriptions` → `/app/inscriptions`

    | ID | Décision | Preuve |
    |----|----------|--------|
    | D-01 | Move physique de la page admin | `git mv apps/web/src/app/app/preinscriptions/* → /app/inscriptions/*`. Stub Placeholder écrasé. |
    | D-02 | Redirect 308 reverse | `apps/web/next.config.mjs` : 2 entries `/app/preinscriptions(/:path*)` → `/app/inscriptions(/:path*)` |
    | D-03 | Formulaire public PRÉSERVÉ | `apps/web/src/app/preinscription/[token]/page.tsx` non touché |
    | D-04 | Sidebar nav-config | 1 entrée `'Inscriptions'` (icône Inbox, ADMIN/MANAGER/COMMERCIAL) + doublon stub supprimé |
    | D-05 | 30+ refs migrées | Grep `preinscriptions` apps/web/src/ : 0 URL admin restante (les noms internes `components/preinscriptions/`, `server/actions/preinscription*`, constante MinIO `PREENROLLMENT_BUCKET` préservés intentionnellement) |

    ### MOD-02 — Catalogue templates read-only

    | ID | Décision | Preuve |
    |----|----------|--------|
    | D-06 | Read-only (pas d'éditeur, pas de versioning, pas de BDD) | `apps/web/src/app/app/templates/page.tsx` : Server Component listing tableau, aucun handler de mutation |
    | D-07 | 3 catégories : Qualiopi (≥10) + AGEFICE (≥2) + Email (≥4) | `apps/web/src/lib/templates-catalog.ts` : 25+ entrées au total |
    | D-08 | Affichage : nom + catégorie + sourcePath + variables | Tableau 3 colonnes dans page.tsx |
    | D-09 | RBAC ADMIN+MANAGER+LECTEUR | `requireRole(['ADMIN','MANAGER','LECTEUR'])` page + `allowedRoles` sidebar |
    | D-10 | Catalogue centralisé `lib/templates-catalog.ts` | 1 source de vérité, exports TEMPLATES_CATALOG/getTemplatesByCategory/getTemplateById/countByCategory |
    | D-11 | Aperçu V1 SKIPPED | Décision planner — ROI insuffisant. Note V1 affichée dans la page. v2 possible via screenshots statiques |

    ---

    ## Plans

    | Plan | Objective | Tests Wave 0 |
    |------|-----------|--------------|
    | [12-01](./12-01-PLAN.md) | Rename route admin + redirect 308 + sidebar + 30 refs | 5 verts (3 redirect + 2 nav) |
    | [12-02](./12-02-PLAN.md) | Catalogue + Server Component templates + RBAC | 10 verts (6 catalogue + 4 page) |
    | [12-03](./12-03-PLAN.md) | Bookkeeping + SUMMARY + SMOKE + checkpoint Laurent | n/a |

    ---

    ## Fichiers modifiés (phase)

    ### Plan 12-01 (MOD-01)
    - **Déplacés :** `apps/web/src/app/app/preinscriptions/page.tsx` → `/app/inscriptions/page.tsx` ; `[id]/page.tsx` idem
    - **Supprimés :** stub Placeholder `apps/web/src/app/app/inscriptions/page.tsx` (avant move), dossier `apps/web/src/app/app/preinscriptions/`
    - **Modifiés :** `apps/web/next.config.mjs` (2 redirects), `apps/web/src/components/layout/nav-config.ts` (rename + suppression doublon), `apps/web/src/app/app/page.tsx` (2 hrefs), `apps/web/src/components/command-palette/command-palette.tsx` (1 href), `apps/web/src/components/preinscriptions/{new-link-button,detail-actions}.tsx` (2 refs UI), `apps/web/src/server/actions/{preinscriptions,preinscription-public,preinscription-convert,preinscription-reminders,notifications}.ts` (10+ revalidatePath/href)
    - **Tests Wave 0 :** `apps/web/src/server/actions/__tests__/redirect-308.test.ts` (NEW, 3 tests), `apps/web/src/components/layout/__tests__/nav-config.test.ts` (UPDATE, 2 tests ajoutés)

    ### Plan 12-02 (MOD-02)
    - **Créés :** `apps/web/src/lib/templates-catalog.ts` (catalogue + types + helpers)
    - **Réécrits :** `apps/web/src/app/app/templates/page.tsx` (Server Component listing, ex stub Placeholder)
    - **Modifiés :** `apps/web/src/components/layout/nav-config.ts` (allowedRoles ajouté entrée Modèles)
    - **Tests Wave 0 :** `apps/web/src/lib/__tests__/templates-catalog.test.ts` (NEW, 6 tests), `apps/web/src/app/app/templates/__tests__/page.smoke.test.tsx` (NEW, 4 tests)

    ### Plan 12-03 (Bookkeeping)
    - `.planning/REQUIREMENTS.md` (MOD-01/MOD-02 cochés)
    - `.planning/ROADMAP.md` (Phase 12 Complete + tableau Progress)
    - `.planning/STATE.md` (frontmatter + Current Position + convention « Renommage de route »)
    - `.planning/phases/12-.../12-SUMMARY.md` (ce fichier)
    - `.planning/phases/12-.../12-SMOKE.md` (flows manuels)

    ---

    ## Conventions établies / patterns nouveaux

    **« Renommage de route »** — 1ère application projet (documentée dans STATE.md). Réutilisable pour futurs renames :
    1. `git mv` physique des pages
    2. Stub remplacement supprimé AVANT
    3. Redirect 308 reverse dans `next.config.mjs` (avec `:path*`)
    4. Grep update systématique des hrefs/redirects/revalidatePath
    5. Sidebar nav-config : rename + suppression doublons
    6. Tests Wave 0 (redirect + sidebar snapshot)
    7. Préservations explicites (route publique, constantes externes)

    **Catalogue centralisé `lib/templates-catalog.ts`** — réutilisable Phase 10 (Audit Qualiopi blanc) :
    - Export catalogue en PDF pour l'auditeur
    - Cross-reference avec les 32 indicateurs Qualiopi
    - Source unique pour toute introspection des templates

    ---

    ## Notes pour la suite

    - Composant `apps/web/src/components/ui/placeholder.tsx` potentiellement orphelin après Phase 12 (les 2 derniers consommateurs sont supprimés). Vérifier via `grep -rn "from '@/components/ui/placeholder'" apps/web/src/`. **Recommandation : garder** (peut servir à de futurs stubs courts pendant le développement de phases).
    - Phase v5 RESTANTE : **Phase 10 — Audit Qualiopi blanc** (QBLANC-01 + QBLANC-02 + QBLANC-03). Seule phase v5 non livrée. Le catalogue `lib/templates-catalog.ts` posé en Phase 12 sera réutilisé.
    - Aperçu rendu templates (D-11 V2) : si Laurent en a besoin, pattern recommandé = screenshots statiques `apps/web/public/templates-previews/{id}.png` (1 par template, généré 1× via Gotenberg en dev) + colonne "Aperçu" cliquable dans `/app/templates`. ~2h de boulot dans une v6.
    ```

    ### B. Créer `12-SMOKE.md` :

    ```markdown
    # Phase 12 — Smoke Tests Manuels

    À exécuter par Laurent **après** complétion des Plans 12-01 et 12-02, **avant** clôture officielle de la phase (checkpoint Task 4).

    **Préparation :**
    - Stack dev tournante sur localhost:3010 (`pnpm dev:full`)
    - 6 utilisateurs test disponibles avec rôles différents (ADMIN/MANAGER/COMMERCIAL/COMPTABLE/FORMATEUR/LECTEUR)

    ---

    ## Flow 1 — Redirect 308 `/app/preinscriptions` (CLI curl)

    ```bash
    curl -sI http://localhost:3010/app/preinscriptions | grep -E "(HTTP|location)"
    # Attendu :
    # HTTP/1.1 308 Permanent Redirect
    # location: /app/inscriptions

    curl -sI http://localhost:3010/app/preinscriptions/abc-123 | grep -E "(HTTP|location)"
    # Attendu :
    # HTTP/1.1 308 Permanent Redirect
    # location: /app/inscriptions/abc-123

    curl -sI http://localhost:3010/app/pre-inscriptions | grep -E "(HTTP|location)"
    # Attendu (chaîne BUG-03 préservée) :
    # HTTP/1.1 308 Permanent Redirect
    # location: /app/preinscriptions  (qui redirige ensuite vers /app/inscriptions)
    ```

    ☐ OK ☐ KO + détails : __________

    ---

    ## Flow 2 — Sidebar propre (UI navigateur, login ADMIN)

    1. Se connecter en tant qu'ADMIN sur `http://localhost:3010/login`
    2. Observer la sidebar : section "Essentiel"
    3. **Vérifier :**
       - ☐ 1 entrée "Inscriptions" avec icône Inbox (section Essentiel)
       - ☐ 0 entrée "Pré-inscriptions" (le texte n'apparaît plus)
       - ☐ Dans section "Configuration" : 1 entrée "Modèles de documents" (icône FileText), 0 entrée "Inscriptions" (doublon supprimé)
       - ☐ 0 page placeholder "Bientôt disponible" en cliquant sur Inscriptions OU Modèles

    ---

    ## Flow 3 — Page /app/inscriptions (login ADMIN)

    1. Login ADMIN, cliquer "Inscriptions" dans la sidebar
    2. **Vérifier :**
       - ☐ URL = `/app/inscriptions` (pas de redirect)
       - ☐ Page affiche la liste des pré-inscriptions (PreEnrollments tenant-scoped)
       - ☐ Bouton "Nouveau lien" fonctionne (génère token)
       - ☐ Cliquer une pré-inscription → URL = `/app/inscriptions/{id}` (pas `/preinscriptions/{id}`)
       - ☐ Sur la page détail, le bouton "Retour" remonte vers `/app/inscriptions`

    ---

    ## Flow 4 — Page /app/templates par rôle (sidebar filtrée + accès direct)

    Pour CHAQUE rôle, login → vérifier la sidebar ET tenter l'accès direct `http://localhost:3010/app/templates` :

    | Rôle | Sidebar "Modèles de documents" visible ? | `/app/templates` direct |
    |------|-------------------------------------------|--------------------------|
    | ADMIN | ☐ OUI | ☐ Affiche le catalogue 25+ templates |
    | MANAGER | ☐ OUI | ☐ Affiche le catalogue |
    | LECTEUR | ☐ OUI | ☐ Affiche le catalogue |
    | COMMERCIAL | ☐ NON | ☐ Redirect ou 403 (requireRole bloque) |
    | COMPTABLE | ☐ NON | ☐ Redirect ou 403 |
    | FORMATEUR | ☐ NON | ☐ Redirect ou 403 |

    ---

    ## Flow 5 — Catalogue templates content (login ADMIN)

    1. Login ADMIN, ouvrir `/app/templates`
    2. **Vérifier :**
       - ☐ 3 sections affichées : "Documents Qualiopi" / "AGEFICE" / "Templates email"
       - ☐ Section Qualiopi contient ≥10 entrées (attestation, certificat, qcm, déroulé, etc.)
       - ☐ Section AGEFICE contient ≥2 entrées (Fiche AGEFICE, Formulaire 92 champs, Attestation assiduité)
       - ☐ Section Email contient ≥4 entrées (Relance pré-inscription, Invitation utilisateur, Reset MDP, Relance facture, Lead assigné)
       - ☐ Chaque entrée affiche : Label + Description + sourcePath (code monospace) + Variables (chips bleu primary)
       - ☐ Note V1 affichée en bas de page ("Cette vue est en lecture seule...")

    ---

    ## Flow 6 — Formulaire public préservé (D-03)

    1. Sans login (logout d'abord)
    2. Visiter une URL public form pré-inscription : `http://localhost:3010/preinscription/{token-valide-existant}`
    3. **Vérifier :**
       - ☐ Pas de redirect 308
       - ☐ Formulaire public s'affiche normalement (champs apprenant + upload CNI/RIB/CFP)
       - ☐ Soumission fonctionne (revient à l'écran de confirmation)

    ---

    ## Conclusion

    ☐ Tous les flows passent → checkpoint Laurent Task 4 = "approved"
    ☐ Au moins 1 flow échoue → noter détails et créer issue (Plan 12-04 gap closure ou correctif quick)
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md && test -f .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md && grep -c "^## Flow [1-6]" .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md` returns 0
    - `test -f .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md` returns 0
    - `grep -c "D-0[1-9]\\|D-1[01]" .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md` returns ≥11 (toutes les décisions tracées)
    - `grep -c "^## Flow" .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md` returns ≥6
    - `grep -c "/app/preinscriptions\\|/app/inscriptions\\|/app/templates" .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md` returns ≥3
  </acceptance_criteria>
  <done>12-SUMMARY.md récapitule la phase avec mapping D-01..D-11 → preuves + plans + fichiers modifiés + conventions établies. 12-SMOKE.md liste 6 flows manuels exhaustifs (curl redirect + sidebar UI + page Inscriptions + RBAC templates par rôle + content catalogue + formulaire public préservé).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint Laurent — validation visuelle sidebar propre + flows SMOKE</name>
  <what-built>
    Phase 12 complète :
    - Plan 12-01 : `/app/preinscriptions` admin renommée en `/app/inscriptions` (move + redirect 308 + 30 refs migrées + sidebar 1 entrée)
    - Plan 12-02 : `/app/templates` est maintenant une liste read-only de 25+ templates (catalogue centralisé `lib/templates-catalog.ts` + RBAC ADMIN+MANAGER+LECTEUR)
    - Plan 12-03 : REQUIREMENTS/ROADMAP/STATE updated + 12-SUMMARY.md + 12-SMOKE.md créés
  </what-built>
  <how-to-verify>
    1. **Lire** `.planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md`
    2. **Exécuter** les 6 flows :
       - Flow 1 (curl redirects 308) — 30 sec
       - Flow 2 (sidebar propre login ADMIN) — 1 min
       - Flow 3 (page /app/inscriptions fonctionne) — 1 min
       - Flow 4 (RBAC /app/templates par rôle — 3 rôles autorisés + 3 exclus) — 5 min
       - Flow 5 (catalogue content : 3 sections, 25+ entrées, variables affichées) — 2 min
       - Flow 6 (formulaire public `/preinscription/[token]` préservé D-03) — 1 min
    3. **Cocher** les ☐ au fur et à mesure
    4. **Confirmer** que la sidebar ne contient plus AUCUN placeholder (ni Inscriptions stub ni Modèles stub)
  </how-to-verify>
  <resume-signal>
    Tape "approved" si les 6 flows passent → la phase peut être commitée et clôturée définitivement.
    Tape "issue: {détails}" si au moins 1 flow échoue → un Plan 12-04 gap closure sera créé OU un correctif /gsd:quick sera lancé selon l'ampleur.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit final Phase 12 (déclenché après checkpoint approved)</name>
  <files>
    - (commit des fichiers .planning/ déjà modifiés par Tasks 1-3)
  </files>
  <read_first>
    - .planning/STATE.md (vérifier que les modifs Task 2 ont été persistées)
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. Vérifier le statut git :
       ```bash
       git status .planning/
       ```
       Doivent apparaître modifiés : `STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md` ; ajoutés : `12-SUMMARY.md`, `12-SMOKE.md`.

    2. Commit bookkeeping final :
       ```bash
       git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md \
               .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SUMMARY.md \
               .planning/phases/12-modules-stub-inscriptions-et-modeles/12-SMOKE.md
       git commit -m "docs(12-03): bookkeeping Phase 12 close — MOD-01 + MOD-02 ✅"
       ```

    3. Vérifier que `git log -1 --stat` montre les 5 fichiers commités.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -c "Phase 12 close"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:"%s"` contient `Phase 12 close` ou `12-03 bookkeeping`
    - `git status .planning/` retourne working tree clean (pour ces 5 fichiers)
  </acceptance_criteria>
  <done>Phase 12 officiellement close. 5 fichiers de bookkeeping commités. Prochaine étape utilisateur : `/gsd:plan-phase 10` (seule phase v5 restante : Audit Qualiopi blanc).</done>
</task>

</tasks>

<verification>
**Phase 12 globale vérifiable :**
- REQUIREMENTS.md : MOD-01 + MOD-02 cochés [x] avec preuves
- ROADMAP.md : Phase 12 Complete + 3/3 plans + ligne Progress mise à jour
- STATE.md : frontmatter + Current Position + convention « Renommage de route » documentée
- 12-SUMMARY.md + 12-SMOKE.md créés
- Checkpoint Laurent : 6 flows manuels validés
- 0 placeholder restant dans la sidebar (preuve : Flow 2)
- Tests Wave 0 globaux : 15/15 verts (5 Plan 01 + 10 Plan 02)
- Build Next.js clean
</verification>

<success_criteria>
1. Roadmap success #1 (« Inscriptions : décision documentée — vue agrégée OU retrait avec redirect ») : ✅ rename + redirect 308 (Plan 12-01)
2. Roadmap success #2 (« Modèles de documents : décision documentée — éditeur OU liste read-only OU retrait ») : ✅ liste read-only (Plan 12-02)
3. Roadmap success #3 (« Plus aucun item de sidebar ne renvoie sur une page placeholder ») : ✅ validé Flow 2 SMOKE
4. Convention « Renommage de route » établie et documentée dans STATE.md
5. Catalogue centralisé `lib/templates-catalog.ts` posé comme source unique réutilisable (Phase 10 future)
6. Phase 12 officiellement Complete dans ROADMAP.md + STATE.md, prochaine étape claire : Phase 10
</success_criteria>

<output>
Après checkpoint Laurent "approved" + Task 5 commit, la phase est close. Aucun SUMMARY supplémentaire à créer (12-SUMMARY.md déjà produit par Task 3).

User next step : `/gsd:plan-phase 10` (Audit Qualiopi blanc — QBLANC-01/02/03).
</output>
