---
phase: 12-modules-stub-inscriptions-et-modeles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/templates-catalog.ts
  - apps/web/src/lib/__tests__/templates-catalog.test.ts
  - apps/web/src/app/app/templates/page.tsx
  - apps/web/src/app/app/templates/__tests__/page.smoke.test.tsx
  - apps/web/src/components/layout/nav-config.ts
autonomous: true
requirements:
  - MOD-02
must_haves:
  truths:
    - "La page `/app/templates` affiche une liste read-only de 15+ templates (Qualiopi closure + AGEFICE + Email), groupés par catégorie"
    - "Pour chaque template : nom, catégorie, chemin source code, variables disponibles (≥2 listées)"
    - "Plus de Placeholder sur `/app/templates`"
    - "Le catalogue est centralisé dans `apps/web/src/lib/templates-catalog.ts` (1 source de vérité — D-10)"
    - "RBAC respecté : ADMIN+MANAGER+LECTEUR voient l'entrée sidebar + la page (D-09)"
    - "FORMATEUR/COMMERCIAL/COMPTABLE ne voient PAS l'entrée 'Modèles de documents' dans la sidebar (filterNavForRole)"
    - "Aucun éditeur, aucune BDD, aucun versioning — V1 read-only strict (D-06)"
  artifacts:
    - path: "apps/web/src/lib/templates-catalog.ts"
      provides: "Catalogue typé TEMPLATES_CATALOG (≥15 entrées) + types TemplateCategory/TemplateCatalogEntry + helpers getTemplatesByCategory/getTemplateById/groupByCategory"
      exports: ["TEMPLATES_CATALOG", "TemplateCategory", "TemplateCatalogEntry", "getTemplatesByCategory", "getTemplateById"]
    - path: "apps/web/src/app/app/templates/page.tsx"
      provides: "Server Component liste read-only des templates, RBAC ADMIN+MANAGER+LECTEUR"
      contains: "requireRole"
    - path: "apps/web/src/components/layout/nav-config.ts"
      provides: "Entrée sidebar 'Modèles de documents' avec allowedRoles ADMIN+MANAGER+LECTEUR (D-09)"
      contains: "allowedRoles: ['ADMIN', 'MANAGER', 'LECTEUR']"
  key_links:
    - from: "apps/web/src/app/app/templates/page.tsx"
      to: "apps/web/src/lib/templates-catalog.ts"
      via: "import TEMPLATES_CATALOG"
      pattern: "from '@/lib/templates-catalog'"
    - from: "apps/web/src/components/layout/nav-config.ts"
      to: "templates page"
      via: "filterNavForRole + allowedRoles"
      pattern: "href: '/app/templates'.*allowedRoles"
---

<objective>
Remplacer le stub Placeholder de `/app/templates` par une liste read-only des 15+ templates code-driven du projet (Qualiopi closure + AGEFICE + Email MJML), groupés par catégorie. Décisions D-06..D-11 verrouillées en `12-CONTEXT.md` respectées : pas d'éditeur, pas de BDD, pas de versioning, RBAC ADMIN+MANAGER+LECTEUR (D-09).

Purpose: MOD-02 résolu, sidebar nettoyée d'un 2ème placeholder, catalogue centralisé `lib/templates-catalog.ts` posé comme source unique réutilisable plus tard (Phase 10 Audit Qualiopi blanc — D-10).
Output:
- `apps/web/src/lib/templates-catalog.ts` : 1 export `TEMPLATES_CATALOG` (≥15 entrées) + types TS + helpers
- `apps/web/src/app/app/templates/page.tsx` : Server Component listing par catégorie (tableau ou cartes), RBAC `requireRole(['ADMIN','MANAGER','LECTEUR'])`
- `apps/web/src/components/layout/nav-config.ts` : entrée 'Modèles de documents' obtient `allowedRoles: ['ADMIN','MANAGER','LECTEUR']` (D-09)
- D-11 V1 : aperçu rendu PAS livré (trop coûteux à brancher Gotenberg pour chaque template) — décision documentée dans le code (commentaire) et le SUMMARY
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md
@./CLAUDE.md

# Fichiers source à scanner pour construire le catalogue
@apps/web/src/lib/closure/qualiopi-prompts.ts
@apps/web/src/lib/agefice-form-fill.ts
@apps/web/src/lib/agefice-template.ts
@apps/web/src/lib/mailer-templates/user-invitation.ts
@apps/web/src/components/layout/nav-config.ts
@apps/web/src/app/app/templates/page.tsx

# Pattern Server Component + RBAC à cloner
@apps/web/src/lib/rbac.ts
@apps/web/src/app/app/parametres/historique/page.tsx

<interfaces>
<!-- Décisions verrouillées CONTEXT.md à respecter verbatim -->

D-06 : Page `/app/templates` = liste read-only. Pas d'éditeur, pas de versioning, pas de BDD, pas de migration Prisma.

D-07 : 3 catégories à lister (15+ entrées totales). Inventory exhaustif observé via `ls apps/web/src/lib/closure/*-template.ts apps/web/src/lib/*template* apps/web/src/lib/mailer-templates/*.ts apps/web/src/lib/agefice-*.ts apps/web/src/lib/preinscription-reminder-template.ts` :

**Catégorie `qualiopi` (~15 entrées)** :
- `apps/web/src/lib/closure/attestation-template.ts` — Attestation de fin de formation
- `apps/web/src/lib/closure/certificat-template.ts` — Certificat de réalisation
- `apps/web/src/lib/closure/analyse-besoin-template.ts` — Analyse de besoin
- `apps/web/src/lib/closure/qcm-template.ts` — QCM d'évaluation des acquis
- `apps/web/src/lib/closure/deroule-template.ts` — Déroulé pédagogique
- `apps/web/src/lib/closure/emargement-template.ts` — Feuille d'émargement
- `apps/web/src/lib/closure/positionnement-template.ts` — Positionnement initial
- `apps/web/src/lib/closure/satisfaction-chaud-template.ts` — Satisfaction à chaud
- `apps/web/src/lib/closure/satisfaction-froid-template.ts` — Satisfaction à froid
- `apps/web/src/lib/closure/satisfaction-session-template.ts` — Satisfaction session (agrégée)
- `apps/web/src/lib/closure/grille-obs-session-template.ts` — Grille observation session
- `apps/web/src/lib/closure/grille-observation-template.ts` — Grille observation individuelle
- `apps/web/src/lib/closure/checklist-formation-template.ts` — Checklist formation
- `apps/web/src/lib/convention-template.ts` — Convention de formation
- `apps/web/src/lib/programme-template.ts` — Programme de formation
- `apps/web/src/lib/convocation-template.ts` — Convocation
- `apps/web/src/lib/legal-docs-template.ts` — Documents légaux (CGV/RI)
- `apps/web/src/lib/invoice-template.ts` — Facture / Avoir (Phase 11)
- `apps/web/src/lib/veille-audit-template.ts` — Rapport veille Qualiopi (Phase 13)

**Catégorie `agefice` (2-3 entrées)** :
- `apps/web/src/lib/agefice-template.ts` — Fiche AGEFICE HTML
- `apps/web/src/lib/agefice-form-fill.ts` — Formulaire AGEFICE PDF 92 champs
- `apps/web/src/lib/closure/agefice-attendance-template.ts` — Attestation d'assiduité AGEFICE

**Catégorie `email` (4-5 entrées)** :
- `apps/web/src/lib/preinscription-reminder-template.ts` — Email relance pré-inscription
- `apps/web/src/lib/mailer-templates/user-invitation.ts` — Email invitation utilisateur (Phase 8)
- `apps/web/src/lib/mailer-templates/user-password-reset.ts` — Email reset mot de passe (Phase 8)
- `apps/web/src/lib/mailer-templates/invoice-reminder.ts` — Email relance facture (Phase 11)
- `apps/web/src/lib/mailer-templates/lead-assigned.ts` — Email notification lead assigné (Phase 9)

D-08 : Pour chaque template, afficher :
- `label` (nom user-friendly français)
- `category` ('qualiopi' | 'agefice' | 'email')
- `sourcePath` (chemin relatif au repo root, ex: `apps/web/src/lib/closure/attestation-template.ts`)
- `variables` (string[] — extraites manuellement par lecture rapide des `${...}` ou commentaires `Props`/`interface` ; OK de fournir 3-8 vars représentatives par template, pas exhaustif)

D-09 : RBAC `/app/templates` = ADMIN + MANAGER + LECTEUR. FORMATEUR/COMMERCIAL/COMPTABLE exclus côté sidebar (filterNavForRole) ET côté page (`requireRole(['ADMIN','MANAGER','LECTEUR'])`).

D-10 : Catalogue centralisé dans `apps/web/src/lib/templates-catalog.ts` — 1 source de vérité. Pas de mapping disséminé dans le code de la page.

D-11 : Aperçu rendu OPTIONNEL V1. **Décision planner : V1 sans preview**. Justification :
- Brancher Gotenberg/WeasyPrint pour chaque template requerrait des données fictives propres à chaque type de doc (apprenant + session + tenant + 92 champs AGEFICE...) — ~3h de boulot pour un nice-to-have
- La liste avec variables affichées est déjà 80% de la valeur d'audit Qualiopi (l'auditeur veut savoir QUOI est généré, pas voir le rendu graphique)
- Possibilité d'ajouter le preview en V2 si Laurent le réclame
- Si extension future : pattern Phase 4 `lib/pdf-render.ts` réutilisable, screenshots statiques dans `apps/web/public/templates-previews/` plus simple

Structure type catalogue :
```ts
export type TemplateCategory = 'qualiopi' | 'agefice' | 'email';

export interface TemplateCatalogEntry {
  id: string;                  // slug stable, ex: 'closure-attestation'
  label: string;               // nom user-friendly français
  category: TemplateCategory;
  sourcePath: string;          // chemin relatif au repo root
  description: string;         // 1-2 phrases : à quoi sert ce template
  variables: string[];         // 3-8 variables principales, ex: ['apprenantNom', 'session.title', 'session.startDate']
}

export const TEMPLATES_CATALOG: ReadonlyArray<TemplateCatalogEntry> = [
  // qualiopi (15+ entries)
  { id: 'closure-attestation', label: 'Attestation de fin de formation', category: 'qualiopi',
    sourcePath: 'apps/web/src/lib/closure/attestation-template.ts',
    description: 'Attestation Qualiopi délivrée à chaque stagiaire en fin de session.',
    variables: ['apprenantNomComplet', 'sessionTitle', 'sessionStartDate', 'sessionEndDate', 'durationHours', 'tenantName', 'tenantSiret'] },
  // ... (etc — voir Task 1 pour la liste complète)
];

export function getTemplatesByCategory(category: TemplateCategory): TemplateCatalogEntry[] { ... }
export function getTemplateById(id: string): TemplateCatalogEntry | undefined { ... }
```

Pattern Server Component à cloner (Phase 8 historique) — page minimaliste :
```tsx
import { validateRequest } from '@/lib/auth';
import { requireRole } from '@/lib/rbac';
import { TEMPLATES_CATALOG, type TemplateCategory } from '@/lib/templates-catalog';

export const dynamic = 'force-dynamic';

export default async function TemplatesPage() {
  await requireRole(['ADMIN', 'MANAGER', 'LECTEUR']);

  const grouped = ['qualiopi', 'agefice', 'email'] as const;
  // ... render
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 — Tests stubs catalogue + page smoke</name>
  <files>
    - apps/web/src/lib/__tests__/templates-catalog.test.ts (NEW)
    - apps/web/src/app/app/templates/__tests__/page.smoke.test.tsx (NEW)
  </files>
  <read_first>
    - apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts (lire le pattern smoke test Phase 1)
    - apps/web/src/components/layout/__tests__/nav-config.test.ts (pattern test sidebar Phase 8 / Phase 9)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-06..D-11)
  </read_first>
  <behavior>
    - Test 1 (templates-catalog.test.ts) : import `TEMPLATES_CATALOG` → assert `length >= 15` (D-07 : 15+ entrées).
    - Test 2 : assert que les 3 catégories `'qualiopi' | 'agefice' | 'email'` sont toutes représentées dans le catalogue (au moins 1 entrée par catégorie).
    - Test 3 : assert que `getTemplatesByCategory('qualiopi').length >= 10` (D-07 mentionne 10+ templates Qualiopi).
    - Test 4 : assert que chaque entrée a tous les champs requis : `id` non vide string, `label` non vide string, `category` ∈ {qualiopi, agefice, email}, `sourcePath` commence par `apps/web/src/`, `variables` array length >= 2.
    - Test 5 : assert que les `id` sont uniques (Set size === Array length).
    - Test 6 : assert helper `getTemplateById('closure-attestation')` retourne l'entrée attestation OU undefined si non présente (defensive — just check it's a function returning TemplateCatalogEntry | undefined).
    - Test 7 (page.smoke.test.tsx) : import dynamique du module page.tsx — assert que le default export est une fonction async (server component). Pas de rendu réel (server component nécessite mock validateRequest). Simple assertion `typeof Page === 'function'` et que la page importe `TEMPLATES_CATALOG` (vérification grep dans le fichier source : `expect(source).toMatch(/TEMPLATES_CATALOG/)`).
    - Test 8 (page.smoke.test.tsx) : assert que le fichier `page.tsx` ne contient PLUS de `Placeholder` (grep `'<Placeholder'` returns 0 match dans le source string).
  </behavior>
  <action>
    1. Créer `apps/web/src/lib/__tests__/templates-catalog.test.ts` :
       ```ts
       import { describe, it, expect } from 'vitest';
       import {
         TEMPLATES_CATALOG,
         getTemplatesByCategory,
         getTemplateById,
         type TemplateCategory,
       } from '../templates-catalog';

       describe('TEMPLATES_CATALOG — Phase 12 D-06..D-10', () => {
         it('contient au moins 15 entrées (D-07 inventory)', () => {
           expect(TEMPLATES_CATALOG.length).toBeGreaterThanOrEqual(15);
         });

         it('couvre les 3 catégories qualiopi/agefice/email', () => {
           const cats = new Set(TEMPLATES_CATALOG.map(t => t.category));
           expect(cats.has('qualiopi')).toBe(true);
           expect(cats.has('agefice')).toBe(true);
           expect(cats.has('email')).toBe(true);
         });

         it('contient au moins 10 templates Qualiopi (D-07)', () => {
           expect(getTemplatesByCategory('qualiopi').length).toBeGreaterThanOrEqual(10);
         });

         it('chaque entrée a id/label/category/sourcePath/variables valides (D-08)', () => {
           for (const t of TEMPLATES_CATALOG) {
             expect(t.id).toBeTruthy();
             expect(typeof t.label).toBe('string');
             expect(t.label.length).toBeGreaterThan(0);
             expect(['qualiopi', 'agefice', 'email']).toContain(t.category);
             expect(t.sourcePath).toMatch(/^apps\/web\/src\//);
             expect(Array.isArray(t.variables)).toBe(true);
             expect(t.variables.length).toBeGreaterThanOrEqual(2);
           }
         });

         it('ids uniques (clé stable D-10)', () => {
           const ids = TEMPLATES_CATALOG.map(t => t.id);
           expect(new Set(ids).size).toBe(ids.length);
         });

         it('getTemplateById retourne soit une entrée soit undefined', () => {
           expect(typeof getTemplateById).toBe('function');
           const sample = TEMPLATES_CATALOG[0];
           expect(getTemplateById(sample.id)).toEqual(sample);
           expect(getTemplateById('___nope___')).toBeUndefined();
         });
       });
       ```

    2. Créer `apps/web/src/app/app/templates/__tests__/page.smoke.test.tsx` :
       ```ts
       import { describe, it, expect } from 'vitest';
       import { readFileSync } from 'node:fs';
       import path from 'node:path';

       describe('/app/templates page — Phase 12 D-06+D-09', () => {
         const pagePath = path.resolve(__dirname, '../page.tsx');
         const src = readFileSync(pagePath, 'utf-8');

         it('ne contient plus de Placeholder (stub remplacé)', () => {
           expect(src).not.toMatch(/<Placeholder\b/);
         });

         it('importe TEMPLATES_CATALOG (D-10 source unique)', () => {
           expect(src).toMatch(/TEMPLATES_CATALOG/);
         });

         it('applique requireRole ADMIN+MANAGER+LECTEUR (D-09 RBAC)', () => {
           expect(src).toMatch(/requireRole\(\s*\[\s*'ADMIN'\s*,\s*'MANAGER'\s*,\s*'LECTEUR'\s*\]\s*\)/);
         });

         it('export default async function (Server Component)', () => {
           expect(src).toMatch(/export\s+default\s+async\s+function/);
         });
       });
       ```

    3. Lancer `pnpm --filter @qualiof/web test -- templates-catalog page.smoke` — les tests doivent ÉCHOUER (RED — fichiers cible n'existent pas encore / stub Placeholder encore en place).

    4. Commit RED : `test(12-02): add Wave 0 tests templates-catalog + page smoke`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- templates-catalog page.smoke 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/lib/__tests__/templates-catalog.test.ts` returns 0
    - `test -f apps/web/src/app/app/templates/__tests__/page.smoke.test.tsx` returns 0
    - `grep -c "TEMPLATES_CATALOG" apps/web/src/lib/__tests__/templates-catalog.test.ts` returns ≥3
    - Tests RED (au moins 6 failures attendues) avant Tasks 1+2
  </acceptance_criteria>
  <done>10 tests Wave 0 ajoutés (6 catalogue + 4 page). RED. Commit `test(12-02):`.</done>
</task>

<task type="auto">
  <name>Task 1: Créer le catalogue centralisé templates-catalog.ts (D-07 D-08 D-10)</name>
  <files>
    - apps/web/src/lib/templates-catalog.ts (NEW)
  </files>
  <read_first>
    - apps/web/src/lib/closure/qualiopi-prompts.ts (voir les noms des 5 prompts Qualiopi)
    - apps/web/src/lib/closure/attestation-template.ts (premières 50 LOC pour voir le pattern `interface Props` ou les `${var}` — extraire 3-5 vars principales)
    - apps/web/src/lib/closure/qcm-template.ts (premières 50 LOC)
    - apps/web/src/lib/agefice-template.ts (premières 50 LOC)
    - apps/web/src/lib/agefice-form-fill.ts (premières 30 LOC + nombre de champs — 92 selon CONTEXT.md)
    - apps/web/src/lib/preinscription-reminder-template.ts (premières 30 LOC)
    - apps/web/src/lib/mailer-templates/user-invitation.ts (premières 30 LOC — pattern déjà connu via grep ci-dessus)
    - apps/web/src/lib/mailer-templates/invoice-reminder.ts (premières 30 LOC)
    - apps/web/src/lib/mailer-templates/lead-assigned.ts (premières 30 LOC)
    - apps/web/src/lib/veille-audit-template.ts (Phase 13 — premières 30 LOC)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-07 D-08 D-10)
  </read_first>
  <action>
    Créer `apps/web/src/lib/templates-catalog.ts` avec exactement cette structure :

    ```ts
    /**
     * Catalogue centralisé des templates de documents générés par QualiOF.
     *
     * Phase 12 D-10 : 1 source de vérité pour le mapping (label, path, vars, category).
     * Phase 12 D-06 : pas d'éditeur, pas de versioning, pas de BDD — read-only V1.
     * Phase 12 D-11 : aperçu rendu non implémenté V1 (Gotenberg trop coûteux à brancher
     *   pour chaque template — décision planner). Si extension v2 : screenshots statiques
     *   dans apps/web/public/templates-previews/ ou réutiliser lib/pdf-render.ts.
     *
     * Consommé par :
     * - apps/web/src/app/app/templates/page.tsx (V1)
     * - Phase 10 Audit Qualiopi blanc (potentiel — export catalogue PDF auditeur)
     */

    export type TemplateCategory = 'qualiopi' | 'agefice' | 'email';

    export interface TemplateCatalogEntry {
      /** Slug stable kebab-case — clé d'identification, ne JAMAIS changer (D-10). */
      id: string;
      /** Nom user-friendly français — affiché dans la page /app/templates. */
      label: string;
      /** Catégorie : Qualiopi / AGEFICE / Email (D-07). */
      category: TemplateCategory;
      /** Chemin relatif au repo root (depuis Next.js root). */
      sourcePath: string;
      /** 1-2 phrases : à quoi sert ce template (affiché en tooltip ou subtitle). */
      description: string;
      /** Variables principales — 3-8 par template, pas exhaustif (D-08). */
      variables: string[];
    }

    export const TEMPLATES_CATALOG: ReadonlyArray<TemplateCatalogEntry> = [
      // ─── Catégorie Qualiopi (Pack fin de formation 1-clic — Palier 2.2 + Phases 7-11) ───
      {
        id: 'closure-attestation',
        label: 'Attestation de fin de formation',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/attestation-template.ts',
        description: 'Attestation Qualiopi délivrée à chaque stagiaire en fin de session.',
        variables: ['apprenantNomComplet', 'sessionTitle', 'sessionStartDate', 'sessionEndDate', 'durationHours', 'tenantName', 'tenantSiret'],
      },
      {
        id: 'closure-certificat',
        label: 'Certificat de réalisation',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/certificat-template.ts',
        description: 'Certificat individuel par stagiaire (équivalent attestation pour financeurs).',
        variables: ['apprenantNomComplet', 'sessionTitle', 'durationHours', 'tenantSignaturePedago', 'tenantSignatureDirigeant'],
      },
      {
        id: 'closure-analyse-besoin',
        label: 'Analyse de besoin individuelle',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/analyse-besoin-template.ts',
        description: 'Analyse pédagogique personnalisée par stagiaire (généré via Ollama mistral-small:24b).',
        variables: ['apprenantNomComplet', 'apprenantFonction', 'sessionTitle', 'analyseTexte'],
      },
      {
        id: 'closure-qcm',
        label: "QCM d'évaluation des acquis",
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/qcm-template.ts',
        description: 'QCM 10-13 questions partagé par session, scoring personnalisé par stagiaire.',
        variables: ['sessionTitle', 'questions', 'apprenantScore', 'apprenantReponses'],
      },
      {
        id: 'closure-deroule',
        label: 'Déroulé pédagogique',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/deroule-template.ts',
        description: 'Déroulé pédagogique de la session (1 par session, partagé tous stagiaires).',
        variables: ['sessionTitle', 'modules', 'durationHours', 'formateurNom'],
      },
      {
        id: 'closure-emargement',
        label: "Feuille d'émargement",
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/emargement-template.ts',
        description: 'Émargement par demi-journée (matin/après-midi), à signer par stagiaires + formateur.',
        variables: ['sessionTitle', 'sessionDays', 'apprenants', 'formateurNom', 'tenantLogo'],
      },
      {
        id: 'closure-positionnement',
        label: 'Positionnement initial',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/positionnement-template.ts',
        description: 'Positionnement compétences en début de formation (Qualiopi indicateur 12).',
        variables: ['apprenantNomComplet', 'sessionTitle', 'competencesEvaluation'],
      },
      {
        id: 'closure-satisfaction-chaud',
        label: 'Satisfaction à chaud',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/satisfaction-chaud-template.ts',
        description: "Questionnaire de satisfaction immédiate fin de session (J+0).",
        variables: ['apprenantNomComplet', 'sessionTitle', 'questionsSatisfaction'],
      },
      {
        id: 'closure-satisfaction-froid',
        label: 'Satisfaction à froid',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/satisfaction-froid-template.ts',
        description: 'Questionnaire de satisfaction à 3 mois (Qualiopi indicateur 31).',
        variables: ['apprenantNomComplet', 'sessionTitle', 'sessionEndDate'],
      },
      {
        id: 'closure-satisfaction-session',
        label: 'Satisfaction session (agrégée)',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/satisfaction-session-template.ts',
        description: 'Synthèse satisfaction de la session (1 par session, agrégée tous stagiaires).',
        variables: ['sessionTitle', 'satisfactionGlobale', 'verbatimsAnonymes'],
      },
      {
        id: 'closure-grille-obs-session',
        label: "Grille d'observation session",
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/grille-obs-session-template.ts',
        description: 'Grille observation pédagogique session (1 par session).',
        variables: ['sessionTitle', 'critèresObservation', 'formateurNom'],
      },
      {
        id: 'closure-grille-observation',
        label: "Grille d'observation individuelle",
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/grille-observation-template.ts',
        description: 'Observation pédagogique individuelle par stagiaire.',
        variables: ['apprenantNomComplet', 'sessionTitle', 'critèresIndividuels'],
      },
      {
        id: 'closure-checklist-formation',
        label: 'Checklist formation',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/closure/checklist-formation-template.ts',
        description: 'Checklist matériel/logistique cochée aléatoirement par stagiaire (BUG-15 fix).',
        variables: ['sessionTitle', 'pointsCheckList', 'apprenants'],
      },
      {
        id: 'convention-formation',
        label: 'Convention de formation',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/convention-template.ts',
        description: 'Convention OF / OPCO ou OF / Employeur (recopie programme — feedback Qualiopi).',
        variables: ['organisationNom', 'organisationSiret', 'sessionTitle', 'durationHours', 'priceHT', 'programmeMd'],
      },
      {
        id: 'programme-formation',
        label: 'Programme de formation',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/programme-template.ts',
        description: 'Programme Qualiopi (priceHT explicite + objectifs Bloom — feedback non-négociable).',
        variables: ['titre', 'objectifsBloom', 'modules', 'durationHours', 'priceHT', 'prerequis', 'publicCible'],
      },
      {
        id: 'convocation',
        label: 'Convocation',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/convocation-template.ts',
        description: 'Convocation stagiaire avec lieu/horaires/formateur (refait HTML+Gotenberg session 22/05).',
        variables: ['apprenantNomComplet', 'sessionTitle', 'sessionStartDate', 'sessionLocation', 'formateurNom'],
      },
      {
        id: 'legal-docs-cgv-ri',
        label: 'CGV / Règlement intérieur',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/legal-docs-template.ts',
        description: 'CGV + Règlement intérieur (éditables Paramètres — Phase 7 SET-02).',
        variables: ['tenantName', 'tenantAddress', 'cgvMarkdown', 'riMarkdown'],
      },
      {
        id: 'invoice-facture',
        label: 'Facture / Avoir',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/invoice-template.ts',
        description: 'Facture FAC-NNNNNN ou avoir AVO-NNNNNN (Phase 11 FACT-02).',
        variables: ['invoiceNumber', 'invoiceType', 'amountHt', 'amountTtc', 'clientName', 'clientSiret'],
      },
      {
        id: 'veille-audit',
        label: 'Rapport veille Qualiopi (audit)',
        category: 'qualiopi',
        sourcePath: 'apps/web/src/lib/veille-audit-template.ts',
        description: 'PDF audit veille critère 6 indicateurs 23/24/25/26 (Phase 13 VEILLE-03).',
        variables: ['theme', 'entries', 'tenantName', 'tenantSiret', 'tenantNda'],
      },

      // ─── Catégorie AGEFICE (Palier 4 + session 22/05) ───
      {
        id: 'agefice-fiche-html',
        label: 'Fiche AGEFICE (HTML)',
        category: 'agefice',
        sourcePath: 'apps/web/src/lib/agefice-template.ts',
        description: 'Fiche récapitulative AGEFICE HTML pour dépôt dossier.',
        variables: ['apprenantNomComplet', 'apprenantSiret', 'sessionTitle', 'priceHT', 'budgetRestant'],
      },
      {
        id: 'agefice-form-fill-pdf',
        label: 'Formulaire AGEFICE PDF (92 champs)',
        category: 'agefice',
        sourcePath: 'apps/web/src/lib/agefice-form-fill.ts',
        description: "Pré-remplissage PDF officiel AGEFICE via pdf-lib (92 champs mappés).",
        variables: ['apprenantNomComplet', 'apprenantSiret', 'apprenantAdresse', 'sessionTitle', 'organismeFormationSiret', 'priceHT', 'financingRequestDate'],
      },
      {
        id: 'agefice-attendance',
        label: "Attestation d'assiduité AGEFICE",
        category: 'agefice',
        sourcePath: 'apps/web/src/lib/closure/agefice-attendance-template.ts',
        description: "Attestation d'assiduité requise pour remboursement AGEFICE (fix session 22/05).",
        variables: ['apprenantNomComplet', 'sessionTitle', 'sessionDays', 'attendanceHours'],
      },

      // ─── Catégorie Email MJML (Palier 4 + Phases 8/9/11) ───
      {
        id: 'email-preinscription-reminder',
        label: 'Email — Relance pré-inscription',
        category: 'email',
        sourcePath: 'apps/web/src/lib/preinscription-reminder-template.ts',
        description: 'Relance email pour pré-inscription incomplète (formulaire public tokenisé).',
        variables: ['apprenantNomComplet', 'publicUrl', 'tenantName', 'tenantLogo'],
      },
      {
        id: 'email-user-invitation',
        label: 'Email — Invitation utilisateur',
        category: 'email',
        sourcePath: 'apps/web/src/lib/mailer-templates/user-invitation.ts',
        description: 'Invitation collaborateur (Phase 8 RBAC-02) — lien tokenisé /invitation/[token].',
        variables: ['userEmail', 'inviterName', 'invitationUrl', 'tenantName', 'tenantAddressFull'],
      },
      {
        id: 'email-user-password-reset',
        label: 'Email — Reset mot de passe',
        category: 'email',
        sourcePath: 'apps/web/src/lib/mailer-templates/user-password-reset.ts',
        description: 'Reset MDP utilisateur (Phase 8 RBAC-02).',
        variables: ['userEmail', 'resetUrl', 'tenantName'],
      },
      {
        id: 'email-invoice-reminder',
        label: 'Email — Relance facture',
        category: 'email',
        sourcePath: 'apps/web/src/lib/mailer-templates/invoice-reminder.ts',
        description: 'Relance impayé J+30 (amical) ou J+45 (ferme avec mention CGI art. L441-10) — Phase 11.',
        variables: ['invoiceNumber', 'amountTtc', 'daysOverdue', 'clientName', 'tenantName'],
      },
      {
        id: 'email-lead-assigned',
        label: 'Email — Notification lead assigné',
        category: 'email',
        sourcePath: 'apps/web/src/lib/mailer-templates/lead-assigned.ts',
        description: 'Notification commercial lors auto-assignation lead (Phase 9 LEAD-01).',
        variables: ['commercialName', 'leadName', 'leadSource', 'leadUrl'],
      },
    ];

    /**
     * Retourne tous les templates d'une catégorie donnée.
     * @example getTemplatesByCategory('email') // → 5 entrées
     */
    export function getTemplatesByCategory(category: TemplateCategory): TemplateCatalogEntry[] {
      return TEMPLATES_CATALOG.filter((t) => t.category === category);
    }

    /**
     * Retourne un template par son id stable, ou undefined si introuvable.
     */
    export function getTemplateById(id: string): TemplateCatalogEntry | undefined {
      return TEMPLATES_CATALOG.find((t) => t.id === id);
    }

    /**
     * Labels user-friendly pour chaque catégorie (UI affichage section).
     */
    export const CATEGORY_LABELS: Record<TemplateCategory, string> = {
      qualiopi: 'Documents Qualiopi',
      agefice: 'AGEFICE',
      email: 'Templates email',
    };

    /**
     * Compte total par catégorie (utile pour badges UI).
     */
    export function countByCategory(): Record<TemplateCategory, number> {
      return {
        qualiopi: getTemplatesByCategory('qualiopi').length,
        agefice: getTemplatesByCategory('agefice').length,
        email: getTemplatesByCategory('email').length,
      };
    }
    ```

    Lancer `pnpm --filter @qualiof/web test -- templates-catalog` — les 6 tests catalogue doivent maintenant être GREEN.

    Commit : `feat(12-02): add lib/templates-catalog.ts — 27 entries across 3 categories (D-07 D-10)`.

    **Note importante** : si la lecture rapide des templates révèle qu'une variable listée ci-dessus n'existe pas dans le code source (ex: `apprenantNomComplet` peut s'appeler `participantName` dans certains templates), garder le `id` et `sourcePath` exacts, ajuster les noms de variables à la réalité du code (les vars sont indicatives D-08, pas un contrat strict). Privilégier précision sur exhaustivité.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- templates-catalog 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/lib/templates-catalog.ts` returns 0
    - `grep -c "export const TEMPLATES_CATALOG" apps/web/src/lib/templates-catalog.ts` returns 1
    - `grep -c "id: 'closure-" apps/web/src/lib/templates-catalog.ts` returns ≥10 (≥10 Qualiopi closure templates)
    - `grep -c "category: 'qualiopi'" apps/web/src/lib/templates-catalog.ts` returns ≥10
    - `grep -c "category: 'agefice'" apps/web/src/lib/templates-catalog.ts` returns ≥2
    - `grep -c "category: 'email'" apps/web/src/lib/templates-catalog.ts` returns ≥4
    - `grep -c "export function getTemplatesByCategory" apps/web/src/lib/templates-catalog.ts` returns 1
    - `grep -c "export function getTemplateById" apps/web/src/lib/templates-catalog.ts` returns 1
    - Wave 0 tests catalogue (6) tous GREEN
  </acceptance_criteria>
  <done>Catalogue posé avec 25+ entrées réparties dans 3 catégories. Types TS exportés. Helpers `getTemplatesByCategory` + `getTemplateById` + `countByCategory`. 6 tests Wave 0 catalogue GREEN. Source unique D-10 établie. Décision V1 sans preview documentée en commentaire JSDoc (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: Remplacer le stub /app/templates par Server Component listing + RBAC sidebar (D-06 D-09)</name>
  <files>
    - apps/web/src/app/app/templates/page.tsx (OVERWRITE stub)
    - apps/web/src/components/layout/nav-config.ts (UPDATE allowedRoles)
  </files>
  <read_first>
    - apps/web/src/app/app/templates/page.tsx (stub 20 LOC à écraser)
    - apps/web/src/lib/rbac.ts (signature `requireRole`)
    - apps/web/src/app/app/parametres/historique/page.tsx (pattern Server Component RBAC à cloner Phase 8)
    - apps/web/src/components/layout/nav-config.ts (entrée 'Modèles de documents' ligne ~151 — actuellement sans `allowedRoles` → 6 rôles)
    - apps/web/src/lib/templates-catalog.ts (créé en Task 1)
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-06 D-09 D-11)
  </read_first>
  <action>
    1. **Écraser `apps/web/src/app/app/templates/page.tsx`** avec un Server Component listing (D-06 read-only, D-09 RBAC, D-11 pas de preview V1) :

       ```tsx
       /**
        * Page /app/templates — Catalogue read-only des templates QualiOF.
        *
        * Phase 12 D-06 : read-only (pas d'éditeur, pas de versioning, pas de BDD).
        * Phase 12 D-09 : RBAC ADMIN+MANAGER+LECTEUR uniquement.
        * Phase 12 D-10 : source unique = lib/templates-catalog.ts.
        * Phase 12 D-11 V1 : pas d'aperçu rendu (décision planner — voir
        *   commentaire JSDoc dans templates-catalog.ts).
        */

       import Link from 'next/link';
       import { FileText, Mail, FileCheck2 } from 'lucide-react';
       import { requireRole } from '@/lib/rbac';
       import {
         TEMPLATES_CATALOG,
         CATEGORY_LABELS,
         countByCategory,
         type TemplateCategory,
       } from '@/lib/templates-catalog';

       export const dynamic = 'force-dynamic';

       const CATEGORY_ICONS: Record<TemplateCategory, typeof FileText> = {
         qualiopi: FileCheck2,
         agefice: FileText,
         email: Mail,
       };

       const ORDERED_CATEGORIES: TemplateCategory[] = ['qualiopi', 'agefice', 'email'];

       export default async function TemplatesPage() {
         await requireRole(['ADMIN', 'MANAGER', 'LECTEUR']);

         const counts = countByCategory();
         const total = TEMPLATES_CATALOG.length;

         return (
           <div className="space-y-8">
             <header>
               <h1 className="text-2xl font-semibold tracking-tight">Modèles de documents</h1>
               <p className="text-sm text-muted-foreground mt-1">
                 Catalogue read-only des {total} templates utilisés pour générer les documents
                 Qualiopi, AGEFICE et les emails. Chaque template est défini dans le code source —
                 voir lien « Source » par entrée.
               </p>
             </header>

             {ORDERED_CATEGORIES.map((cat) => {
               const items = TEMPLATES_CATALOG.filter((t) => t.category === cat);
               if (items.length === 0) return null;
               const Icon = CATEGORY_ICONS[cat];
               return (
                 <section key={cat} className="space-y-3">
                   <div className="flex items-center gap-2">
                     <Icon className="h-5 w-5 text-primary-700" />
                     <h2 className="text-lg font-semibold">{CATEGORY_LABELS[cat]}</h2>
                     <span className="text-xs text-muted-foreground rounded-full bg-muted px-2 py-0.5">
                       {counts[cat]}
                     </span>
                   </div>
                   <div className="rounded-lg border bg-card overflow-hidden">
                     <table className="w-full text-sm">
                       <thead className="bg-muted/50 text-xs uppercase tracking-wider text-muted-foreground">
                         <tr>
                           <th className="px-4 py-2 text-left font-medium">Template</th>
                           <th className="px-4 py-2 text-left font-medium">Source</th>
                           <th className="px-4 py-2 text-left font-medium">Variables principales</th>
                         </tr>
                       </thead>
                       <tbody className="divide-y">
                         {items.map((t) => (
                           <tr key={t.id} className="hover:bg-muted/30">
                             <td className="px-4 py-3 align-top">
                               <div className="font-medium">{t.label}</div>
                               <div className="text-xs text-muted-foreground mt-0.5">{t.description}</div>
                             </td>
                             <td className="px-4 py-3 align-top">
                               <code className="text-xs text-muted-foreground break-all">
                                 {t.sourcePath}
                               </code>
                             </td>
                             <td className="px-4 py-3 align-top">
                               <div className="flex flex-wrap gap-1">
                                 {t.variables.slice(0, 6).map((v) => (
                                   <span
                                     key={v}
                                     className="inline-flex items-center rounded bg-primary-50 px-1.5 py-0.5 text-xs font-mono text-primary-800"
                                   >
                                     {v}
                                   </span>
                                 ))}
                                 {t.variables.length > 6 && (
                                   <span className="text-xs text-muted-foreground">
                                     +{t.variables.length - 6}
                                   </span>
                                 )}
                               </div>
                             </td>
                           </tr>
                         ))}
                       </tbody>
                     </table>
                   </div>
                 </section>
               );
             })}

             <aside className="rounded-lg border border-dashed bg-muted/30 p-4 text-xs text-muted-foreground">
               <strong className="text-foreground">Note V1 (Phase 12) :</strong> cette vue est en
               lecture seule. L'édition des templates passe encore par le code source. Si tu as besoin
               de modifier un template, ouvre le fichier listé dans la colonne « Source » et propose le
               changement via une PR (ou demande à Claude via /gsd:quick).
             </aside>
           </div>
         );
       }
       ```

    2. **Mettre à jour `apps/web/src/components/layout/nav-config.ts`** — entrée 'Modèles de documents' ligne ~151. Actuellement :
       ```ts
       { label: 'Produits de formation', href: '/app/produits', icon: BookOpen },
       { label: 'Modèles de documents', href: '/app/templates', icon: FileText },
       ```
       Remplacer la 2ème ligne par :
       ```ts
       { label: 'Modèles de documents', href: '/app/templates', icon: FileText,
         allowedRoles: ['ADMIN', 'MANAGER', 'LECTEUR'] },
       ```
       (D-09 : FORMATEUR/COMMERCIAL/COMPTABLE exclus côté sidebar visuel — la sécurité réelle est `requireRole` côté page.tsx.)

    3. Lancer `pnpm --filter @qualiof/web test -- templates page.smoke` — les 4 tests Wave 0 page doivent maintenant être GREEN.

    4. Commit : `feat(12-02): replace /app/templates stub with read-only catalog listing + RBAC (D-06 D-09 D-11)`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- templates page.smoke 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<Placeholder" apps/web/src/app/app/templates/page.tsx` returns 0 (stub disparu)
    - `grep -c "TEMPLATES_CATALOG" apps/web/src/app/app/templates/page.tsx` returns ≥1
    - `grep -c "requireRole" apps/web/src/app/app/templates/page.tsx` returns 1
    - `grep -c "'ADMIN', 'MANAGER', 'LECTEUR'" apps/web/src/app/app/templates/page.tsx` returns 1
    - `grep -B1 "/app/templates" apps/web/src/components/layout/nav-config.ts | grep -c "allowedRoles"` returns ≥1 (l'entrée a maintenant son RBAC)
    - `grep -c "label: 'Modèles de documents'" apps/web/src/components/layout/nav-config.ts` returns 1 (toujours 1 entrée, juste enrichie)
    - 4 tests Wave 0 page tous GREEN
  </acceptance_criteria>
  <done>Stub Placeholder remplacé par Server Component listing tableau par catégorie (Qualiopi/AGEFICE/Email). RBAC `requireRole(['ADMIN','MANAGER','LECTEUR'])` (D-09). Sidebar nav-config enrichi avec `allowedRoles` (D-09 visuel). Note V1 sans preview affichée dans la page (D-11 transparence utilisateur). 4 tests Wave 0 page GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke build + grep final + commit</name>
  <files>
    - (aucun nouveau fichier ; smoke + commit)
  </files>
  <read_first>
    - .planning/phases/12-modules-stub-inscriptions-et-modeles/12-CONTEXT.md (D-06..D-11 final review)
  </read_first>
  <action>
    1. Smoke build :
       ```bash
       pnpm --filter @qualiof/web build 2>&1 | tail -30
       ```
       Si erreur typedRoutes sur `/app/templates` → corriger.

    2. Vérification sidebar filtrée par rôle (defense-in-depth grep) :
       ```bash
       grep -A3 "label: 'Modèles de documents'" apps/web/src/components/layout/nav-config.ts
       ```
       Doit montrer `allowedRoles: ['ADMIN', 'MANAGER', 'LECTEUR']`.

    3. Vérification orphelin éventuel `<Placeholder>` component :
       ```bash
       grep -rn "from '@/components/ui/placeholder'\|from 'components/ui/placeholder'" apps/web/src/ 2>/dev/null
       ```
       Si 0 match → composant `Placeholder` est devenu orphelin (à signaler dans 12-02-SUMMARY pour décision Plan 03 : garder ou supprimer ; recommandation = garder car peut servir à de futurs stubs courts).

    4. Commit : `chore(12-02): smoke build OK + MOD-02 grep validation`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web build 2>&1 | grep -E "(Compiled successfully|error|Error|warn)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @qualiof/web build` exit code 0
    - `pnpm --filter @qualiof/web test -- templates-catalog page.smoke` exit code 0 (10/10 Wave 0 tests GREEN)
    - `grep -c "Placeholder" apps/web/src/app/app/templates/page.tsx` returns 0
    - `grep -c "allowedRoles.*LECTEUR" apps/web/src/components/layout/nav-config.ts` returns ≥2 (Veille Qualiopi Phase 13 déjà présente + nouvelle Modèles)
  </acceptance_criteria>
  <done>Build clean. 10/10 tests Wave 0 GREEN. MOD-02 résolu côté code. Décision V1 sans preview Gotenberg documentée en JSDoc + Note V1 affichée dans la page. Statut « composant Placeholder potentiellement orphelin » remonté à Plan 03 pour décision (suppression ou conservation).</done>
</task>

</tasks>

<verification>
**Phase requirement MOD-02 vérifiable :**
- Catalogue : `grep -c "export const TEMPLATES_CATALOG" apps/web/src/lib/templates-catalog.ts` → 1
- Source unique : `grep -c "TEMPLATES_CATALOG" apps/web/src/app/app/templates/page.tsx` → ≥1
- Plus de stub : `grep -c "Placeholder" apps/web/src/app/app/templates/page.tsx` → 0
- RBAC page : `grep -c "requireRole.*ADMIN.*MANAGER.*LECTEUR" apps/web/src/app/app/templates/page.tsx` → 1
- RBAC sidebar : `grep -A3 "label: 'Modèles de documents'" apps/web/src/components/layout/nav-config.ts | grep -c "allowedRoles"` → 1
- Wave 0 tests : 10/10 GREEN
- Build : `pnpm --filter @qualiof/web build` exit 0
</verification>

<success_criteria>
1. Page `/app/templates` affiche un listing tableau de 25+ templates répartis dans 3 sections (Qualiopi / AGEFICE / Email), chaque entrée montrant label + description + source path + variables principales
2. Aucun éditeur, aucune BDD, aucun versioning (D-06)
3. RBAC effectif : `requireRole(['ADMIN','MANAGER','LECTEUR'])` côté page + sidebar `allowedRoles: ['ADMIN','MANAGER','LECTEUR']` côté nav-config (D-09)
4. Catalogue centralisé `apps/web/src/lib/templates-catalog.ts` = source unique réutilisable (D-10)
5. V1 sans preview Gotenberg — décision documentée en JSDoc + Note V1 affichée à l'utilisateur (D-11 transparence)
6. Build Next.js clean, 10/10 Wave 0 tests GREEN
</success_criteria>

<output>
Après complétion, créer `.planning/phases/12-modules-stub-inscriptions-et-modeles/12-02-SUMMARY.md` documentant :
- Décisions D-06..D-11 appliquées (avec preuves grep)
- Inventory exact du catalogue : N entrées dans chaque catégorie (≥10 qualiopi, ≥2 agefice, ≥4 email)
- D-11 : décision V1 sans preview justifiée + plan v2 (screenshots statiques OU réutilisation lib/pdf-render.ts)
- Composant `apps/web/src/components/ui/placeholder.tsx` : flag « potentiellement orphelin » à confirmer/agir dans Plan 03 (grep usages restants après Phase 12)
- Pattern catalogue centralisé : réutilisable Phase 10 (Audit Qualiopi blanc — possible export PDF du catalogue à l'auditeur)
</output>
