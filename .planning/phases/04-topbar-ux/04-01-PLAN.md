---
phase: 04-topbar-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [UX-01]
user_setup: []

must_haves:
  truths:
    - "Le bouton cloche ouvre un panneau Radix DropdownMenu avec la liste des alertes (ou état vide)"
    - "Les items sont cliquables et naviguent vers la page concernée"
  artifacts:
    - "Capture browser du panel ouvert (ou note 'panel vide' si pas d'alerte active)"
  key_links:
    - "Audit 2026-05-12 disait 'pas de panneau' — vérification montre que `notifications-bell.tsx` est déjà implémenté complet"
---

<objective>
Vérifier en runtime que le panneau notifications fonctionne déjà (analyse code montre une implémentation complète Radix DropdownMenu dans `notifications-bell.tsx`). Cette plan = QA de confirmation, pas de modification de code.

Output: SUMMARY documentant le verdict (panel OK / petit polish nécessaire).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/components/layout/notifications-bell.tsx
@apps/web/src/server/actions/notifications.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: QA visuel du panel notifications</name>
  <files>(none — verification only)</files>
  <read_first>
    - apps/web/src/components/layout/notifications-bell.tsx (confirmer implémentation Radix DropdownMenu)
  </read_first>
  <action>
    1. Dev server doit tourner (`pnpm --filter @qualiof/web dev` sur :3000).
    2. Login dans l'app.
    3. Clic sur la cloche Bell dans TopBar :
       - Panel s'ouvre ✓
       - Liste items ou message vide "Tout est en règle..."
    4. Si items présents : clic un item → ferme panel + navigate
    5. Vérifier viewport mobile 390px : badge cloche reste visible, panel s'aligne correctement (Radix `align="end"`)
    6. Documenter dans SUMMARY :
       - Verdict : panel fonctionnel comme attendu / besoin de fix mineur
       - Capture browser (optionnelle)
       - Si panel KO en mobile : note pour patch ultérieur
  </action>
  <verify>
    <automated>grep -q "DropdownMenu.Content" apps/web/src/components/layout/notifications-bell.tsx && grep -q "items.length === 0" apps/web/src/components/layout/notifications-bell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Code review confirme `<DropdownMenu.Content>` + état vide géré
    - Test manuel desktop : panel ouvre/ferme correctement
    - Test manuel mobile : pas de débordement
    - Verdict documenté dans SUMMARY
  </acceptance_criteria>
  <done>UX-01 validé (rien à modifier)</done>
</task>

</tasks>

<output>
SUMMARY pour Plan 04-04 : annotation UX-01 "panel déjà en place, validé en runtime".
</output>
