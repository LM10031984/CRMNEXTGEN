---
phase: 04-topbar-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/layout/user-menu-button.tsx (NEW)
  - apps/web/src/components/layout/top-bar.tsx
autonomous: false
requirements: [UX-02]
user_setup: []

must_haves:
  truths:
    - "Le bloc avatar+nom+role dans TopBar est un trigger qui ouvre un Radix DropdownMenu"
    - "Le menu contient au moins : Paramètres (lien) + Déconnexion (item destructive)"
    - "Cliquer Déconnexion ouvre un AlertDialog (Radix Dialog) de confirmation"
    - "Le AlertDialog a Annuler + Se déconnecter, ce dernier submit le form server action logoutAction"
    - "Aucun bouton Déconnexion direct dans le JSX visible de TopBar"
  artifacts:
    - apps/web/src/components/layout/user-menu-button.tsx (NEW client component)
    - apps/web/src/components/layout/top-bar.tsx (refactor utilise <UserMenuButton />)
---

<objective>
Remplacer le bouton "Déconnexion" direct dans TopBar par un dropdown menu sur l'avatar, avec confirmation AlertDialog. Pattern client subcomponent (TopBar reste server).

Output: 1 nouveau composant + 1 modification TopBar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/components/layout/top-bar.tsx
@apps/web/src/app/login/actions.ts
@apps/web/src/components/sessions/delete-session-button.tsx
@apps/web/src/components/layout/mobile-menu-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Créer user-menu-button.tsx</name>
  <files>apps/web/src/components/layout/user-menu-button.tsx (NEW)</files>
  <read_first>
    - apps/web/src/components/layout/top-bar.tsx (current bloc avatar+nom+form logout à remplacer)
    - apps/web/src/components/sessions/delete-session-button.tsx (pattern AlertDialog Radix)
    - apps/web/src/components/layout/mobile-menu-button.tsx (pattern client subcomponent)
    - apps/web/src/app/login/actions.ts (server action logoutAction)
  </read_first>
  <action>
    Créer `apps/web/src/components/layout/user-menu-button.tsx` avec le contenu suivant :

    ```tsx
    'use client';
    
    import { useState } from 'react';
    import Link from 'next/link';
    import * as DropdownMenu from '@radix-ui/react-dropdown-menu';
    import * as Dialog from '@radix-ui/react-dialog';
    import { Settings, LogOut } from 'lucide-react';
    import type { User } from 'lucia';
    import { logoutAction } from '@/app/login/actions';
    
    /**
     * Avatar utilisateur en TopBar : trigger d'un DropdownMenu contenant
     * Paramètres + Déconnexion. La déconnexion est protégée par un
     * AlertDialog de confirmation (anti-clic accidentel — audit 2026-05-12 UX-02).
     */
    export function UserMenuButton({ user }: { user: User }) {
      const [confirmOpen, setConfirmOpen] = useState(false);
    
      return (
        <>
          <DropdownMenu.Root>
            <DropdownMenu.Trigger asChild>
              <button
                type="button"
                className="flex items-center gap-3 hover:bg-muted rounded-md px-2 py-1 -mr-2 transition-colors"
                aria-label="Menu utilisateur"
              >
                <div className="hidden sm:block text-right text-xs">
                  <div className="font-medium leading-tight">{user.firstName} {user.lastName}</div>
                  <div className="text-muted-foreground">{user.role}</div>
                </div>
                <div className="h-9 w-9 rounded-full bg-primary-100 text-primary-700 font-semibold inline-flex items-center justify-center text-sm">
                  {user.firstName.charAt(0)}{user.lastName.charAt(0)}
                </div>
              </button>
            </DropdownMenu.Trigger>
            <DropdownMenu.Portal>
              <DropdownMenu.Content
                align="end"
                sideOffset={6}
                className="z-50 min-w-[220px] rounded-lg border border-border bg-white shadow-xl p-1 animate-in fade-in zoom-in-95"
              >
                {/* En mobile, le nom+role est caché du trigger, on l'affiche en tête du menu */}
                <div className="px-3 py-2 border-b border-border sm:hidden">
                  <div className="font-medium text-sm">{user.firstName} {user.lastName}</div>
                  <div className="text-xs text-muted-foreground">{user.role}</div>
                </div>
    
                <DropdownMenu.Item asChild>
                  <Link
                    href="/app/parametres"
                    className="flex items-center gap-2.5 px-3 py-2 rounded-md cursor-pointer outline-none text-sm data-[highlighted]:bg-muted"
                  >
                    <Settings className="h-4 w-4 text-muted-foreground" />
                    Paramètres
                  </Link>
                </DropdownMenu.Item>
    
                <DropdownMenu.Separator className="my-1 h-px bg-border" />
    
                <DropdownMenu.Item
                  onSelect={(e) => {
                    e.preventDefault();
                    setConfirmOpen(true);
                  }}
                  className="flex items-center gap-2.5 px-3 py-2 rounded-md cursor-pointer outline-none text-sm text-red-700 data-[highlighted]:bg-red-50"
                >
                  <LogOut className="h-4 w-4" />
                  Déconnexion
                </DropdownMenu.Item>
              </DropdownMenu.Content>
            </DropdownMenu.Portal>
          </DropdownMenu.Root>
    
          <Dialog.Root open={confirmOpen} onOpenChange={setConfirmOpen}>
            <Dialog.Portal>
              <Dialog.Overlay className="fixed inset-0 bg-foreground/40 backdrop-blur-sm z-40 data-[state=open]:animate-in data-[state=open]:fade-in-0" />
              <Dialog.Content className="fixed left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 z-50 w-[420px] max-w-[90vw] rounded-lg border border-border bg-white p-6 shadow-xl data-[state=open]:animate-in data-[state=open]:zoom-in-95 data-[state=open]:fade-in-0">
                <Dialog.Title className="text-lg font-semibold">Confirmer la déconnexion</Dialog.Title>
                <Dialog.Description className="mt-2 text-sm text-muted-foreground">
                  Tu seras redirigé(e) vers la page de connexion. Toute saisie non sauvegardée sera perdue.
                </Dialog.Description>
                <form action={logoutAction} className="mt-5 flex justify-end gap-2">
                  <Dialog.Close asChild>
                    <button
                      type="button"
                      className="h-9 px-4 rounded-md border border-input bg-white text-sm font-medium hover:bg-muted transition-colors"
                    >
                      Annuler
                    </button>
                  </Dialog.Close>
                  <button
                    type="submit"
                    className="h-9 px-4 rounded-md bg-red-600 text-white text-sm font-medium hover:bg-red-700 transition-colors"
                  >
                    Se déconnecter
                  </button>
                </form>
              </Dialog.Content>
            </Dialog.Portal>
          </Dialog.Root>
        </>
      );
    }
    ```
  </action>
  <verify>
    <automated>test -f apps/web/src/components/layout/user-menu-button.tsx && grep -q "export function UserMenuButton" apps/web/src/components/layout/user-menu-button.tsx && grep -q "logoutAction" apps/web/src/components/layout/user-menu-button.tsx && grep -q "Confirmer la déconnexion" apps/web/src/components/layout/user-menu-button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Fichier créé
    - Export `UserMenuButton` avec prop `user: User`
    - Utilise Radix DropdownMenu pour le menu + Radix Dialog pour la confirmation
    - Form action={logoutAction} dans le Dialog
    - 2 items minimum (Paramètres + Déconnexion)
    - `e.preventDefault()` dans onSelect Déconnexion (sinon DropdownMenu ferme avant Dialog)
  </acceptance_criteria>
  <done>UserMenuButton prêt à être intégré</done>
</task>

<task type="auto">
  <name>Task 2.2: Intégrer UserMenuButton dans TopBar</name>
  <files>apps/web/src/components/layout/top-bar.tsx</files>
  <read_first>
    - apps/web/src/components/layout/top-bar.tsx (state actuel : NotificationsBell + bloc text + avatar + form logout)
  </read_first>
  <action>
    Réécrire `apps/web/src/components/layout/top-bar.tsx` pour utiliser le nouveau UserMenuButton.

    AVANT (état post-Phase 2) :
    ```tsx
    import type { User } from 'lucia';
    import { logoutAction } from '@/app/login/actions';
    import { CmdkTrigger } from './cmdk-trigger';
    import { NotificationsBell } from './notifications-bell';
    import { MobileMenuButton } from './mobile-menu-button';
    
    export function TopBar({ user }: { user: User }) {
      return (
        <header className="h-14 border-b border-border bg-white flex items-center px-4 md:px-8 sticky top-0 z-10 gap-3">
          <MobileMenuButton />
          <div className="flex-1 max-w-md">
            <CmdkTrigger />
          </div>
          <div className="flex items-center gap-3">
            <NotificationsBell />
            <div className="text-right text-xs">
              <div className="font-medium leading-tight">{user.firstName} {user.lastName}</div>
              <div className="text-muted-foreground">{user.role}</div>
            </div>
            <div className="h-9 w-9 rounded-full bg-primary-100 text-primary-700 font-semibold inline-flex items-center justify-center text-sm">
              {user.firstName.charAt(0)}{user.lastName.charAt(0)}
            </div>
            <form action={logoutAction}>
              <button type="submit" className="text-xs text-muted-foreground hover:text-foreground px-2 py-1 rounded transition-colors">
                Déconnexion
              </button>
            </form>
          </div>
        </header>
      );
    }
    ```

    APRÈS :
    ```tsx
    import type { User } from 'lucia';
    import { CmdkTrigger } from './cmdk-trigger';
    import { NotificationsBell } from './notifications-bell';
    import { MobileMenuButton } from './mobile-menu-button';
    import { UserMenuButton } from './user-menu-button';
    
    export function TopBar({ user }: { user: User }) {
      return (
        <header className="h-14 border-b border-border bg-white flex items-center px-4 md:px-8 sticky top-0 z-10 gap-3">
          <MobileMenuButton />
          <div className="flex-1 max-w-md">
            <CmdkTrigger />
          </div>
          <div className="flex items-center gap-3">
            <NotificationsBell />
            <UserMenuButton user={user} />
          </div>
        </header>
      );
    }
    ```

    Changements :
    - Supprime `import { logoutAction } from '@/app/login/actions';`
    - Supprime le bloc text (nom+role) → maintenant DANS UserMenuButton
    - Supprime le bloc avatar visuel → maintenant DANS UserMenuButton
    - Supprime le `<form action={logoutAction}>` direct
    - Ajoute `import { UserMenuButton } from './user-menu-button';`
    - Ajoute `<UserMenuButton user={user} />` après `<NotificationsBell />`
  </action>
  <verify>
    <automated>grep -q "UserMenuButton" apps/web/src/components/layout/top-bar.tsx && ! grep -q "form action={logoutAction}" apps/web/src/components/layout/top-bar.tsx && ! grep -q "logoutAction" apps/web/src/components/layout/top-bar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `top-bar.tsx` importe `UserMenuButton`
    - `top-bar.tsx` n'importe plus `logoutAction` (déplacé dans UserMenuButton)
    - `top-bar.tsx` ne contient plus `<form action={logoutAction}>` (déplacé dans Dialog de UserMenuButton)
    - Fichier passe de ~30 lignes à ~25 lignes
  </acceptance_criteria>
  <done>TopBar refactorée, déconnexion protégée</done>
</task>

</tasks>

<verification>
- ✅ Grep checks (UserMenuButton créé + intégré, form logout retiré)
- ✅ Build Next.js complet OK
- ⏳ Manual Laurent : clic avatar → menu, clic Déconnexion → AlertDialog, Annuler ferme, Se déconnecter → /login
</verification>

<success_criteria>
- UserMenuButton est l'unique source de déconnexion
- Pas de bouton "Déconnexion" texte direct visible
- Confirmation systématique (anti-clic accidentel)
- Pas de régression Phase 1+2+3
</success_criteria>

<output>
SUMMARY pour Plan 04-04 : composant créé + intégré.
</output>
