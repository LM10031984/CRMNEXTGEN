---
phase: 04-topbar-ux
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UX-01, UX-02]
user_setup: []

must_haves:
  truths:
    - "UX-01 et UX-02 cochés [x] dans REQUIREMENTS.md"
    - "Phase 4 marquée Complete dans ROADMAP.md"
---

<objective>
Bookkeeping wave 2 après plans 04-01 + 04-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Update REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Remplacer :
    AVANT :
    ```
    - [ ] **UX-01** : Panneau aperçu Notifications derrière la cloche "53" — Radix Popover avec les 10 dernières notifications (récupérées via `/api/notifications` existant) + lien "Tout voir" si > 10 + bouton "Marquer tout comme lu".
    - [ ] **UX-02** : Déconnexion dans Radix DropdownMenu sur avatar avec confirmation — `components/layout/top-bar.tsx`.
    ```

    APRÈS :
    ```
    - [x] **UX-01** : Panneau Notifications. **VALIDATED 2026-05-13** — panel Radix DropdownMenu déjà implémenté dans `notifications-bell.tsx` (audit 2026-05-12 reportait "pas de panneau" mais ce code existait probablement à l'époque ou a été livré avant). Panel affiche items avec icônes + sévérité + drill-down. "Tout voir" et "Marquer comme lu" sont N/A : les notifications QualiOF sont des alertes DÉRIVÉES (préinscriptions, sessions à clôturer, etc.) qui disparaissent quand leur condition se résout. Pas d'entrée DB à marquer.
    - [x] **UX-02** : Déconnexion protégée. **DONE 2026-05-13** — nouveau composant `apps/web/src/components/layout/user-menu-button.tsx` : avatar trigger Radix DropdownMenu avec Paramètres + Déconnexion. Déconnexion ouvre Radix Dialog de confirmation (Annuler / Se déconnecter rouge destructive) qui submit le `<form action={logoutAction}>`. `top-bar.tsx` refactorée : -1 import (logoutAction), -1 bloc form logout, +1 UserMenuButton.
    ```
  </action>
  <verify>
    <automated>test "$(grep -cE '^\s*-\s*\[x\]\s*\*\*UX-0[12]\*\*' .planning/REQUIREMENTS.md)" = "2"</automated>
  </verify>
  <acceptance_criteria>
    - UX-01, UX-02 cochés
  </acceptance_criteria>
  <done>2 requirements fermés</done>
</task>

<task type="auto">
  <name>Task 3.2: ROADMAP.md Phase 4 Complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    1. Checklist : `- [ ] **Phase 4:` → `- [x] **Phase 4:`
    2. Progress : `| 4. TopBar UX | 0/TBD | Not started | - |` → `| 4. TopBar UX | 3/3 | Complete | 2026-05-13 |`
  </action>
  <verify>
    <automated>grep -qE '^\s*-\s*\[x\]\s*\*\*Phase\s+4:' .planning/ROADMAP.md && grep -q "Phase 4.*3/3.*Complete.*2026-05-13" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 4 close</done>
</task>

</tasks>

<output>
SUMMARY Plan 04-03 final.
</output>
