---
phase: 01-smoke-verification-bugs-critiques
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts
  - apps/web/vitest.config.ts
autonomous: false
requirements: [BUG-01]
user_setup: []

must_haves:
  truths:
    - "Page /app/sessions/[id] boote en runtime sans 'FileText is not defined' (audit était cache stale)"
    - "Un test Vitest smoke passe et empêche la régression future"
  artifacts:
    - apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts
  key_links:
    - "Test relie BUG-01 (audit) → vérification runtime systématique"
---

<objective>
Re-vérifier en runtime que la page `/app/sessions/[id]` n'a plus l'erreur "FileText is not defined" rapportée par l'audit 2026-05-12, et ancrer un test smoke Vitest qui empêche toute régression future de symboles lucide-react manquants.

Purpose: L'analyse statique du code (CONCERNS.md) montre que `FileText` est déjà importé ligne 4 et utilisé ligne 656. L'audit reportait ligne 574, ce qui suggère un cache Next.js stale. Cette phase valide l'hypothèse.
Output: Un test smoke + (optionnel) vitest.config.ts si nécessaire + capture browser preuve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-CONTEXT.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@apps/web/src/app/app/sessions/[id]/page.tsx
@apps/web/package.json
@packages/shared/src/helpers/__tests__/siret.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Clean build + manual runtime check</name>
  <files>apps/web/src/app/app/sessions/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/sessions/[id]/page.tsx (verify FileText import line 4 + usage line ~656)
    - apps/web/package.json (confirm dev script)
  </read_first>
  <action>
    1. Depuis la racine du repo : `rm -rf apps/web/.next` (purge cache Next).
    2. Lancer dev server : `pnpm --filter @qualiof/web dev` en background.
    3. Browser : ouvrir http://localhost:3000/login, se connecter (compte seed).
    4. Naviguer sur `/app/sessions/` puis cliquer une session pour atterrir sur `/app/sessions/[id]`.
    5. Ouvrir DevTools Console : confirmer absence de toute erreur `FileText is not defined` ou autre `is not defined`.
    6. Prendre une capture (ou copier le HTML de la zone "Documents partagés") comme preuve attachée au commit.
  </action>
  <verify>
    <automated>MISSING — manual runtime check (no E2E framework configured)</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/.next/` directory has been deleted before the dev run (timestamp check OK)
    - dev server boots sans erreur fatale (process reste vivant > 30s)
    - Page `/app/sessions/[id]` rendue : section "Documents partagés" visible (texte présent)
    - Console DevTools : 0 occurrence de `is not defined`
    - Capture browser ou texte de la console attachée au commit message
  </acceptance_criteria>
  <done>BUG-01 confirmé comme faux positif (cache stale) OU bug réel identifié pour Task 1.3</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Test smoke Vitest pour /app/sessions/[id]</name>
  <files>
    apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts,
    apps/web/vitest.config.ts (créer si manquant)
  </files>
  <read_first>
    - packages/shared/src/helpers/__tests__/siret.test.ts (modèle Vitest existant)
    - apps/web/package.json (vérifier "test" script et présence de vitest)
    - apps/web/tsconfig.json (path alias @/ → src/)
    - apps/web/src/app/app/sessions/[id]/page.tsx (source à valider)
  </read_first>
  <action>
    1. Vérifier si `apps/web/vitest.config.ts` existe. Si NON, créer avec contenu MINIMAL :
       ```ts
       import { defineConfig } from 'vitest/config';
       import path from 'node:path';
       
       export default defineConfig({
         test: {
           environment: 'node',
           include: ['src/**/*.{test,spec}.{ts,tsx}'],
         },
         resolve: {
           alias: { '@': path.resolve(__dirname, './src') },
         },
       });
       ```
       (environnement `node` car on fait import-only, pas de rendu DOM ici)
    2. Vérifier dans `apps/web/package.json` que le script `test` est `"test": "vitest run"` (ou ajouter si manquant).
    3. Créer le dossier `apps/web/src/app/app/sessions/[id]/__tests__/` si absent.
    4. Créer `apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts` avec contenu EXACT :
       ```ts
       import { describe, it, expect } from 'vitest';
       import { readFileSync } from 'node:fs';
       import path from 'node:path';
       
       const pageSrc = readFileSync(
         path.join(__dirname, '..', 'page.tsx'),
         'utf8',
       );
       
       describe('sessions/[id] page — smoke', () => {
         it('imports FileText from lucide-react', () => {
           const importMatch = pageSrc.match(
             /import\s*\{([^}]+)\}\s*from\s*['"]lucide-react['"]/,
           );
           expect(importMatch, 'page.tsx must import from lucide-react').toBeTruthy();
           const importedNames = new Set(
             importMatch![1].split(',').map((s) => s.trim()),
           );
           expect(importedNames.has('FileText')).toBe(true);
         });
       
         it('uses no lucide-react symbol that is not imported', () => {
           const importMatch = pageSrc.match(
             /import\s*\{([^}]+)\}\s*from\s*['"]lucide-react['"]/,
           );
           const imported = new Set(
             (importMatch?.[1] ?? '').split(',').map((s) => s.trim()),
           );
           // Symboles JSX utilisés dans page.tsx — liste figée pour ne capturer que les noms en PascalCase
           const jsxOpeningTags = [...pageSrc.matchAll(/<([A-Z][A-Za-z0-9_]*)\b/g)]
             .map((m) => m[1]);
           // Filtre les symboles lucide-react courants présents dans cette page (voir page.tsx imports)
           const lucideCandidates = [
             'ArrowLeft', 'Calendar', 'Clock', 'Euro', 'Users', 'Briefcase',
             'ClipboardCheck', 'Check', 'Minus', 'Package', 'ChevronRight', 'FileText',
           ];
           const usedFromLucide = jsxOpeningTags.filter((t) =>
             lucideCandidates.includes(t),
           );
           const missing = usedFromLucide.filter((t) => !imported.has(t));
           expect(missing, `lucide-react symbols used but not imported: ${missing.join(', ')}`).toEqual([]);
         });
       });
       ```
    5. Run : `pnpm --filter @qualiof/web exec vitest run src/app/app/sessions/\[id\]/__tests__/page.smoke.test.ts`
    6. Si échec : diagnostiquer cause (cache stale ? import manquant ?) et appliquer fix (cf. Task 1.3 si bug runtime réel).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run src/app/app/sessions/\[id\]/__tests__/page.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts` exists
    - Test fichier contient `describe('sessions/[id] page — smoke'` (vérifiable via grep)
    - Test fichier contient assertion `expect(importedNames.has('FileText')).toBe(true)`
    - `pnpm --filter @qualiof/web exec vitest run …page.smoke.test.ts` exit code 0
    - Test count : 2 tests pass, 0 fail, 0 skip
  </acceptance_criteria>
  <done>Test smoke en place qui catche toute régression future de symboles lucide-react manquants dans cette page</done>
</task>

<task type="auto">
  <name>Task 1.3: Si bug runtime réel — Fix imports lucide-react + retest</name>
  <files>apps/web/src/app/app/sessions/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/sessions/[id]/page.tsx (full)
    - Output de Task 1.1 et 1.2 (savoir si bug réel ou faux positif)
  </read_first>
  <action>
    **CONDITIONNEL** — exécuter uniquement si Task 1.1 OU Task 1.2 a révélé un import manquant réel.

    Cas A (Task 1.1 a vu le bug runtime) :
    1. Identifier le symbole manquant dans le browser error stack.
    2. L'ajouter à la liste d'import `lucide-react` ligne 4 de `apps/web/src/app/app/sessions/[id]/page.tsx`.
    3. Re-purger cache : `rm -rf apps/web/.next`, redémarrer dev, retest browser.
    4. Re-run test smoke (Task 1.2) — doit passer.

    Cas B (Task 1.2 a échoué mais runtime ok) :
    1. Comparer `importedNames` vs `usedFromLucide` dans le test pour identifier manquants.
    2. Ajouter à `lucide-react` import.
    3. Re-run test smoke — doit passer.

    Si aucun bug réel détecté (cas attendu) : marquer cette task comme "skipped — no real bug" dans le commit / SUMMARY et passer à la mise à jour de REQUIREMENTS.md.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run src/app/app/sessions/\[id\]/__tests__/page.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Si exécuté : `git diff apps/web/src/app/app/sessions/[id]/page.tsx` montre exclusivement des additions à la ligne d'import lucide-react
    - Si exécuté : test smoke passe après fix
    - Si skipped : SUMMARY.md mentionne "BUG-01 = faux positif confirmé (cache stale), test smoke ancré"
  </acceptance_criteria>
  <done>BUG-01 résolu : runtime OK + test smoke vert</done>
</task>

<!-- NOTE : la mise à jour REQUIREMENTS.md BUG-01 est extraite dans Plan 01-04 (wave 2)
     pour sérialiser les writes concurrents avec Plans 02/03. Ce plan produit le verdict
     (faux positif vs bug réel + commande de retest) qui sera consommé par 01-04. -->

</tasks>

<verification>
- ✅ `apps/web/.next` purgé puis dev relancé : page sessions/[id] boote sans erreur runtime
- ✅ Vitest smoke test passe (2 tests vert)
- ✅ Capture browser ou log console attachée au commit (preuve runtime)
- 🔗 La mise à jour REQUIREMENTS.md (BUG-01 → [x]) est gérée par Plan 01-04 (wave 2)
</verification>

<success_criteria>
- BUG-01 résolu ou confirmé faux positif avec test smoke ancré
- Test smoke runnable avec une seule commande Vitest, exit 0
- Zero modification autre que test + (si nécessaire) un import lucide-react manquant
- Pas de Playwright ajouté, pas de mock Lucia/Prisma introduit (anti-scope-creep)
</success_criteria>

<output>
After completion, create `.planning/phases/01-smoke-verification-bugs-critiques/01-01-SUMMARY.md` with :
- Verdict BUG-01 (faux positif vs bug réel) — sera consommé par Plan 01-04 pour update REQUIREMENTS.md
- Test smoke commande à relancer pour vérifier
- Captures attachées (ou refs vers commit)
- Justification du choix `environment: 'node'` (lecture fichier source, pas de DOM nécessaire) — alternative explicitement permise par CONTEXT.md
- Note : test smoke utilise une liste `lucideCandidates` figée — moins défensif qu'une heuristique full-auto, compromis accepté pour ce milestone (revisité v2 si régression)
</output>
