---
phase: 01-smoke-verification-bugs-critiques
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/layout/main-content.tsx
autonomous: false
requirements: [BUG-02]
user_setup: []

must_haves:
  truths:
    - "Le TopBar reste visuellement en haut de l'écran pendant le scroll sur dashboard, sessions list, fiche apprenant"
    - "getComputedStyle(header).position === 'sticky' pendant le scroll (pas 'static' ni 'relative')"
  artifacts:
    - "3 captures before/after attached to commit (dashboard, sessions list, apprenant détail)"
  key_links:
    - "Root cause identifiée : min-h-screen sur MainContent cassait le contexte sticky"
---

<objective>
Corriger le bug du header TopBar qui se décolle au scroll (rapporté par audit 2026-05-12, BUG-02). L'hypothèse principale identifiée par la recherche : `min-h-screen` sur `MainContent` (`apps/web/src/components/layout/main-content.tsx`) interfère avec le contexte de positionnement sticky du `<header>`.

Purpose: Sans header sticky correct, l'utilisateur perd la barre de recherche Cmd+K + notifications + profil pendant le scroll d'une longue liste (sessions, apprenants), ce qui détériore le confort opérationnel.
Output: Fix CSS minimal + 3 captures avant/après comme preuve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-CONTEXT.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@apps/web/src/components/layout/main-content.tsx
@apps/web/src/components/layout/top-bar.tsx
@apps/web/src/app/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Capture state actuel (avant fix) sur 3 pages</name>
  <files>(none — checkpoint visuel)</files>
  <read_first>
    - apps/web/src/components/layout/main-content.tsx
    - apps/web/src/components/layout/top-bar.tsx
    - apps/web/src/app/app/layout.tsx
  </read_first>
  <action>
    1. Lancer dev server si pas déjà : `pnpm --filter @qualiof/web dev`.
    2. Pour chacune des 3 pages : `/app`, `/app/sessions`, `/app/apprenants/[un id existant]` :
       a. Naviguer dessus, attendre rendu complet.
       b. Capture #1 : top of page (scrollY = 0).
       c. Scroller manuellement de ~500px (`window.scrollTo(0, 500)`).
       d. Capture #2 : après scroll. Le header DOIT être visible en haut (preuve du bug : il ne l'est probablement pas avant fix).
       e. Dans Console : `getComputedStyle(document.querySelector('header')).position` — noter la valeur (probablement `static` ou `relative` au lieu de `sticky`).
    3. Sauvegarder les 6 captures dans `.planning/phases/01-smoke-verification-bugs-critiques/before/` (ou comme attachements PR si tu commit).
    4. Si AUCUN bug observé sur les 3 pages (header reste bien sticky) : le bug est peut-être déjà fixé ou un faux positif. Marquer Task 3.2/3.3 comme conditionnel "skip if no bug observed".
  </action>
  <verify>
    <automated>MISSING — manual visual capture</automated>
  </verify>
  <acceptance_criteria>
    - 6 captures (3 pages × 2 états scroll) prises ou Path documenté
    - Pour chaque page, valeur de `getComputedStyle(header).position` notée
    - Verdict : bug reproduit (X/3 pages) OU bug non reproduit (header sticky OK)
  </acceptance_criteria>
  <done>État avant-fix documenté avec preuve visuelle</done>
</task>

<task type="auto">
  <name>Task 3.2: Appliquer Fix #1 — retirer min-h-screen de MainContent</name>
  <files>apps/web/src/components/layout/main-content.tsx</files>
  <read_first>
    - apps/web/src/components/layout/main-content.tsx (ligne avec className flex flex-col min-h-screen)
    - apps/web/src/app/app/layout.tsx (confirmer que le wrapper outer a déjà min-h-screen)
  </read_first>
  <action>
    **CONDITIONNEL** — exécuter seulement si Task 3.1 a confirmé le bug (header pas sticky en runtime).

    Dans `apps/web/src/components/layout/main-content.tsx`, dans le `<div className={cn(...)}>` qui wrap les children, remplacer EXACTEMENT :

    AVANT :
    ```tsx
    className={cn(
      'flex flex-col min-h-screen transition-[margin-left] duration-200',
      collapsed ? 'ml-[64px]' : 'ml-64',
    )}
    ```

    APRÈS :
    ```tsx
    className={cn(
      'flex flex-col transition-[margin-left] duration-200',
      collapsed ? 'ml-[64px]' : 'ml-64',
    )}
    ```

    Justification : `apps/web/src/app/app/layout.tsx` enveloppe déjà tout dans `<div className="min-h-screen bg-background">`, donc le min-h-screen est redondant ici. Le retirer libère le contexte de positionnement sticky du `<header>` enfant.
  </action>
  <verify>
    <automated>! grep -F "flex flex-col min-h-screen" apps/web/src/components/layout/main-content.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/layout/main-content.tsx` ne contient PLUS la chaîne `'flex flex-col min-h-screen'` (vérifié par grep négatif)
    - `apps/web/src/components/layout/main-content.tsx` contient `'flex flex-col transition-[margin-left]'` (vérifié par grep positif)
    - Aucune autre modification dans le fichier (vérifié par `git diff`)
    - `apps/web/src/app/app/layout.tsx` contient toujours `min-h-screen` sur le wrapper outer (intact)
  </acceptance_criteria>
  <done>Fix CSS appliqué. Validation runtime à Task 3.3.</done>
</task>

<task type="auto">
  <name>Task 3.3: Re-tester sticky après fix + captures after</name>
  <files>(none — checkpoint visuel)</files>
  <read_first>
    - apps/web/src/components/layout/main-content.tsx (post-Task 3.2)
  </read_first>
  <action>
    1. Si dev server tourne en HMR : attendre la recompilation auto, sinon `Cmd+Shift+R` browser pour purger.
    2. Pour chacune des 3 pages (`/app`, `/app/sessions`, `/app/apprenants/[id]`) :
       a. Naviguer + scroll 500px (comme Task 3.1).
       b. Capture after.
       c. Vérifier en Console : `getComputedStyle(document.querySelector('header')).position === 'sticky'` doit être `true`.
       d. Vérifier visuellement : header reste collé en haut pendant le scroll.
    3. Si les 3 pages OK : commit le fix avec les 6 captures (avant/après × 3 pages).
    4. Si une page échoue (sticky toujours cassé) : fallback Task 3.4.
  </action>
  <verify>
    <automated>MISSING — manual runtime check</automated>
  </verify>
  <acceptance_criteria>
    - 3 captures after prises (post-scroll, header visible en haut)
    - 3 console outputs confirmant `position === 'sticky'`
    - Verdict : 3/3 pages OK OR < 3/3 → déclencher Task 3.4
  </acceptance_criteria>
  <done>Si 3/3 : BUG-02 résolu. Sinon : fallback.</done>
</task>

<task type="auto">
  <name>Task 3.4 (FALLBACK): Si Fix #1 insuffisant, basculer TopBar en fixed</name>
  <files>
    apps/web/src/components/layout/top-bar.tsx,
    apps/web/src/app/app/layout.tsx
  </files>
  <read_first>
    - apps/web/src/components/layout/top-bar.tsx
    - apps/web/src/app/app/layout.tsx
    - apps/web/src/components/layout/main-content.tsx (état post-Task 3.2)
  </read_first>
  <action>
    **CONDITIONNEL** — exécuter seulement si Task 3.3 montre que sticky ne marche pas après le fix #1.

    1. Dans `apps/web/src/components/layout/top-bar.tsx`, remplacer `sticky top-0 z-10` par :
       `fixed top-0 left-64 right-0 z-30`
       (le `z-30` est plus haut pour passer au-dessus du contenu ; `left-64` aligne avec la sidebar 256px).

       **Note responsive Phase 2** : ce `left-64` sera adapté en `left-0 md:left-64` lors de RESP-02. À documenter en commentaire :
       ```tsx
       // TODO (Phase 2 RESP-02): switch to "left-0 md:left-64" once md: breakpoint is restored
       <header className="fixed top-0 left-64 right-0 ...">
       ```

    2. Dans `apps/web/src/app/app/layout.tsx`, ajouter `pt-14` sur le `<main>` pour compenser la TopBar fixed (height 14 = 56px).

       AVANT :
       ```tsx
       <main className="flex-1 p-8 max-w-screen-2xl w-full mx-auto">
       ```

       APRÈS :
       ```tsx
       <main className="flex-1 pt-14 p-8 max-w-screen-2xl w-full mx-auto">
       ```
       (ou `pt-20` si visuellement trop serré)

    3. Re-tester sticky/visibility sur les 3 pages comme Task 3.3.
  </action>
  <verify>
    <automated>grep -q "fixed top-0" apps/web/src/components/layout/top-bar.tsx && grep -q "pt-14" apps/web/src/app/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `top-bar.tsx` contient `fixed top-0 left-64 right-0`
    - `layout.tsx` <main> contient `pt-14`
    - TODO Phase 2 RESP-02 ajouté en commentaire
    - 3 captures after — header reste visible pendant scroll
    - Z-index regression check : ouvrir Cmd+K (command palette Radix Dialog) sur dashboard → overlay s'affiche AU-DESSUS de la TopBar fixed (z-50 Radix > z-30 TopBar). Vérification manuelle 30s.
  </acceptance_criteria>
  <done>BUG-02 résolu avec fallback fixed</done>
</task>

<!-- NOTE : la mise à jour REQUIREMENTS.md BUG-02 est extraite dans Plan 01-04 (wave 2)
     pour sérialiser les writes concurrents avec Plans 01/02. -->

</tasks>

<verification>
- ✅ Captures before sur 3 pages (Task 3.1)
- ✅ Fix CSS appliqué (Task 3.2 ou 3.4)
- ✅ Captures after sur 3 pages, header visible (Task 3.3)
- ✅ `getComputedStyle(header).position === 'sticky'` sur 3 pages
- ✅ REQUIREMENTS.md BUG-02 cochée
</verification>

<success_criteria>
- Aucune autre modification CSS/layout en dehors de min-h-screen sur MainContent (Fix #1) ou position du TopBar (Fix #2)
- TopBar reste visible pendant scroll sur les 3 pages clés (dashboard, sessions list, fiche apprenant)
- Captures avant/après documentent le changement
- Pas de touche au tailwind.config.ts (réservé Phase 2 RESP-01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-smoke-verification-bugs-critiques/01-03-SUMMARY.md` with :
- Fix appliqué (#1 ou #2) avec raisons
- Liens vers les 6 captures avant/après
- Note pour Phase 2 RESP-02 si Fix #2 (à rendre responsive)
</output>
