---
phase: 01-smoke-verification-bugs-critiques
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md a les 3 lignes BUG-01, BUG-02, BUG-03 cochées [x] avec annotation cohérente avec le résultat des Plans 01/02/03"
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "Consomme les SUMMARY.md des 3 plans wave 1 pour déterminer le wording exact (faux positif vs fix réel, Fix #1 vs Fix #2)"
---

<objective>
Sérialiser la mise à jour de `.planning/REQUIREMENTS.md` pour les 3 bugs critiques. Évite le conflit potentiel de writes concurrents si Plans 01/02/03 (wave 1) tournent en parallèle.

Purpose: Plan-checker (2026-05-12) a identifié comme MAJOR le risque que 3 plans wave 1 modifient en parallèle le même fichier. Cette plan en wave 2 garantit que les writes sont sérialisés et que le wording final reflète le verdict réel de chaque plan.
Output: 3 lignes REQUIREMENTS.md cochées avec annotation date + résultat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-01-SUMMARY.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-02-SUMMARY.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Update REQUIREMENTS.md — coche BUG-01, BUG-02, BUG-03 en une seule passe</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/01-smoke-verification-bugs-critiques/01-01-SUMMARY.md (verdict BUG-01)
    - .planning/phases/01-smoke-verification-bugs-critiques/01-02-SUMMARY.md (preuve curl BUG-03)
    - .planning/phases/01-smoke-verification-bugs-critiques/01-03-SUMMARY.md (fix appliqué BUG-02 #1 ou #2)
    - .planning/REQUIREMENTS.md (lignes BUG-01, BUG-02, BUG-03)
  </read_first>
  <action>
    1. Lire les 3 SUMMARY.md pour extraire :
       - BUG-01 verdict : "faux positif cache stale" OU "bug réel — import {symbol} ajouté"
       - BUG-02 verdict : "Fix #1 (retrait min-h-screen)" OU "Fix #2 (TopBar fixed)"
       - BUG-03 verdict : confirmé via curl, redirects 308 actifs

    2. Éditer `.planning/REQUIREMENTS.md` en remplaçant les 3 lignes :

       Ligne BUG-01 — AVANT :
       ```
       - [ ] **BUG-01** : Re-vérifier en runtime le "FileText is not defined" sur `/app/sessions/[id]` (audit code montre import présent ligne 4 — suspect cache `.next` stale). Si confirmé : produire un test de smoke qui boote la page et valide le rendu sans erreur.
       ```
       APRÈS (cas faux positif — attendu) :
       ```
       - [x] **BUG-01** : Re-vérifier en runtime le "FileText is not defined" sur `/app/sessions/[id]`. **RESOLVED 2026-05-12** — faux positif cache stale confirmé via `rm -rf .next` + dev rerun. Test smoke Vitest ancré (`apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts`) pour empêcher régression future.
       ```
       (OU adapter le wording si Plan 01-01 a trouvé un bug réel)

       Ligne BUG-02 — AVANT :
       ```
       - [ ] **BUG-02** : Investiguer + corriger le header sticky qui se décolle au scroll dashboard (suspect `min-h-screen` sur parent) — `components/layout/main-content.tsx`
       ```
       APRÈS (cas Fix #1 — attendu) :
       ```
       - [x] **BUG-02** : Investiguer + corriger le header sticky. **FIXED 2026-05-12** — Fix #1 appliqué : retrait `min-h-screen` de `MainContent` (redondant avec wrapper outer `<div min-h-screen>`). Sticky `position: 'sticky'` confirmé en runtime sur dashboard, sessions list, fiche apprenant. 6 captures avant/après attachées.
       ```
       (OU annoter Fix #2 si fallback appliqué)

       Ligne BUG-03 — AVANT :
       ```
       - [ ] **BUG-03** : Ajouter redirects `/app/pre-inscriptions` → `/app/preinscriptions` et `/app/modeles` → `/app/templates` dans `next.config.mjs` (ou renommer)
       ```
       APRÈS :
       ```
       - [x] **BUG-03** : Redirects 308 ajoutés dans `apps/web/next.config.mjs` pour `/app/pre-inscriptions` → `/app/preinscriptions` et `/app/modeles` → `/app/templates` (incl. variantes `:path*`). **DONE 2026-05-12** — preuve curl 4/4 OK. Convention naming routes documentée dans `CLAUDE.md`.
       ```

    3. Vérifier en grep :
       ```
       grep -E '^\s*-\s*\[x\]\s*\*\*BUG-0[123]\*\*' .planning/REQUIREMENTS.md
       ```
       Doit retourner 3 lignes.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\s*-\s*\[x\]\s*\*\*BUG-0[123]\*\*' .planning/REQUIREMENTS.md)" = "3"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contient `- [x] **BUG-01**` (vérifiable via grep)
    - REQUIREMENTS.md contient `- [x] **BUG-02**` (vérifiable via grep)
    - REQUIREMENTS.md contient `- [x] **BUG-03**` (vérifiable via grep)
    - Chacune des 3 lignes mentionne la date `2026-05-12`
    - Annotation distingue clairement le verdict (faux positif vs fix réel pour BUG-01, Fix #1 vs Fix #2 pour BUG-02)
  </acceptance_criteria>
  <done>BUG-01, BUG-02, BUG-03 fermés dans la backlog avec traçabilité</done>
</task>

<task type="auto">
  <name>Task 4.2: Update Traceability table + Progress de Phase 1 dans ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Progress table, Phase 1 line)
  </read_first>
  <action>
    Dans `.planning/ROADMAP.md`, section "## Progress", modifier la ligne Phase 1 :
    AVANT :
    ```
    | 1. Smoke verification + bugs critiques | 0/TBD | Not started | - |
    ```
    APRÈS :
    ```
    | 1. Smoke verification + bugs critiques | 4/4 | Complete | 2026-05-12 |
    ```

    Et marquer la checkbox Phase 1 cochée en haut du fichier :
    ```
    - [x] **Phase 1: Smoke verification + bugs critiques** - Vérifier en runtime + fixer les 3 bugs critiques de l'audit (FileText, header sticky, routes 404)
    ```
  </action>
  <verify>
    <automated>grep -q "Phase 1.*4/4.*Complete.*2026-05-12" .planning/ROADMAP.md && grep -qE '^\s*-\s*\[x\]\s*\*\*Phase\s+1:' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Progress ligne Phase 1 montre `4/4 | Complete | 2026-05-12`
    - ROADMAP.md checklist en tête montre `- [x] **Phase 1:`
  </acceptance_criteria>
  <done>Phase 1 marquée complete dans ROADMAP.md</done>
</task>

</tasks>

<verification>
- ✅ 3 lignes BUG-01/02/03 cochées dans REQUIREMENTS.md avec annotation cohérente
- ✅ Phase 1 marquée Complete dans ROADMAP.md (checklist + Progress table)
- ✅ Pas de conflit de write : ce plan tourne en wave 2, après que les 3 plans wave 1 ont produit leurs SUMMARY.md
</verification>

<success_criteria>
- Tous les artefacts de Phase 1 (3 PLAN.md, 3 SUMMARY.md, 1 VALIDATION.md, REQUIREMENTS.md, ROADMAP.md) sont en état cohérent
- La phase peut transitionner vers Phase 2 sans dette de bookkeeping
</success_criteria>

<output>
After completion, create `.planning/phases/01-smoke-verification-bugs-critiques/01-04-SUMMARY.md` listant :
- Les 3 lignes finales REQUIREMENTS.md
- Statut Phase 1 dans ROADMAP.md
- Prêt pour `/gsd:plan-phase 2`
</output>
