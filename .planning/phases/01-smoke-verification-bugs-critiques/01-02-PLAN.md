---
phase: 01-smoke-verification-bugs-critiques
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/next.config.mjs
  - CLAUDE.md
autonomous: true
requirements: [BUG-03]
user_setup: []

must_haves:
  truths:
    - "URL /app/pre-inscriptions redirige vers /app/preinscriptions avec statut 308"
    - "URL /app/modeles redirige vers /app/templates avec statut 308"
    - "Convention de naming routes documentée dans CLAUDE.md (évite régression)"
  artifacts:
    - apps/web/next.config.mjs (avec async redirects())
    - CLAUDE.md (section Routes convention)
  key_links:
    - "Redirects vivent dans next.config.mjs (root de l'app), conformes à la doc Next.js 14"
---

<objective>
Ajouter des redirects 308 (permanent) dans `apps/web/next.config.mjs` pour les URLs naturelles `/app/pre-inscriptions` et `/app/modeles`, et documenter la convention de naming routes dans `CLAUDE.md` pour éviter que le piège ne se reproduise.

Purpose: L'analyse code (CONCERNS.md) montre que le menu sidebar utilise déjà les bonnes routes. L'auditeur a tapé les URLs "naturelles" à la main → 404. Plutôt que renommer (casse les bookmarks/audit logs internes), on ajoute des redirects.
Output: 2 redirects fonctionnels + doc convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-CONTEXT.md
@.planning/phases/01-smoke-verification-bugs-critiques/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@apps/web/next.config.mjs
@apps/web/src/components/layout/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Lire next.config.mjs actuel + identifier point d'insertion</name>
  <files>apps/web/next.config.mjs</files>
  <read_first>
    - apps/web/next.config.mjs (full — confirmer structure ESM, vérifier qu'il n'y a pas déjà un async redirects())
    - apps/web/src/components/layout/sidebar.tsx (lignes 55 et 75 — confirmer routes cibles)
  </read_first>
  <action>
    1. Ouvrir `apps/web/next.config.mjs`.
    2. Vérifier le format : doit être ESM (`export default ...`).
    3. Vérifier la présence d'un `async redirects()` existant. Si OUI : ajouter les 2 entrées à la liste retournée. Si NON : ajouter la méthode complète au config.
    4. Vérifier que les routes cibles `/app/preinscriptions` et `/app/templates` existent bien (déjà confirmé en analyse — `apps/web/src/app/app/preinscriptions/` et `apps/web/src/app/app/templates/`).
  </action>
  <verify>
    <automated>test -f apps/web/next.config.mjs && grep -c "export default" apps/web/next.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/next.config.mjs` lu intégralement
    - Format ESM confirmé (`export default`)
    - Point d'insertion identifié : à l'intérieur de l'objet config exporté
    - Routes cibles existantes confirmées
  </acceptance_criteria>
  <done>Connaissance complète du fichier à modifier</done>
</task>

<task type="auto">
  <name>Task 2.2: Ajouter async redirects() dans next.config.mjs</name>
  <files>apps/web/next.config.mjs</files>
  <read_first>
    - apps/web/next.config.mjs (état post-Task 2.1)
  </read_first>
  <action>
    Ajouter (ou augmenter) la méthode `async redirects()` à l'intérieur de l'objet `nextConfig` (avant l'export). Le contenu EXACT à ajouter :

    ```js
    async redirects() {
      return [
        {
          source: '/app/pre-inscriptions',
          destination: '/app/preinscriptions',
          permanent: true,
        },
        {
          source: '/app/pre-inscriptions/:path*',
          destination: '/app/preinscriptions/:path*',
          permanent: true,
        },
        {
          source: '/app/modeles',
          destination: '/app/templates',
          permanent: true,
        },
        {
          source: '/app/modeles/:path*',
          destination: '/app/templates/:path*',
          permanent: true,
        },
      ];
    },
    ```

    Note : les variantes `:path*` couvrent les sous-routes (ex: si un jour `/app/pre-inscriptions/123` est tapée, redirige vers `/app/preinscriptions/123`).

    Si un `async redirects()` existait déjà : merge en ajoutant les 4 entrées ci-dessus à la liste retournée.
  </action>
  <verify>
    <automated>grep -A 2 "source: '/app/pre-inscriptions'" apps/web/next.config.mjs && grep -A 2 "source: '/app/modeles'" apps/web/next.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/next.config.mjs` contient la chaîne exacte `source: '/app/pre-inscriptions'`
    - `apps/web/next.config.mjs` contient la chaîne exacte `destination: '/app/preinscriptions'`
    - `apps/web/next.config.mjs` contient la chaîne exacte `source: '/app/modeles'`
    - `apps/web/next.config.mjs` contient la chaîne exacte `destination: '/app/templates'`
    - `apps/web/next.config.mjs` contient `permanent: true` au moins 4 fois (les 4 entrées + variantes)
    - Fichier reste un module ESM valide (parsing)
  </acceptance_criteria>
  <done>Redirects déclarés. Manque la vérification runtime (Task 2.3).</done>
</task>

<task type="auto">
  <name>Task 2.3: Vérifier les redirects en runtime via curl</name>
  <files>(none — runtime check)</files>
  <read_first>
    - apps/web/next.config.mjs (post-Task 2.2)
  </read_first>
  <action>
    1. Si dev server pas déjà lancé : `pnpm --filter @qualiof/web dev` en background. Attendre "Ready" (~10s).
    2. Lancer 4 vérifications curl :
       ```
       curl -sI http://localhost:3000/app/pre-inscriptions | grep -E "^(HTTP|Location|location)"
       curl -sI http://localhost:3000/app/modeles | grep -E "^(HTTP|Location|location)"
       curl -sI http://localhost:3000/app/pre-inscriptions/foo | grep -E "^(HTTP|Location|location)"
       curl -sI http://localhost:3000/app/modeles/bar | grep -E "^(HTTP|Location|location)"
       ```
    3. Pour chaque curl, vérifier :
       - Statut HTTP `308 Permanent Redirect` (Next 14 retourne 308 pour `permanent: true`)
       - Header `Location` (ou `location`) avec la destination attendue
    4. Coller la sortie des 4 curl dans le commit message comme preuve.
  </action>
  <verify>
    <automated>curl -sI http://localhost:3000/app/pre-inscriptions | grep -qiE "^(HTTP/[0-9.]+ 308|location: /app/preinscriptions)"</automated>
  </verify>
  <acceptance_criteria>
    - 4 curl exécutés
    - Chacun retourne statut `308`
    - Headers Location correspondent aux destinations attendues
    - Sortie copiée dans message de commit
  </acceptance_criteria>
  <done>Redirects effectivement actifs en runtime, BUG-03 résolu</done>
</task>

<task type="auto">
  <name>Task 2.4: Documenter convention naming routes dans CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full, pour identifier la bonne section où ajouter)
  </read_first>
  <action>
    Ajouter une section dans `CLAUDE.md` (à la fin de la section "Project" ou créer une sous-section "Routes" si elle n'existe pas) avec le contenu EXACT :

    ```md
    ### Routes (convention naming)
    
    - French routes, kebab-case where multi-word : `/app/dossiers-opco`, `/app/budget-agefice`
    - Exceptions historiques préservées :
      - `/app/preinscriptions` (no hyphen — depuis palier 4)
      - `/app/templates` (English — depuis palier 3)
    - **Toujours ajouter un redirect 308 dans `apps/web/next.config.mjs`** pour les variantes naturelles
      (avec et sans `:path*`) afin que les URLs tapées à la main par les utilisateurs n'aboutissent pas
      à un 404. Cf. audit 2026-05-12 BUG-03.
    - Avant d'ajouter une nouvelle route : décider hyphen vs no-hyphen, FR vs EN, et documenter ici.
    ```
  </action>
  <verify>
    <automated>grep -q "Routes (convention naming)" CLAUDE.md && grep -q "audit 2026-05-12 BUG-03" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contient la chaîne `Routes (convention naming)`
    - `CLAUDE.md` contient la chaîne `redirect 308`
    - `CLAUDE.md` contient la chaîne `BUG-03`
  </acceptance_criteria>
  <done>Convention documentée — futur contributeur (ou Claude) ne re-créera pas le piège</done>
</task>

<!-- NOTE : la mise à jour REQUIREMENTS.md BUG-03 est extraite dans Plan 01-04 (wave 2)
     pour sérialiser les writes concurrents avec Plans 01/03. -->

</tasks>

<verification>
- ✅ `next.config.mjs` contient les 4 redirects (sources/destinations exactes)
- ✅ Curl confirme les 4 redirects en runtime (statut 308, Location correct)
- ✅ `CLAUDE.md` contient la section convention routes
- 🔗 La mise à jour REQUIREMENTS.md (BUG-03 → [x]) est gérée par Plan 01-04 (wave 2)
</verification>

<success_criteria>
- Tapez n'importe quelle URL "naturelle" parmi `/app/pre-inscriptions`, `/app/modeles`, `/app/pre-inscriptions/foo`, `/app/modeles/bar` → redirige (308) vers la version canonique
- Aucune route renommée (audit logs, bookmarks préservés)
- Convention documentée pour éviter régression future
</success_criteria>

<output>
After completion, create `.planning/phases/01-smoke-verification-bugs-critiques/01-02-SUMMARY.md` with :
- 4 lignes curl avec statut + Location (preuve runtime)
- Lien vers la section CLAUDE.md ajoutée
</output>
