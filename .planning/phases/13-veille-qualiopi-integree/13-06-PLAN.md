---
phase: 13-veille-qualiopi-integree
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/13-veille-qualiopi-integree/13-SMOKE.md
  - .planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md
autonomous: false
requirements: [VEILLE-01, VEILLE-02, VEILLE-03, VEILLE-04]

must_haves:
  truths:
    - "STATE.md mentionne explicitement la convention AuditLog `regulatoryWatch.*` (7e instanciation one-helper-per-entity)."
    - "REQUIREMENTS.md a les 4 VEILLE-01..04 cochés [x] avec preuve (paths fichiers + counts tests verts)."
    - "ROADMAP.md Phase 13 passe à Complete avec 5 plans listés ([x] 13-01..05)."
    - "13-SMOKE.md documente 4 flows DevTools à valider (import xlsx, page UI 3 rôles, export PDF, worker dry-run)."
    - "13-SUMMARY.md récapitule les artefacts (LOC, tests counts, AuditLog verbes, sources RSS)."
    - "Une exécution réelle du worker dry-run + import xlsx + export PDF est documentée avec preuve (counts BDD)."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Position courante + convention AuditLog actualisée"
      contains: "regulatoryWatch.*"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VEILLE-01..04 cochés"
      contains: "[x] **VEILLE-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 Complete + 5 plans listés"
      contains: "13. Veille Qualiopi"
    - path: ".planning/phases/13-veille-qualiopi-integree/13-SMOKE.md"
      provides: "Checklist 4 flows manuels"
      min_lines: 60
    - path: ".planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md"
      provides: "Récap phase + métriques"
      min_lines: 80
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md"
      via: "ligne 'Last completed plan: 13-06'"
      pattern: "13-06|Phase 13.*Complete"
---

<objective>
Plan 06 (bookkeeping fin de phase 13) : Mettre à jour les artefacts de gouvernance GSD (STATE/REQUIREMENTS/ROADMAP), créer 13-SMOKE.md (4 flows manuels) et 13-SUMMARY.md (récap phase), exécuter les smoke manuels Plan 01/04/05, et instancier la convention AuditLog `regulatoryWatch.*` dans STATE.md (7e helper).

Purpose: Clore proprement la Phase 13 avec preuves d'exécution + traçabilité écosystème + nouvelle convention documentée pour les phases futures.

Output: 5 fichiers .planning/ mis à jour + 1 checkpoint human-verify (Laurent valide les 4 flows smoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/13-veille-qualiopi-integree/13-CONTEXT.md
@.planning/phases/13-veille-qualiopi-integree/13-RESEARCH.md
@.planning/phases/13-veille-qualiopi-integree/13-01-SUMMARY.md
@.planning/phases/13-veille-qualiopi-integree/13-02-SUMMARY.md
@.planning/phases/13-veille-qualiopi-integree/13-03-SUMMARY.md
@.planning/phases/13-veille-qualiopi-integree/13-04-SUMMARY.md
@.planning/phases/13-veille-qualiopi-integree/13-05-SUMMARY.md
@.planning/phases/09.1-centralisation-qualiopi-360/09.1-SMOKE.md
@.planning/phases/11-factures-cycle-complet/11-SMOKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke réel des 3 flows critiques (import xlsx, worker dry-run, export PDF)</name>
  <files>
    .planning/phases/13-veille-qualiopi-integree/13-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-SMOKE.md (clone-pattern 8 flows DevTools)
    - .planning/phases/11-factures-cycle-complet/11-SMOKE.md (clone-pattern checklist)
    - .planning/phases/13-veille-qualiopi-integree/13-01-SUMMARY.md (commande import xlsx exacte)
    - .planning/phases/13-veille-qualiopi-integree/13-05-SUMMARY.md (commandes probe/test-veille)
    - .planning/phases/13-veille-qualiopi-integree/13-04-SUMMARY.md (action export PDF)
  </read_first>
  <action>
**Étape A — Exécuter les 3 smokes réels** (ordre important) :

1. **Probe sources RSS** :
   ```bash
   pnpm --filter @qualiof/web probe:veille
   ```
   Attendu : tableau 12 lignes, ≥ 10/12 status 200 ou 302. Noter les sources qui ne répondent pas.

2. **Import xlsx one-shot** :
   ```bash
   pnpm --filter @qualiof/db exec tsx scripts/import-veille-from-xlsx.ts "/Users/laurentmarx/Documents/CRM Next gen/C6.i23-24-25tableau veille.xlsx"
   ```
   Attendu (1er run) : `~84 inserted, 0 updated, 0 skipped`.
   Re-run pour vérifier idempotence : `0 inserted, ~84 updated, 0 skipped`.

3. **Worker dry-run** :
   ```bash
   pnpm --filter @qualiof/web test:veille
   ```
   Attendu : `{ fetched: > 0, classified: > 0, skipped: > 0, inserted: > 0, errors: < 6 }`. 
   Vérifier en BDD : `SELECT count(*) FROM "RegulatoryWatch" WHERE status='DRAFT' AND "suggestedBy"='AUTO';` retourne ≥ 1.

4. **Verifier AuditLog** :
   ```sql
   SELECT action, count(*) FROM "AuditLog" WHERE entity='RegulatoryWatch' GROUP BY action;
   ```
   Attendu : `regulatoryWatch.created` ≥ 84 (import) + `regulatoryWatch.auto_inserted` ≥ 1 (worker dry-run).

**Étape B — Créer 13-SMOKE.md** (clone-style 09.1-SMOKE.md) avec 4 flows :

```markdown
# Phase 13 — Smoke validation manuelle

**Date d'exécution :** {{ today }}
**Validé par :** Laurent
**Statut :** ⬜ pending / ✅ done / ❌ failed

---

## Flow 1 — Probe RSS sources

**Commande :**
```bash
pnpm --filter @qualiof/web probe:veille
```

**Attendu :** ≥ 10 sources / 12 répondent HTTP 200 ou 302 avec content-type compatible XML/RSS.

**Résultat :** {{ paste output here }}

**Sources mortes à remplacer :** {{ list }}

---

## Flow 2 — Import xlsx one-shot

**Commande :**
```bash
pnpm --filter @qualiof/db exec tsx scripts/import-veille-from-xlsx.ts "/Users/laurentmarx/Documents/CRM Next gen/C6.i23-24-25tableau veille.xlsx"
```

**Attendu 1er run :** ~84 inserted, 0 updated.
**Re-run (idempotence) :** 0 inserted, ~84 updated.

**Résultat 1er run :** {{ paste }}
**Résultat 2e run :** {{ paste }}

---

## Flow 3 — Page /app/veille 3 rôles

### 3a — ADMIN
1. Login ADMIN.
2. Naviguer `/app/veille`.
3. Vérifier les 5 onglets visibles (4 thématiques + Inbox).
4. Cliquer un onglet (ex. indic_23) → URL passe en `?tab=indic_23`.
5. Cliquer "Éditer" sur une cellule Exploitation, modifier, Enregistrer → toast success.
6. Vérifier en BDD : `dateLastReviewed` mis à jour + AuditLog `regulatoryWatch.exploitation_updated`.

### 3b — MANAGER
Idem ADMIN, vérifier que Inbox + Édition fonctionnent.

### 3c — LECTEUR (D-03 — critique)
1. Login LECTEUR.
2. Naviguer `/app/veille`.
3. Vérifier que **l'onglet Inbox N'EST PAS rendu** (strictement masqué, pas grisé).
4. Forcer URL `?tab=inbox` → vérifier redirect vers `?tab=indic_23`.
5. Vérifier qu'aucun bouton "Éditer" / "Archiver" / "Exporter PDF" n'est rendu.
6. Vérifier que les watches DRAFT (suggérées AUTO) sont MASQUÉES (LECTEUR ne voit que ACTIVE).

---

## Flow 4 — Export PDF audit

1. Login ADMIN, `/app/veille`.
2. Onglet indic_23 → cliquer "Exporter PDF audit".
3. Attendre la génération (~5-15s WeasyPrint).
4. Toast success → nouvel onglet PDF s'ouvre.
5. Vérifier PDF :
   - Page A4
   - Header : logo Start Academy + tenant name + h1 thème + date export
   - Body : table 5-6 cols, ~84 sources max (limité au theme)
   - Footer paged : tenant name + SIRET + NDA + Page N/M
6. Vérifier en BDD :
   - `prisma.document.findFirst({ where: { type: 'VEILLE_AUDIT', tenantId } })` retourne 1 row
   - `prisma.auditLog.findFirst({ where: { action: 'regulatoryWatch.exported' } })` retourne 1 row

---

## Sign-Off

- [ ] Flow 1 : probe RSS OK
- [ ] Flow 2 : import xlsx OK 1er + idempotent re-run
- [ ] Flow 3a : ADMIN OK
- [ ] Flow 3b : MANAGER OK
- [ ] Flow 3c : LECTEUR — inbox masqué + DRAFT masqués OK
- [ ] Flow 4 : PDF audit + Document MinIO + AuditLog OK

**Bug bloquants :** {{ list }}
**Bug non-bloquants :** {{ list }}
```

Sauvegarder dans `.planning/phases/13-veille-qualiopi-integree/13-SMOKE.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-veille-qualiopi-integree/13-SMOKE.md && wc -l .planning/phases/13-veille-qualiopi-integree/13-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-veille-qualiopi-integree/13-SMOKE.md` exists, ≥ 60 lines
    - Contient les 4 flows numérotés (Flow 1, 2, 3a/b/c, 4)
    - Flow 3c explicite D-03 (LECTEUR inbox masqué + DRAFT masqués)
    - Section Sign-Off avec checkboxes
    - Commandes exactes (pnpm probe:veille, pnpm test:veille, import xlsx avec path absolu)
  </acceptance_criteria>
  <done>13-SMOKE.md créé, 4 flows documentés, commandes exécutables.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md + REQUIREMENTS.md + ROADMAP.md</name>
  <files>
    .planning/STATE.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md (section "Last activity" + "Decisions" + "Workflow Config")
    - .planning/REQUIREMENTS.md (section "Veille Qualiopi intégrée (Phase 13)" — 4 items VEILLE-* à cocher)
    - .planning/ROADMAP.md (section "Phase 13" + tableau Progress en bas)
    - .planning/phases/13-veille-qualiopi-integree/13-{01,02,03,04,05}-SUMMARY.md (extraire dates + commits + counts)
  </read_first>
  <action>
**Étape A — REQUIREMENTS.md** :

Localiser la section "Veille Qualiopi intégrée (Phase 13 — INSERTED 2026-05-23)" (vers ligne 80). Cocher les 4 items :

```markdown
- [x] **VEILLE-01** : Schéma Prisma `RegulatoryWatch` (...). **DONE {{ date }}** — Plan 13-01 : migration phase13_regulatory_watch (3 enums + 3 indexes + relation Tenant) + helper logRegulatoryWatchEvent (7e instance one-helper-per-entity) + parseFlexibleDate (3 formats) + import-veille-from-xlsx.ts idempotent (clone-strict import-smartof.ts). Smoke réel : ~84 entrées importées du xlsx, idempotence vérifiée. Plan 13-01.
- [x] **VEILLE-02** : Page `/app/veille` (...). **DONE {{ date }}** — Plans 13-02 (6 server actions ADMIN+MANAGER + 4 Zod + daysSince) + 13-03 (page Server Component + 8 composants UI + helper shouldShowInbox D-03 strictement LECTEUR sans inbox + sidebar enrichie). AuditLog verbes : regulatoryWatch.{created,updated,exploitation_updated,approved,rejected,archived}. Plans 13-02 + 13-03.
- [x] **VEILLE-03** : Export PDF audit (...). **DONE {{ date }}** — Plan 13-04 : DocType += VEILLE_AUDIT (migration additive) + veille-audit-template.ts (WeasyPrint + footer paged tenant SIRET/NDA) + generateVeilleAuditForTheme server action ADMIN+MANAGER + upload MinIO + INSERT Document (type=VEILLE_AUDIT, entityType=RegulatoryWatch, entityId=theme) + AuditLog regulatoryWatch.exported. Smoke : PDF 4-8 pages A4 lisible. Plan 13-04.
- [x] **VEILLE-04** : Worker BullMQ cron hebdo (...). **DONE {{ date }}** — Plan 13-05 : 8 modules lib/veille/* (core + sources + prompts + fetch-rss + classify + persist + queue + worker), worker safety pattern obligatoire (lib/veille/core.ts SANS imports React/auth/server-actions — grep -rE négatif vérifié = 0). Cron '0 8 * * 1' tz Europe/Paris jobId 'weekly-veille-cron'. Ollama mistral-small:24b classification + Zod validation + 5 guard-rails (OTHER skip + confidence < 50 skip + JSON malformed null + timeout retry 1× + dedup par (url, theme) D-11). AuditLog regulatoryWatch.auto_inserted (actorUserId=null). 3 scripts : worker:veille, test:veille, probe:veille. Plan 13-05.
```

Mettre à jour le bloc "Coverage" en bas : `53 / 53 v1 requirements mapped (100%)` (était 49/49 + 4 VEILLE).

**Étape B — ROADMAP.md** :

Mettre à jour Phase 13 section (vers ligne 240) :

```markdown
### Phase 13: Veille Qualiopi intégrée

**Goal:** Couvrir le critère 6 Qualiopi (...).
**Requirements**: [VEILLE-01, VEILLE-02, VEILLE-03, VEILLE-04]
**Depends on:** Nothing
**Success Criteria** (what must be TRUE): (inchangés — les 6 critères restent visibles)
**Plans:** 5/5 plans complete

Plans:
- [x] 13-01-PLAN.md — Foundation : migration RegulatoryWatch + 3 enums + 3 indexes + parseFlexibleDate + logRegulatoryWatchEvent (7e helper) + import xlsx idempotent ~84 entrées
- [x] 13-02-PLAN.md — Server actions + RBAC + AuditLog : 6 actions ADMIN+MANAGER (create/update/updateExploitation/approve/reject/archive) + 4 Zod schemas + helper daysSince
- [x] 13-03-PLAN.md — Page UI /app/veille : Server Component + 8 composants client + helper shouldShowInbox D-03 + sidebar enrichie
- [x] 13-04-PLAN.md — Export PDF audit : DocType += VEILLE_AUDIT + template HTML WeasyPrint + Document MinIO + AuditLog regulatoryWatch.exported
- [x] 13-05-PLAN.md — Worker BullMQ : 8 modules lib/veille/* (worker safety pattern) + cron hebdo + Ollama mistral-small:24b + 3 scripts dev
- [x] 13-06-PLAN.md — Bookkeeping : SMOKE + SUMMARY + STATE/REQUIREMENTS/ROADMAP
```

Et dans le tableau Progress en bas :
```
| 13. Veille Qualiopi intégrée | 6/6 | Complete    | {{ today }} |
```

**Étape C — STATE.md** :

Section "Last session" :
```markdown
Stopped at: Phase 13 closed — Veille Qualiopi intégrée 5/5 plans + bookkeeping (smoke validated)
Last commit: {{ sha }} — chore(phase-13): bookkeeping close phase 13 veille qualiopi
Last completed plan: 13-06 (bookkeeping)
Next plan: /gsd:plan-phase 12 (Modules stub Inscriptions et Modèles) ou /gsd:plan-phase 10 (Audit Qualiopi blanc) — choisir selon priorité Laurent

Last activity: {{ today }} - Closed Phase 13: convention AuditLog `regulatoryWatch.*` 7e instance (verbes created/updated/exploitation_updated/approved/rejected/archived/auto_inserted/exported), worker safety pattern lib/veille/core.ts vérifié (0 imports React/auth), import xlsx idempotent ~84 entrées, page /app/veille 5 onglets dont inbox masqué LECTEUR D-03, export PDF stocké en MinIO comme Document VEILLE_AUDIT.
```

Section "Decisions" (à la fin du bloc daté) :
```markdown
- {{ today }} — **D-13-A AuditLog convention `regulatoryWatch.*` (7e instanciation one-helper-per-entity)** : helper `apps/web/src/lib/regulatoryWatch-audit.ts` (clone-strict logInvoiceEvent Phase 11). 8 verbes : created (import xlsx + UI manuel), updated, exploitation_updated (inline edit + dateLastReviewed=now), approved (DRAFT→ACTIVE inbox), rejected (DRAFT→ARCHIVED + rejectionReason), archived, auto_inserted (worker, actorUserId=null), exported (PDF audit, targetWatchId='BULK', diff inclut theme+count+documentId). Convention figée pour futures phases si pattern.
- {{ today }} — **D-13-B Worker safety pattern obligatoire `lib/veille/core.ts`** : tous les modules consommés par le worker BullMQ (core, queue, worker, persist, classify, fetch-rss, sources) SANS imports `server/actions/*`, SANS imports `lib/rbac` (validateRequest/requireRole), SANS imports `react`. Vérification automatique : `grep -rE "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/veille/` = 0. Pattern reproduit Phase 11 (invoice-reminder-worker) + mémoire feedback_worker_no_react_imports.md. Toute future server action veille doit être un WRAPPER fin dans `actions/veille.ts` qui appelle `lib/veille/core.ts`. Plan 13-05.
- {{ today }} — **D-13-C LECTEUR strictement masqué de l'inbox (D-03 figée)** : helper isolé `apps/web/src/app/app/veille/page-helpers.ts:shouldShowInbox(user)` retourne false pour LECTEUR. UI : onglet inbox NON RENDU (pas grisé, pas affiché). Defense-in-depth : la page redirige `?tab=inbox` vers `?tab=indic_23` si LECTEUR force l'URL. LECTEUR ne voit aussi QUE les watches ACTIVE (les DRAFT/AUTO sont filtrées WHERE status='ACTIVE' au lieu de WHERE status IN ('ACTIVE','DRAFT')). Justification Qualiopi : la veille validée = source unique vérité, les drafts auto sont du WIP qui peut induire en erreur les LECTEURs. Plans 13-03 + 13-04.
- {{ today }} — **D-13-D PDF audit stocké en MinIO comme Document (D-02 figée)** : chaque export PDF crée 1 row Document (type=VEILLE_AUDIT, entityType='RegulatoryWatch', entityId={theme}, pdfUrl=MinIO key, hashSha256=sha256(pdfBuffer)) — pas de génération volatile. Traçabilité Qualiopi : auditeur peut revoir la version exportée à date X. AuditLog regulatoryWatch.exported référence documentId. Plan 13-04.
- {{ today }} — **D-13-E DocType += VEILLE_AUDIT (extension enum additive)** : 21e valeur ajoutée à enum DocType, sans casser les 20 existantes. Convention `entityType='RegulatoryWatch'` + `entityId={theme}` plutôt que linker sur 1 ligne RegulatoryWatch précise (l'export est un agrégat par thème, pas une fiche par source). Plan 13-04.
```
  </action>
  <verify>
    <automated>grep -c "regulatoryWatch\." .planning/STATE.md && grep -c "\[x\] \*\*VEILLE-" .planning/REQUIREMENTS.md && grep -c "13. Veille Qualiopi.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "regulatoryWatch\." .planning/STATE.md` returns ≥ 5 (convention référencée multiple fois)
    - `grep -c "\[x\] \*\*VEILLE-" .planning/REQUIREMENTS.md` returns 4 (les 4 VEILLE-* cochés)
    - `grep -c "13-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns ≥ 6 (les 6 plans listés)
    - `grep -c "Complete" .planning/ROADMAP.md` increases by 1 (Phase 13 passe Complete)
    - STATE.md "Last completed plan: 13-06" présent
    - D-13-A à D-13-E (≥ 5 décisions) dans STATE.md
    - Coverage ligne dans REQUIREMENTS.md mise à jour (53/53 ou équivalent)
  </acceptance_criteria>
  <done>3 fichiers gouvernance mis à jour, 5 décisions D-13-A..E documentées, traçabilité complète.</done>
</task>

<task type="auto">
  <name>Task 3: 13-SUMMARY.md final + git commit phase</name>
  <files>
    .planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/13-veille-qualiopi-integree/13-{01,02,03,04,05}-SUMMARY.md
    - .planning/phases/09.1-centralisation-qualiopi-360/09.1-SUMMARY.md (clone-pattern style 09.1 fin de phase)
    - .planning/phases/11-factures-cycle-complet/11-SUMMARY.md (clone-pattern style)
  </read_first>
  <action>
**Étape A — Créer 13-SUMMARY.md** récapitulatif :

Structure :
```markdown
# Phase 13 — Veille Qualiopi intégrée — SUMMARY

**Closed:** {{ today }}
**Duration:** {{ X }} jours (de {{ start }} à {{ end }})
**Status:** ✅ Complete (6/6 plans, 4/4 requirements)

## Goal Achieved
Couvrir le critère 6 Qualiopi (indicateurs 23/24/25/26) via une veille intégrée dans QualiOF, 100% locale (RSS + Ollama, 0 coût API), avec exploitation tracée et export PDF audit.

## Plans & Artifacts

### Plan 13-01 — Foundation (VEILLE-01)
- Migration Prisma `phase13_regulatory_watch` : model RegulatoryWatch + 3 enums + 3 indexes + relation Tenant
- Helpers : parseFlexibleDate (3 formats date xlsx) + logRegulatoryWatchEvent (7e instance one-helper-per-entity)
- Script `import-veille-from-xlsx.ts` idempotent (5 feuilles → 4 thèmes, ~84 entrées importées)
- Tests Wave 0 : 8 + 3 + 5 = 16 verts

### Plan 13-02 — Server actions + RBAC (VEILLE-02 backend)
- 6 server actions ADMIN+MANAGER : createWatch / updateWatch / updateExploitation / approveWatch / rejectWatch / archiveWatch
- 4 Zod schemas dans @qualiof/shared (createWatch / updateWatch / updateExploitation / rejectWatch)
- Helper daysSince (KPI X jours)
- Tests Wave 0 : 4 + 6 + 3 + 5 = 18 verts

### Plan 13-03 — Page UI /app/veille (VEILLE-02 frontend)
- Route Server Component + 5 onglets URL state (4 thématiques + inbox conditionnel)
- 8 composants client : VeilleTabsClient, VeilleTable, ExploitationCell, VeilleRowActions, AddVeilleDialog, EditVeilleDialog, VeilleInbox, DaysSinceBadge
- Helper isolé shouldShowInbox (D-03 LECTEUR strictement masqué)
- Sidebar enrichie (1 entrée /app/veille)
- Tests Wave 0 : 3 page smoke + 4 inbox RBAC = 7 verts

### Plan 13-04 — Export PDF audit MinIO (VEILLE-03)
- Migration DocType += VEILLE_AUDIT
- veille-audit-template.ts (WeasyPrint + footer paged tenant SIRET/NDA)
- generateVeilleAuditForTheme action ADMIN+MANAGER + upload MinIO + INSERT Document + AuditLog regulatoryWatch.exported
- export-pdf-button.tsx (client useTransition + toast)
- Tests Wave 0 : 3 + 3 + 4 + 3 = 13 verts

### Plan 13-05 — Worker BullMQ RSS + Ollama (VEILLE-04)
- 8 modules lib/veille/* (sources, prompts, fetch-rss, classify, persist, core, queue, worker)
- Worker safety pattern : 0 imports server-actions/auth/react (grep négatif = 0)
- Cron '0 8 * * 1' tz Europe/Paris jobId fixe
- Ollama mistral-small:24b + Zod + 5 guard-rails (OTHER/low-conf/JSON-fail/timeout/dedup)
- 3 scripts entrypoint : worker:veille, test:veille, probe:veille
- AuditLog regulatoryWatch.auto_inserted (actorUserId=null)
- Tests Wave 0 : 3 + 4 + 3 + 3 + 3 = 16 verts

### Plan 13-06 — Bookkeeping
- 13-SMOKE.md (4 flows manuels)
- 13-SUMMARY.md (ce fichier)
- STATE/REQUIREMENTS/ROADMAP mis à jour
- 5 décisions D-13-A..E figées

## Metrics

| Item | Count |
|------|-------|
| Plans | 6 |
| Tasks total | ~17 |
| Tests Wave 0 verts | 70+ (16 + 18 + 7 + 13 + 16) |
| Production files créés | ~25 (lib/veille/*, components/veille/*, server/actions/veille*, scripts/*) |
| LOC production | ~1500-1800 |
| AuditLog verbes instanciés | 8 (created, updated, exploitation_updated, approved, rejected, archived, auto_inserted, exported) |
| Sources RSS seed | 12 |
| Entrées xlsx importées | ~84 |
| Modèles Prisma ajoutés | 1 (RegulatoryWatch) + 3 enums + 1 valeur enum DocType (VEILLE_AUDIT) |

## Decisions Locked (D-13-*)

| ID | Décision | Plan |
|----|----------|------|
| D-13-A | AuditLog convention `regulatoryWatch.*` (7e helper) | 13-01 |
| D-13-B | Worker safety pattern lib/veille/core.ts (0 imports React/auth) | 13-05 |
| D-13-C | LECTEUR strictement masqué de l'inbox (D-03) | 13-03 |
| D-13-D | PDF audit stocké MinIO comme Document VEILLE_AUDIT (D-02) | 13-04 |
| D-13-E | DocType += VEILLE_AUDIT (extension enum additive) | 13-04 |

## Smoke Validation

Cf. `13-SMOKE.md` — 4 flows :
1. Probe RSS sources (≥ 10/12 OK)
2. Import xlsx (1er + re-run idempotence)
3. Page /app/veille pour 3 rôles (D-03 inbox masqué LECTEUR)
4. Export PDF audit (MinIO + Document + AuditLog)

Statut : ⬜ pending Laurent validation

## Risks & Follow-up

- Sources RSS bougent → audit régulier via `probe:veille` (M+1 à scheduler)
- Latence Ollama mistral-small:24b ~5-10s/item → cron hebdo dimensionné (60 calls max / run, ~10 min)
- Pas d'observabilité worker (logs only) — V2 envisageable : table `VeilleIngestionLog` avec metrics par run
- Confidence Ollama < 50 → skip silent. Si peu d'inserts en pratique, baisser le seuil (passer < 50 → < 30) ou améliorer le prompt §6.1
- 12 sources V1, mais ~30 dans le xlsx original → V2 : interface admin pour ajouter des sources sans PR

## Next Step

`/gsd:plan-phase 12` (Modules stub Inscriptions et Modèles) ou `/gsd:plan-phase 10` (Audit Qualiopi blanc).
```

**Étape B — Commit (si gsd config commit_docs=true, sinon noop)** :

Vu que STATE.md indique `Commit docs | false (local-only)`, NE PAS commit. Juste laisser .planning/ en local sur disque.

Si Laurent demande un commit explicite : 
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-13): close phase Veille Qualiopi (6 plans, 70+ tests)" --files .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md .planning/phases/13-veille-qualiopi-integree/13-SMOKE.md
```
  </action>
  <verify>
    <automated>test -f .planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md && wc -l .planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md` exists, ≥ 80 lines
    - Sections présentes : Goal Achieved, Plans & Artifacts (6 plans détaillés), Metrics (tableau), Decisions Locked (D-13-A..E), Smoke Validation, Risks & Follow-up, Next Step
    - Tableau Metrics inclut counts tests, LOC, AuditLog verbes, sources RSS
    - Référence à `13-SMOKE.md` présente
  </acceptance_criteria>
  <done>13-SUMMARY.md complet, traçabilité phase 13 archivée.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Laurent valide les 4 flows smoke en runtime</name>
  <what-built>
    Phase 13 complète : table RegulatoryWatch + 84 entrées xlsx + page /app/veille (3 rôles) + worker BullMQ RSS+Ollama + export PDF audit MinIO + 70+ tests Wave 0.
  </what-built>
  <how-to-verify>
    Exécuter les 4 flows de `.planning/phases/13-veille-qualiopi-integree/13-SMOKE.md` :

    **Flow 1 — Probe RSS** :
    `pnpm --filter @qualiof/web probe:veille`
    → ≥ 10/12 sources répondent.

    **Flow 2 — Import xlsx** :
    `pnpm --filter @qualiof/db exec tsx scripts/import-veille-from-xlsx.ts "/Users/laurentmarx/Documents/CRM Next gen/C6.i23-24-25tableau veille.xlsx"`
    → ~84 inserted au 1er run, ~84 updated au 2e run.

    **Flow 3 — Page /app/veille 3 rôles** :
    Login successivement ADMIN, MANAGER, LECTEUR (créer un user LECTEUR via /app/parametres/utilisateurs si pas dispo).
    Vérifier surtout pour LECTEUR : inbox NON RENDU (pas grisé), watches DRAFT/AUTO MASQUÉES.

    **Flow 4 — Export PDF + Worker dry-run** :
    Login ADMIN, /app/veille onglet indic_23, "Exporter PDF audit" → vérifier PDF ouvert + ligne Document en BDD.
    `pnpm --filter @qualiof/web test:veille` → vérifier IngestResult non-zero + ≥ 1 RegulatoryWatch DRAFT/AUTO en BDD + AuditLog regulatoryWatch.auto_inserted.

    Cocher les boxes du Sign-Off dans 13-SMOKE.md.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 flows pass, or describe any blocking bug to file as gap closure.</resume-signal>
</task>

</tasks>

<verification>
- 5 fichiers .planning/* mis à jour (STATE/REQUIREMENTS/ROADMAP/SMOKE/SUMMARY).
- 4 flows smoke documentés + checkpoint Laurent.
- Convention AuditLog `regulatoryWatch.*` (8 verbes) explicite dans STATE.md.
- 5 décisions D-13-A..E figées.
- Pas de commit forcé (commit_docs=false en config GSD).
</verification>

<success_criteria>
1. 13-SMOKE.md créé avec 4 flows + sign-off.
2. 13-SUMMARY.md créé (≥ 80 lignes) avec métriques + décisions + risks.
3. REQUIREMENTS.md a 4 VEILLE-* cochés avec preuve.
4. ROADMAP.md Phase 13 passe Complete (6/6 plans).
5. STATE.md référence convention regulatoryWatch.* + décisions D-13-A..E + last completed plan 13-06.
6. Checkpoint Laurent validé OU gaps documentés pour /gsd:plan-phase 13 --gaps.
</success_criteria>

<output>
After Laurent's approval :
- Phase 13 fermée définitivement.
- Prochaine étape : /gsd:plan-phase 12 (Modules stub) ou /gsd:plan-phase 10 (Audit Qualiopi blanc) selon priorité Laurent.

Si bug bloquant trouvé : 
- Documenter dans `.planning/phases/13-veille-qualiopi-integree/13-VERIFICATION.md` (clone style 09.1-VERIFICATION.md)
- Lancer `/gsd:plan-phase 13 --gaps` pour planifier gap closure
</output>
