---
phase: 21-app-vercel-filet-ci-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "Chaque clé storage référencée en base (8 champs / 2 buckets) résout à un objet Supabase existant — 0 lien mort AVANT les tests staging (D-06, bug SES-0094)"
    - "Les objets créés en local depuis le 2026-07-04 (présents uniquement dans MinIO) sont copiés vers Supabase"
    - "MinIO n'est PAS purgé (destructif = étape séparée, Phase 22+)"
  artifacts:
    - path: ".planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md"
      provides: "Rapport daté du backfill : compte DRY, compte WRITE, 0 lien mort"
      min_lines: 20
  key_links:
    - from: "apps/web/scripts/migrate-storage.ts"
      to: "Supabase Storage (projet gntlqyscahbgjrmsbzil)"
      via: "re-run idempotent DRY→WRITE (upsert:true)"
      pattern: "WRITE=1"
---

<objective>
Auditer puis backfiller les objets storage MinIO→Supabase créés depuis la migration Phase 18 (2026-07-04), via le script idempotent existant `migrate-storage.ts` — AVANT toute vague de tests staging (D-06 verrouillée). Sans ce backfill, les smoke/E2E vérifieraient des documents à liens morts (faux verts) : c'est le bug SES-0094 (validation pack du 2026-07-06, objets locaux jamais migrés).

Purpose: prérequis dur de TEST-01/TEST-02 — les tests contre le staging (STORAGE_PROVIDER=supabase) doivent lire des objets réellement présents.
Output: rapport daté 0 lien mort dans `.planning/audit/`, MinIO intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-app-vercel-filet-ci-tests/21-CONTEXT.md
@.planning/audit/STORAGE-MIGRATION-REPORT-2026-07-04.md
@apps/web/scripts/migrate-storage.ts

<interfaces>
<!-- Vérifié 2026-07-06 dans le header de migrate-storage.ts -->
Script : `apps/web/scripts/migrate-storage.ts`
- Périmètre : 8 champs / 2 buckets — qualiof-docs (Person.ribKey, SensitiveData.idDocumentUrl, Invoice.pdfUrl, Quote.pdfUrl, Document.pdfUrl, AgeficeProfile.cfpAttestationKey, PedagogicalAsset.pdfUrl) + preinscriptions (PreEnrollment.cniKey/ribKey/cfpKey)
- Clients DÉDIÉS : S3 (MinIO source, env `S3_*` localhost:9000) + supabase-js (cible, env `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`) — indépendant du switch global `STORAGE_PROVIDER`
- Flags : `WRITE=1` pour persister (défaut = DRY, n'écrit RIEN) ; `BUCKET=docs|preinscriptions` filtre optionnel
- Idempotent (`upsert:true`), SÉQUENTIEL (for...of await, jamais de parallèle — leçon deadlocks), try/catch par clé (orphelins LISTÉS sans action)
- Écrit un rapport daté ancré à la racine repo (fix %20 Phase 18 : `pathToFileURL`/`fileURLToPath`)
- Invocation : `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx scripts/migrate-storage.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DRY — mesurer l'écart MinIO/Supabase depuis le 2026-07-04</name>
  <files>(aucun fichier de code modifié — run DRY + collecte de sortie)</files>
  <read_first>apps/web/scripts/migrate-storage.ts, .planning/audit/STORAGE-MIGRATION-REPORT-2026-07-04.md</read_first>
  <action>
    1. Démarrer MinIO local (source) : `docker compose up -d minio` depuis la racine repo ; vérifier `curl -s -o /dev/null -w "%{http_code}" http://localhost:9000/minio/health/live` → 200. Vérifier que `.env` racine contient `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (posées Phase 18) et les `S3_*` par défaut (localhost:9000).
    2. Run DRY (n'écrit RIEN, ni MinIO ni Supabase) :
       `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx scripts/migrate-storage.ts`
       Capturer la sortie complète : total de clés référencées en base, déjà présentes côté Supabase, MANQUANTES (= à backfiller), orphelines (référencées en base mais absentes des DEUX stores — à lister, pas à corriger).
    3. Comparer avec la baseline `.planning/audit/STORAGE-MIGRATION-REPORT-2026-07-04.md` (3109 objets, 0 lien mort au 2026-07-04). L'écart attendu = objets créés en local entre le 2026-07-04 et aujourd'hui (packs SES-0094 et suivants).
    4. Si le DRY révèle 0 manquant : consigner « backfill sans objet, base déjà synchrone » et passer la Task 2 en simple re-vérification 0 lien mort.
    5. NE PAS lancer WRITE dans cette task (étape séparée, Task 2 — convention projet).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && docker compose ps minio | grep -qi "running\|up" && pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx scripts/migrate-storage.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - MinIO répond sur :9000 (health live 200)
    - La sortie DRY affiche des compteurs par champ/bucket (total / présents / manquants / orphelins) sans throw
    - Les compteurs DRY sont consignés dans le SUMMARY (base du rapport Task 2)
    - AUCUN upload effectué (mode DRY, `WRITE` non posé)
  </acceptance_criteria>
  <done>L'écart exact MinIO→Supabase est mesuré et chiffré, zéro écriture</done>
</task>

<task type="auto">
  <name>Task 2: Backfill WRITE + preuve 0 lien mort + rapport daté</name>
  <files>.planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md</files>
  <read_first>apps/web/scripts/migrate-storage.ts</read_first>
  <action>
    1. Run WRITE (idempotent, additif — copie MinIO→Supabase, ne supprime RIEN nulle part) :
       `WRITE=1 pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx scripts/migrate-storage.ts`
       Capturer la sortie complète (uploadés / déjà présents / orphelins).
    2. Re-run DRY immédiat pour PROUVER la convergence : le compte « manquants » doit être 0 (idempotence : un 2ᵉ WRITE ne changerait rien).
    3. Créer `.planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md` (calqué sur le rapport 2026-07-04) : contexte (bug SES-0094, D-06 Phase 21), compteurs DRY avant / WRITE / DRY après, liste des orphelins éventuels (sans action, convention D-04 Phase 18), phrase de preuve littérale « 0 lien mort — chaque clé référencée en base résout à un objet Supabase », et rappel « MinIO NON purgé (destructif = étape séparée, Phase 22+) ».
    4. Si des ORPHELINS existent (clé en base, objet dans aucun store) : les LISTER dans le rapport avec table+champ+id de row — ne PAS les corriger ni les supprimer (décision Phase 22).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md && grep -q "0 lien mort" .planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md && grep -qi "MinIO NON purgé\|MinIO non purgé" .planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md</automated>
  </verify>
  <acceptance_criteria>
    - Le re-run DRY post-WRITE affiche 0 manquant sur les 8 champs / 2 buckets
    - `.planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md` existe et contient : les compteurs avant/après, la chaîne `0 lien mort`, la mention MinIO non purgé
    - Aucune commande de suppression (rm/delete/remove) exécutée contre MinIO ou Supabase
  </acceptance_criteria>
  <done>Le staging (STORAGE_PROVIDER=supabase) servira des documents réellement lisibles — les tests des vagues suivantes ne peuvent plus produire de faux verts storage</done>
</task>

</tasks>

<verification>
- Rapport daté présent dans `.planning/audit/` avec preuve 0 lien mort
- Re-run DRY = 0 manquant (idempotence prouvée)
- MinIO local intact (aucune purge)
</verification>

<success_criteria>
- D-06 satisfaite : audit + backfill exécutés AVANT toute vague de tests staging
- Le bug SES-0094 (objets jamais migrés) est soldé et documenté
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-vercel-filet-ci-tests/21-02-SUMMARY.md`
</output>
