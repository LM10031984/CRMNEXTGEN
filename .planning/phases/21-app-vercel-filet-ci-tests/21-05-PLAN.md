---
phase: 21-app-vercel-filet-ci-tests
plan: 05
type: execute
wave: 4
depends_on: ["21-04", "21-02"]
files_modified:
  - apps/web/package.json
  - apps/web/playwright.config.ts
  - apps/web/e2e/auth.setup.ts
  - apps/web/e2e/smoke-routes.spec.ts
  - apps/web/e2e/upload-preenrollment.spec.ts
  - apps/web/scripts/create-e2e-user.ts
  - .gitignore
autonomous: true
requirements: [TEST-02, APP-02]
must_haves:
  truths:
    - "Le flux login → /app → logout fonctionne sur le domaine final (cookie session posé, secure+lax) — preuve runtime APP-02"
    - "Les ~9 routes protégées des 4 piliers redirigent vers /login en anonyme et rendent 200 authentifié (D-12)"
    - "Le formulaire public /preinscription/[token] est accessible avec un token valide et refuse proprement un token bidon (JAMAIS /p/[token] — Pitfall 1)"
    - "Un upload 10 Mo direct-to-storage passe sur Vercel déployé sans 413 (PENDING 18-SMOKE re-validé)"
  artifacts:
    - path: "apps/web/playwright.config.ts"
      provides: "Config Playwright : baseURL STAGING_BASE_URL, projets setup/anonymous/authenticated, storageState"
      contains: "STAGING_BASE_URL"
    - path: "apps/web/e2e/auth.setup.ts"
      provides: "Login UI réel → storageState (valide APP-02 en même temps)"
      contains: "storageState"
    - path: "apps/web/e2e/smoke-routes.spec.ts"
      provides: "TEST-02 : table de routes anonyme/authentifié"
      min_lines: 60
    - path: "apps/web/scripts/create-e2e-user.ts"
      provides: "User e2e dédié ADMIN (jamais les credentials de Laurent)"
      contains: "e2e@start-academy.fr"
  key_links:
    - from: "apps/web/e2e/auth.setup.ts"
      to: "https://<staging>/login"
      via: "vrai login UI (E2E_LOGIN_EMAIL/PASSWORD) → e2e/.auth/user.json"
      pattern: "E2E_LOGIN_EMAIL"
    - from: "apps/web/e2e/upload-preenrollment.spec.ts"
      to: "Supabase Storage (PUT direct)"
      via: "signed upload URL — AUCUN octet >4,5MB via le domaine Vercel"
      pattern: "supabase"
---

<objective>
Monter l'infrastructure Playwright (config, user e2e dédié, auth par storageState) et prouver TEST-02 + APP-02 contre le staging déployé : smoke des ~10 routes des 4 piliers (redirect anonyme + 200 authentifié), login/logout réel sur le domaine final, et re-validation du PENDING Phase 18 « upload 10 Mo sans 413 ».

Purpose: filet minimal viable (hors scope : E2E exhaustif). Exécution À LA DEMANDE contre le staging (D-10) — ces tests ne sont PAS dans le gate PR.
Output: specs verts contre le staging, storageState fonctionnel, base pour le plan 21-06 (E2E closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-app-vercel-filet-ci-tests/21-CONTEXT.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-RESEARCH.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md
@apps/web/src/app/login/actions.ts
@apps/web/src/app/preinscription/[token]/page.tsx

<interfaces>
<!-- Vérifié 2026-07-06 -->
- Login : `apps/web/src/app/login/actions.ts` redirige vers `/app` après succès (ligne ~129). Page : `/login`.
- Redirect auth : `apps/web/src/app/app/layout.tsx:27` → `if (!user) redirect('/login')` → Next renvoie 307. En Playwright, asserter `page.url()` finit sur `/login` (plus robuste que le code HTTP).
- Route publique : `apps/web/src/app/preinscription/[token]/page.tsx` (PAS /p/ — Pitfall 1 ; le CLAUDE.md est périmé sur ce point).
- Route admin préinscriptions : `/app/inscriptions` (le répertoire réel — /app/preinscriptions 308-redirige vers /app/inscriptions, vérifié next.config.mjs lignes 49-59).
- Upload direct : composant `direct-upload-field.tsx` → XHR PUT direct vers `*.supabase.co/storage/v1/object/upload/sign/preinscriptions/...` (Phase 18) — le fichier ne transite PAS par Vercel.
- argon2 ^0.41.1 disponible (deps apps/web — utilisé par login/actions.ts).
- Playwright : `@playwright/test` ^1.61.1 (npm vérifié 2026-07-06) — PAS encore installé.
- Si Deployment Protection active : header `x-vercel-protection-bypass` avec le secret relevé au 21-04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer Playwright + config + user e2e dédié + auth.setup</name>
  <files>apps/web/package.json, apps/web/playwright.config.ts, apps/web/e2e/auth.setup.ts, apps/web/scripts/create-e2e-user.ts, .gitignore</files>
  <read_first>apps/web/src/app/login/actions.ts, apps/web/src/app/login/page.tsx, apps/web/package.json, apps/web/vitest.config.ts, packages/db/prisma/seed.ts</read_first>
  <action>
    1. Installer : `pnpm --filter @qualiof/web add -D @playwright/test` puis `pnpm --filter @qualiof/web exec playwright install chromium`.
    2. Créer `apps/web/playwright.config.ts` (RESEARCH Pattern 7) :
       ```ts
       import { defineConfig } from '@playwright/test';
       const bypassSecret = process.env.VERCEL_AUTOMATION_BYPASS_SECRET;
       export default defineConfig({
         testDir: './e2e',
         timeout: 60_000,
         retries: 1,
         use: {
           baseURL: process.env.STAGING_BASE_URL ?? 'http://localhost:3010',
           trace: 'retain-on-failure',
           ...(bypassSecret ? { extraHTTPHeaders: { 'x-vercel-protection-bypass': bypassSecret } } : {}),
         },
         projects: [
           { name: 'setup', testMatch: /auth\.setup\.ts/ },
           { name: 'anonymous', testMatch: /smoke-routes\.spec\.ts/, grep: /@anon/ },
           {
             name: 'authenticated',
             dependencies: ['setup'],
             use: { storageState: 'e2e/.auth/user.json' },
             testMatch: /(smoke-routes|upload-preenrollment|closure-flow)\.spec\.ts/,
             grepInvert: /@anon/,
           },
         ],
       });
       ```
       PAS de `webServer` (cible distante, D-10). Le testDir `e2e/` est HORS du glob vitest (`src/**`, `scripts/**`) → aucune collision de runners.
    3. Créer `apps/web/scripts/create-e2e-user.ts` — script tsx worker/CLI-safe (imports UNIQUEMENT `@qualiof/db` + `argon2`, AUCUN import React/auth — règle worker) : upsert d'un `User` `e2e@start-academy.fr`, rôle `ADMIN`, `firstName: 'E2E'`, `lastName: 'Test'`, `tenantId` du premier Tenant (`prisma.tenant.findFirstOrThrow()`), `hashedPassword: await argon2.hash(process.env.E2E_LOGIN_PASSWORD!)` (throw explicite si `E2E_LOGIN_PASSWORD` absent ; lire le nom EXACT du champ password dans schema.prisma — vérifier via `grep -n "password" packages/db/prisma/schema.prisma`). Garde d'entrée `import.meta.url === pathToFileURL(process.argv[1]).href` (leçon %20 Phase 18). Idempotent (upsert sur email). 0 PII loggé.
       Exécution : générer un mot de passe fort (`openssl rand -base64 24`), l'ajouter à `.env` racine local (gitignoré) sous `E2E_LOGIN_EMAIL=e2e@start-academy.fr` / `E2E_LOGIN_PASSWORD=...`, puis `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx scripts/create-e2e-user.ts`.
    4. Créer `apps/web/e2e/auth.setup.ts` : ouvre `/login`, remplit les champs email/password (lire les name/id exacts dans `login/page.tsx`), submit, `await page.waitForURL('**/app')`, puis `await page.context().storageState({ path: 'e2e/.auth/user.json' })`. Credentials depuis `process.env.E2E_LOGIN_EMAIL/E2E_LOGIN_PASSWORD` (throw si absents).
    5. `.gitignore` racine : ajouter `apps/web/e2e/.auth/` et `apps/web/test-results/` et `apps/web/playwright-report/`.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q '"@playwright/test"' apps/web/package.json && test -f apps/web/playwright.config.ts && grep -q "STAGING_BASE_URL" apps/web/playwright.config.ts && grep -q "e2e@start-academy.fr" apps/web/scripts/create-e2e-user.ts && grep -q "pathToFileURL" apps/web/scripts/create-e2e-user.ts && grep -q "e2e/.auth" .gitignore && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `@playwright/test` dans devDependencies apps/web ; chromium installé (`playwright install chromium` exit 0)
    - `playwright.config.ts` : 3 projets (setup, anonymous, authenticated), storageState `e2e/.auth/user.json`, pas de webServer
    - `create-e2e-user.ts` : 0 import React/auth (`grep -c "from 'react'\|next/" = 0`), garde pathToFileURL, upsert idempotent
    - User créé en base cloud : re-run du script = exit 0 sans doublon
    - `E2E_LOGIN_*` UNIQUEMENT dans `.env` local (gitignoré) — `git grep E2E_LOGIN_PASSWORD` ne matche que du code lisant process.env
    - `tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Auth Playwright opérationnelle via un compte dédié — les credentials de Laurent ne sont jamais exposés</done>
</task>

<task type="auto">
  <name>Task 2: Smoke routes des 4 piliers (TEST-02) — écrire + exécuter contre le staging</name>
  <files>apps/web/e2e/smoke-routes.spec.ts</files>
  <read_first>apps/web/src/app/app/layout.tsx, apps/web/src/app/preinscription/[token]/page.tsx, packages/db/prisma/schema.prisma</read_first>
  <action>
    Créer `apps/web/e2e/smoke-routes.spec.ts` couvrant EXACTEMENT cette table (D-12, routes vérifiées sur le filesystem) :
    | Route | @anon (anonyme) | authentifié | Pilier |
    | `/login` | 200, formulaire visible | — | — |
    | `/app` | URL finale contient `/login` | 200, heading dashboard | transverse |
    | `/app/sessions` | redirect `/login` | 200 | #1 |
    | `/app/sessions/<id>` (1ʳᵉ session : la récupérer via Prisma `trainingSession.findFirst` dans le spec, jamais d'id en dur) | redirect `/login` | 200 | #1 |
    | `/app/apprenants` | redirect `/login` | 200 | #3 |
    | `/app/dossiers-opco` | redirect `/login` | 200 | #2 |
    | `/app/budget-agefice` | redirect `/login` | 200 | #2 |
    | `/app/factures` | redirect `/login` | 200 | #2 |
    | `/app/inscriptions` | redirect `/login` | 200 | #4 |
    | `/preinscription/<token valide>` | **200 public** (formulaire visible) | idem | #4 |
    | `/preinscription/token-bidon-xyz` | refus propre (message erreur/404, pas de formulaire, pas de 500) | idem | #4 |
    Mécanique :
    - Tests anonymes tagués `@anon` (projet `anonymous`, sans storageState) : `await page.goto(route); await expect(page).toHaveURL(/\/login/);`
    - Tests authentifiés (projet `authenticated`) : `const resp = await page.goto(route); expect(resp?.status()).toBe(200); await expect(page).not.toHaveURL(/\/login/);` + assert un élément de contenu (h1/table) pour éviter un 200 vide.
    - Token valide : dans un `beforeAll`, créer via Prisma une row `PreEnrollment` minimale (lire le model dans schema.prisma : champs requis + champ token), avec valeurs préfixées `E2E-` sur les champs identifiants et un token `e2e-smoke-<Date.now()>` ; la supprimer en `afterAll`. RESPECTER le tenantId (multi-tenant).
    - Bandeau staging : 1 assert que `/login` contient le texte `STAGING` (preuve APP-01 runtime).
    - ⚠ rate-limit D-13 : le projet anonymous ne visite /preinscription que 2 fois (token valide + bidon) — sous la fenêtre 30/60s, pas de faux 429.
    Exécuter : `STAGING_BASE_URL=https://<domaine> pnpm --filter @qualiof/web exec dotenv -e ../../.env -- playwright test e2e/auth.setup.ts e2e/smoke-routes.spec.ts` → tout vert. Coller la sortie dans le SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -c "preinscription" apps/web/e2e/smoke-routes.spec.ts | awk '{exit ($1>=2)?0:1}' && ! grep -q "'/p/" apps/web/e2e/smoke-routes.spec.ts && grep -q "@anon" apps/web/e2e/smoke-routes.spec.ts && grep -q "inscriptions" apps/web/e2e/smoke-routes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le spec référence `/preinscription/` (≥2 occurrences) et JAMAIS `/p/[token]`
    - Les 9 routes protégées + /login + les 2 cas publics sont présents (11+ tests)
    - Run Playwright contre le staging : exit 0, sortie collée dans le SUMMARY (nombre de tests passés)
    - La row PreEnrollment de test est supprimée en afterAll (vérif : `prisma.preEnrollment.count` sur le token = 0 après run)
    - Assert bandeau STAGING vert (preuve APP-01)
  </acceptance_criteria>
  <done>TEST-02 prouvé contre le staging déployé : auth gate + 200 sur les pages clés des 4 piliers + form public sur la vraie route</done>
</task>

<task type="auto">
  <name>Task 3: Re-validation PENDING 18-SMOKE — upload 10 Mo direct-to-storage sans 413</name>
  <files>apps/web/e2e/upload-preenrollment.spec.ts</files>
  <read_first>apps/web/src/components/direct-upload-field.tsx, apps/web/src/app/preinscription/[token]/page.tsx, .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md</read_first>
  <action>
    Créer `apps/web/e2e/upload-preenrollment.spec.ts` (projet authenticated ou anonymous selon la page — le form public est anonyme, utiliser un `test.describe` sans storageState si nécessaire) :
    1. `beforeAll` : créer une PreEnrollment de test via Prisma (même mécanique E2E- que Task 2).
    2. Générer un fichier JPEG de ~10 Mo dans le tmp du test : buffer aléatoire préfixé des magic bytes JPEG `FF D8 FF E0` + suffixe `FF D9` (le composant vérifie type/taille, pas le contenu image ; l'OCR réel n'est PAS l'objet de ce test — il est couvert par 20-05/21-06).
    3. Ouvrir `/preinscription/<token>`, localiser le champ CNI (`direct-upload-field`), `setInputFiles` avec le fichier 10 Mo.
    4. Intercepter le réseau (`page.on('response')` / `page.waitForResponse`) et ASSERTER :
       - une requête `PUT` vers un host `*.supabase.co` (path contient `/storage/v1/object/upload/sign/preinscriptions/`) répond **200**
       - AUCUNE réponse **413** sur toute la session
       - AUCUNE requête vers le domaine Vercel ne porte un body ≥ 4 Mo (le fichier ne transite pas par Next — preuve anti-413 structurelle)
    5. `afterAll` : supprimer la PreEnrollment de test + l'objet storage uploadé (clé sous le token de test, bucket preinscriptions, via supabase-js service role ou storage.ts).
    6. Exécuter contre le staging : `STAGING_BASE_URL=https://<domaine> pnpm --filter @qualiof/web exec dotenv -e ../../.env -- playwright test e2e/upload-preenrollment.spec.ts` → vert.
    7. Consigner dans le SUMMARY le sort des 3 PENDING 18-SMOKE : ① 413 sur Vercel réel = **RE-VALIDÉ** (ce test) ; ② retry coupure réseau mobile réelle = MANUEL/reporté (non simulable de façon fiable — noter comme manual-only) ; ③ expiration signed URL 11 min temps réel = déjà couverte par le mécanisme JWT exp prouvé en 18-04, non re-testée (noter).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "supabase.co" apps/web/e2e/upload-preenrollment.spec.ts && grep -q "413" apps/web/e2e/upload-preenrollment.spec.ts && grep -qi "10" apps/web/e2e/upload-preenrollment.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le spec asserte : PUT supabase.co 200, zéro 413, fichier ~10 Mo (10 * 1024 * 1024 octets ±)
    - Run contre staging : exit 0
    - Données et objet storage de test nettoyés (count = 0 post-run)
    - SUMMARY documente le statut des 3 PENDING 18-SMOKE (1 re-validé, 2 tracés)
  </acceptance_criteria>
  <done>Le cap 4,5 Mo Vercel est structurellement contourné EN PROD RÉELLE — le pilier #4 survit au déploiement (Pitfall 10 fermé)</done>
</task>

</tasks>

<verification>
- `STAGING_BASE_URL=… playwright test e2e/auth.setup.ts e2e/smoke-routes.spec.ts e2e/upload-preenrollment.spec.ts` → exit 0 contre le staging
- `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0 ; suite vitest inchangée (e2e/ hors glob vitest)
- Aucune donnée E2E- résiduelle en base après les runs
</verification>

<success_criteria>
- TEST-02 : ~10 routes des 4 piliers vérifiées (redirect anonyme + 200 authentifié)
- APP-02 : login réel sur le domaine final (storageState = preuve cookie fonctionnel), form public accessible sur `/preinscription/[token]`
- PENDING 18-SMOKE ① (413) re-validé sur Vercel déployé
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-vercel-filet-ci-tests/21-05-SUMMARY.md`
</output>
