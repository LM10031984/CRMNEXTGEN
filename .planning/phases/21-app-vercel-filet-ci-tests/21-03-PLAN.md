---
phase: 21-app-vercel-filet-ci-tests
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "Une PR vers main ne peut PAS être mergée si lint + tsc + vitest ne sont pas verts (branch protection, contexte `test`)"
    - "La suite vitest complète (1171+ tests dont les 2 tests d'intégration DB) tourne verte sur un runner GitHub nu (service Postgres 16 + .env CI)"
    - "Sur push main, `prisma migrate deploy` s'exécute contre le Supabase cloud via secrets chiffrés (D-09)"
    - "L'image Docker worker compile en CI (preuve build, sans push)"
    - "main contient tout cloud-migration (gardes staging 21-01 incluses) — la branche staging-vercel obsolète n'est PAS mergée"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Gate PR : lint + tsc + vitest avec service Postgres + build image worker"
      contains: "postgres:16"
    - path: ".github/workflows/deploy.yml"
      provides: "prisma migrate deploy sur push main (secrets chiffrés)"
      contains: "migrate deploy"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/db schema"
      via: "prisma generate + db push sur la base qualiof_test du service container"
      pattern: "db push --skip-generate"
    - from: ".github/workflows/deploy.yml"
      to: "Supabase DIRECT_URL :5432"
      via: "secrets.DIRECT_URL (GitHub encrypted secret)"
      pattern: "secrets.DIRECT_URL"
    - from: "branch protection main"
      to: "job `test` de ci.yml"
      via: "required_status_checks.contexts"
      pattern: "\"contexts\""
---

<objective>
Monter le filet CI complet (CI-01, D-07/D-08/D-09) : workflows GitHub Actions (gate PR lint+tsc+vitest avec service Postgres, build Docker worker, migrate deploy sur main), merge de `cloud-migration` dans `main`, secrets chiffrés, branch protection, et PR témoin qui prouve le gate.

Purpose: aucune bascule prod sans filet vert. Le gate PR reste rapide (D-10 : PAS de Playwright ici). D-08 : `shared-template.test.ts` passe DÉJÀ 11/11 (Pitfall 2 RESEARCH, vérifié par exécution 2026-07-06) — la tâche est de PROUVER la suite 100 % verte en CI, pas de corriger un test. Le vrai risque est `dedupe.merge.test.ts` qui THROW à la collection sans `TEST_DATABASE_URL` (Pitfall 3).
Output: CI verte sur main, protection active, PR témoin mergée via `gh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-app-vercel-filet-ci-tests/21-CONTEXT.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-RESEARCH.md
@apps/web/scripts/__tests__/dedupe.merge.test.ts
@package.json

<interfaces>
<!-- Points durs vérifiés (RESEARCH 2026-07-06) -->
- `packageManager: pnpm@10.33.2` dans package.json racine → `pnpm/action-setup@v4` le lit seul (ne PAS fixer `version:`).
- `next lint` ET vitest chargent `next.config.mjs` → `await import('@qualiof/shared/env')` → `createEnv()` fail-loud : le runner DOIT avoir un `.env` racine avec valeurs factices VALIDES (`AUTH_SECRET` ≥ 32 chars, URLs bien formées).
- `apps/web/scripts/__tests__/dedupe.merge.test.ts` (ligne ~30) : `throw new Error('REFUS: …')` à la COLLECTION si `TEST_DATABASE_URL` absent OU si le nom de base ne finit pas par `_test`. C'est un test d'intégration réel (écrit/purge des rows).
- `prisma generate` requis AVANT tsc/vitest (types PrismaClient) — `packages/db` a désormais `postinstall: prisma generate` (plan 21-01), donc `pnpm install` le déclenche ; garder quand même l'étape explicite en fallback documentaire.
- Leçon memory sandbox : `prisma db push --skip-generate` (jamais `migrate dev` en environnement non interactif).
- Scripts racine : `pnpm lint` = turbo lint (next lint + tsc packages), `pnpm test` = turbo test (vitest web + db).
- Dockerfile worker : `docker/worker/Dockerfile`, context = racine repo (turbo prune).
- Repo : `LM10031984/CRMNEXTGEN`, `gh` authentifié. Branches : `cloud-migration` (courante, en avance), `main` (en retard), `staging-vercel` (OBSOLÈTE — 94 commits derrière, NE JAMAIS merger depuis elle).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire ci.yml (gate PR + service Postgres + image worker) et deploy.yml (migrate deploy)</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy.yml</files>
  <read_first>package.json, apps/web/scripts/__tests__/dedupe.merge.test.ts, docker/worker/Dockerfile, packages/db/package.json</read_first>
  <action>
    Créer `.github/workflows/ci.yml` avec exactement cette structure (adapter uniquement si un chemin vérifié dans read_first diffère) :
    ```yaml
    name: CI
    on:
      pull_request:
      push:
        branches: [main]
    jobs:
      test:
        runs-on: ubuntu-latest
        services:
          postgres:
            image: postgres:16
            env:
              POSTGRES_USER: qualiof
              POSTGRES_PASSWORD: qualiof
              POSTGRES_DB: qualiof_test
            ports: ['5432:5432']
            options: >-
              --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
          - uses: actions/setup-node@v4
            with:
              node-version: 20
              cache: pnpm
          - run: pnpm install --frozen-lockfile
          - name: Write CI .env (env.ts est fail-loud — valeurs factices VALIDES)
            run: |
              cat > .env <<'EOF'
              DATABASE_URL=postgresql://qualiof:qualiof@localhost:5432/qualiof_test
              DIRECT_URL=postgresql://qualiof:qualiof@localhost:5432/qualiof_test
              TEST_DATABASE_URL=postgresql://qualiof:qualiof@localhost:5432/qualiof_test
              AUTH_SECRET=ci-secret-0123456789abcdef0123456789abcdef
              STORAGE_PROVIDER=minio
              EOF
          - run: pnpm --filter @qualiof/db exec prisma generate
          - name: Push schema to test DB (tests intégration dedupe/match-treso)
            run: pnpm --filter @qualiof/db exec dotenv -e ../../.env -- prisma db push --skip-generate
          - run: pnpm lint
          - run: pnpm --filter @qualiof/web exec tsc --noEmit
          - run: pnpm test
      worker-image:
        # CI-01 « build Docker worker » — preuve que l'image compile, PAS de push.
        # Sur push main uniquement : garde le gate PR rapide (D-10, Open Q2 RESEARCH).
        if: github.event_name == 'push'
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: docker/build-push-action@v6
            with:
              context: .
              file: docker/worker/Dockerfile
              push: false
    ```
    Créer `.github/workflows/deploy.yml` :
    ```yaml
    name: Deploy migrations
    on:
      push:
        branches: [main]
    jobs:
      migrate:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v4
          - uses: actions/setup-node@v4
            with:
              node-version: 20
              cache: pnpm
          - run: pnpm install --frozen-lockfile
          - run: pnpm --filter @qualiof/db exec prisma migrate deploy
            env:
              DATABASE_URL: ${{ secrets.DATABASE_URL }}
              DIRECT_URL: ${{ secrets.DIRECT_URL }}
    ```
    (schema.prisma lit `url` + `directUrl` — les DEUX secrets sont requis ; la migration passe par directUrl :5432. Race migration/deploy Vercel acceptée à cette échelle : baseline `0_init` déjà appliquée, migrations futures additives par convention — RESEARCH Pattern 5.)
    Committer sur `cloud-migration`.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .github/workflows/ci.yml && test -f .github/workflows/deploy.yml && grep -q "postgres:16" .github/workflows/ci.yml && grep -q "TEST_DATABASE_URL" .github/workflows/ci.yml && grep -q "db push --skip-generate" .github/workflows/ci.yml && grep -q "docker/worker/Dockerfile" .github/workflows/ci.yml && grep -q "migrate deploy" .github/workflows/deploy.yml && grep -q "secrets.DIRECT_URL" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml` contient : `pull_request`, service `postgres:16` avec `POSTGRES_DB: qualiof_test`, heredoc `.env` avec les 5 clés (DATABASE_URL, DIRECT_URL, TEST_DATABASE_URL, AUTH_SECRET ≥32 chars, STORAGE_PROVIDER=minio), `prisma generate`, `db push --skip-generate`, `pnpm lint`, `tsc --noEmit`, `pnpm test`
    - job `worker-image` présent avec `if: github.event_name == 'push'` et `push: false`
    - `deploy.yml` contient `branches: [main]`, `prisma migrate deploy`, `secrets.DATABASE_URL` ET `secrets.DIRECT_URL`
    - Aucun secret en clair dans les 2 workflows (uniquement valeurs factices CI + `${{ secrets.* }}`)
  </acceptance_criteria>
  <done>Les 2 workflows sont committés — la preuve verte arrive au push (Task 2)</done>
</task>

<task type="auto">
  <name>Task 2: Merger cloud-migration → main, secrets GitHub, CI verte, branch protection</name>
  <files>(opérations git/gh — aucun fichier de code)</files>
  <read_first>.github/workflows/ci.yml, .env</read_first>
  <action>
    ORDRE STRICT (Pattern 6 RESEARCH : merger AVANT de protéger, sinon la protection bloque le merge initial) :
    1. Vérifier l'arbre propre sur `cloud-migration` (les commits 21-01 + Task 1 sont poussés). `git push origin cloud-migration`.
    2. Poser les secrets chiffrés AVANT le merge (deploy.yml se déclenche au push main) — valeurs lues depuis `.env` racine local (qui pointe le cloud Supabase, Phase 19) :
       `grep '^DATABASE_URL=' .env | cut -d= -f2- | gh secret set DATABASE_URL --repo LM10031984/CRMNEXTGEN --body -` (idem pour `DIRECT_URL`). Ne JAMAIS échoer les valeurs dans les logs.
    3. Merger : `git fetch origin && git checkout main && git pull origin main && git merge cloud-migration --no-ff -m "merge: cloud-migration -> main (v6 phases 17-21, gardes staging + CI)"` puis `git push origin main`. NE RIEN merger depuis `staging-vercel` (obsolète, 94 commits derrière).
    4. Surveiller les runs : `gh run list --repo LM10031984/CRMNEXTGEN --branch main --limit 5` puis `gh run watch <id>` sur le run CI ET le run Deploy migrations. Attendus : job `test` vert (1171+ tests), `worker-image` vert (build only), `migrate` vert avec « No pending migrations to apply. » dans les logs.
    5. Si le run CI échoue : lire les logs (`gh run view <id> --log-failed`), corriger sur `cloud-migration` (fichier workflow ou test), re-merger — NE PAS by-passer.
    6. Une fois main verte, activer la protection :
       ```bash
       gh api -X PUT repos/LM10031984/CRMNEXTGEN/branches/main/protection --input - <<'EOF'
       {
         "required_status_checks": { "strict": false, "contexts": ["test"] },
         "enforce_admins": false,
         "required_pull_request_reviews": null,
         "restrictions": null,
         "allow_force_pushes": false,
         "allow_deletions": false
       }
       EOF
       ```
       (`contexts: ["test"]` = nom du job gate ; pas de review humaine requise — Laurent est seul, Claude ouvre/merge les PR via gh, D-07.)
    7. Revenir sur `git checkout cloud-migration` (le travail des plans suivants continue sur cette branche).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && gh run list --repo LM10031984/CRMNEXTGEN --branch main --limit 3 --json conclusion,name | grep -q '"conclusion":"success"' && gh api repos/LM10031984/CRMNEXTGEN/branches/main/protection --jq '.required_status_checks.contexts' | grep -q '"test"'</automated>
  </verify>
  <acceptance_criteria>
    - `gh secret list --repo LM10031984/CRMNEXTGEN` liste `DATABASE_URL` et `DIRECT_URL`
    - Dernier run CI sur main : conclusion `success` (job test + worker-image)
    - Run « Deploy migrations » sur main : success, logs contiennent `No pending migrations` OU un apply propre
    - `gh api .../branches/main/protection` retourne `contexts` contenant `test`, `allow_force_pushes.enabled: false`
    - `git log main --oneline -1` montre le commit de merge incluant les gardes 21-01
  </acceptance_criteria>
  <done>main = source de vérité protégée, CI 100 % verte, migrations auto sur merge (D-09)</done>
</task>

<task type="auto">
  <name>Task 3: PR témoin — prouver le gate de bout en bout</name>
  <files>(branche jetable ci/pr-temoin — modification triviale)</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
    1. Depuis `main` à jour : `git checkout -b ci/pr-temoin`, modification triviale et réversible (ex. ligne de doc dans `.env.example` : commentaire « CI gate actif depuis 2026-07-06 (Phase 21) »), commit, push.
    2. `gh pr create --repo LM10031984/CRMNEXTGEN --base main --head ci/pr-temoin --title "ci: PR témoin gate Phase 21" --body "Preuve CI-01 : le merge doit être bloqué tant que le job test n'est pas vert."`
    3. AVANT que la CI passe : `gh pr view --json mergeStateStatus` doit montrer `BLOCKED` (preuve que la protection mord).
    4. Attendre la CI verte (`gh pr checks --watch`), puis merger : `gh pr merge --squash --delete-branch`.
    5. Consigner dans le SUMMARY : numéro de PR, capture du statut BLOCKED→MERGEABLE, id du run vert.
    6. Revenir sur `cloud-migration` et `git merge main` (récupérer le commit témoin pour éviter la divergence).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && gh pr list --repo LM10031984/CRMNEXTGEN --state merged --search "PR témoin gate Phase 21" --json number,title | grep -q "témoin"</automated>
  </verify>
  <acceptance_criteria>
    - Une PR titrée « ci: PR témoin gate Phase 21 » existe à l'état merged
    - Le SUMMARY documente l'état BLOCKED observé avant CI verte (preuve du gate)
    - La branche `ci/pr-temoin` est supprimée
    - `cloud-migration` contient le merge de main (pas de divergence)
  </acceptance_criteria>
  <done>CI-01 prouvé de bout en bout : gate PR actif, merge impossible sans CI verte, flux D-07 opérationnel via gh</done>
</task>

</tasks>

<verification>
- `gh run list --branch main` : CI + Deploy migrations verts
- `gh api repos/LM10031984/CRMNEXTGEN/branches/main/protection` : contexte `test` requis
- PR témoin mergée après blocage observé
- `shared-template.test.ts` vert EN CI (D-08 confirmé de facto — Pitfall 2 : il passait déjà 11/11 en local)
</verification>

<success_criteria>
- CI-01 complet : lint + tsc + vitest en gate PR, build Docker worker prouvé, `prisma migrate deploy` en étape de déploiement via secrets chiffrés
- D-07 : main protégée, flux PR via gh opérationnel
- D-09 : merge main → Vercel/Railway redéploient (git integration native) + migrations GitHub Actions
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-vercel-filet-ci-tests/21-03-SUMMARY.md`
</output>
