---
phase: 21-app-vercel-filet-ci-tests
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md
autonomous: false
requirements: [APP-01, APP-02, APP-03]
user_setup:
  - service: vercel
    why: "Hébergement app Next.js (APP-01) — création projet, env, domaine, WAF = dashboard uniquement (pattern Laurent, Vercel CLI explicitement exclu par RESEARCH)"
    env_vars:
      - name: "~35 clés (checklist complète dans 21-DEPLOY-VERCEL.md)"
        source: "Valeurs .env local + Railway dashboard (URLs doc-engines publiques) + NEXT_PUBLIC_APP_ENV=staging"
    dashboard_config:
      - task: "Créer le projet (import LM10031984/CRMNEXTGEN, Root Directory apps/web, branche main), vérifier plan Pro actif"
        location: "Vercel Dashboard -> Add New Project"
      - task: "Règle WAF rate-limit /preinscription (D-13)"
        location: "Vercel Dashboard -> Firewall -> New Rule"
      - task: "Brancher le sous-domaine final (D-04, à confirmer avec Laurent, ex. app.start-academy.fr) via CNAME chez le registrar"
        location: "Vercel Dashboard -> Project -> Domains + zone DNS start-academy.fr"
must_haves:
  truths:
    - "L'app répond en HTTPS sur le domaine final, servie depuis cdg1 (x-vercel-id), build vert"
    - "L'env Vercel est complète AVANT le 1er build (next.config.mjs fail-loud) — dont GOTENBERG_URL/WEASYPRINT_URL publics Railway + DOC_ENGINE_TOKEN (APP-03 = câblage env) et les 22 OF_*"
    - "NEXT_PUBLIC_APP_ENV=staging actif : bandeau visible sur /login, MAIL_DRY_RUN=true posé"
    - "Le rate-limit par IP répond 429 sur rafale vers /preinscription (D-13)"
    - "Aucune clé morte copiée (REDIS_URL, DOC_ENGINE_URL, SMARTOF_*, YOUSIGN_*, RESEND_API_KEY) et pas de SMTP_HOST (dry-run garanti)"
  artifacts:
    - path: ".planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md"
      provides: "Runbook dashboard non-technicien + section RÉSULTATS avec evidence curl"
      min_lines: 80
  key_links:
    - from: "env Vercel GOTENBERG_URL/WEASYPRINT_URL"
      to: "services publics Railway (proxy Caddy + WeasyPrint, plan 20-05)"
      via: "pdf-render.ts authHeaders() Bearer DOC_ENGINE_TOKEN"
      pattern: "GOTENBERG_URL"
    - from: "domaine final (CNAME)"
      to: "projet Vercel"
      via: "DNS registrar start-academy.fr"
      pattern: "cname.vercel-dns.com"
---

<objective>
Déployer l'app sur Vercel Pro EU (cdg1) en mode staging gardé, sur le domaine final : runbook dashboard exhaustif (checklist env ~35 clés, WAF rate-limit D-13, domaine D-04), exécution gatée avec Laurent, puis vérification runtime curl (région, bandeau staging, redirect auth, 429 rate-limit).

Purpose: APP-01 (déploiement + staging dégelé), APP-02 (domaine réel HTTPS → cookies secure), APP-03 (câblage env doc-engines — `pdf-render.ts` porte déjà le Bearer conditionnel, il ne manque QUE les 3 variables). La preuve login/PDF complète arrive aux plans 21-05/21-06 (Playwright).
Output: staging accessible sur le domaine final, evidence consignée dans le runbook.

⚠ PRÉREQUIS DUR : plan 20-05 déployé (URLs publiques Railway du proxy Caddy Gotenberg + WeasyPrint disponibles, relevé 24h attendu 2026-07-07). Si 20-05 n'est pas terminé, STOPPER ce plan et le signaler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-app-vercel-filet-ci-tests/21-CONTEXT.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-RESEARCH.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md
@.env.example

<interfaces>
<!-- Vérifié 2026-07-06 -->
From apps/web/src/lib/pdf-render.ts (RIEN à coder pour APP-03 — env pur) :
```typescript
const GOTENBERG_URL = sharedEnv.GOTENBERG_URL;    // → domaine PUBLIC du proxy Caddy Railway (Gotenberg reste privé)
const WEASYPRINT_URL = sharedEnv.WEASYPRINT_URL;  // → service WeasyPrint public Railway
function authHeaders() { const token = sharedEnv.DOC_ENGINE_TOKEN; return token ? { Authorization: `Bearer ${token}` } : {}; }
```
Clés env.ts SANS défaut (build Vercel échoue si absentes — Pitfall 4) : `DATABASE_URL`, `DIRECT_URL`, `AUTH_SECRET` (≥32), `WEASYPRINT_URL`, `STORAGE_PROVIDER`.
Les 22 vars OF_* (footer PDF, lues par of-config.ts — mêmes valeurs que le worker Railway, memory 2026-07-06) :
OF_NAME, OF_SIRET, OF_RNQ, OF_TVA_INTRA, OF_ADDRESS_STREET, OF_ADDRESS_CP, OF_ADDRESS_VILLE, OF_EMAIL, OF_PHONE, OF_IBAN, OF_BIC,
OF_CONTACT_CIVILITE, OF_CONTACT_NOM, OF_CONTACT_PRENOM, OF_CONTACT_EMAIL, OF_CONTACT_PHONE,
OF_RESP_CIVILITE, OF_RESP_NOM, OF_RESP_PRENOM, OF_RESP_TITRE, OF_RESP_EMAIL, OF_RESP_PHONE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire le runbook 21-DEPLOY-VERCEL.md (dashboard, non-technicien, checklist env exhaustive)</name>
  <files>.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</files>
  <read_first>.env, .env.example, .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md, apps/web/vercel.json</read_first>
  <action>
    Écrire un runbook calqué sur `20-DEPLOY.md` (dashboard, zéro CLI, pattern Laurent) avec ces sections :
    1. **Prérequis** : plan Vercel **Pro** actif (maxDuration 800s + WAF rate-limit en dépendent — vérifier AVANT) ; plan 20-05 terminé → relever dans le dashboard Railway les domaines publics du **proxy Caddy Gotenberg** et de **WeasyPrint**, et le `DOC_ENGINE_TOKEN` (même secret que Railway).
    2. **Création projet** : Add New Project → import `LM10031984/CRMNEXTGEN` → **Root Directory = `apps/web`** (Pitfall 6 — Vercel détecte pnpm workspace + Turborepo et installe depuis la racine) → Framework Next.js → Production Branch = `main`. NE PAS déclencher le build avant l'étape env (Pitfall 4 : `next.config.mjs` fail-loud). Vérifier Project Settings → Functions region = `cdg1` (le `vercel.json` du repo force `regions: ["cdg1"]` ; défaut US = iad1, checklist 17-REGIONS.md).
    3. **Tableau env COMPLET (~35 clés)** — colonnes Nom / Valeur ou Source / Type (Sensitive pour les secrets) :
       - Base : `DATABASE_URL` (pooler :6543 `?pgbouncer=true&connection_limit=1`), `DIRECT_URL` (:5432) — valeurs du `.env` local
       - Auth : `AUTH_SECRET`, `SESSION_LIFETIME`
       - Storage : `STORAGE_PROVIDER=supabase`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`
       - Doc-engines (APP-03) : `GOTENBERG_URL` = https du proxy Caddy Railway public, `WEASYPRINT_URL` = https du service WeasyPrint Railway public, `DOC_ENGINE_TOKEN` (Sensitive)
       - IA : `AI_PROVIDER=openrouter`, `OPENROUTER_API_KEY` (Sensitive), `OPENROUTER_MODEL_FAST`, `OPENROUTER_MODEL_QUALITY`, `OPENROUTER_MODEL_VISION`, `OPENROUTER_BASE_URL`, `OPENROUTER_APP_NAME`, `OPENROUTER_SITE_URL` (= https://domaine staging)
       - App : `NEXT_PUBLIC_APP_URL` (= https://domaine), `NEXT_PUBLIC_APP_NAME`, `NEXT_PUBLIC_APP_ENV=staging`, `TENANT_DEFAULT_NAME`, `TENANT_DEFAULT_NUM_DA`, `TENANT_DEFAULT_SIRET`, `LOG_LEVEL=info`
       - Garde D-02 : `MAIL_DRY_RUN=true` (explicite, plus sûr que SMTP_HOST vide) — et NE PAS poser SMTP_HOST/SMTP_PASS
       - Les **22 `OF_*`** (liste complète du bloc interfaces, valeurs du `.env` local)
       - **NE PAS COPIER** (clés mortes/locales) : `REDIS_URL`, `DOC_ENGINE_URL`, `SMARTOF_*`, `YOUSIGN_*`, `RESEND_API_KEY`, `OLLAMA_*` (défauts OK), `S3_*` (MinIO local, inutiles en mode supabase), `TEST_DATABASE_URL`, `QUALIOPI_GEN_*`
    4. **1er deploy** : déclencher, attendre build vert. Dépannage : « @prisma/client did not initialize » → vérifier le postinstall `packages/db` (plan 21-01) ; erreur `createEnv` → clé manquante, relire le tableau.
    5. **Deployment Protection (Open Q4)** : Settings → Deployment Protection — la PROD du projet doit être publique (sinon smoke/E2E KO) ; si Vercel Authentication reste active sur les previews c'est OK ; noter le « Protection Bypass for Automation » secret si activé (utilisé par Playwright en `x-vercel-protection-bypass`).
    6. **Domaine final (D-04)** : demander à Laurent le sous-domaine exact (proposer `app.start-academy.fr`) → Project → Domains → Add → CNAME `cname.vercel-dns.com` dans la zone DNS start-academy.fr. Ne PAS communiquer le domaine à l'équipe (Phase 22).
    7. **WAF rate-limit (D-13)** : Firewall → New Rule : IF path **starts with `/preinscription`** (⚠ la vraie route — PAS `/p/`, Pitfall 1) THEN Rate Limit **fixed window, clé IP, 30 requêtes / 60 s**, action **Deny (429)**. Couvre GET page + POST server actions (même path). Le dialog pricing usage-based s'affiche à la 1ʳᵉ règle : valider le montant à l'écran (attendu négligeable) ; si refusé → fallback documenté = table Postgres fixed-window dans `preinscription-public.ts` (PAS de Redis, PAS de middleware in-memory).
    8. **Crons : AUCUN** (Pitfall 8 — jamais `closure-worker` ; reminders tranchés Phase 22).
    9. **Section RÉSULTATS DE VALIDATION** (vide, à remplir Task 3) : tableau Étape | Commande | Attendu | Résultat | Date.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && grep -q "apps/web" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && grep -q "cdg1" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && grep -q "NEXT_PUBLIC_APP_ENV=staging" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && grep -q "MAIL_DRY_RUN=true" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && test $(grep -c "OF_" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md) -ge 22 && grep -q "/preinscription" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && ! grep -q "REDIS_URL=" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</automated>
  </verify>
  <acceptance_criteria>
    - Le runbook contient : Root Directory `apps/web`, `cdg1`, `NEXT_PUBLIC_APP_ENV=staging`, `MAIL_DRY_RUN=true`, `GOTENBERG_URL`, `WEASYPRINT_URL`, `DOC_ENGINE_TOKEN`, les 22 noms `OF_*`, la règle WAF `/preinscription` 30/60s
    - Section « NE PAS COPIER » listant REDIS_URL, DOC_ENGINE_URL, SMARTOF_, YOUSIGN_, RESEND_API_KEY, S3_, TEST_DATABASE_URL
    - AUCUNE valeur de secret en clair dans le runbook (colonnes Source uniquement : « .env local », « Railway dashboard »)
    - Section RÉSULTATS DE VALIDATION présente avec tableau vide
  </acceptance_criteria>
  <done>Laurent (ou Claude guidant Laurent) peut exécuter le déploiement de bout en bout sans CLI et sans oubli d'env</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Exécuter le runbook — création projet Vercel + env + domaine + WAF</name>
  <files>(actions dashboard Vercel + zone DNS — aucun fichier repo)</files>
  <read_first>.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</read_first>
  <action>
    Exécuter `21-DEPLOY-VERCEL.md` section par section AVEC Laurent (Claude guide, Laurent clique — dashboard uniquement, pattern verrouillé) : vérif plan Pro + relevé URLs Railway/DOC_ENGINE_TOKEN → création projet (Root Directory apps/web, branche main, SANS builder avant l'env) → saisie des ~35 variables (Sensitive pour les secrets) → deploy build vert → confirmation du sous-domaine avec Laurent + CNAME → règle WAF /preinscription 30 req/60s Deny (valider le dialog pricing) → check Deployment Protection (prod publique). En cas d'échec build : lire les logs Vercel, croiser avec la section Dépannage du runbook.
  </action>
  <verify>Laurent (ou Claude sur écran partagé) confirme : URL staging accessible + build vert + domaine attaché + règle WAF publiée</verify>
  <acceptance_criteria>
    - URL staging communiquée (https://…) et joignable
    - Build Vercel : status Ready (vert)
    - Domaine final listé dans Project -> Domains (status Valid)
    - Règle WAF rate-limit publiée (visible dans Firewall)
  </acceptance_criteria>
  <done>Projet Vercel déployé en staging gardé sur le domaine final — vérification runtime automatisée en Task 3</done>
  <what-built>Runbook complet 21-DEPLOY-VERCEL.md (Task 1) : projet, ~35 variables, domaine, règle WAF. Claude a automatisé tout ce qui est automatisable (code mergé dans main par 21-03, vercel.json, postinstall) — il ne reste QUE les actions dashboard Vercel + DNS registrar, sans CLI (pattern verrouillé RESEARCH/CONTEXT).</what-built>
  <how-to-verify>
    Suivre `21-DEPLOY-VERCEL.md` section par section, dans l'ordre :
    1. Vérifier plan Pro actif, relever les URLs publiques Railway (proxy Caddy + WeasyPrint) + DOC_ENGINE_TOKEN
    2. Créer le projet (Root Directory apps/web, branche main) SANS builder
    3. Saisir les ~35 variables du tableau (type Sensitive pour les secrets)
    4. Déclencher le deploy → build vert attendu
    5. Confirmer le sous-domaine avec Laurent (ex. app.start-academy.fr) et poser le CNAME
    6. Créer la règle WAF rate-limit /preinscription (valider le dialog pricing)
    7. Vérifier Deployment Protection : prod publique
  </how-to-verify>
  <resume-signal>Donner l'URL staging finale (https://…) + « build vert » — ou décrire le blocage rencontré (étape + message d'erreur exact)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Vérification runtime curl + evidence dans le runbook</name>
  <files>.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</files>
  <read_first>.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</read_first>
  <action>
    Avec `DOMAIN` = URL staging donnée au checkpoint, exécuter et consigner CHAQUE résultat brut dans la section RÉSULTATS DE VALIDATION :
    1. Région + HTTPS : `curl -sI https://$DOMAIN/login` → HTTP 200 ; header `x-vercel-id` contient `cdg1` (préfixe région).
    2. Bandeau staging (APP-01) : `curl -s https://$DOMAIN/login | grep -c "STAGING"` ≥ 1.
    3. Redirect auth (APP-02) : `curl -s -o /dev/null -w "%{http_code}" https://$DOMAIN/app` → 307 (redirect /login) ; `curl -sI https://$DOMAIN/app | grep -i location` contient `/login`.
    4. Route publique (Pitfall 1 — la VRAIE route) : `curl -s -o /dev/null -w "%{http_code}" https://$DOMAIN/preinscription/token-bidon-e2e` → 200 ou 404 (refus propre), JAMAIS 500.
    5. Rate-limit D-13 : `for i in $(seq 1 40); do curl -s -o /dev/null -w "%{http_code}\n" https://$DOMAIN/preinscription/rl-probe-$i; done | sort | uniq -c` → au moins un `429` après ~30 requêtes. ⚠ Attendre 2 min après le test (fenêtre 60s) avant tout autre appel /preinscription.
    6. Cookie flags (APP-02, preuve partielle avant Playwright) : `curl -sI https://$DOMAIN/login` — noter que le set-cookie Lucia complet n'apparaît qu'au POST login (preuve finale = plan 21-05 auth.setup) ; consigner « secure garanti par NODE_ENV=production Vercel (Pitfall 7), sameSite lax explicite (21-01) ».
    7. Remplir le tableau RÉSULTATS (Étape | Commande | Attendu | Résultat | Date 2026-07-XX) + coller les sorties brutes en annexe.
    Si un check échoue : diagnostiquer (env manquante → tableau runbook ; 429 absent → règle WAF pas publiée), corriger via checkpoint si dashboard requis, re-tester.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "RÉSULTATS DE VALIDATION" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && grep -q "429" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md && grep -q "x-vercel-id" .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</automated>
  </verify>
  <acceptance_criteria>
    - Section RÉSULTATS remplie : 200 sur /login, `cdg1` dans x-vercel-id, bandeau STAGING présent, 307→/login sur /app, pas de 500 sur token bidon, ≥1 réponse 429 observée sur la rafale
    - Sorties curl brutes collées en annexe du runbook (evidence datée)
    - Tout échec résiduel est listé avec cause + action (pas de faux vert)
  </acceptance_criteria>
  <done>Staging déployé et vérifié : région EU, gardes actives, rate-limit vivant, domaine final branché — prêt pour Playwright (21-05)</done>
</task>

</tasks>

<verification>
- `curl -sI https://<domaine>/login` → 200 + x-vercel-id cdg1
- Bandeau STAGING dans le HTML de /login
- /app → 307 /login ; /preinscription/token-bidon → refus propre (pas de 500)
- Rafale /preinscription → 429 observé
- Runbook 21-DEPLOY-VERCEL.md avec RÉSULTATS remplis
</verification>

<success_criteria>
- APP-01 : app déployée cdg1, staging dégelé (flag actif, filigrane armé, vercel.json en prod)
- APP-02 (infra) : domaine final HTTPS actif — preuve login complète au 21-05
- APP-03 (câblage) : GOTENBERG_URL/WEASYPRINT_URL/DOC_ENGINE_TOKEN posés — preuve PDF au 21-06
- D-13 : rate-limit par IP actif sur /preinscription
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-vercel-filet-ci-tests/21-04-SUMMARY.md`
</output>
