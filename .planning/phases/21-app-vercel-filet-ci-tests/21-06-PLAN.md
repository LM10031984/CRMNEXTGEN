---
phase: 21-app-vercel-filet-ci-tests
plan: 06
type: execute
wave: 5
depends_on: ["21-05"]
files_modified:
  - apps/web/e2e/closure-flow.spec.ts
  - apps/web/e2e/teardown-e2e-data.ts
  - apps/web/e2e/README.md
  - .planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md
  - .planning/phases/21-app-vercel-filet-ci-tests/21-VALIDATION.md
autonomous: true
requirements: [TEST-01, APP-03]
must_haves:
  truths:
    - "Un pack closure complet se génère depuis le staging Vercel sur une session E2E- jetable avec la VRAIE génération IA OpenRouter — 0 stub (D-11, preuve pilier #1 cloud)"
    - "Un PDF synchrone téléchargé depuis le staging commence par les magic bytes %PDF — preuve APP-03 (Vercel → doc-engine Railway public + Bearer)"
    - "Le teardown ne supprime QUE les données préfixées E2E- (destructif = étape séparée, convention projet) et est relançable"
    - "21-SMOKE.md consolide toute l'evidence runtime de la phase (routes, upload, closure, PDF)"
  artifacts:
    - path: "apps/web/e2e/closure-flow.spec.ts"
      provides: "TEST-01 : session → participants → pack → docs, timeout 15 min"
      contains: "E2E-"
    - path: "apps/web/e2e/teardown-e2e-data.ts"
      provides: "Purge Prisma des données E2E- uniquement (worker/CLI-safe)"
      contains: "pathToFileURL"
    - path: "apps/web/e2e/README.md"
      provides: "Commandes à la demande (D-10) : smoke, closure, teardown"
      contains: "STAGING_BASE_URL"
    - path: ".planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md"
      provides: "Evidence consolidée phase 21 (phase gate)"
      min_lines: 40
  key_links:
    - from: "apps/web/e2e/closure-flow.spec.ts"
      to: "worker Railway (queue Postgres SKIP LOCKED)"
      via: "déclenchement pack via UI staging → ClosureJob QUEUED → worker poll"
      pattern: "closure"
    - from: "apps/web/e2e/teardown-e2e-data.ts"
      to: "prisma (cascade sessions/participants/docs/jobs)"
      via: "delete WHERE nom/label startsWith E2E- UNIQUEMENT"
      pattern: "E2E-"
---

<objective>
Prouver TEST-01 de bout en bout dans le cloud : Playwright crée une session jetable `E2E-` via l'UI staging (produit + stagiaires de test), déclenche le vrai pack closure (génération IA OpenRouter réelle, ~quelques centimes — D-11 verrouillée), vérifie 0 stub + un PDF valide (`%PDF`), nettoie tout via un teardown idempotent, et consolide l'evidence de la phase dans `21-SMOKE.md`. Clore par la PR `cloud-migration → main` (tout le code de tests passe le gate CI).

Purpose: preuve complète du pilier #1 (Pack 1-clic) en prod cloud, Mac hors boucle, + preuve APP-03 (PDF synchrone via doc-engine public Bearer).
Output: E2E vert, base propre, 21-SMOKE.md rempli, PR mergée.

⚠ Dépendances runtime réelles (Pitfall 9) : worker Railway vivant, budget OpenRouter, cold start Gotenberg — préflight obligatoire avant le run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-app-vercel-filet-ci-tests/21-CONTEXT.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-RESEARCH.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-05-SUMMARY.md
@apps/web/src/server/actions/closure-pack.ts

<interfaces>
<!-- Repères vérifiés -->
- Queue closure : Postgres SKIP LOCKED (Phase 20 — `queue-postgres.ts`, `enqueueClosureJob` = update ClosureJob status QUEUED). Le worker Railway est le SEUL consommateur.
- Témoin de durée : pack SES-0093 ≈ 3 min via OpenRouter (16/16 docs, 0 stub) → `test.setTimeout(15 * 60_000)` = marge cold start.
- Préflight doc-engine : `GET <GOTENBERG_URL_PUBLIC>/health` → 200 SANS Bearer (route /health ouverte, plan 20-03). URL relevée dans 21-DEPLOY-VERCEL.md.
- Playwright download : `const [download] = await Promise.all([page.waitForEvent('download'), …click…]); const stream = await download.createReadStream()` → lire les 5 premiers octets, attendre `%PDF-`.
- Teardown pattern : calqué `db-smoke-cloud.ts` (Phase 19) — imports UNIQUEMENT `@qualiof/db`, garde `import.meta.url === pathToFileURL(process.argv[1]).href`, 0 PII loggé.
- Session wizard : `/app/sessions/nouvelle` (répertoire vérifié). Fiche session : `/app/sessions/[id]` avec onglets (Phase 15) — lire les composants closure de la fiche pour localiser le CTA pack + l'indicateur de complétion/stubs AVANT d'écrire le spec.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teardown E2E idempotent + README e2e (commandes à la demande)</name>
  <files>apps/web/e2e/teardown-e2e-data.ts, apps/web/e2e/README.md</files>
  <read_first>apps/web/scripts/db-smoke-cloud.ts, packages/db/prisma/schema.prisma, apps/web/src/lib/storage.ts</read_first>
  <action>
    1. Créer `apps/web/e2e/teardown-e2e-data.ts` — script tsx worker/CLI-safe (imports UNIQUEMENT `@qualiof/db` + storage, AUCUN import React/auth) :
       - Cible EXCLUSIVE : rows dont le champ nom/label/email commence par `E2E-` ou `e2e-` (lire schema.prisma pour les champs exacts : nom de la session sur `TrainingSession`, `Person.firstName/lastName/email`, titre de `TrainingProduct`, `PreEnrollment` par token `e2e-`).
       - ORDRE de suppression (respecter les FK, lire les relations dans schema.prisma) : Document → ClosureJob/ClosureBatch → Attendance → SessionParticipant → SessionSlot/SessionTrainer → TrainingSession → PreEnrollment e2e- → Person E2E- → TrainingProduct E2E-. Utiliser `deleteMany({ where: { <champ>: { startsWith: 'E2E-' } } })` — si une cascade Prisma existe, la documenter et simplifier.
       - Supprimer AUSSI les objets storage des Documents supprimés (collecter les pdfUrl/keys AVANT le delete, puis remove via le client storage service-role).
       - Logger UNIQUEMENT des compteurs par table (0 PII).
       - Garde `pathToFileURL` (leçon %20 Phase 18). Idempotent : re-run sur base propre = tous compteurs 0, exit 0.
       - GARDE-FOU : throw explicite si un `where` se retrouve vide/undefined (interdiction structurelle d'un deleteMany global). Exporter la fonction principale (`export async function teardownE2EData()`) pour réutilisation en afterAll du spec closure.
    2. Créer `apps/web/e2e/README.md` documentant (D-10 — exécution à la demande, PAS dans le gate PR) :
       - Prérequis : `.env` local avec `E2E_LOGIN_EMAIL`/`E2E_LOGIN_PASSWORD`, `STAGING_BASE_URL`, optionnels `VERCEL_AUTOMATION_BYPASS_SECRET` + `E2E_DOCENGINE_HEALTH_URL`, worker Railway UP, budget OpenRouter
       - Smoke : `STAGING_BASE_URL=https://<domaine> pnpm --filter @qualiof/web exec dotenv -e ../../.env -- playwright test e2e/auth.setup.ts e2e/smoke-routes.spec.ts`
       - Upload : idem avec `e2e/upload-preenrollment.spec.ts`
       - Closure (long, ~5-15 min, coût ~centimes OpenRouter) : idem avec `e2e/closure-flow.spec.ts`
       - Teardown standalone (relance après crash) : `pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx e2e/teardown-e2e-data.ts`
       - Quand lancer : avant bascule Phase 22 + après tout déploiement important
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "pathToFileURL" apps/web/e2e/teardown-e2e-data.ts && grep -q "startsWith" apps/web/e2e/teardown-e2e-data.ts && ! grep -q "from 'react'" apps/web/e2e/teardown-e2e-data.ts && grep -q "STAGING_BASE_URL" apps/web/e2e/README.md && pnpm --filter @qualiof/web exec dotenv -e ../../.env -- tsx e2e/teardown-e2e-data.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `teardown-e2e-data.ts` : garde pathToFileURL, `startsWith: 'E2E-'` sur chaque deleteMany, 0 import React/auth, garde-fou anti-deleteMany-global, `export async function teardownE2EData`
    - Run à blanc sur base sans données E2E : exit 0, tous compteurs à 0 (idempotence prouvée)
    - README avec les 4 commandes exactes + prérequis
    - `tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Le filet de nettoyage existe et est prouvé AVANT de créer des données de test (ordre sûr)</done>
</task>

<task type="auto">
  <name>Task 2: Spec closure-flow — session E2E- via UI, pack IA réel, 0 stub, %PDF</name>
  <files>apps/web/e2e/closure-flow.spec.ts</files>
  <read_first>apps/web/src/app/app/sessions/nouvelle/page.tsx, apps/web/src/app/app/sessions/[id]/page.tsx, apps/web/src/server/actions/closure-pack.ts, .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md</read_first>
  <action>
    Créer `apps/web/e2e/closure-flow.spec.ts` (projet `authenticated`, `test.setTimeout(15 * 60_000)`) — TEST-01 exact : « création session → participants → pack → docs générés » :
    1. **Préflight (Pitfall 9)** : `request.get(process.env.E2E_DOCENGINE_HEALTH_URL)` → 200 attendu (URL du /health public du proxy Caddy, relevée dans 21-DEPLOY-VERCEL.md ; si la variable est absente, skip du préflight avec warning). Échec préflight → `test.skip` avec message explicite « worker/doc-engine Railway down » (pas de faux rouge code).
    2. **Fixtures via UI** (le cœur de TEST-01 — pas de seed Prisma pour le flux principal) :
       - produit : créer `E2E-Produit-<ts>` via Prisma en `beforeAll` (préfixé E2E-, tenantId du user e2e) — le requirement TEST-01 porte sur « création session → participants », pas sur la création produit ; documenter ce choix en commentaire du spec
       - wizard `/app/sessions/nouvelle` : session nommée `E2E-Session-<ts>`, dates courtes (1 jour), formateur existant sélectionné dans le picker
       - ajouter 2 participants : personnes `E2E-Alice`/`E2E-Bob` — via l'UI de la fiche session si le picker permet la création inline, sinon créées via Prisma en beforeAll puis AJOUTÉES à la session via l'UI (documenter le choix)
    3. **Déclencher le pack closure** depuis la fiche session (CTA repéré en read_first), avec la VRAIE génération OpenRouter (D-11 — coût ~centimes accepté, JAMAIS de mock du pipeline).
    4. **Poller l'UI** (reload toutes les 30 s, max 15 min) jusqu'à complétion du batch ; ASSERTER 0 stub via l'indicateur UI de complétion (repérer en read_first comment la fiche expose docs générés/stubs — sinon fallback : assert Prisma `document.count` attendu ET `closureJob` status DONE + errorMessage null pour la session E2E-).
    5. **Télécharger un doc du pack** : `page.waitForEvent('download')` → lire les premiers octets → `expect(head.toString('latin1')).toMatch(/^%PDF-/)`.
    6. **Preuve APP-03 dédiée** : télécharger un PDF SYNCHRONE rendu par Vercel (ex. convocation depuis la fiche session — action qui passe par `renderHtmlToPdf` de pdf-render.ts) → magic bytes `%PDF-` → PROUVE Vercel→Gotenberg public+Bearer. Noter en annotation : ce PDF DOIT porter le filigrane STAGING (rendu Vercel), alors que les docs du pack (rendus worker Railway) n'en portent PAS — comportement attendu (Open Q1 tranchée : flag staging sur Vercel uniquement).
    7. **Cleanup** : `afterAll` appelle `teardownE2EData()` importée de `./teardown-e2e-data` (pas de duplication).
    Exécuter contre le staging : `STAGING_BASE_URL=https://<domaine> pnpm --filter @qualiof/web exec dotenv -e ../../.env -- playwright test e2e/closure-flow.spec.ts` → vert. Coller la sortie + durée réelle dans le SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "15 \* 60_000\|900_000" apps/web/e2e/closure-flow.spec.ts && grep -q "%PDF" apps/web/e2e/closure-flow.spec.ts && grep -q "E2E-" apps/web/e2e/closure-flow.spec.ts && grep -qi "health" apps/web/e2e/closure-flow.spec.ts && grep -q "teardownE2EData" apps/web/e2e/closure-flow.spec.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Spec : timeout 15 min, préflight health, fixtures E2E-, assert 0 stub, assert `%PDF-` sur un doc du pack ET sur un PDF synchrone (APP-03)
    - Run contre staging : exit 0 — sortie Playwright + durée réelle du pack collées dans le SUMMARY
    - Post-run : `tsx e2e/teardown-e2e-data.ts` → tous compteurs 0 (aucune donnée E2E- résiduelle en base cloud)
    - Le coût OpenRouter du run est noté dans le SUMMARY (ordre de grandeur)
  </acceptance_criteria>
  <done>Pilier #1 prouvé 100 % cloud depuis le staging : session créée dans l'UI → pack IA réel → docs PDF valides → base rendue propre</done>
</task>

<task type="auto">
  <name>Task 3: 21-SMOKE.md consolidé (phase gate) + 21-VALIDATION à jour + PR finale cloud-migration → main</name>
  <files>.planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md, .planning/phases/21-app-vercel-filet-ci-tests/21-VALIDATION.md</files>
  <read_first>.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md, .planning/phases/21-app-vercel-filet-ci-tests/21-05-SUMMARY.md, .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md, .planning/phases/21-app-vercel-filet-ci-tests/21-VALIDATION.md</read_first>
  <action>
    1. Créer `.planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md` (calqué 19-SMOKE.md) consolidant TOUTE l'evidence runtime de la phase, par requirement :
       - **APP-01** : deploy cdg1 (x-vercel-id), bandeau STAGING, filigrane armé (test unit 21-01 + annotation filigrane du PDF synchrone Task 2), vercel.json + maxDuration
       - **APP-02** : login/logout Playwright sur domaine final (storageState), sameSite lax (grep auth.ts), redirect 307 anonyme, form public `/preinscription/[token]` 200 + token bidon refusé
       - **APP-03** : PDF synchrone `%PDF-` depuis le staging (Vercel → proxy Caddy public → Bearer) + les 3 env posées (GOTENBERG_URL/WEASYPRINT_URL/DOC_ENGINE_TOKEN)
       - **CI-01** : run CI vert sur main, PR témoin BLOCKED→merged, migrate deploy vert, image worker build
       - **TEST-01** : sortie closure-flow (durée, 0 stub, teardown 0 résiduel, coût OpenRouter)
       - **TEST-02** : sortie smoke-routes (n tests verts) + upload 10 Mo sans 413
       - Tableaux Étape | Commande | Attendu | Résultat | Date + sorties brutes en annexe. Lister explicitement les items MANUAL/reportés (retry réseau mobile, expiration signed URL — tracés au 21-05) : ce ne sont PAS des échecs.
    2. Mettre à jour `.planning/phases/21-app-vercel-filet-ci-tests/21-VALIDATION.md` : remplir la Per-Task Verification Map avec les statuts réels (✅ green), passer `nyquist_compliant: true` si toutes les vérifs automatisées existent, approval.
    3. **PR finale (D-07)** : pousser `cloud-migration`, `gh pr create --repo LM10031984/CRMNEXTGEN --base main --head cloud-migration --title "feat(21): filet tests staging (Playwright smoke + E2E closure + teardown)"`, attendre CI verte (`gh pr checks --watch`), `gh pr merge --merge` (merge commit — préserver l'historique des commits GSD), puis `git checkout cloud-migration && git merge main` (resynchroniser).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md && grep -q "APP-03" .planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md && grep -q "TEST-01" .planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md && grep -q "%PDF" .planning/phases/21-app-vercel-filet-ci-tests/21-SMOKE.md && gh pr list --repo LM10031984/CRMNEXTGEN --state merged --search "filet tests staging" --json number,title | grep -q "filet"</automated>
  </verify>
  <acceptance_criteria>
    - `21-SMOKE.md` contient une section par requirement (APP-01/02/03, CI-01, TEST-01/02) avec résultats datés et sorties brutes
    - Les items MANUAL/reportés sont listés séparément des succès (pas de faux vert)
    - `21-VALIDATION.md` : map remplie, statuts réels
    - PR « feat(21): filet tests staging » mergée dans main après CI verte ; `cloud-migration` resynchronisée
  </acceptance_criteria>
  <done>Toute l'evidence Phase 21 est consolidée pour /gsd:verify-work ; le code de tests vit dans main via le gate CI qu'il a lui-même prouvé</done>
</task>

</tasks>

<verification>
- `playwright test e2e/closure-flow.spec.ts` vert contre le staging (durée + coût consignés)
- `tsx e2e/teardown-e2e-data.ts` → 0 donnée E2E- résiduelle
- `21-SMOKE.md` présent avec les 6 requirements couverts
- PR finale mergée, main = cloud-migration
</verification>

<success_criteria>
- TEST-01 : flow closure complet prouvé (création session UI → participants → pack IA réelle → docs PDF valides, 0 stub)
- APP-03 : rendu PDF synchrone Vercel→doc-engine public authentifié prouvé par magic bytes
- Phase gate : evidence consolidée, base propre, D-10 documenté (runs à la demande)
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-vercel-filet-ci-tests/21-06-SUMMARY.md`
</output>
