---
phase: 21-app-vercel-filet-ci-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/env.ts
  - turbo.json
  - .env.example
  - packages/db/package.json
  - apps/web/vercel.json
  - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md
  - apps/web/src/lib/pdf-render.ts
  - apps/web/src/lib/__tests__/pdf-render.watermark.test.ts
  - apps/web/src/lib/calendar/sync-session.ts
  - apps/web/src/lib/auth.ts
  - apps/web/src/app/layout.tsx
  - apps/web/src/components/staging-banner.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/app/app/factures/page.tsx
  - apps/web/src/app/app/factures/[id]/page.tsx
  - apps/web/src/app/app/veille/page.tsx
autonomous: true
requirements: [APP-01, APP-02]
must_haves:
  truths:
    - "Un PDF rendu quand NEXT_PUBLIC_APP_ENV=staging porte un filigrane STAGING visible ; le même rendu en production/development est strictement inchangé"
    - "Le footer HTML in-body des PDF n'est pas régressé par l'injection filigrane (anti-pattern CLAUDE.md)"
    - "syncSessionCalendar ne crée AUCUN événement Google Calendar quand l'app est en staging (D-02)"
    - "Le cookie de session Lucia déclare explicitement sameSite lax (grep-vérifiable, success criterion 2)"
    - "vercel.json déclare la région cdg1 et AUCUN cron closure-worker"
  artifacts:
    - path: "apps/web/vercel.json"
      provides: "Région Vercel cdg1 verrouillée"
      contains: "cdg1"
    - path: "apps/web/src/lib/pdf-render.ts"
      provides: "Filigrane staging au chokepoint unique des 9 actions PDF"
      exports: ["withStagingWatermark", "renderHtmlToPdf", "renderHtmlToPdfWeasy"]
    - path: "apps/web/src/lib/__tests__/pdf-render.watermark.test.ts"
      provides: "Preuve unit du filigrane conditionnel + non-régression footer"
      min_lines: 30
    - path: "packages/shared/src/env.ts"
      provides: "Flag NEXT_PUBLIC_APP_ENV validé t3-env"
      contains: "NEXT_PUBLIC_APP_ENV"
    - path: "packages/db/package.json"
      provides: "postinstall prisma generate (Pitfall 5 Vercel/CI)"
      contains: "postinstall"
  key_links:
    - from: "apps/web/src/lib/pdf-render.ts"
      to: "packages/shared/src/env.ts"
      via: "sharedEnv.NEXT_PUBLIC_APP_ENV lu par withStagingWatermark"
      pattern: "NEXT_PUBLIC_APP_ENV"
    - from: "apps/web/src/lib/calendar/sync-session.ts"
      to: "packages/shared/src/env.ts"
      via: "early-return staging en tête de syncSessionCalendar"
      pattern: "NEXT_PUBLIC_APP_ENV === 'staging'"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/staging-banner.tsx"
      via: "import StagingBanner rendu dans le body"
      pattern: "StagingBanner"
---

<objective>
Poser TOUT le code des gardes staging AVANT que l'app soit exposée sur Vercel : flag `NEXT_PUBLIC_APP_ENV` (t3-env), filigrane STAGING au chokepoint `pdf-render.ts`, garde Google Calendar, bandeau UI, `sameSite: 'lax'` explicite, `vercel.json` région cdg1, `postinstall: prisma generate`, `maxDuration` par segment. Amender aussi `17-REGIONS.md` (D-05 Irlande définitive).

Purpose: APP-01 (staging dégelé : flag + filigrane + garde) et APP-02 (cookie secure+lax vérifiable). Tout est du code local testable — aucune dépendance Vercel/Railway. C'est le contenu qui sera mergé dans `main` par le plan 21-03 puis déployé par le plan 21-04.
Output: gardes staging commitées sur `cloud-migration`, suite Vitest verte (baseline 1166+ tests + nouveaux tests filigrane), tsc exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-app-vercel-filet-ci-tests/21-CONTEXT.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-RESEARCH.md
@apps/web/src/lib/pdf-render.ts
@packages/shared/src/env.ts

<interfaces>
<!-- Extraits vérifiés du codebase 2026-07-06. Utiliser tels quels. -->

From apps/web/src/lib/pdf-render.ts (état actuel — chokepoint app + worker):
```typescript
import { sharedEnv } from '@qualiof/shared/env';
function authHeaders(): Record<string, string>;   // Bearer conditionnel DOC_ENGINE_TOKEN — NE PAS TOUCHER
export async function renderHtmlToPdf(html: string, options?: { footerHtml?: string; headerHtml?: string }): Promise<Buffer>;
export async function renderHtmlToPdfWeasy(html: string): Promise<Buffer>;
// Commentaire lignes 4-7 : Chromium ne répète PAS `position:fixed` multi-pages
// → le filigrane DOIT être un background répété, jamais un position:fixed.
```

From apps/web/src/lib/calendar/sync-session.ts:
```typescript
export interface SyncSessionRecap {
  inserted: number;
  updated: number;
  skipped: number;
  total: number;
  errors: { eventKey: string; message: string }[];
}
export async function syncSessionCalendar(/* ligne 76 */): Promise<SyncSessionRecap>;
```

From apps/web/src/lib/auth.ts (lignes 18-24 — état actuel):
```typescript
export const lucia = new Lucia(adapter, {
  sessionCookie: {
    expires: false,
    attributes: {
      secure: process.env.NODE_ENV === 'production',
    },
  },
  ...
```

From packages/shared/src/env.ts — bloc `client` actuel (lignes 104-112) :
`NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_APP_NAME`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Chaque clé client doit AUSSI être ajoutée dans `runtimeEnv`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag NEXT_PUBLIC_APP_ENV + plomberie config (turbo, .env.example, postinstall, vercel.json, 17-REGIONS)</name>
  <files>packages/shared/src/env.ts, turbo.json, .env.example, packages/db/package.json, apps/web/vercel.json, .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md</files>
  <read_first>packages/shared/src/env.ts, turbo.json, .env.example, packages/db/package.json, .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md</read_first>
  <action>
    1. `packages/shared/src/env.ts` — dans le bloc `client:` (après `NEXT_PUBLIC_SUPABASE_ANON_KEY`), ajouter :
       `NEXT_PUBLIC_APP_ENV: z.enum(['development', 'staging', 'production']).default('development'),`
       et dans `runtimeEnv:` ajouter : `NEXT_PUBLIC_APP_ENV: process.env.NEXT_PUBLIC_APP_ENV,`
       (variable inlinée au build côté client ; côté serveur lue au runtime — les deux chemins passent par `sharedEnv.NEXT_PUBLIC_APP_ENV`).
    2. `turbo.json` — ajouter `"NEXT_PUBLIC_APP_ENV"` au tableau `globalEnv` (invalidation cache, Phase 17 pattern).
    3. `.env.example` — ajouter un bloc commenté :
       ```
       # Environnement applicatif (v6 Phase 21) — development | staging | production
       # staging = filigrane PDF + bandeau UI + gardes sorties (calendar skip, MAIL_DRY_RUN)
       NEXT_PUBLIC_APP_ENV=development
       # Force le dry-run mail même si SMTP_HOST est renseigné (garde staging D-02)
       MAIL_DRY_RUN=false
       ```
       NB : `MAIL_DRY_RUN` est DÉJÀ lu par `apps/web/src/lib/mailer.ts` (`isDryRun()` retourne true si `process.env.MAIL_DRY_RUN === 'true'` OU si `SMTP_HOST` absent) — AUCUN code mailer à modifier, on documente seulement la clé.
    4. `packages/db/package.json` — ajouter dans `scripts:` : `"postinstall": "prisma generate"` (Pitfall 5 RESEARCH : en monorepo pnpm sur Vercel/CI, le postinstall auto de @prisma/client ne trouve pas le schema ; `prisma generate` ne lit PAS `.env` donc safe partout — PAS de `dotenv -e` sur cette ligne, contrairement aux autres scripts du fichier).
    5. Créer `apps/web/vercel.json` (DANS apps/web, PAS à la racine — Root Directory Vercel = apps/web, Pitfall 6) avec exactement :
       ```json
       {
         "$schema": "https://openapi.vercel.sh/vercel.json",
         "regions": ["cdg1"]
       }
       ```
       AUCUN bloc `crons` (Pitfall 8 : déclarer `api/cron/closure-worker` créerait un 2ᵉ consommateur 60s en concurrence avec le worker Railway → jobs tronqués ; les 2 reminders sont tranchés Phase 22).
    6. `.planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md` — ajouter une section datée 2026-07-06 « Amendement D-05 (Phase 21) : Supabase = eu-west-1 Irlande DÉFINITIVE » : projet `gntlqyscahbgjrmsbzil`, UE donc RGPD conforme, base + 3109 objets migrés et prouvés (Phases 18-19), dérogation à eu-west-3 Paris actée par Laurent — ne plus re-proposer Paris.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -c "NEXT_PUBLIC_APP_ENV" packages/shared/src/env.ts | grep -q 2 && grep -q '"NEXT_PUBLIC_APP_ENV"' turbo.json && grep -q '"postinstall": "prisma generate"' packages/db/package.json && grep -q '"cdg1"' apps/web/vercel.json && ! grep -q '"crons"' apps/web/vercel.json && node -e "JSON.parse(require('fs').readFileSync('apps/web/vercel.json','utf8'))" && pnpm --filter @qualiof/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/env.ts` contient exactement 2 occurrences de `NEXT_PUBLIC_APP_ENV` (bloc client + runtimeEnv) avec `z.enum(['development', 'staging', 'production']).default('development')`
    - `turbo.json` globalEnv contient `"NEXT_PUBLIC_APP_ENV"`
    - `packages/db/package.json` contient `"postinstall": "prisma generate"` (sans dotenv)
    - `apps/web/vercel.json` existe, JSON valide, contient `"regions": ["cdg1"]`, ne contient PAS `crons`
    - `.env.example` contient `NEXT_PUBLIC_APP_ENV=` et `MAIL_DRY_RUN=`
    - `17-REGIONS.md` contient `eu-west-1` et `D-05`
    - `tsc --noEmit` exit 0 sur packages/shared
  </acceptance_criteria>
  <done>Flag validé t3-env + toute la plomberie config Vercel/CI posée, aucun secret ajouté</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filigrane STAGING au chokepoint pdf-render.ts (background SVG répété, testé)</name>
  <files>apps/web/src/lib/pdf-render.ts, apps/web/src/lib/__tests__/pdf-render.watermark.test.ts</files>
  <read_first>apps/web/src/lib/pdf-render.ts, apps/web/src/lib/of-pdf-footer.ts, apps/web/vitest.config.ts</read_first>
  <behavior>
    - Test 1: `withStagingWatermark(html, 'staging')` sur un HTML avec `</head>` → le résultat contient `STAGING` (le style filigrane) injecté AVANT `</head>`
    - Test 2: `withStagingWatermark(html, 'production')` → résultat STRICTEMENT identique à l'entrée (===)
    - Test 3: `withStagingWatermark(html, 'development')` → résultat identique à l'entrée
    - Test 4: HTML SANS `</head>` + 'staging' → le style est préfixé en tête du HTML
    - Test 5 (non-régression footer): un HTML contenant un footer in-body (`<footer style="position:fixed;bottom:0">...</footer>` et/ou `position: running(footer)`) passé en 'staging' → le footer est toujours présent à l'identique dans la sortie (le filigrane est purement additif)
  </behavior>
  <action>
    Dans `apps/web/src/lib/pdf-render.ts`, ajouter (technique RESEARCH Pattern 2 — background répété, JAMAIS `position:fixed` car Chromium ne le répète pas multi-pages, commentaire existant lignes 4-7) :
    ```ts
    const WATERMARK_SVG = encodeURIComponent(
      `<svg xmlns="http://www.w3.org/2000/svg" width="400" height="300">
         <text x="50%" y="50%" text-anchor="middle" transform="rotate(-30 200 150)"
           font-family="Helvetica" font-size="48" fill="rgba(220,38,38,0.16)" font-weight="bold">STAGING</text>
       </svg>`,
    );
    const WATERMARK_STYLE = `<style>
      body { background-image: url("data:image/svg+xml,${WATERMARK_SVG}") !important;
             background-repeat: repeat !important; -webkit-print-color-adjust: exact; }
    </style>`;

    /** Injecte le filigrane STAGING si appEnv=staging. Purement additif — ne touche jamais le footer in-body. */
    export function withStagingWatermark(
      html: string,
      appEnv: string = sharedEnv.NEXT_PUBLIC_APP_ENV,
    ): string {
      if (appEnv !== 'staging') return html;
      return html.includes('</head>')
        ? html.replace('</head>', `${WATERMARK_STYLE}</head>`)
        : WATERMARK_STYLE + html;
    }
    ```
    Câblage :
    - `renderHtmlToPdf` : première ligne du corps → `html = withStagingWatermark(html);` (déclarer le paramètre non-readonly ou une variable locale). Après les `form.append` existants, ajouter : `if (sharedEnv.NEXT_PUBLIC_APP_ENV === 'staging') form.append('printBackground', 'true');` (Gotenberg n'imprime pas les backgrounds sans ce champ).
    - `renderHtmlToPdfWeasy` : `body: withStagingWatermark(html)` (WeasyPrint imprime les backgrounds par défaut, rien d'autre).
    - NE PAS toucher `authHeaders()`, les marges, `preferCssPageSize`, ni le mécanisme footer.html/footer in-body.
    Test `apps/web/src/lib/__tests__/pdf-render.watermark.test.ts` : importer `withStagingWatermark` et couvrir les 5 comportements du bloc behavior. Le paramètre explicite `appEnv` rend les tests hermétiques (pas de mutation d'env). L'import du module charge `sharedEnv` → lancer via le pattern projet `dotenv -e ../../.env -- vitest`.
    RED→GREEN : écrire le test d'abord, le voir échouer (fonction absente), implémenter, vert.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec dotenv -e ../../.env -- vitest run src/lib/__tests__/pdf-render.watermark.test.ts && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/pdf-render.ts` contient `export function withStagingWatermark(`
    - `grep -c "withStagingWatermark" apps/web/src/lib/pdf-render.ts` ≥ 3 (déclaration + 2 call sites Gotenberg/Weasy)
    - `apps/web/src/lib/pdf-render.ts` contient `printBackground`
    - `apps/web/src/lib/pdf-render.ts` ne contient PAS `position:fixed` ni `position: fixed` dans le bloc WATERMARK (le filigrane est un background)
    - `pdf-render.watermark.test.ts` : 5 tests verts via la commande vitest ci-dessus
    - `tsc --noEmit` exit 0 sur apps/web
  </acceptance_criteria>
  <done>Les 9 actions PDF synchrones ET le worker partagent le filigrane conditionnel ; seul l'env Vercel (`NEXT_PUBLIC_APP_ENV=staging`) l'active — le worker Railway (sans cette variable) produit des packs propres (Open Q1 RESEARCH tranchée : flag sur Vercel uniquement)</done>
</task>

<task type="auto">
  <name>Task 3: Garde calendar staging + bandeau UI + sameSite lax + maxDuration par segment</name>
  <files>apps/web/src/lib/calendar/sync-session.ts, apps/web/src/lib/auth.ts, apps/web/src/app/layout.tsx, apps/web/src/components/staging-banner.tsx, apps/web/src/app/app/sessions/[id]/page.tsx, apps/web/src/app/app/factures/page.tsx, apps/web/src/app/app/factures/[id]/page.tsx, apps/web/src/app/app/veille/page.tsx</files>
  <read_first>apps/web/src/lib/calendar/sync-session.ts, apps/web/src/lib/auth.ts, apps/web/src/app/layout.tsx, apps/web/src/app/app/sessions/[id]/page.tsx</read_first>
  <action>
    1. **Garde calendar (D-02)** — `apps/web/src/lib/calendar/sync-session.ts` : en TÊTE du corps de `syncSessionCalendar` (ligne ~78), ajouter :
       ```ts
       if (sharedEnv.NEXT_PUBLIC_APP_ENV === 'staging') {
         console.info('[calendar] sync skipped — staging guard (D-02, Phase 21)');
         return { inserted: 0, updated: 0, skipped: 0, total: 0, errors: [] };
       }
       ```
       (shape EXACT de `SyncSessionRecap` : inserted/updated/skipped/total/errors — vérifié ligne 51). Ajouter l'import `sharedEnv` depuis `@qualiof/shared/env` s'il est absent. Raison au-delà de D-02 : le token OAuth vit dans `files/secrets/google-token.json`, ABSENT du déploiement Vercel — sans garde, tout sync staging échouerait bruyamment.
    2. **sameSite lax explicite (APP-02, success criterion 2)** — `apps/web/src/lib/auth.ts` lignes 21-23, remplacer `attributes: { secure: process.env.NODE_ENV === 'production' }` par :
       ```ts
       attributes: {
         secure: process.env.NODE_ENV === 'production',
         sameSite: 'lax',
       },
       ```
       (Vercel force NODE_ENV=production → secure on automatiquement, Pitfall 7 ; on rend `lax` grep-vérifiable au lieu d'implicite Lucia).
    3. **Bandeau UI staging** — créer `apps/web/src/components/staging-banner.tsx` (Server Component, PAS de 'use client') :
       ```tsx
       import { sharedEnv } from '@qualiof/shared/env';
       export function StagingBanner() {
         if (sharedEnv.NEXT_PUBLIC_APP_ENV !== 'staging') return null;
         return (
           <div className="fixed top-0 inset-x-0 z-[100] bg-amber-500 text-amber-950 text-center text-xs font-bold py-1 tracking-wider">
             ENVIRONNEMENT DE TEST — STAGING — les documents portent un filigrane, aucun email n&apos;est envoyé
           </div>
         );
       }
       ```
       L'insérer dans le layout RACINE `apps/web/src/app/layout.tsx` (couvre /login ET /app en une seule insertion) : `<StagingBanner />` en premier enfant du `<body>`.
    4. **maxDuration par segment (APP-01)** — voie officielle App Router (RESEARCH : le glob `vercel.json functions` cible mal les pages App Router). Ajouter `export const maxDuration = 300;` (avec commentaire `// Vercel Pro — rendu PDF synchrone via doc-engine Railway (Phase 21 APP-01)`) en tête des pages qui déclenchent des rendus PDF synchrones :
       - `apps/web/src/app/app/sessions/[id]/page.tsx` (convocation, grilles, docs session)
       - `apps/web/src/app/app/factures/page.tsx` et `apps/web/src/app/app/factures/[id]/page.tsx` (PDF factures/avoirs)
       - `apps/web/src/app/app/veille/page.tsx` (veille-export)
       - si `apps/web/src/app/app/produits/[id]/page.tsx` existe : idem (programme/déroulé)
       300 = défaut Pro mais explicite et grep-able ; AUCUNE route n'a besoin de 800s (rendu Gotenberg = secondes, gros travaux = worker Railway).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "staging guard" apps/web/src/lib/calendar/sync-session.ts && grep -q "sameSite: 'lax'" apps/web/src/lib/auth.ts && grep -q "StagingBanner" apps/web/src/app/layout.tsx && test $(grep -rl "export const maxDuration" apps/web/src/app/app | wc -l) -ge 4 && pnpm --filter @qualiof/web exec tsc --noEmit && pnpm --filter @qualiof/web exec dotenv -e ../../.env -- vitest run src/lib/calendar</automated>
  </verify>
  <acceptance_criteria>
    - `sync-session.ts` contient `NEXT_PUBLIC_APP_ENV === 'staging'` et `return { inserted: 0, updated: 0, skipped: 0, total: 0, errors: [] }`
    - `auth.ts` contient `sameSite: 'lax'`
    - `staging-banner.tsx` existe, contient `STAGING`, ne contient PAS `'use client'`
    - `apps/web/src/app/layout.tsx` contient `<StagingBanner />`
    - ≥ 4 fichiers sous `apps/web/src/app/app/` contiennent `export const maxDuration`
    - `tsc --noEmit` exit 0 ; tests calendar existants toujours verts
    - Suite complète `pnpm test` verte avant commit final du plan (baseline 1166+ et nouveaux tests)
  </acceptance_criteria>
  <done>Toutes les gardes staging D-02 sont en place côté code ; l'app peut être exposée sans risque d'email/calendar/doc officiel fuyant du staging</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0 et `pnpm --filter @qualiof/shared exec tsc --noEmit` exit 0
- Suite complète `pnpm test` verte (baseline 1166+ tests + 5 tests filigrane)
- `git log --oneline` montre les commits du plan sur `cloud-migration`
- Aucun secret dans les fichiers modifiés (`.env.example` = placeholders uniquement)
</verification>

<success_criteria>
- APP-01 (partie code) : flag + filigrane testé + vercel.json cdg1 + maxDuration + postinstall prêts à déployer
- APP-02 (partie code) : `sameSite: 'lax'` explicite, `secure` garanti par NODE_ENV=production Vercel
- D-02 : emails (MAIL_DRY_RUN documenté), calendar (early-return), PDF (filigrane) — les 3 sorties gardées
- D-05 : dérogation Irlande actée dans 17-REGIONS.md
</success_criteria>

<output>
After completion, create `.planning/phases/21-app-vercel-filet-ci-tests/21-01-SUMMARY.md`
</output>
