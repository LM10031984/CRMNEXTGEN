---
phase: 22-bascule-prod-conformit-rgpd
plan: 08
type: execute
wave: 3
depends_on: ["22-06"]
files_modified:
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
autonomous: false
requirements: [CUT-02]

must_haves:
  truths:
    - "Une alerte coût email vers laurent@start-academy.fr est active sur les 4 plateformes réelles (OpenRouter, Supabase, Vercel, Railway — Upstash n'existe plus, D-11)"
    - "AUCUNE alerte ne peut éteindre la prod toute seule : pas d'auto-pause Vercel, pas de hard limit Railway bas (Pitfall 5)"
    - "Les backups Supabase daily sont confirmés ACTIFS avec preuve dashboard, projet en eu-west-1 (D-12 — research flag roadmap résolu)"
  artifacts:
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md"
      provides: "Preuves datées : 4 alertes configurées + capture backups daily"
  key_links:
    - from: "22-COSTS-BACKUPS.md"
      to: "dashboards 4 plateformes"
      via: "captures/preuves datées par plateforme"
      pattern: "OpenRouter|Supabase|Vercel|Railway"
---

<objective>
Confirmer les garde-fous d'exploitation (CUT-02, seconde moitié) : alertes coûts sur les 4 plateformes (seuils ~1,5× le coût attendu, email laurent@start-academy.fr) et backups Supabase daily actifs avec preuve de région EU.

Purpose: la prod cloud coûte de l'argent chaque mois — sans alertes, une dérive (boucle IA, trafic) se découvre sur la facture ; sans preuve backups, le critère 3 de la phase n'est pas satisfait.
Output: 4 alertes actives + preuve backups, documentées dans 22-COSTS-BACKUPS.md + runbook §9.6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-RESEARCH.md

<interfaces>
Réglages cibles (research §Pattern 4, confiance MEDIUM sur les libellés d'écrans — configurer « l'équivalent de », prouver par capture) :
| Plateforme | Mécanique | Cible D-11 (~1,5×) | Garde-fou Pitfall 5 |
|---|---|---|---|
| Vercel | Team Settings → Billing → Spend Management | budget ~45 $, notifications email | NE PAS activer l'auto-pause |
| Railway | Workspace → Usage → Set Usage Limits | soft alert email ~35 € | PAS de hard limit (ou ≥ 3× budget) — il couperait worker + doc-engines |
| Supabase | Org → Billing → spend cap | vérifier spend cap ON (défaut Pro 25 $) — plafond dur SANS couper la DB | rien à faire de plus |
| OpenRouter | openrouter.ai → Credits + Keys | Auto Top-Up OFF, solde ~15 €, credit limit sur la clé prod | documenter « HTTP 402 → recharger » (runbook §7) |

Coûts attendus : OpenRouter ~10 €/mois, Supabase 25 $/mois, Vercel 30 $/mois (plan+add-on), Railway ~20-25 €/mois.
Justification manual-only (22-RESEARCH §Validation Architecture) : état de configuration SaaS billing, pas de code — preuve = captures datées.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configurer les alertes coûts (4 dashboards) + capturer la page backups</name>
  <action>PAUSE human-action (rare cas légitime : écrans billing Owner-only sans API fiable). Présenter à Laurent le tableau des réglages cibles par plateforme (bloc interfaces + runbook §6) avec les garde-fous Pitfall 5 en gras. Guider écran par écran ; si un réglage s'avère accessible par API/CLI, l'exécuter à sa place sur autorisation. Collecter les 5 captures/confirmations avant de reprendre.</action>
  <what-built>Claude a préparé le tableau exact des réglages cibles (bloc interfaces + runbook §6). Les écrans de billing/spend management des 4 plateformes exigent le compte propriétaire (rôle Owner/Billing) sans API fiable — c'est l'un des rares vrais human-action. Claude guide en direct et exécute à ta place tout ce qui s'avère accessible par API, sur autorisation (pattern 21-04).</what-built>
  <how-to-verify>
    Effectuer, dans l'ordre, en suivant le runbook §6 :
    1. **Vercel** (team laurents-projects-3806ab87) : Billing → Spend Management → budget 45 $ + notifications email — SANS activer l'option de pause automatique des projets (elle couperait la prod).
    2. **Railway** (workspace du worker) : Usage → Usage Limits → alerte email ~35 € — NE PAS poser de hard limit (ou alors ≥ 100 € : un hard limit coupe worker + Gotenberg + WeasyPrint).
    3. **Supabase** (org du projet gntlqyscahbgjrmsbzil) : Billing → vérifier que le spend cap est ON ; capturer AUSSI Database → Backups (liste des snapshots quotidiens) et la région du projet (eu-west-1).
    4. **OpenRouter** : Credits → vérifier Auto Top-Up OFF + solde ~15 € ; Keys → credit limit sur la clé de prod.
    Pour chaque écran : une capture (ou copie du texte de confirmation) transmise à Claude.
  </how-to-verify>
  <resume-signal>Répondre « alertes faites » + fournir les 5 captures/confirmations (4 alertes + 1 backups). Signaler tout écran introuvable — Claude adaptera (« configurer l'équivalent »).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Documenter les preuves alertes + backups (22-COSTS-BACKUPS.md, runbook §9.6)</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md, .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md (§6 et §9.6 — gabarit evidence)
  </read_first>
  <action>
    Créer `22-COSTS-BACKUPS.md` :
    - Par plateforme (4 sections) : réglage effectif (seuil exact, type d'alerte, email cible laurent@start-academy.fr), garde-fou Pitfall 5 vérifié (auto-pause OFF / pas de hard limit bas / spend cap ON / auto top-up OFF), référence de la capture (stocker les fichiers de capture transmis dans `.planning/phases/22-bascule-prod-conformit-rgpd/evidence/`), date.
    - Section backups : « Backups Supabase daily ACTIFS (plan Pro, rétention 7 jours), projet eu-west-1 (Irlande, UE ✓) — preuve capture dashboard du {date}. Limite assumée (Pitfall 7) : snapshots stockés dans la même région AWS que le projet, non off-site — une défaillance régionale emporterait base ET backups ; pg_dump hors vendor = backlog assumé (D-12, Future Requirements). »
    - Remplir le runbook §9.6 (renvoi vers ce fichier + horodatage).
    - Si un des 5 items n'a pas pu être configuré : le lister en « ⚠ PENDING » explicite avec la raison — le plan n'est PAS complet tant qu'un garde-fou coût manque (déviation à remonter).
    Commit : `docs(22-08): cost alerts on 4 platforms + Supabase daily backups proof (CUT-02, D-11/D-12)`
  </action>
  <verify>
    <automated>grep -q "eu-west-1" ".planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md" && grep -qi "auto-pause" ".planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md" && grep -q "laurent@start-academy.fr" ".planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md" && ! grep -q "PENDING" ".planning/phases/22-bascule-prod-conformit-rgpd/22-COSTS-BACKUPS.md"</automated>
  </verify>
  <acceptance_criteria>
    - 4 sections plateformes avec seuil exact + email cible + garde-fou Pitfall 5 tracé
    - Section backups avec « eu-west-1 », « 7 jours », « daily » et la limite non off-site assumée (Pitfall 7)
    - 0 item « PENDING » (sinon déviation documentée et plan incomplet)
    - Runbook §9.6 rempli avec horodatage
  </acceptance_criteria>
  <done>Critère 3 de la phase prouvé : alertes coûts actives sans risque d'extinction auto, backups daily EU confirmés.</done>
</task>

</tasks>

<verification>
- 22-COSTS-BACKUPS.md complet (4 alertes + backups), captures archivées dans evidence/.
- Aucun garde-fou configuré ne peut éteindre la prod (Pitfall 5 vérifié plateforme par plateforme).
</verification>

<success_criteria>
CUT-02 (partie alertes + backups) satisfait : les 4 plateformes alertent laurent@start-academy.fr avant dérive, les backups daily Supabase sont prouvés actifs en région EU.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-08-SUMMARY.md`
</output>
