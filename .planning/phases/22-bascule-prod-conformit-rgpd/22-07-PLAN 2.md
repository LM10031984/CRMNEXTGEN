---
phase: 22-bascule-prod-conformit-rgpd
plan: 07
type: execute
wave: 3
depends_on: ["22-06", "22-11"]
files_modified:
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
  - apps/web/scripts/_reset-burned-reminders.ts
autonomous: false
requirements: [CUT-01, CUT-02]

must_haves:
  truths:
    - "MAIL_DRY_RUN=false est actif sur Vercel ET Railway (Pitfall 10) — mais SEULEMENT après validation nominative du rapport par Laurent (D-06)"
    - "La décision sur les compteurs de relance brûlés en dry-run (Pitfall 1) est prise par Laurent et appliquée AVANT le flip — pas de silence ni d'envoi surprise"
    - "Aucun envoi de masse vers les apprenants : preuve que seuls les envois listés et validés peuvent partir"
    - "Un email réel de test est parti (messageId, pas dryRun) — la chaîne SMTP prod est prouvée"
  artifacts:
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md"
      provides: "Rapport rafraîchi post-bascule + décision Laurent tracée + remédiation appliquée"
  key_links:
    - from: "checkpoint décision Laurent"
      to: "MAIL_DRY_RUN=false (2 plateformes)"
      via: "séquence D-06 stricte : rapport → validation → remédiation → flip"
      pattern: "MAIL_DRY_RUN"
    - from: "apps/web/scripts/_reset-burned-reminders.ts"
      to: "Invoice.reminderCount / lastReminderAt"
      via: "data migration ciblée, snapshot avant, uniquement si décidée"
      pattern: "reminderCount"
---

<objective>
La sortie du dry-run (runbook §4) : rafraîchir le rapport des envois en attente, faire décider Laurent (remédiation des compteurs brûlés + validation de la liste nominative), appliquer la remédiation choisie, puis flipper `MAIL_DRY_RUN=false` sur les DEUX plateformes et prouver un envoi réel.

Purpose: exigence forte de Laurent (répétée 2×) — jamais d'envoi de masse ni de surprise ; et sans remédiation Pitfall 1, le premier run réel serait faux (niveaux brûlés à blanc).
Output: emails réels actifs, décision tracée, preuves §9.4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
@apps/web/scripts/pending-reminders-report.ts

<interfaces>
Cron relances : worker Railway, quotidien 8h Europe/Paris (croner, Phase 20) — le flip Railway prend effet au redeploy automatique ; le prochain run réel est le lendemain 8h.
Remédiation éventuelle : `Invoice.reminderCount` (Int) + `lastReminderAt` (DateTime?) — reset = restaurer l'état pré-dry-run par facture, traçable via AuditLog `invoices.reminder_sent` `diff.dryRun=true` (le nombre de relances brûlées PAR facture = le décrément à appliquer).
Convention projet « destructif = étape séparée » : la remédiation est une UPDATE ciblée → snapshot des lignes avant, exécution seulement après décision explicite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rafraîchir le rapport des envois en attente (post-bascule)</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md</files>
  <read_first>
    - apps/web/scripts/pending-reminders-report.ts (script 22-04 — ré-exécution, pas de réécriture)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md (version Wave 1 — conserver en historique)
  </read_first>
  <action>
    Ré-exécuter `pnpm tsx apps/web/scripts/pending-reminders-report.ts` (le cron dry-run a continué de tourner entre la Wave 1 et maintenant — les chiffres ont bougé). Ajouter au rapport une section « Rafraîchi le {date} (post-bascule, pré-flip) » avec les tableaux A (envois qui partiraient, flags ⚠ APPRENANT) et B (relances brûlées par facture) à jour, SANS écraser la version Wave 1 (append daté). Mettre en évidence pour le checkpoint : nombre total d'emails au premier run réel, dont X vers des apprenants (règle payeur), et l'impact de chaque option de remédiation sur ces chiffres (option ① reset complet → N1 emails, option ③ en l'état → N2 emails).
    Commit : `docs(22-07): refreshed pending-sends report pre-flip (D-06)`
  </action>
  <verify>
    <automated>grep -q "Rafraîchi le" ".planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md" && pnpm tsx apps/web/scripts/pending-reminders-report.ts >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Section « Rafraîchi le {date} » présente avec les 2 tableaux à jour
    - Chiffrage par option de remédiation (au moins options ① et ③ chiffrées en nombre d'emails)
    - La version Wave 1 du rapport reste lisible (historique conservé)
  </acceptance_criteria>
  <done>Laurent a des chiffres du jour, pas de la Wave 1.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Décision Laurent — remédiation des compteurs brûlés + validation de la liste d'envoi (D-06)</name>
  <action>PAUSE bloquante. Présenter le rapport rafraîchi (tableaux A et B, flags ⚠ APPRENANT, chiffrage par option). Recueillir : ① le choix de remédiation (reset-complet / reset-selectif + liste / accepter) ; ② la validation de la liste d'envoi (ou exclusions). Tracer la double décision datée dans 22-PENDING-SENDS-REPORT.md. Aucun flip MAIL_DRY_RUN sans cette réponse.</action>
  <decision>① Que faire des niveaux de relance « brûlés » en dry-run depuis la Phase 20 ? ② Valider (ou amender) la liste nominative des relances qui partiront au premier run réel.</decision>
  <context>Le cron Railway incrémente `reminderCount` même en dry-run (bug de conception Pitfall 1) : des factures ont « consommé » des niveaux sans qu'aucun email ne parte. Un reset aveugle re-enverrait un niveau 1 à des payeurs peut-être déjà relancés MANUELLEMENT hors outil (Open Question 2). Le rapport rafraîchi (Task 1) liste tout, avec flags ⚠ APPRENANT quand le destinataire est l'apprenant lui-même (auto-entrepreneur = son propre payeur).</context>
  <options>
    <option id="reset-complet">
      <name>Reset complet des compteurs brûlés</name>
      <pros>État propre : chaque facture repart au niveau réel (0 email parti = niveau 0) ; le cycle 30/45 j reprend fidèlement</pros>
      <cons>Peut re-relancer niveau 1 des payeurs déjà relancés manuellement hors outil ; volume d'emails du 1er run = maximum</cons>
    </option>
    <option id="reset-selectif">
      <name>Reset sélectif (Laurent coche facture par facture sur le rapport)</name>
      <pros>Contrôle nominal total — cohérent avec l'exigence « jamais de surprise »</pros>
      <cons>Demande une passe manuelle de Laurent sur le tableau B</cons>
    </option>
    <option id="accepter">
      <name>Acceptation en l'état (pas de reset)</name>
      <pros>Zéro data migration ; zéro risque de double relance</pros>
      <cons>Des factures en retard ne seront JAMAIS relancées (maxLevel atteint à blanc) — silences définitifs à assumer</cons>
    </option>
  </options>
  <resume-signal>Répondre : « option reset-complet | reset-selectif (+ liste des factures) | accepter » ET « liste d'envoi validée » (ou exclusions). Cette double réponse vaut autorisation du flip MAIL_DRY_RUN=false (Task 4).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Appliquer la remédiation décidée (data migration ciblée, snapshot avant)</name>
  <files>apps/web/scripts/_reset-burned-reminders.ts</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md (tableau B + décision tracée)
    - apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts (sémantique reminderCount/lastReminderAt — le reset doit restaurer l'état pré-dry-run, pas mettre 0 aveuglément si une relance MANUELLE in-app réelle a existé)
  </read_first>
  <action>
    SI option « accepter » : ne rien migrer — documenter dans le rapport « acceptation en l'état, aucune facture modifiée » et passer à Task 4.
    SINON créer `apps/web/scripts/_reset-burned-reminders.ts` :
    - DRY par défaut, `WRITE=1` pour exécuter (pattern migrate-storage).
    - Pour chaque facture ciblée (toutes celles du tableau B en reset-complet, ou la liste de Laurent en sélectif) : décrément = nombre d'entrées AuditLog `invoices.reminder_sent` avec `diff.dryRun=true` pour cette facture ; `reminderCount = max(0, reminderCount - décrément)` ; `lastReminderAt` = date de la dernière relance NON-dry-run (AuditLog `diff.dryRun` absent/false) ou `null` s'il n'y en a aucune.
    - AVANT écriture : snapshot des lignes touchées (id, number, reminderCount, lastReminderAt) imprimé + collé dans le rapport (réversibilité).
    - Chaque update loggé en AuditLog `action: 'invoices.reminder_reset'`, `diff: { reason: 'phase22-burned-dryrun', before, after }` (traçabilité).
    Exécuter DRY → coller la sortie dans le rapport → `WRITE=1` → re-exécuter `pending-reminders-report.ts` et vérifier que le tableau A post-remédiation correspond EXACTEMENT à la liste validée par Laurent.
    Commit : `fix(22-07): apply decided burned-reminders remediation with snapshot (Pitfall 1)`
  </action>
  <verify>
    <automated>pnpm tsx apps/web/scripts/pending-reminders-report.ts >/dev/null && grep -qE "reminder_reset|acceptation en l'état" ".planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md"</automated>
  </verify>
  <acceptance_criteria>
    - Si migration : script DRY→WRITE existe, snapshot avant collé au rapport, AuditLog invoices.reminder_reset écrits (requête de contrôle collée)
    - Le rapport post-remédiation (tableau A re-généré) = liste validée par Laurent, écart 0
    - Si « accepter » : mention explicite datée dans le rapport, aucun UPDATE exécuté
  </acceptance_criteria>
  <done>L'état des compteurs correspond à la décision de Laurent, snapshoté et réversible.</done>
</task>

<task type="auto">
  <name>Task 4: Flip MAIL_DRY_RUN=false sur Vercel ET Railway + preuve d'envoi réel (runbook §4)</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md (§4 et §9.4)
    - apps/web/src/lib/mailer.ts (isDryRun : MAIL_DRY_RUN==='true' OU SMTP_HOST vide — vérifier que SMTP_HOST/SMTP_PASS sont posés sur les 2 plateformes, sinon le flip est silencieusement sans effet)
  </read_first>
  <action>
    Pré-condition : Task 2 résolue (autorisation explicite) + Task 3 appliquée. Puis :
    ① Vérifier que `SMTP_HOST` (ssl0.ovh.net), `SMTP_PORT` (465), `SMTP_USER`, `SMTP_PASS` sont posés sur Vercel Production ET sur le service worker Railway (sans lire les valeurs sensitive : présence des clés suffit) — sinon poser depuis source assainie AVANT le flip (dry-run auto si SMTP_HOST vide !).
    ② Flip `MAIL_DRY_RUN` → `false` sur Vercel (Production) + redeploy.
    ③ Flip `MAIL_DRY_RUN` → `false` sur le service worker Railway (redeploy automatique — attendre le service healthy).
    ④ Preuve d'envoi réel SANS toucher un apprenant : script one-shot `pnpm tsx` appelant `sendMail({ to: 'laurent@start-academy.fr', subject: '[QualiOF] Test bascule prod — email réel', context: { tenantId, category: <catégorie du contrat 22-11> }, ... })` → attendu `{ ok: true, messageId }` SANS `dryRun` ; Laurent confirme réception. ⚠ Post-22-11 le garde applicatif est fail-closed : AVANT ce test, Laurent doit avoir activé dans Paramètres organisme → « Envois d'emails » l'interrupteur général (ou une session test applicable) ET la catégorie utilisée par le script — sinon l'envoi sera `suppressed` et la preuve échouera à tort (warning checker 22-11). Lire le 22-11-SUMMARY pour la signature exacte du context.
    ⑤ Rappel runbook §7 collé dans l'évidence : les relances réelles partiront au prochain cron 8h selon la liste validée ; la case « notifier les apprenants » du sync calendar est désormais réelle (Pitfall 4).
    Remplir §9.4 (captures des 2 flips + messageId + confirmation).
    Commit : `docs(22-07): MAIL_DRY_RUN=false on both platforms — evidence (D-06, CUT-02)`
  </action>
  <verify>
    <automated>grep -q "messageId" ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md" && grep -c "MAIL_DRY_RUN" ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md" | awk '$1>=2{print "OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - §9.4 contient : preuve des 2 flips (Vercel ET Railway — Pitfall 10), messageId de l'email test, confirmation de réception Laurent
    - Présence des 4 clés SMTP vérifiée sur les 2 plateformes AVANT le flip (tracé dans §9.4)
    - Aucun email vers un apprenant dans cette task (le test part vers laurent@start-academy.fr uniquement)
  </acceptance_criteria>
  <done>Sorties email réelles actives sur les 2 plateformes, prouvées, sans aucun envoi non validé.</done>
</task>

</tasks>

<verification>
- Séquence D-06 respectée dans l'ordre strict : rapport → décision → remédiation → flip → preuve.
- 0 envoi de masse ; 0 email apprenant non validé.
- Runbook §9.4 rempli.
</verification>

<success_criteria>
Les sorties réelles sont actives et sous contrôle : la liste exacte des prochaines relances est connue et validée, les compteurs sont sains, la chaîne SMTP prod est prouvée.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-07-SUMMARY.md`
</output>
