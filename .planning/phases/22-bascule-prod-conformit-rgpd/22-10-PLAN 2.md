---
phase: 22-bascule-prod-conformit-rgpd
plan: 10
type: execute
wave: 5
depends_on: ["22-07", "22-08", "22-09"]
files_modified:
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
autonomous: false
requirements: [CUT-01]

must_haves:
  truths:
    - "Un pg_dump d'archive du Postgres local ET un snapshot MinIO existent et sont vérifiés AVANT toute destruction (D-05)"
    - "La purge n'est exécutée qu'après présentation de la liste finale + mot de validation explicite de Laurent, en tours distincts (convention projet : destructif = étape séparée)"
    - "Après purge, l'app et le worker cloud fonctionnent toujours (rien ne dépendait du Docker local) — le Mac est définitivement hors de la boucle"
  artifacts:
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md"
      provides: "Liste finale, chemins d'archives + checksums, mot de validation tracé, preuve de purge"
  key_links:
    - from: "archives"
      to: "conteneur qualiof_postgres"
      via: "docker exec pg_dump (pg_dump absent du PATH host — fallback research)"
      pattern: "docker exec"
---

<objective>
Dernier plan de la phase, destructif et gaté (D-05) : archiver (pg_dump via docker exec + snapshot MinIO, checksums vérifiés), présenter la liste finale des éléments à purger, obtenir le mot de validation de Laurent, puis purger les conteneurs/volumes Docker locaux — en tours distincts.

Purpose: clore la migration v6 — le Mac de Laurent sort physiquement de la boucle, sans perte d'archive.
Output: archives vérifiées hors repo + purge prouvée + runbook §9.7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md
@docker-compose.yml

<interfaces>
docker-compose.yml local (5 services) : postgres (container `qualiof_postgres`, user qualiof, db qualiof), minio, gotenberg, weasyprint (+ ollama). Volumes : postgres_data, minio_data, ollama_data.
pg_dump/psql ABSENTS du PATH host (vérifié research) → `docker exec qualiof_postgres pg_dump -U qualiof -Fc qualiof`.
Baseline locale MinIO ~3109 objets (Phase 18) ; le local est FIGÉ au 2026-07-03 (prouvé 22-03, cloud = unique vérité).
⚠ Le `.env` racine pointe déjà le CLOUD (Phase 19) — la purge ne casse pas le dev, mais `dev:full` perdait déjà ses services locaux : à documenter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archiver (pg_dump + snapshot MinIO) et préparer la liste finale de purge</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md</files>
  <read_first>
    - docker-compose.yml (noms exacts des services/volumes/conteneurs)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md (verdict PASS — pré-condition absolue de la purge)
  </read_first>
  <action>
    AUCUNE DESTRUCTION dans cette task. Créer le dossier d'archives HORS repo : `~/QualiOF-archives-locales/` (jamais commité).
    ① Démarrer les conteneurs si nécessaire (`docker compose up -d postgres minio`).
    ② pg_dump d'archive : `docker exec qualiof_postgres pg_dump -U qualiof -Fc qualiof > ~/QualiOF-archives-locales/qualiof-local-final-$(date +%F).dump` — vérifier : taille > 1 Mo + `docker exec qualiof_postgres pg_restore --list` sur le dump (via docker cp ou stdin) sort la table de matières sans erreur.
    ③ Snapshot MinIO : identifier le volume réel (`docker volume ls | grep minio`), puis `docker run --rm -v <volume_minio>:/data:ro -v ~/QualiOF-archives-locales:/backup alpine tar czf /backup/minio-snapshot-$(date +%F).tar.gz -C /data .` — vérifier : `tar -tzf` liste > 3000 entrées (baseline ~3109 objets).
    ④ Checksums : `shasum -a 256` des 2 archives, consignés.
    ⑤ Écrire `22-LOCAL-DECOMMISSION.md` — section « Liste finale à purger » :
    - Conteneurs : qualiof_postgres, minio, gotenberg, weasyprint, ollama (noms réels relevés via `docker ps -a`)
    - Volumes : postgres_data, minio_data, ollama_data (noms réels via `docker volume ls`)
    - Dépendances vérifiées : l'app Vercel/worker Railway n'utilisent RIEN de local (URLs cloud partout) ; `.env` pointe le cloud depuis Phase 19 ; scripts `dev:full` locaux impactés (documentation du nouveau mode dev : services cloud)
    - Ce qui est CONSERVÉ : archives ~/QualiOF-archives-locales/ (chemins + checksums + tailles), `files/secrets/` (fallback Google dev), le repo
    Commit : `docs(22-10): local archives + final purge list — nothing destroyed yet (D-05)`
  </action>
  <verify>
    <automated>ls ~/QualiOF-archives-locales/qualiof-local-final-*.dump ~/QualiOF-archives-locales/minio-snapshot-*.tar.gz >/dev/null && grep -q "Liste finale" ".planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md" && docker ps -a --format '{{.Names}}' | grep -q qualiof_postgres</automated>
  </verify>
  <acceptance_criteria>
    - Les 2 archives existent hors repo avec checksums SHA-256 consignés ; pg_restore --list OK ; tar -tzf > 3000 entrées
    - 22-LOCAL-DECOMMISSION.md contient la liste finale (conteneurs + volumes réels) + dépendances vérifiées + section « conservé »
    - AUCUN conteneur ni volume supprimé (docker ps -a inchangé — la purge attend le mot de validation)
  </acceptance_criteria>
  <done>Tout est archivé et vérifié ; la liste finale est prête pour le gate — rien n'est détruit.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: GATE destructif — mot de validation de Laurent (D-05)</name>
  <action>PAUSE bloquante (convention projet : destructif = étape séparée). Présenter la liste finale de 22-LOCAL-DECOMMISSION.md (conteneurs, volumes, dépendances vérifiées, ce qui est conservé) + les checksums des archives. Attendre le mot exact « PURGE CONFIRMÉE » (+ option/exclusions). Ne RIEN détruire dans ce tour : l'exécution est la Task 3, tour distinct.</action>
  <decision>Autoriser (ou non) la purge définitive des conteneurs et volumes Docker locaux listés dans 22-LOCAL-DECOMMISSION.md.</decision>
  <context>Convention projet : destructif = étape séparée — liste finale + dépendances présentées, mot de validation explicite, exécution dans un tour distinct. Les archives (pg_dump + snapshot MinIO, checksums vérifiés) sont dans ~/QualiOF-archives-locales/. Le local est figé au 2026-07-03 et le cloud est l'unique vérité (audit 22-03). Après purge : plus AUCUN service QualiOF ne tourne sur le Mac ; le dev local utilisera les services cloud (déjà le cas pour la base depuis Phase 19). Ollama (~plusieurs Go) est inclus dans la liste — il n'est plus utilisé depuis la Phase 16.</context>
  <options>
    <option id="purge-totale">
      <name>Purge totale (5 conteneurs + 3 volumes, Ollama inclus)</name>
      <pros>Mac entièrement libéré (disque, RAM) ; fin propre de v6</pros>
      <cons>Rejouer un environnement local complet = re-provisionner docker compose + restore</cons>
    </option>
    <option id="purge-partielle">
      <name>Purge partielle (préciser ce qui est conservé, ex. garder gotenberg/weasyprint pour le dev PDF local)</name>
      <pros>Conserve un confort dev ponctuel</pros>
      <cons>Le Mac garde des services — la sortie de boucle est incomplète</cons>
    </option>
    <option id="reporter">
      <name>Reporter la purge</name>
      <pros>Zéro risque immédiat</pros>
      <cons>La phase se clôt avec une dette explicite (à tracer)</cons>
    </option>
  </options>
  <resume-signal>Écrire le MOT DE VALIDATION exact : « PURGE CONFIRMÉE » (+ option choisie et exclusions éventuelles). Sans ce mot exact, rien n'est détruit. L'exécution aura lieu dans un tour distinct (Task 3).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Exécuter la purge validée + preuve post-purge (tour distinct)</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md, .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md (liste validée + exclusions du checkpoint)
  </read_first>
  <action>
    PRÉ-CONDITION STRICTE : le mot « PURGE CONFIRMÉE » a été donné au checkpoint (tour précédent). Exécuter UNIQUEMENT la liste validée (respecter les exclusions) :
    ① Re-vérifier les archives une dernière fois (checksums identiques à ceux consignés).
    ② `docker compose down` puis suppression ciblée : `docker rm` des conteneurs listés, `docker volume rm` des volumes listés, `docker image prune` des images orphelines correspondantes (postgres:16-alpine, minio, gotenberg, weasyprint, ollama — selon liste).
    ③ Preuves post-purge : `docker ps -a` + `docker volume ls` ne montrent plus les éléments purgés ; espace disque récupéré (`docker system df` avant/après).
    ④ Preuve de non-régression cloud : `curl -s -o /dev/null -w "%{http_code}" https://qualiof.vercel.app/login` = 200 ET un `pnpm tsx apps/web/scripts/pending-reminders-report.ts` (lecture cloud) tourne — rien ne dépendait du local.
    ⑤ Compléter 22-LOCAL-DECOMMISSION.md (mot de validation cité, sorties avant/après, date) + runbook §9.7. Documenter le nouveau mode dev (services cloud ; `docker-compose.yml` conservé dans le repo pour re-provisionner si besoin).
    Commit : `chore(22-10): local Docker decommission executed after explicit validation (D-05, CUT-01)`
  </action>
  <verify>
    <automated>! docker ps -a --format '{{.Names}}' | grep -q qualiof_postgres && curl -s -o /dev/null -w "%{http_code}" https://qualiof.vercel.app/login | grep -q 200 && grep -q "PURGE CONFIRMÉE" ".planning/phases/22-bascule-prod-conformit-rgpd/22-LOCAL-DECOMMISSION.md"</automated>
  </verify>
  <acceptance_criteria>
    - Les éléments de la liste validée n'apparaissent plus dans docker ps -a / docker volume ls (sorties collées)
    - Le mot de validation exact est cité dans 22-LOCAL-DECOMMISSION.md avec la date du checkpoint
    - Preuve cloud post-purge : /login = 200 + un script lecture cloud fonctionne
    - Les archives et leurs checksums sont intacts (re-vérification tracée)
    - Runbook §9.7 rempli
  </acceptance_criteria>
  <done>Le Mac est hors de la boucle : plus aucun service QualiOF local, archives sûres, cloud intact — v6 clôturée côté infra.</done>
</task>

</tasks>

<verification>
- Séquence D-05 stricte respectée : archive → liste → mot de validation → purge, en tours distincts.
- Archives vérifiées 2× (avant checkpoint, avant purge).
- Zéro destruction sans le mot exact « PURGE CONFIRMÉE ».
</verification>

<success_criteria>
Dernier critère de CUT-01 (cutover proprement exécuté jusqu'au décommissionnement) : le local est purgé APRÈS gate validé, avec archives conservées — ou le report est tracé comme dette explicite.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-10-SUMMARY.md`
</output>
