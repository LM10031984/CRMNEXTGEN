---
phase: 22-bascule-prod-conformit-rgpd
plan: 11
type: execute
wave: 3
depends_on: ["22-06"]
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/*_tenant_email_settings/migration.sql
  - apps/web/src/lib/email-policy.ts
  - apps/web/src/lib/__tests__/email-policy.test.ts
  - apps/web/src/lib/mailer.ts
  - apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts
  - apps/web/src/lib/closure/worker.ts
  - apps/web/src/lib/lead-notifications.ts
  - apps/web/src/lib/__tests__/lead-notifications.test.ts
  - apps/web/src/server/actions/invoices.ts
  - apps/web/src/server/actions/preinscription-reminders.ts
  - apps/web/src/server/actions/dossier-reminder.ts
  - apps/web/src/server/actions/opco-submission.ts
  - apps/web/src/server/actions/tenant-users.ts
  - apps/web/src/server/actions/__tests__/send-reminder.test.ts
  - apps/web/src/server/actions/__tests__/tenant-users.test.ts
  - apps/web/src/lib/invoice-reminders/__tests__/*
  - apps/web/src/app/api/cron/preinscription-reminders/route.ts
  - apps/web/src/app/api/cron/opco-submission-reminders/route.ts
  - packages/shared/src/schemas/tenant.ts
  - packages/shared/src/schemas/index.ts
  - apps/web/src/server/actions/email-settings.ts
  - apps/web/src/components/parametres/email-settings-form.tsx
  - apps/web/src/app/app/parametres/page.tsx
autonomous: true
requirements: [CUT-02]

must_haves:
  truths:
    - "Par défaut (aucun réglage tenant saisi), AUCUN email applicatif ne part — même après MAIL_DRY_RUN=false : fail-closed, tout est décoché au déploiement"
    - "Laurent peut activer les emails sur une session test uniquement : interrupteur général OFF + catégorie cochée + session dans la liste test → seuls les emails rattachés à cette session partent"
    - "Tout email bloqué par les réglages est tracé « suppressed by settings » (destinataire masqué, D-17) et retourné comme dry-run au call-site — jamais d'erreur silencieuse"
    - "Un email supprimé (dry-run env OU réglages) ne consomme AUCUN compteur de relance (Invoice.reminderCount, PreEnrollment.reminderCount) — la classe Pitfall 1 est fermée à la racine"
    - "L'ADMIN pilote tout depuis Paramètres organisme → section « Envois d'emails » (interrupteur, cases par catégorie, sessions test)"
    - "Impossible d'ajouter un envoi non catégorisé : le contexte {tenantId, category} est REQUIS par le type de sendMail — tsc échoue sinon"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Modèle TenantEmailSettings (booleans default(false) + testSessionIds)"
      contains: "model TenantEmailSettings"
    - path: "apps/web/src/lib/email-policy.ts"
      provides: "Décision pure resolveEmailPolicy (matrice env × interrupteur × catégorie × session test) + type EmailCategory"
      exports: ["resolveEmailPolicy", "EmailCategory"]
    - path: "apps/web/src/lib/mailer.ts"
      provides: "Chokepoint unique : sendMail exige un contexte et applique la policy"
      contains: "suppressed"
    - path: "apps/web/src/server/actions/email-settings.ts"
      provides: "Server action ADMIN scopée tenantId, pattern { ok, error }, Zod"
      exports: ["updateEmailSettings"]
    - path: "apps/web/src/components/parametres/email-settings-form.tsx"
      provides: "UI section Envois d'emails"
  key_links:
    - from: "apps/web/src/lib/mailer.ts"
      to: "apps/web/src/lib/email-policy.ts"
      via: "resolveEmailPolicy(settings, ctx) après le check MAIL_DRY_RUN env"
      pattern: "resolveEmailPolicy"
    - from: "les 12 call-sites sendMail (10 fichiers)"
      to: "sendMail context"
      via: "champ requis { tenantId, category, sessionId? } — complétude garantie par tsc"
      pattern: "category:"
    - from: "apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts"
      to: "Invoice.reminderCount"
      via: "incrément CONDITIONNEL au départ réel (ok && !dryRun && !suppressed)"
      pattern: "reminderCount"
    - from: "apps/web/src/app/app/parametres/page.tsx"
      to: "apps/web/src/server/actions/email-settings.ts"
      via: "EmailSettingsForm → updateEmailSettings"
      pattern: "updateEmailSettings"
---

<objective>
Garde-fou applicatif des envois emails, en BDD par tenant (pas env) : interrupteur général `emailsEnabled` (défaut FALSE), toggles par catégorie, et « sessions autorisées en mode test » — le tout piloté depuis Paramètres organisme. La décision d'envoyer = MAIL_DRY_RUN env (plomberie) ET réglages tenant (métier), appliquée au chokepoint unique `mailer.ts`.

Purpose: demande explicite de Laurent — « pouvoir décocher les mails dans un premier temps et les activer par exemple sur une session test pour voir si tout est ok ». Ce plan DÉBLOQUE le 22-07 (suspendu) : le flip `MAIL_DRY_RUN=false` devient sans danger car la couche applicative reste fermée par défaut ; Laurent ouvre ensuite catégorie par catégorie, session par session.
Output: modèle `TenantEmailSettings` migré (migrate deploy), garde central testé TDD, 12 call-sites contextualisés, section UI Paramètres, mergé sur main via le même circuit CI que 22-06.

Périmètre : indépendant de 22-08/22-09/22-10. Le 22-07 (Task 4 flip) ne doit s'exécuter QU'APRÈS le merge+deploy de ce plan — à noter dans le SUMMARY pour l'orchestrateur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md
@apps/web/src/lib/mailer.ts
@apps/web/src/server/actions/invoice-settings.ts
@apps/web/src/components/parametres/invoice-settings-form.tsx

<interfaces>
<!-- Contrats extraits du codebase — l'executor n'a pas besoin d'explorer. -->

**mailer.ts actuel (chokepoint) :**
```typescript
export interface SendMailInput {
  to: string; subject: string; html: string; text?: string;
  attachments?: Array<{ filename: string; content: Buffer; contentType?: string }>;
}
export interface SendMailResult { ok: boolean; messageId?: string; dryRun?: boolean; error?: string; }
function isDryRun(): boolean; // MAIL_DRY_RUN==='true' OU SMTP_HOST vide
export async function sendMail(input: SendMailInput): Promise<SendMailResult>;
```

**Recensement EXHAUSTIF des 12 call-sites sendMail (10 fichiers) → 6 catégories** (vérifié par grep le 2026-08-03 — re-vérifier à l'exécution) :

| Call-site | Catégorie | tenantId dispo | sessionId dispo |
|---|---|---|---|
| `lib/invoice-reminders/invoice-reminder-core.ts:149` (cron Railway + action manuelle via core) | `invoice_reminder` | `tenantId` (local) | `invoice.sessionId ?? invoice.participant.sessionId` (ajouter `sessionId` aux selects) |
| `server/actions/invoices.ts:796` (relance manuelle historique 11-03) | `invoice_reminder` | `tenantId` | idem |
| `server/actions/preinscription-reminders.ts:106` | `preinscription_reminder` | `user.tenantId` | `pre.intendedSessionId ?? null` |
| `app/api/cron/preinscription-reminders/route.ts:104` (cron NON branché, D-06) | `preinscription_reminder` | `pre.tenantId` | `pre.intendedSessionId ?? null` |
| `server/actions/dossier-reminder.ts:139` | `opco_reminder` | `user.tenantId` | `p.session.id` (déjà chargé) |
| `app/api/cron/opco-submission-reminders/route.ts:94` (cron NON branché, D-06) | `opco_reminder` | `sub.tenantId` | `sub.participant.sessionId` (via include existant) |
| `server/actions/opco-submission.ts:256` (`sendOpcoSubmission`, PJ MinIO) | `opco_submission` | `user.tenantId` | `sub.participant.sessionId` (ajouter au findFirst) |
| `lib/closure/worker.ts:407` (notif pack → admin déclencheur) | `internal_notification` | `batch.tenantId` | `batch.sessionId` |
| `lib/lead-notifications.ts:102` (notif lead → commercial interne) | `internal_notification` | `opts.tenantId` | `null` |
| `server/actions/tenant-users.ts:161/321/445` (invitation / renvoi / reset) | `user_invitation` | `user.tenantId` | `null` |

**Aucune « convocation par email » n'existe** : convocations/docs = boutons de téléchargement PDF manuels (état vérifié D-06). Pas de catégorie fantôme à inventer.

**Modèle Prisma — pattern FK-less** (précédent `RevenueTarget` : « évite de toucher le model Tenant ») :
```prisma
model TenantEmailSettings {
  id                             String   @id @default(uuid())
  tenantId                       String   @unique
  emailsEnabled                  Boolean  @default(false)
  invoiceRemindersEnabled        Boolean  @default(false)
  preinscriptionRemindersEnabled Boolean  @default(false)
  opcoRemindersEnabled           Boolean  @default(false)
  opcoSubmissionsEnabled         Boolean  @default(false)
  internalNotificationsEnabled   Boolean  @default(false)
  userInvitationsEnabled         Boolean  @default(false)
  testSessionIds                 String[] @default([])
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @default(now()) @updatedAt
}
```

**Pattern server action Paramètres** (invoice-settings.ts) : `requireRole(['ADMIN'])` → Zod safeParse `{ ok:false, error, fieldErrors }` → `prisma.tenant...where: { id: user.tenantId }` → `logTenantSettingsChange({ action: 'parameters.update', diff: computeDiff(before, after) })` → `revalidatePath('/app/parametres')` → `{ ok: true }`.

**Pattern UI** : `SettingsSection` (components/settings/settings-section) enveloppant un form client RHF + Zod (clone `invoice-settings-form.tsx`), schéma partagé dans `packages/shared/src/schemas/` exporté par `index.ts`, toasts sonner, cva primitives.

**Pièges connus applicables :** `.env` racine pointe le CLOUD Supabase (toute migration = prod live) · `migrate deploy` obligatoire (feedback projet) · `dotenv-cli` non installé (Phase 19 : utiliser export shell ou `--env-file`) · mailer/worker : JAMAIS d'import React/auth · PRs cloud-migration→main = MERGE COMMIT jamais squash · `pnpm test` (dotenv) ≠ `pnpm vitest` direct.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modèle TenantEmailSettings + garde central TDD (matrice env × interrupteur × catégorie × session test)</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*_tenant_email_settings/migration.sql, apps/web/src/lib/email-policy.ts, apps/web/src/lib/__tests__/email-policy.test.ts, apps/web/src/lib/mailer.ts</files>
  <behavior>
    Fonction PURE `resolveEmailPolicy(settings: TenantEmailSettings | null, ctx: { category: EmailCategory; sessionId?: string | null }): { decision: 'send' | 'suppress'; reason?: 'no-settings' | 'category-off' | 'master-off' }` — testable sans mock prisma. Matrice (12+ cas) :
    - settings null (aucune ligne en BDD) → suppress/no-settings (fail-closed)
    - catégorie décochée + interrupteur ON → suppress/category-off
    - catégorie cochée + interrupteur ON → send
    - catégorie cochée + interrupteur OFF + sessionId absent → suppress/master-off
    - catégorie cochée + interrupteur OFF + sessionId ∈ testSessionIds → send (mode session test)
    - catégorie cochée + interrupteur OFF + sessionId ∉ testSessionIds → suppress/master-off
    - catégorie DÉCOCHÉE + interrupteur OFF + sessionId ∈ testSessionIds → suppress/category-off (les catégories cochées restent nécessaires même en mode test)
    - chaque catégorie mappe sur son boolean (test paramétré sur les 6 catégories)
    Côté `sendMail` (tests avec mock prisma, pattern vi.hoisted du projet) :
    - MAIL_DRY_RUN env actif → retour `{ ok:true, dryRun:true }` SANS lecture BDD (plomberie prioritaire, dev local intact)
    - policy suppress → retour `{ ok:true, dryRun:true, suppressed: true }` + log `[mailer:suppressed-by-settings] category=... reason=... to=<masqué>` — destinataire TOUJOURS masqué (D-17, réutiliser le masquage existant), jamais de throw
    - policy send → envoi SMTP normal (transporter inchangé)
  </behavior>
  <action>
    RED d'abord (commit `test(22-11): add failing guard matrix for email policy`), puis GREEN :

    1. **Schéma** : ajouter `model TenantEmailSettings` (bloc exact dans interfaces — tout à `default(false)`, `testSessionIds String[] @default([])`, commentaire de doc « Phase 22 Plan 22-11 D-06 : garde-fou applicatif envois — fail-closed par défaut »). Générer la migration ADDITIVE : `prisma migrate dev --create-only --name tenant_email_settings` (si le shadow DB bloque en sandbox : `prisma migrate diff --from-migrations --to-schema-datamodel --script` écrit à la main dans un dossier horodaté). Appliquer via `prisma migrate deploy` (DIRECT_URL :5432 du .env racine — convention projet, la base cloud est la prod live : migration 100 % additive, AUCUN impact sur l'existant), puis `prisma generate`. Leçon Phase 19 : dotenv-cli absent → exporter les URLs en shell depuis le .env racine.
    2. **`apps/web/src/lib/email-policy.ts`** (NOUVEAU, module neutre worker-safe — zéro import React/auth/env) : type `EmailCategory = 'invoice_reminder' | 'preinscription_reminder' | 'opco_reminder' | 'opco_submission' | 'internal_notification' | 'user_invitation'`, map catégorie→champ boolean, `resolveEmailPolicy` pure (logique de la matrice ci-dessus), export `EMAIL_CATEGORY_LABELS: Record<EmailCategory, string>` (libellés FR pour l'UI Task 3 : « Relances factures », « Rappels pré-inscription », « Relances dossiers OPCO », « Envoi dossiers OPCO », « Notifications internes (équipe) », « Invitations utilisateurs »).
    3. **`mailer.ts`** : `SendMailInput` gagne un champ REQUIS `context: { tenantId: string; category: EmailCategory; sessionId?: string | null }` (le type force l'exhaustivité des call-sites — tsc est le filet) ; `SendMailResult` gagne `suppressed?: boolean`. Ordre dans `sendMail` : ① `isDryRun()` env inchangé (retour immédiat, log dry-run existant enrichi de `category=`) ; ② sinon `prisma.tenantEmailSettings.findUnique({ where: { tenantId } })` (import `prisma` de `@qualiof/db` — même provenance que les libs worker existantes, pas de cache : volume d'emails faible, réglage pris en compte immédiatement) ; ③ `resolveEmailPolicy` ; ④ suppress → log + `{ ok:true, dryRun:true, suppressed:true }` ; ⑤ send → chemin SMTP existant intact. Doc-comment de tête mis à jour (2 couches : env = plomberie, BDD = métier).

    NE PAS toucher les call-sites dans cette task (Task 2) — la suite `email-policy.test.ts` passe au vert via vitest (qui ne type-checke pas le reste du repo) ; le tsc global redevient vert en Task 2.
    Commit GREEN : `feat(22-11): TenantEmailSettings model + central mail guard (fail-closed)`.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm test -- src/lib/__tests__/email-policy.test.ts && cd ../../packages/db && grep -q "model TenantEmailSettings" prisma/schema.prisma && ls prisma/migrations | grep -q "tenant_email_settings"</automated>
  </verify>
  <done>Matrice verte (12+ tests), `migrate deploy` = migration appliquée puis « No pending migrations », `migrate status` clean, guard branché au chokepoint avec contexte requis par le type.</done>
</task>

<task type="auto">
  <name>Task 2: Contextualiser les 12 call-sites + compteurs de relance conditionnels (fermeture Pitfall 1)</name>
  <files>apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts, apps/web/src/lib/closure/worker.ts, apps/web/src/lib/lead-notifications.ts, apps/web/src/server/actions/invoices.ts, apps/web/src/server/actions/preinscription-reminders.ts, apps/web/src/server/actions/dossier-reminder.ts, apps/web/src/server/actions/opco-submission.ts, apps/web/src/server/actions/tenant-users.ts, apps/web/src/app/api/cron/preinscription-reminders/route.ts, apps/web/src/app/api/cron/opco-submission-reminders/route.ts, apps/web/src/lib/__tests__/lead-notifications.test.ts, apps/web/src/server/actions/__tests__/send-reminder.test.ts, apps/web/src/server/actions/__tests__/tenant-users.test.ts, apps/web/src/lib/invoice-reminders/__tests__/*</files>
  <action>
    Adapter les 12 appels selon le tableau de recensement (bloc interfaces) : chaque `sendMail({...})` gagne `context: { tenantId, category, sessionId }` avec la catégorie et le sessionId indiqués. Ajustements de requêtes nécessaires : `invoice-reminder-core.ts` et `invoices.ts` ajoutent `sessionId` au select de `participant` (fallback `invoice.sessionId ?? invoice.participant?.sessionId ?? null` — champ facture groupée) ; `opco-submission.ts` ajoute `participant: { select: { sessionId: true } }` au findFirst. La complétude est PROUVÉE par `tsc --noEmit` (context requis → tout oubli casse la compil).

    **Compteurs conditionnels (fermeture durable de la classe Pitfall 1 — les compteurs brûlés du 22-07)** :
    - `invoice-reminder-core.ts` : l'update `{ lastReminderAt, reminderCount: increment }` ne s'exécute QUE si `mailResult.ok && !mailResult.dryRun` (env dry-run ET suppression réglages ne consomment plus de niveau). L'AuditLog `invoices.reminder_sent` continue de tracer CHAQUE tentative, avec `diff: { ..., dryRun, suppressedBySettings: mailResult.suppressed === true, counterConsumed: <bool> }` — le rapport 22-04/22-07 (`pending-reminders-report.ts`) reste lisible ; noter dans le SUMMARY que les entrées suppressed ne consomment plus de niveau (impact tableau B du 22-07 : structurellement vide à l'avenir).
    - `preinscription-reminders.ts` (action) + `app/api/cron/preinscription-reminders/route.ts` : `reminderCount/lastReminderSentAt` incrémentés uniquement sur départ réel (`r.ok && !r.dryRun`) ; sinon retourner/compter comme skipped avec raison.
    - `dossier-reminder.ts` + cron OPCO : même principe si un compteur est incrémenté (`sub.reminderCount`/`lastReminderSentAt`).
    - **NE PAS toucher les transitions de statut** (`sendOpcoSubmission` DRAFT→SENT etc.) : comportement dry-run existant conservé, le flag `dryRun/suppressed` remonte déjà dans le retour de l'action — scope minimal, seuls les COMPTEURS DE RELANCE deviennent conditionnels.
    - `lead-notifications.ts` : conserver la décision documentée « on ignore dryRun » (Pitfall 5 option b) pour l'AuditLog, mais passer le contexte (`internal_notification`, tenantId d'opts, sessionId null). Les toggles legacy `Tenant.notifyOnLeadAssignEmail/Bell` restent en place (ET logique : les deux couches doivent autoriser).

    Mettre à jour les mocks/assertions des tests existants qui stubbent `sendMail` (`lead-notifications.test.ts`, `send-reminder.test.ts`, `tenant-users.test.ts`, `invoice-reminders/__tests__/*`) : `expect.objectContaining` sur to/subject + nouveau champ context ; ajouter 1-2 cas « suppressed → compteur NON incrémenté » sur le core relances factures (le plus critique).
    Commit : `feat(22-11): thread email context through all 12 call-sites, conditional reminder counters`.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc --noEmit && pnpm test 2>&1 | tail -5 && ! grep -rn "sendMail({ to:" src --include="*.ts" | grep -v __tests__ | grep -q .</automated>
  </verify>
  <done>tsc exit 0 (aucun call-site sans contexte), suite web complète verte, aucun compteur de relance incrémenté sans départ réel d'email.</done>
</task>

<task type="auto">
  <name>Task 3: UI Paramètres « Envois d'emails » + server action + merge main (circuit CI 22-06)</name>
  <files>packages/shared/src/schemas/tenant.ts, packages/shared/src/schemas/index.ts, apps/web/src/server/actions/email-settings.ts, apps/web/src/components/parametres/email-settings-form.tsx, apps/web/src/app/app/parametres/page.tsx</files>
  <action>
    1. **Schéma Zod partagé** (`packages/shared/src/schemas/tenant.ts` + export index) : `EmailSettingsSchema` = 7 booleans + `testSessionIds: z.array(z.string().uuid()).max(20).default([])` ; type `EmailSettingsInput` inféré.
    2. **Server action** `apps/web/src/server/actions/email-settings.ts` (clone strict du pattern `invoice-settings.ts`) : `updateEmailSettings(input)` — `requireRole(['ADMIN'])`, safeParse `{ ok:false, error, fieldErrors }`, **upsert** `prisma.tenantEmailSettings.upsert({ where: { tenantId: user.tenantId }, create: {...}, update: {...} })` (scoping tenant strict — jamais d'id fourni par le client), `logTenantSettingsChange({ action: 'parameters.update', diff: computeDiff(before ?? {}, after) })`, `revalidatePath('/app/parametres')`, retour `{ ok: true }`.
    3. **Composant** `email-settings-form.tsx` (`'use client'`, clone `invoice-settings-form.tsx`) : ① interrupteur général « Activer les envois d'emails » avec hint explicite (« OFF : seules les sessions de test ci-dessous peuvent recevoir des emails, dans les catégories cochées ») ; ② 6 checkboxes de catégories (libellés `EMAIL_CATEGORY_LABELS`, hint par catégorie : qui reçoit — ⚠ mentionner « peut toucher un apprenant (règle payeur) » sur Relances factures) ; ③ sélecteur « Sessions autorisées en mode test » : liste de checkboxes sur les sessions passées en props (chips des sélectionnées) — pas de composant lourd, primitives cva/Tailwind existantes, fallback natif si Dialog Radix capricieux (leçon projet). RHF + safeParse client avant l'appel server, toasts sonner.
    4. **Page** `parametres/page.tsx` : charger `tenantEmailSettings` (nullable → défauts tout OFF) + sessions sélectionnables (`prisma.trainingSession.findMany` scopé tenant : id, code, startDate, product.title — 30 plus récentes UNION celles déjà dans testSessionIds pour ne jamais masquer une sélection) ; nouvelle `<SettingsSection title="Envois d'emails">` APRÈS la section Facturation, badge d'état (« Coupés — mode test » quand emailsEnabled=false / « Actifs » sinon) ; les sections existantes Phase 7/11 intactes.
    5. **Merge vers main — MÊME CIRCUIT CI QUE 22-06** : PR `cloud-migration` → `main`, gate CI `test` VERT obligatoire, **MERGE COMMIT jamais squash** (leçon 21-04), diff post-merge 0. Le push main déclenche Deploy (`prisma migrate deploy` = « No pending migrations », déjà appliquée en Task 1) + build Vercel ; **redéployer aussi le worker Railway** (le cron relances doit embarquer le garde) et vérifier service healthy. MAIL_DRY_RUN reste `true` partout — aucun email réel possible dans ce plan ; la levée env reste l'affaire exclusive du 22-07.
    Commit : `feat(22-11): email sending settings UI in Paramètres organisme (default all OFF)`.
  </action>
  <verify>
    <automated>cd "apps/web" && pnpm exec tsc --noEmit && pnpm test 2>&1 | tail -5 && grep -q "EmailSettingsForm" src/app/app/parametres/page.tsx && grep -q "upsert" src/server/actions/email-settings.ts && cd ../../packages/shared && pnpm test 2>&1 | tail -3</automated>
  </verify>
  <done>Section visible dans /app/parametres (ADMIN), sauvegarde upsert scopée tenant + AuditLog, PR mergée en merge commit avec CI verte, Vercel + worker Railway redéployés avec le garde actif, défauts = tout OFF.</done>
</task>

</tasks>

<verification>
- Fail-closed prouvé : sans ligne `TenantEmailSettings`, `resolveEmailPolicy` supprime tout (test) — le flip 22-07 ne peut provoquer aucun envoi applicatif tant que Laurent n'a rien coché.
- Mode session test prouvé par la matrice : master OFF + catégorie ON + session listée = SEUL chemin d'envoi.
- Pitfall 1 fermé : aucun `reminderCount` incrémenté sans départ réel (tests ciblés sur invoice-reminder-core).
- tsc = filet d'exhaustivité : 0 call-site sans catégorie.
- Suite complète verte (web + shared), migration `migrate deploy` clean, main = cloud-migration diff 0.
- SUMMARY : signaler à l'orchestrateur que 22-07 Task 4 (flip) est désormais débloquée ET conditionnée au déploiement effectif de ce plan (Vercel + Railway) ; impact sur le tableau B du rapport 22-07 (plus de brûlage possible).
</verification>

<success_criteria>
Laurent peut, depuis Paramètres organisme, tout décocher (état par défaut), puis activer une session test et les catégories voulues pour vérifier ses réglages — pendant que le reste du parc reste silencieux, même MAIL_DRY_RUN=false. Chaque suppression est tracée, aucun compteur n'est consommé à blanc, et aucun nouvel envoi ne peut être ajouté au code sans catégorie.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-11-SUMMARY.md`
</output>
