---
phase: 22-bascule-prod-conformit-rgpd
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/audit-data-gap.ts
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md
  - .planning/audit/STORAGE-REAUDIT-FINAL.md
autonomous: true
requirements: [CUT-01]

must_haves:
  truths:
    - "Il est PROUVÉ que le Postgres local Docker ne contient aucune donnée postérieure au dump du 2026-07-03 — le cloud peut être déclaré unique source de vérité (D-01)"
    - "Chaque clé storage référencée par la base cloud FINALE résout à un objet Supabase — 0 lien mort (D-02)"
    - "Aucune écriture sur la base locale ni re-dump (interdit D-01)"
  artifacts:
    - path: "apps/web/scripts/audit-data-gap.ts"
      provides: "Audit d'écart local↔cloud lecture seule, verdict machine-checkable (exit code)"
      min_lines: 60
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md"
      provides: "Rapport daté : comptages par table, max timestamps, verdict PASS + déclaration cloud=vérité"
    - path: ".planning/audit/STORAGE-REAUDIT-FINAL.md"
      provides: "Re-audit storage final : présents/manquants/orphelins, preuve 0 lien mort"
  key_links:
    - from: "apps/web/scripts/audit-data-gap.ts"
      to: "Postgres local Docker"
      via: "LOCAL_DATABASE_URL passé explicitement (jamais lu du .env — il pointe le CLOUD)"
      pattern: "LOCAL_DATABASE_URL"
    - from: ".planning/audit/STORAGE-REAUDIT-FINAL.md"
      to: "apps/web/scripts/migrate-storage.ts"
      via: "ré-exécution DRY + pattern d'écart 21-02"
      pattern: "migrate-storage"
---

<objective>
Les deux audits de données pré-bascule (Wave 1 du gate CUT-01) : ① audit d'écart local↔cloud (D-01) — script lecture seule comparant les 47 modèles, verdict « rien de postérieur au 2026-07-03 côté local » ; ② re-audit storage contre l'état FINAL de la base cloud (D-02) — ré-exécution de migrate-storage.ts DRY + audit d'écart pattern 21-02, WRITE uniquement si écart.

Purpose: sans ces preuves, « cloud = unique source de vérité » est une affirmation, pas un fait — et le pack témoin pourrait produire des faux verts storage.
Output: script réutilisable + 2 rapports datés, verdicts PASS requis pour ouvrir la fenêtre de bascule (runbook §0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md
@.planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md
@apps/web/scripts/migrate-storage.ts

<interfaces>
<!-- Contrats vérifiés 2026-07-06 -->

Postgres local Docker (docker-compose.yml) :
- container_name: `qualiof_postgres`, image postgres:16-alpine, port 5432
- credentials : user=qualiof / password=qualiof_dev / db=qualiof
- URL locale : `postgresql://qualiof:qualiof_dev@localhost:5432/qualiof`

⚠ Le `.env` racine pointe le CLOUD depuis la Phase 19 : `DATABASE_URL`/`DIRECT_URL` du .env = Supabase. L'URL LOCALE doit être passée explicitement (Pitfall 9).

PrismaClient avec URL explicite (Prisma 5) :
```typescript
new PrismaClient({ datasources: { db: { url: process.env.LOCAL_DATABASE_URL! } } })
```

Baseline storage (STORAGE-BACKFILL-REPORT-2026-07-06.md) : 899→902 clés cloud, toutes résolvent Supabase au 2026-07-06 ; `migrate-storage.ts` exporte `collectAllKeys` (réutilisé par l'audit d'écart 21-02) ; MinIO NON purgé.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script audit-data-gap.ts + exécution + rapport 22-DATA-GAP-AUDIT.md (D-01)</name>
  <files>apps/web/scripts/audit-data-gap.ts, .planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md</files>
  <read_first>
    - docker-compose.yml (credentials Postgres local : qualiof/qualiof_dev/qualiof, container qualiof_postgres)
    - packages/db/prisma/schema.prisma (survol : modèles avec/sans createdAt/updatedAt)
    - apps/web/scripts/_gen-session-pack.ts (en-tête seulement — conventions des scripts tsx du projet)
  </read_first>
  <action>
    Créer `apps/web/scripts/audit-data-gap.ts` — LECTURE SEULE STRICTE (aucun UPDATE/INSERT/DELETE, aucun `prisma.$executeRaw`) :

    - Usage documenté en tête : `LOCAL_DATABASE_URL=postgresql://qualiof:qualiof_dev@localhost:5432/qualiof CLOUD_DATABASE_URL=<DIRECT_URL du .env> pnpm tsx apps/web/scripts/audit-data-gap.ts`
    - Refuser de démarrer si `LOCAL_DATABASE_URL` absent OU s'il contient `supabase` (garde anti-inversion) ; refuser si `CLOUD_DATABASE_URL` absent.
    - `const DUMP_DATE = new Date('2026-07-03T23:59:59Z');`
    - 2 clients : `new PrismaClient({ datasources: { db: { url } } })` local et cloud.
    - Introspection via `$queryRawUnsafe` sur `information_schema.tables` (schema `public`, type BASE TABLE) + `information_schema.columns` pour détecter la présence de `createdAt`/`updatedAt` par table. Exclure `_prisma_migrations` du verdict timestamps (écart de comptage ATTENDU : local = 29 migrations archivées, cloud = baseline `0_init` — le noter comme informatif).
    - Par table : `SELECT count(*)::int` sur les DEUX bases (comptages exacts, jamais reltuples — Pitfall 9) + côté LOCAL uniquement : `max("createdAt")`, `max("updatedAt")` si colonnes présentes.
    - Verdict : **FAIL (process.exit(1))** si une table locale a `max(createdAt) > DUMP_DATE` OU `max(updatedAt) > DUMP_DATE`. **PASS (exit 0)** sinon. Les déltas cloud (comptages cloud > local) sont LISTÉS à titre informatif — ils sont ATTENDUS (E2E, backfill storage, régénérations post-dump) et ne font PAS échouer le verdict.
    - Sortie : markdown sur stdout (tableau {table, count_local, count_cloud, delta, max_local_createdAt, max_local_updatedAt, verdict_table}).

    Puis EXÉCUTER : démarrer le conteneur local si nécessaire (`docker compose up -d postgres` + attendre le healthcheck), lancer le script avec `CLOUD_DATABASE_URL` = valeur `DIRECT_URL` du `.env` racine, capturer la sortie dans `.planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md` avec : date d'exécution, commande exacte (URLs masquées), tableau complet, verdict final, et le paragraphe de déclaration : « Verdict PASS — aucune donnée locale postérieure au dump du 2026-07-03. La base cloud Supabase (projet gntlqyscahbgjrmsbzil, eu-west-1) est déclarée UNIQUE SOURCE DE VÉRITÉ (D-01). Le Postgres local Docker est obsolète et sera purgé au plan 22-10 après archive. »

    Si le verdict est FAIL : NE PAS déclarer cloud=vérité ; documenter les lignes fautives dans le rapport et remonter en déviation (décision Laurent requise — donnée locale à reporter à la main ou à abandonner).
    Commit : `feat(22-03): audit-data-gap script + local↔cloud gap report (D-01)`
  </action>
  <verify>
    <automated>LOCAL_DATABASE_URL=postgresql://qualiof:qualiof_dev@localhost:5432/qualiof CLOUD_DATABASE_URL="$DIRECT_URL" pnpm tsx apps/web/scripts/audit-data-gap.ts >/dev/null && grep -q "UNIQUE SOURCE DE VÉRITÉ" ".planning/phases/22-bascule-prod-conformit-rgpd/22-DATA-GAP-AUDIT.md"</automated>
  </verify>
  <acceptance_criteria>
    - Le script existe, grep "LOCAL_DATABASE_URL" et "DUMP_DATE" et "2026-07-03" présents ; grep négatif `$executeRaw`, `update(`, `create(`, `delete(` (lecture seule)
    - grep "supabase" dans la garde anti-inversion (refus si LOCAL_DATABASE_URL contient supabase)
    - Exit code 0 sur l'exécution réelle (verdict PASS) — sinon déviation documentée
    - 22-DATA-GAP-AUDIT.md contient : date, tableau ≥40 tables, verdict, déclaration cloud=vérité
    - `_prisma_migrations` marquée « informatif » dans le rapport (pas dans le verdict)
  </acceptance_criteria>
  <done>Verdict PASS machine-checkable + rapport daté ; cloud déclaré unique source de vérité, sans re-dump ni écriture.</done>
</task>

<task type="auto">
  <name>Task 2: Re-audit storage contre l'état FINAL + rapport (D-02)</name>
  <files>.planning/audit/STORAGE-REAUDIT-FINAL.md</files>
  <read_first>
    - apps/web/scripts/migrate-storage.ts (script DRY→WRITE idempotent, export `collectAllKeys` — NE PAS MODIFIER)
    - .planning/audit/STORAGE-BACKFILL-REPORT-2026-07-06.md (baseline 902/902, méthode d'audit d'écart 21-02 : script temporaire lecture seule réutilisant collectAllKeys + list Supabase paginé)
    - .planning/phases/21-app-vercel-filet-ci-tests/21-02-SUMMARY.md si présent (détail de la méthode)
  </read_first>
  <action>
    Re-jouer l'audit storage du 21-02 contre l'état FINAL de la base cloud (post-E2E 21-06, post-régénérations) :

    ① DRY : `pnpm tsx apps/web/scripts/migrate-storage.ts` (mode DRY par défaut, base = cloud via .env) — collecter : nombre de clés référencées par bucket (`qualiof-docs`, `preinscriptions`), clés invalides.
    ② Audit d'écart lecture seule (même méthode que 21-02) : script TEMPORAIRE réutilisant `collectAllKeys` exporté + `list` Supabase paginé (supabase-js service role, vars du .env) → compter présents / MANQUANTS (référencés en base mais absents de Supabase) / orphelins. Le script temporaire est SUPPRIMÉ après (comme en 21-02) — `migrate-storage.ts` : 0 modification.
    ③ SI manquants > 0 : `WRITE=1 pnpm tsx apps/web/scripts/migrate-storage.ts` (upsert idempotent, strictement séquentiel — JAMAIS de runs parallèles, leçon deadlocks) puis re-jouer ② → 0 manquant exigé.
    ④ SI manquants = 0 dès ② : pas de WRITE.
    ⑤ MinIO n'est PAS purgé (destructif = plan 22-10).

    Écrire `.planning/audit/STORAGE-REAUDIT-FINAL.md` (modèle : STORAGE-BACKFILL-REPORT-2026-07-06.md) : date, compteurs avant/WRITE éventuel/après, comparaison à la baseline 902 du 2026-07-06 (délta expliqué : nouveaux docs générés depuis), et la phrase littérale : « 0 lien mort — chaque clé référencée en base résout à un objet Supabase (état final pré-bascule). »
    Commit : `chore(22-03): final storage re-audit before cutover — 0 dead links (D-02)`
  </action>
  <verify>
    <automated>grep -q "0 lien mort" ".planning/audit/STORAGE-REAUDIT-FINAL.md" && grep -q "MinIO" ".planning/audit/STORAGE-REAUDIT-FINAL.md"</automated>
  </verify>
  <acceptance_criteria>
    - STORAGE-REAUDIT-FINAL.md existe, daté, avec compteurs présents/manquants/orphelins et manquants=0 en état final
    - La phrase « 0 lien mort — chaque clé référencée en base résout à un objet Supabase » est présente littéralement
    - Mention explicite « MinIO NON purgé » (purge = plan 22-10)
    - `git diff apps/web/scripts/migrate-storage.ts` vide (0 modification du script)
    - Si WRITE a couru : le rapport contient le compteur d'objets copiés + preuve du re-audit post-WRITE
  </acceptance_criteria>
  <done>État final storage prouvé 0 lien mort, comparé à la baseline, rapport commité — le pack témoin ne peut plus produire de faux verts storage.</done>
</task>

</tasks>

<verification>
- `pnpm tsx apps/web/scripts/audit-data-gap.ts` (avec les 2 URLs) exit 0.
- Les 2 rapports existent, datés, avec verdicts explicites.
- Aucune écriture locale, aucun re-dump, migrate-storage.ts intact.
</verification>

<success_criteria>
Les 2 pré-requis data du runbook §0 sont PROUVÉS : local figé au 2026-07-03 (cloud=vérité) et storage final 0 lien mort. La fenêtre de bascule peut s'ouvrir côté données.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-03-SUMMARY.md`
</output>
