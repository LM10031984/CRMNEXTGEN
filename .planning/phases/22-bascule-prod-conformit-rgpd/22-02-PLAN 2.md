---
phase: 22-bascule-prod-conformit-rgpd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/calendar/__tests__/google-client.test.ts
  - apps/web/src/lib/calendar/google-client.ts
  - packages/shared/src/env.ts
  - turbo.json
  - .env.example
  - apps/web/src/lib/mailer.ts
  - apps/web/src/lib/closure/worker.ts
  - apps/web/src/server/actions/ai-fill-product.ts
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-PII-LOGS-AUDIT.md
autonomous: true
requirements: [CUT-01, RGPD-01]

must_haves:
  truths:
    - "Le sync Google Calendar fonctionne sur Vercel sans le Mac de Laurent : credentials lus depuis l'env, fallback fichiers pour le dev local (D-07)"
    - "Aucun console.* du worker/generators ne loggue de PII brut (email, nom, CNI, RIB) — critère RGPD-01"
    - "Le label d'erreur périmé « Erreur Ollama » est corrigé (D-18 ③)"
  artifacts:
    - path: "apps/web/src/lib/calendar/google-client.ts"
      provides: "loadOAuthConfig() env-first avec fallback files/secrets/, module toujours worker-safe"
      contains: "loadOAuthConfig"
    - path: "packages/shared/src/env.ts"
      provides: "3 vars serveur optionnelles GOOGLE_OAUTH_CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN"
      contains: "GOOGLE_OAUTH_REFRESH_TOKEN"
    - path: "apps/web/src/lib/calendar/__tests__/google-client.test.ts"
      provides: "Tests hermétiques env-first + fallback fichier + all-or-nothing"
      min_lines: 40
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-PII-LOGS-AUDIT.md"
      provides: "Audit D-17 : liste des console.* scannés, verdicts, corrections"
  key_links:
    - from: "apps/web/src/lib/calendar/google-client.ts"
      to: "@qualiof/shared/env"
      via: "import sharedEnv (worker-safe, déjà importé au boot worker)"
      pattern: "from '@qualiof/shared/env'"
    - from: "apps/web/src/lib/mailer.ts"
      to: "logs Railway"
      via: "email destinataire masqué en dry-run"
      pattern: "maskEmail|\\$\\{masked"
---

<objective>
Le seul vrai code de la phase : (1) portage des credentials Google Calendar en variables d'env — 3 valeurs, pas 1 (Pitfall 3) — avec fallback fichiers pour le dev local, en TDD ; (2) audit D-17 des `console.*` du worker/generators + corrections ciblées des logs PII (`mailer.ts:79`, `closure/worker.ts:409`) ; (3) fix cosmétique du label « Erreur Ollama » (D-18 ③).

Purpose: sans le portage Google, le premier sync calendar post-bascule crashe en `ENOENT .../secrets/oauth-client.json` sur Vercel ; sans les fixes logs, des emails apprenants/payeurs partent en clair dans les logs Railway chaque jour.
Output: code mergeable (suite verte, tsc exit 0) + rapport d'audit PII.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-RESEARCH.md
@apps/web/src/lib/calendar/google-client.ts
@packages/shared/src/env.ts

<interfaces>
<!-- État actuel vérifié 2026-07-06 — l'exécuteur implémente contre ces contrats. -->

From apps/web/src/lib/calendar/google-client.ts (actuel) :
```typescript
const SECRETS_DIR = path.resolve(process.cwd(), '../../secrets');
export function getCalendarClient(): calendar_v3.Calendar {
  // lit oauth-client.json → { client_id, client_secret } via clientRaw.installed ?? clientRaw.web ?? clientRaw
  // lit google-token.json → { refresh_token }
  // new google.auth.OAuth2(client_id, client_secret) + setCredentials({ refresh_token })
}
```
⚠ RÈGLE WORKER (header du fichier) : ce module ne doit importer que node:fs, node:path, googleapis — `sharedEnv` de `@qualiof/shared/env` est ACCEPTABLE (le worker l'importe déjà au boot), JAMAIS de module auth-gated/React.

From apps/web/src/lib/mailer.ts:79 (à corriger) :
```typescript
console.log(`[mailer:dry-run] to=${input.to} subject="${input.subject}" (no SMTP_HOST configuré)`);
```

From apps/web/src/lib/closure/worker.ts:409 (à corriger) :
```typescript
console.log(`[closure-worker] ✉ notif sent to ${user.email} (batch=${batch.id}, status=${finalStatus})`);
```

From apps/web/src/server/actions/ai-fill-product.ts:297 (à corriger) :
```typescript
return { ok: false, error: `Erreur Ollama : ${e?.message ?? e}` };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tests hermétiques loadOAuthConfig (env-first, fallback fichier, all-or-nothing)</name>
  <files>apps/web/src/lib/calendar/__tests__/google-client.test.ts</files>
  <read_first>
    - apps/web/src/lib/calendar/google-client.ts (état actuel : 2 readFileSync, pas de loadOAuthConfig)
    - apps/web/scripts/__tests__/cron-workers.test.ts (pattern existant de mock `@qualiof/shared/env` via getter + vi.hoisted — À RÉPLIQUER)
    - apps/web/src/lib/calendar/__tests__/sync-session.test.ts (conventions de mock du dossier)
  </read_first>
  <behavior>
    - Test 1 : les 3 vars env posées (`GOOGLE_OAUTH_CLIENT_ID='id-env'`, `GOOGLE_OAUTH_CLIENT_SECRET='secret-env'`, `GOOGLE_OAUTH_REFRESH_TOKEN='rt-env'`) → `loadOAuthConfig()` retourne `{ client_id:'id-env', client_secret:'secret-env', refresh_token:'rt-env' }` et `fs.readFileSync` n'est JAMAIS appelé
    - Test 2 : les 3 vars env `undefined` → fallback fichiers : `readFileSync` appelé sur un chemin finissant par `oauth-client.json` (mock retourne `{"installed":{"client_id":"id-file","client_secret":"secret-file"}}`) puis `google-token.json` (mock retourne `{"refresh_token":"rt-file"}`) → retourne les valeurs fichier
    - Test 3 : env PARTIEL (seul `GOOGLE_OAUTH_REFRESH_TOKEN` posé) → all-or-nothing : fallback fichiers complet (les 3 valeurs viennent des fichiers, pas de mélange env/fichier)
    - Test 4 : fallback fichiers avec format `web` (`{"web":{...}}`) → résolu comme `installed` (cascade `installed ?? web ?? racine` préservée)
  </behavior>
  <action>
    Créer `apps/web/src/lib/calendar/__tests__/google-client.test.ts` (Vitest). Mocks hermétiques :
    - `vi.mock('@qualiof/shared/env', ...)` avec un objet `sharedEnv` contrôlable par test (pattern getter/vi.hoisted de `cron-workers.test.ts` — les tests ne chargent JAMAIS le vrai createEnv, pas de .env requis)
    - `vi.mock('node:fs')` : `readFileSync` spyable retournant les JSON ci-dessus selon le nom de fichier demandé
    - `vi.mock('googleapis')` minimal si l'import du module le requiert (le test cible `loadOAuthConfig`, pas `getCalendarClient`)
    - `vi.resetModules()` entre les tests (l'import du module est dynamique par test pour re-lire le mock env)
    Les tests importent `{ loadOAuthConfig }` depuis `../google-client` — la fonction N'EXISTE PAS ENCORE : la suite doit être ROUGE.
    Commit : `test(22-02): add failing tests for google-client env-first loadOAuthConfig`
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/google-client.test.ts 2>&1 | grep -q "fail" && echo "RED OK"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier de test existe avec 4 tests (grep -c "it(" >= 4)
    - `pnpm vitest run src/lib/calendar/__tests__/google-client.test.ts` échoue (loadOAuthConfig introuvable) — état RED prouvé
    - Aucun test ne lit le vrai `.env` ni les vrais fichiers `files/secrets/` (grep négatif "files/secrets" hors chaînes mockées)
  </acceptance_criteria>
  <done>4 tests rouges décrivant le contrat env-first/fallback/all-or-nothing, commités.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — loadOAuthConfig + 3 vars env.ts + turbo.json + .env.example</name>
  <files>apps/web/src/lib/calendar/google-client.ts, packages/shared/src/env.ts, turbo.json, .env.example</files>
  <read_first>
    - apps/web/src/lib/calendar/google-client.ts (fichier modifié — préserver CALENDAR_ID, mémoïsation _client, règle worker-safe du header)
    - packages/shared/src/env.ts (bloc server + bloc runtimeEnv — répliquer le pattern exact des clés optionnelles existantes comme SUPABASE_URL)
    - turbo.json (bloc globalEnv)
    - .env.example (format des blocs commentés)
  </read_first>
  <behavior>
    - Les 4 tests de Task 1 passent (GREEN)
    - `getCalendarClient()` utilise `loadOAuthConfig()` (une seule source de credentials)
  </behavior>
  <action>
    ① `packages/shared/src/env.ts` — bloc `server`, ajouter (après le bloc Cloud v6, avec commentaire) :
    ```typescript
    // Google Calendar OAuth (Phase 22 D-07) — portage cloud ; fallback files/secrets/ en dev local
    GOOGLE_OAUTH_CLIENT_ID: z.string().optional(),
    GOOGLE_OAUTH_CLIENT_SECRET: z.string().optional(),
    GOOGLE_OAUTH_REFRESH_TOKEN: z.string().optional(),
    ```
    et dans le bloc `runtimeEnv` (ou équivalent existant) les 3 mappings `GOOGLE_OAUTH_X: process.env.GOOGLE_OAUTH_X`.

    ② `apps/web/src/lib/calendar/google-client.ts` :
    - Ajouter `import { sharedEnv } from '@qualiof/shared/env';` (autorisé worker-safe — mettre à jour le doc-comment du header qui liste les imports permis)
    - Exporter :
    ```typescript
    export function loadOAuthConfig(): { client_id: string; client_secret: string; refresh_token: string } {
      const { GOOGLE_OAUTH_CLIENT_ID, GOOGLE_OAUTH_CLIENT_SECRET, GOOGLE_OAUTH_REFRESH_TOKEN } = sharedEnv;
      if (GOOGLE_OAUTH_CLIENT_ID && GOOGLE_OAUTH_CLIENT_SECRET && GOOGLE_OAUTH_REFRESH_TOKEN) {
        return { client_id: GOOGLE_OAUTH_CLIENT_ID, client_secret: GOOGLE_OAUTH_CLIENT_SECRET, refresh_token: GOOGLE_OAUTH_REFRESH_TOKEN };
      }
      // Fallback dev local : files/secrets/ (comportement historique inchangé)
      const clientRaw = JSON.parse(fs.readFileSync(path.join(SECRETS_DIR, 'oauth-client.json'), 'utf8'));
      const { client_id, client_secret } = clientRaw.installed ?? clientRaw.web ?? clientRaw;
      const token = JSON.parse(fs.readFileSync(path.join(SECRETS_DIR, 'google-token.json'), 'utf8'));
      return { client_id, client_secret, refresh_token: token.refresh_token };
    }
    ```
    - `getCalendarClient()` : remplacer les 2 readFileSync par `const { client_id, client_secret, refresh_token } = loadOAuthConfig();` — mémoïsation `_client` et `CALENDAR_ID` inchangés.

    ③ `turbo.json` — ajouter les 3 clés `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `GOOGLE_OAUTH_REFRESH_TOKEN` à `globalEnv`.

    ④ `.env.example` — bloc commenté :
    ```
    # Google Calendar OAuth (Phase 22 D-07) — prod cloud : 3 vars (sensitive sur Vercel).
    # En dev local : laisser vides, fallback automatique sur files/secrets/oauth-client.json + google-token.json.
    # GOOGLE_OAUTH_CLIENT_ID=
    # GOOGLE_OAUTH_CLIENT_SECRET=
    # GOOGLE_OAUTH_REFRESH_TOKEN=
    ```
    ⚠ JAMAIS de valeur réelle dans .env.example ni dans le repo.
    Commit : `feat(22-02): google-client env-first credentials with file fallback (D-07)`
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/google-client.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 4/4 tests google-client verts ; `tsc --noEmit` exit 0 (apps/web ET packages/shared)
    - grep "GOOGLE_OAUTH_REFRESH_TOKEN" présent dans : env.ts (×2 : schema + runtimeEnv), turbo.json, .env.example
    - grep "loadOAuthConfig" dans google-client.ts (export) ; `getCalendarClient` ne contient plus de `readFileSync` direct (les 2 readFileSync vivent dans loadOAuthConfig)
    - grep négatif : aucun import auth-gated/React ajouté dans google-client.ts (seuls node:fs, node:path, googleapis, @qualiof/shared/env)
    - Suite calendar existante toujours verte : `pnpm vitest run src/lib/calendar/`
  </acceptance_criteria>
  <done>Portage D-07 fonctionnel : env-first sur Vercel, fallback fichiers en dev, tests verts.</done>
</task>

<task type="auto">
  <name>Task 3: Audit D-17 des console.* PII + corrections ciblées + label Ollama</name>
  <files>apps/web/src/lib/mailer.ts, apps/web/src/lib/closure/worker.ts, apps/web/src/server/actions/ai-fill-product.ts, .planning/phases/22-bascule-prod-conformit-rgpd/22-PII-LOGS-AUDIT.md</files>
  <read_first>
    - apps/web/src/lib/mailer.ts (ligne ~79, fonction sendMail)
    - apps/web/src/lib/closure/worker.ts (ligne ~409, notifyBatchCompletion)
    - apps/web/src/server/actions/ai-fill-product.ts (ligne ~297)
  </read_first>
  <action>
    ① Scan exhaustif du périmètre D-17 :
    ```bash
    grep -rn "console\." apps/web/src/lib/closure apps/web/src/lib/veille apps/web/src/lib/invoice-reminders apps/web/src/lib/calendar apps/web/src/lib/preinscription-extractor.ts apps/web/src/lib/mailer.ts apps/web/scripts/*worker*.ts
    ```
    Pour chaque occurrence, verdict : OK (loggue id/code session/compteur), PII (interpole email, firstName, lastName, nom complet, ribKey/CNI, adresse) → à corriger, ou JUSTIFIÉ (expliquer).

    ② Corrections connues (research) :
    - `mailer.ts:79` — remplacer par :
    ```typescript
    const maskedTo = String(input.to).replace(/^(.)[^@]*(@.+)$/, '$1***$2');
    console.log(`[mailer:dry-run] to=${maskedTo} subject="${input.subject}" (no SMTP_HOST configuré)`);
    ```
    - `closure/worker.ts:409` — remplacer `notif sent to ${user.email}` par `notif sent to user=${user.id}` (l'objet `user` porte un id — vérifier le select en amont ; sinon logger uniquement `batch.id`).
    - Toute autre occurrence PII trouvée au scan ① : même règle — logger un id, jamais la valeur.

    ③ D-18 ③ (cosmétique) : `ai-fill-product.ts:297` — `Erreur Ollama :` → `Erreur IA :` (le provider est OpenRouter depuis Phase 16).

    ④ Écrire `.planning/phases/22-bascule-prod-conformit-rgpd/22-PII-LOGS-AUDIT.md` : tableau {fichier:ligne, extrait, verdict OK/CORRIGÉ/JUSTIFIÉ, correction appliquée}, + la commande de scan reproductible, + compteur final « occurrences PII restantes = 0 ».

    ⑤ Si un test existant assertait le format exact des logs corrigés → adapter le test (Rule 3, consigner en déviation).
    Commit : `fix(22-02): mask PII in worker/mailer logs + stale Ollama label (D-17, D-18)`
  </action>
  <verify>
    <automated>! grep -rn 'console\.[a-z]*(.*\${input\.to}' apps/web/src/lib/mailer.ts && ! grep -rn 'sent to \${user\.email}' apps/web/src/lib/closure/worker.ts && ! grep -n "Erreur Ollama" apps/web/src/server/actions/ai-fill-product.ts && cd apps/web && pnpm vitest run src/lib/mailer 2>/dev/null; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep négatif `\${input.to}` dans un console.* de mailer.ts (le log utilise maskedTo)
    - grep négatif `user.email` dans les console.* de closure/worker.ts
    - grep négatif "Erreur Ollama" dans ai-fill-product.ts ; grep "Erreur IA" présent
    - 22-PII-LOGS-AUDIT.md existe avec la commande de scan, ≥1 tableau, et la ligne « occurrences PII restantes = 0 »
    - Suite complète verte : `pnpm --filter @qualiof/web test` (1176+ tests, ajustés si formats de logs assertés) + `tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Audit D-17 documenté, 0 console.* avec PII brut dans le périmètre worker/generators, label IA corrigé.</done>
</task>

</tasks>

<verification>
- `pnpm test` (turbo, 3 tâches) vert ; `tsc --noEmit` exit 0 web+shared.
- grep gate RGPD-01 : `grep -rn "console\." <périmètre D-17> | grep -iE "\\$\\{[a-z]*\\.(email|firstName|lastName)"` → 0 occurrence non justifiée.
- Aucun secret réel commité (grep des valeurs de files/secrets/ dans le diff = 0).
</verification>

<success_criteria>
- D-07 : sync calendar déployable sur Vercel (env-first prouvé par tests, fallback dev intact).
- RGPD-01 (partie logs) : audit écrit, corrections appliquées, 0 PII brut.
- Code prêt pour la PR cloud-migration→main du plan 22-06 (MERGE COMMIT).
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-02-SUMMARY.md`
</output>
