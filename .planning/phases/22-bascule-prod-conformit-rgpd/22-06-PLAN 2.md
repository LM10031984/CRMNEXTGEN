---
phase: 22-bascule-prod-conformit-rgpd
plan: 06
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03", "22-04", "22-05"]
files_modified:
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-GONOGO-SES-0094.md
autonomous: false
requirements: [CUT-01, CUT-02]

must_haves:
  truths:
    - "L'app tourne en NEXT_PUBLIC_APP_ENV=production sur https://qualiof.vercel.app : plus de bandeau STAGING, PDF sans filigrane (D-08), MAIL_DRY_RUN toujours true"
    - "Les 3 vars Google sont posées (sensitive) sur Vercel — le sync calendar fonctionne sans le Mac (D-07)"
    - "Le pack témoin SES-0094 régénéré post-bascule est le gate go/no-go : 0 stub, footer 22 vars OF_*, docs Qualiopi conformes, aucun 404 sur les preuves (D-10)"
    - "En cas de NO-GO, le rollback D-04 (re-flag staging ~5 min) est appliqué"
  artifacts:
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-GONOGO-SES-0094.md"
      provides: "Preuves datées du gate : compteurs stub, footer, curl signed URLs, filigrane absent"
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md"
      provides: "§9.1–9.3 evidence remplies"
  key_links:
    - from: "Vercel env NEXT_PUBLIC_APP_ENV=production"
      to: "filigrane + bandeau + garde calendar"
      via: "levée automatique des gardes (aucun code, D-08)"
      pattern: "NEXT_PUBLIC_APP_ENV"
    - from: "22-GONOGO-SES-0094.md"
      to: "Document.usedStub"
      via: "vérif Prisma usedStub=false sur tous les docs du pack"
      pattern: "usedStub"
---

<objective>
Exécuter la bascule (runbook §0–§3) : pre-flight gate, merge du code 22-02 sur main, sanity/re-pose env, pose des 3 vars Google, flip `NEXT_PUBLIC_APP_ENV=production`, puis pack témoin SES-0094 en gate go/no-go. `MAIL_DRY_RUN` reste `true` partout — les sorties réelles sont au plan 22-07.

Purpose: c'est LA bascule — encadrée par le runbook écrit en 22-01 et gatée par le témoin habituel.
Output: prod live gardée (emails encore dry-run), preuves §9.1–9.3, verdict GO/NO-GO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-ENV-SANITY.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md

<interfaces>
Vercel : projet `qualiof` (prj_uI2HKJRGchDOXkI7fKuX9ckpfyY5, team laurents-projects-3806ab87), production branch `main`, URL https://qualiof.vercel.app. ⚠ NEXT_PUBLIC_* inliné au BUILD → redeploy obligatoire après changement.
Git : PRs cloud-migration→main = MERGE COMMIT, JAMAIS squash (leçon 21-04 — le squash fait diverger les branches). Gate CI `test` en branch protection.
Secrets Google : `files/secrets/oauth-client.json` → champs `installed.client_id` / `installed.client_secret` (ou `web.`) ; `files/secrets/google-token.json` → champ `refresh_token`. Pose en SENSITIVE via valeur PROPRE (jamais une ligne .env brute avec commentaire — leçon PROD-0674 ; poser via stdin/API avec la valeur seule).
Pack témoin SES-0094 : protocole Phases 20/21 — régénération via worker Railway (script `_gen-session-pack.ts` ou bouton pack), vérifs : Prisma `usedStub=false` sur tous les Document du pack, footer 22 vars OF_*, curl des signed URLs → 200 + `%PDF-`, positionnement varié (v11).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight GO (runbook §0) + autorisation d'exécution</name>
  <action>PAUSE bloquante AVANT toute mutation. Présenter la checklist §0 du runbook item par item avec preuve (rapports 22-03/22-04, gate D-13 du 22-05, statut Phase 20). Attendre le « GO bascule » explicite + l'autorisation d'exécuter les opérations Vercel/GitHub via API/CLI (pattern 21-04). Sans GO, aucune task suivante ne démarre.</action>
  <what-built>Tous les gates de Wave 1 sont censés être verts : runbook écrit (22-01), code D-07/D-17 prêt (22-02), audit data PASS + storage 0 lien mort (22-03), sanity env fait (22-04), registre RGPD VALIDÉ (22-05, gate D-13). Reste une condition externe : la clôture de la Phase 20 (relevé stabilité 24 h Railway attendu 2026-07-07 ~08h45 + verify-work 20).</what-built>
  <how-to-verify>
    1. Confirmer que la Phase 20 est clôturée (relevé 24 h fait, verify-work 20 passé).
    2. Parcourir la checklist §0 du runbook `22-CUTOVER-RUNBOOK.md` — Claude présente l'état de chaque item avec sa preuve.
    3. Décision : ouvrir la fenêtre de bascule MAINTENANT (D-03 : dès que prêt, pas de contrainte calendrier).
    4. Autoriser explicitement Claude à exécuter les opérations Vercel/GitHub via API/CLI (pattern 21-04 : délégation sur autorisation explicite).
  </how-to-verify>
  <resume-signal>Répondre « GO bascule » (ou lister les items §0 manquants — la fenêtre reste fermée).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Exécuter runbook §1–§2 — merge main, env assaini, 3 vars Google, flip production</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md (§1, §2, §9.1, §9.2 — la procédure à suivre)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-ENV-SANITY.md (clés polluées éventuelles à re-poser)
    - files/secrets/oauth-client.json + files/secrets/google-token.json (valeurs à poser — JAMAIS committées, JAMAIS loggées)
  </read_first>
  <action>
    Dérouler dans l'ordre (chaque étape prouvée avant la suivante) :
    ① **PR cloud-migration→main** : `gh pr create` puis merge en **MERGE COMMIT** (`gh pr merge --merge`, jamais --squash) après gate CI `test` vert. Vérifier post-merge : `git diff origin/main origin/cloud-migration` vide.
    ② **Re-poses env** (§1) : si 22-ENV-SANITY.md liste des clés polluées → re-poser chacune sur Vercel (environment Production) avec la VALEUR SEULE assainie (parser dotenv, strip commentaire inline, trim) — passer par stdin/API, pas de copier-coller de ligne .env.
    ③ **Pose des 3 vars Google** (sensitive, Production) : `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET` (depuis oauth-client.json champ installed/web), `GOOGLE_OAUTH_REFRESH_TOKEN` (depuis google-token.json). Sanity check de chaque valeur AVANT pose (regex `[^\x20-\x7E]|#| +$` = 0 match). Les fichiers `files/secrets/` restent en place (fallback dev, D-07).
    ④ **Flip** : `NEXT_PUBLIC_APP_ENV` = `production` (Production). `MAIL_DRY_RUN` reste `true`. ⚠ NE PAS toucher `MAIL_DRY_RUN` dans cette task.
    ⑤ **Redeploy production** (NEXT_PUBLIC_* inliné au build) et attendre READY.
    ⑥ **Preuves** → runbook §9.2 : `curl -s https://qualiof.vercel.app/login` → 200 + grep bandeau STAGING = 0 occurrence ; login e2e@start-academy.fr fonctionne (ou preuve cookie) ; `NEXT_PUBLIC_APP_ENV` lisible = production ; auto-fill IA produit re-testé OK (preuve comportementale sensitive, §9.1).
    Remplir §9.1 et §9.2 du runbook avec sorties datées.
    Commit : `docs(22-06): cutover §1-§2 executed — production flag live, evidence (CUT-01)`
  </action>
  <verify>
    <automated>curl -s https://qualiof.vercel.app/login | grep -c "STAGING" | grep -q "^0$" && echo "OK: no staging banner"</automated>
  </verify>
  <acceptance_criteria>
    - PR mergée en merge commit (gh pr view → mergedAt non nul, mergeCommit non nul) ; diff main↔cloud-migration = 0
    - `curl -s https://qualiof.vercel.app/login` retourne 200 et 0 occurrence de « STAGING »
    - Les 3 vars GOOGLE_OAUTH_* listées par `vercel env ls` (target production, type sensitive)
    - MAIL_DRY_RUN toujours `true` sur Vercel ET Railway (vérifié, prouvé dans §9.2)
    - §9.1 et §9.2 du runbook remplis avec horodatage
  </acceptance_criteria>
  <done>Prod live gardée : flag production actif, emails toujours dry-run, Google porté, preuves datées.</done>
</task>

<task type="auto">
  <name>Task 3: Pack témoin SES-0094 — collecte des preuves go/no-go (runbook §3)</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-GONOGO-SES-0094.md</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md (§3 : critères exacts)
    - apps/web/scripts/_gen-session-pack.ts (protocole de régénération — EXCLUDE_KINDS, séquentiel)
    - .planning/audit/STORAGE-REAUDIT-FINAL.md (baseline storage post-22-03)
  </read_first>
  <action>
    Régénérer le pack complet SES-0094 via le worker Railway (protocole habituel Phases 20/21 — Mac hors boucle, runs STRICTEMENT séquentiels, jamais parallèles). Puis collecter dans `22-GONOGO-SES-0094.md` :
    - **0 stub** : requête Prisma sur les Document du pack SES-0094 → `usedStub=false` pour 100 % (compteur exact n/n)
    - **Footer** : ouvrir 2 PDF (1 par moteur si possible) → footer avec les 22 vars OF_* renseignées
    - **0 404** : curl de CHAQUE signed URL du pack → 200 + `head -c 5` = `%PDF-` (compteur n/n, aucun 404)
    - **D-08 filigrane** : preuve que les PDF sortent SANS filigrane STAGING (le worker Railway n'a jamais eu le flag ; vérifier AUSSI un PDF synchrone rendu par Vercel post-flip — ex. convocation — qui portait le filigrane en staging 21-06)
    - **Qualité Qualiopi** : positionnement varié (v11), satisfaction non uniforme, QCM cohérent — échantillonnage 3 docs
    - Comparaison aux runs témoins précédents (Phases 20/21 : 16/16, ~3 min, 0 stub) + coût OpenRouter du run
    Verdict proposé GO/NO-GO en fin de rapport.
    Commit : `docs(22-06): SES-0094 witness pack evidence — go/no-go gate (CUT-02)`
  </action>
  <verify>
    <automated>grep -q "usedStub" ".planning/phases/22-bascule-prod-conformit-rgpd/22-GONOGO-SES-0094.md" && grep -qE "GO|NO-GO" ".planning/phases/22-bascule-prod-conformit-rgpd/22-GONOGO-SES-0094.md"</automated>
  </verify>
  <acceptance_criteria>
    - Rapport daté avec les 5 blocs de preuve (stub, footer, 404, filigrane, qualité) + compteurs exacts n/n
    - 0 stub prouvé par sortie Prisma brute collée ; 0 404 prouvé par compteur curl
    - Preuve filigrane sur un PDF SYNCHRONE Vercel (pas seulement worker)
    - Verdict explicite en dernière ligne
  </acceptance_criteria>
  <done>Toutes les preuves du gate sont collectées et comparables aux témoins des Phases 20/21.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verdict GO/NO-GO (D-10)</name>
  <action>PAUSE bloquante. Présenter 22-GONOGO-SES-0094.md + liens vers les docs du pack sur https://qualiof.vercel.app. Si « GO » : consigner le verdict daté dans le rapport et le runbook §9.3, la Wave 3 s'ouvre. Si « NO-GO » : appliquer IMMÉDIATEMENT le rollback runbook §8 (re-flag NEXT_PUBLIC_APP_ENV=staging + redeploy), prouver le retour du bandeau STAGING, documenter la cause — AVANT toute analyse.</action>
  <what-built>Bascule §1–§2 exécutée (prod flag live, emails dry-run) + pack témoin SES-0094 régénéré avec rapport de preuves `22-GONOGO-SES-0094.md`.</what-built>
  <how-to-verify>
    1. Lire `22-GONOGO-SES-0094.md` : 0 stub, footer 22 OF_*, 0 404, PDF sans filigrane, qualité Qualiopi.
    2. Ouvrir 1–2 documents du pack sur https://qualiof.vercel.app (fiche session SES-0094) et juger la conformité métier.
    3. Verdict : GO → la Wave 3 (emails réels 22-07, alertes 22-08) démarre. NO-GO → Claude applique IMMÉDIATEMENT le rollback §8 (re-flag staging, ~5 min) et documente la cause.
  </how-to-verify>
  <resume-signal>Répondre « GO » ou « NO-GO + motif ». En cas de NO-GO, le rollback est appliqué et prouvé avant toute analyse.</resume-signal>
</task>

</tasks>

<verification>
- https://qualiof.vercel.app en production (0 bandeau), MAIL_DRY_RUN=true intact sur les 2 plateformes.
- 22-GONOGO-SES-0094.md complet, verdict rendu par Laurent.
- Runbook §9.1–9.3 remplis.
</verification>

<success_criteria>
CUT-01 (bascule exécutée proprement selon runbook) + CUT-02 (pack témoin gate go/no-go réussi) — ou rollback D-04 propre et documenté.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-06-SUMMARY.md`
</output>
