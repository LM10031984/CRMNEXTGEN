---
phase: 22-bascule-prod-conformit-rgpd
plan: 09
type: execute
wave: 4
depends_on: ["22-07"]
files_modified:
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-TEAM-INVITES.md
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
autonomous: false
requirements: [CUT-01]

must_haves:
  truths:
    - "L'équipe Start Academy est invitée avec les bons rôles RBAC (Phase 8) via le flux existant — APRÈS le gate go/no-go et APRÈS l'activation des emails réels (les invitations partent par email)"
    - "Au moins un membre invité s'est connecté avec succès sur https://qualiof.vercel.app — le multi-utilisateurs cloud est prouvé (objectif v6)"
    - "Aucun développement : utilisation du flux tenant-users.ts existant (D-09)"
  artifacts:
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-TEAM-INVITES.md"
      provides: "Liste rôles attribués (sans PII superflue) + preuves d'envoi et de première connexion"
  key_links:
    - from: "invitations"
      to: "apps/web/src/server/actions/tenant-users.ts"
      via: "inviteUser / resendInvitation (flux Phase 8, RBAC 6 rôles)"
      pattern: "inviteUser"
---

<objective>
Inviter la petite équipe de départ (D-09) : Laurent fournit noms + emails + rôles RBAC au checkpoint, les invitations partent via le flux existant `tenant-users.ts`, et une première connexion tierce est prouvée — le critère « invitations équipe envoyées » de CUT-01 est soldé et l'objectif v6 « multi-utilisateurs sans le Mac » devient observable.

Purpose: dernier critère fonctionnel de la bascule — l'équipe travaille sur la prod cloud.
Output: invitations envoyées + preuve de connexion, runbook §9.5 rempli.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
@apps/web/src/server/actions/tenant-users.ts

<interfaces>
From apps/web/src/server/actions/tenant-users.ts (flux Phase 8, vérifié — UTILISER, ne pas re-développer) :
```typescript
export async function inviteUser(...)        // création + invitation
export async function resendInvitation(...)  // relance d'invitation
export async function changeUserRole(input: ChangeUserRoleInput)
```
Rôles RBAC disponibles (enum UserRole) : ADMIN, MANAGER, FORMATEUR, COMMERCIAL, COMPTABLE, LECTEUR.
Pré-requis : MAIL_DRY_RUN=false actif (plan 22-07) — sinon les emails d'invitation ne partent pas réellement.
UI : Paramètres → Utilisateurs (le flux passe par l'app — pas de script direct nécessaire).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Laurent fournit la liste de l'équipe (noms + emails + rôles RBAC)</name>
  <action>PAUSE human-action : seule information manquante = la liste de l'équipe (noms + emails + rôles parmi ADMIN/MANAGER/FORMATEUR/COMMERCIAL/COMPTABLE/LECTEUR). Présenter le rappel des rôles, recueillir la liste, vérifier la validité syntaxique des emails, reformuler la liste finale et obtenir la confirmation avant tout envoi.</action>
  <what-built>La prod est live et gatée (go/no-go GO), les emails réels sont actifs. Le flux d'invitation Phase 8 est prêt (aucun code à écrire). Il manque UNIQUEMENT la liste de la petite équipe de départ — information que seul Laurent détient (D-09).</what-built>
  <how-to-verify>
    Fournir la liste : pour chaque membre — prénom/nom, email, rôle parmi : ADMIN, MANAGER, FORMATEUR, COMMERCIAL, COMPTABLE, LECTEUR.
    Rappels pour choisir les rôles : COMPTABLE = trésorerie/factures ; COMMERCIAL = leads/dossiers ; FORMATEUR = accès sessions ; LECTEUR = lecture seule ; MANAGER = tout sauf administration ; ADMIN = tout (à limiter).
  </how-to-verify>
  <resume-signal>Fournir la liste « Prénom Nom &lt;email&gt; — RÔLE » (une ligne par membre). Claude vérifie la validité des emails et confirme la liste avant envoi.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Envoyer les invitations via le flux existant + prouver une première connexion</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-TEAM-INVITES.md, .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - apps/web/src/server/actions/tenant-users.ts (signature exacte d'inviteUser : shape de l'input, comportement email vs mot de passe temporaire)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md (§5 et §9.5)
  </read_first>
  <action>
    ① Lire `inviteUser` pour confirmer le mécanisme réel : email d'invitation OU mot de passe temporaire affiché — adapter la procédure en conséquence (si mot de passe temporaire : le transmettre à Laurent par canal sûr, JAMAIS dans un fichier commité).
    ② Envoyer les invitations pour chaque membre de la liste validée — de préférence via l'UI (https://qualiof.vercel.app → Paramètres → Utilisateurs, connecté en ADMIN), sinon via la server action en script (scopée tenantId — contrainte projet).
    ③ Vérifier en base : `User` créés avec le bon `role` et le bon `tenantId` (requête Prisma de contrôle, sortie collée).
    ④ Preuve de première connexion : demander à Laurent qu'UN membre au moins se connecte (ou que Laurent teste avec le compte d'un membre en sa présence) — capturer la preuve (AuthSession créée en base pour ce user, requête Prisma).
    ⑤ Écrire `22-TEAM-INVITES.md` : tableau {membre (initiales ou prénom), rôle, invitation envoyée le, première connexion le} — PAS de PII superflue (pas d'email complet si initiales suffisent) ; remplir le runbook §9.5.
    Commit : `docs(22-09): team invitations sent via existing RBAC flow — first login proved (CUT-01, D-09)`
  </action>
  <verify>
    <automated>test -f ".planning/phases/22-bascule-prod-conformit-rgpd/22-TEAM-INVITES.md" && grep -qE "ADMIN|MANAGER|FORMATEUR|COMMERCIAL|COMPTABLE|LECTEUR" ".planning/phases/22-bascule-prod-conformit-rgpd/22-TEAM-INVITES.md" && grep -q "connexion" ".planning/phases/22-bascule-prod-conformit-rgpd/22-TEAM-INVITES.md"</automated>
  </verify>
  <acceptance_criteria>
    - Chaque membre de la liste validée a un User en base avec le rôle exact demandé (sortie Prisma collée dans le rapport)
    - Preuve d'au moins UNE première connexion tierce (AuthSession en base, datée)
    - 22-TEAM-INVITES.md sans mot de passe ni PII superflue ; runbook §9.5 rempli
    - 0 code nouveau dans apps/web/src (le flux existant a suffi) — `git diff apps/web/src` vide sur ce plan
  </acceptance_criteria>
  <done>L'équipe est à bord avec les bons rôles, une connexion tierce prouvée — multi-utilisateurs cloud réel.</done>
</task>

</tasks>

<verification>
- Users créés avec rôles corrects, ≥1 connexion tierce prouvée.
- Aucun développement — utilisation pure du flux Phase 8.
</verification>

<success_criteria>
CUT-01 (dernier critère : « invitations équipe envoyées, RBAC Phase 8 ») satisfait avec preuve d'usage réel.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-09-SUMMARY.md`
</output>
