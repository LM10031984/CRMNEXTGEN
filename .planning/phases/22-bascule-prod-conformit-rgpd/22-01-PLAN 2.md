---
phase: 22-bascule-prod-conformit-rgpd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md
autonomous: true
requirements: [CUT-01]

must_haves:
  truths:
    - "Le runbook de bascule existe et est écrit AVANT la fenêtre de bascule (critère CUT-01 littéral)"
    - "Le plan de rollback (D-04 : re-flag staging ~5 min) est écrit dans le même document, avec le tableau exact var→valeur"
    - "Un non-technicien (Laurent) peut suivre la bascule au dashboard, sans CLI"
  artifacts:
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md"
      provides: "Runbook complet : pré-requis, sanity env, 2 flips, gate go/no-go, rollback, gabarit evidence"
      min_lines: 200
  key_links:
    - from: "22-CUTOVER-RUNBOOK.md §4"
      to: "MAIL_DRY_RUN sur Vercel ET Railway"
      via: "les 2 flips listés explicitement (Pitfall 10)"
      pattern: "Railway.*MAIL_DRY_RUN|MAIL_DRY_RUN.*Railway"
    - from: "22-CUTOVER-RUNBOOK.md §8"
      to: "rollback D-04"
      via: "re-flag NEXT_PUBLIC_APP_ENV=staging + MAIL_DRY_RUN=true"
      pattern: "NEXT_PUBLIC_APP_ENV=staging"
---

<objective>
Écrire le runbook de bascule prod + plan de rollback AVANT la fenêtre (CUT-01 littéral). Le runbook est une composition des deux runbooks existants (20-DEPLOY.md Railway, 21-DEPLOY-VERCEL.md Vercel) : non-technicien, dashboard-first, avec sections numérotées référençables par les plans 22-06..22-10 qui l'exécuteront.

Purpose: aucune bascule sans procédure écrite et réversible — c'est le premier critère de succès de la phase.
Output: `.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-RESEARCH.md
@.planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rédiger le runbook de bascule (§0–§7, chemin nominal)</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md (modèle de forme : tableaux de vars, §9 evidence datée)
    - .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md (pattern non-technicien dashboard-first, tableau ~15 vars worker)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md (D-01..D-18)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-RESEARCH.md (§Architecture Patterns 1 et 4, §Common Pitfalls 1-10)
  </read_first>
  <action>
    Créer `22-CUTOVER-RUNBOOK.md` en français, style non-technicien dashboard-first (modèle 20-DEPLOY.md). Sections numérotées EXACTEMENT ainsi (référencées par les plans 22-06..22-10) :

    **§0 Pré-requis de la fenêtre** (checklist) :
    - Phase 20 clôturée : relevé stabilité 24 h Railway (attendu 2026-07-07 ~08h45) + `/gsd:verify-work 20` passé
    - Audit d'écart data local↔cloud = PASS (plan 22-03, rapport `22-DATA-GAP-AUDIT.md`)
    - Re-audit storage final = 0 lien mort (plan 22-03, rapport `.planning/audit/`)
    - Sanity check env = 0 valeur polluée (plan 22-04, D-18 ②)
    - Gate RGPD validé par Laurent (plan 22-05, D-13 : registre + 7 fiches DPA)
    - CI verte sur `main` (rappel : PR cloud-migration→main = MERGE COMMIT, JAMAIS squash)

    **§1 Sanity check env (D-18 ②)** : procédure — ① `vercel env pull .env.vercel-prod --environment=production` (vars non-sensitive) puis `pnpm tsx apps/web/scripts/sanity-check-env.ts .env.vercel-prod` (regex `[^\x20-\x7E]|#| +$`) ; ② vars sensitive NON relisibles (Pitfall 2) → re-pose depuis source assainie (parser dotenv strippant les commentaires inline — leçon PROD-0674, 2 incidents ByteString) + preuve comportementale : re-test auto-fill IA produit (bouton fiche produit) ; ③ ne JAMAIS coller une ligne `.env` brute contenant ` # …` dans un dashboard.

    **§2 Flip 1 — passage en production (Vercel, projet `qualiof`, team laurents-projects-3806ab87)** :
    - Poser les 3 vars Google (sensitive) : `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET` (valeurs depuis `files/secrets/oauth-client.json`, champ `installed` ou `web`), `GOOGLE_OAUTH_REFRESH_TOKEN` (depuis `files/secrets/google-token.json`, champ `refresh_token`) — 3 valeurs, PAS 1 (Pitfall 3)
    - Changer `NEXT_PUBLIC_APP_ENV` : `staging` → `production` (environment Production)
    - `MAIL_DRY_RUN` reste `true` partout à ce stade (Vercel ET Railway)
    - Redeploy production, puis preuves : `/login` 200 SANS bandeau STAGING, login OK
    - ⚠ `NEXT_PUBLIC_*` est inliné au build : le redeploy est OBLIGATOIRE après le changement

    **§3 Gate go/no-go — pack témoin SES-0094 (D-10)** : régénération complète, critères Phases 20/21 : 0 stub (`usedStub=false` en base sur tous les docs), footer 22 vars OF_*, docs Qualiopi conformes, aucun 404 sur les signed URLs, `%PDF-` en tête de fichier, ET (D-08) PDF SANS filigrane STAGING. Si NO-GO → §8 rollback.

    **§4 Flip 2 — emails réels (D-06)** : séquence stricte : rapport des envois en attente (`22-PENDING-SENDS-REPORT.md`, inclut les relances « brûlées » en dry-run — Pitfall 1) → validation Laurent + décision remédiation compteurs → `MAIL_DRY_RUN=false` sur **DEUX plateformes** : Vercel (projet qualiof, env Production, + redeploy) ET Railway (service worker — le changement de var redéploie automatiquement, prévoir la fenêtre) — Pitfall 10. Preuve : email test réel vers laurent@start-academy.fr (messageId, pas dryRun).

    **§5 Invitations équipe (D-09)** : après gate §3 vert et §4 fait — Laurent fournit noms + emails + rôles RBAC (ADMIN/MANAGER/FORMATEUR/COMMERCIAL/COMPTABLE/LECTEUR) ; invitations via le flux existant (Paramètres → Utilisateurs, server action `inviteUser` de `tenant-users.ts`).

    **§6 Alertes coûts + backups (D-11/D-12)** : tableau par plateforme (copier depuis 22-RESEARCH.md §Pattern 4) : Vercel Spend Management budget ~45 $ alertes email SANS auto-pause ; Railway Usage Limits soft alert ~35 € SANS hard limit bas ; Supabase spend cap ON (défaut Pro) ; OpenRouter Auto Top-Up OFF + solde ~15 € + credit limit sur la clé prod. Backups : capture dashboard Supabase Database → Backups (daily, 7 j, projet eu-west-1). Email des alertes : laurent@start-academy.fr. Formulation « configurer l'équivalent de X » + capture en preuve (les dashboards évoluent).

    **§7 Avertissements post-bascule** :
    - La case « notifier les apprenants » du sync calendar devient RÉELLE (Pitfall 4) : défaut `false`, sessions passées → `sendUpdates='none'` automatique — mais cocher la case sur une session future envoie de vrais emails Google aux apprenants
    - JAMAIS d'envoi de masse aux apprenants sans action explicite (exigence Laurent) ; crons relances préinscriptions/OPCO (`CRON_SECRET`) restent volontairement DÉBRANCHÉS
    - OpenRouter : si packs closure en échec HTTP 402 → recharger les crédits (plafond dur voulu, auto top-up OFF)
    - MinIO local NON purgé à la bascule — purge = plan 22-10 séparé, gate destructif
  </action>
  <verify>
    <automated>grep -c "^## §" ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md" | awk '$1 >= 8 {print "OK"} $1 < 8 {print "FAIL"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier contient 8+ sections titrées `## §0` à `## §7` (le §8/§9 arrivent Task 2)
    - grep "GOOGLE_OAUTH_CLIENT_ID" ET "GOOGLE_OAUTH_CLIENT_SECRET" ET "GOOGLE_OAUTH_REFRESH_TOKEN" → 3 matchs distincts (Pitfall 3 : 3 valeurs)
    - grep "MAIL_DRY_RUN" apparaît dans §2 (reste true) ET §4 (flip false Vercel ET Railway)
    - grep "SES-0094" présent en §3 avec les critères « 0 stub », « 22 vars OF_* », « 404 », « filigrane »
    - grep "auto-pause" et "hard limit" présents en §6 (anti-Pitfall 5)
    - Aucune commande CLI obligatoire dans le chemin nominal Laurent (les commandes tsx/vercel citées sont exécutées par Claude, mentionné comme tel)
  </acceptance_criteria>
  <done>Runbook §0–§7 écrit, chemin nominal complet de la bascule couvert, tous les garde-fous D-06/D-18/Pitfalls intégrés.</done>
</task>

<task type="auto">
  <name>Task 2: Rédiger le plan de rollback (§8) + gabarit evidence (§9) + contrôle de cohérence</name>
  <files>.planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md (état après Task 1)
    - .planning/phases/21-app-vercel-filet-ci-tests/21-DEPLOY-VERCEL.md (§9 evidence datée — modèle)
  </read_first>
  <action>
    Ajouter au runbook :

    **§8 Plan de rollback (D-04)** — retour au mode staging gardé, ~5 min, réversible :
    | Variable | Plateforme | Valeur rollback |
    |---|---|---|
    | `NEXT_PUBLIC_APP_ENV` | Vercel (Production) | `staging` |
    | `MAIL_DRY_RUN` | Vercel (Production) | `true` |
    | `MAIL_DRY_RUN` | Railway (service worker) | `true` |
    + redeploy Vercel (NEXT_PUBLIC_* inliné au build) ; Railway redéploie seul au changement de var.
    Préciser : la base cloud Supabase RESTE la vérité (PAS de rollback vers le Mac local — obsolète, D-04) ; les 3 vars Google peuvent rester posées (la garde staging de `sync-session.ts:84` re-bloque le sync) ; critères de déclenchement du rollback : pack témoin NO-GO, login cassé, PDF cassés, erreur env non diagnostiquée en <30 min.

    **§9 Evidence (gabarit à remplir par les plans 22-06..22-10)** — sous-sections datées vides :
    - 9.1 Sanity env (sortie script + preuve auto-fill)
    - 9.2 Flip production (capture vars, /login sans bandeau, x-vercel-id)
    - 9.3 Pack témoin SES-0094 (compteurs 0 stub, curl signed URLs, PDF sans filigrane)
    - 9.4 Rapport relances + décision Laurent + flip MAIL_DRY_RUN ×2 + email test réel
    - 9.5 Invitations équipe (liste rôles, sans PII inutile)
    - 9.6 Alertes coûts (4 captures) + backups Supabase (capture daily, eu-west-1)
    - 9.7 Purge locale (archives + mot de validation + preuve purge)

    Contrôle de cohérence final : relire le runbook entier ; vérifier que chaque section §1–§7 référence le script ou l'artefact exact qui la prouve (`sanity-check-env.ts`, `22-PENDING-SENDS-REPORT.md`, `22-DATA-GAP-AUDIT.md`), et que les noms de variables sont exacts (`NEXT_PUBLIC_APP_ENV`, `MAIL_DRY_RUN`, les 3 `GOOGLE_OAUTH_*`).
  </action>
  <verify>
    <automated>grep -q "## §8" ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md" && grep -q "## §9" ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md" && grep -q "NEXT_PUBLIC_APP_ENV.*staging" ".planning/phases/22-bascule-prod-conformit-rgpd/22-CUTOVER-RUNBOOK.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - §8 contient le tableau de rollback avec les 3 lignes exactes ci-dessus (grep "staging" et "true" dans le tableau)
    - §8 contient la mention « la base cloud reste la vérité » (ou équivalent grep "cloud.*vérité")
    - §9 contient 7 sous-sections 9.1–9.7
    - grep "sanity-check-env.ts" et "22-PENDING-SENDS-REPORT.md" et "22-DATA-GAP-AUDIT.md" → présents dans le runbook
  </acceptance_criteria>
  <done>Runbook complet §0–§9, rollback D-04 écrit, gabarit evidence prêt pour les plans d'exécution.</done>
</task>

</tasks>

<verification>
- Le runbook existe, ≥200 lignes, sections §0–§9 présentes.
- Rollback = re-flag staging (D-04), jamais de restore local.
- Aucun secret en clair dans le runbook (les valeurs Google sont référencées par chemin de fichier, jamais copiées).
</verification>

<success_criteria>
CUT-01 partie « runbook + rollback écrits AVANT la fenêtre » satisfaite : un lecteur non-technicien peut dérouler la bascule et le rollback depuis ce seul document.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-01-SUMMARY.md`
</output>
