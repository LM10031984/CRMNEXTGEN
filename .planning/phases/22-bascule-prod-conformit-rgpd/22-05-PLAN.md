---
phase: 22-bascule-prod-conformit-rgpd
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/rgpd/REGISTRE-TRAITEMENTS.md
  - docs/rgpd/dpa/openrouter.md
  - docs/rgpd/dpa/anthropic.md
  - docs/rgpd/dpa/supabase.md
  - docs/rgpd/dpa/vercel.md
  - docs/rgpd/dpa/railway.md
  - docs/rgpd/dpa/google.md
  - docs/rgpd/dpa/ovh-smtp.md
  - apps/web/scripts/_export-registre-rgpd.ts
  - docs/rgpd/REGISTRE-TRAITEMENTS.pdf
autonomous: false
requirements: [RGPD-01]

must_haves:
  truths:
    - "Le registre des traitements (art. 30 RGPD) est complet, versionné dans le repo, et exportable en PDF pour un auditeur Qualiopi/CNIL (D-15)"
    - "Les 7 sous-traitants réels (OpenRouter, Anthropic, Supabase, Vercel, Railway, Google, OVH SMTP) ont chacun une fiche DPA honnête — les trous sont signalés, jamais inventés (D-14, D-16)"
    - "Laurent a VALIDÉ le registre en tant que responsable de traitement AVANT la levée des gardes PII (gate D-13) — la bascule 22-06 ne peut pas démarrer sans"
  artifacts:
    - path: "docs/rgpd/REGISTRE-TRAITEMENTS.md"
      provides: "Registre art. 30 : traitements, finalités, catégories, durées, mesures techniques, sous-traitants"
      min_lines: 120
    - path: "docs/rgpd/dpa/openrouter.md"
      provides: "Fiche DPA honnête : DPA signé = enterprise only, garanties réelles (no-retention défaut, ZDR)"
      contains: "enterprise"
    - path: "docs/rgpd/REGISTRE-TRAITEMENTS.pdf"
      provides: "Export imprimable via le pipeline pdf-render interne"
  key_links:
    - from: "docs/rgpd/REGISTRE-TRAITEMENTS.md"
      to: ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md"
      via: "localisation données : Supabase eu-west-1, Vercel cdg1, Railway europe-west4"
      pattern: "eu-west-1"
    - from: "apps/web/scripts/_export-registre-rgpd.ts"
      to: "apps/web/src/lib/pdf-render.ts"
      via: "marked → renderHtmlToPdf (pipeline interne, pas de nouvelle lib PDF)"
      pattern: "renderHtmlToPdf"
---

<objective>
Solder la dette RGPD engagée par le GO vision de la Phase 16 (D-02b) : registre des traitements art. 30 + 7 fiches DPA sous-traitants, versionnés dans le repo avec export PDF, rédigés par Claude et VALIDÉS par Laurent (responsable de traitement). Ce plan est le GATE BLOQUANT D-13 : les PII prod ne circulent pas tant qu'il n'est pas validé.

Purpose: RGPD-01 — la conformité précède la bascule, pas l'inverse.
Output: `docs/rgpd/` complet + validation Laurent (checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md
@.planning/phases/22-bascule-prod-conformit-rgpd/22-RESEARCH.md
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md

<interfaces>
<!-- Faits vérifiés (research 2026-07-06) à reprendre tels quels dans les fiches — ne rien inventer au-delà. -->

Localisations (17-REGIONS.md) : Supabase projet gntlqyscahbgjrmsbzil = eu-west-1 (Irlande, DÉFINITIVE) ; Vercel = cdg1 (Paris) ; Railway = europe-west4 (Pays-Bas) ; SMTP = OVH ssl0.ovh.net:465 (France).

DPA publics (URLs research, à re-vérifier à la rédaction) :
- Supabase : supabase.com/legal/dpa (acceptation via dashboard org)
- Vercel : vercel.com/legal/dpa
- Railway : railway.com/legal/dpa (+ Trust Center ; subprocessors : GCP, Stripe, Cloudflare)
- Anthropic : anthropic.com/legal (commercial terms + DPA) — ⚠ atteint VIA OpenRouter = sous-sous-traitant, pas de relation contractuelle directe
- OpenRouter : ⚠ DPA signé = tier ENTERPRISE uniquement (vérifié support 2026-07). Garanties self-serve réelles : politique par défaut de non-rétention des prompts (métadonnées seules) + réglages compte ZDR/logging OFF à vérifier et capturer. openrouter.ai/docs/guides/features/zdr
- Google : dépend du type de compte — Open Question 1 : compte gratuit (conditions grand public, PAS de DPA processeur) vs Workspace. QUESTION À POSER À LAURENT AU CHECKPOINT.
- OVH : société française, données UE, DPA GDPR dans les conditions contractuelles.

Données transmises par sous-traitant (code vérifié) :
- OpenRouter/Anthropic : prompts closure (noms stagiaires, contexte sessions), OCR vision CNI/RIB (pilier #4)
- Supabase : TOUTE la base (PII apprenants, SensitiveData) + Storage (CNI/RIB/PDF)
- Vercel : app runtime (cookies session, formulaire public préinscription)
- Railway : worker (logs — cf. audit D-17 plan 22-02), génération docs
- Google : Calendar (noms sessions/formateurs dans les events, emails apprenants en attendees) + Drive (programmes)
- OVH SMTP : emails apprenants/payeurs (convocations, relances factures)

Mesures techniques existantes (CLAUDE.md + phases) : bucket Storage privé + signed URLs TTL minutes, table SensitiveData séparée, RBAC 6 rôles (ADMIN/MANAGER/FORMATEUR/COMMERCIAL/COMPTABLE/LECTEUR), multi-tenant tenantId, régions EU verrouillées, backups daily 7 j même région, MAIL_DRY_RUN, audit logs PII (22-02).

Export PDF : pattern scripts `_*.ts` + `marked` (markdown→HTML) + `renderHtmlToPdf` de `apps/web/src/lib/pdf-render.ts` — footer in-body position:fixed bottom:0 (convention projet), JAMAIS le footer Gotenberg natif.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registre des traitements art. 30 (REGISTRE-TRAITEMENTS.md)</name>
  <files>docs/rgpd/REGISTRE-TRAITEMENTS.md</files>
  <read_first>
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md (localisations exactes)
    - CLAUDE.md racine (mesures RGPD projet : ribKey bucket privé, SensitiveData, signed URLs)
    - packages/db/prisma/schema.prisma (modèles Person, SensitiveData, PreEnrollment — catégories de données réelles)
  </read_first>
  <action>
    Créer `docs/rgpd/REGISTRE-TRAITEMENTS.md` — registre art. 30 RGPD du responsable de traitement (Start Academy, OF Qualiopi ; contact : laurent@start-academy.fr). En-tête : version, date, responsable, statut « en attente de validation ». Un tableau/section PAR TRAITEMENT :

    1. **Gestion des apprenants et sessions (CRM 360°)** — base légale exécution contractuelle ; données : identité, coordonnées, casquettes EI/Enseigne ; personnes : apprenants, formateurs, contacts.
    2. **Pré-inscriptions self-service + OCR IA** — CNI/RIB/CFP uploadés (bucket privé, signed URLs TTL minutes, direct-to-storage), OCR via OpenRouter (vision) ; données SENSIBLES par nature documentaire.
    3. **Génération des documents Qualiopi (pack closure IA)** — prompts vers OpenRouter/Anthropic contenant noms stagiaires + contexte session.
    4. **Facturation et relances (trésorerie OPCO/AGEFICE)** — payeurs, montants, emails de relance ; règle payeur : l'auto-entrepreneur est son propre payeur.
    5. **Emails transactionnels** (convocations, notifications) via OVH SMTP — opt-in explicite pour tout envoi de masse apprenants (exigence Laurent, notifyLearners défaut false).
    6. **Synchronisation Google Calendar** — noms sessions/formateurs + emails apprenants en attendees ; sendUpdates='none' par défaut.
    7. **Comptes utilisateurs internes (RBAC)** — 6 rôles, sessions Lucia, argon2.
    8. **Veille réglementaire** (RSS + IA) — pas de PII apprenant.

    Pour chaque traitement : finalité, base légale, catégories de données, catégories de personnes, destinataires/sous-traitants (renvoi vers `dpa/<fournisseur>.md`), durée de conservation (proposer des durées standard OF Qualiopi : dossiers formation ≥ durée d'audit Qualiopi, marquer « à confirmer par Laurent » — D-16 pas d'invention juridique), mesures techniques (reprendre la liste vérifiée du bloc interfaces).

    Sections transverses : localisation des données (tableau 4 plateformes + régions, source 17-REGIONS.md), transferts hors UE (OpenRouter/Anthropic US → à documenter honnêtement : clauses/DPF selon fiches DPA), registre des sous-traitants (tableau 7 lignes pointant les fiches), limites connues (backups même région non off-site — backlog pg_dump hors vendor D-12 ; audit logs PII fait en 22-02).
    Toute incertitude juridique = encadré « ⚠ À VALIDER PAR LE RESPONSABLE DE TRAITEMENT ».
    Commit : `docs(22-05): registre des traitements art. 30 (RGPD-01)`
  </action>
  <verify>
    <automated>test -f docs/rgpd/REGISTRE-TRAITEMENTS.md && grep -c "dpa/" docs/rgpd/REGISTRE-TRAITEMENTS.md | awk '$1>=7{print "OK"} $1<7{exit 1}' && grep -q "eu-west-1" docs/rgpd/REGISTRE-TRAITEMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 8 traitements documentés (grep -c "finalité" ou titres ≥ 8)
    - grep "eu-west-1" ET "cdg1" ET "europe-west4" (localisation sourcée)
    - 7 renvois vers docs/rgpd/dpa/*.md
    - grep "À VALIDER" présent (les incertitudes sont marquées, pas masquées)
    - grep "signed URL" et "SensitiveData" et "RBAC" (mesures techniques réelles reprises)
  </acceptance_criteria>
  <done>Registre art. 30 complet, honnête, versionné — prêt pour la validation Laurent.</done>
</task>

<task type="auto">
  <name>Task 2: Les 7 fiches DPA sous-traitants (docs/rgpd/dpa/)</name>
  <files>docs/rgpd/dpa/openrouter.md, docs/rgpd/dpa/anthropic.md, docs/rgpd/dpa/supabase.md, docs/rgpd/dpa/vercel.md, docs/rgpd/dpa/railway.md, docs/rgpd/dpa/google.md, docs/rgpd/dpa/ovh-smtp.md</files>
  <read_first>
    - docs/rgpd/REGISTRE-TRAITEMENTS.md (créé Task 1 — cohérence des renvois)
    - .planning/phases/22-bascule-prod-conformit-rgpd/22-RESEARCH.md (§Pitfall 6, §Sources : URLs DPA et caveats vérifiés)
  </read_first>
  <action>
    Créer 7 fiches sur un gabarit commun : {Fournisseur, Rôle (sous-traitant / sous-sous-traitant), Service utilisé, Données transmises (reprendre le bloc interfaces), Localisation, Document DPA public (URL + statut : signé / accepté via dashboard / CGU), Garanties de transfert hors UE, Points ouverts / limites, Date de vérification}. Vérifier chaque URL DPA au moment de la rédaction (WebFetch) — si une URL a changé, citer la nouvelle et le noter.

    Spécificités OBLIGATOIRES (honnêteté D-16) :
    - **openrouter.md** : « DPA mutuellement signé = tier enterprise UNIQUEMENT — non disponible en self-serve (statut actuel). Garanties effectives : politique par défaut de non-rétention des prompts (métadonnées seules) + réglages compte à vérifier/capturer : logging OFF, option ZDR (Zero Data Retention) sur les endpoints. Réponse préparée pour un auditeur qui demanderait le DPA signé. Mitigation possible : ZDR enforcement ; tier enterprise si exigé un jour. »
    - **anthropic.md** : « Atteint VIA OpenRouter (sous-sous-traitant) — pas de relation contractuelle directe Start Academy↔Anthropic. Référencer anthropic.com/legal pour la politique du modèle provider ; la chaîne contractuelle passe par OpenRouter. »
    - **google.md** : « ⚠ Type de compte À CONFIRMER par Laurent (checkpoint Task 3) : compte Google gratuit = conditions grand public, PAS de DPA processeur → point d'attention majeur (noms sessions/formateurs + emails apprenants dans les events Calendar) ; mitigation backlog : migration Google Workspace. » Rédiger les DEUX variantes (gratuit/Workspace), la bonne sera conservée au checkpoint.
    - **supabase.md** : DPA accepté via dashboard org (noter la procédure d'acceptation comme action à faire si pas déjà fait) ; eu-west-1 Irlande définitive ; backups daily 7 j même région (non off-site — limite assumée D-12).
    - **railway.md** : DPA public + subprocessors GCP/Stripe/Cloudflare ; europe-west4.
    - **vercel.md** : DPA public ; fonctions cdg1 ; noter que le réseau edge est global (les réponses transitent par le POP le plus proche du visiteur).
    - **ovh-smtp.md** : société française, données UE, DPA dans les conditions contractuelles ; ssl0.ovh.net:465 ; contenu transmis = emails apprenants/payeurs.
    Commit : `docs(22-05): 7 DPA fiches — honest gaps flagged (D-14, D-16)`
  </action>
  <verify>
    <automated>ls docs/rgpd/dpa/ | wc -l | awk '$1>=7{print "OK"} $1<7{exit 1}' && grep -qi "enterprise" docs/rgpd/dpa/openrouter.md && grep -qi "sous-sous-traitant" docs/rgpd/dpa/anthropic.md && grep -qi "confirmer" docs/rgpd/dpa/google.md</automated>
  </verify>
  <acceptance_criteria>
    - 7 fichiers présents dans docs/rgpd/dpa/ (openrouter, anthropic, supabase, vercel, railway, google, ovh-smtp)
    - openrouter.md : grep "enterprise" (limite DPA signée documentée) + grep -i "ZDR"
    - anthropic.md : grep "sous-sous-traitant"
    - google.md : grep -i "confirmer" (question type de compte) + les 2 variantes présentes
    - Chaque fiche a une URL DPA vérifiée + date de vérification
    - Aucune clause inventée : les affirmations non sourcées portent « ⚠ à vérifier »
  </acceptance_criteria>
  <done>Les 7 fiches existent, honnêtes, avec les caveats OpenRouter/Anthropic/Google explicites.</done>
</task>

<task type="auto">
  <name>Task 3: Export PDF du registre (pipeline interne)</name>
  <files>apps/web/scripts/_export-registre-rgpd.ts, docs/rgpd/REGISTRE-TRAITEMENTS.pdf</files>
  <read_first>
    - apps/web/src/lib/pdf-render.ts (signature de renderHtmlToPdf, gestion DOC_ENGINE_TOKEN/GOTENBERG_URL)
    - apps/web/src/lib/of-pdf-footer.ts (footer in-body — convention non-négociable)
    - apps/web/scripts/_export-qualiopi-produit-xlsx.ts (en-tête : conventions des scripts _*)
  </read_first>
  <action>
    Créer `apps/web/scripts/_export-registre-rgpd.ts` : lit `docs/rgpd/REGISTRE-TRAITEMENTS.md` + concatène les 7 fiches `docs/rgpd/dpa/*.md` (ordre alphabétique, saut de page CSS `page-break-before: always` entre fiches), convertit via `marked`, enveloppe dans un HTML A4 sobre (styles inline : police système, h1/h2 hiérarchisés, tableaux bordés) avec footer in-body `position:fixed bottom:0` 11pt « Start Academy — Registre des traitements — généré le {date} » (JAMAIS le footer Gotenberg natif — convention projet), rend via `renderHtmlToPdf` et écrit `docs/rgpd/REGISTRE-TRAITEMENTS.pdf`.
    Exécuter (Gotenberg local docker OU l'endpoint Railway avec Bearer — selon dispo), vérifier `%PDF-` en tête de fichier, committer le PDF.
    Commit : `docs(22-05): registre RGPD PDF export via internal pipeline (D-15)`
  </action>
  <verify>
    <automated>head -c 5 docs/rgpd/REGISTRE-TRAITEMENTS.pdf | grep -q "%PDF-" && grep -q "renderHtmlToPdf" apps/web/scripts/_export-registre-rgpd.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le PDF existe, commence par %PDF-, taille > 20 Ko
    - Le script utilise renderHtmlToPdf (grep) et un footer in-body (grep "position:fixed" ou "position: fixed")
    - grep négatif : aucune nouvelle dépendance PDF dans package.json (pas de md-to-pdf/puppeteer)
  </acceptance_criteria>
  <done>Export imprimable reproductible pour l'auditeur Qualiopi/CNIL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: GATE D-13 — validation du registre par Laurent (responsable de traitement)</name>
  <action>PAUSE bloquante. Présenter à Laurent : le PDF du registre, les 7 fiches, la question Google (gratuit vs Workspace), les durées proposées et les 2 limites assumées. Après réponse : figer google.md sur la bonne variante, appliquer les amendements, régénérer le PDF, marquer le registre « validé le {date} par Laurent » et committer. Cette validation lève le gate D-13.</action>
  <what-built>Registre art. 30 complet (8 traitements) + 7 fiches DPA (docs/rgpd/) + export PDF. Les incertitudes sont marquées « ⚠ À VALIDER » : durées de conservation proposées, limite DPA OpenRouter (enterprise only, ZDR en mitigation), Google = type de compte inconnu.</what-built>
  <how-to-verify>
    1. Ouvrir `docs/rgpd/REGISTRE-TRAITEMENTS.pdf` (ou le .md) et relire les 8 traitements + durées proposées.
    2. Répondre à la QUESTION BLOQUANTE : le compte Google utilisé pour Calendar/Drive est-il un compte GRATUIT (grand public) ou GOOGLE WORKSPACE (payant) ? → la fiche google.md sera figée sur la bonne variante.
    3. Confirmer (ou amender) les durées de conservation proposées.
    4. Prendre connaissance des 2 limites assumées : OpenRouter sans DPA signé (self-serve) et backups Supabase non off-site.
    En tant que responsable de traitement, valider le registre — cette validation EST le gate D-13 : sans elle, la bascule (plan 22-06) ne démarre pas.
  </how-to-verify>
  <resume-signal>Répondre : « registre validé + compte Google = gratuit|workspace » (+ amendements éventuels). Claude fige alors google.md, applique les amendements, régénère le PDF et marque le registre « validé le {date} ».</resume-signal>
</task>

</tasks>

<verification>
- `ls docs/rgpd/` : REGISTRE-TRAITEMENTS.md + .pdf + dpa/ (7 fiches).
- Statut du registre passé à « validé le {date} par Laurent » après le checkpoint.
- Gate D-13 tracé : le SUMMARY note explicitement que la validation autorise la Wave 2.
</verification>

<success_criteria>
RGPD-01 (partie registre/DPA) satisfait par sur-couverture (7 sous-traitants > 6 requis) ; gate D-13 levé par validation explicite de Laurent AVANT toute circulation de PII prod.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-05-SUMMARY.md`
</output>
