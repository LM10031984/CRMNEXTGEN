---
phase: 22-bascule-prod-conformit-rgpd
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/sanity-check-env.ts
  - apps/web/scripts/pending-reminders-report.ts
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-ENV-SANITY.md
  - .planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md
  - .gitignore
autonomous: true
requirements: [CUT-01, CUT-02]

must_haves:
  truths:
    - "Aucune variable d'env Vercel prod ne contient de caractère non-ASCII, de '#' inline ou d'espace de fin (D-18 ② — leçon PROD-0674)"
    - "Laurent peut voir la liste nominative exacte des relances factures qui partiraient au premier run réel, AVANT tout flip MAIL_DRY_RUN (D-06)"
    - "Les relances « brûlées » en dry-run depuis la Phase 20 (reminderCount incrémenté à blanc — Pitfall 1) sont inventoriées facture par facture"
  artifacts:
    - path: "apps/web/scripts/sanity-check-env.ts"
      provides: "Scan dotenv : valeurs polluées détectées avec index du caractère fautif"
      min_lines: 30
    - path: "apps/web/scripts/pending-reminders-report.ts"
      provides: "Reproduction lecture seule de la sélection du cron relances + inventaire AuditLog dryRun"
      min_lines: 60
    - path: ".planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md"
      provides: "Rapport D-06 : envois en attente + relances brûlées + flags règle payeur"
  key_links:
    - from: "apps/web/scripts/pending-reminders-report.ts"
      to: "apps/web/src/lib/invoice-reminders/worker.ts"
      via: "réplication EXACTE des filtres (REMINDER_START_DATE, status, dueDate, dedup 24h)"
      pattern: "REMINDER_START_DATE|2026-05-19"
    - from: "apps/web/scripts/pending-reminders-report.ts"
      to: "AuditLog.diff (Json)"
      via: "filtre Prisma path:['dryRun'] equals:true"
      pattern: "invoices.reminder_sent"
---

<objective>
Les deux outils de pré-vérification des sorties : ① sanity check env (D-18 ②) — détecter toute valeur polluée (commentaires inline, non-ASCII, espaces) dans les env Vercel/racine avant la bascule ; ② rapport des envois en attente (D-06 + Pitfall 1 CRITIQUE) — liste nominative des relances factures qui partiraient + inventaire des niveaux de relance brûlés à blanc par le cron dry-run Railway depuis la Phase 20.

Purpose: 2 incidents ByteString ont déjà eu lieu (OPENROUTER_API_KEY) ; et sans le rapport relances, le flip MAIL_DRY_RUN=false produirait soit des envois surprises vers des apprenants (règle payeur : l'auto-entrepreneur est son propre payeur), soit des silences (maxLevel atteint à blanc).
Output: 2 scripts réutilisables + 2 rapports pour le runbook §1 et §4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bascule-prod-conformit-rgpd/22-CONTEXT.md
@apps/web/src/lib/invoice-reminders/worker.ts
@apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts

<interfaces>
<!-- Contrats vérifiés 2026-07-06 — le script de rapport REPRODUIT ces filtres, il ne les réinvente pas. -->

From apps/web/src/lib/invoice-reminders/worker.ts (sélection du cron quotidien 8h Railway) :
```typescript
export const REMINDER_START_DATE = new Date('2026-05-19T00:00:00Z');
const REMINDER_DEDUP_MS = 24 * 60 * 60 * 1000;
// Par tenant : reminderDays = tenant.invoiceReminderDays ?? [30, 45]; maxLevel = reminderDays.length;
// minDays = reminderDays[0]; minOverdueDate = now - minDays jours
// where: {
//   status: { in: ['ISSUED', 'PARTIAL', 'OVERDUE'] },
//   issueDate: { gte: REMINDER_START_DATE },
//   OR: [ { dueDate: { lte: minOverdueDate } }, { dueDate: null, issueDate: { lte: minOverdueDate } } ],
// }
// + core : skip si reminderCount >= maxLevel ; skip si lastReminderAt < 24h ; niveau = reminderCount + 1
```

From apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts (destinataire) :
```typescript
// recipient = invoice.payerOrg?.emailBilling ?? invoice.payerOrg?.email ?? invoice.participant?.person?.email ?? null
// payerName = payerOrg?.legalName ?? `${person.firstName} ${person.lastName}`
// AuditLog écrit : action 'invoices.reminder_sent', diff: { level, channel, triggered_by, dryRun, daysOverdue }
```

From packages/db/prisma/schema.prisma :
```prisma
model AuditLog { ... action String ; diff Json ; createdAt DateTime @default(now()) ... }
```
→ `diff` est bien `Json` : le filtre Prisma `diff: { path: ['dryRun'], equals: true }` est valide (Open Question 4 résolue).

Regex de détection valeurs polluées (research D-18 ②) :
```typescript
const BAD = /[^\x20-\x7E]|#| +$/; // non-ASCII imprimable, dièse, espaces de fin
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script sanity-check-env.ts + exécution sur les env Vercel prod et .env racine</name>
  <files>apps/web/scripts/sanity-check-env.ts, .planning/phases/22-bascule-prod-conformit-rgpd/22-ENV-SANITY.md, .gitignore</files>
  <read_first>
    - .env (racine — repérer les lignes avec commentaires inline « # ← À REMPLIR » à l'origine de PROD-0674 ; NE PAS committer de valeurs)
    - .gitignore (vérifier la couverture des fichiers .env.*)
  </read_first>
  <action>
    ① Créer `apps/web/scripts/sanity-check-env.ts` :
    - Argument : chemin d'un fichier dotenv. Parse ligne à ligne (KEY=VALUE, ignorer lignes vides/commentaires pleins commençant par #, gérer les valeurs quotées).
    - Pour chaque valeur : test `const BAD = /[^\x20-\x7E]|#| +$/;` — si match, afficher `✗ KEY: caractère suspect à l'index ${value.search(BAD)} (codepoint U+${hex})` SANS afficher la valeur complète (secrets !) — seulement le codepoint et l'index.
    - Résumé final : `N variables scannées, M polluées`. `process.exit(1)` si M > 0.

    ② Vérifier que le fichier de pull est ignoré : `git check-ignore .env.vercel-prod` — sinon ajouter la ligne `.env.vercel-prod` à `.gitignore` AVANT le pull.

    ③ Exécuter : `vercel env pull .env.vercel-prod --environment=production` (projet qualiof — si le CLI demande une auth/scope, créer un checkpoint d'auth dynamique). Puis `pnpm tsx apps/web/scripts/sanity-check-env.ts .env.vercel-prod` ET `pnpm tsx apps/web/scripts/sanity-check-env.ts .env` (le .env racine est la SOURCE des re-poses — leçon : c'est lui qui portait les « ← À REMPLIR »).

    ④ Écrire `.planning/phases/22-bascule-prod-conformit-rgpd/22-ENV-SANITY.md` : date, commandes, sortie des 2 scans (clés + index, JAMAIS les valeurs), et la note Pitfall 2 : « Les vars sensitive ne sont PAS dans le pull — leur assainissement = re-pose depuis source propre + preuve comportementale (auto-fill IA produit re-testé OK post-D-18 ①, à re-prouver au runbook §9.1). »

    ⑤ Supprimer `.env.vercel-prod` après le scan (ne pas laisser traîner un dump d'env prod).

    Si des valeurs polluées sont trouvées côté Vercel : les lister dans le rapport (clés seulement) — la CORRECTION (re-pose) appartient au plan 22-06 §1 du runbook, pas à ce plan (pas de mutation d'env prod en Wave 1).
    Commit : `feat(22-04): env sanity-check script + Vercel prod scan report (D-18)`
  </action>
  <verify>
    <automated>pnpm tsx apps/web/scripts/sanity-check-env.ts .env.example >/dev/null 2>&1; test -f ".planning/phases/22-bascule-prod-conformit-rgpd/22-ENV-SANITY.md" && ! test -f .env.vercel-prod && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Le script existe ; grep la regex `[^\\x20-\\x7E]` présente ; grep négatif d'un `console.log` affichant la valeur complète d'une variable
    - 22-ENV-SANITY.md existe : date + 2 scans (Vercel pull, .env racine) + note sur les sensitive (Pitfall 2)
    - `.env.vercel-prod` absent du disque ET couvert par .gitignore (`git check-ignore .env.vercel-prod` exit 0)
    - Aucune valeur de secret dans le rapport ni dans le diff commité
  </acceptance_criteria>
  <done>Outillage sanity env opérationnel + état des lieux daté des env prod ; les re-poses éventuelles sont tracées pour le runbook §1.</done>
</task>

<task type="auto">
  <name>Task 2: Script pending-reminders-report.ts + rapport des envois en attente et relances brûlées (D-06 / Pitfall 1)</name>
  <files>apps/web/scripts/pending-reminders-report.ts, .planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md</files>
  <read_first>
    - apps/web/src/lib/invoice-reminders/worker.ts (sélection EXACTE à répliquer : REMINDER_START_DATE, status, OR dueDate, dedup — importer REMINDER_START_DATE plutôt que redéclarer)
    - apps/web/src/lib/invoice-reminders/invoice-reminder-core.ts (cascade destinataire + shape du diff AuditLog)
    - packages/db/prisma/schema.prisma (modèles Invoice, AuditLog — champs exacts : reminderCount, lastReminderAt, payerOrgId, participant)
  </read_first>
  <action>
    Créer `apps/web/scripts/pending-reminders-report.ts` — LECTURE SEULE (aucune écriture) contre la base du `.env` (= cloud) :

    **Partie A — envois en attente au prochain run réel :** répliquer EXACTEMENT la sélection du worker (importer `REMINDER_START_DATE` depuis `@/lib/invoice-reminders/worker` si importable en tsx, sinon redéclarer `new Date('2026-05-19T00:00:00Z')` avec commentaire de parité) : par tenant, `reminderDays = tenant.invoiceReminderDays ?? [30,45]`, factures `status IN (ISSUED, PARTIAL, OVERDUE)`, `issueDate >= REMINDER_START_DATE`, échéance dépassée du 1er seuil (OR dueDate/issueDate), puis filtres du core : `reminderCount < maxLevel`, `lastReminderAt` null ou > 24 h. Include `payerOrg { legalName, email, emailBilling }` et `participant.person { firstName, lastName, email }`.
    Pour chaque facture : numéro, payerName, email destinataire (cascade `emailBilling ?? email ?? person.email`), niveau qui partirait (`reminderCount + 1`), `reminderCount` actuel, `lastReminderAt`, montant restant. **Flag `⚠ APPRENANT`** si le destinataire résolu est `participant.person.email` (règle payeur : l'auto-entrepreneur est son propre payeur — une relance touche alors un apprenant).

    **Partie B — relances brûlées en dry-run (Pitfall 1) :**
    ```typescript
    const burned = await prisma.auditLog.findMany({
      where: { action: 'invoices.reminder_sent', diff: { path: ['dryRun'], equals: true } },
      orderBy: { createdAt: 'asc' },
    });
    ```
    Grouper par `entityId` (facture) : nombre de relances brûlées, niveaux (`diff.level`), dates. Croiser avec `Invoice.reminderCount` actuel : colonne « compteur actuel vs relances réellement parties (0) ».

    **Sortie :** markdown → `.planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md` : date, tableau A (envois en attente + flags apprenant), tableau B (brûlées par facture), et la section « Décision requise (checkpoint plan 22-07) : ① reset des compteurs brûlés (remise à l'état pré-dry-run) ② reset sélectif ③ acceptation en l'état — ATTENTION : un reset aveugle re-enverrait un niveau 1 à des payeurs peut-être déjà relancés manuellement hors outil. »
    EXÉCUTER le script et committer le rapport.
    Commit : `feat(22-04): pending-sends report — cron selection replica + burned dry-run reminders (D-06)`
  </action>
  <verify>
    <automated>pnpm tsx apps/web/scripts/pending-reminders-report.ts >/dev/null && grep -q "invoices.reminder_sent" apps/web/scripts/pending-reminders-report.ts && grep -q "Décision requise" ".planning/phases/22-bascule-prod-conformit-rgpd/22-PENDING-SENDS-REPORT.md"</automated>
  </verify>
  <acceptance_criteria>
    - Script lecture seule : grep négatif `\.update(`, `\.create(`, `\.delete(`, `$executeRaw` dans pending-reminders-report.ts
    - grep "2026-05-19" OU import de REMINDER_START_DATE (parité de sélection)
    - grep "emailBilling" (cascade destinataire répliquée) et "APPRENANT" (flag règle payeur)
    - 22-PENDING-SENDS-REPORT.md : date + tableau A + tableau B + section « Décision requise » avec les 3 options
    - Le rapport croise reminderCount actuel et relances brûlées (colonne comparative)
  </acceptance_criteria>
  <done>Laurent dispose de la liste nominative complète (envois futurs + historique brûlé) pour décider en connaissance au checkpoint 22-07 — aucune surprise au flip.</done>
</task>

</tasks>

<verification>
- Les 2 scripts tournent en lecture seule contre les bonnes bases ; les 2 rapports sont datés et commités.
- Aucun secret ni valeur d'env dans les rapports ; `.env.vercel-prod` détruit.
</verification>

<success_criteria>
Runbook §1 (sanity env) et §4 (rapport relances) sont adossés à des outils exécutés et des rapports réels — le flip MAIL_DRY_RUN de 22-07 est décidable sans inconnue.
</success_criteria>

<output>
After completion, create `.planning/phases/22-bascule-prod-conformit-rgpd/22-04-SUMMARY.md`
</output>
