---
phase: 20-worker-3-h-te-doc-engines
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/actions/storage-upload.ts
  - apps/web/src/lib/preinscription-ocr-queue.ts
  - apps/web/scripts/preinscription-ocr-worker.ts
  - apps/web/src/lib/__tests__/preinscription-ocr-queue.test.ts
autonomous: true
requirements: [WORK-04]
must_haves:
  truths:
    - "La confirmation d'upload pré-inscription (server action Vercel) NE lance PLUS l'OCR en fire-and-forget — elle laisse la PreEnrollment en statut SUBMITTED"
    - "Un worker long-vivant poll les PreEnrollment en statut SUBMITTED (claim atomique SKIP LOCKED) et exécute extractPreEnrollmentDocuments sur l'hôte qui possède pdftoppm"
    - "Un échec OCR laisse un statut/message explicite (SUBMITTED + aiErrorMsg), jamais un EXTRACTED vide silencieux"
  artifacts:
    - path: "apps/web/src/lib/preinscription-ocr-queue.ts"
      provides: "Driver de poll SKIP LOCKED des PreEnrollment SUBMITTED (module worker-safe, 0 import React)"
      exports: ["processNextPreEnrollmentOcr"]
    - path: "apps/web/scripts/preinscription-ocr-worker.ts"
      provides: "Entry-point worker OCR (poll loop)"
      contains: "processNextPreEnrollmentOcr"
  key_links:
    - from: "apps/web/src/server/actions/storage-upload.ts"
      to: "PreEnrollment.status = SUBMITTED"
      via: "suppression du fire-and-forget, le worker prend le relais via poll"
      pattern: "status: 'SUBMITTED'"
    - from: "apps/web/src/lib/preinscription-ocr-queue.ts"
      to: "extractPreEnrollmentDocuments"
      via: "appel après claim atomique"
      pattern: "extractPreEnrollmentDocuments"
---

<objective>
Relocaliser le déclenchement de l'OCR pré-inscription du fire-and-forget dans la server action Vercel (`confirmPreEnrollmentUpload`, qui MEURT en serverless et n'a pas `pdftoppm`) vers un worker long-vivant qui poll les PreEnrollment en statut `SUBMITTED` et exécute `extractPreEnrollmentDocuments` sur le 3ᵉ hôte (qui a poppler). C'est le cœur de WORK-04 / pilier #4.

Purpose: En serverless Vercel, `Promise.resolve().then(() => extractPreEnrollmentDocuments(...))` est tué dès la réponse HTTP renvoyée, et `pdftoppm` n'existe pas sur Vercel → l'OCR des PDF scannés ne se ferait jamais (pilier #4 dégradé silencieusement). Le déclenchement doit produire un « job » consommé par un process qui survit et possède le binaire.
Output: Server action qui laisse SUBMITTED sans OCR + driver de poll SKIP LOCKED réutilisant le statut PreEnrollment comme queue (aucune migration schema) + entry-point worker + tests.

Design (D discrétion, RESEARCH Pattern 4 option 2) : réutiliser le statut `PreEnrollment.SUBMITTED` comme file d'attente native (pas de nouvelle table, pas de migration Prisma). Le worker claim les rows SUBMITTED via `UPDATE ... WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING`, passe en EXTRACTING, puis appelle `extractPreEnrollmentDocuments` (qui gère déjà EXTRACTING→EXTRACTED / →SUBMITTED+aiErrorMsg). Flux utilisateur strictement identique.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-CONTEXT.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md

<interfaces>
Pipeline OCR existant — `apps/web/src/lib/preinscription-extractor.ts` (worker-safe, à ne pas toucher) :
```typescript
export async function extractPreEnrollmentDocuments(preEnrollmentId: string): Promise<void>
// interne : status EXTRACTING au début (ligne 166), EXTRACTED en succès (ligne 223),
// SUBMITTED + aiErrorMsg en échec (lignes 235-236) → contrat anti-dégradation D-06 DÉJÀ garanti
```

Statut enum — `packages/db/prisma/schema.prisma:1280` :
```prisma
enum PreEnrollmentStatus {
  PENDING_FORM   // lien généré
  SUBMITTED      // formulaire soumis, fichiers reçus  ← queue OCR
  EXTRACTING     // pipeline IA en cours
  EXTRACTED      // prêt à valider
}
model PreEnrollment { id String @id ; status PreEnrollmentStatus @default(PENDING_FORM) ; @@index([tenantId, status]) }
```

Pattern claim SKIP LOCKED de référence — `apps/web/src/lib/closure/queue-postgres.ts` :
```typescript
export async function processNextClosureJobs(maxJobs = 3): Promise<{ processed; succeeded; failed }>
// claimJobs fait un UPDATE ... FOR UPDATE SKIP LOCKED RETURNING via $queryRaw ; réutiliser ce pattern
```

Entry-point de référence — `apps/web/scripts/closure-worker-postgres.ts` :
```typescript
import '@qualiof/shared/env';
const POLL_INTERVAL_MS = Number(process.env.QUEUE_POLL_INTERVAL_MS ?? 3000);
// while (!stopped) { await processNextClosureJobs(CONCURRENCY); await sleep(POLL) } + SIGINT/SIGTERM
```

Server action à modifier — `apps/web/src/server/actions/storage-upload.ts:161-166` (le fire-and-forget à retirer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer le driver de poll SKIP LOCKED des PreEnrollment SUBMITTED (module worker-safe)</name>
  <read_first>
    - apps/web/src/lib/closure/queue-postgres.ts (pattern claim atomique FOR UPDATE SKIP LOCKED + $queryRaw + gestion attempts)
    - apps/web/src/lib/preinscription-extractor.ts (extractPreEnrollmentDocuments : signature, gestion statuts EXTRACTING/EXTRACTED/SUBMITTED+aiErrorMsg)
    - packages/db/prisma/schema.prisma (lignes 1280-1345 : enum PreEnrollmentStatus + model PreEnrollment + index [tenantId, status])
    - apps/web/src/lib/veille/core.ts (exemple de module worker-safe : commentaire "ZÉRO import server-action / React / next/cache")
  </read_first>
  <action>
    Créer `apps/web/src/lib/preinscription-ocr-queue.ts` — module NEUTRE (0 import React / server-action / next/cache ; import autorisés : `@qualiof/db`, `./preinscription-extractor`) :

    ```typescript
    /**
     * Phase 20 WORK-04 — Driver de poll OCR pré-inscription (remplace le
     * fire-and-forget serverless mort sur Vercel).
     * Réutilise le statut PreEnrollment.SUBMITTED comme file d'attente (pas de
     * nouvelle table). Claim atomique FOR UPDATE SKIP LOCKED → EXTRACTING →
     * extractPreEnrollmentDocuments (qui gère EXTRACTED / échec explicite D-06).
     * Worker-safe : aucun import React/auth.
     */
    import { prisma } from '@qualiof/db';
    import { extractPreEnrollmentDocuments } from './preinscription-extractor';

    export async function processNextPreEnrollmentOcr(maxJobs = 3): Promise<{
      processed: number; succeeded: number; failed: number;
    }> {
      // 1. Claim atomique : passe SUBMITTED → EXTRACTING et RETURNING les ids
      //    FOR UPDATE SKIP LOCKED évite qu'un 2e worker prenne la même row.
      const claimed = await prisma.$queryRaw<{ id: string }[]>`
        UPDATE "PreEnrollment"
        SET "status" = 'EXTRACTING', "updatedAt" = now()
        WHERE "id" IN (
          SELECT "id" FROM "PreEnrollment"
          WHERE "status" = 'SUBMITTED'
          ORDER BY "submittedAt" ASC NULLS LAST
          LIMIT ${maxJobs}
          FOR UPDATE SKIP LOCKED
        )
        RETURNING "id"
      `;
      if (claimed.length === 0) return { processed: 0, succeeded: 0, failed: 0 };

      let succeeded = 0, failed = 0;
      // Séquentiel (convention projet : jamais de runs parallèles massifs → deadlocks)
      for (const row of claimed) {
        try {
          // extractPreEnrollmentDocuments repasse EXTRACTING (idempotent) →
          // EXTRACTED en succès, ou SUBMITTED + aiErrorMsg en échec (D-06 anti-dégradation).
          await extractPreEnrollmentDocuments(row.id);
          succeeded++;
        } catch (e: any) {
          failed++;
          // Filet : si l'extractor a jeté sans repasser SUBMITTED, on le repasse
          // SUBMITTED + message (jamais laisser bloqué EXTRACTING silencieux — D-06).
          await prisma.preEnrollment.update({
            where: { id: row.id },
            data: { status: 'SUBMITTED', aiErrorMsg: `OCR worker error: ${e?.message ?? String(e)}` },
          }).catch(() => {});
          console.error(`[ocr-queue] preenrollment ${row.id.slice(0, 8)}… failed: ${e?.message?.slice(0, 200)}`);
        }
      }
      return { processed: claimed.length, succeeded, failed };
    }
    ```
    IMPORTANT : utiliser le nom de table/colonnes EXACT tel qu'en base (`"PreEnrollment"`, `"status"`, `"submittedAt"`, `"updatedAt"`) — Prisma mappe les modèles PascalCase en tables PascalCase (pas de `@@map` sur ce modèle, vérifié). Enum values `'SUBMITTED'`/`'EXTRACTING'` sont les valeurs Postgres du type enum.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/preinscription-ocr-queue.ts` existe
    - `grep -c "export async function processNextPreEnrollmentOcr" apps/web/src/lib/preinscription-ocr-queue.ts` retourne 1
    - `grep -c "FOR UPDATE SKIP LOCKED" apps/web/src/lib/preinscription-ocr-queue.ts` retourne 1
    - `grep -c "extractPreEnrollmentDocuments" apps/web/src/lib/preinscription-ocr-queue.ts` retourne au moins 1
    - `grep -c "from 'react'\|next/cache\|@/lib/auth\|@/lib/rbac\|validateRequest" apps/web/src/lib/preinscription-ocr-queue.ts` retourne 0 (module worker-safe)
    - `grep -c "aiErrorMsg" apps/web/src/lib/preinscription-ocr-queue.ts` retourne au moins 1 (filet anti-dégradation D-06)
    - `tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Le driver claim atomiquement les PreEnrollment SUBMITTED, passe EXTRACTING, exécute l'extractor, et garantit qu'un échec ne laisse jamais une row bloquée silencieusement. Module worker-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Retirer le fire-and-forget de la server action + créer l'entry-point worker OCR + tests</name>
  <read_first>
    - apps/web/src/server/actions/storage-upload.ts (lignes 118-169 : confirmPreEnrollmentUpload + fire-and-forget lignes 161-166 + import ligne 22)
    - apps/web/scripts/closure-worker-postgres.ts (structure entry-point : poll loop while + sleep + SIGINT/SIGTERM + import env fail-loud)
    - apps/web/src/lib/preinscription-ocr-queue.ts (le driver créé en Task 1)
  </read_first>
  <action>
    1. Dans `apps/web/src/server/actions/storage-upload.ts` :
       - SUPPRIMER le bloc fire-and-forget (lignes ~161-166) :
         ```typescript
         // Recâblage OCR (Pitfall 4) : fire-and-forget, l'utilisateur a déjà sa confirmation.
         Promise.resolve().then(() =>
           extractPreEnrollmentDocuments(pe.id).catch((err) =>
             console.error('Extraction IA échouée pour', pe.id, err),
           ),
         );
         ```
         Le remplacer par un commentaire :
         ```typescript
         // Phase 20 WORK-04 : l'OCR n'est PLUS déclenché ici (fire-and-forget mort en
         // serverless Vercel + pas de pdftoppm). La PreEnrollment reste en statut SUBMITTED ;
         // le worker cloud (preinscription-ocr-worker) la poll et exécute l'extraction sur
         // l'hôte qui possède poppler. Flux utilisateur identique (confirmation immédiate).
         ```
       - SUPPRIMER l'import devenu inutile `import { extractPreEnrollmentDocuments } from '@/lib/preinscription-extractor';` (ligne 22) SI plus aucun autre usage dans le fichier (vérifier par grep avant suppression — s'il reste un usage, garder l'import).
       - CONSERVER le `status: 'SUBMITTED'` dans le `prisma.preEnrollment.update` (c'est ce qui alimente la queue).

    2. Créer `apps/web/scripts/preinscription-ocr-worker.ts` (calqué sur `closure-worker-postgres.ts`) :
    ```typescript
    import '@qualiof/shared/env';
    import { processNextPreEnrollmentOcr } from '../src/lib/preinscription-ocr-queue';

    const POLL_INTERVAL_MS = Number(process.env.OCR_POLL_INTERVAL_MS ?? 5000);
    const CONCURRENCY = Number(process.env.OCR_CONCURRENCY ?? 2);
    let stopped = false;

    async function loop() {
      console.log(`[ocr-worker] started — concurrency=${CONCURRENCY}, poll=${POLL_INTERVAL_MS}ms`);
      while (!stopped) {
        try {
          const r = await processNextPreEnrollmentOcr(CONCURRENCY);
          if (r.processed > 0) console.log(`[ocr-worker] processed=${r.processed} ok=${r.succeeded} fail=${r.failed}`);
        } catch (e: any) {
          console.error('[ocr-worker] loop error:', e.message);
        }
        await new Promise((resolve) => setTimeout(resolve, POLL_INTERVAL_MS));
      }
      console.log('[ocr-worker] stopped');
    }
    const shutdown = (signal: string) => { console.log(`[ocr-worker] received ${signal}, draining…`); stopped = true; setTimeout(() => process.exit(0), 2000); };
    process.on('SIGINT', () => shutdown('SIGINT'));
    process.on('SIGTERM', () => shutdown('SIGTERM'));
    loop().catch((e) => { console.error('[ocr-worker] fatal:', e); process.exit(1); });
    ```

    3. Ajouter le script pnpm dans `apps/web/package.json` scripts :
       `"worker:ocr": "dotenv -e ../../.env -- tsx watch --clear-screen=false scripts/preinscription-ocr-worker.ts"`

    4. Créer `apps/web/src/lib/__tests__/preinscription-ocr-queue.test.ts` — tests hermétiques (mock `@qualiof/db` + `../preinscription-extractor` + `@qualiof/shared/env`) :
       - Test 1 : quand `$queryRaw` retourne `[{id:'a'},{id:'b'}]`, `extractPreEnrollmentDocuments` est appelé 2× avec 'a' puis 'b', retour `{ processed:2, succeeded:2, failed:0 }`.
       - Test 2 : quand `$queryRaw` retourne `[]`, retour `{ processed:0, ... }` et `extractPreEnrollmentDocuments` jamais appelé.
       - Test 3 (anti-dégradation D-06) : si `extractPreEnrollmentDocuments` throw pour un id, `prisma.preEnrollment.update` est appelé avec `status:'SUBMITTED'` et un `aiErrorMsg` non vide, retour `failed>=1`.
       - Utiliser `vi.hoisted` pour les mocks (convention projet).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- preinscription-ocr-queue 2>&1 | tail -15 && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Promise.resolve().then" apps/web/src/server/actions/storage-upload.ts` retourne 0 (fire-and-forget retiré)
    - `grep -c "status: 'SUBMITTED'" apps/web/src/server/actions/storage-upload.ts` retourne au moins 1 (queue alimentée)
    - `apps/web/scripts/preinscription-ocr-worker.ts` existe et `grep -c "processNextPreEnrollmentOcr" apps/web/scripts/preinscription-ocr-worker.ts` retourne 1
    - `grep -c "import '@qualiof/shared/env'" apps/web/scripts/preinscription-ocr-worker.ts` retourne 1
    - `grep '"worker:ocr"' apps/web/package.json` retourne une ligne
    - `apps/web/src/lib/__tests__/preinscription-ocr-queue.test.ts` existe
    - `pnpm --filter @qualiof/web test -- preinscription-ocr-queue` exit 0, au moins 3 tests passent (dont le test D-06 statut SUBMITTED + aiErrorMsg)
    - `tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>La server action ne lance plus l'OCR (laisse SUBMITTED), le worker OCR poll et exécute l'extraction sur le 3ᵉ hôte, un échec produit un message explicite (jamais EXTRACTED vide). Tests verts. WORK-04 (code) satisfait.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web test -- preinscription-ocr-queue` vert
- `tsc --noEmit` exit 0
- La preuve INFRA réelle (PDF scanné sans couche texte → pdftoppm sur le worker → vision OpenRouter → EXTRACTED données réelles) est déléguée au plan 20-05 (smoke gaté Laurent, exige l'image avec poppler déployée)
</verification>

<success_criteria>
Le déclenchement OCR est relocalisé du fire-and-forget serverless vers un worker long-vivant qui poll les PreEnrollment SUBMITTED. Aucune dégradation silencieuse possible : un échec repasse SUBMITTED avec aiErrorMsg. WORK-04 (partie code) satisfait ; la validation poppler réelle est au plan 20-05.
</success_criteria>

<output>
After completion, create `.planning/phases/20-worker-3-h-te-doc-engines/20-02-SUMMARY.md`
</output>
