---
phase: 20-worker-3-h-te-doc-engines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/scripts/veille-worker.ts
  - apps/web/scripts/invoice-reminder-worker.ts
  - apps/web/src/lib/veille/worker.ts
  - apps/web/src/lib/invoice-reminders/worker.ts
  - apps/web/scripts/__tests__/cron-workers.test.ts
autonomous: true
requirements: [WORK-02]
must_haves:
  truths:
    - "Le worker veille se planifie via croner (lundi 8h Europe/Paris) sans aucune connexion Redis"
    - "Le worker relances-factures se planifie via croner (quotidien 8h Europe/Paris) sans aucune connexion Redis"
    - "Aucun des 2 entry-points croner n'importe bullmq ni ioredis"
    - "Le handler métier (processVeilleJob / processReminderJob) est fonctionnellement inchangé (mêmes requêtes DB, même idempotence 24h factures)"
  artifacts:
    - path: "apps/web/scripts/veille-worker.ts"
      provides: "Entry-point croner veille (remplace BullMQ)"
      contains: "croner"
    - path: "apps/web/scripts/invoice-reminder-worker.ts"
      provides: "Entry-point croner relances factures (remplace BullMQ)"
      contains: "croner"
    - path: "apps/web/src/lib/veille/worker.ts"
      provides: "Handler veille sans startVeilleWorker BullMQ"
      exports: ["processVeilleJob"]
    - path: "apps/web/src/lib/invoice-reminders/worker.ts"
      provides: "Handler relances sans startInvoiceReminderWorker BullMQ"
      exports: ["processReminderJob"]
  key_links:
    - from: "apps/web/scripts/veille-worker.ts"
      to: "processVeilleJob"
      via: "import direct du handler (plus de Worker BullMQ)"
      pattern: "import.*processVeilleJob"
    - from: "apps/web/scripts/invoice-reminder-worker.ts"
      to: "processReminderJob"
      via: "import direct du handler (plus de Worker BullMQ)"
      pattern: "import.*processReminderJob"
---

<objective>
Porter les 2 workers de fond planifiés (veille hebdo + relances factures quotidiennes) hors de BullMQ/Redis vers un cron interne au process (`croner`), conformément à D-03 (« Redis viré partout — 0 Redis »). Le closure utilise déjà le driver Postgres SKIP LOCKED (plan hors scope ici, entry-point `closure-worker-postgres.ts` déjà écrit) ; ce plan traite UNIQUEMENT les 2 workers cron.

Purpose: Sans Redis sur le 3ᵉ hôte, la planification hebdo/quotidienne doit renaître du code au boot du worker via un cron DST-safe. C'est la brique WORK-02 (remplacement BullMQ→croner) qui rend l'image Docker (plan 20-04) déployable sans service Redis.
Output: 2 entry-points croner + 2 handlers découplés de BullMQ + `croner` en dépendance + tests unitaires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-CONTEXT.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md

<interfaces>
<!-- Contrats existants que l'executor DOIT respecter. Extraits du codebase. -->

Handler veille — `apps/web/src/lib/veille/worker.ts` (à conserver, retirer la partie BullMQ) :
```typescript
export async function processVeilleJob(job: Job<VeilleJobPayload>): Promise<TenantIngestSummary[]>
// itère prisma.tenant.findMany → ingestRssOnceForTenant(t.id) pour chaque tenant
// job.data.triggered_by est le SEUL champ lu du Job
```

Handler relances — `apps/web/src/lib/invoice-reminders/worker.ts` (à conserver, retirer la partie BullMQ) :
```typescript
export async function processReminderJob(job: Job<ReminderJobPayload>): Promise<{ processed: number }>
// job.data.triggered_by est le SEUL champ lu du Job ('cron' | 'manual_admin_trigger')
// idempotence 24h via REMINDER_DEDUP_MS, filtre REMINDER_START_DATE — NE PAS TOUCHER
export const REMINDER_START_DATE: Date // conserver
```

Handler ingest — `apps/web/src/lib/veille/core.ts` :
```typescript
export async function ingestRssOnceForTenant(tenantId: string): Promise<IngestResult>
```

croner API (vérifié RESEARCH 2026-07-05, version 10.0.1) :
```typescript
import { Cron } from 'croner';
new Cron('0 8 * * 1', { name, timezone: 'Europe/Paris', catch: (e) => {...} }, async () => {...});
// timezone via Intl → DST Europe/Paris correct ; catch intégré → une erreur n'arrête pas le process
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter croner et découpler les 2 handlers de BullMQ (Job → payload neutre)</name>
  <read_first>
    - apps/web/package.json (deps + scripts actuels)
    - apps/web/src/lib/veille/worker.ts (état actuel : Worker BullMQ + processVeilleJob + startVeilleWorker)
    - apps/web/src/lib/invoice-reminders/worker.ts (état actuel : Worker BullMQ + processReminderJob + startInvoiceReminderWorker + scheduleDailyReminders + REMINDER_START_DATE + REMINDER_DEDUP_MS)
    - apps/web/src/lib/veille/core.ts (ingestRssOnceForTenant — worker-safe, à ne pas toucher)
  </read_first>
  <action>
    1. Ajouter `croner@^10.0.1` aux dependencies de `apps/web/package.json` (bloc `"dependencies"`, ordre alphabétique après `clsx`). Exécuter `pnpm --filter @qualiof/web add croner` (résout la version exacte 10.0.1 vérifiée au registre).

    2. Dans `apps/web/src/lib/veille/worker.ts` :
       - RETIRER l'import `import { Worker, type Job } from 'bullmq';` et `import { getWorkerRedis } from '../closure/redis';` et `import { VEILLE_QUEUE_NAME, type VeilleJobPayload } from './queue';`.
       - Remplacer la signature de `processVeilleJob` pour ne plus dépendre du type `Job<VeilleJobPayload>` de BullMQ : `export async function processVeilleJob(input: { triggered_by: string }): Promise<TenantIngestSummary[]>`. À l'intérieur, remplacer `job.data.triggered_by` par `input.triggered_by` et `job.id` par une string fixe `'cron'` dans le console.log.
       - SUPPRIMER entièrement la fonction `startVeilleWorker()` (elle construit un Worker BullMQ — morte après bascule).
       - Conserver l'interface `TenantIngestSummary` et l'itération `prisma.tenant.findMany` → `ingestRssOnceForTenant`.

    3. Dans `apps/web/src/lib/invoice-reminders/worker.ts` :
       - RETIRER l'import `import { Worker, type Job } from 'bullmq';`, `import { getWorkerRedis } from '../closure/redis';`, et `import { getInvoiceReminderQueue, INVOICE_REMINDER_QUEUE_NAME } from './queue';`.
       - Remplacer la signature : `export async function processReminderJob(input: { triggered_by: 'cron' | 'manual_admin_trigger' }): Promise<{ processed: number }>`. Remplacer `job.data.triggered_by` par `input.triggered_by`.
       - SUPPRIMER entièrement `startInvoiceReminderWorker()` ET `scheduleDailyReminders()` (BullMQ morts).
       - CONSERVER STRICTEMENT : `REMINDER_START_DATE`, `REMINDER_DEDUP_MS`, toute la logique de scan factures (statuts ISSUED/PARTIAL/OVERDUE, filtre issueDate >= REMINDER_START_DATE, idempotence 24h, niveau max). Ne rien changer au comportement métier.
       - ⚠ `sendInvoiceReminder` est importé depuis `@/server/actions/invoices` (server action `'use server'` important `next/cache`). Elle tournait déjà en worker BullMQ → conserver l'import tel quel (à re-valider en conteneur au plan 20-05).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'bullmq'" apps/web/src/lib/veille/worker.ts` retourne 0
    - `grep -c "from 'bullmq'" apps/web/src/lib/invoice-reminders/worker.ts` retourne 0
    - `grep -c "startVeilleWorker" apps/web/src/lib/veille/worker.ts` retourne 0
    - `grep -c "startInvoiceReminderWorker\|scheduleDailyReminders" apps/web/src/lib/invoice-reminders/worker.ts` retourne 0
    - `grep -c "REMINDER_START_DATE" apps/web/src/lib/invoice-reminders/worker.ts` retourne au moins 2 (déclaration + usage conservés)
    - `grep -c "export async function processVeilleJob" apps/web/src/lib/veille/worker.ts` retourne 1
    - `grep -c "export async function processReminderJob" apps/web/src/lib/invoice-reminders/worker.ts` retourne 1
    - `grep '"croner"' apps/web/package.json` retourne une ligne avec version `^10`
    - `tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Les 2 handlers acceptent un payload neutre `{ triggered_by }`, ne référencent plus bullmq/ioredis/redis, `croner` est en dépendance, tsc passe.</done>
</task>

<task type="auto">
  <name>Task 2: Réécrire les 2 entry-points en croner + tests + scripts pnpm</name>
  <read_first>
    - apps/web/scripts/veille-worker.ts (état actuel : startVeilleWorker + scheduleWeeklyVeille + mode dégradé Redis)
    - apps/web/scripts/invoice-reminder-worker.ts (état actuel : startInvoiceReminderWorker + scheduleDailyReminders + mode dégradé Redis)
    - apps/web/scripts/closure-worker-postgres.ts (pattern entry-point worker cloud : import '@qualiof/shared/env' en tête, gestion SIGINT/SIGTERM, console.log de démarrage)
    - apps/web/package.json (scripts worker:* actuels)
  </read_first>
  <action>
    1. Réécrire `apps/web/scripts/veille-worker.ts` intégralement :
    ```typescript
    import '@qualiof/shared/env'; // fail-loud au boot (parité closure-worker-postgres.ts)
    import { Cron } from 'croner';
    import { processVeilleJob } from '../src/lib/veille/worker';

    // Hebdo lundi 8h Europe/Paris (remplace repeat { pattern:'0 8 * * 1', tz:'Europe/Paris' } BullMQ)
    const job = new Cron('0 8 * * 1', {
      name: 'veille',
      timezone: 'Europe/Paris',
      catch: (e: unknown) => console.error('[veille-worker] cron error', e),
    }, async () => {
      await processVeilleJob({ triggered_by: 'cron' });
    });
    console.log('[veille-worker] croner registered (lundi 08:00 Europe/Paris), next:', job.nextRun());

    const shutdown = (signal: string) => {
      console.log(`[veille-worker] received ${signal}, stopping cron…`);
      job.stop();
      process.exit(0);
    };
    process.on('SIGINT', () => shutdown('SIGINT'));
    process.on('SIGTERM', () => shutdown('SIGTERM'));
    ```
    Retirer TOUT le bloc `try/catch` « mode dégradé Redis » et le keepalive `setInterval` (le process reste vivant grâce au cron enregistré).

    2. Réécrire `apps/web/scripts/invoice-reminder-worker.ts` intégralement, même structure :
    ```typescript
    import '@qualiof/shared/env';
    import { Cron } from 'croner';
    import { processReminderJob } from '../src/lib/invoice-reminders/worker';

    // Quotidien 8h Europe/Paris (remplace repeat { pattern:'0 8 * * *', tz:'Europe/Paris' } BullMQ)
    const job = new Cron('0 8 * * *', {
      name: 'invoice-reminders',
      timezone: 'Europe/Paris',
      catch: (e: unknown) => console.error('[invoice-reminder-worker] cron error', e),
    }, async () => {
      await processReminderJob({ triggered_by: 'cron' });
    });
    console.log('[invoice-reminder-worker] croner registered (quotidien 08:00 Europe/Paris), next:', job.nextRun());

    const shutdown = (signal: string) => {
      console.log(`[invoice-reminder-worker] received ${signal}, stopping cron…`);
      job.stop();
      process.exit(0);
    };
    process.on('SIGINT', () => shutdown('SIGINT'));
    process.on('SIGTERM', () => shutdown('SIGTERM'));
    ```

    3. Dans `apps/web/package.json` scripts, retirer `dotenv -e ../../.env.local` du worker closure:pg si présent est hors scope ; pour les 2 workers cron, garder le format `dotenv -e ../../.env -- tsx …` MAIS retirer `watch` (le conteneur ne watch pas) n'est PAS requis ici — les scripts dev restent en `tsx watch`. Laisser `worker:veille` et `worker:reminders` inchangés (ils pointent déjà les bons fichiers). L'exécution conteneur passera par pm2 (plan 20-04), pas par ces scripts pnpm.

    4. Créer `apps/web/scripts/__tests__/cron-workers.test.ts` — tests unitaires hermétiques (Vitest, mock `croner` et les handlers) :
       - Test 1 : importer le module `veille-worker.ts` construit un `Cron` avec le pattern `'0 8 * * 1'` et `timezone: 'Europe/Paris'` (spy sur le constructeur croner mocké).
       - Test 2 : importer `invoice-reminder-worker.ts` construit un `Cron` avec le pattern `'0 8 * * *'` et `timezone: 'Europe/Paris'`.
       - Test 3 : le callback passé à Cron appelle `processVeilleJob({ triggered_by: 'cron' })` (resp. `processReminderJob`).
       - Mocker `@qualiof/shared/env` (getter sur mockEnv hoisté via `vi.hoisted`, convention projet 17-02/18-01) pour ne pas charger `.env` au collect.
       - Mocker `croner` via `vi.mock('croner', ...)` exposant un faux `Cron` class avec constructeur spyable + méthodes `nextRun()`/`stop()`.
       - Mocker `../src/lib/veille/worker` et `../src/lib/invoice-reminders/worker` pour capturer les appels handler.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web test -- cron-workers 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'croner'" apps/web/scripts/veille-worker.ts` retourne 1
    - `grep -c "from 'croner'" apps/web/scripts/invoice-reminder-worker.ts` retourne 1
    - `grep -c "'0 8 \* \* 1'" apps/web/scripts/veille-worker.ts` retourne 1 (lundi)
    - `grep -c "'0 8 \* \* \*'" apps/web/scripts/invoice-reminder-worker.ts` retourne 1 (quotidien)
    - `grep -c "Europe/Paris" apps/web/scripts/veille-worker.ts` retourne 1
    - `grep -c "setInterval\|mode dégradé\|Redis indisponible" apps/web/scripts/veille-worker.ts` retourne 0 (keepalive Redis retiré)
    - `grep -c "import '@qualiof/shared/env'" apps/web/scripts/veille-worker.ts` retourne 1 (fail-loud boot)
    - `apps/web/scripts/__tests__/cron-workers.test.ts` existe
    - `pnpm --filter @qualiof/web test -- cron-workers` exit 0, au moins 3 tests passent
  </acceptance_criteria>
  <done>Les 2 entry-points enregistrent leur cron croner aux bons horaires Europe/Paris, importent env fail-loud, gèrent SIGINT/SIGTERM, et sont couverts par des tests verts. Zéro trace de mode dégradé Redis.</done>
</task>

</tasks>

<verification>
- `tsc --noEmit` exit 0 sur apps/web
- `pnpm --filter @qualiof/web test -- cron-workers` vert
- `grep -rc "from 'bullmq'" apps/web/src/lib/veille/ apps/web/src/lib/invoice-reminders/ apps/web/scripts/veille-worker.ts apps/web/scripts/invoice-reminder-worker.ts` = 0 partout (hors fichiers queue.ts BullMQ morts traités au plan 20-04)
</verification>

<success_criteria>
Les workers veille et relances-factures se planifient via croner (lundi 8h / quotidien 8h Europe/Paris) sans aucune connexion Redis, en conservant leur logique métier intacte (idempotence 24h factures, ingestion RSS multi-tenant). WORK-02 (partie code) satisfait.
</success_criteria>

<output>
After completion, create `.planning/phases/20-worker-3-h-te-doc-engines/20-01-SUMMARY.md`
</output>
