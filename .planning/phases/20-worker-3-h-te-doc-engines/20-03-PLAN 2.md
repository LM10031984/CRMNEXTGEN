---
phase: 20-worker-3-h-te-doc-engines
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/weasyprint/server.py
  - docker/gotenberg-proxy/Caddyfile
  - docker/gotenberg-proxy/Dockerfile
  - docker/weasyprint/test_auth.py
autonomous: true
requirements: [WORK-01]
must_haves:
  truths:
    - "Le micro-service WeasyPrint refuse (401) une requete POST /pdf sans header Authorization Bearer DOC_ENGINE_TOKEN"
    - "Le micro-service WeasyPrint accepte une requete avec le bon Bearer et laisse /health non authentifie"
    - "Un proxy Bearer devant Gotenberg valide le Bearer avant de forward (Gotenberg 8 ne parle que basic-auth, pas Bearer)"
    - "Le check Bearer est conditionnel au token (dev local sans token non casse, parite pdf-render.ts authHeaders())"
  artifacts:
    - path: "docker/weasyprint/server.py"
      provides: "Enforcement Bearer server-side dans Flask"
      contains: "before_request"
    - path: "docker/gotenberg-proxy/Caddyfile"
      provides: "Reverse-proxy Bearer devant Gotenberg"
      contains: "Authorization"
  key_links:
    - from: "docker/weasyprint/server.py before_request"
      to: "DOC_ENGINE_TOKEN (env)"
      via: "comparaison Authorization == Bearer token"
      pattern: "DOC_ENGINE_TOKEN"
    - from: "docker/gotenberg-proxy/Caddyfile"
      to: "gotenberg upstream"
      via: "reverse_proxy apres validation du header"
      pattern: "reverse_proxy"
---

<objective>
Ajouter l'enforcement server-side du Bearer DOC_ENGINE_TOKEN sur les 2 doc-engines exposes en HTTPS public. Le client pdf-render.ts envoie deja Authorization Bearer DOC_ENGINE_TOKEN (cable Phase 17) ; il manque le controle cote serveur avant l'exposition publique.

Purpose: En Phase 21, Vercel (hors reseau prive Railway) appellera Gotenberg/WeasyPrint par leur domaine PUBLIC. Des endpoints de rendu PDF ouverts sans auth = abus/DoS. WeasyPrint = notre code Flask (check trivial). Gotenberg 8 ne connait QUE basic-auth (Pitfall 4 RESEARCH), pas Bearer. Un mini reverse-proxy Bearer (Caddy) le protege en amont, coherent avec le client existant.
Output: check Bearer Flask WeasyPrint + proxy Caddy Bearer devant Gotenberg + Dockerfile proxy + tests Python.

Decision (D discretion, Open Question 1 RESEARCH tranchee) : proxy Caddy leger devant Gotenberg (pas de bascule du client sur basic-auth). Le client pdf-render.ts reste homogene (Bearer partout), aucune regression Phase 17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-CONTEXT.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md

<interfaces>
Client Bearer deja cable — apps/web/src/lib/pdf-render.ts (Phase 17, a ne pas toucher) :
- authHeaders() renvoie { Authorization: `Bearer ${token}` } si token defini, sinon {}.
- token = DOC_ENGINE_TOKEN ; CONDITIONNEL. Dev local sans token n'envoie pas de header.
- renderHtmlToPdf (Gotenberg, multipart) et renderHtmlToPdfWeasy (WeasyPrint, body string) portent authHeaders().

WeasyPrint actuel — docker/weasyprint/server.py :
- from flask import Flask, request, Response
- route POST /pdf (body = HTML brut, renvoie PDF) ; route GET /health ({status: ok})
- lance par gunicorn -w 2 -b 0.0.0.0:5001 (Dockerfile) OU app.run(port=5001) en dev.

Gotenberg — image gotenberg/gotenberg:8, ecoute :3000 en interne. Endpoint /forms/chromium/convert/html. Ne parle que basic-auth via --api-enable-basic-auth.

Reference check Bearer Flask (RESEARCH Code Examples) : lire Authorization, comparer a Bearer + token env, 401 sinon, /health exempte, conditionnel au token.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check Bearer server-side dans WeasyPrint Flask + tests Python</name>
  <read_first>
    - docker/weasyprint/server.py (etat actuel : routes /pdf et /health, imports Flask)
    - docker/weasyprint/Dockerfile (base python:3.12-slim, gunicorn, pip weasyprint/flask)
    - apps/web/src/lib/pdf-render.ts (authHeaders conditionnel — contrat client : header omis si pas de token)
  </read_first>
  <action>
    1. Modifier docker/weasyprint/server.py : ajouter `import os` (apres `import io`), lire le token apres `app = Flask(__name__)` via `DOC_ENGINE_TOKEN = os.environ.get("DOC_ENGINE_TOKEN")`, et ajouter un hook decore `@app.before_request` nomme `_enforce_bearer` qui :
       - retourne None si `request.path == "/health"` (probe liveness Railway non authentifiee) ;
       - si `DOC_ENGINE_TOKEN` est truthy : lit `request.headers.get("Authorization", "")` et si different de la chaine `Bearer ` concatenee au token, retourne `Response("Unauthorized", status=401)` ;
       - sinon retourne None (conditionnel au token = parite pdf-render.ts authHeaders, dev local sans token non casse).
       `Response` est deja importe. NE PAS toucher la route /pdf ni le rendu/footer.

    2. NE PAS ajouter pytest a l'image runtime Dockerfile (garder l'image legere). Les tests tournent dans un venv local (voir verify).

    3. Creer docker/weasyprint/test_auth.py — tests Flask test client (pytest) avec un helper `_load(monkeypatch, token)` qui set/del la var DOC_ENGINE_TOKEN via monkeypatch puis `importlib.reload(server)` et retourne `server.app.test_client()`. Cinq tests :
       - test_health_always_open : GET /health avec token defini renvoie 200.
       - test_pdf_rejects_missing_bearer : POST /pdf sans header avec token defini renvoie 401.
       - test_pdf_rejects_wrong_bearer : POST /pdf avec header "Bearer wrong" renvoie 401.
       - test_pdf_accepts_correct_bearer : POST /pdf avec "Bearer secret" (body HTML > 50 chars) renvoie 200.
       - test_no_token_dev_mode_open : sans token, POST /pdf (body HTML valide) renvoie 200 (parite dev).
       Le body HTML doit faire plus de 50 caracteres (la route rejette 400 sinon).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files/docker/weasyprint" && python3 -m venv /tmp/wp-venv 2>/dev/null; /tmp/wp-venv/bin/pip install -q weasyprint==60.2 pydyf==0.8.0 flask==3.0.3 pytest==8.3.3 2>&1 | tail -1; /tmp/wp-venv/bin/python -m pytest test_auth.py -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "before_request" docker/weasyprint/server.py retourne 1
    - grep -c "DOC_ENGINE_TOKEN" docker/weasyprint/server.py retourne au moins 2 (lecture + comparaison)
    - grep -c "status=401" docker/weasyprint/server.py retourne 1
    - grep -c "/health" docker/weasyprint/server.py retourne au moins 2 (route + exemption auth)
    - docker/weasyprint/test_auth.py existe avec 5 fonctions test_*
    - pytest test_auth.py exit 0, 5 tests passent (dont test_no_token_dev_mode_open et test_pdf_rejects_missing_bearer)
  </acceptance_criteria>
  <done>WeasyPrint refuse 401 sans bon Bearer, accepte avec, exempte /health, et reste ouvert en dev sans token. Tests Python verts.</done>
</task>

<task type="auto">
  <name>Task 2: Proxy Caddy Bearer devant Gotenberg (Gotenberg ne parle pas Bearer)</name>
  <read_first>
    - .planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md (Pitfall 4 : Gotenberg 8 basic-auth only ; Open Question 1 : proxy Bearer recommande)
    - docker-compose.yml (service gotenberg actuel, image gotenberg/gotenberg:8, port expose)
    - apps/web/src/lib/pdf-render.ts (le client envoie Bearer en multipart FormData sur Gotenberg — le proxy doit forwarder le body multipart intact)
  </read_first>
  <action>
    Creer un mini reverse-proxy Caddy qui valide le Bearer puis forward vers Gotenberg (service sibling Railway). Ce proxy sera le service PUBLIC ; Gotenberg reste prive (gotenberg.railway.internal:3000).

    1. Creer docker/gotenberg-proxy/Caddyfile :
       - Ecoute sur le port `{$PORT:8080}` (Railway injecte PORT ; Caddy le lit).
       - Route /health (ou GET /) : `respond "ok" 200` sans auth (probe liveness).
       - Pour toutes les autres routes : matcher les requetes dont le header `Authorization` est different de `Bearer {$DOC_ENGINE_TOKEN}` et repondre 401 ; sinon `reverse_proxy {$GOTENBERG_UPSTREAM:gotenberg.railway.internal:3000}`.
       - Utiliser la directive Caddy `@unauthorized` avec `not header Authorization "Bearer {$DOC_ENGINE_TOKEN}"` puis `respond @unauthorized 401`, PUIS `reverse_proxy` pour le reste. Caddy transmet le body multipart tel quel (streaming) — ne PAS bufferiser ni reecrire le Content-Type (le boundary FormData doit rester intact, parite Pitfall 3 Phase 17).
       - Desactiver l'auto-HTTPS interne de Caddy sur ce listener (Railway termine le TLS en amont) : bloc global `{ auto_https off }` et servir en HTTP sur PORT.

    2. Creer docker/gotenberg-proxy/Dockerfile :
       - FROM caddy:2-alpine
       - COPY Caddyfile /etc/caddy/Caddyfile
       - Pas de CMD custom (l'image caddy lance `caddy run --config /etc/caddy/Caddyfile` par defaut ; verifier via ENTRYPOINT de l'image de base, sinon ajouter `CMD ["caddy", "run", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]`).

    3. Valider la syntaxe du Caddyfile avec l'image Caddy (voir verify). Le proxy ne sera reellement branche (URL GOTENBERG_URL cote worker/Vercel = domaine public du proxy) qu'au deploiement (plan 20-04/20-05) ; ce plan LIVRE et VALIDE la config.

    Note : ce proxy Gotenberg est le mecanisme d'enforcement Bearer server-side pour Gotenberg. Cote WeasyPrint, le check Flask (Task 1) suffit (pas besoin de proxy). En Phase 21, GOTENBERG_URL pointera vers ce proxy public, WEASYPRINT_URL vers le service WeasyPrint public.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files/docker/gotenberg-proxy" && docker run --rm -v "$PWD/Caddyfile:/etc/caddy/Caddyfile:ro" -e DOC_ENGINE_TOKEN=test -e GOTENBERG_UPSTREAM=localhost:3000 caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile 2>&1 | tail -8 || echo "DOCKER_UNAVAILABLE — verifier la syntaxe manuellement au plan 20-04"</automated>
  </verify>
  <acceptance_criteria>
    - docker/gotenberg-proxy/Caddyfile existe
    - grep -c "reverse_proxy" docker/gotenberg-proxy/Caddyfile retourne au moins 1
    - grep -c "Authorization" docker/gotenberg-proxy/Caddyfile retourne au moins 1
    - grep -c "DOC_ENGINE_TOKEN" docker/gotenberg-proxy/Caddyfile retourne au moins 1
    - grep -c "401" docker/gotenberg-proxy/Caddyfile retourne au moins 1
    - grep -c "health\|respond" docker/gotenberg-proxy/Caddyfile retourne au moins 1 (probe sans auth)
    - docker/gotenberg-proxy/Dockerfile existe et grep -c "caddy" docker/gotenberg-proxy/Dockerfile retourne au moins 1
    - `caddy validate` exit 0 (ou DOCKER_UNAVAILABLE consigne si Docker absent, syntaxe a re-valider au 20-04)
  </acceptance_criteria>
  <done>Un proxy Caddy Bearer valide le token avant de forwarder vers Gotenberg (body multipart intact), expose /health sans auth, et sa config passe caddy validate. Gotenberg peut ainsi etre appele en Bearer par Vercel/worker sans modifier le client.</done>
</task>

</tasks>

<verification>
- pytest docker/weasyprint/test_auth.py vert (5 tests)
- caddy validate sur docker/gotenberg-proxy/Caddyfile exit 0 (si Docker dispo)
- Aucune regression du client pdf-render.ts (non touche) ni du footer HTML in-body WeasyPrint
- La preuve HTTPS reelle (401 sans token / 200 avec, apres deploiement Railway) est au plan 20-05
</verification>

<success_criteria>
Les 2 doc-engines refusent server-side toute requete de rendu PDF sans Bearer DOC_ENGINE_TOKEN valide, tout en restant ouverts en dev local sans token. WeasyPrint via check Flask, Gotenberg via proxy Caddy. Contribue a WORK-01 (doc-engines exposables en HTTPS public authentifie).
</success_criteria>

<output>
After completion, create `.planning/phases/20-worker-3-h-te-doc-engines/20-03-SUMMARY.md`
</output>
