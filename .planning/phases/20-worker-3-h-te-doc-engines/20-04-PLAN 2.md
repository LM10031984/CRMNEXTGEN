---
phase: 20-worker-3-h-te-doc-engines
plan: 04
type: execute
wave: 2
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - docker/worker/Dockerfile
  - ecosystem.config.cjs
  - railway.json
  - apps/web/package.json
  - .env.example
  - .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md
files_removed:
  - apps/web/src/lib/closure/redis.ts
  - apps/web/src/lib/closure/queue.ts
  - apps/web/src/lib/veille/queue.ts
  - apps/web/src/lib/invoice-reminders/queue.ts
  - apps/web/scripts/closure-worker.ts
autonomous: true
requirements: [WORK-01, WORK-03]
must_haves:
  truths:
    - "Une image Docker monorepo pruee (turbo prune @qualiof/web) build, contient poppler-utils (pdftoppm) et lance 4 workers via pm2-runtime"
    - "Les 4 workers (closure Postgres SKIP LOCKED, veille croner, relances croner, OCR poll) demarrent chacun fail-loud si l'env manque"
    - "Les deps bullmq/ioredis et les fichiers de queue BullMQ morts sont retires (0 connexion Redis fantome au boot)"
    - "Le worker recalibre pour le cloud (concurrency ~3, poll interval documentes) via variables d'env, sans toucher le code metier"
    - "Un runbook 20-DEPLOY.md decrit les ~15 variables Railway, les 3 services (worker + gotenberg-proxy + weasyprint), la region europe-west4"
  artifacts:
    - path: "docker/worker/Dockerfile"
      provides: "Image worker monorepo prunee + poppler + pm2"
      contains: "poppler-utils"
    - path: "ecosystem.config.cjs"
      provides: "Config pm2 4 workers"
      contains: "pm2"
    - path: "railway.json"
      provides: "Config deploiement Railway (Dockerfile path, healthcheck, restart)"
      contains: "Dockerfile"
    - path: ".planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md"
      provides: "Runbook de provisioning Railway (variables, services, region)"
      contains: "europe-west4"
  key_links:
    - from: "docker/worker/Dockerfile CMD"
      to: "ecosystem.config.cjs"
      via: "pm2-runtime lance les 4 entry-points"
      pattern: "pm2-runtime"
    - from: "ecosystem.config.cjs"
      to: "scripts/closure-worker-postgres.ts + veille + reminders + ocr"
      via: "4 apps pm2 pointant les entry-points tsx"
      pattern: "closure-worker-postgres"
---

<objective>
Conteneuriser le 3e hote : une image Docker monorepo prunee (turbo prune) embarquant poppler-utils et pilotee par pm2-runtime pour lancer les 4 workers de fond (closure Postgres, veille croner, relances croner, OCR poll), retirer les deps/fichiers BullMQ morts, recalibrer le worker pour la latence cloud via env, et ecrire le runbook de provisioning Railway. C'est la brique WORK-01 (image + deploiement) et WORK-03 (recalibrage).

Purpose: Sans image Docker propre + supervision pm2, le worker ne peut pas tourner sur Railway. Le retrait BullMQ/ioredis elimine tout risque de connexion Redis fantome au boot (l'image n'a pas de Redis). Le runbook rend le deploiement executable par Laurent (non-technicien) au plan 20-05.
Output: Dockerfile worker + ecosystem pm2 + railway.json + retrait deps/fichiers BullMQ + .env.example a jour + 20-DEPLOY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-CONTEXT.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-01-SUMMARY.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-02-SUMMARY.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-03-SUMMARY.md

<interfaces>
Entry-points a lancer par pm2 (existants + crees plans 20-01/20-02) :
- apps/web/scripts/closure-worker-postgres.ts (existant, poll ClosureJob SKIP LOCKED ; env QUEUE_POLL_INTERVAL_MS defaut 3000, QUEUE_CONCURRENCY defaut 3)
- apps/web/scripts/veille-worker.ts (croner lundi 8h — plan 20-01)
- apps/web/scripts/invoice-reminder-worker.ts (croner quotidien 8h — plan 20-01)
- apps/web/scripts/preinscription-ocr-worker.ts (poll SUBMITTED — plan 20-02 ; env OCR_POLL_INTERVAL_MS defaut 5000, OCR_CONCURRENCY defaut 2)
Chaque entry-point importe deja `@qualiof/shared/env` en tete = fail-loud au boot.

Dockerfile turbo prune de reference (RESEARCH Pattern 1, verifie 2026-07-05) :
- base node:20-slim (Debian/glibc — evite les soucis musl pour poppler et Prisma)
- stage pruner : turbo@2.3.0 + `turbo prune @qualiof/web --docker` -> out/json (deps) + out/full (src)
- stage installer : COPY out/json, `pnpm install --frozen-lockfile`, COPY out/full, `pnpm --filter @qualiof/web exec prisma generate`
- stage runner : apt install poppler-utils, `pnpm add -g pm2`, COPY installer, CMD pm2-runtime ecosystem.config.cjs
- pnpm 10.33.2 via corepack ; PAS de next build (tsx execute les .ts des workers directement)

Fichiers/deps BullMQ morts a retirer (RESEARCH Runtime State Inventory) :
- deps apps/web/package.json : bullmq (^5.76.4), ioredis (^5.10.1)
- fichiers : apps/web/src/lib/closure/redis.ts, closure/queue.ts (BullMQ), veille/queue.ts, invoice-reminders/queue.ts
- entry-point BullMQ mort : apps/web/scripts/closure-worker.ts (le closure prod = closure-worker-postgres.ts)
- ATTENTION : grep les usages avant suppression (tests, imports residuels). Le worker closure BullMQ (closure/worker.ts) exporte processClosureJobPayload PARTAGE avec le driver Postgres — NE PAS supprimer closure/worker.ts, seulement redis.ts/queue.ts BullMQ.

Region cible (17-REGIONS.md) : Railway europe-west4 (Amsterdam, EU). Les donnees restent sur Supabase Paris.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrait des deps/fichiers BullMQ morts (0 Redis fantome)</name>
  <read_first>
    - apps/web/package.json (deps bullmq/ioredis + scripts)
    - apps/web/src/lib/closure/redis.ts, apps/web/src/lib/closure/queue.ts (BullMQ closure)
    - apps/web/src/lib/veille/queue.ts, apps/web/src/lib/invoice-reminders/queue.ts (BullMQ veille/factures)
    - apps/web/scripts/closure-worker.ts (entry-point BullMQ mort)
    - .planning/phases/20-worker-3-h-te-doc-engines/20-01-SUMMARY.md (confirme que worker.ts veille/factures ne referencent plus bullmq)
  </read_first>
  <action>
    1. AVANT toute suppression, grep les usages residuels pour chaque fichier a supprimer :
       - `grep -rn "closure/redis\|getWorkerRedis" apps/web/src apps/web/scripts` — doit ne rester que dans les fichiers a supprimer eux-memes.
       - `grep -rn "closure/queue'\|from '.*closure/queue\"" apps/web/src apps/web/scripts` (le BullMQ queue.ts, PAS queue-postgres.ts).
       - `grep -rn "veille/queue\|invoice-reminders/queue" apps/web/src apps/web/scripts` — apres plan 20-01, worker.ts ne les importe plus ; verifier les tests.
       - Si un fichier de TEST importe encore un de ces modules, mettre a jour/supprimer le test correspondant (ex : un test qui mockait le Worker BullMQ veille est caduc apres 20-01).
    2. Supprimer les fichiers : apps/web/src/lib/closure/redis.ts, apps/web/src/lib/closure/queue.ts, apps/web/src/lib/veille/queue.ts, apps/web/src/lib/invoice-reminders/queue.ts, apps/web/scripts/closure-worker.ts.
       NE PAS supprimer : closure/queue-postgres.ts (driver prod), closure/worker.ts (processClosureJobPayload partage), veille/core.ts, veille/worker.ts, invoice-reminders/worker.ts (handlers conserves plan 20-01).
    3. Retirer les deps `bullmq` et `ioredis` de apps/web/package.json (bloc dependencies) via `pnpm --filter @qualiof/web remove bullmq ioredis`.
    4. Retirer les scripts pnpm devenus caducs de apps/web/package.json :
       - `worker:closure` (BullMQ) — remplacer sa cible ou le supprimer ; garder `worker:closure:pg` comme worker closure de reference.
       - Ajuster `dev:full` et `dev:full:reminders` : ils lancent `pnpm worker:closure` (BullMQ). Les repointer sur `worker:closure:pg` (le driver Postgres est desormais le seul worker closure).
    5. Retirer le service `redis` de docker-compose.yml (bloc `redis:` + volume `redis_data`) — plus utilise en local non plus. Retirer la cle REDIS_URL de .env.example (ligne ~32) et de turbo.json globalEnv si presente.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | tail -5 && pnpm --filter @qualiof/web test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Les fichiers closure/redis.ts, closure/queue.ts, veille/queue.ts, invoice-reminders/queue.ts, scripts/closure-worker.ts n'existent plus (ls retourne "No such file")
    - closure/queue-postgres.ts et closure/worker.ts existent TOUJOURS
    - grep -c '"bullmq"' apps/web/package.json retourne 0
    - grep -c '"ioredis"' apps/web/package.json retourne 0
    - grep -rc "from 'bullmq'\|getWorkerRedis" apps/web/src apps/web/scripts retourne 0 partout
    - grep -c "REDIS_URL" .env.example retourne 0
    - grep -c "worker:closure\"" apps/web/package.json (BullMQ) : le script BullMQ n'existe plus ou pointe closure-worker-postgres
    - tsc --noEmit exit 0
    - suite Vitest apps/web verte (hormis l'echec pre-existant shared-template.test.ts MIME jpeg/jpg documente baseline)
  </acceptance_criteria>
  <done>Tout le code BullMQ/Redis mort est retire, la suite passe, aucune connexion Redis ne peut plus etre tentee au boot. dev:full pointe le worker closure Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile worker (turbo prune + poppler + pm2) + ecosystem pm2 + recalibrage env</name>
  <read_first>
    - .planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md (Pattern 1 Dockerfile turbo prune, Pattern 3 recalibrage, Pitfall 5 Prisma node:slim glibc)
    - apps/web/scripts/closure-worker-postgres.ts (env QUEUE_CONCURRENCY/QUEUE_POLL_INTERVAL_MS)
    - apps/web/scripts/preinscription-ocr-worker.ts (env OCR_* — plan 20-02)
    - turbo.json (tasks, globalEnv — pour comprendre le graphe prune)
    - package.json racine (packageManager pnpm 10.33.2, turbo 2.3.0)
  </read_first>
  <action>
    1. Creer docker/worker/Dockerfile multi-stage (adapte de RESEARCH Pattern 1) :
       - `FROM node:20-slim AS base` ; `RUN corepack enable && corepack prepare pnpm@10.33.2 --activate`
       - stage pruner : `RUN pnpm add -g turbo@2.3.0` ; `COPY . .` ; `RUN turbo prune @qualiof/web --docker`
       - stage installer : `COPY --from=pruner /app/out/json/ .` ; `RUN pnpm install --frozen-lockfile` ; `COPY --from=pruner /app/out/full/ .` ; `RUN pnpm --filter @qualiof/web exec prisma generate`
       - stage runner : `RUN apt-get update && apt-get install -y --no-install-recommends poppler-utils && rm -rf /var/lib/apt/lists/*` ; `RUN pnpm add -g pm2` ; `COPY --from=installer /app .` ; `COPY ecosystem.config.cjs ./` ; `CMD ["pm2-runtime", "ecosystem.config.cjs"]`
       - WORKDIR /app dans chaque stage. Pas de `next build` (tsx execute les .ts).
       - NB Prisma : `prisma generate` doit produire le client pour la libc glibc de node:slim (default binaryTarget OK). Verifier au smoke (plan 20-05) que pdftoppm -v repond.

    2. Creer ecosystem.config.cjs a la RACINE du repo (copie dans l'image) :
    ```
    module.exports = {
      apps: [
        { name: 'closure',   script: 'apps/web/scripts/closure-worker-postgres.ts', interpreter: 'tsx', autorestart: true, env: { QUEUE_CONCURRENCY: '3', QUEUE_POLL_INTERVAL_MS: '3000' } },
        { name: 'veille',    script: 'apps/web/scripts/veille-worker.ts',           interpreter: 'tsx', autorestart: true },
        { name: 'reminders', script: 'apps/web/scripts/invoice-reminder-worker.ts', interpreter: 'tsx', autorestart: true },
        { name: 'ocr',       script: 'apps/web/scripts/preinscription-ocr-worker.ts', interpreter: 'tsx', autorestart: true, env: { OCR_CONCURRENCY: '2', OCR_POLL_INTERVAL_MS: '5000' } },
      ],
    };
    ```
       - RECALIBRAGE WORK-03/D-09 : concurrency=3 (garder ~3 sous pooler connection_limit=1, Pitfall 1 — ne PAS monter agressivement). Le "timeout 600s->120s" de D-09 est deja fait cote LLM (OpenRouter timeoutMs 240000, la branche 600000 Ollama ne s'execute pas en prod, RESEARCH Pattern 3) : le vrai levier est poll/concurrency, documente ici. Ces valeurs sont surchargeables par les variables de service Railway.
       - `interpreter: 'tsx'` suppose tsx resolvable ; verifier que tsx est en devDependencies (il l'est, ^4.21.0) et present apres `pnpm install`. Si pm2 ne resout pas `tsx` comme interpreter, fallback : `script: 'tsx'`, `args: 'apps/web/scripts/...'` — documenter le fallback en commentaire.

    3. Creer railway.json a la racine :
    ```
    { "$schema": "https://railway.com/railway.schema.json",
      "build": { "builder": "DOCKERFILE", "dockerfilePath": "docker/worker/Dockerfile" },
      "deploy": { "restartPolicyType": "ON_FAILURE", "restartPolicyMaxRetries": 10 } }
    ```
       (Ce railway.json cible le service worker. Les 2 autres services — gotenberg-proxy et weasyprint — auront leur propre dockerfilePath configure au dashboard/plan 20-05 : docker/gotenberg-proxy/Dockerfile et docker/weasyprint/Dockerfile.)
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && node -e "require('./ecosystem.config.cjs').apps.forEach(a=>console.log(a.name,a.script))" && node -e "JSON.parse(require('fs').readFileSync('railway.json','utf8')); console.log('railway.json valid JSON')" && test -f docker/worker/Dockerfile && echo "Dockerfile present" && grep -c poppler-utils docker/worker/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - docker/worker/Dockerfile existe
    - grep -c "turbo prune @qualiof/web --docker" docker/worker/Dockerfile retourne 1
    - grep -c "poppler-utils" docker/worker/Dockerfile retourne 1
    - grep -c "prisma generate" docker/worker/Dockerfile retourne 1
    - grep -c "pm2-runtime" docker/worker/Dockerfile retourne 1
    - grep -c "node:20-slim" docker/worker/Dockerfile retourne au moins 1 (glibc, Pitfall 5)
    - ecosystem.config.cjs charge sans erreur et liste 4 apps (closure, veille, reminders, ocr)
    - grep -c "closure-worker-postgres" ecosystem.config.cjs retourne 1
    - grep -c "preinscription-ocr-worker" ecosystem.config.cjs retourne 1
    - railway.json est du JSON valide et grep -c "docker/worker/Dockerfile" railway.json retourne 1
  </acceptance_criteria>
  <done>Une image Docker monorepo prunee avec poppler + pm2 lancant 4 workers est definie, le recalibrage cloud est encode en env pm2, et railway.json cible le Dockerfile worker. Le build reel est valide au plan 20-05.</done>
</task>

<task type="auto">
  <name>Task 3: Runbook de provisioning Railway (20-DEPLOY.md) + .env.example a jour</name>
  <read_first>
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md (region europe-west4, checklist anti-defaut-US)
    - .planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md (Runtime State Inventory : ~15 variables env worker ; Pitfall 2 SMTP Pro-only ; private DNS *.railway.internal)
    - packages/shared/src/env.ts (les cles requises au boot fail-loud : DATABASE_URL, DIRECT_URL, STORAGE_PROVIDER, SUPABASE_*, WEASYPRINT_URL, DOC_ENGINE_TOKEN, AUTH_SECRET min 32, OPENROUTER_API_KEY, SMTP_*, GOTENBERG_URL)
    - .env.example (etat actuel des cles documentees)
  </read_first>
  <action>
    Creer .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md — runbook orientAe NON-technicien (Laurent pilote au dashboard, D-01) :

    1. Section "Compte + region" : creer le projet Railway plan Pro (20 $/mo, requis pour egress SMTP OVH :465 — Pitfall 2), region europe-west4. Rappeler la checklist anti-defaut-US de 17-REGIONS (verifier la region EU explicitement a la creation, jamais le defaut US).

    2. Section "3 services du projet" (tableau) :
       | Service | Source | Dockerfile | Port | Public ? |
       | worker | ce repo | docker/worker/Dockerfile | (aucun, workers) | non |
       | gotenberg-proxy | ce repo | docker/gotenberg-proxy/Dockerfile | PORT (8080) | OUI (domaine public, Bearer) |
       | weasyprint | ce repo | docker/weasyprint/Dockerfile | 5001 | OUI (domaine public, Bearer) |
       Gotenberg lui-meme = 4e service prive (image gotenberg/gotenberg:8, pas de Dockerfile repo, ecoute :3000, joint en gotenberg.railway.internal par le proxy).

    3. Section "Variables de service worker" (tableau nom | valeur/source | secret ?) — lister les ~15 cles avec leur origine (reprendre les valeurs cloud de Phase 17/18/19) :
       - DATABASE_URL (pooler :6543 ?pgbouncer=true&connection_limit=1), DIRECT_URL (:5432) — Supabase (Phase 19)
       - STORAGE_PROVIDER=supabase, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY — Supabase (Phase 18)
       - OPENROUTER_API_KEY — OpenRouter (Phase 16)
       - DOC_ENGINE_TOKEN — secret partage worker/proxy/weasyprint (genere, meme valeur partout)
       - AUTH_SECRET (min 32 chars, requis par le boot fail-loud meme si le worker ne l'utilise pas)
       - SMTP_HOST=ssl0.ovh.net, SMTP_PORT=465, SMTP_SECURE=true, SMTP_USER, SMTP_PASS, SMTP_FROM — OVH (relances factures)
       - GOTENBERG_URL, WEASYPRINT_URL — pointer les URL Railway (interne gotenberg-proxy.railway.internal / weasyprint.railway.internal pour le worker ; public pour Vercel en Phase 21)
       - Recalibrage optionnel : QUEUE_CONCURRENCY, QUEUE_POLL_INTERVAL_MS, OCR_CONCURRENCY, OCR_POLL_INTERVAL_MS (defauts dans ecosystem, surchargeables)
       Marquer secret=OUI pour SUPABASE_SERVICE_ROLE_KEY, OPENROUTER_API_KEY, DOC_ENGINE_TOKEN, AUTH_SECRET, SMTP_PASS (variables de service chiffrees Railway — jamais en clair, CLAUDE.md).

    4. Section "Variables des 2 doc-engines" : gotenberg-proxy a besoin de DOC_ENGINE_TOKEN + GOTENBERG_UPSTREAM=gotenberg.railway.internal:3000 ; weasyprint a besoin de DOC_ENGINE_TOKEN.

    5. Section "Preuves a produire" (renvoie au plan 20-05) : pack closure 100 % cloud Mac eteint, OCR PDF scanne, egress SMTP :465, stabilite 24 h + cout projete sous budget D-07 (~20-25 €).

    6. Mettre a jour .env.example : documenter les nouvelles cles worker si absentes (OCR_CONCURRENCY, OCR_POLL_INTERVAL_MS, QUEUE_*), retirer la ligne REDIS_URL (deja fait Task 1, verifier), et ajuster les commentaires GOTENBERG_URL/WEASYPRINT_URL pour mentionner les URL Railway en prod.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md && grep -c "europe-west4" .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md && grep -c "DOC_ENGINE_TOKEN" .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md && grep -c "Pro" .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md existe
    - grep -c "europe-west4" 20-DEPLOY.md retourne au moins 1
    - grep -c "gotenberg-proxy\|weasyprint\|worker" 20-DEPLOY.md couvre les 3 services
    - grep -c "DATABASE_URL\|DIRECT_URL\|SUPABASE\|OPENROUTER\|DOC_ENGINE_TOKEN\|AUTH_SECRET\|SMTP" 20-DEPLOY.md retourne au moins 8 (les ~15 variables listees)
    - grep -c "465\|SMTP_SECURE" 20-DEPLOY.md retourne au moins 1 (egress OVH Pro)
    - grep -c "24 h\|24h\|stabilite\|budget" 20-DEPLOY.md retourne au moins 1 (renvoi preuve 20-05)
    - grep -c "REDIS_URL" .env.example retourne 0
    - grep -c "OCR_CONCURRENCY\|OCR_POLL_INTERVAL_MS" .env.example retourne au moins 1
  </acceptance_criteria>
  <done>Un runbook lisible par un non-technicien decrit le provisioning Railway complet (Pro, europe-west4, 3+1 services, ~15 variables dont secrets chiffres, private DNS), et .env.example est aligne. Le deploiement reel est execute au plan 20-05.</done>
</task>

</tasks>

<verification>
- tsc --noEmit exit 0, suite Vitest apps/web verte (hors baseline shared-template)
- ecosystem.config.cjs charge et liste 4 workers ; railway.json JSON valide
- 20-DEPLOY.md couvre region + 3 services + ~15 variables + egress SMTP + renvoi preuves 24h
- Aucun bullmq/ioredis/Redis residuel (grep = 0)
- Le BUILD Docker reel + pdftoppm -v dans le conteneur = plan 20-05 (necessite Docker/deploiement)
</verification>

<success_criteria>
L'image Docker worker (turbo prune + poppler + pm2 x4) est definie, le code BullMQ/Redis mort est retire, le worker est recalibre cloud via env, et le runbook Railway est pret. WORK-01 (image, non deployee) et WORK-03 (recalibrage code) satisfaits ; le deploiement + preuve 24h sont au plan 20-05.
</success_criteria>

<output>
After completion, create `.planning/phases/20-worker-3-h-te-doc-engines/20-04-SUMMARY.md`
</output>
