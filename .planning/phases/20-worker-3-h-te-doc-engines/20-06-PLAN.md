---
phase: 20-worker-3-h-te-doc-engines
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/server/actions/preinscription-public.ts
  - apps/web/src/server/actions/__tests__/preinscription-public.test.ts
  - apps/web/src/server/actions/storage-upload.ts
  - apps/web/src/components/preinscriptions/retry-extraction-button.tsx
  - apps/web/src/lib/pdf-extract.ts
  - apps/web/src/lib/preinscription-extractor.ts
  - apps/web/src/lib/__tests__/pdf-extract.test.ts
  - apps/web/src/lib/__tests__/preinscription-extractor.test.ts
autonomous: true
gap_closure: true
requirements: [WORK-04]

must_haves:
  truths:
    - "Le bouton admin « Relancer l'extraction » repasse la pré-inscription en SUBMITTED (extractedData purgé) et c'est le worker Railway qui l'extrait — plus AUCUNE exécution OCR inline sur Vercel via retriggerExtraction"
    - "Le wizard admin « Créer un apprenant » face à un PDF scanné affiche UN message clair demandant une photo JPEG/PNG — jamais de hint « brew install poppler », jamais de tentative pdftoppm sur Vercel"
    - "submitPreEnrollmentForm (fire-and-forget legacy, 0 call-site UI) n'existe plus dans le code"
    - "Le chemin worker Railway (formulaire public → SUBMITTED → poll SKIP LOCKED → EXTRACTED) est INCHANGÉ : la rasterisation pdftoppm reste active par défaut (non-régression smoke P6)"
    - "Contrat D-06 respecté : jamais de row bloquée EXTRACTING en silence (retrigger débloque une EXTRACTING orpheline), jamais d'EXTRACTED produit par un chemin sans capacité OCR"
  artifacts:
    - path: "apps/web/src/server/actions/preinscription-public.ts"
      provides: "retriggerExtraction délégué au worker (re-flag SUBMITTED + purge), legacy supprimé"
      contains: "status: 'SUBMITTED'"
    - path: "apps/web/src/server/actions/__tests__/preinscription-public.test.ts"
      provides: "Tests TDD délégation + machine à statuts + scope tenant"
      min_lines: 60
    - path: "apps/web/src/lib/pdf-extract.ts"
      provides: "Option rasterizeScannedPdf (défaut true) + flag scannedPdf sur ExtractedDoc"
      contains: "rasterizeScannedPdf"
    - path: "apps/web/src/lib/preinscription-extractor.ts"
      provides: "extractDocsFromBuffers en mode Vercel-safe (rasterizeScannedPdf: false, message clair)"
      contains: "rasterizeScannedPdf: false"
  key_links:
    - from: "apps/web/src/components/preinscriptions/retry-extraction-button.tsx"
      to: "retriggerExtraction"
      via: "server action (import inchangé)"
      pattern: "retriggerExtraction"
    - from: "apps/web/src/server/actions/preinscription-public.ts"
      to: "worker Railway preinscription-ocr-worker"
      via: "re-flag status SUBMITTED consommé par processNextPreEnrollmentOcr (FOR UPDATE SKIP LOCKED) — AUCUN import de l'extracteur côté server action"
      pattern: "status: 'SUBMITTED'"
    - from: "apps/web/src/lib/preinscription-extractor.ts"
      to: "apps/web/src/lib/pdf-extract.ts"
      via: "extractTextFromFile(buffer, contentType, { rasterizeScannedPdf: false })"
      pattern: "rasterizeScannedPdf"
---

<objective>
Fermer le gap UAT Phase 20 test 2 (severity: major) : l'OCR admin tourne encore inline sur Vercel — runtime SANS poppler — sur 2 chemins résiduels que le plan 20-02 n'a pas relocalisés : ① le wizard « Créer un apprenant » (`extractApprenantDocs` → `extractDocsFromBuffers`, émetteur prouvé du trio de messages vu par Laurent, dont le hint « brew install poppler » absurde en prod), ② le bouton admin « Relancer l'extraction » (`retriggerExtraction` exécute `extractPreEnrollmentDocuments` inline — risque de passer une row en EXTRACTED avec cni:null et de voler le job au worker Railway). Plus un fire-and-forget legacy mort (`submitPreEnrollmentForm`, 0 call-site).

Fix (fix_recommendation du gap, root cause prouvée `.planning/debug/uat20-ocr-pdftoppm-cni.md`) :
1. `retriggerExtraction` → délégation au worker Railway : re-flag SUBMITTED + purge extractedData (le worker poll les SUBMITTED toutes les ~5 s).
2. Wizard Vercel → refus clair des PDF scannés : message unique demandant un JPEG/PNG, SANS hint brew, SANS tentative pdftoppm. Les images JPEG/PNG continuent de passer par l'OCR vision (qui marche sur Vercel — c'est le binaire poppler qui manque, pas le LLM).
3. Suppression du legacy `submitPreEnrollmentForm`.

Purpose : préserver le pilier #4 (pré-inscriptions IA) sans dégradation silencieuse — requirement WORK-04, contrat D-06.
Output : 2 chemins inline soldés + tests TDD délégation/statuts + non-régression complète du chemin worker.

⚠ Ce plan ne dépend de RIEN de la Phase 22 (exécutable avant ou après la bascule prod). Il ne touche NI `preinscription-ocr-queue.ts`, NI `scripts/preinscription-ocr-worker.ts`, NI `confirmPreEnrollmentUpload` (chemin public conforme 20-02, prouvé smoke P6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-worker-3-h-te-doc-engines/20-UAT.md
@.planning/debug/uat20-ocr-pdftoppm-cni.md
@apps/web/src/server/actions/preinscription-public.ts
@apps/web/src/server/actions/extract-apprenant-docs.ts
@apps/web/src/lib/preinscription-extractor.ts
@apps/web/src/lib/pdf-extract.ts
@apps/web/src/lib/preinscription-ocr-queue.ts
@apps/web/src/components/preinscriptions/retry-extraction-button.tsx
@apps/web/src/server/actions/__tests__/storage-upload.test.ts

<interfaces>
<!-- Contrats extraits du codebase — l'exécuteur les utilise directement, pas d'exploration. -->

Statuts (packages/db/prisma/schema.prisma:1280) :
```prisma
enum PreEnrollmentStatus { PENDING_FORM SUBMITTED EXTRACTING EXTRACTED VALIDATED CONVERTED EXPIRED REJECTED }
```
`PreEnrollment.extractedData` est un `Json?` → purge via `Prisma.DbNull` (PAS `null` TS direct sur un champ Json nullable — importer `Prisma` de `@qualiof/db`).

Server action actuelle à déléguer (apps/web/src/server/actions/preinscription-public.ts:123) :
```typescript
export async function retriggerExtraction(preEnrollmentId: string): Promise<{ ok: boolean; error?: string }> {
  const { user } = await validateRequest();               // scope tenant DÉJÀ présent
  if (!user) return { ok: false, error: 'Non authentifié' };
  const pe = await prisma.preEnrollment.findFirst({ where: { id: preEnrollmentId, tenantId: user.tenantId } });
  if (!pe) return { ok: false, error: 'Pré-inscription introuvable' };
  Promise.resolve().then(() => extractPreEnrollmentDocuments(preEnrollmentId).catch(...)); // ← À SUPPRIMER (inline Vercel)
  ...
}
```
Le legacy à supprimer : `submitPreEnrollmentForm` (lignes 39-117, 0 call-site hors commentaires — vérifié par grep) + son `interface SubmitInput`, `safeKey`, `MAX_FILE_SIZE_MB` et les imports `uploadFile`/`PREENROLLMENT_BUCKET`/`extractPreEnrollmentDocuments` devenus inutilisés.

File d'attente worker (apps/web/src/lib/preinscription-ocr-queue.ts — NE PAS MODIFIER) :
```typescript
// Claim atomique : SUBMITTED → EXTRACTING via FOR UPDATE SKIP LOCKED, puis
// extractPreEnrollmentDocuments(id) → EXTRACTED ou SUBMITTED + aiErrorMsg (D-06).
export async function processNextPreEnrollmentOcr(maxJobs = 3): Promise<{ processed: number; succeeded: number; failed: number }>;
```
→ La délégation = simplement remettre la row en SUBMITTED : le worker Railway (pm2 `ocr`, poll OCR_POLL_INTERVAL_MS=5000) la reprend seul.

Extraction texte (apps/web/src/lib/pdf-extract.ts) :
```typescript
export interface ExtractedDoc { text: string; pages: number; warnings: string[] }
export async function extractTextFromFile(buffer: Buffer, contentType: string): Promise<ExtractedDoc>;
// Chemin PDF : unpdf → si couche texte < 30 chars → rasterizePdfPagesToPng (pdftoppm CLI,
// fonction INTERNE non exportée, dynamic import node:child_process) → OCR vision par page.
// C'est CE fallback qui produit ENOENT + hint brew sur Vercel (lignes 146-155).
```

Extracteur wizard (apps/web/src/lib/preinscription-extractor.ts:270) :
```typescript
export type DocFile = { kind: 'CNI' | 'RIB' | 'CFP'; buffer: Buffer; contentType: string };
export interface ExtractedDocs { cni: CniExtraction | null; rib: RibExtraction | null; cfp: CfpExtraction | null; warnings: string[]; durationMs: number }
export async function extractDocsFromBuffers(files: DocFile[]): Promise<ExtractedDocs>;
// Appelé UNIQUEMENT par extract-apprenant-docs.ts (server action Vercel) — gating sûr.
// Ligne 279 : le message « Texte extrait trop court (< 20 char), skip » (3e du trio UAT).
```

Pattern de test à répliquer (apps/web/src/server/actions/__tests__/storage-upload.test.ts) :
mocks hermétiques `vi.hoisted` + `vi.mock` de `@/lib/auth` (validateRequest), `@qualiof/db` (prisma.preEnrollment.findFirst/update — exposer aussi `Prisma: { DbNull: Symbol('DbNull') }` factice si besoin), `next/cache` (revalidatePath no-op). Vitest ne charge pas `.env` → aucun import réel de l'extracteur.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: retriggerExtraction délègue au worker Railway (re-flag SUBMITTED) + suppression du legacy submitPreEnrollmentForm</name>
  <files>apps/web/src/server/actions/preinscription-public.ts, apps/web/src/server/actions/__tests__/preinscription-public.test.ts, apps/web/src/components/preinscriptions/retry-extraction-button.tsx, apps/web/src/server/actions/storage-upload.ts (commentaires uniquement), apps/web/src/lib/preinscription-extractor.ts (commentaire d'en-tête uniquement)</files>
  <behavior>
    Tests TDD (RED d'abord — créer `preinscription-public.test.ts` sur le pattern storage-upload.test.ts, ~8 tests) :
    - Non authentifié → `{ ok: false, error: 'Non authentifié' }`, aucun appel prisma.update.
    - Tenant mismatch (findFirst → null) → `{ ok: false, error: 'Pré-inscription introuvable' }` (scope multi-tenant conservé).
    - Status `PENDING_FORM` → refus `{ ok: false }` avec message clair (le formulaire n'a pas encore été soumis, rien à extraire), aucun update.
    - Status `VALIDATED` / `CONVERTED` / `REJECTED` / `EXPIRED` → refus `{ ok: false }` « dossier déjà traité », aucun update (jamais de ré-extraction d'un dossier finalisé).
    - Status `EXTRACTED` → update appelé avec `{ status: 'SUBMITTED', extractedData: Prisma.DbNull, aiErrorMsg: null, aiExtractedAt: null }` → `{ ok: true }`.
    - Status `EXTRACTING` (row bloquée orpheline, worker crashé) → MÊME re-flag SUBMITTED → `{ ok: true }` (débloque — D-06 : jamais EXTRACTING bloqué silencieux).
    - Status `SUBMITTED` → re-flag idempotent `{ ok: true }` (purge extractedData/aiErrorMsg quand même — état propre pour le worker).
    - Dans TOUS les cas : `extractPreEnrollmentDocuments` n'est JAMAIS appelé (l'import doit avoir disparu du fichier ; le test le prouve en mockant `@/lib/preinscription-extractor` avec un spy à 0 appel — le mock ne cassera pas même une fois l'import supprimé si déclaré defensive).
  </behavior>
  <action>
    GREEN — réécrire `apps/web/src/server/actions/preinscription-public.ts` (fix_recommendation ① et ③ du gap) :

    1. `retriggerExtraction(preEnrollmentId)` : conserver `validateRequest` + `findFirst({ id, tenantId })` (convention multi-tenant). Ajouter la machine à statuts ci-dessus (refus PENDING_FORM et VALIDATED/CONVERTED/REJECTED/EXPIRED avec messages français façon `{ ok: false, error }`). Pour SUBMITTED/EXTRACTING/EXTRACTED : `prisma.preEnrollment.update({ where: { id: pe.id }, data: { status: 'SUBMITTED', extractedData: Prisma.DbNull, aiErrorMsg: null, aiExtractedAt: null } })` — le worker Railway (poll ~5 s) reprend la row. SUPPRIMER le `Promise.resolve().then(() => extractPreEnrollmentDocuments(...))` et l'import correspondant. Conserver les 2 `revalidatePath`.
    2. SUPPRIMER intégralement `submitPreEnrollmentForm` + `SubmitInput` + `safeKey` + `MAX_FILE_SIZE_MB` + les imports morts (`uploadFile`, `PREENROLLMENT_BUCKET`, `extractPreEnrollmentDocuments`). 0 call-site (grep vérifié au diagnostic) — le flux public passe par `confirmPreEnrollmentUpload` (storage-upload.ts, NE PAS TOUCHER). Mettre à jour les COMMENTAIRES périmés qui portent encore les chaînes littérales des symboles inline — commentaires uniquement, zéro logique (elles rendraient les gates grep impassables) : ① `storage-upload.ts` lignes 13 et 114-115 — retirer `submitPreEnrollmentForm` ET `extractPreEnrollmentDocuments` (le docblock de `confirmPreEnrollmentUpload` décrit désormais la délégation worker WORK-04, plus le « recâblage OCR fire-and-forget ») ; ② l'en-tête de `preinscription-extractor.ts` ligne 11 (« appelé en fire-and-forget depuis submitPreEnrollmentForm ») → référencer le worker Railway (`preinscription-ocr-queue`) et le wizard (`extract-apprenant-docs`).
    3. UI `retry-extraction-button.tsx` : adapter le toast et le libellé au nouveau contrat asynchrone — ex. `toast.info('Extraction relancée — reprise par le worker (~30-90 s)')` ; passer le polling de rafraîchissement à 6 itérations × 10 s (le worker claim sous ~5 s mais vision LLM = 10-60 s). `detail-actions.tsx` (handleReExtract) fonctionne tel quel (simple refresh) — ne pas le modifier.

    Note dev local : sans le worker OCR lancé (`tsx apps/web/scripts/preinscription-ocr-worker.ts`), une row re-flaggée reste SUBMITTED — comportement voulu (visible, jamais EXTRACTED vide), à mentionner en commentaire dans la server action.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run src/server/actions/__tests__/preinscription-public.test.ts && grep -c "extractPreEnrollmentDocuments" src/server/actions/preinscription-public.ts | grep -qx 0 && ! grep -rn "submitPreEnrollmentForm" src --include="*.ts" --include="*.tsx"</automated>
  </verify>
  <done>Les ~8 tests passent ; `preinscription-public.ts` ne contient plus ni `extractPreEnrollmentDocuments` ni `submitPreEnrollmentForm` (grep repo = 0) ; retrigger = pur re-flag SUBMITTED + purge, scoped tenant, avec machine à statuts explicite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refus clair des PDF scannés côté wizard Vercel (option rasterizeScannedPdf, message sans hint brew)</name>
  <files>apps/web/src/lib/pdf-extract.ts, apps/web/src/lib/preinscription-extractor.ts, apps/web/src/lib/__tests__/pdf-extract.test.ts, apps/web/src/lib/__tests__/preinscription-extractor.test.ts</files>
  <behavior>
    Tests TDD (RED d'abord — étendre les 2 fichiers de tests existants, mocks hermétiques déjà en place) :
    - `extractTextFromFile(pdfScanné, 'application/pdf', { rasterizeScannedPdf: false })` → retourne `{ text: '', scannedPdf: true }` avec UN warning clair qui contient « JPEG » et « PNG » et ne contient NI « brew » NI « pdftoppm » ; `node:child_process` (execFile) JAMAIS invoqué (mocker le module et asserter 0 appel).
    - `extractTextFromFile(pdfScanné, 'application/pdf')` SANS option (défaut) → comportement actuel intact : la rasterisation pdftoppm est tentée (non-régression du chemin worker Railway / smoke P6).
    - `extractTextFromFile(pdfNatif avec couche texte, 'application/pdf', { rasterizeScannedPdf: false })` → texte extrait normalement, pas de flag scannedPdf (l'option ne bride QUE le fallback scan).
    - `extractDocsFromBuffers([{ kind: 'CNI', buffer: pdfScanné, contentType: 'application/pdf' }])` → `cni: null`, warnings = LE message clair préfixé `[CNI]`, et PAS de « Texte extrait trop court (< 20 char), skip » redondant (fini le trio UAT) ; jamais de données inventées.
    - `extractDocsFromBuffers` avec image JPEG → chemin OCR vision inchangé (fonctionne sur Vercel — le LLM vision ne dépend pas de poppler).
  </behavior>
  <action>
    GREEN — fix_recommendation ② du gap :

    1. `pdf-extract.ts` : signature `extractTextFromFile(buffer, contentType, opts?: { rasterizeScannedPdf?: boolean })`, défaut `true` (les call-sites existants — extracteur worker via `extractPreEnrollmentDocuments` — sont inchangés sans modification). Ajouter `scannedPdf?: boolean` à `ExtractedDoc`. Dans la branche PDF < 30 chars : si `rasterizeScannedPdf === false`, retourner SANS tenter `rasterizePdfPagesToPng` : `{ text: '', pages: parsed.pages, scannedPdf: true, warnings: ['PDF scanné détecté — ce PDF est une image sans couche texte et ne peut pas être lu ici. Dépose une photo JPEG ou PNG du document : l\'extraction automatique pourra alors le traiter.'] }` (message UNIQUE remplaçant le hint « Texte extrait très court… » ; si `extractTextFromPdf` a signalé un échec de parsing, conserver ce warning-là en plus). Ne PAS toucher la branche rasterisation par défaut (hint brew conservé pour le dev local Mac — jamais atteint sur Railway qui a poppler, plus jamais atteint sur Vercel qui passe l'option false).
    2. `preinscription-extractor.ts` / `extractDocsFromBuffers` : appeler `extractTextFromFile(f.buffer, f.contentType, { rasterizeScannedPdf: false })` (ce chemin ne tourne QUE dans la server action Vercel `extract-apprenant-docs.ts` — gating sûr, vérifié au diagnostic). Quand `ext.scannedPdf === true`, `continue` SANS ajouter le message générique « < 20 char, skip » (le message clair suffit). `extractPreEnrollmentDocuments` (chemin worker) : AUCUN changement.
    3. Aucun changement dans `extract-apprenant-docs.ts` ni `create-person-button.tsx` : le wizard affiche déjà `warnings.slice(0,3)` — il montrera désormais le message unique et actionnable.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run src/lib/__tests__/pdf-extract.test.ts src/lib/__tests__/preinscription-extractor.test.ts && grep -c "rasterizeScannedPdf: false" src/lib/preinscription-extractor.ts | grep -qx 1</automated>
  </verify>
  <done>Tests verts ; un PDF scanné dans le wizard produit un message unique JPEG/PNG sans brew/pdftoppm et 0 spawn child_process ; le défaut (worker) rasterise toujours ; `extractPreEnrollmentDocuments` intact.</done>
</task>

<task type="auto">
  <name>Task 3: Gates de non-régression transverses + test de puissance (mutation)</name>
  <files>(aucun fichier modifié — vérification ; toute correction retombe sur les fichiers des Tasks 1-2)</files>
  <action>
    1. Suite complète + types : `pnpm --filter @qualiof/web test` (attendu ~1180+ verts, dont les nouveaux) et `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0. Vérifier qu'AUCUN test existant de `storage-upload.test.ts` / `preinscription-ocr-queue.test.ts` n'a régressé (chemin public + queue intouchés).
    2. Greps de clôture du gap (les 3 items `missing` du 20-UAT.md) :
       - `grep -rn "extractPreEnrollmentDocuments" apps/web/src/server/actions --include="*.ts" | grep -v __tests__` = 0 occurrence hors tests (plus aucun OCR inline déclenché par une server action — le spy du test Task 1 contient légitimement la chaîne) ;
       - `grep -rn "submitPreEnrollmentForm" apps/web/src apps/web/scripts --include="*.ts" --include="*.tsx"` = 0 (legacy supprimé) ;
       - `grep -rn "from.*preinscription-extractor" apps/web/src --include="*.ts" --include="*.tsx" | grep -v __tests__` = exactement 2 lignes d'import réelles (preinscription-ocr-queue.ts + extract-apprenant-docs.ts — l'import de preinscription-public.ts a disparu, aucun nouveau call-site) ;
       - `grep -c "rasterizeScannedPdf" apps/web/src/lib/pdf-extract.ts` ≥ 2 (option déclarée + consommée).
    3. Test de puissance (convention projet — mutation au gate) : casser temporairement UNE branche (ex. dans `retriggerExtraction`, remplacer `status: 'SUBMITTED'` par `status: 'EXTRACTED'`) → relancer `preinscription-public.test.ts` → DOIT être ROUGE → restaurer → re-vert. Prouve que les tests mordent réellement le contrat D-06.
    4. Consigner dans le SUMMARY : le flux public formulaire→worker (confirmPreEnrollmentUpload → SUBMITTED → poll) n'a subi AUCUNE modification — la preuve runtime Railway existante (smoke P6, TEST-OCR-P6D EXTRACTED) reste valide.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test && pnpm exec tsc --noEmit && ! { grep -rn "extractPreEnrollmentDocuments" src/server/actions --include="*.ts" | grep -v __tests__; } && ! grep -rn "submitPreEnrollmentForm" src --include="*.ts" --include="*.tsx"</automated>
  </verify>
  <done>Suite complète verte + tsc exit 0 + les 4 greps de clôture passent + mutation test prouvée rouge-puis-verte (consignée dans le SUMMARY avec la branche mutée).</done>
</task>

</tasks>

<verification>
- Les 3 items `missing` du gap 20-UAT.md sont soldés : ① wizard délesté du pdftoppm Vercel (refus clair JPEG/PNG), ② retriggerExtraction = délégation worker (plus de vol de job ni d'EXTRACTED cni:null), ③ legacy supprimé.
- Contrat D-06 : retrigger débloque une EXTRACTING orpheline (re-flag SUBMITTED visible) ; aucun chemin Vercel ne peut plus produire EXTRACTED ; le refus wizard est un message explicite, pas un échec silencieux.
- Chemin worker Railway strictement inchangé (queue, worker script, extracteur PE, rasterisation par défaut) — indépendant de la Phase 22.
</verification>

<success_criteria>
- `pnpm --filter @qualiof/web test` vert (nouveaux tests délégation/statuts + scan-refusal inclus) et `tsc --noEmit` exit 0.
- Greps : 0 `extractPreEnrollmentDocuments` dans `src/server/actions`, 0 `submitPreEnrollmentForm` dans le repo, `rasterizeScannedPdf: false` présent dans `extractDocsFromBuffers`.
- Rejouer le scénario UAT (PDF scanné dans le wizard « Créer un apprenant ») ne peut structurellement plus produire le trio de messages avec hint brew — un seul message actionnable JPEG/PNG.
- Un clic « Relancer l'extraction » repasse la row en SUBMITTED + purge, et sur un environnement avec worker actif elle finit EXTRACTED avec données réelles (vérité UAT test 2 re-testable via `/gsd:verify-work 20` post-exécution).
</success_criteria>

<output>
After completion, create `.planning/phases/20-worker-3-h-te-doc-engines/20-06-SUMMARY.md`
</output>
