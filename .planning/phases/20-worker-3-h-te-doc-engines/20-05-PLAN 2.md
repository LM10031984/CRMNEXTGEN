---
phase: 20-worker-3-h-te-doc-engines
plan: 05
type: execute
wave: 3
depends_on: ["20-04"]
files_modified:
  - .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md
autonomous: false
requirements: [WORK-01, WORK-02, WORK-03, WORK-04]
must_haves:
  truths:
    - "L'image worker build sur Railway et pdftoppm -v repond dans le conteneur deploye"
    - "Un pack closure complet se genere de bout en bout avec le worker Railway et le Mac eteint — 0 stub, contenu Qualiopi conforme"
    - "Un appel Vercel/local -> Gotenberg-proxy et -> WeasyPrint reussit avec Bearer et echoue 401 sans, apres redeploiement/restart (cold start = restart, D-08)"
    - "Un PDF scanne sans couche texte declenche pdftoppm sur le worker -> vision OpenRouter -> PreEnrollment EXTRACTED avec donnees reelles ; un echec OCR laisse un message explicite (jamais EXTRACTED vide)"
    - "Le worker est stable 24 h sans Redis et le cout mensuel projete Railway est sous budget D-07 (~20-25 €)"
    - "Un envoi email test OVH :465 depuis le worker aboutit (egress SMTP debloque sur Pro)"
  artifacts:
    - path: ".planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md"
      provides: "Preuves runtime datees des 4 requirements WORK-*, calquees sur 18-SMOKE/19-SMOKE"
      contains: "RESULTATS DE VALIDATION"
  key_links:
    - from: "worker Railway"
      to: "Supabase pooler :6543 + OpenRouter + Supabase Storage"
      via: "generation pack closure 100% cloud"
      pattern: "0 stub"
    - from: "PreEnrollment SUBMITTED"
      to: "EXTRACTED (donnees reelles)"
      via: "worker OCR poll + pdftoppm + vision"
      pattern: "EXTRACTED"
---

<objective>
Deployer les 3 services sur Railway (Pro, europe-west4) selon le runbook 20-DEPLOY.md, puis PROUVER les 4 requirements WORK-* par des smokes runtime contre l'infra reelle, Mac eteint : pack closure 100 % cloud, doc-engines Bearer, OCR PDF scanne, egress SMTP, stabilite 24 h + cout sous budget. Consigner les preuves datees dans 20-SMOKE.md.

Purpose: Les plans 20-01..20-04 ont livre le code et la config ; SEULE une execution reelle contre Railway prouve que le pilier #1 (pack closure) et le pilier #4 (pre-inscription OCR) tournent Mac eteint. Convention projet : destructif/infra reelle = etape separee, gatee Laurent.
Output: 20-SMOKE.md avec les 6 preuves ci-dessus + tableaux Etape|Commande|Attendu|Resultat|Date + sorties brutes.

Ce plan est NON autonome : il contient des checkpoints (deploiement au dashboard Railway par Laurent, gate SMTP/credentials, observation 24 h). L'orchestrateur peut resoudre certains checkpoints en autonome sur delegation explicite de Laurent (modalite Phase 18/19).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-CONTEXT.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md
@.planning/phases/20-worker-3-h-te-doc-engines/20-04-SUMMARY.md
@.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md

<interfaces>
Scripts de preuve existants :
- apps/web smoke:closure (`pnpm --filter @qualiof/web smoke:closure`) — genere un pack closure, verifie 0 stub. Existant.
- db:smoke:cloud (racine) — round-trip pooler (Phase 19), utilisable pour prouver que le worker joint la base.

Contrat anti-degradation OCR (D-06) — deja garanti dans le code (plans 18/20-02) :
- pdf-extract.ts:146 distingue "pdftoppm introuvable" (warning explicite) d'une autre erreur.
- preinscription-extractor.ts repasse SUBMITTED + aiErrorMsg en cas d'echec (jamais EXTRACTED vide).
- Le worker OCR (plan 20-02) ajoute un filet supplementaire (SUBMITTED + aiErrorMsg si l'extractor jette).

Format cible 20-SMOKE.md : calquer 19-SMOKE.md (section RESULTATS DE VALIDATION datee, tableaux 4 colonnes remplis, sorties CLI brutes, phase gate coche).

Budget D-07 : ~20-25 €/mois pour worker + Gotenberg + WeasyPrint. D-08 : tout toujours chaud (pas de scale-to-zero) — "cold start" = restart/redeploiement.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploiement Railway des 3 services (dashboard, gate credentials)</name>
  <read_first>
    - .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md (runbook complet : Pro, europe-west4, 4 services, ~15 variables)
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md (checklist anti-defaut-US region)
  </read_first>
  <what-built>Le runbook 20-DEPLOY.md, le Dockerfile worker (turbo prune + poppler + pm2 x4), le Dockerfile gotenberg-proxy (Caddy Bearer), le Dockerfile weasyprint (check Bearer Flask), railway.json.</what-built>
  <action>
    CHECKPOINT (human-action, gate credentials/plan/region = decisions Laurent). Suivre 20-DEPLOY.md sur le dashboard Railway (plan Pro, europe-west4) :
    1. Creer le projet + les 4 services : worker (docker/worker/Dockerfile), gotenberg-proxy (docker/gotenberg-proxy/Dockerfile, public), weasyprint (docker/weasyprint/Dockerfile, public), gotenberg (image gotenberg/gotenberg:8, prive).
    2. Renseigner les ~15 variables de service du worker + DOC_ENGINE_TOKEN/GOTENBERG_UPSTREAM du proxy + DOC_ENGINE_TOKEN du weasyprint (secrets chiffres Railway, jamais en clair — CLAUDE.md).
    3. Attendre le build worker ; verifier dans les logs pm2 = 4 process up (closure/veille/reminders/ocr), 0 crash au boot (fail-loud env OK = env complet).
    4. Exec sur le service worker : `pdftoppm -v` doit repondre une version poppler.
    Aucun edit de code ici — pilotage infra. L'orchestrateur peut piloter en autonome UNIQUEMENT sur delegation explicite de Laurent (modalite Phase 18/19).
  </action>
  <verify>Logs de boot pm2 (4 process up) + sortie `pdftoppm -v` collee dans le fil (ou delegation autonome actee).</verify>
  <done>Les 4 services Railway sont deployes, le worker boot 4 process pm2 sans crash env, pdftoppm repond dans le conteneur.</done>
  <resume-signal>Coller les logs de boot pm2 (4 process up) + la sortie `pdftoppm -v`, ou taper "gere tout toi stp" pour deleguer.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Executer les smokes runtime et consigner 20-SMOKE.md</name>
  <read_first>
    - .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md (format de reference : section RESULTATS DE VALIDATION datee, tableaux 4 colonnes, sorties brutes)
    - .planning/phases/20-worker-3-h-te-doc-engines/20-RESEARCH.md (Validation Architecture : Req->Test map WORK-01..04)
    - apps/web/scripts/preinscription-ocr-worker.ts + apps/web/src/lib/preinscription-extractor.ts (chemin OCR a prouver)
    - .planning/phases/20-worker-3-h-te-doc-engines/20-DEPLOY.md (section "Preuves a produire")
  </read_first>
  <action>
    Creer .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md (calque 19-SMOKE.md) et executer/consigner les preuves. Chaque preuve = une ligne de tableau Etape|Commande|Attendu|Resultat|Date + la sortie brute.

    WORK-01 (image + deploiement) :
    - P1 : logs boot pm2 = 4 process up (closure/veille/reminders/ocr), 0 crash env. + `pdftoppm -v` repond dans le conteneur. (Coller depuis Task 1.)

    WORK-01 (doc-engines Bearer, apres redeploiement/restart = "cold start" D-08) :
    - P2 : depuis une machine hors reseau Railway (local), `curl` sur le domaine PUBLIC du gotenberg-proxy SANS Authorization -> 401 ; AVEC `Authorization: Bearer <DOC_ENGINE_TOKEN>` -> passe (ou health 200 sans auth). Idem WeasyPrint public : POST /pdf sans Bearer -> 401, avec -> 200. Faire ces appels APRES un restart du service (prouve la reussite post-cold-start D-08).

    WORK-02 (0 Redis, cron) :
    - P3 : logs worker montrent "croner registered" pour veille (lundi 8h) et reminders (quotidien 8h), et AUCUNE trace de connexion Redis/ioredis au boot. `grep -rc "from 'bullmq'" apps/web/src apps/web/scripts` = 0 (rappel code). Optionnel : declencher manuellement le handler reminders via un exec worker.

    WORK-03 (pack closure 100 % cloud, Mac eteint) :
    - P4 : Mac de Laurent ETEINT (ou worker local arrete). Enqueuer un ClosureJob reel (session temoin) -> le worker Railway le claim (SKIP LOCKED), genere le pack -> verifier 0 stub, contenu Qualiopi conforme, PDF ecrits dans Supabase Storage. Consigner stub rate (0) + temps de generation. Round-trip DB via `db:smoke:cloud` si utile.
    - P5 : envoi email test OVH :465 depuis le worker (relance facture test ou sendMail direct) -> aboutit (egress SMTP Pro, Pitfall 2). Consigner le log de succes (0 PII — RGPD).

    WORK-04 (OCR PDF scanne, pilier #4) :
    - P6 : deposer via /p/[token] un PDF scanne SANS couche texte (CNI iPhone "Scanner") -> PreEnrollment SUBMITTED -> worker OCR (poll) rasterise via pdftoppm -> vision OpenRouter -> EXTRACTED avec extractedData reelles (nom/prenom/numero). Consigner statut + extrait anonymise. PUIS prouver l'anti-degradation D-06 : un echec OCR laisse SUBMITTED + aiErrorMsg visible (jamais EXTRACTED vide), ou citer le test unitaire pdf-extract branche "pdftoppm introuvable".

    WORK-02/03 (stabilite 24 h + cout, D-04/D-07) :
    - P7 : apres ~24 h, consigner : worker up (pm2 uptime, 0 restart en boucle) + facturation Railway observee 24 h projetee au mois vs D-07 (~20-25 €). Sous budget : OK. Au-dessus : documenter le levier (dimensionner au minimum, RESEARCH Open Q3).

    Remplir "Validation Sign-Off" et cocher le phase gate. 0 PII brut dans les sorties (RGPD, convention worker logs).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md && grep -c "RESULTATS DE VALIDATION\|RÉSULTATS DE VALIDATION" .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md && grep -c "stub" .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md && grep -c "EXTRACTED" .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md existe
    - Contient une section RESULTATS DE VALIDATION datee
    - P1 consigne : 4 process pm2 up + pdftoppm -v (sortie brute)
    - P2 consigne : 401 sans Bearer et succes avec Bearer sur gotenberg-proxy ET weasyprint publics
    - P4 consigne : pack closure genere Mac eteint, stub rate = 0, PDF dans Supabase Storage (sortie brute)
    - P5 consigne : envoi OVH :465 abouti (log succes, 0 PII)
    - P6 consigne : PDF scanne -> EXTRACTED avec donnees reelles (anonymisees) + preuve anti-degradation D-06
    - P7 consigne : uptime 24 h + cout projete vs budget D-07
    - Aucun numero de CNI/RIB brut consigne (RGPD)
    - Phase gate coche dans 20-SMOKE.md
  </acceptance_criteria>
  <done>Les 4 requirements WORK-* sont prouves runtime contre Railway reel, Mac eteint, et consignes dans 20-SMOKE.md avec sorties brutes datees.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validation finale du pack temoin cloud (gate Laurent)</name>
  <read_first>
    - .planning/phases/20-worker-3-h-te-doc-engines/20-SMOKE.md (les preuves P4 et P6 a valider visuellement)
  </read_first>
  <what-built>Un pack closure genere 100 % dans le cloud (Mac eteint) + une pre-inscription OCR d'un PDF scanne, tous deux prouves dans 20-SMOKE.md.</what-built>
  <action>
    CHECKPOINT (human-verify, gate Laurent). Verification visuelle finale du pack temoin cloud :
    1. Ouvrir le pack closure temoin genere en P4 : les 10 docs Qualiopi conformes (0 stub, footer HTML in-body present, contenu par stagiaire coherent).
    2. Ouvrir la fiche PreEnrollment de P6 : donnees extraites de la CNI scannee correctes (nom/prenom/numero) — pas d'auto-fill vide.
    3. Confirmer que le Mac etait bien eteint/worker local arrete pendant P4 et P6.
    4. Confirmer que le cout projete 24 h (P7) est acceptable (~20-25 €, D-07).
  </action>
  <verify>Approbation Laurent (pack temoin conforme + OCR correct + Mac eteint confirme + cout OK) ou description des ecarts.</verify>
  <done>Le pack closure temoin cloud et l'OCR du PDF scanne sont valides visuellement par Laurent ; le cout est sous budget. Phase 20 approuvee.</done>
  <resume-signal>Taper "approved" si le pack temoin et l'OCR sont conformes et le cout OK, sinon decrire les ecarts.</resume-signal>
</task>

</tasks>

<verification>
- 20-SMOKE.md contient les 6+1 preuves datees avec sorties brutes
- Pack closure genere Mac eteint, 0 stub (WORK-03)
- OCR PDF scanne -> EXTRACTED donnees reelles, anti-degradation D-06 prouvee (WORK-04)
- doc-engines Bearer : 401 sans / 200 avec (WORK-01)
- worker stable 24 h sans Redis + cout sous budget (WORK-02/03)
- 0 PII brut dans les logs/preuves (RGPD)
</verification>

<success_criteria>
Les 4 requirements WORK-01/02/03/04 sont satisfaits par preuve runtime contre Railway reel, Mac eteint. Le pilier #1 (pack closure 1-clic) et le pilier #4 (pre-inscription OCR) tournent 100 % cloud. Phase 20 = complete.
</success_criteria>

<output>
After completion, create `.planning/phases/20-worker-3-h-te-doc-engines/20-05-SUMMARY.md`
</output>
