---
phase: 18-supabase-storage-migration-objets-direct-to-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/storage.ts
  - apps/web/src/lib/__tests__/storage.test.ts
autonomous: true
requirements: [STOR-01]
must_haves:
  truths:
    - "L'adaptateur storage.ts expose createSignedUploadUrl (upload direct navigateur, Supabase uniquement)"
    - "L'adaptateur storage.ts expose objectExists sans télécharger l'objet (list metadata)"
    - "ensureBucket crée les buckets Supabase en public:false + fileSizeLimit 50 MiB (déjà en place, vérifié)"
    - "createSignedDownloadUrl renvoie une URL signée TTL 600s côté Supabase"
  artifacts:
    - path: "apps/web/src/lib/storage.ts"
      provides: "createSignedUploadUrl + objectExists ajoutés à l'interface unique storage"
      contains: "export async function createSignedUploadUrl"
    - path: "apps/web/src/lib/__tests__/storage.test.ts"
      provides: "Tests hermétiques mock @supabase/supabase-js pour les 4 fonctions clés"
      contains: "createSignedUploadUrl"
  key_links:
    - from: "apps/web/src/lib/storage.ts"
      to: "@supabase/supabase-js createSignedUploadUrl"
      via: "supabase().storage.from(bucket).createSignedUploadUrl"
      pattern: "createSignedUploadUrl\\("
    - from: "apps/web/src/lib/storage.ts"
      to: "@supabase/supabase-js list (metadata only)"
      via: "supabase().storage.from(bucket).list(prefix, { search })"
      pattern: "\\.list\\("
---

<objective>
Étendre l'adaptateur unique `apps/web/src/lib/storage.ts` avec les deux primitives Supabase manquantes que STOR-02 (migration) et STOR-03 (upload direct) consommeront : `createSignedUploadUrl` (upload direct navigateur→Supabase, contourne le cap 4,5 Mo Vercel) et `objectExists` (vérification 0 lien mort SANS télécharger l'objet). Ces deux fonctions sont les CONTRATS d'interface que les plans 02, 03 et 04 utiliseront — elles sont écrites en premier (interface-first).

Purpose: L'adaptateur est le chokepoint unique du stockage (~30 call sites passent par lui, RESEARCH.md « key insight »). N'ajouter QUE ces 2 fonctions ici préserve l'interface et évite tout appel `@supabase/supabase-js` hors de storage.ts.
Output: `storage.ts` étendu + tests hermétiques verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-CONTEXT.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md
@apps/web/src/lib/storage.ts

<interfaces>
<!-- Interface EXISTANTE dans storage.ts (à préserver, NE PAS réécrire) : -->
```typescript
export const PREENROLLMENT_BUCKET = 'preinscriptions';
export const DOCS_BUCKET = process.env.S3_BUCKET_DOCS ?? 'qualiof-docs';
// PROVIDER = sharedEnv.STORAGE_PROVIDER ('minio' | 'supabase')
// supabase(): SupabaseClient  — helper interne, service_role, persistSession:false
export async function ensureBucket(bucket: string): Promise<void>;       // crée public:false + 50 MiB côté Supabase (DÉJÀ FAIT)
export async function uploadFile(bucket, key, body, contentType?): Promise<{ key; bucket; size }>;
export async function downloadFile(bucket: string, key: string): Promise<Buffer>;
export async function createSignedDownloadUrl(bucket, key, expiresInSec=600): Promise<string>; // Supabase natif, throw sur MinIO
```

<!-- NOUVELLES fonctions à ajouter (contrats pour plans 02/03/04) : -->
```typescript
export async function createSignedUploadUrl(bucket: string, key: string):
  Promise<{ path: string; token: string; signedUrl: string }>;
export async function objectExists(bucket: string, key: string): Promise<boolean>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Écrire les tests hermétiques storage (Wave 0 — RED d'abord)</name>
  <files>apps/web/src/lib/__tests__/storage.test.ts</files>
  <read_first>
    - apps/web/src/lib/storage.ts (état actuel : PROVIDER, ensureBucket, createSignedDownloadUrl)
    - apps/web/src/lib/veille/__tests__/classify.test.ts (patron de mock hermétique du projet : vi.mock + vi.hoisted)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (section « Validation Architecture » + « NB pattern projet »)
  </read_first>
  <behavior>
    - Test 1 (createSignedUploadUrl, provider=supabase) : appelle `supabase().storage.from(bucket).createSignedUploadUrl(key, { upsert: true })` et retourne `{ path, token, signedUrl }` tirés de `data`.
    - Test 2 (createSignedUploadUrl, provider=minio) : throw une erreur explicite « Supabase uniquement » (MinIO local = upload serveur, pas de direct-to-storage).
    - Test 3 (objectExists true) : `list(prefix, { search: name })` renvoie un objet dont `.name === name` → retourne `true`, SANS jamais appeler `download`.
    - Test 4 (objectExists false) : `list` renvoie un tableau vide → retourne `false`.
    - Test 5 (ensureBucket Supabase) : quand le bucket n'existe pas, appelle `createBucket(bucket, { public: false, fileSizeLimit: 52428800 })` (50 * 1024 * 1024).
    - Test 6 (createSignedDownloadUrl Supabase) : appelle `createSignedUrl(key, 600)` par défaut et retourne `data.signedUrl`.
  </behavior>
  <action>
    Créer `apps/web/src/lib/__tests__/storage.test.ts`. Mocker `@supabase/supabase-js` : `vi.mock('@supabase/supabase-js', ...)` avec un `createClient` renvoyant un faux client dont `.storage.from()` expose des mocks `createSignedUploadUrl`, `list`, `createSignedUrl`, `getBucket`, `createBucket`. Déclarer les mocks via `vi.hoisted` (les factories vi.mock sont hoistées au-dessus des const). Mocker AUSSI `@qualiof/shared/env` via un getter sur un objet `mockEnv` hoisté pour piloter `STORAGE_PROVIDER` ('supabase' ou 'minio') sans charger `.env` (le harness ne charge pas `.env`, cf. RESEARCH « NB pattern projet »). Pour tester le provider minio (Test 2), il faut ré-importer le module avec `vi.resetModules()` + `mockEnv.STORAGE_PROVIDER = 'minio'` car `PROVIDER` est capturé au load du module.

    Valeurs exactes attendues dans les assertions :
    - Test 1 : mock `createSignedUploadUrl` résout `{ data: { path: 'p/x.pdf', token: 'tok-123', signedUrl: 'https://sb/signed/p/x.pdf?token=tok-123' }, error: null }` → assertion `result.token === 'tok-123'` et `result.signedUrl` contient 'signed'. Assertion mutation-safe : `expect(fromMock().createSignedUploadUrl).toHaveBeenCalledWith('p/x.pdf', { upsert: true })`.
    - Test 2 : `await expect(createSignedUploadUrl('preinscriptions', 'k')).rejects.toThrow(/Supabase uniquement/)`.
    - Test 3 : `list` résout `{ data: [{ name: 'x.pdf' }], error: null }` avec key `'apprenants/t1/uuid/cni.pdf'` → prefix `'apprenants/t1/uuid'`, name `'cni.pdf'` → `list` NON appelé avec le nom seul mais `expect(listMock).toHaveBeenCalledWith('apprenants/t1/uuid', { search: 'cni.pdf' })` ; `objectExists` retourne `true`. Vérifier que `download` du mock N'est PAS appelé (`expect(downloadMock).not.toHaveBeenCalled()`).
    - Test 4 : `list` résout `{ data: [], error: null }` → `false`.
    - Test 5 : `getBucket` résout `{ data: null }`, assertion `expect(createBucketMock).toHaveBeenCalledWith(expect.any(String), { public: false, fileSizeLimit: 52428800 })`.
    - Test 6 : `createSignedUrl` résout `{ data: { signedUrl: 'https://sb/dl?tok' }, error: null }` → `expect(createSignedUrl).toHaveBeenCalledWith('k', 600)`.

    Commande verify : `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/storage.test.ts`. À ce stade RED attendu (fonctions createSignedUploadUrl/objectExists absentes → import échoue ou undefined). NE PAS committer un RED masqué par une erreur d'import de test — le RED doit venir des fonctions manquantes, pas d'une faute de frappe.
  </action>
  <verify>
    <automated>MISSING — ce test EST le Wave 0 gap ; il doit être RED tant que Task 2 n'a pas ajouté les fonctions</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/__tests__/storage.test.ts` existe
    - `grep -c "createSignedUploadUrl" apps/web/src/lib/__tests__/storage.test.ts` ≥ 2
    - `grep -c "objectExists" apps/web/src/lib/__tests__/storage.test.ts` ≥ 2
    - `grep -c "not.toHaveBeenCalled" apps/web/src/lib/__tests__/storage.test.ts` ≥ 1 (prouve objectExists ne télécharge pas)
    - `grep "fileSizeLimit: 52428800\|50 \* 1024 \* 1024\|52428800" apps/web/src/lib/__tests__/storage.test.ts` trouve une occurrence
    - `grep "@supabase/supabase-js" apps/web/src/lib/__tests__/storage.test.ts` trouve le mock
  </acceptance_criteria>
  <done>Le fichier de test existe, couvre les 6 comportements, est RED (fonctions cibles absentes) — prouvé par l'exécution vitest qui échoue sur createSignedUploadUrl/objectExists non exportés.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ajouter createSignedUploadUrl + objectExists à l'adaptateur (GREEN)</name>
  <files>apps/web/src/lib/storage.ts</files>
  <read_first>
    - apps/web/src/lib/storage.ts (structure : PROVIDER, supabase(), ensureBucket, createSignedDownloadUrl — répliquer le style)
    - apps/web/src/lib/__tests__/storage.test.ts (les assertions à satisfaire, créées en Task 1)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (section « Code Examples » : signatures exactes)
  </read_first>
  <behavior>
    - createSignedUploadUrl throw sur MinIO, sinon appelle ensureBucket puis createSignedUploadUrl Supabase avec upsert:true, retourne {path, token, signedUrl}.
    - objectExists sur Supabase utilise list(prefix, { search: name }) et teste la présence par nom exact ; sur MinIO utilise HeadObjectCommand (existe → true, NotFound/404 → false).
  </behavior>
  <action>
    Dans `apps/web/src/lib/storage.ts`, ajouter APRÈS `createSignedDownloadUrl` (avant `export const _internals`) :

    1. `createSignedUploadUrl(bucket: string, key: string): Promise<{ path: string; token: string; signedUrl: string }>` :
    ```typescript
    export async function createSignedUploadUrl(
      bucket: string,
      key: string,
    ): Promise<{ path: string; token: string; signedUrl: string }> {
      if (PROVIDER !== 'supabase') {
        throw new Error(
          'createSignedUploadUrl : Supabase uniquement (MinIO local = upload serveur, pas de direct-to-storage)',
        );
      }
      await ensureBucket(bucket);
      const { data, error } = await supabase()
        .storage.from(bucket)
        .createSignedUploadUrl(key, { upsert: true });
      if (error) throw new Error(`Supabase signed upload URL failed : ${error.message}`);
      return { path: data.path, token: data.token, signedUrl: data.signedUrl };
    }
    ```

    2. `objectExists(bucket: string, key: string): Promise<boolean>` — Supabase via list(metadata), MinIO via HeadObjectCommand :
    ```typescript
    export async function objectExists(bucket: string, key: string): Promise<boolean> {
      const idx = key.lastIndexOf('/');
      const prefix = idx >= 0 ? key.slice(0, idx) : '';
      const name = idx >= 0 ? key.slice(idx + 1) : key;
      if (PROVIDER === 'supabase') {
        const { data, error } = await supabase()
          .storage.from(bucket)
          .list(prefix, { search: name });
        if (error) throw new Error(`Supabase list failed : ${error.message}`);
        return (data ?? []).some((o) => o.name === name);
      }
      // MinIO : HeadObjectCommand — existe → true, 404/NotFound → false
      try {
        await s3().send(new HeadObjectCommand({ Bucket: bucket, Key: key }));
        return true;
      } catch (e: any) {
        if (e?.name === 'NotFound' || e?.$metadata?.httpStatusCode === 404) return false;
        throw e;
      }
    }
    ```
    Ajouter `HeadObjectCommand` à l'import `@aws-sdk/client-s3` en tête de fichier (à côté de `HeadBucketCommand`). NE PAS toucher aux fonctions existantes (ensureBucket/uploadFile/downloadFile/createSignedDownloadUrl).

    Commande verify : `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/storage.test.ts` (doit passer GREEN 6/6). Puis test de puissance (convention projet, memory feedback_test_de_puissance_mutation) : casser temporairement `{ upsert: true }` → `{ upsert: false }` dans createSignedUploadUrl, vérifier que le Test 1 devient ROUGE, restaurer, re-vérifier 6/6, `git diff --stat` propre.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function createSignedUploadUrl" apps/web/src/lib/storage.ts` = 1
    - `grep -c "export async function objectExists" apps/web/src/lib/storage.ts` = 1
    - `grep "HeadObjectCommand" apps/web/src/lib/storage.ts` trouve l'import ET l'usage (≥ 2 occurrences)
    - `grep "Supabase uniquement" apps/web/src/lib/storage.ts` trouve le throw MinIO
    - `grep "{ upsert: true }" apps/web/src/lib/storage.ts` trouve l'upsert dans createSignedUploadUrl
    - `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/storage.test.ts` exit 0 (6/6 verts)
    - `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>createSignedUploadUrl et objectExists exportés, tests 6/6 verts, tsc clean, test de puissance prouvé (mutation upsert → rouge → restauré). Aucune fonction existante modifiée.</done>
</task>

</tasks>

<verification>
- Suite storage verte : `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/storage.test.ts` → 6/6
- tsc clean : `pnpm --filter @qualiof/web exec tsc --noEmit` → exit 0
- Aucun appel `@supabase/supabase-js` ajouté HORS storage.ts : `grep -rn "createSignedUploadUrl\|@supabase/supabase-js" apps/web/src --include="*.ts" --include="*.tsx" | grep -v "storage.ts\|__tests__"` → vide à ce stade (les consommateurs viennent aux plans 02/03/04)
</verification>

<success_criteria>
- STOR-01 (moitié adaptateur) : `createSignedUploadUrl` + `objectExists` disponibles dans l'interface unique, testés hermétiquement. `ensureBucket` public:false + 50 MiB confirmé par test. `createSignedDownloadUrl` TTL 600s confirmé par test. La validation « accès non-signé refusé » (infra prod réelle) est déléguée au 18-SMOKE.md (plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-01-SUMMARY.md`
</output>
