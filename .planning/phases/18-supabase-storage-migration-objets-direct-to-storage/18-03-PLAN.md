---
phase: 18-supabase-storage-migration-objets-direct-to-storage
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - packages/shared/src/env.ts
  - turbo.json
  - .env.example
  - apps/web/src/server/actions/storage-upload.ts
  - apps/web/src/server/actions/__tests__/storage-upload.test.ts
  - apps/web/src/app/api/documents/[id]/route.ts
  - apps/web/src/app/api/apprenants/[id]/docs/[kind]/route.ts
  - apps/web/src/app/api/pedagogical-assets/[id]/route.ts
autonomous: true
requirements: [STOR-03]
must_haves:
  truths:
    - "Une server action génère un signed upload URL scopé par user.tenantId, sans exposer le service_role au client"
    - "Une server action de confirmation post-upload recâble l'OCR (extractPreEnrollmentDocuments / chemin admin) — l'OCR ne meurt pas dans la refonte direct-to-storage"
    - "L'OCR reçoit une image downscalée si l'original dépasse la limite vision (pas d'échec silencieux sur photo 30-50 Mo)"
    - "Les 3 routes de serving redirigent 302 vers une signed URL fraîche en prod Supabase (contourne le cap 4,5 Mo réponse Vercel)"
    - "NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY déclarés fail-loud dans env.ts (client) + turbo.json globalEnv"
  artifacts:
    - path: "apps/web/src/server/actions/storage-upload.ts"
      provides: "createApprenantUploadUrl (signed upload URL, scope tenant) + confirmApprenantUpload (recâble OCR)"
      contains: "createSignedUploadUrl"
    - path: "packages/shared/src/env.ts"
      provides: "NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY (client)"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "apps/web/src/server/actions/storage-upload.ts"
      to: "createSignedUploadUrl (adaptateur, plan 01)"
      via: "génération token signé côté serveur"
      pattern: "createSignedUploadUrl\\("
    - from: "apps/web/src/server/actions/storage-upload.ts confirm"
      to: "extractPreEnrollmentDocuments / OCR"
      via: "recâblage OCR sur confirmation post-upload"
      pattern: "extractPreEnrollmentDocuments\\("
    - from: "apps/web/src/app/api/documents/[id]/route.ts"
      to: "createSignedDownloadUrl → NextResponse.redirect"
      via: "redirect 302 en prod Supabase"
      pattern: "NextResponse.redirect"
---

<objective>
Poser le CÔTÉ SERVEUR du direct-to-storage (STOR-03) : (1) déclarer les clés client Supabase (`NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`) fail-loud, (2) une server action `createApprenantUploadUrl` qui génère un signed upload URL scopé tenant, (3) une server action `confirmApprenantUpload` qui recâble l'OCR après upload direct (Pitfall 4 : l'OCR ne doit pas mourir dans la refonte) avec downscale image avant vision (Pitfall 3), (4) faire rediriger 302 les 3 routes de serving vers une signed URL fraîche en prod (Pitfall 5 : cap 4,5 Mo sur la réponse Vercel). Le composant client d'upload (progression, retry) vient au plan 04.

Purpose: STOR-03 côté serveur. Aucun octet de fichier ne doit transiter par une server action après cette refonte — seuls le token et la confirmation passent par Vercel (Pitfall 2 : 413).
Output: env client + 2 server actions + 3 routes redirect + tests hermétiques.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-CONTEXT.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md
@apps/web/src/lib/storage.ts
@apps/web/src/server/actions/upload-apprenant-docs.ts
@apps/web/src/server/actions/preinscription-public.ts
@apps/web/src/lib/preinscription-extractor.ts
@apps/web/src/app/api/documents/[id]/route.ts
@packages/shared/src/env.ts

<interfaces>
<!-- Disponible depuis plan 01 (adaptateur) : -->
```typescript
export async function createSignedUploadUrl(bucket, key):
  Promise<{ path: string; token: string; signedUrl: string }>;
export async function createSignedDownloadUrl(bucket, key, expiresInSec=600): Promise<string>;
export async function downloadFile(bucket, key): Promise<Buffer>;
export const DOCS_BUCKET: string;          // 'qualiof-docs'
export const PREENROLLMENT_BUCKET: string; // 'preinscriptions'
```
<!-- OCR à recâbler (extractor existant) : -->
```typescript
export async function extractPreEnrollmentDocuments(preEnrollmentId: string): Promise<void>;
// (chemin admin manuel : extractDocsFromBuffers(files) — pure, sans side-effect DB)
```
<!-- Auth : -->
```typescript
const { user } = await validateRequest(); // user.tenantId pour scope
```
<!-- Clé serveur pour signer côté PreEnrollment (public) : le token public /p/[token] -->
<!-- n'a PAS de session Lucia → l'action publique doit valider via PreEnrollment.token, PAS validateRequest. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Déclarer les clés client Supabase fail-loud (env + turbo + example)</name>
  <files>packages/shared/src/env.ts, turbo.json, .env.example</files>
  <read_first>
    - packages/shared/src/env.ts (bloc `client:` ligne 105, runtimeEnv ligne 109, patron des clés NEXT_PUBLIC_*)
    - turbo.json (globalEnv — comment les clés cloud Phase 17 y sont listées)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Runtime State Inventory : nouvelles clés client)
  </read_first>
  <action>
    Le client browser doit uploader via `uploadToSignedUrl`/XHR sans jamais recevoir le `SUPABASE_SERVICE_ROLE_KEY`. Il a besoin de l'URL publique + ANON key (publiable, sûre côté client).

    1. `packages/shared/src/env.ts`, bloc `client:` (après `NEXT_PUBLIC_APP_NAME` ligne 107) ajouter :
    ```typescript
    NEXT_PUBLIC_SUPABASE_URL: z.string().url().optional(),
    NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().optional(),
    ```
    (optional : le dev local MinIO n'en a pas besoin ; la validation d'usage réel se fait au call site upload direct qui throw si absent en mode supabase.)

    2. Même fichier, bloc `runtimeEnv` (après `NEXT_PUBLIC_APP_NAME: process.env.NEXT_PUBLIC_APP_NAME,` ligne 157) ajouter :
    ```typescript
    NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
    NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
    ```

    3. `turbo.json` globalEnv : ajouter les 2 clés `NEXT_PUBLIC_SUPABASE_URL` et `NEXT_PUBLIC_SUPABASE_ANON_KEY` à la liste (invalidation cache).

    4. `.env.example` : ajouter sous le bloc Supabase existant :
    ```
    # Supabase Storage — clés CLIENT (upload direct-to-storage, publiables)
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    ```
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NEXT_PUBLIC_SUPABASE_URL" packages/shared/src/env.ts` ≥ 2 (client + runtimeEnv)
    - `grep -c "NEXT_PUBLIC_SUPABASE_ANON_KEY" packages/shared/src/env.ts` ≥ 2
    - `grep "NEXT_PUBLIC_SUPABASE_URL" turbo.json` trouve la clé
    - `grep "NEXT_PUBLIC_SUPABASE_ANON_KEY" turbo.json` trouve la clé
    - `grep "NEXT_PUBLIC_SUPABASE_URL" .env.example` trouve la clé
    - `pnpm --filter @qualiof/shared exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>2 clés client déclarées dans env.ts (client + runtimeEnv), turbo.json globalEnv, .env.example ; tsc shared clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server actions signed upload URL + confirmation OCR (avec downscale)</name>
  <files>apps/web/src/server/actions/storage-upload.ts, apps/web/src/server/actions/__tests__/storage-upload.test.ts</files>
  <read_first>
    - apps/web/src/server/actions/upload-apprenant-docs.ts (safeExt, guessContentType, scope user.tenantId, clé apprenants/{tenantId}/{uuid}/{kind}.{ext})
    - apps/web/src/server/actions/preinscription-public.ts (validation par PreEnrollment.token pour le chemin PUBLIC, safeKey, fire-and-forget OCR)
    - apps/web/src/lib/preinscription-extractor.ts (extractPreEnrollmentDocuments, downloadFile depuis PREENROLLMENT_BUCKET)
    - apps/web/src/lib/storage.ts (createSignedUploadUrl, downloadFile)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Pattern 1 direct-to-storage + Pitfall 3 downscale + Pitfall 4 OCR)
  </read_first>
  <behavior>
    - createApprenantUploadUrl(kind, ext) [ADMIN] : validateRequest, scope tenant, génère path `apprenants/{tenantId}/{uuid}/{kind}.{ext}` + signed upload URL, retourne { ok, path, token, signedUrl }.
    - createPreEnrollmentUploadUrl(token, kind, ext) [PUBLIC] : valide via PreEnrollment.token (PAS validateRequest), path `{token}/{kind}-{stamp}.{ext}` bucket preinscriptions, retourne { ok, path, token: uploadToken, signedUrl }.
    - confirmPreEnrollmentUpload(token, keysByKind) : met à jour PreEnrollment.cniKey/ribKey/cfpKey + status SUBMITTED, PUIS déclenche extractPreEnrollmentDocuments (fire-and-forget) — recâble l'OCR (Pitfall 4).
    - confirmApprenantUpload(keysByKind) [ADMIN] : retourne les keys pour le wizard (extraction admin déclenchée côté appelant via extractDocsFromBuffers, ou via downloadFile+OCR — conserver le comportement actuel).
    - downscaleForOcr(buffer, contentType) : si image et taille > seuil, réduit avant vision ; sinon renvoie tel quel.
  </behavior>
  <action>
    Créer `apps/web/src/server/actions/storage-upload.ts` (`'use server'`). Toutes les actions retournent `{ ok, error }` discriminé, erreurs en FRANÇAIS.

    Helpers (copier de upload-apprenant-docs.ts) : `safeExt(name)`, `guessContentType`. Type `ApprenantDocKind = 'CNI'|'RIB'|'CFP'`.

    1. `createApprenantUploadUrl(kind: ApprenantDocKind, ext: string)` :
    ```typescript
    const { user } = await validateRequest();
    if (!user) return { ok: false, error: 'Non authentifié' };
    const folder = randomUUID();
    const path = `apprenants/${user.tenantId}/${folder}/${kind.toLowerCase()}.${safeExt('x.' + ext)}`;
    try {
      const { token, signedUrl } = await createSignedUploadUrl(DOCS_BUCKET, path);
      return { ok: true, path, token, signedUrl };
    } catch (e: any) { return { ok: false, error: `Préparation upload échouée : ${e?.message ?? e}` }; }
    ```

    2. `createPreEnrollmentUploadUrl(peToken: string, kind: ApprenantDocKind, ext: string)` [PUBLIC — pas de session] :
    ```typescript
    const pe = await prisma.preEnrollment.findUnique({ where: { token: peToken } });
    if (!pe) return { ok: false, error: 'Lien invalide' };
    if (pe.expiresAt < new Date()) return { ok: false, error: 'Ce lien a expiré' };
    const stamp = Date.now();
    const path = `${peToken}/${kind.toLowerCase()}-${stamp}.${safeExt('x.' + ext)}`;
    try {
      const { token, signedUrl } = await createSignedUploadUrl(PREENROLLMENT_BUCKET, path);
      return { ok: true, path, token, signedUrl };
    } catch (e: any) { return { ok: false, error: `Préparation upload échouée : ${e?.message ?? e}` }; }
    ```

    3. `confirmPreEnrollmentUpload(peToken, keys, fields)` — met à jour PreEnrollment avec les keys uploadées + les champs texte du formulaire (reprend la logique de mise à jour de `submitPreEnrollmentForm` : firstName/lastName/email/... + cniKey/ribKey/cfpKey + rgpdAcceptedAt + submittedAt + status 'SUBMITTED'), PUIS **recâble l'OCR** (Pitfall 4) :
    ```typescript
    Promise.resolve().then(() =>
      extractPreEnrollmentDocuments(pe.id).catch((err) =>
        console.error('Extraction IA échouée pour', pe.id, err)),
    );
    return { ok: true };
    ```
    Valider token + expiration + status comme submitPreEnrollmentForm. `revalidatePath('/app/inscriptions')`.

    4. `confirmApprenantUpload(keys: Partial<Record<ApprenantDocKind,string>>)` [ADMIN] : `validateRequest` scope tenant, retourne `{ ok: true, keys }` (le wizard admin persiste les keys + déclenche son extraction comme aujourd'hui — pas de régression de contrat).

    5. `downscaleForOcr(buffer: Buffer, contentType: string): Promise<Buffer>` — Pitfall 3. Décision downscale (RESEARCH Open Q2, sharp ABSENT du package.json) : **utiliser une réduction conditionnelle sans nouvelle dépendance lourde** — si `contentType` commence par `image/` ET `buffer.length > 4 * 1024 * 1024` (4 Mo), tenter un downscale. Ajouter `sharp` UNIQUEMENT si retenu : `pnpm add sharp -F @qualiof/web` puis `sharp(buffer).resize({ width: 2000, withoutEnlargement: true }).jpeg({ quality: 80 }).toBuffer()`. Si `sharp` non ajouté, implémenter `downscaleForOcr` comme NO-OP documenté (retourne le buffer tel quel) + commentaire TODO clair ; le seuil vision est géré par le message d'erreur OCR existant. **Trancher : ajouter sharp** (photo smartphone 10-50 Mo = critère de succès #3, le NO-OP risque l'échec OCR silencieux Pitfall 3). Wraper l'appel sharp en try/catch → fallback buffer original si sharp échoue. Exporter `downscaleForOcr` et l'appeler dans le chemin OCR si tu recâbles le download→vision ici (sinon le laisser exporté pour consommation par l'extractor — documenter).

    Créer le test `apps/web/src/server/actions/__tests__/storage-upload.test.ts` (hermétique) :
    - mock `@/lib/storage` (createSignedUploadUrl → `{ path:'p', token:'tok', signedUrl:'https://sb/put' }`), `@/lib/auth` (validateRequest → `{ user: { tenantId: 't1' } }`), `@qualiof/db` (prisma preEnrollment), `@/lib/preinscription-extractor` (extractPreEnrollmentDocuments spy).
    - Test 1 : `createApprenantUploadUrl('CNI','jpg')` → `ok:true`, `path` contient `apprenants/t1/` et `cni.jpg`, `createSignedUploadUrl` appelé avec DOCS_BUCKET.
    - Test 2 : `createApprenantUploadUrl` sans user → `{ ok:false, error:'Non authentifié' }`.
    - Test 3 : `createPreEnrollmentUploadUrl('badtok',...)` avec prisma → null → `{ ok:false, error:'Lien invalide' }`.
    - Test 4 : `confirmPreEnrollmentUpload` valide → `extractPreEnrollmentDocuments` appelé exactement 1 fois (recâblage OCR prouvé). Assertion mutation-safe.
    - Test 5 : `createPreEnrollmentUploadUrl` sur token expiré → `{ ok:false, error:'Ce lien a expiré' }`.

    Commande verify : `pnpm --filter @qualiof/web exec vitest run src/server/actions/__tests__/storage-upload.test.ts`. Test de puissance : retirer l'appel `extractPreEnrollmentDocuments` de confirmPreEnrollmentUpload → Test 4 ROUGE → restaurer → 5/5, git diff propre.

    ⚠ NE PAS supprimer `upload-apprenant-docs.ts` ni `submitPreEnrollmentForm` dans ce plan (le composant client bascule au plan 04 ; garder les anciens chemins fonctionnels jusqu'à la bascule client pour ne rien casser). Le plan 04 retirera l'ancien code base64 une fois le nouveau chemin câblé.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run src/server/actions/__tests__/storage-upload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function createApprenantUploadUrl\|export async function createPreEnrollmentUploadUrl\|export async function confirmPreEnrollmentUpload\|export async function confirmApprenantUpload" apps/web/src/server/actions/storage-upload.ts` = 4
    - `grep "createSignedUploadUrl" apps/web/src/server/actions/storage-upload.ts` trouve l'appel (≥ 2, admin + public)
    - `grep "extractPreEnrollmentDocuments" apps/web/src/server/actions/storage-upload.ts` trouve le recâblage OCR
    - `grep "user.tenantId" apps/web/src/server/actions/storage-upload.ts` trouve le scope tenant admin
    - `grep "PreEnrollment.findUnique\|preEnrollment.findUnique" apps/web/src/server/actions/storage-upload.ts` trouve la validation par token public
    - `grep "downscaleForOcr" apps/web/src/server/actions/storage-upload.ts` trouve la fonction downscale
    - `grep -c "not.*authentifié\|Non authentifié\|Lien invalide\|expiré" apps/web/src/server/actions/storage-upload.ts` ≥ 3 (erreurs françaises)
    - `pnpm --filter @qualiof/web exec vitest run src/server/actions/__tests__/storage-upload.test.ts` exit 0 (5/5)
  </acceptance_criteria>
  <done>4 server actions (2 génération signed URL admin+public, 2 confirmation), OCR recâblé (test 4 prouvé mutation-safe), downscale avant vision, scope tenant, erreurs françaises, 5/5 verts.</done>
</task>

<task type="auto">
  <name>Task 3: Redirect 302 signed URL sur les 3 routes de serving (prod Supabase)</name>
  <files>apps/web/src/app/api/documents/[id]/route.ts, apps/web/src/app/api/apprenants/[id]/docs/[kind]/route.ts, apps/web/src/app/api/pedagogical-assets/[id]/route.ts</files>
  <read_first>
    - apps/web/src/app/api/documents/[id]/route.ts (proxy downloadFile actuel + Cache-Control no-store)
    - apps/web/src/app/api/apprenants/[id]/docs/[kind]/route.ts (résolution key CNI/RIB/CFP multi-source)
    - apps/web/src/app/api/pedagogical-assets/[id]/route.ts (symétrique documents)
    - apps/web/src/lib/storage.ts (createSignedDownloadUrl Supabase, _internals.PROVIDER)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Pattern 3 redirect 302 + Pitfall 5)
  </read_first>
  <action>
    Objectif : en prod Supabase, servir les gros PDF/images via redirect 302 vers une signed URL FRAÎCHE (TTL 600s, régénérée à chaque hit = préserve le comportement no-store, cf. leçon Laurent 2026-07-01). En MinIO local, conserver le proxy `downloadFile` (createSignedDownloadUrl throw sur MinIO).

    Dans les 3 routes, après avoir résolu `key` (et vérifié auth + tenant + not-found comme aujourd'hui), remplacer le bloc de streaming par un aiguillage sur le provider. Importer `createSignedDownloadUrl` et `_internals` depuis `@/lib/storage`.

    Pattern à appliquer aux 3 routes (adapter le bucket/key résolu de chaque route) :
    ```typescript
    if (_internals.PROVIDER === 'supabase') {
      const url = await createSignedDownloadUrl(DOCS_BUCKET, key, 600);
      return NextResponse.redirect(url, 302);
    }
    // MinIO local : proxy inchangé (downloadFile + stream buffer + Cache-Control no-store)
    const buffer = await downloadFile(DOCS_BUCKET, key);
    return new NextResponse(new Uint8Array(buffer), { status: 200, headers: { ... } });
    ```
    - `documents/[id]/route.ts` : bucket DOCS_BUCKET, key = `doc.pdfUrl`.
    - `apprenants/[id]/docs/[kind]/route.ts` : bucket DOCS_BUCKET, key = résolu (cni→sensitiveData.idDocumentUrl, rib→ribKey, cfp→cfpAttestationKey).
    - `pedagogical-assets/[id]/route.ts` : bucket DOCS_BUCKET, key = `asset.pdfUrl`.

    Conserver INTÉGRALEMENT la logique auth/tenant/not-found/try-catch existante. Ne modifier QUE la branche « servir l'objet ». Le redirect 302 NE porte PAS de Cache-Control (l'URL signée est déjà fraîche/éphémère). Garder le `console.warn` dev + `return new NextResponse('Document indisponible', { status: 500 })` dans le catch.

    NB : `_internals` est déjà exporté par storage.ts (`export const _internals = { PROVIDER }`). Si `_internals.PROVIDER` n'est pas assez pratique, tu peux ajouter un export `export const STORAGE_PROVIDER = PROVIDER;` dans storage.ts — mais préfère réutiliser `_internals` pour ne pas élargir l'interface.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NextResponse.redirect" apps/web/src/app/api/documents/[id]/route.ts` ≥ 1
    - `grep -c "NextResponse.redirect" apps/web/src/app/api/apprenants/[id]/docs/[kind]/route.ts` ≥ 1
    - `grep -c "NextResponse.redirect" apps/web/src/app/api/pedagogical-assets/[id]/route.ts` ≥ 1
    - `grep "createSignedDownloadUrl" apps/web/src/app/api/documents/[id]/route.ts` trouve l'appel
    - `grep "PROVIDER === 'supabase'\|_internals.PROVIDER" apps/web/src/app/api/documents/[id]/route.ts` trouve l'aiguillage
    - Les 3 routes conservent `validateRequest` (auth non régressée) : `grep -c "validateRequest" apps/web/src/app/api/documents/[id]/route.ts apps/web/src/app/api/apprenants/[id]/docs/[kind]/route.ts apps/web/src/app/api/pedagogical-assets/[id]/route.ts` = 3
    - `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Les 3 routes redirigent 302 vers signed URL en prod Supabase, conservent le proxy MinIO local + toute la logique auth/tenant/not-found. tsc clean.</done>
</task>

</tasks>

<verification>
- Tests server action verts : `pnpm --filter @qualiof/web exec vitest run src/server/actions/__tests__/storage-upload.test.ts` → 5/5
- tsc web clean : `pnpm --filter @qualiof/web exec tsc --noEmit` → exit 0
- Aucun octet de fichier ne transite par les nouvelles server actions (seuls token/confirmation) : les actions ne reçoivent PAS de File/base64/Buffer d'upload — `grep -c "arrayBuffer()\|base64" apps/web/src/server/actions/storage-upload.ts` = 0
- OCR recâblé : `grep "extractPreEnrollmentDocuments" apps/web/src/server/actions/storage-upload.ts` présent
</verification>

<success_criteria>
- STOR-03 (serveur) : signed upload URL scopé tenant (admin) + public par token, confirmation qui recâble l'OCR (Pitfall 4 évité), downscale avant vision (Pitfall 3), 3 routes redirect 302 (Pitfall 5), clés client fail-loud. La preuve prod (photo 10 Mo, pas de 413) vient du composant client (plan 04) + 18-SMOKE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-03-SUMMARY.md`
</output>
