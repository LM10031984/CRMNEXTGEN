---
phase: 18-supabase-storage-migration-objets-direct-to-storage
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - apps/web/scripts/migrate-storage.ts
  - apps/web/scripts/__tests__/migrate-storage.test.ts
  - apps/web/package.json
files_created_runtime:
  - .planning/audit/STORAGE-MIGRATION-REPORT-{date}.md
autonomous: true
requirements: [STOR-02]
must_haves:
  truths:
    - "Un script DRY par défaut (WRITE=1 explicite) collecte TOUTES les clés storage référencées en base (8 champs sur 8 tables, 2 buckets)"
    - "En DRY, le script COMPTE et n'écrit RIEN (aucun uploadFile appelé)"
    - "Le script détecte les clés invalides Supabase (leading /, //, %, non-ASCII) et les liste sans tenter l'upload"
    - "Après WRITE, la vérif objectExists confirme 0 lien mort sur chaque clé migrée ; les orphelins (clé en base sans objet MinIO) sont listés SANS action automatique"
    - "Un rapport daté est écrit dans .planning/audit/ (total par bucket, liens par table, orphelins, clés invalides)"
  artifacts:
    - path: "apps/web/scripts/migrate-storage.ts"
      provides: "Migration idempotente MinIO→Supabase DRY→WRITE + vérif 0 lien mort + rapport"
      contains: "const WRITE = process.env.WRITE === '1'"
    - path: "apps/web/scripts/__tests__/migrate-storage.test.ts"
      provides: "Tests hermétiques : collecte clés, DRY n'écrit pas, détection clé invalide, orphelins"
      contains: "collectAllKeys"
  key_links:
    - from: "apps/web/scripts/migrate-storage.ts"
      to: "MinIO (source) via downloadFile puis Supabase (cible) via uploadFile"
      via: "downloadFromMinio → uploadToSupabase (upsert idempotent)"
      pattern: "for .*of .*allKeys"
    - from: "apps/web/scripts/migrate-storage.ts"
      to: "objectExists (adaptateur, plan 01)"
      via: "vérif post-migration 0 lien mort"
      pattern: "objectExists\\("
---

<objective>
Créer le script de migration idempotent `apps/web/scripts/migrate-storage.ts` qui, en mode DRY par défaut (WRITE=1 explicite), copie chaque objet référencé en base de MinIO vers Supabase, puis vérifie 0 lien mort et écrit un rapport daté audit-réutilisable. Périmètre COMPLET : les 8 champs de clé storage réels du schéma (pas seulement les 3 du critère de succès — sinon liens morts sur pièces apprenants et factures après bascule, RESEARCH.md « CRITIQUE »).

Purpose: STOR-02 (D-01 big-bang contrôlé, D-03 rapport archivé, D-04 orphelins listés sans action auto). C'est le filet de sécurité avant la bascule `STORAGE_PROVIDER=supabase`. Tant que la vérif ne passe pas 0 lien mort, l'app reste sur MinIO.
Output: script DRY→WRITE + tests hermétiques + npm script `storage:migrate`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-CONTEXT.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md
@apps/web/scripts/calendar-backfill.ts
@apps/web/src/lib/storage.ts

<interfaces>
<!-- PÉRIMÈTRE EXACT des clés à migrer (vérifié dans schema.prisma) : -->
```
Bucket qualiof-docs (DOCS_BUCKET) :
  Person.ribKey            (schema:155, String?)  — PII RIB
  SensitiveData.idDocumentUrl (schema:185, String?) — CNI apprenant
  Invoice.pdfUrl           (schema:830, String?)
  Quote.pdfUrl             (schema:900, String?)
  Document.pdfUrl          (schema:1008, String NON-null)
  AgeficeProfile.cfpAttestationKey (schema:1273, String?) — CFP
  PedagogicalAsset.pdfUrl  (schema:1392, String?)
Bucket preinscriptions (PREENROLLMENT_BUCKET) :
  PreEnrollment.cniKey / ribKey / cfpKey (schema:1313-1315, String?) — lus par l'OCR
```

<!-- Interface storage disponible (adaptateur, plan 01) — mais ATTENTION : -->
<!-- downloadFile/uploadFile aiguillent sur PROVIDER=sharedEnv.STORAGE_PROVIDER. -->
<!-- Le script a besoin de LIRE MinIO (source) ET d'ÉCRIRE Supabase (cible) EN MÊME TEMPS. -->
<!-- → Le script instancie ses PROPRES clients (S3 pour MinIO source, supabase-js pour cible), -->
<!--   il NE peut PAS s'appuyer sur le switch PROVIDER global. Voir Task 2 action. -->
```typescript
export async function objectExists(bucket: string, key: string): Promise<boolean>; // plan 01 — vérif, mais tourne sur PROVIDER global
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Écrire les tests hermétiques de la migration (Wave 0 — RED)</name>
  <files>apps/web/scripts/__tests__/migrate-storage.test.ts</files>
  <read_first>
    - apps/web/scripts/calendar-backfill.ts (patron DRY→WRITE, Recap, séquentiel for...of)
    - apps/web/src/lib/veille/__tests__/classify.test.ts (patron mock hermétique vi.hoisted)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Pattern 2 migration + Pitfall 1 clés invalides)
  </read_first>
  <behavior>
    - Test 1 (collectAllKeys) : mock prisma renvoyant 1 ligne par table → la fonction retourne un tableau `{ bucket, table, field, id, key }` couvrant les 8 champs, avec le bon bucket (preinscriptions pour PreEnrollment.*, qualiof-docs pour les 7 autres). Total attendu = somme des clés non-null.
    - Test 2 (isInvalidSupabaseKey) : `/leading`, `a//b`, `a%b`, `café/x.pdf` (accent) → true ; `apprenants/t1/uuid/cni.pdf`, `token/rib-123.jpg` → false.
    - Test 3 (DRY n'écrit pas) : en mode DRY (WRITE=false), le runner de migration ne doit JAMAIS appeler l'uploader cible — `expect(uploadToSupabaseMock).not.toHaveBeenCalled()`.
    - Test 4 (orphelin) : quand la lecture MinIO throw (objet absent), la clé va dans `report.orphans`, PAS dans `report.migrated`, et le run continue (try/catch par clé).
    - Test 5 (clé invalide court-circuitée) : une clé invalide (accent) va dans `report.invalidKeys` et N'est PAS envoyée à l'uploader même en WRITE.
  </behavior>
  <action>
    Créer `apps/web/scripts/__tests__/migrate-storage.test.ts`. Le script `migrate-storage.ts` doit EXPORTER ses fonctions pures pour être testable sans exécuter `main()` : `collectAllKeys(prisma)`, `isInvalidSupabaseKey(key: string): boolean`, et un runner `runMigration({ keys, write, downloadFromMinio, uploadToSupabase, verifyExists })` qui prend ses dépendances en injection (pas de client réel en test). Mocker `@qualiof/db` (prisma) via vi.hoisted. Passer des fakes pour download/upload/verify.

    Valeurs exactes :
    - Test 1 : prisma mock — `person.findMany` → `[{ id:'p1', ribKey:'apprenants/t/u/rib.pdf' }]`, `sensitiveData.findMany` → `[{ id:'s1', idDocumentUrl:'apprenants/t/u/cni.jpg' }]`, `invoice.findMany` → `[{ id:'i1', pdfUrl:'factures/f1.pdf' }]`, `quote.findMany` → `[{ id:'q1', pdfUrl:'devis/q1.pdf' }]`, `document.findMany` → `[{ id:'d1', pdfUrl:'docs/d1.pdf' }]`, `ageficeProfile.findMany` → `[{ id:'a1', cfpAttestationKey:'cfp/a1.pdf' }]`, `pedagogicalAsset.findMany` → `[{ id:'pa1', pdfUrl:'assets/pa1.pdf' }]`, `preEnrollment.findMany` → `[{ id:'pe1', cniKey:'tok/cni.jpg', ribKey:'tok/rib.jpg', cfpKey:null }]`. Assertion : `collectAllKeys` retourne 9 entrées (8 champs, cfpKey null exclu), dont exactement 2 avec `bucket === 'preinscriptions'` et 7 avec `bucket === 'qualiof-docs'`.
    - Test 2 : `expect(isInvalidSupabaseKey('/x')).toBe(true)`, `isInvalidSupabaseKey('a//b')` true, `isInvalidSupabaseKey('a%b')` true, `isInvalidSupabaseKey('café/x')` true, `isInvalidSupabaseKey('apprenants/t1/uuid/cni.pdf')` false.
    - Test 3 : `runMigration({ keys:[{bucket:'qualiof-docs',key:'docs/d1.pdf'}], write:false, downloadFromMinio: async()=>Buffer.from('x'), uploadToSupabase: upMock, verifyExists: async()=>true })` → `expect(upMock).not.toHaveBeenCalled()` et `report.migrated` vide (DRY ne migre pas, il simule).
    - Test 4 : `downloadFromMinio` rejette `new Error('NoSuchKey')` pour une clé → `report.orphans` contient cette clé, `report.migrated` ne la contient pas, le run ne throw pas.
    - Test 5 : `keys` contient `{ bucket:'qualiof-docs', key:'café/x.pdf' }`, `write:true` → clé dans `report.invalidKeys`, `upMock` NON appelé pour cette clé.

    Commande verify : `pnpm --filter @qualiof/web exec vitest run scripts/__tests__/migrate-storage.test.ts`. RED attendu (module migrate-storage.ts inexistant).
  </action>
  <verify>
    <automated>MISSING — Wave 0 gap ; RED tant que Task 2 n'a pas créé migrate-storage.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/scripts/__tests__/migrate-storage.test.ts` existe
    - `grep -c "collectAllKeys" apps/web/scripts/__tests__/migrate-storage.test.ts` ≥ 1
    - `grep -c "isInvalidSupabaseKey" apps/web/scripts/__tests__/migrate-storage.test.ts` ≥ 3
    - `grep "not.toHaveBeenCalled" apps/web/scripts/__tests__/migrate-storage.test.ts` trouve l'assertion DRY
    - `grep "orphans\|invalidKeys" apps/web/scripts/__tests__/migrate-storage.test.ts` trouve les deux catégories de rapport
    - `grep -c "preinscriptions" apps/web/scripts/__tests__/migrate-storage.test.ts` ≥ 1
  </acceptance_criteria>
  <done>Fichier de test existant couvrant collecte 8 champs / détection clé invalide / DRY sans écriture / orphelins, RED (module cible absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Écrire le script de migration DRY→WRITE (GREEN)</name>
  <files>apps/web/scripts/migrate-storage.ts, apps/web/package.json</files>
  <read_first>
    - apps/web/scripts/calendar-backfill.ts (WRITE flag, TENANT const, Recap, SESSION_DELAY_MS, séquentiel, main() + $disconnect finally)
    - apps/web/src/lib/storage.ts (PREENROLLMENT_BUCKET, DOCS_BUCKET, ensureBucket, objectExists)
    - apps/web/scripts/__tests__/migrate-storage.test.ts (contrats exacts à satisfaire)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Pattern 2 + Runtime State Inventory)
  </read_first>
  <behavior>
    - Exporte collectAllKeys / isInvalidSupabaseKey / runMigration (pures, injectables) + un main() CLI.
    - DRY par défaut, WRITE=1 explicite. Séquentiel for...of await, JAMAIS Promise.all.
    - Copie = downloadFromMinio(bucket,key) → uploadToSupabase(bucket,key,buf, upsert). Idempotent (upsert:true).
    - Écrit un rapport daté dans .planning/audit/.
  </behavior>
  <action>
    Créer `apps/web/scripts/migrate-storage.ts`, calqué sur `calendar-backfill.ts`.

    Constantes/flags : `const WRITE = process.env.WRITE === '1';`, `const ONLY_BUCKET = process.env.BUCKET;` (filtre optionnel 'docs'|'preinscriptions'), `const DELAY_MS = 50;` (lissage anti-throttle entre objets).

    Import `PREENROLLMENT_BUCKET`, `DOCS_BUCKET` depuis `@/lib/storage` — MAIS pour la SOURCE MinIO, instancier un client S3 DÉDIÉ dans le script (car le switch PROVIDER global sera `supabase` au moment de la migration côté cible ; le script doit lire MinIO indépendamment). Réutiliser les mêmes valeurs par défaut que storage.ts : endpoint `process.env.S3_ENDPOINT ?? 'http://localhost:9000'`, region `us-east-1`, accessKey `process.env.S3_ACCESS_KEY ?? 'qualiof'`, secretKey `process.env.S3_SECRET_KEY ?? 'qualiof_dev_minio'`, forcePathStyle true. `downloadFromMinio(bucket,key)` = `GetObjectCommand` → Buffer (répliquer le stream→Buffer de storage.ts:144-150). Pour la CIBLE Supabase, instancier `createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, { auth:{persistSession:false} })` DÉDIÉ ; `uploadToSupabase(bucket,key,buf)` = `.storage.from(bucket).upload(key, buf, { upsert: true, contentType })` (contentType déduit de l'extension). `verifyExists(bucket,key)` = list Supabase sur prefix/search (même logique qu'objectExists plan 01, mais sur le client cible dédié).

    `isInvalidSupabaseKey(key)` : retourne true si `key.startsWith('/')` OU `key.includes('//')` OU `key.includes('%')` OU `/[^\x00-\x7F]/.test(key)` (Pitfall 1).

    `collectAllKeys(prisma)` : pour chaque table (Person.ribKey, SensitiveData.idDocumentUrl, Invoice.pdfUrl, Quote.pdfUrl, Document.pdfUrl, AgeficeProfile.cfpAttestationKey, PedagogicalAsset.pdfUrl → bucket DOCS ; PreEnrollment.cniKey/ribKey/cfpKey → bucket PREINSCRIPTIONS), `findMany({ select: { id: true, <field>: true } })`, filtrer les valeurs non-null/non-vides, pousser `{ bucket, table, field, id, key }`.

    `runMigration({ keys, write, downloadFromMinio, uploadToSupabase, verifyExists })` :
    ```
    report = { migrated: [], orphans: [], invalidKeys: [], deadLinks: [], byBucket: {} }
    for (const item of keys) {           // SÉQUENTIEL
      if (isInvalidSupabaseKey(item.key)) { report.invalidKeys.push(item); continue; }
      try {
        const buf = await downloadFromMinio(item.bucket, item.key);
        if (write) await uploadToSupabase(item.bucket, item.key, buf);
        report.migrated.push({ ...item, size: buf.length });      // en DRY : migrated reste vide (voir Test 3)
      } catch (e) { report.orphans.push({ ...item, error: String(e) }); }
      if (write) await sleep(DELAY_MS);
    }
    // Vérif 0 lien mort (seulement en WRITE, sur ce qui a été migré) :
    if (write) for (const m of report.migrated) {
      if (!(await verifyExists(m.bucket, m.key))) report.deadLinks.push(m);
    }
    return report
    ```
    ⚠ EN DRY, `report.migrated` doit rester VIDE (Test 3) : n'ajouter à `migrated` QUE si `write===true`. En DRY, compter dans `report.byBucket[bucket].simulated++` à la place. Adapter la structure pour satisfaire Test 3 exactement.

    `main()` : log mode (DRY/WRITE), `collectAllKeys`, `runMigration`, puis écrire un rapport Markdown daté `.planning/audit/STORAGE-MIGRATION-REPORT-${new Date().toISOString().slice(0,10)}.md` via `fs.writeFileSync` (mkdir -p `.planning/audit` si besoin) contenant : mode, total par bucket, nb migrés, liste orphelins (table/field/id/key), liste clés invalides, liste liens morts (DOIT être vide pour autoriser la bascule). `finally { await prisma.$disconnect(); }`. Si `require.main === module` → `main()`.

    Ajouter à `apps/web/package.json` scripts : `"storage:migrate": "tsx scripts/migrate-storage.ts"`.

    Commande verify : `pnpm --filter @qualiof/web exec vitest run scripts/__tests__/migrate-storage.test.ts` (GREEN 5/5). Test de puissance : casser `if (write)` autour de `migrated.push` (le rendre inconditionnel) → Test 3 ROUGE (DRY migre) → restaurer → 5/5, git diff propre.

    ⚠ NE PAS exécuter le script (ni DRY ni WRITE) : l'exécution réelle DRY puis WRITE est une étape humaine GATÉE Laurent (D-01/D-02, règle « destructif = étape séparée », nécessite MinIO en marche + projet Supabase réel). Ce plan LIVRE le code + tests seulement.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec vitest run scripts/__tests__/migrate-storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const WRITE = process.env.WRITE === '1'" apps/web/scripts/migrate-storage.ts` = 1
    - `grep "for (const .* of " apps/web/scripts/migrate-storage.ts` trouve la boucle séquentielle ; `grep -c "Promise.all" apps/web/scripts/migrate-storage.ts` = 0
    - `grep -c "export function isInvalidSupabaseKey\|export async function collectAllKeys\|export async function runMigration\|export function collectAllKeys" apps/web/scripts/migrate-storage.ts` ≥ 3
    - `grep "ribKey\|idDocumentUrl\|cfpAttestationKey\|cniKey\|cfpKey" apps/web/scripts/migrate-storage.ts` couvre les 8 champs (tous présents)
    - `grep "STORAGE-MIGRATION-REPORT" apps/web/scripts/migrate-storage.ts` trouve le nom de rapport daté
    - `grep "\.planning/audit" apps/web/scripts/migrate-storage.ts` trouve le dossier de rapport
    - `grep "storage:migrate" apps/web/package.json` trouve le script
    - `grep "prisma.\$disconnect" apps/web/scripts/migrate-storage.ts` trouve le cleanup
    - `pnpm --filter @qualiof/web exec vitest run scripts/__tests__/migrate-storage.test.ts` exit 0 (5/5)
  </acceptance_criteria>
  <done>Script DRY→WRITE couvrant les 8 champs, séquentiel, détection clé invalide, rapport daté, tests 5/5 verts, test de puissance prouvé. Script NON exécuté (étape humaine gatée).</done>
</task>

</tasks>

<verification>
- Suite migration verte : `pnpm --filter @qualiof/web exec vitest run scripts/__tests__/migrate-storage.test.ts` → 5/5
- Séquentiel garanti : `grep -c "Promise.all" apps/web/scripts/migrate-storage.ts` = 0
- Périmètre complet : les 8 champs storage présents dans collectAllKeys
- Le script N'A PAS été exécuté (pas de rapport dans .planning/audit/ à ce stade — l'exécution DRY→WRITE est gatée Laurent)
</verification>

<success_criteria>
- STOR-02 (code) : script idempotent DRY→WRITE prêt, couvre TOUTES les clés (8 champs / 2 buckets), détecte clés invalides Supabase, vérifie 0 lien mort en WRITE, écrit rapport daté audit (D-03), liste orphelins sans action auto (D-04). La preuve « 0 lien mort réel » viendra de l'exécution humaine gatée (DRY→WRITE) documentée en SMOKE/audit.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-02-SUMMARY.md`
</output>
