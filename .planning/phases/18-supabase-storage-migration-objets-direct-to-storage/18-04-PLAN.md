---
phase: 18-supabase-storage-migration-objets-direct-to-storage
plan: 04
type: execute
wave: 3
depends_on: ["18-03"]
files_modified:
  - apps/web/src/components/shared/direct-upload-field.tsx
  - apps/web/src/components/preinscriptions/public-form.tsx
  - apps/web/src/components/forms/create-person-button.tsx
  - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md
autonomous: false
requirements: [STOR-03]
must_haves:
  truths:
    - "Le navigateur uploade le File brut DIRECTEMENT vers Supabase (aucun octet via une server action) — pas de 413 sur photo 10 Mo en prod"
    - "Une barre de progression réelle (pourcentage par fichier) s'affiche pendant l'upload (XHR onprogress)"
    - "1 retry automatique silencieux ; si nouvel échec, message français + bouton Réessayer sans perdre les autres champs saisis"
    - "Limite passée à 50 Mo (D-05) dans les 2 chemins ; base64 retiré du formulaire public"
    - "Le formulaire public et l'écran admin partagent le même composant d'upload direct"
  artifacts:
    - path: "apps/web/src/components/shared/direct-upload-field.tsx"
      provides: "Composant upload direct partagé : signed URL → XHR PUT progress → confirmation → retry"
      contains: "xhr.upload.onprogress"
    - path: "apps/web/src/components/preinscriptions/public-form.tsx"
      provides: "Formulaire public refondu direct-to-storage (base64 retiré)"
      contains: "createPreEnrollmentUploadUrl"
    - path: ".planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md"
      provides: "Procédure manuelle STOR-01 (accès non-signé refusé) + STOR-03 (photo 10 Mo prod → OCR)"
      contains: "10 Mo"
  key_links:
    - from: "apps/web/src/components/shared/direct-upload-field.tsx"
      to: "createApprenantUploadUrl / createPreEnrollmentUploadUrl (plan 03)"
      via: "génère signed URL puis XHR PUT direct vers Supabase"
      pattern: "createPreEnrollmentUploadUrl\\(|createApprenantUploadUrl\\("
    - from: "direct-upload-field.tsx"
      to: "Supabase signed URL (bypass Vercel)"
      via: "XMLHttpRequest PUT sur signedUrl"
      pattern: "xhr.open\\('PUT'"
---

<objective>
Poser le CÔTÉ CLIENT du direct-to-storage (STOR-03) : un composant partagé `direct-upload-field.tsx` (progression réelle D-06, 1 retry silencieux + bouton Réessayer D-07, 50 Mo D-05) qui génère un signed URL via les server actions du plan 03, envoie le `File` brut en XHR PUT DIRECTEMENT vers Supabase (aucun octet via Vercel → pas de 413 Pitfall 2), puis confirme au serveur (recâble l'OCR). Câbler ce composant dans le formulaire public `/p/[token]` (retirer le base64) ET l'écran admin (D-08). Écrire le `18-SMOKE.md` (validations prod manuelles). Checkpoint final : Laurent valide la photo 10 Mo réelle en prod (critère de succès #3, non négociable).

Purpose: Le cœur non négociable de la phase — une vraie photo CNI 10 Mo prise au smartphone passe en prod et déclenche l'OCR, pas de 413, pas d'échec silencieux.
Output: composant partagé + 2 formulaires refondus + SMOKE.md + validation humaine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-CONTEXT.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md
@apps/web/src/components/preinscriptions/public-form.tsx
@apps/web/src/server/actions/storage-upload.ts
@apps/web/src/components/forms/create-person-button.tsx

<interfaces>
<!-- Server actions disponibles (plan 03) : -->
```typescript
// PUBLIC (pas de session) :
createPreEnrollmentUploadUrl(peToken, kind, ext):
  Promise<{ ok:true; path; token; signedUrl } | { ok:false; error }>;
confirmPreEnrollmentUpload(peToken, keys: Partial<Record<'CNI'|'RIB'|'CFP',string>>, fields):
  Promise<{ ok:true } | { ok:false; error }>;
// ADMIN (session) :
createApprenantUploadUrl(kind, ext):
  Promise<{ ok:true; path; token; signedUrl } | { ok:false; error }>;
confirmApprenantUpload(keys): Promise<{ ok:true; keys } | { ok:false; error }>;
```
<!-- Upload direct via XHR PUT sur signedUrl (progression D-06). RESEARCH Code Example : -->
```typescript
const xhr = new XMLHttpRequest();
xhr.open('PUT', signedUrl);
xhr.setRequestHeader('Content-Type', file.type || 'application/octet-stream');
xhr.upload.onprogress = (e) => { if (e.lengthComputable) onProgress(Math.round((e.loaded/e.total)*100)); };
xhr.onload = () => (xhr.status>=200 && xhr.status<300 ? resolve() : reject(new Error(`HTTP ${xhr.status}`)));
xhr.onerror = () => reject(new Error('Erreur réseau pendant l\'upload'));
xhr.send(file);
```
<!-- NB Open Q1 : si le PUT direct sur signedUrl ne suffit pas (headers x-upsert/authorization), -->
<!-- fallback = client supabase-js browser via createClient(NEXT_PUBLIC_SUPABASE_URL, ANON_KEY) + -->
<!-- uploadToSignedUrl(path, token, file) — MAIS sans progress. Trancher au 1er run réel. -->
<!-- Priorité : XHR pour la progression D-06 ; documenter le résultat dans le SUMMARY. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composant partagé direct-upload-field (progress + retry + 50 Mo)</name>
  <files>apps/web/src/components/shared/direct-upload-field.tsx</files>
  <read_first>
    - apps/web/src/components/preinscriptions/public-form.tsx (FileSlot, setSlot, icônes lucide, style Tailwind du projet, cn())
    - apps/web/src/server/actions/storage-upload.ts (signatures des 4 actions — plan 03)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Pattern 1 + Code Examples XHR progress + Open Q1)
    - CLAUDE.md (next/image INTERDIT sur PII → <img> natif pour les aperçus CNI/RIB)
  </read_first>
  <action>
    Créer `apps/web/src/components/shared/direct-upload-field.tsx` (`'use client'`). Composant réutilisable public + admin (D-08).

    Props :
    ```typescript
    interface DirectUploadFieldProps {
      kind: 'CNI' | 'RIB' | 'CFP';
      label: string;
      description: string;
      required?: boolean;
      // fonction injectée qui génère le signed URL (diffère public vs admin) :
      requestUploadUrl: (kind: 'CNI'|'RIB'|'CFP', ext: string) =>
        Promise<{ ok: true; path: string; token: string; signedUrl: string } | { ok: false; error: string }>;
      // remonte la clé confirmée au parent (qui appellera la confirmation serveur) :
      onUploaded: (kind: 'CNI'|'RIB'|'CFP', path: string) => void;
      onCleared: (kind: 'CNI'|'RIB'|'CFP') => void;
    }
    ```

    État interne : `file`, `progress` (0-100), `status` ('idle'|'uploading'|'done'|'error'), `errorMsg`.

    Constante : `const MAX_FILE_SIZE_MB = 50;` (D-05, remplace 10).

    Helper `uploadWithProgress(signedUrl, file, onProgress)` = XHR PUT (copier le Code Example RESEARCH lignes 300-311) qui resolve à la fin ; `xhr.upload.onprogress` met à jour le state via `onProgress`.

    Flux au choix d'un fichier (`attemptUpload(file)`) :
    1. Valider taille : `if (file.size > MAX_FILE_SIZE_MB*1024*1024)` → status error, message français `Le fichier dépasse ${MAX_FILE_SIZE_MB} Mo`.
    2. `const ext = file.name.split('.').pop() ?? 'bin';`
    3. `const r = await requestUploadUrl(kind, ext);` si `!r.ok` → status error + `r.error`.
    4. `await uploadWithProgress(r.signedUrl, file, setProgress)`.
    5. Sur succès → status 'done', `onUploaded(kind, r.path)`.

    Retry D-07 : envelopper 3-4 ; en cas d'échec, RE-tenter automatiquement 1 fois EN SILENCE (pas de message). Si le 2ᵉ échoue → status 'error', message français clair + bouton « Réessayer » qui rappelle `attemptUpload(file)`. Les autres champs du formulaire NE sont PAS touchés (le composant gère UNIQUEMENT son propre fichier).

    UI : reprendre le style des slots de public-form.tsx (bordure, icône, description). Barre de progression (`<div>` largeur `${progress}%`) pendant 'uploading'. Aperçu image PII : `<img>` natif (JAMAIS `next/image`, CLAUDE.md + flag roadmap). Bouton « Réessayer » visible seulement en status 'error'.

    Ne PAS coder les server actions ici — injectées via `requestUploadUrl`. Composant agnostique public/admin.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" apps/web/src/components/shared/direct-upload-field.tsx` = 1
    - `grep "xhr.upload.onprogress" apps/web/src/components/shared/direct-upload-field.tsx` trouve la progression réelle (D-06)
    - `grep "xhr.open('PUT'" apps/web/src/components/shared/direct-upload-field.tsx` trouve le PUT direct
    - `grep "MAX_FILE_SIZE_MB = 50" apps/web/src/components/shared/direct-upload-field.tsx` trouve la limite 50 Mo (D-05)
    - `grep -i "réessayer" apps/web/src/components/shared/direct-upload-field.tsx` trouve le bouton retry (D-07)
    - `grep -c "next/image" apps/web/src/components/shared/direct-upload-field.tsx` = 0 (PII → <img> natif)
    - `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Composant partagé : upload direct XHR PUT, progression réelle, 1 retry auto + bouton Réessayer, 50 Mo, pas de next/image, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler le composant dans le formulaire public + admin (retirer base64)</name>
  <files>apps/web/src/components/preinscriptions/public-form.tsx, apps/web/src/components/forms/create-person-button.tsx</files>
  <read_first>
    - apps/web/src/components/preinscriptions/public-form.tsx (fileToBase64, handleSubmit, submitPreEnrollmentForm, slots, champs texte préservés)
    - apps/web/src/components/forms/create-person-button.tsx (comment uploadApprenantDocs est appelé côté admin aujourd'hui)
    - apps/web/src/components/shared/direct-upload-field.tsx (props DirectUploadField — Task 1)
    - apps/web/src/server/actions/storage-upload.ts (createPreEnrollmentUploadUrl, confirmPreEnrollmentUpload, createApprenantUploadUrl, confirmApprenantUpload)
  </read_first>
  <action>
    **public-form.tsx :**
    - RETIRER `fileToBase64` (lignes 93-104) et la construction `fileData`/base64 dans `handleSubmit`. RETIRER l'appel `submitPreEnrollmentForm` avec `files: fileData`.
    - Remplacer chaque slot d'upload par `<DirectUploadField>` (import Task 1), en injectant `requestUploadUrl={(kind, ext) => createPreEnrollmentUploadUrl(token, kind, ext)}`. Chaque champ confirmé remonte sa `path` via `onUploaded` → state `uploadedKeys: Partial<Record<'CNI'|'RIB'|'CFP', string>>`.
    - `handleSubmit` devient : valider champs texte + RGPD + « au moins une pièce » (basé sur `uploadedKeys` non vide), puis `confirmPreEnrollmentUpload(token, uploadedKeys, { firstName, lastName, email, phone, birthDate, birthPlace, professionalStatus, diploma, educationLevel, professionalExperience, rgpdAccepted: true })`. Sur `ok` → `setDone(true)`. Les fichiers sont DÉJÀ uploadés au choix du fichier (pas à la soumission) ; la soumission confirme + persiste les champs.
    - Conserver TOUS les champs texte, l'écran `done`, la gestion d'erreur, `startTransition`.
    - Import `createPreEnrollmentUploadUrl, confirmPreEnrollmentUpload` from `@/server/actions/storage-upload`.

    **create-person-button.tsx (admin, D-08) :**
    - Remplacer l'usage de `uploadApprenantDocs(formData)` (upload serveur FormData) par le même `<DirectUploadField>` avec `requestUploadUrl={(kind, ext) => createApprenantUploadUrl(kind, ext)}`, collecter les `path` confirmées, puis `confirmApprenantUpload(keys)` dans le flux de création apprenant. Garder la logique de création + extraction existante.
    - Si le flux admin est trop couplé, garder `uploadApprenantDocs` en place (NON supprimé, fallback non régressif) mais router le NOUVEAU chemin d'upload via DirectUploadField pour respecter D-08 (les deux chemins passent par Vercel → cap 4,5 Mo).

    Erreurs en français partout. Ne perdre aucun champ déjà saisi (D-07).

    Une fois le nouveau chemin câblé et tsc vert, si `grep` confirme 0 consommateur restant de `submitPreEnrollmentForm`, le retirer ; sinon le laisser (fallback non régressif). Documenter le choix dans le SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fileToBase64" apps/web/src/components/preinscriptions/public-form.tsx` = 0 (base64 retiré)
    - `grep "DirectUploadField" apps/web/src/components/preinscriptions/public-form.tsx` trouve l'usage du composant
    - `grep "createPreEnrollmentUploadUrl" apps/web/src/components/preinscriptions/public-form.tsx` trouve l'injection
    - `grep "confirmPreEnrollmentUpload" apps/web/src/components/preinscriptions/public-form.tsx` trouve la confirmation
    - `grep "DirectUploadField\|createApprenantUploadUrl" apps/web/src/components/forms/create-person-button.tsx` trouve le câblage admin
    - `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Formulaire public refondu (base64 retiré, DirectUploadField câblé sur createPreEnrollmentUploadUrl + confirmPreEnrollmentUpload), écran admin bascule sur le même composant (D-08), tsc clean, champs préservés.</done>
</task>

<task type="auto">
  <name>Task 3: Écrire 18-SMOKE.md (validations prod manuelles STOR-01 + STOR-03)</name>
  <files>.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md</files>
  <read_first>
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-RESEARCH.md (Validation Architecture : manuel-only justifié, Phase gate)
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md (checklist anti-défaut-US création projet Supabase eu-west-3)
  </read_first>
  <action>
    Créer `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md`. Procédures manuelles pour Laurent sur le projet Supabase réel (eu-west-3, verrouillé Phase 17). Sections :

    ## Pré-requis
    - Projet Supabase EU `eu-west-3` créé (checklist anti-défaut-US 17-REGIONS.md), clés `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` renseignées dans `.env`.

    ## STOR-01 — Bucket privé + signed URL (accès non-signé refusé)
    1. `STORAGE_PROVIDER=supabase`, uploader un objet test via l'app.
    2. Ouvrir l'URL PUBLIQUE brute (`{SUPABASE_URL}/storage/v1/object/public/qualiof-docs/{key}`) → doit renvoyer 400/403 (bucket public=false). ✅ accès non-signé REFUSÉ.
    3. Cliquer le doc dans l'app → redirect 302 vers signed URL fraîche → le fichier s'affiche. ✅ signed URL donne accès.
    4. Attendre 11 min, recopier l'ancienne signed URL brute → doit expirer (TTL 600s). ✅

    ## STOR-02 — Migration 0 lien mort (DRY→WRITE gaté)
    1. MinIO en marche. `pnpm --filter @qualiof/web storage:migrate` (DRY) → lire le rapport `.planning/audit/STORAGE-MIGRATION-REPORT-*.md` : total par bucket, clés invalides (vide/assumé), orphelins listés.
    2. Feu vert Laurent → `WRITE=1 pnpm --filter @qualiof/web storage:migrate` → vérifier `deadLinks` VIDE. ✅ 0 lien mort.
    3. Basculer `STORAGE_PROVIDER=supabase` SEULEMENT après deadLinks vide. MinIO conservé (D-02, suppression = étape séparée hors phase).

    ## STOR-03 — Photo 10 Mo prod → OCR (critère #3 NON négociable)
    1. Sur mobile, ouvrir le formulaire public `/p/[token]` en PROD Vercel.
    2. Prendre une VRAIE photo CNI ~10 Mo, la déposer. Observer la barre de progression réelle.
    3. Vérifier : PAS de 413, upload réussit (fichier DIRECT chez Supabase, pas via Vercel).
    4. Soumettre → vérifier `PreEnrollment.status` SUBMITTED → EXTRACTED (OCR déclenché). ✅
    5. Retry : couper le réseau en plein upload → 1 retry auto silencieux → si échec, bouton « Réessayer » + champs préservés. ✅

    ## Phase gate
    - Suite web complète verte + les 3 sections SMOKE validées par Laurent avant `/gsd:verify-work 18`.
  </action>
  <verify>
    <automated>test -f ".planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md" && grep -q "10 Mo" ".planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md` existe
    - `grep -c "STOR-01\|STOR-02\|STOR-03" 18-SMOKE.md` couvre les 3 requirements
    - `grep "public=false\|non-signé\|400/403" 18-SMOKE.md` trouve la vérif accès refusé (STOR-01)
    - `grep "10 Mo" 18-SMOKE.md` trouve le critère #3
    - `grep "deadLinks" 18-SMOKE.md` trouve la vérif 0 lien mort (STOR-02)
  </acceptance_criteria>
  <done>18-SMOKE.md écrit couvrant STOR-01 (accès non-signé refusé + signed URL), STOR-02 (DRY→WRITE gaté, deadLinks vide), STOR-03 (photo 10 Mo prod → OCR, retry).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Validation prod Supabase (STOR-01 bucket privé + STOR-03 photo 10 Mo)</name>
  <action>PAUSE — validation humaine. Laurent exécute la procédure 18-SMOKE.md sur le projet Supabase eu-west-3 réel + prod Vercel (voir how-to-verify). Aucune action Claude : les critères #1 et #3 exigent l'infra prod, non reproductibles en test hermétique.</action>
  <what-built>
    Le chemin direct-to-storage complet (composant client XHR direct + server actions signed URL + confirmation OCR + serving redirect 302) et le script de migration. La validation des critères de succès #1 (bucket privé) et #3 (photo 10 Mo prod) exige un projet Supabase réel + prod Vercel — non reproductible en test hermétique.
  </what-built>
  <how-to-verify>
    Suivre `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md` :
    1. STOR-01 : URL publique brute du bucket → 400/403 (accès non-signé refusé) ; clic doc dans l'app → 302 signed URL → fichier s'affiche.
    2. STOR-02 : `pnpm --filter @qualiof/web storage:migrate` (DRY), lire le rapport, puis `WRITE=1 …` après feu vert → `deadLinks` VIDE. Basculer `STORAGE_PROVIDER=supabase`.
    3. STOR-03 (NON négociable) : sur mobile en PROD, photo CNI ~10 Mo sur `/p/[token]` → progression réelle → PAS de 413 → soumission → `PreEnrollment.status` passe EXTRACTED (OCR déclenché). Tester le retry (coupure réseau).
    Consigner les résultats dans 18-SMOKE.md.
  </how-to-verify>
  <resume-signal>Tape « approuvé » si les 3 sections passent, sinon décris l'échec (413, OCR non déclenché, accès non-signé accepté, lien mort) pour un plan de correction.</resume-signal>
</task>

</tasks>

<verification>
- tsc web clean : `pnpm --filter @qualiof/web exec tsc --noEmit` → exit 0
- Suite web complète non régressée : `pnpm --filter @qualiof/web exec vitest run` → baseline 1145/1146 (+ nouveaux tests plans 01/02/03 verts ; seul échec toléré = `shared-template.test.ts:175` MIME jpeg/jpg PRÉ-EXISTANT hors scope)
- base64 retiré : `grep -c "fileToBase64" apps/web/src/components/preinscriptions/public-form.tsx` = 0
- Upload direct (aucun octet via Vercel) : le composant envoie le File via XHR PUT sur signedUrl (`grep "xhr.send(file)" apps/web/src/components/shared/direct-upload-field.tsx`)
- SMOKE.md présent avec critère 10 Mo
</verification>

<success_criteria>
- STOR-03 (client + preuve) : composant partagé upload direct (progression D-06, retry D-07, 50 Mo D-05), formulaire public + admin refondus (D-08), base64 retiré. Critères de succès #1 (bucket privé/signed URL) et #3 (photo 10 Mo prod → OCR, pas de 413) validés par le checkpoint humain via 18-SMOKE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-04-SUMMARY.md`
</output>
