---
phase: 02-responsive-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
  - CLAUDE.md
autonomous: true
requirements: [RESP-01]
user_setup: []

must_haves:
  truths:
    - "Les breakpoints Tailwind par défaut (sm/md/lg/xl/2xl) sont opérationnels dans QualiOF"
    - "L'analyse erronée précédente (CONCERNS.md sections #4 et #5) est corrigée"
    - "CLAUDE.md > Patterns to fix ne mentionne plus une fausse 'screens override'"
  artifacts:
    - .planning/codebase/CONCERNS.md (mis à jour)
    - CLAUDE.md (mis à jour)
  key_links:
    - "Comprend ce que RESP-02 et RESP-03 doivent faire vraiment (= ajouter responsive variants aux composants, pas modifier tailwind.config.ts)"
---

<objective>
Auditer `apps/web/tailwind.config.ts` pour confirmer que les breakpoints par défaut fonctionnent (le `screens` problématique est dans `theme.container.screens`, scope limité), et corriger l'analyse erronée laissée dans `CONCERNS.md` et `CLAUDE.md`.

Purpose: Phase 1 (et l'audit) supposait que les breakpoints étaient supprimés. Vérification du fichier montre que c'est faux. Cette correction documentaire évite à Phase 2/3 (et tout futur dev) de partir sur une fausse piste.
Output: Test runtime du breakpoint OK + 2 docs corrigées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/tailwind.config.ts
@.planning/codebase/CONCERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Breakpoint runtime test</name>
  <files>(temporaire — à retirer après test)</files>
  <read_first>
    - apps/web/tailwind.config.ts (confirmer `screens` est bien dans `theme.container.screens`, pas `theme.screens`)
  </read_first>
  <action>
    1. Identifier une page rendue publique ou simple (ex: `/login` ou un wrapper layout).
    2. Ajouter temporairement un `<div>` test :
       ```tsx
       <div className="bg-red-500 sm:bg-green-500 md:bg-blue-500 lg:bg-yellow-500 xl:bg-purple-500 p-4 text-white">
         BREAKPOINT TEST (delete me)
       </div>
       ```
    3. Lancer dev server (`pnpm --filter @qualiof/web dev`).
    4. Ouvrir browser, redimensionner de 320 px à 1600 px.
    5. **Verdict :** couleur change à 640 / 768 / 1024 / 1280 ? Si OUI → breakpoints OK, pas de modif tailwind.config.ts. Si NON → tailwind.config.ts à corriger (déplacer `screens` vers `theme.extend.screens`).
    6. Retirer le div test (commit séparé optionnel).
  </action>
  <verify>
    <automated>MISSING — manual viewport color shift verification</automated>
  </verify>
  <acceptance_criteria>
    - Verdict documenté dans SUMMARY.md : "breakpoints OK" ou "breakpoints KO + fix appliqué"
    - Si breakpoints KO : `apps/web/tailwind.config.ts` modifié pour mettre `screens` dans `theme.extend.screens` (sinon, fichier intact)
    - Div test retiré avant fin de task
  </acceptance_criteria>
  <done>Statut des breakpoints connu et figé</done>
</task>

<task type="auto">
  <name>Task 1.2: Corriger CONCERNS.md sections #4 et #5</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (sections "### 4." et "### 5.")
  </read_first>
  <action>
    Remplacer le diagnostic erroné des sections #4 et #5 de `CONCERNS.md` (qui affirme que `theme.screens` override les défauts) par le diagnostic corrigé :
    
    Section #4 (audit "No responsive") — remplacer le passage "Root cause identified" par :
    ```
    **Root cause corrigée (2026-05-12 Phase 2 audit) :**
    
    `apps/web/tailwind.config.ts` a `screens: { '2xl': '1400px' }` dans `theme.container.screens` — scope limité à la classe `container` (sa max-width). **Les breakpoints par défaut Tailwind (sm/md/lg/xl/2xl) sont intacts.**
    
    La vraie cause du non-responsive : les composants `Sidebar` (`apps/web/src/components/layout/sidebar.tsx`) et `MainContent` (`apps/web/src/components/layout/main-content.tsx`) utilisent des classes fixes (`w-64`, `ml-64`, `fixed`) sans aucun variant responsive (`hidden md:block`, `ml-0 md:ml-64`).
    
    **Fix Phase 2 :** Ajouter les variants responsive aux composants, pas de modif `tailwind.config.ts`.
    ```
    
    Section #5 (audit "Layout tronqué 1456px") — supprimer ou commenter le calcul lié à `max-w-screen-2xl = 1400px` (faux : avec breakpoints défauts, `screen-2xl = 1536px`). Remplacer par :
    ```
    **Root cause (à investiguer Phase 3) :** Les grilles internes (dashboard KPI, pipeline, financeurs) utilisent probablement `grid-cols-N` fixes sans variants responsive (`sm:grid-cols-2`, `lg:grid-cols-4`). Phase 3 (RESP-04) audit chaque grille.
    ```
  </action>
  <verify>
    <automated>grep -q "Root cause corrigée (2026-05-12 Phase 2 audit)" .planning/codebase/CONCERNS.md && ! grep -q "supprime sm/md/lg/xl par défaut" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - CONCERNS.md contient la chaîne "Root cause corrigée"
    - CONCERNS.md ne contient plus l'affirmation "supprime sm/md/lg/xl par défaut" (suppression de l'affirmation fausse)
    - Section #5 réfère à Phase 3 pour l'investigation des grilles
  </acceptance_criteria>
  <done>Codebase map à jour avec le bon diagnostic</done>
</task>

<task type="auto">
  <name>Task 1.3: Corriger CLAUDE.md > Patterns to fix</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (section "## Patterns to fix (audit-flagged)")
  </read_first>
  <action>
    Dans `CLAUDE.md`, section "## Patterns to fix (audit-flagged)", remplacer la ligne :
    ```
    - ❌ Tailwind `screens: { '2xl': '1400px' }` overrides defaults — kills responsive utilities. **Move to `theme.extend.screens` or restore defaults.**
    ```
    Par :
    ```
    - ⚠️ Tailwind `screens: { '2xl': '1400px' }` est dans `theme.container.screens` (scope limité au utility `container`). **Les breakpoints par défaut fonctionnent — vérifié Phase 2.** Le vrai chantier responsive est dans les composants (Sidebar, MainContent, grilles).
    ```
  </action>
  <verify>
    <automated>grep -q "scope limité au utility container" CLAUDE.md && ! grep -q "overrides defaults — kills responsive utilities" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contient "scope limité au utility container"
    - CLAUDE.md ne contient plus "overrides defaults — kills responsive utilities"
  </acceptance_criteria>
  <done>Convention projet à jour</done>
</task>

</tasks>

<!-- NOTE : la mise à jour REQUIREMENTS.md RESP-01 est extraite dans Plan 02-04 (wave 2). -->

<verification>
- ✅ Test runtime breakpoints documenté (verdict OK ou KO+fix)
- ✅ CONCERNS.md sections #4 et #5 corrigées (verifiable via grep)
- ✅ CLAUDE.md > Patterns to fix corrigé (verifiable via grep)
</verification>

<success_criteria>
- RESP-01 fermé : statut des breakpoints connu, docs corrigées
- Aucune modification du code app, juste docs + un test temporaire retiré
</success_criteria>

<output>
After completion, create `.planning/phases/02-responsive-foundation/02-01-SUMMARY.md` avec :
- Verdict breakpoints (OK / KO + fix)
- Lignes diff CONCERNS.md / CLAUDE.md
</output>
