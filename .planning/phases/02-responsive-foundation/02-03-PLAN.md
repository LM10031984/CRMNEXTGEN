---
phase: 02-responsive-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/layout/main-content.tsx
autonomous: true
requirements: [RESP-03]
user_setup: []

must_haves:
  truths:
    - "Sur viewport < 768px, <MainContent> a margin-left: 0 (toute la largeur disponible)"
    - "Sur viewport >= 768px, <MainContent> a margin-left: 256px (ou 64px si collapsed) comme avant"
  artifacts:
    - apps/web/src/components/layout/main-content.tsx (1 ligne modifiée)
  key_links:
    - "Synergie avec Plan 02-02 : la sidebar est cachée < md, MainContent ne décale plus → contenu pleine largeur"
---

<objective>
Adapter `MainContent` pour qu'il n'applique sa margin-left de 256px / 64px qu'à partir du breakpoint `md` (768px). En dessous, margin = 0 pour que le contenu prenne toute la largeur (la sidebar étant cachée).

Purpose: Sans ce fix, même avec la sidebar cachée en mobile (Plan 02-02), le contenu serait décalé de 256px à droite et coupé.
Output: 1 ligne modifiée dans `apps/web/src/components/layout/main-content.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/components/layout/main-content.tsx
@.planning/phases/02-responsive-foundation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Adapter className MainContent — ml-0 md:ml-…</name>
  <files>apps/web/src/components/layout/main-content.tsx</files>
  <read_first>
    - apps/web/src/components/layout/main-content.tsx (full — vérifier état post-Phase 1)
  </read_first>
  <action>
    Dans `apps/web/src/components/layout/main-content.tsx`, modifier la className du `<div>` enfant.
    
    AVANT (après Phase 1) :
    ```tsx
    <div
      className={cn(
        'flex flex-col transition-[margin-left] duration-200',
        collapsed ? 'ml-[64px]' : 'ml-64',
      )}
    >
    ```
    
    APRÈS :
    ```tsx
    <div
      className={cn(
        'flex flex-col transition-[margin-left] duration-200',
        collapsed ? 'ml-0 md:ml-[64px]' : 'ml-0 md:ml-64',
      )}
    >
    ```
    
    Mettre également à jour le commentaire JSX qui précédait (`// min-h-screen volontairement absent : ...`) pour ajouter une ligne :
    ```
    // ml-0 md:ml-… : la sidebar est cachée < md (Phase 2 RESP-02),
    // donc on neutralise la margin en mobile pour reprendre toute la largeur.
    ```
  </action>
  <verify>
    <automated>grep -q "ml-0 md:ml-64" apps/web/src/components/layout/main-content.tsx && grep -q "ml-0 md:ml-\\[64px\\]" apps/web/src/components/layout/main-content.tsx && ! grep -E "'\\s*ml-64\\s*'" apps/web/src/components/layout/main-content.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `main-content.tsx` contient `'ml-0 md:ml-64'`
    - `main-content.tsx` contient `'ml-0 md:ml-[64px]'`
    - Aucune autre modification (vérifié `git diff` minimal — 1 ligne effective)
    - Commentaire ajouté pour expliquer le `ml-0 md:ml-…`
  </acceptance_criteria>
  <done>MainContent responsive</done>
</task>

</tasks>

<!-- NOTE : la mise à jour REQUIREMENTS.md RESP-03 est extraite dans Plan 02-04 (wave 2). -->

<verification>
- ✅ Grep checks (positifs et négatif)
- ⏳ Manual viewport (Laurent) : DevTools 390px → MainContent computed margin-left: 0px ; DevTools 1024px → margin-left: 256px (ou 64 si collapsed)
</verification>

<success_criteria>
- RESP-03 fermé : 1 ligne de code, comportement correct sur mobile et desktop
- Pas de régression sticky header (la transition `[margin-left]` ne casse pas sticky)
</success_criteria>

<output>
After completion, create `.planning/phases/02-responsive-foundation/02-03-SUMMARY.md` avec :
- Diff main-content.tsx (1 ligne)
- Pour Plan 02-04 : annotation RESP-03 "DONE 2026-05-12 — `ml-0 md:ml-64` (et variante collapsed `ml-0 md:ml-[64px]`)"
</output>
