---
phase: 02-responsive-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/layout/nav-config.ts (NEW)
  - apps/web/src/components/layout/sidebar-nav.tsx (NEW)
  - apps/web/src/components/layout/sidebar.tsx
  - apps/web/src/components/layout/mobile-nav-drawer.tsx (NEW)
  - apps/web/src/components/layout/top-bar.tsx
autonomous: false
requirements: [RESP-02]
user_setup: []

must_haves:
  truths:
    - "Sur viewport < 768px : <aside Sidebar> absente du DOM (display:none), un bouton hamburger Menu visible dans TopBar"
    - "Clic sur hamburger ouvre un Radix Dialog drawer left-side avec le même menu de navigation"
    - "Clic sur un item du drawer ferme le drawer + navigate vers la page"
    - "Sur viewport >= 768px : sidebar visible comme avant, pas de hamburger"
    - "Pas de duplication de la config NAV (un seul array `NAV` source de vérité)"
  artifacts:
    - apps/web/src/components/layout/nav-config.ts (NEW — NAV config + types)
    - apps/web/src/components/layout/sidebar-nav.tsx (NEW — composant rendu nav réutilisable)
    - apps/web/src/components/layout/sidebar.tsx (refactor utilise SidebarNav + hidden md:flex)
    - apps/web/src/components/layout/mobile-nav-drawer.tsx (NEW — drawer Radix Dialog)
    - apps/web/src/components/layout/top-bar.tsx (ajout bouton hamburger md:hidden)
  key_links:
    - "MobileNavDrawer importe SidebarNav (déduplication)"
    - "TopBar contrôle l'état open du drawer via useState local"
---

<objective>
Rendre la sidebar QualiOF responsive : visible >= 768px (comportement actuel), remplacée par un drawer mobile accessible via bouton hamburger en < 768px. Refactor préalable : extraire la config NAV et le rendu en composant partagé pour éviter toute duplication.

Purpose: Rendre l'app utilisable sur mobile/tablette (qui sont actuellement bloqués par la sidebar pleine largeur). Pattern Radix Dialog déjà éprouvé dans le repo (`delete-session-button.tsx`).
Output: 3 nouveaux fichiers + 2 fichiers modifiés. Comportement responsive complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-responsive-foundation/02-CONTEXT.md
@.planning/phases/02-responsive-foundation/02-RESEARCH.md
@apps/web/src/components/layout/sidebar.tsx
@apps/web/src/components/layout/top-bar.tsx
@apps/web/src/components/sessions/delete-session-button.tsx
@apps/web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Extraire NAV config dans nav-config.ts</name>
  <files>apps/web/src/components/layout/nav-config.ts (NEW)</files>
  <read_first>
    - apps/web/src/components/layout/sidebar.tsx (lignes 25-80 — interface NavItem, NavSection, const NAV)
  </read_first>
  <action>
    Créer le fichier `apps/web/src/components/layout/nav-config.ts` avec EXACTEMENT le contenu suivant (déplacé depuis sidebar.tsx) :
    
    ```ts
    import {
      Users,
      Building2,
      GraduationCap,
      BookOpen,
      FileText,
      Calendar,
      Receipt,
      Megaphone,
      Settings,
      LayoutDashboard,
      ListChecks,
      Landmark,
      ClipboardCheck,
      Inbox,
      Wallet,
    } from 'lucide-react';
    
    export interface NavItem {
      label: string;
      href: string;
      icon: React.ComponentType<{ className?: string }>;
    }
    
    export interface NavSection {
      title?: string;
      /** Identifiant pour persister l'état déplié/replié (localStorage). */
      id?: string;
      /** Section repliable — démarre repliée par défaut. */
      collapsible?: boolean;
      items: NavItem[];
    }
    
    // Hiérarchie simplifiée 2026-05 : 3 sections claires, focus sur le workflow
    // quotidien (Sessions + Apprenants + Pré-inscriptions). La configuration
    // (référentiels, paramètres, etc.) est repliable pour réduire le bruit visuel.
    export const NAV: NavSection[] = [
      {
        title: 'Essentiel',
        items: [
          { label: 'Tableau de bord', href: '/app', icon: LayoutDashboard },
          { label: 'Sessions', href: '/app/sessions', icon: Calendar },
          { label: 'Apprenants', href: '/app/apprenants', icon: Users },
          { label: 'Pré-inscriptions', href: '/app/preinscriptions', icon: Inbox },
        ],
      },
      {
        title: 'Suivi',
        items: [
          { label: 'Dossiers OPCO', href: '/app/dossiers-opco', icon: ClipboardCheck },
          { label: 'Factures', href: '/app/factures', icon: Receipt },
          { label: 'Budget AGEFICE', href: '/app/budget-agefice', icon: Wallet },
          { label: 'Leads', href: '/app/leads', icon: Megaphone },
        ],
      },
      {
        title: 'Configuration',
        id: 'config',
        collapsible: true,
        items: [
          { label: 'Organisations', href: '/app/organisations', icon: Building2 },
          { label: 'Formateurs', href: '/app/formateurs', icon: GraduationCap },
          { label: 'Produits de formation', href: '/app/produits', icon: BookOpen },
          { label: 'Modèles de documents', href: '/app/templates', icon: FileText },
          { label: 'Financeurs', href: '/app/financeurs', icon: Landmark },
          { label: 'Inscriptions', href: '/app/inscriptions', icon: ListChecks },
          { label: 'Paramètres', href: '/app/parametres', icon: Settings },
        ],
      },
    ];
    
    export const SECTION_STORAGE_PREFIX = 'qualiof-sidebar-section-';
    ```
    
    Important : conserver l'icône `ChevronDown` dans `sidebar.tsx` (utilisée par le toggle de section, pas dans la config NAV).
  </action>
  <verify>
    <automated>test -f apps/web/src/components/layout/nav-config.ts && grep -q "export const NAV" apps/web/src/components/layout/nav-config.ts && grep -q "export interface NavSection" apps/web/src/components/layout/nav-config.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `apps/web/src/components/layout/nav-config.ts` créé
    - Contient `export const NAV` avec 3 sections
    - Contient `export interface NavItem` et `export interface NavSection`
    - Contient `export const SECTION_STORAGE_PREFIX`
    - Imports lucide-react inclus
  </acceptance_criteria>
  <done>NAV config extraite, prête à être consommée par Sidebar + MobileNavDrawer</done>
</task>

<task type="auto">
  <name>Task 2.2: Créer composant SidebarNav (rendu réutilisable)</name>
  <files>apps/web/src/components/layout/sidebar-nav.tsx (NEW)</files>
  <read_first>
    - apps/web/src/components/layout/sidebar.tsx (lignes 138-230 — JSX du <nav> + map sections)
    - apps/web/src/components/layout/nav-config.ts (créé en Task 2.1)
  </read_first>
  <action>
    Créer `apps/web/src/components/layout/sidebar-nav.tsx` avec un composant `SidebarNav` qui rend la liste des sections de NAV. Signature :
    
    ```tsx
    'use client';
    
    import { useState, useEffect } from 'react';
    import Link from 'next/link';
    import type { Route } from 'next';
    import { usePathname } from 'next/navigation';
    import { ChevronDown } from 'lucide-react';
    import { cn } from '@/lib/utils';
    import { NAV, SECTION_STORAGE_PREFIX, type NavSection } from './nav-config';
    
    interface SidebarNavProps {
      /** True = mode sidebar repliée (icônes seules). False (mobile drawer) = toujours full. */
      collapsed: boolean;
      /** Callback optionnel appelé après clic sur un item (utilisé par mobile drawer pour fermer). */
      onNavigate?: () => void;
    }
    
    export function SidebarNav({ collapsed, onNavigate }: SidebarNavProps) {
      const pathname = usePathname();
      const [sectionOpen, setSectionOpen] = useState<Record<string, boolean>>({});
    
      useEffect(() => {
        try {
          const next: Record<string, boolean> = {};
          for (const s of NAV) {
            if (s.collapsible && s.id) {
              const hasActive = s.items.some(
                (item) => pathname === item.href || pathname.startsWith(item.href + '/'),
              );
              const stored = localStorage.getItem(SECTION_STORAGE_PREFIX + s.id);
              next[s.id] = hasActive ? true : stored === '1';
            }
          }
          setSectionOpen(next);
        } catch {
          // ignore (Safari incognito etc.)
        }
      }, [pathname]);
    
      const toggleSection = (id: string) => {
        setSectionOpen((prev) => {
          const next = { ...prev, [id]: !prev[id] };
          try {
            localStorage.setItem(SECTION_STORAGE_PREFIX + id, next[id] ? '1' : '0');
          } catch {
            // ignore
          }
          return next;
        });
      };
    
      return (
        <nav className="h-full overflow-y-auto py-4 pb-10">
          {NAV.map((section, sIdx) => {
            const isCollapsibleSection = section.collapsible && section.id;
            const sectionExpanded = isCollapsibleSection
              ? sectionOpen[section.id!] !== false
              : true;
            const showItems = !isCollapsibleSection || sectionExpanded;
            return (
              <div key={sIdx} className="mb-6">
                {section.title && !collapsed && (
                  isCollapsibleSection ? (
                    <button
                      type="button"
                      onClick={() => toggleSection(section.id!)}
                      className="w-full px-6 mb-1.5 flex items-center justify-between text-[10px] uppercase tracking-wider font-semibold text-muted-foreground hover:text-foreground transition-colors"
                    >
                      <span>{section.title}</span>
                      <ChevronDown
                        className={cn(
                          'h-3 w-3 transition-transform',
                          sectionExpanded ? 'rotate-0' : '-rotate-90',
                        )}
                      />
                    </button>
                  ) : (
                    <div className="px-6 mb-1.5 text-[10px] uppercase tracking-wider font-semibold text-muted-foreground">
                      {section.title}
                    </div>
                  )
                )}
                {showItems && (
                  <ul className={cn('space-y-0.5', collapsed ? 'px-2' : 'px-3')}>
                    {section.items.map((item) => {
                      const active =
                        item.href === '/app'
                          ? pathname === '/app'
                          : pathname === item.href || pathname.startsWith(item.href + '/');
                      const Icon = item.icon;
                      return (
                        <li key={item.href}>
                          <Link
                            href={item.href as Route}
                            title={collapsed ? item.label : undefined}
                            onClick={onNavigate}
                            className={cn(
                              'flex items-center gap-2.5 rounded-lg text-sm transition-colors',
                              collapsed ? 'justify-center p-2' : 'px-3 py-2',
                              active
                                ? 'bg-primary-50 text-primary-700 font-medium'
                                : 'text-foreground hover:bg-muted',
                            )}
                          >
                            <Icon className="h-4 w-4 shrink-0" />
                            {!collapsed && <span className="truncate">{item.label}</span>}
                          </Link>
                        </li>
                      );
                    })}
                  </ul>
                )}
              </div>
            );
          })}
        </nav>
      );
    }
    ```
  </action>
  <verify>
    <automated>test -f apps/web/src/components/layout/sidebar-nav.tsx && grep -q "export function SidebarNav" apps/web/src/components/layout/sidebar-nav.tsx && grep -q "onNavigate" apps/web/src/components/layout/sidebar-nav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `apps/web/src/components/layout/sidebar-nav.tsx` créé
    - Export `SidebarNav` avec props `{ collapsed, onNavigate? }`
    - Importe `NAV` depuis `./nav-config`
    - Gère sections repliables avec localStorage (logique préservée depuis sidebar.tsx)
    - Appelle `onNavigate?.()` au clic sur un Link
  </acceptance_criteria>
  <done>Composant SidebarNav prêt</done>
</task>

<task type="auto">
  <name>Task 2.3: Refactor Sidebar pour utiliser SidebarNav + hidden md:flex</name>
  <files>apps/web/src/components/layout/sidebar.tsx</files>
  <read_first>
    - apps/web/src/components/layout/sidebar.tsx (full)
    - apps/web/src/components/layout/sidebar-nav.tsx (post-Task 2.2)
    - apps/web/src/components/layout/nav-config.ts (post-Task 2.1)
  </read_first>
  <action>
    Réécrire `apps/web/src/components/layout/sidebar.tsx` en supprimant :
    - Les imports lucide redondants (déplacés vers nav-config.ts)
    - L'interface `NavItem`, `NavSection`, le `const NAV` (déplacés vers nav-config.ts)
    - La constante `SECTION_STORAGE_PREFIX` (déplacée vers nav-config.ts)
    - Toute la logique `sectionOpen` / `toggleSection` (déplacée dans SidebarNav)
    - Le bloc JSX `{NAV.map((section, sIdx) => ...)}` (remplacé par `<SidebarNav collapsed={collapsed} />`)
    
    Conserver :
    - L'icône `ChevronLeft`, `ChevronRight` (toggle collapse), le logo en haut
    - La logique `collapsed` + `toggle()` + persistance localStorage `qualiof-sidebar-collapsed`
    - `ActiveBatchesBadge`
    - Le footer avec v0.1.0 + bouton toggle
    
    **Critique :** Ajouter `hidden md:flex` au début de la className `<aside>` pour cacher la sidebar en mobile.
    
    Structure cible :
    ```tsx
    'use client';
    
    import { useEffect, useState } from 'react';
    import Link from 'next/link';
    import { ChevronLeft, ChevronRight } from 'lucide-react';
    import { cn } from '@/lib/utils';
    import { ActiveBatchesBadge } from './active-batches-badge';
    import { SidebarNav } from './sidebar-nav';
    
    const STORAGE_KEY = 'qualiof-sidebar-collapsed';
    
    export function Sidebar() {
      const [collapsed, setCollapsed] = useState(false);
    
      useEffect(() => {
        try {
          setCollapsed(localStorage.getItem(STORAGE_KEY) === '1');
        } catch {
          // ignore
        }
      }, []);
    
      const toggle = () => {
        setCollapsed((c) => {
          const next = !c;
          try {
            localStorage.setItem(STORAGE_KEY, next ? '1' : '0');
          } catch {
            // ignore
          }
          return next;
        });
      };
    
      return (
        <aside
          className={cn(
            'hidden md:flex shrink-0 border-r border-border bg-white h-screen fixed top-0 left-0 z-30 flex-col transition-[width] duration-200',
            collapsed ? 'w-[64px]' : 'w-64',
          )}
        >
          <div
            className={cn(
              'border-b border-border flex items-center',
              collapsed ? 'px-3 py-5 justify-center' : 'px-6 py-5',
            )}
          >
            <Link href="/app" className="flex items-center gap-3">
              <div className="h-9 w-9 rounded-lg bg-primary text-white font-bold inline-flex items-center justify-center shrink-0">
                Q
              </div>
              {!collapsed && (
                <div>
                  <div className="font-semibold text-sm leading-tight">QualiOF</div>
                  <div className="text-[11px] text-muted-foreground">Start Academy</div>
                </div>
              )}
            </Link>
          </div>
    
          <div className="flex-1 relative overflow-hidden">
            <SidebarNav collapsed={collapsed} />
            <div className="pointer-events-none absolute bottom-0 left-0 right-0 h-8 bg-gradient-to-t from-white to-transparent" />
          </div>
    
          <ActiveBatchesBadge collapsed={collapsed} />
    
          <div
            className={cn(
              'border-t border-border flex items-center',
              collapsed ? 'p-2 justify-center' : 'px-6 py-3 justify-between',
            )}
          >
            {!collapsed && (
              <span className="text-[11px] text-muted-foreground">v0.1.0</span>
            )}
            <button
              type="button"
              onClick={toggle}
              title={collapsed ? 'Déplier la sidebar' : 'Replier la sidebar'}
              className="p-1.5 rounded-md hover:bg-muted text-muted-foreground"
            >
              {collapsed ? <ChevronRight className="h-4 w-4" /> : <ChevronLeft className="h-4 w-4" />}
            </button>
          </div>
        </aside>
      );
    }
    ```
    
    Le fichier passe de 258 lignes à ~75 lignes.
  </action>
  <verify>
    <automated>grep -q "hidden md:flex" apps/web/src/components/layout/sidebar.tsx && grep -q "import { SidebarNav }" apps/web/src/components/layout/sidebar.tsx && ! grep -q "const NAV" apps/web/src/components/layout/sidebar.tsx && test "$(wc -l < apps/web/src/components/layout/sidebar.tsx)" -lt 100</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/layout/sidebar.tsx` contient `hidden md:flex`
    - Importe `SidebarNav` depuis `./sidebar-nav`
    - Plus de `const NAV` (déplacé vers nav-config.ts)
    - Fichier réduit à < 100 lignes
    - Comportement collapsed/toggle préservé
  </acceptance_criteria>
  <done>Sidebar desktop visible >= md, refactor utilise SidebarNav</done>
</task>

<task type="auto">
  <name>Task 2.4: Créer MobileNavDrawer (Radix Dialog)</name>
  <files>apps/web/src/components/layout/mobile-nav-drawer.tsx (NEW)</files>
  <read_first>
    - apps/web/src/components/sessions/delete-session-button.tsx (pattern Radix Dialog)
    - apps/web/src/components/layout/sidebar-nav.tsx (post-Task 2.2)
  </read_first>
  <action>
    Créer `apps/web/src/components/layout/mobile-nav-drawer.tsx` :

    ```tsx
    'use client';

    import { useEffect } from 'react';
    import Link from 'next/link';
    import * as Dialog from '@radix-ui/react-dialog';
    import { X } from 'lucide-react';
    import { usePathname } from 'next/navigation';
    import { SidebarNav } from './sidebar-nav';

    interface MobileNavDrawerProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
    }

    export function MobileNavDrawer({ open, onOpenChange }: MobileNavDrawerProps) {
      const pathname = usePathname();

      // Ferme le drawer automatiquement quand l'utilisateur navigue (clic sur item).
      // Sécurité : si pathname change pour une raison externe (Cmd+K), on ferme aussi.
      useEffect(() => {
        if (open) onOpenChange(false);
        // eslint-disable-next-line react-hooks/exhaustive-deps
      }, [pathname]);

      return (
        <Dialog.Root open={open} onOpenChange={onOpenChange}>
          <Dialog.Portal>
            <Dialog.Overlay className="fixed inset-0 bg-foreground/40 backdrop-blur-sm z-40 data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=closed]:animate-out data-[state=closed]:fade-out-0" />
            <Dialog.Content
              className="fixed inset-y-0 left-0 z-50 flex flex-col w-72 max-w-[85vw] bg-white border-r border-border shadow-xl data-[state=open]:animate-in data-[state=open]:slide-in-from-left data-[state=closed]:animate-out data-[state=closed]:slide-out-to-left"
              aria-describedby={undefined}
            >
              <Dialog.Title className="sr-only">Navigation principale</Dialog.Title>

              <div className="border-b border-border flex items-center justify-between px-6 py-5">
                <Link href="/app" className="flex items-center gap-3">
                  <div className="h-9 w-9 rounded-lg bg-primary text-white font-bold inline-flex items-center justify-center shrink-0">
                    Q
                  </div>
                  <div>
                    <div className="font-semibold text-sm leading-tight">QualiOF</div>
                    <div className="text-[11px] text-muted-foreground">Start Academy</div>
                  </div>
                </Link>
                <Dialog.Close className="p-1.5 rounded-md hover:bg-muted text-muted-foreground" aria-label="Fermer le menu">
                  <X className="h-4 w-4" />
                </Dialog.Close>
              </div>

              <div className="flex-1 overflow-hidden">
                <SidebarNav collapsed={false} onNavigate={() => onOpenChange(false)} />
              </div>
            </Dialog.Content>
          </Dialog.Portal>
        </Dialog.Root>
      );
    }
    ```

    Notes :
    - `tailwindcss-animate` plugin déjà dans le repo → les classes `slide-in-from-left` / `fade-in-0` sont supportées.
    - `Dialog.Title` `sr-only` requis pour a11y (Radix avertit en console sinon).
    - `useEffect` sur pathname ferme le drawer si l'utilisateur navigue par un autre chemin (Cmd+K, etc.).
  </action>
  <verify>
    <automated>test -f apps/web/src/components/layout/mobile-nav-drawer.tsx && grep -q "export function MobileNavDrawer" apps/web/src/components/layout/mobile-nav-drawer.tsx && grep -q "@radix-ui/react-dialog" apps/web/src/components/layout/mobile-nav-drawer.tsx && grep -q "<SidebarNav" apps/web/src/components/layout/mobile-nav-drawer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `mobile-nav-drawer.tsx` créé
    - Importe `@radix-ui/react-dialog` et `SidebarNav`
    - Props `{ open, onOpenChange }`
    - Dialog.Title sr-only présent (a11y)
    - onNavigate callback ferme le drawer
    - Animations `slide-in-from-left` Tailwind présentes
  </acceptance_criteria>
  <done>Drawer mobile prêt, contrôlable depuis TopBar</done>
</task>

<task type="auto">
  <name>Task 2.5: Intégrer hamburger button dans TopBar</name>
  <files>apps/web/src/components/layout/top-bar.tsx</files>
  <read_first>
    - apps/web/src/components/layout/top-bar.tsx (full)
    - apps/web/src/components/layout/mobile-nav-drawer.tsx (post-Task 2.4)
  </read_first>
  <action>
    Modifier `apps/web/src/components/layout/top-bar.tsx` pour :
    1. Le rendre `'use client'` (si pas déjà — il n'est probablement pas client component, vérifier)
    2. Ajouter un state local `useState` pour `mobileNavOpen`
    3. Ajouter le `<MobileNavDrawer />` à la fin du composant
    4. Ajouter un bouton hamburger Lucide `<Menu />` au début du `<header>`, visible uniquement `md:hidden`

    Si TopBar est server-only actuellement (cas probable car il prend `user: User` en prop) : extraire les éléments interactifs dans un sous-composant client, OU rendre TopBar client.

    Approche recommandée : créer un wrapper client `<MobileMenuButton />` qui gère le state du drawer, et l'ajouter dans le JSX server de TopBar. Cela évite de transformer toute la TopBar en client.

    ```tsx
    // apps/web/src/components/layout/mobile-menu-button.tsx (NEW)
    'use client';
    import { useState } from 'react';
    import { Menu } from 'lucide-react';
    import { MobileNavDrawer } from './mobile-nav-drawer';

    export function MobileMenuButton() {
      const [open, setOpen] = useState(false);
      return (
        <>
          <button
            type="button"
            onClick={() => setOpen(true)}
            className="md:hidden -ml-1 p-2 rounded-md hover:bg-muted text-foreground"
            aria-label="Ouvrir le menu"
          >
            <Menu className="h-5 w-5" />
          </button>
          <MobileNavDrawer open={open} onOpenChange={setOpen} />
        </>
      );
    }
    ```

    Puis dans `top-bar.tsx`, ajouter `<MobileMenuButton />` au début du `<header>` JSX :
    ```tsx
    import { MobileMenuButton } from './mobile-menu-button';
    // ...
    <header className="h-14 border-b border-border bg-white flex items-center px-4 md:px-8 sticky top-0 z-10 gap-3">
      <MobileMenuButton />
      <div className="flex-1 max-w-md">
        <CmdkTrigger />
      </div>
      ...
    </header>
    ```

    Note : `px-8` → `px-4 md:px-8` pour économiser de l'espace horizontal en mobile.
  </action>
  <verify>
    <automated>grep -q "MobileMenuButton" apps/web/src/components/layout/top-bar.tsx && test -f apps/web/src/components/layout/mobile-menu-button.tsx && grep -q "md:hidden" apps/web/src/components/layout/mobile-menu-button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/layout/mobile-menu-button.tsx` créé
    - `top-bar.tsx` importe et rend `<MobileMenuButton />`
    - Le bouton est `md:hidden` (visible < 768px)
    - `top-bar.tsx` reste server component (interactivité dans MobileMenuButton client)
    - `px-8` → `px-4 md:px-8` pour respiration mobile
  </acceptance_criteria>
  <done>Hamburger visible en mobile, ouvre le drawer</done>
</task>

</tasks>

<!-- NOTE : la mise à jour REQUIREMENTS.md RESP-02 est extraite dans Plan 02-04 (wave 2). -->

<verification>
- ✅ 4 fichiers nouveaux + 2 fichiers modifiés
- ✅ Build `pnpm --filter @qualiof/web build` doit passer
- ⏳ Manual viewport test (Laurent) : 390px sidebar absente + hamburger visible + drawer ouvre/navigate
</verification>

<success_criteria>
- RESP-02 livré : sidebar cachée en mobile, hamburger ouvre drawer fonctionnel
- Refactor propre : NAV config dans 1 seul fichier, SidebarNav réutilisé sidebar desktop + drawer mobile
- Aucune régression desktop (sidebar collapse/toggle fonctionne comme avant)
</success_criteria>

<output>
After completion, create `.planning/phases/02-responsive-foundation/02-02-SUMMARY.md` avec :
- 4 fichiers créés (nav-config.ts, sidebar-nav.tsx, mobile-nav-drawer.tsx, mobile-menu-button.tsx)
- 2 fichiers modifiés (sidebar.tsx, top-bar.tsx)
- Résultat build
- Pour Plan 02-04 : annotation RESP-02 finale
</output>
