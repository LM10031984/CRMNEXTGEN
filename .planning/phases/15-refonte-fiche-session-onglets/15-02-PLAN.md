---
phase: 15-refonte-fiche-session-onglets
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/web/src/components/sessions/tabs/tab-avant.tsx
  - apps/web/src/components/sessions/tabs/tab-apres.tsx
  - apps/web/src/components/sessions/tabs/tab-tous-documents.tsx
  - apps/web/src/components/sessions/tabs/__tests__/avant-tab-actions.test.tsx
  - apps/web/src/components/sessions/tabs/__tests__/apres-session-docs.test.tsx
  - apps/web/src/components/sessions/tabs/__tests__/doc-completion-source.test.ts
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/components/sessions/qualiopi-matrix/session-only-docs-block.tsx
  - apps/web/src/components/sessions/doc-dock-drawer.tsx
  - apps/web/src/components/sessions/docs-button.tsx
autonomous: true
requirements: [FS-ONGLETS-REEMBARQUE, FS-ONGLETS-DEDUP]
must_haves:
  truths:
    - "L'onglet « Avant la formation » a un bouton « Tout générer » qui appelle dispatchGenerateMissing avec les docs pré-formation manquants."
    - "L'onglet « Avant » a une ligne par doc/stagiaire (CONVENTION, CONVOCATION, AGEFICE, ANALYSE_BESOIN, ASSIDUITE_AGEFICE) qui appelle dispatchGenerateDoc avec le bon docType."
    - "L'onglet « Après » réembarque ClosureFormationBlock + le suivi batch + les 4 boutons unitaires des docs niveau session (Déroulé, Grille obs session, Checklist, Bilan satisfaction session)."
    - "L'onglet « Tous les documents » affiche ParticipantDocMatrix plein écran + ZIP, en LECTURE SEULE (pas de génération)."
    - "Le compteur « manquants » de l'onglet Après dérive de la MÊME source d'état (getSessionCompleteness/docCompletion) que la matrice — pas de désync."
    - "DocDockDrawer, DocsButton et SessionOnlyDocsBlock ne sont plus consommés nulle part dans le code (UI supprimée), mais les server actions dispatchGenerateDoc/dispatchGenerateMissing restent intactes (moteur)."
  artifacts:
    - path: "apps/web/src/components/sessions/tabs/tab-avant.tsx"
      provides: "Onglet Avant : Tout générer + lignes par doc/stagiaire via dispatchGenerate*"
      contains: "dispatchGenerateMissing"
    - path: "apps/web/src/components/sessions/tabs/tab-apres.tsx"
      provides: "Onglet Après : pack + suivi batch + 4 docs niveau session"
      contains: "generateGrilleObsSessionForSession"
    - path: "apps/web/src/components/sessions/tabs/tab-tous-documents.tsx"
      provides: "Onglet Tous les documents : ParticipantDocMatrix plein écran + ZIP, lecture seule"
      contains: "ParticipantDocMatrix"
  key_links:
    - from: "apps/web/src/components/sessions/tabs/tab-avant.tsx"
      to: "apps/web/src/server/actions/dispatch-generate-doc.ts"
      via: "dispatchGenerateMissing (Tout générer) + dispatchGenerateDoc (par stagiaire)"
      pattern: "dispatchGenerate"
    - from: "apps/web/src/components/sessions/tabs/tab-apres.tsx"
      to: "server/actions/generate-grille-obs-session + generate-satisfaction-session + generate-checklist-formation + deroule-product-generator"
      via: "boutons unitaires des 4 docs niveau session"
      pattern: "generate(GrilleObsSession|SatisfactionSession|Checklist|Deroule)"
    - from: "apps/web/src/components/sessions/tabs/tab-apres.tsx"
      to: "apps/web/src/lib/sessions/doc-completion.ts"
      via: "docCompletion(items) — même source que la matrice"
      pattern: "docCompletion"
---

<objective>
Lot 2 — Réembarquer le contenu par onglet + SUPPRIMER les surfaces redondantes. C'est le lot le plus critique : la pré-condition LOCKED est de RÉEMBARQUER les actions uniques AVANT de supprimer leurs surfaces. `dispatchGenerateDoc`/`dispatchGenerateMissing` n'ont AUCUN autre consommateur que `DocDockDrawer` (vérifié RESEARCH Q2) ; les 4 docs niveau session vivent dans `SessionOnlyDocsBlock` (à supprimer). On extrait des composants d'onglet propres (`tab-avant`, `tab-apres`, `tab-tous-documents`), on y recâble ces actions (une ligne par doc, lisible), PUIS on supprime l'UI redondante (drawer + bouton + bloc 4 cartes). On garde les server actions (moteur), on supprime l'UI seulement.

Purpose: « 1 doc = 1 maison » + lisibilité. Chaque doc vit dans l'onglet de son moment ; « Tous les documents » montre (lecture seule), les onglets de phase agissent.
Output: 3 composants d'onglet + 3 tests + page.tsx câblé sur les nouveaux onglets + suppression des 3 surfaces redondantes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-refonte-fiche-session-onglets/15-CONTEXT.md
@.planning/phases/15-refonte-fiche-session-onglets/15-RESEARCH.md
@files/CLAUDE.md

<interfaces>
From apps/web/src/server/actions/dispatch-generate-doc.ts (MOTEUR à conserver, recâbler) :
```ts
// type DispatchableDocType inclut : CONVENTION | CONVOCATION | AGEFICE | ASSIDUITE_AGEFICE | ANALYSE_BESOIN | (+ docs session)
export async function dispatchGenerateDoc(
  input: { sessionId: string; docType: DispatchableDocType; participantId?: string; force?: boolean }
): Promise<{ ok: boolean; error?: string; docId?: string; resourceKind?: string }>;
// switch sur input.docType : CONVENTION/CONVOCATION/AGEFICE/ASSIDUITE_AGEFICE/ANALYSE_BESOIN (par stagiaire → participantId requis)
export async function dispatchGenerateMissing(input: {
  sessionId: string;
  items: Array<{ docType: DispatchableDocType; participantId?: string }>;
}): Promise<{ ok: boolean; ... }>;
```

From apps/web/src/server/actions (4 docs niveau session — find-or-create idempotent, RESEARCH Q3) :
```ts
generateDerouleForProduct(productId, { force })        // deroule-product-generator.ts
generateGrilleObsSessionForSession(sessionId, { force }) // generate-grille-obs-session.ts:31
generateChecklistForSession(sessionId, { force })       // generate-checklist-formation.ts:22
generateSatisfactionSessionForSession(sessionId)        // generate-satisfaction-session.ts:16 (PAS de force — re-génère)
```

From apps/web/src/server/actions/closure-status.ts (état pack, source agrégée — NE PAS re-query) :
```ts
// getSessionClosureStatus(sessionId) → { grilleObsSession: boolean; bilanSatisfaction: boolean; latestBatchStatus?; ... }
// L160 grilleObsSession = sessionDocs.some(d => d.type === 'GRILLE_OBS_SESSION')
// L161 bilanSatisfaction = sessionDocs.some(d => d.type === 'SATISFACTION_SESSION')
```

From apps/web/src/lib/sessions/doc-completion.ts :
```ts
// docCompletion(items) → compteur « X manquants ». SOURCE UNIQUE. Ne pas recompter par onglet.
```

From doc-dock-drawer.tsx (logique à transposer, UI à supprimer) :
```ts
// L186-213 handleGenerateAll → dispatchGenerateMissing({ sessionId, items: missing })
// L154-184 handleGenerate(item, force?) → dispatchGenerateDoc({ sessionId, docType, participantId, force })
// puis router.refresh()
```

From page.tsx :
```ts
// L50 import ParticipantDocMatrix · L51 import SessionOnlyDocsBlock
// L585 <DocsButton ...> (ouvre le drawer) · L1015 <SessionOnlyDocsBlock> · L1052 <ParticipantDocMatrix>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0) : Tests « aucune action perdue » + « non-divergence source » (RED)</name>
  <files>apps/web/src/components/sessions/tabs/__tests__/avant-tab-actions.test.tsx, apps/web/src/components/sessions/tabs/__tests__/apres-session-docs.test.tsx, apps/web/src/components/sessions/tabs/__tests__/doc-completion-source.test.ts</files>
  <read_first>
    - apps/web/src/components/sessions/doc-dock-drawer.tsx (L154-213 : handleGenerate / handleGenerateAll — la logique à préserver)
    - apps/web/src/server/actions/dispatch-generate-doc.ts (signatures, docTypes du switch)
    - apps/web/src/lib/sessions/doc-completion.ts (docCompletion — source unique)
    - apps/web/src/server/actions/closure-status.ts (getSessionClosureStatus L160-161)
    - 15-RESEARCH.md §Validation Architecture (Lot 2 : non-divergence + aucune action perdue + 4 docs session)
  </read_first>
  <action>
    Créer 3 fichiers de test (comportemental, actions mockées via `vi.mock`) :
    1. `tabs/__tests__/avant-tab-actions.test.tsx` — rendre `<TabAvant>` (mock `dispatchGenerateMissing` et `dispatchGenerateDoc`) :
       - Cliquer « Tout générer » appelle `dispatchGenerateMissing` une fois avec `{ sessionId, items: [...] }` où items = docs pré-formation manquants.
       - Pour chaque docType pré-formation (CONVENTION, CONVOCATION, AGEFICE, ANALYSE_BESOIN, ASSIDUITE_AGEFICE), cliquer son bouton « générer » appelle `dispatchGenerateDoc` avec le BON `docType` et, pour les docs par stagiaire, le bon `participantId`. Assertion explicite sur le `docType` passé (c'est la branche que le test de puissance cassera).
    2. `tabs/__tests__/apres-session-docs.test.tsx` — rendre `<TabApres>` (mock les 4 actions niveau session) :
       - Les 4 boutons (Déroulé / Grille obs session / Checklist / Bilan satisfaction session) appellent respectivement `generateDerouleForProduct`, `generateGrilleObsSessionForSession`, `generateChecklistForSession`, `generateSatisfactionSessionForSession`. Assertion sur l'action exacte appelée par bouton.
    3. `tabs/__tests__/doc-completion-source.test.ts` — test PUR : prouver que le compteur « manquants » affiché par l'onglet Après dérive de `docCompletion(items)` et BOUGE avec la source : muter un item (passer un doc de présent→manquant) doit faire varier le compteur dans le même sens que la matrice (les deux consomment `docCompletion`). Pas de re-implémentation parallèle du calcul.
    Lancer : les 3 tests doivent ÉCHOUER (composants pas écrits) — RED.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- avant-tab-actions apres-session-docs doc-completion-source</automated>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers de test existent sous `apps/web/src/components/sessions/tabs/__tests__/`.
    - `grep -q "dispatchGenerateMissing" apps/web/src/components/sessions/tabs/__tests__/avant-tab-actions.test.tsx` OK.
    - `grep -E "CONVENTION|CONVOCATION|AGEFICE|ANALYSE_BESOIN|ASSIDUITE_AGEFICE" apps/web/src/components/sessions/tabs/__tests__/avant-tab-actions.test.tsx` retourne ≥ 5 occurrences (les 5 docTypes).
    - `grep -E "generateGrilleObsSessionForSession|generateSatisfactionSessionForSession|generateChecklistForSession|generateDerouleForProduct" apps/web/src/components/sessions/tabs/__tests__/apres-session-docs.test.tsx` retourne ≥ 4 occurrences.
    - `grep -q "docCompletion" apps/web/src/components/sessions/tabs/__tests__/doc-completion-source.test.ts` OK.
    - `pnpm --filter @qualiof/web test -- avant-tab-actions apres-session-docs doc-completion-source` ÉCHOUE (RED).
  </acceptance_criteria>
  <done>3 tests écrits, rouges. Couvrent : aucune action perdue (Avant), 4 docs session (Après), non-divergence source (compteur).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Onglet Avant — réembarquer Tout générer + lignes par doc/stagiaire (GREEN)</name>
  <files>apps/web/src/components/sessions/tabs/tab-avant.tsx</files>
  <read_first>
    - apps/web/src/components/sessions/doc-dock-drawer.tsx (handleGenerate L154-184, handleGenerateAll L186-213 — logique à transposer sans le drawer)
    - apps/web/src/components/sessions/preparation-pedagogique-block.tsx (bloc Avant existant à réembarquer ; repérer les lignes docs dupliquées à retirer)
    - apps/web/src/server/actions/dispatch-generate-doc.ts (signatures exactes)
    - apps/web/src/components/sessions/editable-field.tsx (édition inline réutilisable)
    - Le test avant-tab-actions (Task 1)
  </read_first>
  <behavior>
    - « Tout générer » → un seul appel `dispatchGenerateMissing` avec les manquants.
    - Bouton par docType → `dispatchGenerateDoc` avec le bon `docType` (+ `participantId` pour les docs par stagiaire).
    - Statut par doc visible (à générer / prêt) lu depuis les props (source unique), idempotent.
  </behavior>
  <action>
    Créer `apps/web/src/components/sessions/tabs/tab-avant.tsx`. Composant client réembarquant la logique du drawer pour la phase « Avant » :
    - `PreparationPedagogiqueBlock` (réutilisé) pour le bloc préparation, MAIS les lignes docs dupliquées internes seront retirées (cf. Task 4) — ici le tab-avant rend la liste des docs pré-formation UNE ligne par doc, lisible (pas de cartes `text-[11px]`).
    - CTA « Tout générer » : `handleGenerateAll` repris de `doc-dock-drawer.tsx:186-213` → `await dispatchGenerateMissing({ sessionId, items: missing })` puis `router.refresh()`. `missing` = liste des docs pré-formation manquants dérivée des props (source unique).
    - Une ligne par doc/stagiaire pour CONVENTION, CONVOCATION, AGEFICE, ANALYSE_BESOIN, ASSIDUITE_AGEFICE : bouton « Générer » → `dispatchGenerateDoc({ sessionId, docType, participantId })` (participantId requis pour les docs par stagiaire ; ASSIDUITE_AGEFICE explicitement présent — RESEARCH note qu'il n'est couvert NI par les 4 cartes NI par SessionOnlyDocsBlock). Bouton « Régénérer » → même action avec `force: true`.
    - Statut par doc lu depuis les props passées par page.tsx (pas de recompute). `'use client'`.
    Lancer le test avant-tab-actions : GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- avant-tab-actions</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/tabs/tab-avant.tsx` existe, 1re ligne `'use client';`.
    - `grep -q "dispatchGenerateMissing" apps/web/src/components/sessions/tabs/tab-avant.tsx` OK.
    - `grep -q "dispatchGenerateDoc" apps/web/src/components/sessions/tabs/tab-avant.tsx` OK.
    - `grep -q "ASSIDUITE_AGEFICE" apps/web/src/components/sessions/tabs/tab-avant.tsx` OK (action préservée explicitement).
    - `grep -q "router.refresh" apps/web/src/components/sessions/tabs/tab-avant.tsx` OK.
    - `pnpm --filter @qualiof/web test -- avant-tab-actions` exit 0 (GREEN).
  </acceptance_criteria>
  <done>Onglet Avant câblé sur le moteur dispatchGenerate*, 5 docTypes + ASSIDUITE_AGEFICE préservé, test vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : Onglet Après + Onglet Tous les documents (GREEN)</name>
  <files>apps/web/src/components/sessions/tabs/tab-apres.tsx, apps/web/src/components/sessions/tabs/tab-tous-documents.tsx</files>
  <read_first>
    - apps/web/src/components/sessions/qualiopi-matrix/session-only-docs-block.tsx (L25-28, L115-133 : les 4 boutons unitaires des docs niveau session à réembarquer)
    - apps/web/src/components/sessions/closure-formation-block.tsx (pack à réembarquer ; retirer lignes docs dupliquées en Task 4)
    - apps/web/src/components/sessions/batch-progress-auto-refresh.tsx + closure-batch-progress.tsx (suivi batch à mettre DANS l'onglet)
    - apps/web/src/components/sessions/generate-closure-pack-button.tsx (CTA « Générer le pack »)
    - apps/web/src/components/sessions/qualiopi-matrix/participant-doc-matrix.tsx (matrice à promouvoir plein écran)
    - apps/web/src/server/actions/closure-pack.ts (ZIP « Télécharger le ZIP »)
    - apps/web/src/lib/sessions/doc-completion.ts (docCompletion — source unique compteur)
    - Les tests apres-session-docs + doc-completion-source (Task 1)
  </read_first>
  <behavior>
    - Après : les 4 docs session ont chacun un bouton câblé sur la bonne action.
    - Le compteur « manquants » de l'onglet Après = `docCompletion(items)` (même source que la matrice).
    - Tous les documents : matrice rendue, ZIP présent, AUCUNE action de génération (lecture seule).
  </behavior>
  <action>
    Créer 2 composants :
    1. `apps/web/src/components/sessions/tabs/tab-apres.tsx` (`'use client'` pour la partie boutons ; partie statique en props) :
       - Réembarque `GenerateClosurePackButton` (CTA « Générer le pack »), `ClosureFormationBlock`, et le suivi batch `BatchProgressAutoRefresh`/`ClosureBatchProgress` DANS l'onglet (plus de bandeau page-wide).
       - 4 boutons unitaires des docs niveau session (repris de `session-only-docs-block.tsx`), une ligne par doc : Déroulé → `generateDerouleForProduct(productId,{force})`, Grille obs session → `generateGrilleObsSessionForSession(sessionId,{force})`, Checklist → `generateChecklistForSession(sessionId,{force})`, Bilan satisfaction session → `generateSatisfactionSessionForSession(sessionId)` (pas de force). État présent/absent lu depuis `getSessionClosureStatus` (props : `grilleObsSession`, `bilanSatisfaction`) — pas de re-query. `router.refresh()` après chaque génération.
       - Le compteur « manquants » affiché dérive de `docCompletion(items)` (source unique, props), JAMAIS recompté.
    2. `apps/web/src/components/sessions/tabs/tab-tous-documents.tsx` (server ou client selon matrice) :
       - Rend `ParticipantDocMatrix` PLEIN ÉCRAN (hors `<details>`), ses `MatrixFilters`, et un bouton « Télécharger le ZIP » (action ZIP closure-pack existante).
       - LECTURE SEULE : aucune action de génération de doc ici (les actions vivent dans Avant/Après). La régénération par cellule de la matrice (menu existant CENTRAL-02) est conservée telle quelle si déjà présente dans `ParticipantDocMatrix` — ne PAS ajouter de nouvelle surface d'action.
    Lancer apres-session-docs + doc-completion-source : GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- apres-session-docs doc-completion-source</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/tabs/tab-apres.tsx` existe ; `grep -E "generateGrilleObsSessionForSession|generateSatisfactionSessionForSession|generateChecklistForSession|generateDerouleForProduct" apps/web/src/components/sessions/tabs/tab-apres.tsx` retourne ≥ 4.
    - `grep -q "docCompletion" apps/web/src/components/sessions/tabs/tab-apres.tsx` OK.
    - `grep -q "BatchProgressAutoRefresh\|ClosureBatchProgress" apps/web/src/components/sessions/tabs/tab-apres.tsx` OK (suivi batch dans l'onglet).
    - `apps/web/src/components/sessions/tabs/tab-tous-documents.tsx` existe ; `grep -q "ParticipantDocMatrix" apps/web/src/components/sessions/tabs/tab-tous-documents.tsx` OK.
    - `grep -E "dispatchGenerateDoc|dispatchGenerateMissing|generateGrilleObsSessionForSession" apps/web/src/components/sessions/tabs/tab-tous-documents.tsx` retourne 0 (lecture seule, aucune génération).
    - `pnpm --filter @qualiof/web test -- apres-session-docs doc-completion-source` exit 0 (GREEN).
  </acceptance_criteria>
  <done>Onglets Après (pack + suivi + 4 docs session, source unique) et Tous les documents (matrice + ZIP lecture seule) écrits, tests verts.</done>
</task>

<task type="auto">
  <name>Task 4 : Câbler les onglets dans page.tsx + SUPPRIMER les 3 surfaces redondantes</name>
  <files>apps/web/src/app/app/sessions/[id]/page.tsx, apps/web/src/components/sessions/doc-dock-drawer.tsx, apps/web/src/components/sessions/docs-button.tsx, apps/web/src/components/sessions/qualiopi-matrix/session-only-docs-block.tsx, apps/web/src/components/sessions/preparation-pedagogique-block.tsx, apps/web/src/components/sessions/closure-formation-block.tsx</files>
  <read_first>
    - apps/web/src/app/app/sessions/[id]/page.tsx (rendu Lot 1 : props session/avant/apres/docs/agenda ; L585 DocsButton, L1015 SessionOnlyDocsBlock, L1052 ParticipantDocMatrix)
    - apps/web/src/components/sessions/preparation-pedagogique-block.tsx + closure-formation-block.tsx (lignes docs dupliquées à retirer)
    - apps/web/src/lib/sessions/doc-dock-items.ts (buildDocDockItems — alimente le drawer, devient orphelin)
    - Les 3 composants d'onglet (Tasks 2-3)
  </read_first>
  <action>
    1. Remplacer dans `page.tsx` les props passées à `<SessionTabs>` :
       - `avant={<TabAvant .../>}`, `apres={<TabApres .../>}`, `docs={<TabTousDocuments .../>}` (importer les 3 composants). Passer les props dérivées de la source unique (manquants, statuts, productId, sessionId, participants).
    2. PRÉ-CONDITION DE SUPPRESSION (LOCKED) — vérifier d'abord par grep qu'aucune action n'est perdue : `dispatchGenerateMissing`/`dispatchGenerateDoc` doivent être appelés depuis `tab-avant.tsx` ; les 4 actions session depuis `tab-apres.tsx`. Une fois prouvé :
    3. SUPPRIMER l'UI redondante (garder les server actions) :
       - Retirer `<DocsButton>` (L585) du rendu de `page.tsx` et SUPPRIMER `apps/web/src/components/sessions/docs-button.tsx`.
       - SUPPRIMER `apps/web/src/components/sessions/doc-dock-drawer.tsx` (et son import).
       - SUPPRIMER `apps/web/src/components/sessions/qualiopi-matrix/session-only-docs-block.tsx` (et son import/rendu L1015) — ses 4 actions sont désormais dans tab-apres.
       - Retirer les lignes docs DUPLIQUÉES à l'intérieur de `preparation-pedagogique-block.tsx` et `closure-formation-block.tsx` (les docs vivent maintenant dans tab-avant/tab-apres). Ne PAS supprimer ces deux blocs eux-mêmes (réutilisés), seulement leurs lignes docs redondantes.
       - Si `lib/sessions/doc-dock-items.ts` (`buildDocDockItems`) devient orphelin (0 import), le supprimer aussi (discipline « orphelins stashés »).
    4. Ne PAS toucher : `apps/web/src/server/actions/dispatch-generate-doc.ts` (MOTEUR conservé), les 4 actions niveau session, `getSessionCompleteness`/`docCompletion`/`getSessionClosureStatus`.
    NE PAS supprimer un composant tant qu'un grep prouve un consommateur résiduel.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "DocDockDrawer" apps/web/src` retourne 0 ligne (composant supprimé, aucun consommateur).
    - `grep -rn "DocsButton" apps/web/src` retourne 0 ligne.
    - `grep -rn "SessionOnlyDocsBlock" apps/web/src` retourne 0 ligne.
    - `test ! -f apps/web/src/components/sessions/doc-dock-drawer.tsx` (fichier supprimé) ; idem `docs-button.tsx` et `qualiopi-matrix/session-only-docs-block.tsx`.
    - `test -f apps/web/src/server/actions/dispatch-generate-doc.ts` (MOTEUR conservé) ET `grep -q "dispatchGenerateMissing\|dispatchGenerateDoc" apps/web/src/components/sessions/tabs/tab-avant.tsx` (toujours appelé depuis Avant).
    - `grep -q "buildDocDockItems" apps/web/src` retourne 0 si supprimé ; sinon le fichier a encore un consommateur légitime (documenter).
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : aucune erreur (pas d'import cassé).
    - `pnpm --filter @qualiof/web test` : suite complète verte.
  </acceptance_criteria>
  <done>page.tsx rend les 3 nouveaux onglets ; drawer + bouton + bloc 4 cartes supprimés ; moteur dispatchGenerate* intact ; tsc + suite verts ; 0 consommateur résiduel.</done>
</task>

</tasks>

<verification>
- Test de puissance (mutation) au gate : dans `tab-avant.tsx`, faire passer le `docType` d'un bouton (ex. CONVOCATION→CONVENTION) → le test avant-tab-actions DOIT virer ROUGE → restaurer. Idem : dans `tab-apres.tsx`, recompter le « manquants » localement au lieu de `docCompletion` → le test doc-completion-source DOIT virer ROUGE → restaurer.
- Grep de non-régression : 0 occurrence de DocDockDrawer/DocsButton/SessionOnlyDocsBlock hors fichiers supprimés.
- Checkpoint visuel Laurent : un doc apparaît dans EXACTEMENT un onglet (+ statut dans « Tous les documents ») ; plus de cartes minuscules ; suivi pack dans l'onglet Après.
</verification>

<success_criteria>
- Onglets Avant/Après/Tous les documents remplis, lisibles (1 ligne par doc).
- Aucune action perdue (Tout générer + lignes par stagiaire + ASSIDUITE_AGEFICE + 4 docs session).
- 1 doc = 1 maison ; « Tous les documents » lecture seule.
- 3 surfaces redondantes supprimées ; moteur conservé ; tsc + suite verts.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/15-refonte-fiche-session-onglets/15-02-SUMMARY.md`.
</output>
