---
phase: 15-refonte-fiche-session-onglets
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - apps/web/src/lib/closure/close-zombie-batches.ts
  - apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts
  - apps/web/scripts/close-zombie-batches.ts
  - apps/web/src/components/sessions/step-creation.tsx
  - apps/web/src/components/sessions/__tests__/session-no-ai-validator.test.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/components/sessions/inline-ai-draft-validator.tsx
  - apps/web/package.json
autonomous: true
requirements: [FS-PROGRAMME-PRODUIT, FS-ZOMBIES, FS-CORRECTIFS]
must_haves:
  truths:
    - "La validation programme IA (« Valider le programme IA ») n'apparaît plus sur la fiche session ; le programme y est en lecture seule + lien vers la fiche produit."
    - "La validation IA reste disponible sur la fiche produit (AiDraftValidationBanner — déjà existant, non touché)."
    - "Un ClosureBatch resté RUNNING/PENDING stale > 15min SANS job actif récent est clos (PARTIAL/FAILED/COMPLETED selon doneDocs/errorDocs)."
    - "Un ClosureBatch avec un job PROCESSING/QUEUED récent (< 15min) n'est JAMAIS touché par le nettoyage."
    - "Un ClosureBatch déjà COMPLETED/PARTIAL/FAILED est ignoré par le nettoyage."
    - "Le nettoyage est DRY-run par défaut ; WRITE=1 applique (pattern destructif memory)."
    - "Le bug du doublon tarif en-tête est soit corrigé, soit documenté NO-OP avec preuve runtime."
  artifacts:
    - path: "apps/web/src/lib/closure/close-zombie-batches.ts"
      provides: "Prédicat pur isZombieBatch + finalStatusFor worker-safe (no react/auth import)"
      contains: "isZombieBatch"
    - path: "apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts"
      provides: "Test prédicat sur 3 cas (stale clos / actif intact / completed ignoré) + mutation"
      contains: "isZombieBatch"
    - path: "apps/web/scripts/close-zombie-batches.ts"
      provides: "Script tsx DRY/WRITE clôturant les batches zombies"
      contains: "WRITE"
  key_links:
    - from: "apps/web/src/components/sessions/step-creation.tsx"
      to: "fiche produit"
      via: "retrait de InlineAiDraftValidator ; programme lecture seule + lien /app/produits/{id}?tab=programme"
      pattern: "produits/"
    - from: "apps/web/scripts/close-zombie-batches.ts"
      to: "apps/web/src/lib/closure/close-zombie-batches.ts"
      via: "isZombieBatch + finalStatusFor"
      pattern: "isZombieBatch"
---

<objective>
Lot 4 — Programme au niveau produit + nettoyage batches zombies + correctifs visuels résiduels.
(a) RETIRER `InlineAiDraftValidator` de la session : la validation existe DÉJÀ sur `produits/[id]?tab=programme` (`AiDraftValidationBanner`/`validateAiDraftProduct`, RESEARCH Q5). Programme en lecture seule + lien produit ; rien à recréer.
(b) Nettoyage batches zombies : prédicat PUR worker-safe `isZombieBatch` + script DRY/WRITE clôturant les `ClosureBatch` restés RUNNING/PENDING stale > 15min SANS job actif récent (réutilise la logique `doneDocs`/`errorDocs` vs `totalDocs` du worker). DRY-run par défaut.
(c) Bug tarif dupliqué en-tête : vérifier en runtime — possiblement déjà corrigé (`session-price-inline.tsx:54-61`, RESEARCH Q7). Si non reproductible → NO-OP documenté ; sinon fix ciblé.
(d) CTA `NextActionHero` cohérent avec la phase réelle (sessionStage).

Purpose: Finir la refonte propre — plus de validation IA égarée sur la session, plus de « 4 packs en cours » fantômes, en-tête sans doublon tarif.
Output: prédicat + script de nettoyage (testés), retrait du validator session, correctifs visuels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-refonte-fiche-session-onglets/15-CONTEXT.md
@.planning/phases/15-refonte-fiche-session-onglets/15-RESEARCH.md
@files/CLAUDE.md

<interfaces>
From apps/web/src/lib/closure/worker.ts (logique de finalisation à réutiliser — worker-safe) :
```ts
// bumpAndFinalize : handled = doneDocs + errorDocs ; si handled >= totalDocs →
//   next = errorDocs === 0 ? 'COMPLETED' : doneDocs === 0 ? 'FAILED' : 'PARTIAL'
//   update where status IN ('PENDING','RUNNING') → next
// STUCK_PROCESSING_MINUTES = 15 (seuil aligné, cf. requeue-stuck-closure-jobs.ts)
```

From apps/web/scripts/requeue-stuck-closure-jobs.ts (pattern script worker-safe à suivre) :
```ts
import { prisma } from '@qualiof/db';   // import autorisé (worker-safe : 0 react/auth)
const STUCK_PROCESSING_MINUTES = 15;
// cutoff = new Date(Date.now() - 15*60*1000)
// DRY par défaut ; WRITE pour appliquer (pattern memory destructif = DRY→WRITE=1)
```

Critère de zombie sûr (RESEARCH Q6) :
```
isZombieBatch(batch, jobs, now) =
  batch.status IN ('PENDING','RUNNING')
  AND (batch.updatedAt | batch.startedAt) < now - 15min
  AND aucun job du batch en ('QUEUED'|'PROCESSING') avec startedAt/updatedAt >= now - 15min
finalStatusFor(batch) = errorDocs === 0 ? 'COMPLETED' : doneDocs === 0 ? 'FAILED' : 'PARTIAL'
```

From apps/web/src/components/sessions/step-creation.tsx (validator à retirer) :
```ts
// L5 import { InlineAiDraftValidator } from './inline-ai-draft-validator';
// L195-201 {productAiDraftedAt && productId && (<InlineAiDraftValidator productId aiDraftedAt canValidate=canValidateAi />)}
```
From apps/web/src/components/produits/ai-draft-validation-banner.tsx (CIBLE déjà existante — NE PAS toucher).
From apps/web/src/components/sessions/session-price-inline.tsx (L54-61 : suffixe « / stagiaire » déjà dans `display`, fix A3 — vérifier non-régression).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0) : Test prédicat clôture batch zombie (RED) — 3 cas + mutation</name>
  <files>apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts</files>
  <read_first>
    - apps/web/src/lib/closure/worker.ts (bumpAndFinalize : doneDocs/errorDocs/totalDocs → COMPLETED/PARTIAL/FAILED)
    - apps/web/scripts/requeue-stuck-closure-jobs.ts (STUCK_PROCESSING_MINUTES=15, structure)
    - packages/db/prisma/schema.prisma (ClosureBatch + ClosureJob : champs status/updatedAt/startedAt/totalDocs/doneDocs/errorDocs)
    - 15-RESEARCH.md §Runtime State Inventory Q6 (critère de zombie sûr)
  </read_first>
  <action>
    Créer `apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts`.
    Tester les fonctions PURES `isZombieBatch(batch, jobs, now)` et `finalStatusFor(batch)` (implémentées Task 2) sur 3 cas :
    1. Batch `RUNNING`, `updatedAt = now - 30min`, tous ses jobs `DONE`/`ERROR` (aucun QUEUED/PROCESSING récent) → `isZombieBatch === true`. `finalStatusFor` : errorDocs=0 → 'COMPLETED' ; doneDocs=0 → 'FAILED' ; mixte → 'PARTIAL' (3 sous-assertions).
    2. Batch `RUNNING`, `updatedAt = now - 30min`, MAIS un job `PROCESSING` avec `startedAt = now - 2min` (< 15min) → `isZombieBatch === false` (batch réellement actif, NE PAS toucher).
    3. Batch `RUNNING` récent (`updatedAt = now - 5min`) → `false`. Batch `COMPLETED` → `false`. Batch `PENDING` stale sans job actif → `true`.
    Lancer : ÉCHEC (fonctions pas écrites) — RED.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- close-zombie-batches</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts` existe.
    - `grep -q "isZombieBatch" apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts` OK.
    - `grep -E "PROCESSING|COMPLETED|PARTIAL|FAILED" apps/web/src/lib/closure/__tests__/close-zombie-batches.test.ts` retourne ≥ 3 occurrences (les 3 cas).
    - `pnpm --filter @qualiof/web test -- close-zombie-batches` ÉCHOUE (RED).
  </acceptance_criteria>
  <done>Test prédicat écrit, rouge. Couvre stale→zombie, actif→intact, completed→ignoré + statut final.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Prédicat pur + script DRY/WRITE de clôture zombie (GREEN)</name>
  <files>apps/web/src/lib/closure/close-zombie-batches.ts, apps/web/scripts/close-zombie-batches.ts, apps/web/package.json</files>
  <read_first>
    - apps/web/src/lib/closure/worker.ts (bumpAndFinalize)
    - apps/web/scripts/requeue-stuck-closure-jobs.ts (structure script, DRY/WRITE, $disconnect)
    - apps/web/package.json (scripts pnpm existants pour ajouter l'entrée)
    - feedback memory « Worker jamais d'imports auth React » + « destructif = DRY→WRITE=1 »
    - Le test close-zombie-batches (Task 1)
  </read_first>
  <behavior>
    - `isZombieBatch` pur : true seulement si stale > 15min ET aucun job actif récent.
    - `finalStatusFor` : COMPLETED/FAILED/PARTIAL selon doneDocs/errorDocs.
    - Script DRY par défaut (log seulement) ; WRITE=1 applique l'update.
  </behavior>
  <action>
    1. Créer `apps/web/src/lib/closure/close-zombie-batches.ts` (WORKER-SAFE : importer UNIQUEMENT `@qualiof/db` si besoin de types ; AUCUN import `react`/`requireRole`/`validateRequest` — sinon crash boot worker, cf. memory). Exporter :
       - `isZombieBatch(batch, jobs, now)` : fonction pure (pas d'I/O), critère RESEARCH Q6 (status PENDING/RUNNING + stale > 15min + aucun job QUEUED/PROCESSING récent). `STALE_MINUTES = 15`.
       - `finalStatusFor(batch)` : pure, `errorDocs === 0 ? 'COMPLETED' : doneDocs === 0 ? 'FAILED' : 'PARTIAL'`.
       Ne PAS citer littéralement les tokens interdits dans les commentaires (déclencherait un grep d'acceptance) — décrire la contrainte sans les écrire.
    2. Créer `apps/web/scripts/close-zombie-batches.ts` (tsx, pattern `requeue-stuck-closure-jobs.ts`) : importe `prisma` + `isZombieBatch`/`finalStatusFor`. Charge les batches `PENDING`/`RUNNING` + leurs jobs, applique le prédicat. DRY par défaut (log la liste des batches à clore + statut cible). `WRITE=1` (env) → `prisma.closureBatch.updateMany` (where status IN PENDING/RUNNING) vers le statut final + `completedAt`. Optionnel filtre `SESSION_CODE`. `$disconnect` en finally.
    3. Ajouter le script pnpm dans `apps/web/package.json` : `"closure:close-zombies": "dotenv -e ../../.env -- tsx scripts/close-zombie-batches.ts"` (aligné sur le pattern des autres scripts tsx).
    Lancer close-zombie-batches : GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- close-zombie-batches</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/closure/close-zombie-batches.ts` existe ; `grep -q "export function isZombieBatch" ...` OK ; `grep -q "finalStatusFor" ...` OK.
    - `grep -E "from 'react'|requireRole|validateRequest" apps/web/src/lib/closure/close-zombie-batches.ts` retourne 0 (worker-safe).
    - `apps/web/scripts/close-zombie-batches.ts` existe ; `grep -q "WRITE" apps/web/scripts/close-zombie-batches.ts` OK ; `grep -q "isZombieBatch" apps/web/scripts/close-zombie-batches.ts` OK.
    - `grep -q "closure:close-zombies" apps/web/package.json` OK.
    - `pnpm --filter @qualiof/web test -- close-zombie-batches` exit 0 (GREEN).
  </acceptance_criteria>
  <done>Prédicat pur worker-safe + script DRY/WRITE écrits, test vert, 0 import interdit, entrée pnpm ajoutée.</done>
</task>

<task type="auto">
  <name>Task 3 : Retirer InlineAiDraftValidator de la session (lecture seule + lien produit)</name>
  <files>apps/web/src/components/sessions/step-creation.tsx, apps/web/src/components/sessions/__tests__/session-no-ai-validator.test.tsx, apps/web/src/components/sessions/inline-ai-draft-validator.tsx</files>
  <read_first>
    - apps/web/src/components/sessions/step-creation.tsx (L5 import, L195-201 rendu du validator)
    - apps/web/src/components/sessions/inline-ai-draft-validator.tsx (L101-108 : le lien produit existe déjà, à conserver pour la lecture seule)
    - apps/web/src/components/produits/ai-draft-validation-banner.tsx (CIBLE produit — NE PAS toucher, confirmer qu'elle existe)
    - apps/web/src/components/produits/tabs/product-programme-tab.tsx (validation déjà rendue côté produit)
  </read_first>
  <action>
    1. Créer d'abord le test `apps/web/src/components/sessions/__tests__/session-no-ai-validator.test.tsx` : rendre `<StepCreation>` (ou le composant Session) avec `productAiDraftedAt` non null → AUCUN bouton/action « Valider le programme IA » n'est rendu ; un LIEN vers `/app/produits/{id}?tab=programme` (programme lecture seule) EST présent. (RED d'abord.)
    2. Modifier `apps/web/src/components/sessions/step-creation.tsx` : retirer le rendu `InlineAiDraftValidator` (L195-201) et son import (L5). À la place, afficher le programme en LECTURE SEULE + un lien « Modifier le programme sur la fiche produit » vers `/app/produits/{productId}?tab=programme`. Retirer les props devenues inutiles (`canValidateAi`, `productAiDraftedAt`) si plus aucun usage dans le composant — sinon les garder pour le statut lecture seule.
    3. Si `inline-ai-draft-validator.tsx` n'a plus AUCUN consommateur après ce retrait (grep), le SUPPRIMER (la cible produit `AiDraftValidationBanner` reste). Sinon le conserver et documenter le consommateur restant.
    4. Ne PAS toucher `validateAiDraftProduct` ni la bannière produit (la validation produit reste fonctionnelle).
    Lancer session-no-ai-validator : GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- session-no-ai-validator</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/__tests__/session-no-ai-validator.test.tsx` existe et passe.
    - `grep -q "produits/" apps/web/src/components/sessions/__tests__/session-no-ai-validator.test.tsx` OK (lien produit asserté).
    - `grep -rn "InlineAiDraftValidator" apps/web/src` retourne 0 SI le composant est supprimé ; sinon le seul résultat est un consommateur légitime documenté (et plus `step-creation.tsx`).
    - `grep -q "InlineAiDraftValidator" apps/web/src/components/sessions/step-creation.tsx` retourne 0 (retiré de la session).
    - `test -f apps/web/src/components/produits/ai-draft-validation-banner.tsx` (cible produit intacte).
    - `pnpm --filter @qualiof/web test -- session-no-ai-validator` exit 0 (GREEN).
  </acceptance_criteria>
  <done>Validation IA retirée de la session (lecture seule + lien produit), cible produit intacte, test vert.</done>
</task>

<task type="auto">
  <name>Task 4 : Correctifs visuels résiduels — tarif en-tête (NO-OP ou fix) + CTA NextActionHero</name>
  <files>apps/web/src/components/sessions/session-header-bar.tsx, apps/web/src/components/sessions/next-action-hero.tsx</files>
  <read_first>
    - apps/web/src/components/sessions/session-header-bar.tsx (L146-156 : either/or priceSlot vs pricePerLearner)
    - apps/web/src/components/sessions/session-price-inline.tsx (L54-61 : suffixe dans display, fix A3 — vérifier non-régression)
    - apps/web/src/components/sessions/editable-field.tsx (display/suffix résiduel éventuel — cause possible si bug persiste)
    - apps/web/src/components/sessions/next-action-hero.tsx + apps/web/src/lib/sessions/session-stage.ts (CTA piloté par sessionStage)
    - 15-RESEARCH.md §Pitfall 1 (Q7) + Open Question 3
  </read_first>
  <action>
    1. Tarif dupliqué en-tête : vérifier en runtime sur `:3010` une session avec prix en mode édition. Chercher si « € / stagiaire » apparaît 2× quand `priceSlot` ET le bloc par défaut coexistent.
       - SI non reproductible (déjà corrigé par le fix A3) → NO-OP DOCUMENTÉ : ajouter un commentaire bref dans `session-header-bar.tsx` près de L146 (« tarif dédupliqué — fix A3 session-price-inline, vérifié runtime 2026-06-26 ») et le noter dans le SUMMARY. Aucune modif de code.
       - SI reproductible → fix CIBLÉ : retirer le suffixe résiduel (probable `EditableField` `display`+`suffix` en double) pour qu'il n'apparaisse qu'une fois en mode `canEdit`.
    2. CTA `NextActionHero` : confirmer qu'il lit `sessionStage.cta` (source unique) et n'affiche pas un CTA d'étape périmée. Si une incohérence subsiste après la refonte onglets (ex. « Générer le pack » alors que sessionStage est en étape ultérieure), aligner le rendu sur `sessionStage.cta` (pas de calcul parallèle). Sinon NO-OP documenté.
    3. Garder l'en-tête persistant allégé (identité + statut + 1 CTA contextuel + Paramètres) — pas de barre de 6 boutons : si des boutons résiduels traînent dans l'en-tête après les Lots 2/3 (DocsButton déjà supprimé, toggle agenda déplacé), confirmer qu'il ne reste qu'1 CTA contextuel ; documenter.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Soit `session-header-bar.tsx` contient un commentaire NO-OP daté justifiant la non-modif du tarif, soit le diff montre le retrait d'un suffixe « / stagiaire » dupliqué (un seul rendu en mode canEdit).
    - `grep -q "sessionStage" apps/web/src/components/sessions/next-action-hero.tsx` OK (CTA piloté par la source unique, pas de calcul parallèle introduit).
    - `grep -c "DocsButton\|SessionCalendarSyncToggle" apps/web/src/components/sessions/session-header-bar.tsx` = 0 (en-tête allégé, surfaces déplacées).
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : aucune erreur.
    - `pnpm --filter @qualiof/web test` : suite complète verte.
  </acceptance_criteria>
  <done>Tarif en-tête fixé ou NO-OP documenté ; CTA NextActionHero cohérent (sessionStage) ; en-tête allégé ; tsc + suite verts.</done>
</task>

</tasks>

<verification>
- Test de puissance (mutation) au gate : dans `close-zombie-batches.ts`, retirer la condition « aucun job actif récent » de `isZombieBatch` → le test cas 2 (batch avec job PROCESSING récent intact) DOIT virer ROUGE → restaurer. Prouve que le prédicat protège bien les batches réellement actifs.
- Worker-safe : `grep` 0 import react/auth dans `close-zombie-batches.ts`.
- Nettoyage réel (gated Laurent, hors automatisé, pattern DRY→WRITE) : `pnpm --filter @qualiof/web closure:close-zombies` (DRY) → feu vert Laurent → `WRITE=1 pnpm --filter @qualiof/web closure:close-zombies` → re-DRY = 0 zombie.
- Checkpoint Laurent : plus aucun « pack en cours » fantôme sur la fiche ; plus de « Valider le programme IA » sur la session ; en-tête sans doublon tarif.
</verification>

<success_criteria>
- Validation IA absente de la session (lecture seule + lien produit) ; cible produit intacte.
- Prédicat zombie sûr (3 cas + mutation) + script DRY/WRITE worker-safe ; 0 batch zombie après run.
- Tarif en-tête corrigé ou NO-OP prouvé ; CTA NextActionHero cohérent ; en-tête allégé.
- tsc + suite Vitest verts.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/15-refonte-fiche-session-onglets/15-04-SUMMARY.md`.
</output>
