---
phase: 15-refonte-fiche-session-onglets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/sessions/tabs/session-tabs.tsx
  - apps/web/src/components/sessions/tabs/__tests__/session-tabs.test.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
autonomous: true
requirements: [FS-ONGLETS-COQUILLE]
must_haves:
  truths:
    - "L'utilisateur voit 5 onglets (Session · Avant la formation · Après la formation · Tous les documents · Agenda) en haut de la fiche session, sous l'en-tête."
    - "Cliquer un onglet affiche sa section sans recharger la page ni refetch serveur (switch instantané)."
    - "L'onglet actif est reflété dans l'URL via ?tab= (deep-link partageable)."
    - "Ouvrir l'URL ?tab=apres affiche directement l'onglet Après au chargement (deep-link serveur)."
    - "Un ?tab= inconnu retombe sur l'onglet Session (coerce)."
    - "Après une génération de doc qui déclenche router.refresh(), l'onglet actif est préservé (lu depuis l'URL)."
  artifacts:
    - path: "apps/web/src/components/sessions/tabs/session-tabs.tsx"
      provides: "Conteneur client à onglets ?tab= avec pushState, role=tablist, 5 panneaux pré-rendus en props"
      min_lines: 40
      contains: "useSearchParams"
    - path: "apps/web/src/components/sessions/tabs/__tests__/session-tabs.test.tsx"
      provides: "Tests routage onglet + coerce + a11y"
      contains: "role"
  key_links:
    - from: "apps/web/src/app/app/sessions/[id]/page.tsx"
      to: "apps/web/src/components/sessions/tabs/session-tabs.tsx"
      via: "rendu <SessionTabs defaultTab={coerceTab(sp.tab)} session=... avant=... apres=... docs=... agenda=... />"
      pattern: "SessionTabs"
    - from: "apps/web/src/components/sessions/tabs/session-tabs.tsx"
      to: "URL ?tab="
      via: "useSearchParams().get('tab') + window.history.pushState"
      pattern: "pushState"
---

<objective>
Lot 1 — Coquille à onglets + en-tête allégé. Introduire un conteneur client `<SessionTabs>` qui lit l'onglet actif dans `?tab=` (deep-link + survie au `router.refresh()`), affiche 5 panneaux pré-rendus passés en props par la page RSC, et navigue par `window.history.pushState` (0 round-trip, 0 refetch). À ce stade les onglets ENVELOPPENT les blocs EXISTANTS de `page.tsx` : aucune suppression, aucun réembarquement de contenu (c'est le Lot 2). L'objectif est purement structurel : la coquille à 5 onglets fonctionne, deep-link OK, refresh préserve l'onglet.

Purpose: Remplacer le scroll vertical de 1069 lignes par une structure navigable, sans toucher au câblage métier. C'est la fondation que les Lots 2-4 remplissent.
Output: `session-tabs.tsx` (conteneur client) + son test Vitest + `page.tsx` qui rend les 5 sections existantes dans le conteneur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-refonte-fiche-session-onglets/15-CONTEXT.md
@.planning/phases/15-refonte-fiche-session-onglets/15-RESEARCH.md
@files/CLAUDE.md

<interfaces>
<!-- Pattern de référence repo : ProductTabs (à cloner/adapter, ne PAS réinventer). -->

From apps/web/src/components/produits/tabs/product-tabs.tsx (pattern à reprendre) :
```tsx
'use client';
import Link from 'next/link';
import { usePathname, useSearchParams } from 'next/navigation';
import { cn } from '@/lib/utils';
// nav role="tablist" aria-label ; chaque tab role="tab" aria-selected aria-controls={`tab-panel-${id}`} id={`tab-${id}`}
// défaut = params.delete('tab') (URL propre), sinon params.set('tab', id)
```

Squelette recommandé (RESEARCH §Code Examples, approche C — conteneur client recevant les sections en props) :
```tsx
// apps/web/src/components/sessions/tabs/session-tabs.tsx
'use client';
import { useSearchParams } from 'next/navigation';
import { cn } from '@/lib/utils';

export const SESSION_TABS = [
  { id: 'session', label: 'Session' },
  { id: 'avant', label: 'Avant la formation' },
  { id: 'apres', label: 'Après la formation' },
  { id: 'docs', label: 'Tous les documents' },
  { id: 'agenda', label: 'Agenda' },
] as const;
export type SessionTabId = (typeof SESSION_TABS)[number]['id'];

export function coerceTab(raw: string | undefined): SessionTabId {
  return SESSION_TABS.some((t) => t.id === raw) ? (raw as SessionTabId) : 'session';
}

export function SessionTabs(props: {
  defaultTab: SessionTabId;
  session: React.ReactNode; avant: React.ReactNode; apres: React.ReactNode;
  docs: React.ReactNode; agenda: React.ReactNode;
}) {
  const sp = useSearchParams();
  const active = coerceTab(sp.get('tab') ?? undefined); // 'session' par défaut + coerce inconnu
  function go(id: SessionTabId) {
    const params = new URLSearchParams(sp.toString());
    id === 'session' ? params.delete('tab') : params.set('tab', id);
    const qs = params.toString();
    window.history.pushState(null, '', qs ? `?${qs}` : window.location.pathname);
  }
  // nav role=tablist + boutons onClick=go(t.id) ; panneaux <div role=tabpanel hidden={active!==t.id}>
}
```

From apps/web/src/app/app/sessions/[id]/page.tsx (état serveur déjà calculé, NE PAS dupliquer) :
```tsx
// L63  export default async function SessionDetailPage({ params }: { params: Promise<{ id: string }> })
// L398 const sessionCompleteness = getSessionCompleteness({...})
// L506 const stage = sessionStage({...})
// L988 <SessionEvaluationBlock> · L991 <StepFacturation> = HORS SCOPE (déféré)
// Signature actuelle : reçoit `params` (Promise) mais PAS encore `searchParams`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0) : Écrire le test de routage d'onglet AVANT le composant</name>
  <files>apps/web/src/components/sessions/tabs/__tests__/session-tabs.test.tsx</files>
  <read_first>
    - apps/web/src/components/produits/tabs/__tests__/ (lire un fichier pour le style render/screen des tests d'onglets existants)
    - apps/web/vitest.config.ts (config Vitest, jsdom)
    - 15-RESEARCH.md §Validation Architecture (Lot 1 : routage `?tab=` déterministe ; défaut = session ; tab inconnu → session ; a11y role=tablist/role=tab/aria-selected)
  </read_first>
  <action>
    Créer `apps/web/src/components/sessions/tabs/__tests__/session-tabs.test.tsx`.
    Tests (comportemental, pas source-grep) — rendre `<SessionTabs>` avec 5 sections marquées par un texte distinct (« PANEL_SESSION », « PANEL_AVANT », « PANEL_APRES », « PANEL_DOCS », « PANEL_AGENDA ») :
    1. `coerceTab(undefined)` → `'session'` ; `coerceTab('apres')` → `'apres'` ; `coerceTab('zzz-inconnu')` → `'session'`.
    2. searchParams vide → panneau visible = PANEL_SESSION (les autres ont `hidden`).
    3. `?tab=apres` (mocker `useSearchParams` via `vi.mock('next/navigation', ...)` retournant `new URLSearchParams('tab=apres')`) → PANEL_APRES visible, PANEL_SESSION masqué.
    4. `?tab=zzz` inconnu → fallback PANEL_SESSION visible.
    5. a11y : le `<nav>` a `role="tablist"` ; exactement 5 `role="tab"` ; l'onglet actif `aria-selected="true"`, les autres `aria-selected="false"`.
    6. Cliquer « Avant la formation » appelle `window.history.pushState` (mocké `vi.fn()`) avec une URL contenant `tab=avant` ; cliquer « Session » appelle pushState SANS `tab=`.
    Lancer le test : il doit ÉCHOUER (composant pas encore écrit) — RED Nyquist.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- session-tabs</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/tabs/__tests__/session-tabs.test.tsx` existe.
    - `grep -E "role.*tab|getAllByRole\('tab'\)" apps/web/src/components/sessions/tabs/__tests__/session-tabs.test.tsx` retourne ≥ 1 ligne.
    - Le fichier mentionne `coerceTab`, `tab=apres`, `pushState`.
    - `pnpm --filter @qualiof/web test -- session-tabs` se termine en ÉCHEC (module introuvable / assertions rouges) — RED prouvé.
  </acceptance_criteria>
  <done>Test écrit, exécuté, rouge (RED). Couvre coerce + routage + a11y + pushState.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Implémenter le conteneur client SessionTabs (GREEN)</name>
  <files>apps/web/src/components/sessions/tabs/session-tabs.tsx</files>
  <read_first>
    - apps/web/src/components/produits/tabs/product-tabs.tsx (pattern role=tablist, aria-*, delete('tab') pour défaut)
    - apps/web/src/lib/utils.ts (helper `cn`)
    - 15-RESEARCH.md §Code Examples (squelette approche C, repris dans <interfaces>)
    - Le test écrit en Task 1
  </read_first>
  <behavior>
    - `coerceTab` : valide la liste des 5 ids, fallback `'session'`.
    - Panneau actif rendu visible, les 4 autres montés mais `hidden`.
    - Clic onglet → pushState avec/sans `tab=` selon défaut.
    - a11y : 1 tablist, 5 tabs, aria-selected cohérent.
  </behavior>
  <action>
    Créer `apps/web/src/components/sessions/tabs/session-tabs.tsx` (`'use client'`) selon le squelette des `<interfaces>` :
    - Exporter `SESSION_TABS` (5 onglets, ids `session|avant|apres|docs|agenda`, labels FR exacts), le type `SessionTabId`, la fonction pure `coerceTab(raw)`, et le composant `SessionTabs`.
    - `const active = coerceTab(sp.get('tab') ?? undefined)`.
    - `<nav role="tablist" aria-label="Onglets fiche session" className="border-b border-border flex overflow-x-auto">` + 5 `<button role="tab" aria-selected={active===t.id} aria-controls={\`tab-panel-${t.id}\`} id={\`tab-${t.id}\`} type="button" onClick={() => go(t.id)}>` avec classes `cn(...)` reprises de ProductTabs (border-b-2, primary actif).
    - 5 `<div role="tabpanel" id={\`tab-panel-${t.id}\`} aria-labelledby={\`tab-${t.id}\`} hidden={active !== t.id}>{panels[t.id]}</div>` — panneaux MONTÉS (`hidden`, pas démontés) pour switch instantané (anti-pattern : `{active && ...}` INTERDIT, cf. RESEARCH §Anti-Patterns).
    - `go(id)` : `window.history.pushState(null, '', qs ? \`?${qs}\` : window.location.pathname)` (0 round-trip).
    NE PAS utiliser `<Link>` (déclencherait un refetch RSC complet). Pas de recalcul d'état dans le composant : tout vient des props/children.
    Lancer le test : il doit PASSER (GREEN).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- session-tabs</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/tabs/session-tabs.tsx` existe et la 1re ligne est `'use client';`.
    - `grep -q "useSearchParams" apps/web/src/components/sessions/tabs/session-tabs.tsx` OK.
    - `grep -q "window.history.pushState" apps/web/src/components/sessions/tabs/session-tabs.tsx` OK.
    - `grep -q 'role="tablist"' apps/web/src/components/sessions/tabs/session-tabs.tsx` OK.
    - `grep -q "export function coerceTab" apps/web/src/components/sessions/tabs/session-tabs.tsx` OK.
    - `grep -c "from 'next/link'\|next/link" apps/web/src/components/sessions/tabs/session-tabs.tsx` = 0 (pas de `<Link>` → pas de refetch).
    - `pnpm --filter @qualiof/web test -- session-tabs` exit 0 (GREEN).
  </acceptance_criteria>
  <done>Composant écrit, test vert. 5 onglets, pushState, a11y, panneaux montés masqués.</done>
</task>

<task type="auto">
  <name>Task 3 : Câbler SessionTabs dans page.tsx + lire searchParams (deep-link serveur)</name>
  <files>apps/web/src/app/app/sessions/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/sessions/[id]/page.tsx (L63 signature ; L506-1069 zone de rendu — repérer l'ordre : SessionHeaderBar, RecordRecentVisit, NextActionHero, status/dates, BatchProgressAutoRefresh L849, blocs timeline StepCreation/SessionParticipantsList/PreparationPedagogiqueBlock/StepPendantFormation/ClosureFormationBlock, SessionEvaluationBlock L988, StepFacturation L991, SessionOnlyDocsBlock L1015, ParticipantDocMatrix L1052 dans `<details>`)
    - apps/web/src/app/app/produits/[id]/page.tsx (précédent : page RSC qui lit `searchParams` et passe `activeTab` au composant onglets)
    - Le composant SessionTabs (Task 2)
  </read_first>
  <action>
    Modifier `apps/web/src/app/app/sessions/[id]/page.tsx` :
    1. Étendre la signature : `export default async function SessionDetailPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ tab?: string }> })`. `const sp = await searchParams;`.
    2. Importer `{ SessionTabs, coerceTab }` depuis `@/components/sessions/tabs/session-tabs`.
    3. GARDER l'en-tête persistant AU-DESSUS des onglets : `RecordRecentVisit` + `SessionHeaderBar` restent hors `<SessionTabs>` (l'allègement fin de l'en-tête est Lot 2/4 — ici on ne casse rien).
    4. Regrouper les blocs EXISTANTS en 5 fragments JSX passés en props, SANS modifier leur contenu interne (enveloppement seulement) :
       - `session=` → status/dates + StepCreation + SessionParticipantsList.
       - `avant=` → PreparationPedagogiqueBlock.
       - `apres=` → BatchProgressAutoRefresh + StepPendantFormation + ClosureFormationBlock + SessionOnlyDocsBlock (le réembarquement propre = Lot 2 ; ici on déplace tels quels).
       - `docs=` → ParticipantDocMatrix (sortie de son `<details>`).
       - `agenda=` → placeholder `<div>Agenda — synchro Google Calendar (Lot 3)</div>` (contenu réel = Lot 3).
       Rendre `<SessionTabs defaultTab={coerceTab(sp.tab)} session={...} avant={...} apres={...} docs={...} agenda={...} />` après l'en-tête.
    5. `SessionEvaluationBlock` (L988) et `StepFacturation` (L991) : les laisser HORS onglets (déféré, conservés en bas de page), avec un commentaire `// hors flux onglets (déféré, cf. CONTEXT §deferred)`. Ne PAS les supprimer.
    NE PAS toucher aux ~10 requêtes Prisma ni à `getSessionCompleteness`/`sessionStage`/`getSessionClosureStatus` : ils restent en tête de fonction (source unique), props descendantes.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "searchParams" apps/web/src/app/app/sessions/[id]/page.tsx` OK.
    - `grep -q "SessionTabs" apps/web/src/app/app/sessions/[id]/page.tsx` OK et `grep -q "coerceTab(sp.tab)" apps/web/src/app/app/sessions/[id]/page.tsx` OK.
    - `grep -q "defaultTab=" apps/web/src/app/app/sessions/[id]/page.tsx` OK.
    - `pnpm --filter @qualiof/web exec tsc --noEmit` n'émet AUCUNE nouvelle erreur sur `sessions/[id]/page.tsx` ni `session-tabs.tsx`.
    - `pnpm --filter @qualiof/web test` : suite complète verte (baseline ~700+ tests, aucune régression).
  </acceptance_criteria>
  <done>page.tsx rend les 5 sections existantes dans SessionTabs, deep-link serveur via coerceTab(sp.tab), tsc + suite verts. Aucun bloc métier modifié.</done>
</task>

</tasks>

<verification>
- Test de puissance (mutation) au gate : dans `session-tabs.tsx`, faire renvoyer `props.defaultTab` au lieu de `coerceTab(sp.get('tab'))` → le test « ?tab=apres affiche PANEL_APRES » DOIT virer ROUGE → restaurer.
- Checkpoint visuel Laurent (manuel, hors automatisé) : ouvrir `:3010/app/sessions/<id>`, vérifier les 5 onglets, deep-link `?tab=apres`, et que `router.refresh()` (déclenché par une génération de doc dans l'onglet) préserve l'onglet actif.
- `pnpm --filter @qualiof/web test` vert sur toute la suite.
</verification>

<success_criteria>
- 5 onglets visibles et navigables, switch instantané (pushState, 0 refetch).
- `?tab=` deep-linkable ; tab inconnu → Session ; refresh préserve l'onglet.
- Aucun bloc métier supprimé ni modifié dans son contenu (Lot 1 = structure seulement).
- Suite Vitest verte + tsc clean.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/15-refonte-fiche-session-onglets/15-01-SUMMARY.md`.
</output>
