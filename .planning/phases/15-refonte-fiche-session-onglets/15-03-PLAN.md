---
phase: 15-refonte-fiche-session-onglets
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - apps/web/src/components/sessions/tabs/tab-agenda.tsx
  - apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
autonomous: true
requirements: [FS-ONGLETS-AGENDA]
must_haves:
  truths:
    - "L'onglet Agenda contient le toggle/bouton de synchro Google Calendar (SessionCalendarSyncToggle, Phase 14)."
    - "Cliquer « Synchroniser l'agenda » appelle syncSessionCalendarAction({ sessionId, notifyLearners })."
    - "L'onglet Agenda affiche les créneaux de la session jour par jour en lecture (pas d'édition)."
    - "La synchro reste idempotente : re-sync = 0 doublon (clé déterministe Phase 14, non régressée)."
    - "Le toggle de synchro n'apparaît plus en doublon ailleurs (en-tête / Paramètres) — une seule maison : l'onglet Agenda."
  artifacts:
    - path: "apps/web/src/components/sessions/tabs/tab-agenda.tsx"
      provides: "Onglet Agenda : SessionCalendarSyncToggle + affichage créneaux lecture"
      contains: "SessionCalendarSyncToggle"
    - path: "apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx"
      provides: "Test : l'onglet appelle syncSessionCalendarAction"
      contains: "syncSessionCalendarAction"
  key_links:
    - from: "apps/web/src/components/sessions/tabs/tab-agenda.tsx"
      to: "apps/web/src/server/actions/calendar-sync.ts"
      via: "SessionCalendarSyncToggle → syncSessionCalendarAction"
      pattern: "syncSessionCalendarAction"
---

<objective>
Lot 3 — Onglet Agenda. Réembarquer la synchro Google Calendar livrée en Phase 14 (`SessionCalendarSyncToggle` → `syncSessionCalendarAction`) dans l'onglet Agenda, et y afficher les créneaux de la session en lecture. Réutilise intégralement la suite calendar Phase 14 (67 tests, idempotence prouvée en prod). Aucune nouvelle logique de synchro : on déplace l'UI existante dans sa maison définitive (l'onglet Agenda) et on retire son doublon de l'en-tête / Paramètres. Les créneaux éditables interactifs (SessionSlot) restent hors phase.

Purpose: Une maison unique pour l'agenda ; cohérence avec « 1 surface = 1 endroit ». La Phase 14 fournit le moteur idempotent, on ne le retouche pas.
Output: `tab-agenda.tsx` (toggle + créneaux lecture) + test + page.tsx câblé sur l'onglet Agenda (retrait du doublon header).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-refonte-fiche-session-onglets/15-CONTEXT.md
@.planning/phases/15-refonte-fiche-session-onglets/15-RESEARCH.md
@files/CLAUDE.md

<interfaces>
From apps/web/src/server/actions/calendar-sync.ts (Phase 14 — MOTEUR idempotent, NE PAS retoucher) :
```ts
export async function syncSessionCalendarAction(
  input: { sessionId: string; notifyLearners: boolean }
): Promise<{ ok: boolean; ... }>;
// requireRole(['ADMIN','MANAGER']) ; loadSessionEventCtx → syncSessionCalendar ; AuditLog sessions.calendarSynced ; revalidatePath
```

From apps/web/src/components/sessions/session-calendar-sync-toggle.tsx (Phase 14 — composant à déplacer) :
```ts
interface Props { sessionId: string; isPastSession: boolean; /* + canEdit garde */ }
export function SessionCalendarSyncToggle({ sessionId, isPastSession, ... }: Props);
// useState(notifyLearners), bouton « Synchroniser l'agenda », runSync → syncSessionCalendarAction({ sessionId, notifyLearners })
```

From page.tsx (Lot 1/2) : le composant `SessionCalendarSyncToggle` est aujourd'hui rendu dans l'en-tête (L594/L725 section « Agenda / Rappels ») — c'est le doublon à retirer ; sa maison devient l'onglet Agenda.
Données créneaux : `SessionSlot` (schema Prisma) liés à la session — lecture seule jour par jour (les créneaux interactifs sont hors phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0) : Test onglet Agenda → appelle syncSessionCalendarAction (RED)</name>
  <files>apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx</files>
  <read_first>
    - apps/web/src/server/actions/__tests__/calendar-sync.test.ts (suite Phase 14 — style de mock de l'action)
    - apps/web/src/components/sessions/session-calendar-sync-toggle.tsx (props + runSync)
    - 15-RESEARCH.md §Validation Architecture (Lot 3 : réutilise suite calendar ; idempotence re-sync = 0 doublon déjà prouvée Phase 14)
  </read_first>
  <action>
    Créer `apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx`.
    Tests (comportemental, action mockée via `vi.mock('@/server/actions/calendar-sync', ...)`) :
    1. Rendre `<TabAgenda sessionId="..." isPastSession={false} slots={[...]} canEdit />` → le toggle de synchro est présent ; cliquer « Synchroniser l'agenda » appelle `syncSessionCalendarAction` une fois avec `{ sessionId, notifyLearners: <bool du toggle> }`.
    2. Les créneaux passés en prop `slots` sont affichés en lecture (un élément par créneau, pas de champ éditable / pas de bouton de modification).
    3. Garde `canEdit` : si `canEdit={false}`, le bouton de synchro n'est pas actionnable (ou absent) — respecte la garde Phase 14.
    Lancer : ÉCHEC (composant pas écrit) — RED.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- tab-agenda</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx` existe.
    - `grep -q "syncSessionCalendarAction" apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx` OK.
    - `grep -q "notifyLearners" apps/web/src/components/sessions/tabs/__tests__/tab-agenda.test.tsx` OK.
    - `pnpm --filter @qualiof/web test -- tab-agenda` ÉCHOUE (RED).
  </acceptance_criteria>
  <done>Test écrit, rouge. Couvre appel action + créneaux lecture + garde canEdit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Implémenter tab-agenda (toggle Phase 14 + créneaux lecture) (GREEN)</name>
  <files>apps/web/src/components/sessions/tabs/tab-agenda.tsx</files>
  <read_first>
    - apps/web/src/components/sessions/session-calendar-sync-toggle.tsx (composant Phase 14 à réutiliser tel quel)
    - packages/db/prisma/schema.prisma (modèle SessionSlot — champs jour/horaires pour l'affichage lecture)
    - apps/web/src/lib/business-days.ts (horaires/jours ouvrés si besoin d'afficher matin/après-midi)
    - Le test tab-agenda (Task 1)
  </read_first>
  <behavior>
    - L'onglet rend `SessionCalendarSyncToggle` (réutilisé), qui appelle `syncSessionCalendarAction`.
    - Les créneaux sont rendus en lecture, un par ligne (jour + horaires).
    - Aucune nouvelle logique de synchro ni de modification de créneau.
  </behavior>
  <action>
    Créer `apps/web/src/components/sessions/tabs/tab-agenda.tsx` :
    - Réutiliser `SessionCalendarSyncToggle` (import existant) en lui passant `sessionId`, `isPastSession`, et la garde `canEdit` (props venant de page.tsx). NE PAS recoder la synchro.
    - Sous le toggle, afficher les créneaux de la session jour par jour en LECTURE (props `slots`/dates) : une ligne par créneau (date + horaires 9h-13h/14h-18h tels que figés, cf. conventions métier). Pas de champ éditable, pas de bouton modifier (créneaux interactifs = hors phase).
    - Composant simple ; `'use client'` uniquement si nécessaire (le toggle est déjà client).
    Lancer tab-agenda : GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- tab-agenda calendar</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/sessions/tabs/tab-agenda.tsx` existe.
    - `grep -q "SessionCalendarSyncToggle" apps/web/src/components/sessions/tabs/tab-agenda.tsx` OK.
    - `grep -E "syncSessionCalendar\b|new google\.|events.insert" apps/web/src/components/sessions/tabs/tab-agenda.tsx` retourne 0 (aucune re-implémentation de la synchro — réutilise le moteur Phase 14).
    - `pnpm --filter @qualiof/web test -- tab-agenda` exit 0 (GREEN).
    - `pnpm --filter @qualiof/web test -- calendar` exit 0 (suite Phase 14 non régressée).
  </acceptance_criteria>
  <done>Onglet Agenda réutilise le toggle Phase 14 + créneaux lecture, tests verts, suite calendar intacte.</done>
</task>

<task type="auto">
  <name>Task 3 : Câbler tab-agenda dans page.tsx + retirer le doublon header</name>
  <files>apps/web/src/app/app/sessions/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/sessions/[id]/page.tsx (prop `agenda=` du SessionTabs posée en Lot 1 ; rendu doublon du toggle dans l'en-tête / section « Agenda / Rappels » L594/L725)
    - Le composant TabAgenda (Task 2)
  </read_first>
  <action>
    Modifier `apps/web/src/app/app/sessions/[id]/page.tsx` :
    1. Remplacer le placeholder `agenda=` par `agenda={<TabAgenda sessionId={session.id} isPastSession={...} slots={...} canEdit={canEdit} />}` (importer `TabAgenda`). Calculer `isPastSession` comme déjà fait en Phase 14 (date de fin < aujourd'hui).
    2. RETIRER le rendu doublon de `SessionCalendarSyncToggle` dans l'en-tête / section « Agenda / Rappels » (L594/L725) : sa maison unique est désormais l'onglet Agenda (« 1 surface = 1 endroit »). Conserver l'import seulement s'il sert encore ailleurs ; sinon retirer l'import.
    NE PAS toucher à `syncSessionCalendarAction` ni à la lib calendar.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test && pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "TabAgenda" apps/web/src/app/app/sessions/[id]/page.tsx` OK et `grep -q "agenda={" apps/web/src/app/app/sessions/[id]/page.tsx` OK.
    - `grep -c "SessionCalendarSyncToggle" apps/web/src/app/app/sessions/[id]/page.tsx` = 0 (doublon header retiré ; le toggle vit dans tab-agenda).
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : aucune erreur (import non cassé).
    - `pnpm --filter @qualiof/web test` : suite complète verte.
  </acceptance_criteria>
  <done>Onglet Agenda câblé dans page.tsx, doublon header retiré, tsc + suite verts.</done>
</task>

</tasks>

<verification>
- Test de puissance (mutation) au gate : dans `tab-agenda.tsx`, retirer le passage de `sessionId` (ou figer notifyLearners) → le test tab-agenda DOIT virer ROUGE → restaurer.
- Suite calendar Phase 14 (`-- calendar`) reste verte = idempotence non régressée.
- Checkpoint Laurent (manuel) : depuis l'onglet Agenda, synchroniser une session à venir ; double-clic → 2e run = 0 doublon (procédure 14-SMOKE.md).
</verification>

<success_criteria>
- Onglet Agenda = synchro Google Calendar (toggle Phase 14) + créneaux en lecture.
- Doublon du toggle retiré de l'en-tête.
- Idempotence Phase 14 préservée (suite calendar verte).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/15-refonte-fiche-session-onglets/15-03-SUMMARY.md`.
</output>
