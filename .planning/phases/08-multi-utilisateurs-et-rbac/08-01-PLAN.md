---
phase: 08-multi-utilisateurs-et-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/
  - apps/web/src/lib/auth.ts
  - apps/web/src/lib/rbac.ts
  - apps/web/src/lib/__tests__/rbac.test.ts
  - packages/shared/src/constants/permissions.ts
  - packages/shared/src/constants/index.ts
  - packages/shared/src/schemas/user.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/schemas/__tests__/user.test.ts
autonomous: true
requirements: [RBAC-01, RBAC-03, RBAC-04]
must_haves:
  truths:
    - "Migration Prisma applied: User has disabledAt, lastLoginAt, invitedAt, invitedBy columns"
    - "Migration applied: UserInvitation model exists with token unique index"
    - "Module @/lib/rbac exports requireRole, hasRole, UnauthorizedError, ForbiddenError"
    - "validateRequest() returns { user: null, session: null } and invalidates session when user.disabledAt is not null"
    - "@qualiof/shared exports PERMISSIONS, canRead, canWrite, rolesWithAccess from constants/permissions"
    - "@qualiof/shared exports inviteUserSchema, setPasswordSchema, changeUserRoleSchema"
    - "Lucia DatabaseUserAttributes typing includes disabledAt: Date | null"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "User extensions + UserInvitation model"
      contains: "model UserInvitation"
    - path: "apps/web/src/lib/rbac.ts"
      provides: "requireRole guard"
      exports: ["requireRole", "hasRole", "UnauthorizedError", "ForbiddenError"]
    - path: "apps/web/src/lib/auth.ts"
      provides: "validateRequest rejects disabled users"
      contains: "disabledAt"
    - path: "packages/shared/src/constants/permissions.ts"
      provides: "RBAC matrix source of truth"
      exports: ["PERMISSIONS", "canRead", "canWrite", "rolesWithAccess"]
    - path: "packages/shared/src/schemas/user.ts"
      provides: "Zod schemas for users"
      exports: ["inviteUserSchema", "setPasswordSchema", "changeUserRoleSchema"]
  key_links:
    - from: "apps/web/src/lib/rbac.ts"
      to: "apps/web/src/lib/auth.ts"
      via: "import { validateRequest } from './auth'"
      pattern: "validateRequest"
    - from: "apps/web/src/lib/auth.ts"
      to: "user.disabledAt check"
      via: "in validateRequest after lucia.validateSession"
      pattern: "disabledAt"
    - from: "packages/shared/src/schemas/index.ts"
      to: "packages/shared/src/schemas/user.ts"
      via: "export *"
      pattern: "export \\* from './user'"
---

<objective>
Fondations RBAC pour Phase 8 : (1) migration Prisma `User` +4 colonnes nullables + nouveau model `UserInvitation`, (2) helper `lib/rbac.ts` (`requireRole`, `hasRole`, `UnauthorizedError`, `ForbiddenError`), (3) extension `lib/auth.ts` pour exposer `disabledAt` via Lucia getUserAttributes ET invalider la session si user désactivé, (4) matrice permissions D-02 dans `packages/shared/src/constants/permissions.ts` (source de vérité unique), (5) schémas Zod user (`inviteUserSchema`, `setPasswordSchema`, `changeUserRoleSchema`).

Purpose: poser les briques techniques que tous les plans 08-02..08-06 vont consommer. Sans cela, server actions et UI ne compilent pas.
Output: schema + 4 fichiers de code + 2 fichiers de tests Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md

@packages/db/prisma/schema.prisma
@apps/web/src/lib/auth.ts
@packages/shared/src/schemas/index.ts
@packages/shared/src/schemas/tenant.ts
@apps/web/src/lib/audit-log.ts

<interfaces>
<!-- Contracts existants à respecter (lus dans le codebase, ne pas re-explorer) -->

Existing Lucia setup (auth.ts L25-44):
```typescript
getUserAttributes: (data) => ({
  email: data.email,
  firstName: data.firstName,
  lastName: data.lastName,
  role: data.role,
  tenantId: data.tenantId,
}),

declare module 'lucia' {
  interface Register {
    Lucia: typeof lucia;
    DatabaseUserAttributes: {
      email: string; firstName: string; lastName: string;
      role: string; tenantId: string;
    };
  }
}
```

Existing User model (schema.prisma L57-74):
```
model User {
  id, tenantId, tenant, email @unique, hashedPwd, firstName, lastName,
  role @default(LECTEUR), authSessions, auditLogs, leadsOwned, tasksOwned,
  createdAt, updatedAt
  @@index([tenantId])
}
```

Existing UserRole enum (schema.prisma L48-55):
```
enum UserRole { ADMIN MANAGER FORMATEUR COMMERCIAL COMPTABLE LECTEUR }
```

Existing schemas/index.ts (5 lines):
```typescript
export * from './auth';
export * from './person';
export * from './organization';
export * from './address';
export * from './tenant';
```

Existing packages/shared/src/ layout : `constants/`, `helpers/`, `schemas/`, `env.ts`, `index.ts`.
Existing tenant schema pattern (tenant.ts has tenantBillingSchema with z.preprocess for IBAN cleaning) — REUSE pattern for trimming/lowercasing emails.

D-02 PERMISSIONS matrix (from CONTEXT.md L31-48):
| Section UI | ADMIN | MANAGER | COMMERCIAL | FORMATEUR | COMPTABLE | LECTEUR |
| Dashboard       | RW | RW | R  | R  | R  | R  |
| Apprenants      | RW | RW | RW | R  | R  | R  |
| Sessions        | RW | RW | RW | R  | R  | R  |  -- Phase 8: FORMATEUR=R (scoping personnel deferred Phase 9)
| Produits        | RW | RW | R  | R  | R  | R  |
| Formateurs      | RW | RW | -  | R  | -  | R  |
| Leads           | RW | RW | RW | -  | -  | -  |
| Pré-inscriptions| RW | RW | RW | -  | -  | -  |
| Dossiers OPCO   | RW | RW | RW | -  | RW | R  |
| Budget AGEFICE  | RW | RW | R  | -  | RW | R  |
| Factures        | RW | RW | -  | -  | RW | R  |
| Financeurs      | R  | R  | R  | -  | R  | -  |
| Organisations   | RW | RW | RW | R  | R  | R  |
| Qualiopi bilan  | RW | RW | -  | R  | R  | -  |
| tenantSettings  | RW | -  | -  | -  | -  | -  |
| users           | RW | -  | -  | -  | -  | -  |
| auditHistory    | R  | -  | -  | -  | -  | -  |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration Prisma User + UserInvitation</name>
  <read_first>
    - packages/db/prisma/schema.prisma (L48-81 pour User + AuthSession, L1000-1017 pour AuditLog)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md (Example 1 — schema exact)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md (D-03 — schema décrit)
    - Phase 7 example pattern : voir la migration `phase_07_*` qui a ajouté 10 colonnes nullables au model `Tenant` dans packages/db/prisma/migrations/
  </read_first>
  <files>
    - packages/db/prisma/schema.prisma (MODIFIER : étendre model User L57-74 avec 4 colonnes nullables, AJOUTER model UserInvitation après AuthSession L81)
    - packages/db/prisma/migrations/{TIMESTAMP}_phase_08_users_rbac/migration.sql (CRÉER via `pnpm --filter @qualiof/db prisma migrate dev --name phase_08_users_rbac`)
  </files>
  <behavior>
    - User model has 4 NEW nullable columns: disabledAt DateTime?, lastLoginAt DateTime?, invitedAt DateTime?, invitedBy String?
    - User has new relation `invitations UserInvitation[] @relation("UserInvitations")` and new index `@@index([tenantId, disabledAt])`
    - New UserInvitation model: id (uuid pk), tenantId String, email String, token String @unique, role UserRole, expiresAt DateTime, usedAt DateTime?, userId String? + relation User @relation("UserInvitations") onDelete: Cascade, invitedBy String, createdAt @default(now())
    - Three indexes on UserInvitation: @@index([tenantId]), @@index([token]), @@index([email, tenantId])
    - `pnpm --filter @qualiof/db prisma migrate dev --name phase_08_users_rbac` runs without error and applies migration
    - `pnpm --filter @qualiof/db prisma generate` produces a Client with UserInvitation model
    - Existing seed user remains queryable (all new columns null = active)
  </behavior>
  <action>
    Étape 1 — Éditer `packages/db/prisma/schema.prisma` :

    A) Dans `model User` (L57-74), AJOUTER avant `createdAt` :
    ```
    // Phase 8 — soft-delete + métadonnées invitation
    disabledAt   DateTime?     // null = actif. Si !null → validateRequest rejette + sessions invalidées
    lastLoginAt  DateTime?     // mis à jour dans loginAction sur succès
    invitedAt    DateTime?     // null = créé manuellement (seed)
    invitedBy    String?       // FK User.id de l'admin (pas de Prisma relation pour éviter cycle)
    invitations  UserInvitation[] @relation("UserInvitations")
    ```
    Et AJOUTER `@@index([tenantId, disabledAt])` en plus de l'index existant `@@index([tenantId])`.

    B) APRÈS `model AuthSession` (L82), AJOUTER :
    ```prisma
    model UserInvitation {
      id        String     @id @default(uuid())
      tenantId  String
      email     String
      token     String     @unique  // random 32 hex, exposé dans URL — pas hashé (cf RESEARCH Finding #4)
      role      UserRole
      expiresAt DateTime   // J+7 par défaut
      usedAt    DateTime?  // null = pas encore utilisée
      userId    String?
      user      User?      @relation("UserInvitations", fields: [userId], references: [id], onDelete: Cascade)
      invitedBy String     // FK User.id de l'admin qui a déclenché l'invitation
      createdAt DateTime   @default(now())

      @@index([tenantId])
      @@index([token])
      @@index([email, tenantId])
    }
    ```

    Étape 2 — Générer + appliquer la migration :
    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files"
    pnpm --filter @qualiof/db prisma migrate dev --name phase_08_users_rbac
    pnpm --filter @qualiof/db prisma generate
    ```

    Tous les nouveaux champs sont nullables → pas de backfill nécessaire (l'admin existant aura `disabledAt=null` = actif, `invitedAt=null` = créé manuellement seed).
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "model UserInvitation" packages/db/prisma/schema.prisma &&
      grep -q "disabledAt   DateTime?" packages/db/prisma/schema.prisma &&
      grep -q "@@index(\[tenantId, disabledAt\])" packages/db/prisma/schema.prisma &&
      pnpm --filter @qualiof/db prisma migrate status &&
      pnpm --filter @qualiof/db prisma generate
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model UserInvitation" packages/db/prisma/schema.prisma` ≥ 1
    - `grep -c "disabledAt\|lastLoginAt\|invitedAt\|invitedBy" packages/db/prisma/schema.prisma` ≥ 4 (au moins ces 4 mots-clés)
    - `ls packages/db/prisma/migrations/*phase_08_users_rbac*` retourne 1 dossier
    - `pnpm --filter @qualiof/db prisma migrate status` reports "Database schema is up to date!"
    - Le fichier `node_modules/.prisma/client/index.d.ts` contient `UserInvitation` (preuve generate OK)
  </acceptance_criteria>
  <done>
    Migration appliquée en BDD locale. `prisma migrate status` clean. Le type `UserInvitation` est généré côté `@qualiof/db`. User existant (seed admin) reste queryable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Matrice permissions + Zod schemas user</name>
  <read_first>
    - packages/shared/src/schemas/tenant.ts (pattern Zod schema avec preprocess pour cleaning input — réutiliser pour email lowercase)
    - packages/shared/src/schemas/index.ts (5 lignes — pattern barrel export)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md Example 5 (permissions matrix exact code)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md D-02 (matrix 6 rôles × 16 sections)
  </read_first>
  <files>
    - packages/shared/src/constants/permissions.ts (CRÉER : matrix PERMISSIONS + helpers canRead/canWrite/rolesWithAccess)
    - packages/shared/src/constants/index.ts (CRÉER si absent OU MODIFIER pour ajouter `export * from './permissions'`)
    - packages/shared/src/schemas/user.ts (CRÉER : inviteUserSchema + setPasswordSchema + changeUserRoleSchema)
    - packages/shared/src/schemas/index.ts (MODIFIER : ajouter `export * from './user';`)
    - packages/shared/src/schemas/__tests__/user.test.ts (CRÉER : 8 tests minimum)
  </files>
  <behavior>
    - `PERMISSIONS` is a typed Record<AppSection, Partial<Record<UserRole, 'R' | 'RW'>>> covering 16 sections from D-02 (dashboard, learners, sessions, products, trainers, leads, preenrollments, opcoFiles, budgetAgefice, invoices, funders, organizations, qualiopiBilan, tenantSettings, users, auditHistory).
    - `canRead('LECTEUR', 'users')` returns false ; `canRead('ADMIN', 'users')` returns true.
    - `canWrite('LECTEUR', 'learners')` returns false ; `canWrite('COMMERCIAL', 'learners')` returns true.
    - `rolesWithAccess('users', 'RW')` returns `['ADMIN']`.
    - `rolesWithAccess('sessions', 'R')` returns all 6 roles (everyone can read sessions per D-02 row).
    - `inviteUserSchema.parse({ email: 'A@B.fr', firstName: 'Jean', lastName: 'Dupont', role: 'COMMERCIAL' })` returns email lowercased to `a@b.fr` (preprocess).
    - `inviteUserSchema.parse({ email: 'bad', firstName: '', lastName: 'X', role: 'BAD' })` throws ZodError with field errors on email + firstName + role.
    - `setPasswordSchema.parse({ password: 'short', confirm: 'short' })` throws (8 chars min).
    - `setPasswordSchema.parse({ password: 'longenough', confirm: 'longenougb' })` throws on confirm mismatch (refine).
    - `setPasswordSchema.parse({ password: 'longenough', confirm: 'longenough' })` returns the parsed object.
    - `changeUserRoleSchema.parse({ userId: 'uuid', role: 'MANAGER' })` returns parsed object ; invalid role throws.
  </behavior>
  <action>
    Étape 1 — `packages/shared/src/constants/permissions.ts` :

    ```typescript
    // Source de vérité D-02 CONTEXT.md Phase 8.
    // Phase 8 note: FORMATEUR sur sessions = 'R' (scoping personnel deferred Phase 9).
    import type { UserRole } from '@prisma/client';

    export type AppSection =
      | 'dashboard' | 'learners' | 'sessions' | 'products' | 'trainers'
      | 'leads' | 'preenrollments' | 'opcoFiles' | 'budgetAgefice' | 'invoices'
      | 'funders' | 'organizations' | 'qualiopiBilan'
      | 'tenantSettings' | 'users' | 'auditHistory';

    export type Permission = 'R' | 'RW';

    export const PERMISSIONS: Record<AppSection, Partial<Record<UserRole, Permission>>> = {
      dashboard:      { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'R',  FORMATEUR: 'R',  COMPTABLE: 'R',  LECTEUR: 'R'  },
      learners:       { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'RW', FORMATEUR: 'R',  COMPTABLE: 'R',  LECTEUR: 'R'  },
      // Phase 8: FORMATEUR=R (Phase 9 ajoutera scoping personnel via User.personId)
      sessions:       { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'RW', FORMATEUR: 'R',  COMPTABLE: 'R',  LECTEUR: 'R'  },
      products:       { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'R',  FORMATEUR: 'R',  COMPTABLE: 'R',  LECTEUR: 'R'  },
      trainers:       { ADMIN: 'RW', MANAGER: 'RW',                   FORMATEUR: 'R',                   LECTEUR: 'R'  },
      leads:          { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'RW' },
      preenrollments: { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'RW' },
      opcoFiles:      { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'RW',                  COMPTABLE: 'RW', LECTEUR: 'R'  },
      budgetAgefice:  { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'R',                   COMPTABLE: 'RW', LECTEUR: 'R'  },
      invoices:       { ADMIN: 'RW', MANAGER: 'RW',                                    COMPTABLE: 'RW', LECTEUR: 'R'  },
      funders:        { ADMIN: 'R',  MANAGER: 'R',  COMMERCIAL: 'R',                   COMPTABLE: 'R'                },
      organizations:  { ADMIN: 'RW', MANAGER: 'RW', COMMERCIAL: 'RW', FORMATEUR: 'R',  COMPTABLE: 'R',  LECTEUR: 'R'  },
      qualiopiBilan:  { ADMIN: 'RW', MANAGER: 'RW',                   FORMATEUR: 'R',  COMPTABLE: 'R'                },
      tenantSettings: { ADMIN: 'RW' },
      users:          { ADMIN: 'RW' },
      auditHistory:   { ADMIN: 'R'  },
    };

    export function canRead(role: UserRole, section: AppSection): boolean {
      const p = PERMISSIONS[section][role];
      return p === 'R' || p === 'RW';
    }
    export function canWrite(role: UserRole, section: AppSection): boolean {
      return PERMISSIONS[section][role] === 'RW';
    }
    export function rolesWithAccess(section: AppSection, mode: 'R' | 'RW'): UserRole[] {
      return (Object.entries(PERMISSIONS[section]) as [UserRole, Permission][])
        .filter(([, p]) => (mode === 'R' ? (p === 'R' || p === 'RW') : p === 'RW'))
        .map(([r]) => r);
    }
    ```

    Étape 2 — Créer/maj `packages/shared/src/constants/index.ts` :
    ```typescript
    export * from './permissions';
    // (conserver les autres exports si fichier existant — vérifier d'abord avec `ls packages/shared/src/constants/`)
    ```
    Si le dossier n'a pas encore d'index.ts barrel, créer `packages/shared/src/index.ts` qui ré-exporte depuis `./constants/permissions` également (vérifier `packages/shared/src/index.ts` actuel et faire un add ligne dédié).

    Étape 3 — `packages/shared/src/schemas/user.ts` :
    ```typescript
    import { z } from 'zod';

    const userRoleEnum = z.enum(['ADMIN', 'MANAGER', 'FORMATEUR', 'COMMERCIAL', 'COMPTABLE', 'LECTEUR']);

    export const inviteUserSchema = z.object({
      email: z.preprocess(
        (v) => (typeof v === 'string' ? v.trim().toLowerCase() : v),
        z.string().email('Email invalide').max(254),
      ),
      firstName: z.string().min(1, 'Prénom requis').max(80),
      lastName: z.string().min(1, 'Nom requis').max(80),
      role: userRoleEnum,
    });
    export type InviteUserInput = z.infer<typeof inviteUserSchema>;

    export const setPasswordSchema = z.object({
      password: z.string().min(8, '8 caractères minimum').max(128),
      confirm: z.string(),
    }).refine((d) => d.password === d.confirm, {
      message: 'Les mots de passe ne correspondent pas',
      path: ['confirm'],
    });
    export type SetPasswordInput = z.infer<typeof setPasswordSchema>;

    export const changeUserRoleSchema = z.object({
      userId: z.string().uuid('Identifiant invalide'),
      role: userRoleEnum,
    });
    export type ChangeUserRoleInput = z.infer<typeof changeUserRoleSchema>;
    ```

    Étape 4 — Modifier `packages/shared/src/schemas/index.ts` : ajouter `export * from './user';` à la liste.

    Étape 5 — `packages/shared/src/schemas/__tests__/user.test.ts` (Vitest, ≥ 8 tests) :
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { inviteUserSchema, setPasswordSchema, changeUserRoleSchema } from '../user';

    describe('inviteUserSchema', () => {
      it('lowercases email and accepts valid input', () => {
        const r = inviteUserSchema.parse({ email: 'A@B.FR', firstName: 'Jean', lastName: 'Dupont', role: 'COMMERCIAL' });
        expect(r.email).toBe('a@b.fr');
        expect(r.role).toBe('COMMERCIAL');
      });
      it('rejects invalid email', () => {
        expect(() => inviteUserSchema.parse({ email: 'bad', firstName: 'X', lastName: 'Y', role: 'ADMIN' })).toThrow();
      });
      it('rejects empty firstName', () => {
        expect(() => inviteUserSchema.parse({ email: 'a@b.fr', firstName: '', lastName: 'Y', role: 'ADMIN' })).toThrow();
      });
      it('rejects unknown role', () => {
        expect(() => inviteUserSchema.parse({ email: 'a@b.fr', firstName: 'A', lastName: 'B', role: 'GOD' as never })).toThrow();
      });
    });
    describe('setPasswordSchema', () => {
      it('rejects < 8 chars', () => {
        expect(() => setPasswordSchema.parse({ password: 'short', confirm: 'short' })).toThrow();
      });
      it('rejects mismatched confirm', () => {
        expect(() => setPasswordSchema.parse({ password: 'longenough', confirm: 'longenougB' })).toThrow();
      });
      it('accepts matching ≥ 8 chars', () => {
        const r = setPasswordSchema.parse({ password: 'longenough', confirm: 'longenough' });
        expect(r.password).toBe('longenough');
      });
    });
    describe('changeUserRoleSchema', () => {
      it('requires uuid userId', () => {
        expect(() => changeUserRoleSchema.parse({ userId: 'not-uuid', role: 'ADMIN' })).toThrow();
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "export const PERMISSIONS" packages/shared/src/constants/permissions.ts &&
      grep -q "export const inviteUserSchema" packages/shared/src/schemas/user.ts &&
      grep -q "export \* from './user'" packages/shared/src/schemas/index.ts &&
      pnpm --filter @qualiof/shared test --run __tests__/user.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADMIN\|MANAGER\|FORMATEUR\|COMMERCIAL\|COMPTABLE\|LECTEUR" packages/shared/src/constants/permissions.ts` ≥ 30 (≥ 5 occurrences × 6 rôles dans la matrice)
    - `grep -c "PERMISSIONS\[" packages/shared/src/constants/permissions.ts` ≥ 2 (utilisés dans canRead/canWrite)
    - `grep -c "export const" packages/shared/src/schemas/user.ts` ≥ 3 (inviteUserSchema, setPasswordSchema, changeUserRoleSchema)
    - Tests Vitest : `pnpm --filter @qualiof/shared test --run` rapporte 8 passes ≥
    - `grep -q "^export \* from './user'" packages/shared/src/schemas/index.ts` retourne 0 (la ligne existe)
  </acceptance_criteria>
  <done>
    Matrice PERMISSIONS exposée depuis `@qualiof/shared`. 3 schémas Zod exportés. 8 tests Vitest verts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/rbac.ts + extension auth.ts disabledAt</name>
  <read_first>
    - apps/web/src/lib/auth.ts (L25-44 : getUserAttributes + DatabaseUserAttributes typing + validateRequest L47-68)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md Pattern 1 (requireRole code complet) + Pitfall #1 (validateRequest cache + disabledAt)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md D-08 (signature exacte requireRole)
  </read_first>
  <files>
    - apps/web/src/lib/auth.ts (MODIFIER : étendre getUserAttributes + DatabaseUserAttributes + validateRequest)
    - apps/web/src/lib/rbac.ts (CRÉER : requireRole + hasRole + 2 error classes)
    - apps/web/src/lib/__tests__/rbac.test.ts (CRÉER : ≥ 5 tests)
  </files>
  <behavior>
    - `getUserAttributes` returns `disabledAt: Date | null` in addition to existing fields (email, firstName, lastName, role, tenantId).
    - TypeScript `DatabaseUserAttributes` declares `disabledAt: Date | null` in `declare module 'lucia'` block.
    - `validateRequest()` reads `disabledAt` from session.user, and if not null: calls `lucia.invalidateSession(session.id)`, sets blank session cookie, and returns `{ user: null, session: null }`.
    - `requireRole(['ADMIN'])` throws `UnauthorizedError` when validateRequest returns user=null.
    - `requireRole(['ADMIN'])` throws `ForbiddenError` when user.role='LECTEUR' (not in allowed list).
    - `requireRole(['ADMIN'])` returns the user object when user.role='ADMIN'.
    - `hasRole(user, ['ADMIN', 'MANAGER'])` returns true for an ADMIN user, false for COMMERCIAL.
    - `UnauthorizedError` and `ForbiddenError` extend Error with `.name` set correctly.
  </behavior>
  <action>
    Étape 1 — Éditer `apps/web/src/lib/auth.ts` :

    A) Dans `getUserAttributes` (L25-31), AJOUTER `disabledAt: data.disabledAt`.

    B) Dans `declare module 'lucia'` (L34-44), AJOUTER `disabledAt: Date | null;` dans `DatabaseUserAttributes`.

    C) Dans `validateRequest` (L47-68), MODIFIER la logique. Après `const result = await lucia.validateSession(sessionId);` et AVANT le try { } qui gère les cookies, AJOUTER :
    ```typescript
    if (result.user && result.user.disabledAt) {
      await lucia.invalidateSession(result.session!.id);
      try {
        const blank = lucia.createBlankSessionCookie();
        (await cookies()).set(blank.name, blank.value, blank.attributes);
      } catch {
        // ignore — cookies() throws hors Route Handler/Action
      }
      return { user: null, session: null };
    }
    ```
    Important : ce check est APRÈS `validateSession` (sinon on n'a pas l'user). L'ordre est validateSession → check disabledAt → cookies maintenance → return.

    Étape 2 — Créer `apps/web/src/lib/rbac.ts` :
    ```typescript
    /**
     * RBAC guard — Phase 8 (D-08).
     *
     * Pattern d'usage dans une server action :
     *   const admin = await requireRole(['ADMIN']);
     *   // ... admin.id, admin.tenantId, admin.role dispo
     * Catch côté action :
     *   try { ... }
     *   catch (e) {
     *     if (e instanceof UnauthorizedError) return { ok: false, error: e.message };
     *     if (e instanceof ForbiddenError)    return { ok: false, error: e.message };
     *     throw e;
     *   }
     */
    import type { User as LuciaUser } from 'lucia';
    import type { UserRole } from '@prisma/client';
    import { validateRequest } from './auth';

    export class UnauthorizedError extends Error {
      constructor(message = 'Non authentifié') {
        super(message);
        this.name = 'UnauthorizedError';
      }
    }
    export class ForbiddenError extends Error {
      constructor(message = 'Accès refusé') {
        super(message);
        this.name = 'ForbiddenError';
      }
    }

    export async function requireRole(allowed: UserRole[]): Promise<LuciaUser> {
      const { user } = await validateRequest();
      if (!user) throw new UnauthorizedError();
      // validateRequest gère déjà disabledAt → si on arrive ici l'user est actif.
      if (!allowed.includes(user.role as UserRole)) {
        throw new ForbiddenError(`Rôle ${user.role} non autorisé`);
      }
      return user;
    }

    export function hasRole(user: LuciaUser, allowed: UserRole[]): boolean {
      return allowed.includes(user.role as UserRole);
    }
    ```

    Étape 3 — Tests `apps/web/src/lib/__tests__/rbac.test.ts` (Vitest, ≥ 5 tests) :
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    vi.mock('../auth', () => ({
      validateRequest: vi.fn(),
    }));

    import { validateRequest } from '../auth';
    import { requireRole, hasRole, UnauthorizedError, ForbiddenError } from '../rbac';

    describe('requireRole', () => {
      beforeEach(() => { vi.clearAllMocks(); });

      it('throws UnauthorizedError when user is null', async () => {
        vi.mocked(validateRequest).mockResolvedValue({ user: null, session: null });
        await expect(requireRole(['ADMIN'])).rejects.toBeInstanceOf(UnauthorizedError);
      });

      it('throws ForbiddenError when role not allowed', async () => {
        vi.mocked(validateRequest).mockResolvedValue({
          user: { id: 'u1', role: 'LECTEUR', tenantId: 't1', email: 'a', firstName: 'A', lastName: 'B' } as never,
          session: { id: 's' } as never,
        });
        await expect(requireRole(['ADMIN'])).rejects.toBeInstanceOf(ForbiddenError);
      });

      it('returns user when role is allowed', async () => {
        const user = { id: 'u1', role: 'ADMIN', tenantId: 't1', email: 'a', firstName: 'A', lastName: 'B' };
        vi.mocked(validateRequest).mockResolvedValue({ user: user as never, session: { id: 's' } as never });
        const got = await requireRole(['ADMIN']);
        expect(got.id).toBe('u1');
      });
    });

    describe('hasRole', () => {
      it('returns true when role in allowed', () => {
        expect(hasRole({ role: 'ADMIN' } as never, ['ADMIN', 'MANAGER'])).toBe(true);
      });
      it('returns false when role not in allowed', () => {
        expect(hasRole({ role: 'LECTEUR' } as never, ['ADMIN'])).toBe(false);
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "disabledAt: Date | null" apps/web/src/lib/auth.ts &&
      grep -q "result.user.disabledAt" apps/web/src/lib/auth.ts &&
      grep -q "export async function requireRole" apps/web/src/lib/rbac.ts &&
      grep -q "class UnauthorizedError" apps/web/src/lib/rbac.ts &&
      grep -q "class ForbiddenError" apps/web/src/lib/rbac.ts &&
      pnpm --filter @qualiof/web test --run src/lib/__tests__/rbac.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "disabledAt" apps/web/src/lib/auth.ts` ≥ 3 (getUserAttributes + DatabaseUserAttributes typing + check dans validateRequest)
    - `grep -c "lucia.invalidateSession" apps/web/src/lib/auth.ts` ≥ 1 (nouveau call dans check disabledAt)
    - `grep -c "export " apps/web/src/lib/rbac.ts` ≥ 4 (requireRole + hasRole + 2 error classes)
    - Tests Vitest : ≥ 5 passes
    - `pnpm --filter @qualiof/web exec tsc --noEmit` retourne 0 erreur
  </acceptance_criteria>
  <done>
    `requireRole` est utilisable dans toute server action. `validateRequest` rejette automatiquement les users désactivés. 5 tests verts.
  </done>
</task>

</tasks>

<verification>
- Migration appliquée (`prisma migrate status` clean)
- `pnpm --filter @qualiof/web exec tsc --noEmit` retourne 0 erreur (extension `DatabaseUserAttributes` + nouveaux modèles ne cassent rien)
- `pnpm --filter @qualiof/shared test --run` : 8 tests user schemas verts
- `pnpm --filter @qualiof/web test --run src/lib/__tests__/rbac.test.ts` : 5 tests verts
- `pnpm --filter @qualiof/web build` reste vert (extension Lucia compatible)
</verification>

<success_criteria>
- Migration Prisma `phase_08_users_rbac` appliquée
- `@qualiof/db` export type `UserInvitation`
- `@qualiof/shared` exporte `PERMISSIONS`, `canRead`, `canWrite`, `rolesWithAccess`, `inviteUserSchema`, `setPasswordSchema`, `changeUserRoleSchema`
- `apps/web/src/lib/rbac.ts` exporte `requireRole`, `hasRole`, `UnauthorizedError`, `ForbiddenError`
- `validateRequest()` invalide la session des users désactivés en transparent
- 13+ tests Vitest verts au total
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-utilisateurs-et-rbac/08-01-SUMMARY.md`
</output>