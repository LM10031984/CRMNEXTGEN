---
phase: 08-multi-utilisateurs-et-rbac
plan: 06
type: execute
wave: 5
depends_on: [08-05]
files_modified:
  - apps/web/src/server/actions/tenant-settings.ts
  - apps/web/src/server/actions/tenant-assets.ts
  - apps/web/src/server/actions/invoices.ts
  - apps/web/src/server/actions/budget-agefice.ts
  - apps/web/src/server/actions/dossiers-opco.ts
  - apps/web/src/server/actions/dossiers-opco-bulk.ts
  - apps/web/src/server/actions/persons.ts
  - apps/web/src/server/actions/organizations.ts
  - apps/web/src/server/actions/closure-pack.ts
  - apps/web/src/server/actions/sessions-create.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [RBAC-04]
must_haves:
  truths:
    - "All mutations in tenant-settings.ts replace `const { user } = await validateRequest()` + 'if (!user)' check with `const user = await requireRole(['ADMIN'])` (4 actions)"
    - "All mutations in tenant-assets.ts use requireRole(['ADMIN']) (4 upload + 2 reset actions)"
    - "Mutations in invoices.ts (createInvoiceFromParticipant, createInvoiceForSponsorGroup, recordInvoicePayment) use requireRole(['ADMIN','MANAGER','COMPTABLE'])"
    - "Mutations in budget-agefice.ts (write actions only — listLearnersWithAgeficeBudget and getAgeficeBudgetSummary remain validateRequest as they are read) use requireRole(['ADMIN','MANAGER','COMPTABLE','COMMERCIAL'])"
    - "Destructive deletes in persons.ts / organizations.ts / sessions-create.ts (delete endpoints) use requireRole(['ADMIN','MANAGER'])"
    - "closure-pack.ts mutations use requireRole(['ADMIN','MANAGER','FORMATEUR'])"
    - "Each modified action catches UnauthorizedError/ForbiddenError and returns { ok: false, error: e.message } (consistent with Phase 7 + 08-02 pattern)"
    - "ROADMAP.md marks Phase 8 plans 01..06 checked; STATE.md updated; REQUIREMENTS.md RBAC-01..05 marked [x] with commit refs"
    - "pnpm --filter @qualiof/web build returns 0 errors, pnpm --filter @qualiof/web test --run all green, pnpm --filter @qualiof/web exec tsc --noEmit clean"
  artifacts:
    - path: "apps/web/src/server/actions/tenant-settings.ts"
      provides: "ADMIN-only guard applied"
      contains: "requireRole"
    - path: "apps/web/src/server/actions/invoices.ts"
      provides: "ADMIN+MANAGER+COMPTABLE guard"
      contains: "requireRole"
    - path: ".planning/STATE.md"
      provides: "Phase 8 closed entry"
      contains: "Phase 8 closed"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 marked complete with plan checks"
      contains: "[x] **Phase 8"
  key_links:
    - from: "apps/web/src/server/actions/tenant-settings.ts"
      to: "apps/web/src/lib/rbac.ts"
      via: "requireRole(['ADMIN'])"
      pattern: "requireRole"
    - from: "apps/web/src/server/actions/invoices.ts"
      to: "apps/web/src/lib/rbac.ts"
      via: "requireRole(['ADMIN','MANAGER','COMPTABLE'])"
      pattern: "requireRole"
---

<objective>
Appliquer `requireRole` sur les server actions sensibles existantes (au-delà de `validateRequest`), conformément à la matrice D-02 + D-08 : ADMIN-only sur tenant-settings/tenant-assets ; ADMIN+MANAGER+COMPTABLE sur factures ; ADMIN+MANAGER+COMPTABLE+COMMERCIAL sur budget AGEFICE ; ADMIN+MANAGER sur destructions ; etc. PUIS bookkeeping fin de phase : ROADMAP plans cochés, REQUIREMENTS RBAC-01..05 marqués livrés, STATE.md mis à jour, smoke build + tests.

Purpose: livrer RBAC-04 (guards effectifs côté serveur) + fermer formellement la Phase 8. Tous les server actions sensibles sont protégés par rôle, pas seulement par auth.
Output: ~10 fichiers server-actions modifiés (1 ligne par action — remplacer validateRequest par requireRole) + 3 docs `.planning/` mises à jour + verification gates verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-01-PLAN.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-02-PLAN.md

@apps/web/src/server/actions/tenant-settings.ts
@apps/web/src/server/actions/tenant-assets.ts
@apps/web/src/server/actions/invoices.ts
@apps/web/src/server/actions/budget-agefice.ts
@apps/web/src/server/actions/persons.ts
@apps/web/src/server/actions/organizations.ts
@apps/web/src/server/actions/dossiers-opco.ts
@apps/web/src/server/actions/closure-pack.ts
@apps/web/src/server/actions/sessions-create.ts

<interfaces>
<!-- From 08-01 -->

```typescript
import { requireRole, UnauthorizedError, ForbiddenError } from '@/lib/rbac';

// Usage in server action:
export async function doSomething(...): Promise<ActionResult> {
  try {
    const user = await requireRole(['ADMIN']);
    // ... user.id, user.tenantId disponibles directement
  } catch (e) {
    if (e instanceof UnauthorizedError || e instanceof ForbiddenError) {
      return { ok: false, error: e.message };
    }
    throw e;
  }
  // ... reste de l'action
}
```

Replaces pattern Phase 7 (tenant-settings.ts L48-49):
```typescript
const { user } = await validateRequest();
if (!user) return { ok: false, error: 'Non authentifié' };
```

D-08 mapping per action file:
- tenant-settings.ts : 4 actions → ADMIN
- tenant-assets.ts : 4 upload + 2 reset → ADMIN
- invoices.ts : 3 mutations (create×2 + recordPayment) → ADMIN, MANAGER, COMPTABLE
- budget-agefice.ts : write actions only → ADMIN, MANAGER, COMPTABLE, COMMERCIAL (read actions stay validateRequest)
- persons.ts (deletes) → ADMIN, MANAGER
- organizations.ts (deletes) → ADMIN, MANAGER
- sessions-create.ts (createSession/updateSession/deleteSession) → ADMIN, MANAGER (delete) + ADMIN/MANAGER/COMMERCIAL (create/update)
- closure-pack.ts mutations → ADMIN, MANAGER, FORMATEUR (FORMATEUR peut declencher closure pour ses sessions)
- dossiers-opco.ts mutations → ADMIN, MANAGER, COMMERCIAL, COMPTABLE
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply requireRole on sensitive server actions (audit scope first, then replace)</name>
  <read_first>
    - apps/web/src/server/actions/tenant-settings.ts (4 mutations — pattern Phase 7)
    - apps/web/src/server/actions/tenant-assets.ts (4+2 actions)
    - apps/web/src/server/actions/invoices.ts (L30-450 — 3 mutations à protéger)
    - apps/web/src/server/actions/budget-agefice.ts (separate read vs write — laisser validateRequest sur les reads)
    - apps/web/src/server/actions/persons.ts (chercher actions deleteX)
    - apps/web/src/server/actions/organizations.ts (idem)
    - apps/web/src/server/actions/dossiers-opco.ts + dossiers-opco-bulk.ts
    - apps/web/src/server/actions/sessions-create.ts (createSession/updateSession/deleteSession L40+, L79+, L113+, L231+)
    - apps/web/src/server/actions/closure-pack.ts (5 actions L60+, L255+, L315+, L416+, L455+)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md D-02 + D-08
  </read_first>
  <files>
    - apps/web/src/server/actions/tenant-settings.ts (MODIFIER : 4 actions → requireRole(['ADMIN']))
    - apps/web/src/server/actions/tenant-assets.ts (MODIFIER : 6 actions → requireRole(['ADMIN']))
    - apps/web/src/server/actions/invoices.ts (MODIFIER : 3 actions → requireRole(['ADMIN','MANAGER','COMPTABLE']))
    - apps/web/src/server/actions/budget-agefice.ts (MODIFIER : actions write uniquement → requireRole(['ADMIN','MANAGER','COMPTABLE','COMMERCIAL']))
    - apps/web/src/server/actions/persons.ts (MODIFIER : deletes → requireRole(['ADMIN','MANAGER']))
    - apps/web/src/server/actions/organizations.ts (MODIFIER : deletes → requireRole(['ADMIN','MANAGER']))
    - apps/web/src/server/actions/sessions-create.ts (MODIFIER : deleteSession → requireRole(['ADMIN','MANAGER']) ; createSession/updateSession → requireRole(['ADMIN','MANAGER','COMMERCIAL']))
    - apps/web/src/server/actions/closure-pack.ts (MODIFIER : 5 mutations → requireRole(['ADMIN','MANAGER','FORMATEUR']))
    - apps/web/src/server/actions/dossiers-opco.ts + dossiers-opco-bulk.ts (MODIFIER : mutations → requireRole(['ADMIN','MANAGER','COMMERCIAL','COMPTABLE']))
  </files>
  <behavior>
    - Pour chaque fichier, scope d'abord les exports (commencer par `grep -n "^export async function" <file>`) puis classer chacun en "read" (laisser validateRequest) ou "write" (passer à requireRole).
    - Le pattern de remplacement (1 action à la fois) :
      AVANT :
      ```typescript
      export async function X(...): Promise<...> {
        const { user } = await validateRequest();
        if (!user) return { ok: false, error: 'Non authentifié' };
        // ...
      }
      ```
      APRÈS :
      ```typescript
      export async function X(...): Promise<...> {
        let user;
        try {
          user = await requireRole(['ADMIN', /* etc. */]);
        } catch (e) {
          if (e instanceof UnauthorizedError || e instanceof ForbiddenError) {
            return { ok: false, error: e.message };
          }
          throw e;
        }
        // ...
      }
      ```
      OU plus concis (pattern wrapper, cf 08-02 task 2 inviteUser) :
      ```typescript
      try {
        const user = await requireRole(['ADMIN', ...]);
        // ... tout le corps
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
      ```
    - Pour les actions retournant `Promise<void>` (e.g. delete server actions sans `{ ok, ... }`) ou utilisant un autre type de retour : conserver le throw (laisser propager UnauthorizedError/ForbiddenError) — Next.js error.tsx capturera.
    - Pour les actions actuellement non protégées (rares — la plupart ont validateRequest), AJOUTER requireRole en début.
    - IMPORTANT : ne PAS toucher aux actions de read (queries) — `listLearnersWithAgeficeBudget`, `getAgeficeBudgetSummary`, etc. — qui doivent rester ouvertes selon la matrice D-02 (R sur de nombreux rôles).
  </behavior>
  <action>
    Étape 1 — `tenant-settings.ts` (4 mutations : updateTenantIdentity, updateTenantAddress, updateTenantBilling, updateTenantEmail) :
    - Importer `requireRole, UnauthorizedError, ForbiddenError` depuis '@/lib/rbac' en haut.
    - Pour chaque action, remplacer le bloc `const { user } = await validateRequest(); if (!user) return ...;` par le pattern requireRole(['ADMIN']) avec try/catch.
    - Garder `import { validateRequest } from '@/lib/auth';` SEULEMENT si une autre fonction du fichier l'utilise. Sinon, supprimer l'import.

    Étape 2 — `tenant-assets.ts` (uploadTenantLogo, uploadTenantSignature, resetTenantLogo, resetTenantSignature ; possiblement +2 variantes) :
    - Même pattern, requireRole(['ADMIN']).

    Étape 3 — `invoices.ts` (createInvoiceFromParticipant L30, createInvoiceForSponsorGroup L164, recordInvoicePayment L312 — vérifier via grep) :
    - requireRole(['ADMIN', 'MANAGER', 'COMPTABLE']).
    - Les autres exports (queries, getInvoice, listInvoices, etc.) : laisser validateRequest (lecture).

    Étape 4 — `budget-agefice.ts` :
    - `grep -n "^export async function" budget-agefice.ts` pour identifier la liste.
    - Read actions (listLearnersWithAgeficeBudget L31, getAgeficeAvailableYears L143, getAgeficeBudgetSummary L172) → laisser validateRequest.
    - Write actions (s'il y en a — vérifier upload de fiche AGEFICE, save consumption manuel, etc.) → requireRole(['ADMIN','MANAGER','COMPTABLE','COMMERCIAL']).

    Étape 5 — `persons.ts` :
    - `grep -n "^export async function" persons.ts` → identifier deletePerson / softDeletePerson / archivePerson.
    - Pour TOUS les deletes/archive : requireRole(['ADMIN', 'MANAGER']).
    - Pour create/update : laisser validateRequest (D-02 learners: COMMERCIAL=RW).

    Étape 6 — `organizations.ts` : idem persons.ts. deletes → ADMIN+MANAGER.

    Étape 7 — `sessions-create.ts` :
    - createSession L40, updateSession L79, etc. → requireRole(['ADMIN', 'MANAGER', 'COMMERCIAL']).
    - deleteSession → requireRole(['ADMIN', 'MANAGER']).

    Étape 8 — `closure-pack.ts` :
    - Toutes les 5 mutations (queueClosureBatch, regenerateDocument, downloadPack, etc.) → requireRole(['ADMIN', 'MANAGER', 'FORMATEUR']).

    Étape 9 — `dossiers-opco.ts` + `dossiers-opco-bulk.ts` :
    - Mutations (createDossier, updateDossier, submitDossier, bulkSubmit, etc.) → requireRole(['ADMIN', 'MANAGER', 'COMMERCIAL', 'COMPTABLE']).
    - Read actions : laisser validateRequest.

    Étape 10 — Vérifier que tous les fichiers compilent (`pnpm --filter @qualiof/web exec tsc --noEmit`). Si erreurs de typage sur le retour de `requireRole(...)` (Lucia User vs ce que l'action attend), ajuster le type ou casts si nécessaire.
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      # Count requireRole usages — should hit at least 25 total across modified files
      RR_COUNT=$(grep -rc "requireRole(\\[" apps/web/src/server/actions/tenant-settings.ts apps/web/src/server/actions/tenant-assets.ts apps/web/src/server/actions/invoices.ts apps/web/src/server/actions/persons.ts apps/web/src/server/actions/organizations.ts apps/web/src/server/actions/sessions-create.ts apps/web/src/server/actions/closure-pack.ts apps/web/src/server/actions/dossiers-opco.ts 2>/dev/null | awk -F: '{s+=$2} END {print s}') &&
      [ "$RR_COUNT" -ge 20 ] &&
      # ADMIN-only sur tenant-settings + tenant-assets
      grep -q "requireRole(\\['ADMIN'\\])" apps/web/src/server/actions/tenant-settings.ts &&
      grep -q "requireRole(\\['ADMIN'\\])" apps/web/src/server/actions/tenant-assets.ts &&
      # Multi-role sur invoices
      grep -E "requireRole\\(\\['ADMIN', ?'MANAGER', ?'COMPTABLE'\\]\\)" apps/web/src/server/actions/invoices.ts &&
      # tests + build verts
      pnpm --filter @qualiof/web exec tsc --noEmit &&
      pnpm --filter @qualiof/web test --run &&
      pnpm --filter @qualiof/shared test --run &&
      pnpm --filter @qualiof/web build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "requireRole(\\[" apps/web/src/server/actions/` ≥ 20 (cumul à travers les fichiers modifiés)
    - `grep -l "validateRequest" apps/web/src/server/actions/tenant-settings.ts` retourne 0 ou seulement utilisé par des reads — `grep -c "validateRequest" apps/web/src/server/actions/tenant-settings.ts` ≤ 1 OR 0 (4 mutations remplacées)
    - `grep -c "requireRole" apps/web/src/server/actions/invoices.ts` ≥ 3 (3 mutations protégées)
    - Build Next.js OK
    - Vitest full suite vert (`pnpm --filter @qualiof/web test --run` rapporte 0 fail)
    - `tsc --noEmit` clean
  </acceptance_criteria>
  <done>
    Tous les server actions sensibles sont gardés par rôle effectif. Build + tests verts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bookkeeping ROADMAP / REQUIREMENTS / STATE</name>
  <read_first>
    - .planning/REQUIREMENTS.md L51-55 (RBAC-01..05 actuels)
    - .planning/ROADMAP.md L117-127 (Phase 8 section)
    - .planning/STATE.md (frontmatter + Last session)
    - .planning/phases/07-param-tres-organisme-ditables/07-05-SUMMARY.md (pattern bookkeeping fin de phase)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md (MODIFIER : marquer RBAC-01..05 [x] avec refs et résumé)
    - .planning/ROADMAP.md (MODIFIER : Phase 8 [x] + bloc Plans avec 6 cases cochées + Plans Complete 6/6)
    - .planning/STATE.md (MODIFIER : current_position, progress, accumulated context, last session)
  </files>
  <behavior>
    - REQUIREMENTS.md lines for RBAC-01..05 changed from `- [ ] **RBAC-X** : ...` to `- [x] **RBAC-X** : ... **DONE 2026-05-15** — <one-line summary> ; refs : Plans 08-01/02/...`.
    - ROADMAP.md Phase 8 line `- [ ] **Phase 8:** ...` → `- [x] **Phase 8:** ...`.
    - ROADMAP.md Phase 8 section `**Plans**: TBD` → bloc :
      ```
      **Plans:** 6/6 plans complete
      ...
      **Plans**:
      - [x] 08-01-PLAN.md — Foundation: migration Prisma + permissions matrix + rbac helper + auth.ts disabledAt
      - [x] 08-02-PLAN.md — Server actions tenant-users (6 exports) + email templates + audit-log logUserAction helper
      - [x] 08-03-PLAN.md — Public /invitation/[token] page + acceptInvitation server action (single-use)
      - [x] 08-04-PLAN.md — UI page /app/parametres/utilisateurs + sidebar filterNavForRole D-07
      - [x] 08-05-PLAN.md — UI page /app/parametres/historique + login hooks (auth.login.success/failed + lastLoginAt)
      - [x] 08-06-PLAN.md — Apply requireRole sur server actions sensibles + bookkeeping
      ```
    - ROADMAP.md progress table : `| 8. Multi-utilisateurs et RBAC | 6/6 | Complete | 2026-05-XX |`.
    - STATE.md frontmatter `progress.completed_phases: 4`, `progress.total_plans: ?`, `progress.completed_plans: 19` (13 + 6).
    - STATE.md `Current Position` updated to Phase 9 or "Phase 8 closed, awaiting next instruction".
    - STATE.md `Roadmap Evolution` gains a new bullet for 2026-05-XX summarizing phase 8 closure.
    - STATE.md `Last session` line: `Stopped at: Phase 8 closed. Prochaine étape : /gsd:plan-phase 9.`
  </behavior>
  <action>
    Étape 1 — REQUIREMENTS.md, remplacer L51-55 :
    ```markdown
    - [x] **RBAC-01** : Page Utilisateurs dans Paramètres. **DONE 2026-05-15** — page `/app/parametres/utilisateurs` ADMIN-only (requireRole). Liste users tenant (Email · Nom · Rôle · Statut · Dernière connexion · Actions), bouton "Inviter un utilisateur" (Radix Dialog + RHF + zodResolver), row actions DropdownMenu (Modifier rôle / Reset MDP / Désactiver|Réactiver / Renvoyer invitation). Self-protection (admin ne peut pas se désactiver ni se retirer ADMIN). Plans 08-02 + 08-04.
    - [x] **RBAC-02** : Flow invitation par email. **DONE 2026-05-15** — server action `inviteUser` crée User (hashedPwd='') + UserInvitation (token random 32 hex, 7d expiry) en transaction + envoie email (template HTML + text dans `lib/mailer-templates/user-invitation.ts`). Route publique `/invitation/[token]` (Server Component force-dynamic, 3 états : expired / used / fresh). `acceptInvitation` server action atomique single-use (updateMany usedAt=null + expiresAt>now), Argon2 hash, lucia.createSession, redirect /app. AuditLog `users.invite` + `users.password.set`. Plans 08-02 + 08-03.
    - [x] **RBAC-03** : Permissions effectives par rôle (sidebar filtrée). **DONE 2026-05-15** — `nav-config.ts` étendue (NavItem.allowedRoles?: UserRole[]), `filterNavForRole(NAV, role)` pure fn ; layout `app/app/layout.tsx` applique filtre côté serveur + propage `nav` en prop à Sidebar/SidebarNav/MobileNavDrawer (plus d'import statique NAV côté client). Matrice D-02 traduite en `packages/shared/src/constants/permissions.ts` (PERMISSIONS + canRead/canWrite/rolesWithAccess). Plans 08-01 + 08-04.
    - [x] **RBAC-04** : Guards par rôle dans server actions sensibles. **DONE 2026-05-15** — helper `lib/rbac.ts` (`requireRole(allowed) → User`, `UnauthorizedError`, `ForbiddenError`). Appliqué dans tenant-settings (4 actions ADMIN), tenant-assets (6 actions ADMIN), invoices (3 mutations ADMIN+MANAGER+COMPTABLE), budget-agefice writes (ADMIN+MANAGER+COMPTABLE+COMMERCIAL), persons/organizations/sessions deletes (ADMIN+MANAGER), closure-pack (ADMIN+MANAGER+FORMATEUR), dossiers-opco mutations (ADMIN+MANAGER+COMMERCIAL+COMPTABLE). validateRequest étendu pour invalider la session si user.disabledAt. Plans 08-01 + 08-06.
    - [x] **RBAC-05** : AuditLog UI admin. **DONE 2026-05-15** — page `/app/parametres/historique` ADMIN-only, filtres URL state (qui/quand/action) + pagination offset 50/page + Radix Dialog diff modal (render 2 cols Avant/Après si shape before/after, sinon JSON pretty). Pure helper `buildAuditWhere`. login hooks ajoutés dans `app/login/actions.ts` : success → AuditLog `auth.login.success` + user.lastLoginAt updated ; failed (3 raisons : user_not_found / disabled / bad_password) → AuditLog `auth.login.failed`. Plans 08-02 + 08-05.
    ```

    Étape 2 — ROADMAP.md, section Phase 8 :
    - Remplacer `- [ ] **Phase 8:** ...` (L20) par `- [x] **Phase 8:** Multi-utilisateurs et RBAC - Invitation users + permissions effectives par rôle`.
    - Dans `### Phase 8: Multi-utilisateurs et RBAC` (L117), remplacer `**Plans**: TBD` (L127) par :
      ```
      **Plans:** 6/6 plans complete
      **Plans**:
      - [x] 08-01-PLAN.md — Foundation: migration Prisma User+UserInvitation + permissions matrix D-02 + rbac.ts (requireRole/hasRole/UnauthorizedError/ForbiddenError) + auth.ts disabledAt + Zod schemas user
      - [x] 08-02-PLAN.md — Server actions tenant-users.ts (inviteUser/disableUser/enableUser/resetUserPassword/changeUserRole/resendInvitation) + email templates (user-invitation, user-password-reset) + logUserAction helper
      - [x] 08-03-PLAN.md — Public /invitation/[token] page + SetPasswordForm + acceptInvitation server action (atomic single-use)
      - [x] 08-04-PLAN.md — UI page /app/parametres/utilisateurs (UsersTable + InviteUserButton + UserRowActions + ChangeRoleDialog) + sidebar filterNavForRole + propagation nav en prop
      - [x] 08-05-PLAN.md — UI page /app/parametres/historique (AuditLogFilters URL state + AuditDiffModal) + login hooks auth.login.success/failed + lastLoginAt + buildAuditWhere pure fn
      - [x] 08-06-PLAN.md — Apply requireRole sur server actions sensibles (tenant-settings/assets/invoices/budget-agefice/persons/organizations/sessions/closure-pack/dossiers-opco) + bookkeeping fin de phase
      ```
    - Dans la table progress (L180+), remplacer la ligne `| 8. Multi-utilisateurs et RBAC | 0/TBD | Not started | - |` par `| 8. Multi-utilisateurs et RBAC | 6/6 | Complete | 2026-05-15 |`.

    Étape 3 — STATE.md :
    - Frontmatter : `completed_phases: 4`, `completed_plans: 19` (13 + 6), `total_plans: 31` (25 + 6), `stopped_at: "Phase 8 closed. Prochaine étape : /gsd:plan-phase 9."`, `last_updated: "2026-05-15T..."` (use current date).
    - `## Current Position` : `Phase: 9` ou `Phase: 8 (closed)`, `Plan: Not started`.
    - `### Roadmap Evolution` : AJOUTER un bullet `2026-05-15 — Phase 8 closed : RBAC-01/02/03/04/05 livrés. 6 plans exécutés. (1) 08-01 fondations (migration Prisma User +4 cols nullables + nouveau model UserInvitation + lib/rbac.ts requireRole + auth.ts validateRequest invalide disabledAt + matrice PERMISSIONS shared). (2) 08-02 tenant-users.ts 6 actions admin + email templates HTML/text + logUserAction helper. (3) 08-03 /invitation/[token] public + acceptInvitation single-use atomique + Argon2 + auto-login. (4) 08-04 UI Utilisateurs (UsersTable + 4 client components Radix Dialog/DropdownMenu) + sidebar filterNavForRole propagé en prop. (5) 08-05 page Historique (filtres URL state + Radix Dialog diff modal + buildAuditWhere pure fn) + login hooks auth.login.success/failed + lastLoginAt. (6) 08-06 requireRole appliqué sur ~10 fichiers server-actions + bookkeeping. Conventions AuditLog complétées : users.* + auth.* en plus de parameters.* (Phase 7).`
    - `### Key Decisions Recorded` : AJOUTER les décisions D-01..D-10 si pertinentes.
    - `## Last session` : `Stopped at: Phase 8 closed. Prochaine étape : /gsd:plan-phase 9.`
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "\\[x\\] \\*\\*RBAC-01\\*\\*" .planning/REQUIREMENTS.md &&
      grep -q "\\[x\\] \\*\\*RBAC-05\\*\\*" .planning/REQUIREMENTS.md &&
      grep -q "\\[x\\] \\*\\*Phase 8" .planning/ROADMAP.md &&
      grep -q "08-06-PLAN.md" .planning/ROADMAP.md &&
      grep -q "Phase 8 closed" .planning/STATE.md &&
      grep -q "completed_phases: 4" .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[x\\] \\*\\*RBAC-" .planning/REQUIREMENTS.md` ≥ 5
    - `grep -c "\\[x\\] 08-0[1-6]-PLAN.md" .planning/ROADMAP.md` = 6
    - `grep -q "completed_phases: 4" .planning/STATE.md`
    - STATE.md `Last session` mentions "Phase 9" ou indique la suite
  </acceptance_criteria>
  <done>
    Phase 8 formellement fermée dans la doc. STATE pointe vers Phase 9.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Smoke final + verification gates</name>
  <read_first>
    - apps/web/src/app/app/parametres/utilisateurs/page.tsx
    - apps/web/src/app/app/parametres/historique/page.tsx
    - apps/web/src/app/invitation/[token]/page.tsx
  </read_first>
  <files>
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-SMOKE.md (CRÉER : check-list manuelle + résultats build/tests)
  </files>
  <behavior>
    - SMOKE.md documents que :
      - `pnpm --filter @qualiof/web build` réussit (timestamp + version)
      - `pnpm --filter @qualiof/web test --run` réussit (N tests verts)
      - `pnpm --filter @qualiof/shared test --run` réussit
      - `pnpm --filter @qualiof/web exec tsc --noEmit` clean
      - `pnpm --filter @qualiof/db prisma migrate status` clean
      - Manuel checklist : 6 scénarios cochés ou TODO
  </behavior>
  <action>
    Étape 1 — Lancer les commandes :
    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files"
    pnpm --filter @qualiof/web exec tsc --noEmit
    pnpm --filter @qualiof/web test --run
    pnpm --filter @qualiof/shared test --run
    pnpm --filter @qualiof/web build
    pnpm --filter @qualiof/db prisma migrate status
    ```
    Capturer le nombre de tests verts + tout WARN.

    Étape 2 — Créer `.planning/phases/08-multi-utilisateurs-et-rbac/08-SMOKE.md` :
    ```markdown
    # Phase 8 — Smoke Verification

    **Date :** 2026-05-15
    **Status :** PASS / NEEDS-RETRY (à compléter selon résultats)

    ## Automated gates

    | Command | Result | Notes |
    |---------|--------|-------|
    | `pnpm --filter @qualiof/web exec tsc --noEmit` | ✅ / ❌ | 0 erreur |
    | `pnpm --filter @qualiof/web test --run` | ✅ N passes / ❌ | suite complète |
    | `pnpm --filter @qualiof/shared test --run` | ✅ N passes / ❌ | schémas user + autres |
    | `pnpm --filter @qualiof/web build` | ✅ / ❌ | Next.js compile |
    | `pnpm --filter @qualiof/db prisma migrate status` | ✅ / ❌ | Database schema is up to date |

    ## Manual checklist (à valider par Laurent)

    - [ ] Connecté en ADMIN → voir item "Utilisateurs" + "Historique" + "Paramètres" dans sidebar
    - [ ] Inviter un user TEST (rôle COMMERCIAL) → en dev (SMTP vide) → toast indique "mode dev" + lien copiable depuis console serveur
    - [ ] Coller le lien `/invitation/{token}` dans nav privée → définir MDP → redirect /app ✓
    - [ ] Connecter en COMMERCIAL TEST → vérifier que "Paramètres", "Utilisateurs", "Historique" et "Factures" SONT CACHÉS dans la sidebar
    - [ ] Connecter en COMMERCIAL TEST → taper directement `/app/parametres/utilisateurs` → page d'erreur (ForbiddenError)
    - [ ] Retour ADMIN → désactiver le user COMMERCIAL TEST → le user TEST est immédiatement déconnecté (next navigation = /login)
    - [ ] Page Historique : voir lignes parameters.* (Phase 7) + users.invite + users.disable + auth.login.success/failed

    ## Wave timeline

    - 08-01 : Wave 1 (foundation, blocking)
    - 08-02 : Wave 2 (server actions)
    - 08-03 + 08-04 : Wave 3 (parallèle : public route + UI users)
    - 08-05 : Wave 4 (UI historique + login hooks)
    - 08-06 : Wave 5 (apply requireRole + bookkeeping)
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      ls .planning/phases/08-multi-utilisateurs-et-rbac/08-SMOKE.md &&
      pnpm --filter @qualiof/web exec tsc --noEmit &&
      pnpm --filter @qualiof/web test --run &&
      pnpm --filter @qualiof/shared test --run &&
      pnpm --filter @qualiof/web build
    </automated>
  </verify>
  <acceptance_criteria>
    - 08-SMOKE.md existe
    - 5 commandes verification verts (tsc, web test, shared test, build, prisma status)
  </acceptance_criteria>
  <done>
    Phase 8 verrouillée : tous les gates verts, manuel checklist en place pour Laurent.
  </done>
</task>

</tasks>

<verification>
- requireRole appliqué sur ≥ 20 actions de mutation (cumul)
- ROADMAP / REQUIREMENTS / STATE mis à jour pour Phase 8 closure
- SMOKE.md créé avec résultats des 5 gates
- Build + tests verts
</verification>

<success_criteria>
- RBAC-04 livré : guards rôle sur server actions sensibles
- 5 RBAC requirements marqués [x] dans REQUIREMENTS.md
- 6 plans 08-* marqués [x] dans ROADMAP.md
- Progress STATE : 4 phases complete / 19 plans complete
- Tests Vitest verts (full suite)
- Build Next.js vert
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-utilisateurs-et-rbac/08-06-SUMMARY.md`
</output>