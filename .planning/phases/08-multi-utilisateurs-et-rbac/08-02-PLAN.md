---
phase: 08-multi-utilisateurs-et-rbac
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/web/src/lib/audit-log.ts
  - apps/web/src/lib/mailer-templates/user-invitation.ts
  - apps/web/src/lib/mailer-templates/user-password-reset.ts
  - apps/web/src/server/actions/tenant-users.ts
  - apps/web/src/server/actions/__tests__/tenant-users.test.ts
autonomous: true
requirements: [RBAC-01, RBAC-02]
must_haves:
  truths:
    - "Server action inviteUser creates a User (hashedPwd='') + UserInvitation in a single Prisma $transaction, sends an invitation email, and writes AuditLog action='users.invite'"
    - "Server action disableUser updates user.disabledAt=now() AND calls lucia.invalidateUserSessions(userId) AND writes AuditLog action='users.disable'"
    - "Server action enableUser sets user.disabledAt=null AND writes AuditLog action='users.enable'"
    - "Server action resetUserPassword creates a new UserInvitation (7d expiry), sends email, writes AuditLog action='users.password.reset_requested'"
    - "Server action changeUserRole updates user.role AND writes AuditLog action='users.role.change' with diff {role: {before, after}}"
    - "Server action resendInvitation regenerates token + sends email + writes AuditLog action='users.invitation.resend'"
    - "All server actions in tenant-users.ts call requireRole(['ADMIN']) first"
    - "Email templates render HTML + text bodies using OfConfig (loadOfConfig) and respect Phase 7 tenant.emailFrom"
  artifacts:
    - path: "apps/web/src/server/actions/tenant-users.ts"
      provides: "All user lifecycle server actions"
      exports: ["inviteUser", "disableUser", "enableUser", "resetUserPassword", "changeUserRole", "resendInvitation"]
    - path: "apps/web/src/lib/mailer-templates/user-invitation.ts"
      provides: "Email template for invitation"
      exports: ["renderInvitationEmail"]
    - path: "apps/web/src/lib/mailer-templates/user-password-reset.ts"
      provides: "Email template for admin-triggered password reset"
      exports: ["renderPasswordResetEmail"]
    - path: "apps/web/src/lib/audit-log.ts"
      provides: "Extended with logUserAction helper"
      contains: "logUserAction"
  key_links:
    - from: "apps/web/src/server/actions/tenant-users.ts"
      to: "apps/web/src/lib/rbac.ts"
      via: "import { requireRole, UnauthorizedError, ForbiddenError }"
      pattern: "requireRole\\(\\['ADMIN'\\]\\)"
    - from: "apps/web/src/server/actions/tenant-users.ts"
      to: "apps/web/src/lib/mailer.ts"
      via: "import { sendMail }"
      pattern: "sendMail"
    - from: "apps/web/src/server/actions/tenant-users.ts"
      to: "lucia.invalidateUserSessions"
      via: "disableUser invalidates all sessions"
      pattern: "invalidateUserSessions"
    - from: "apps/web/src/server/actions/tenant-users.ts"
      to: "prisma.auditLog.create"
      via: "every mutation logs to AuditLog"
      pattern: "users\\.(invite|disable|enable|password|role|invitation)"
---

<objective>
Implémenter le cycle de vie des utilisateurs côté serveur : 6 server actions ADMIN-only (`inviteUser`, `disableUser`, `enableUser`, `resetUserPassword`, `changeUserRole`, `resendInvitation`) + 2 templates email (invitation et password reset) + extension du helper AuditLog Phase 7 pour les actions `users.*`.

Purpose: poser le contrat des mutations users que les UI (08-04 page utilisateurs) et la route publique (08-03 /invitation/[token]) vont consommer. Toutes les actions retournent `{ ok: true, ... }` ou `{ ok: false, error, fieldErrors? }` (pattern Phase 7).
Output: 1 server action file (6 exports), 2 email templates, extension de audit-log.ts, 1 fichier de tests Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-01-PLAN.md

@apps/web/src/server/actions/tenant-settings.ts
@apps/web/src/lib/audit-log.ts
@apps/web/src/lib/mailer.ts
@apps/web/src/lib/preinscription-reminder-template.ts
@apps/web/src/lib/of-config.ts

<interfaces>
<!-- Disponibles depuis 08-01 (assumés mergés avant ce plan) -->

From apps/web/src/lib/rbac.ts (08-01):
```typescript
export class UnauthorizedError extends Error { /* ... */ }
export class ForbiddenError extends Error { /* ... */ }
export async function requireRole(allowed: UserRole[]): Promise<LuciaUser>;
export function hasRole(user: LuciaUser, allowed: UserRole[]): boolean;
```

From @qualiof/shared (08-01):
```typescript
export const inviteUserSchema: z.ZodSchema;  // { email, firstName, lastName, role }
export type InviteUserInput = z.infer<typeof inviteUserSchema>;
export const changeUserRoleSchema: z.ZodSchema;  // { userId, role }
export type ChangeUserRoleInput = z.infer<typeof changeUserRoleSchema>;
```

From @qualiof/db (08-01):
- prisma.user — étendu avec disabledAt, lastLoginAt, invitedAt, invitedBy
- prisma.userInvitation — nouveau model avec token unique, expiresAt, usedAt, userId, invitedBy

Existing helpers:
- `apps/web/src/lib/auth.ts` exports `lucia` (with `invalidateUserSessions(userId)` method) and `validateRequest`
- `apps/web/src/lib/audit-log.ts` exports `computeDiff(before, after)` and `logTenantSettingsChange({ tenantId, userId, action, diff })`
- `apps/web/src/lib/mailer.ts` exports `sendMail({ to, subject, html, text })` returning `{ ok, dryRun?, error? }` (dry-run when SMTP_HOST is empty)
- `apps/web/src/lib/of-config.ts` exports `loadOfConfig(tenantId): Promise<OfConfig>` (Phase 7) returning { emailFrom, name, address, logoPath, ... }

Pattern Phase 7 server action (tenant-settings.ts L46-81) :
1. `const { user } = await validateRequest()` → Plan 08-02 remplace par `requireRole(['ADMIN'])`
2. `parsed = schema.safeParse(input)` puis `if (!parsed.success) return { ok: false, error, fieldErrors }`
3. `before = await prisma.X.findUnique({ where, select })`
4. `after = await prisma.X.update({ where: { id: user.tenantId }, data, select })`
5. `await logTenantSettingsChange({ ... })`
6. `revalidatePath('/app/parametres')`
7. `return { ok: true }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Email templates + audit-log helper extension</name>
  <read_first>
    - apps/web/src/lib/preinscription-reminder-template.ts (pattern HTML email avec inline CSS + escapeHtml + OfConfig — copier la structure)
    - apps/web/src/lib/of-config.ts (signature `loadOfConfig(tenantId): Promise<OfConfig>` et type OfConfig — pour les props brand)
    - apps/web/src/lib/audit-log.ts (helpers `computeDiff` + `logTenantSettingsChange` — pattern Phase 7 à généraliser)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md D-04, D-06, D-10 (subjects + tone email)
  </read_first>
  <files>
    - apps/web/src/lib/mailer-templates/user-invitation.ts (CRÉER : renderInvitationEmail)
    - apps/web/src/lib/mailer-templates/user-password-reset.ts (CRÉER : renderPasswordResetEmail)
    - apps/web/src/lib/audit-log.ts (MODIFIER : ajouter helper `logUserAction`)
  </files>
  <behavior>
    - `renderInvitationEmail({ firstName, publicUrl, expiresAt, invitedByName }, of)` returns `{ subject: string, html: string, text: string }`.
    - The HTML contains `publicUrl` (the invitation link), the user's firstName, the inviter's name, and a human-readable expiration date in fr-FR locale.
    - Subject is something like "Bienvenue sur QualiOF — définissez votre mot de passe".
    - Both html and text version present (text = fallback for non-HTML clients).
    - `renderPasswordResetEmail({ firstName, publicUrl, expiresAt }, of)` returns same shape with subject "Réinitialisation de votre mot de passe QualiOF".
    - `logUserAction({ tenantId, actorUserId, targetUserId, action, diff? })` writes one AuditLog row with entity='User', entityId=targetUserId, action=action, diff=diff ?? {}.
    - Pre-existing `computeDiff` and `logTenantSettingsChange` continue to work (no breaking change).
  </behavior>
  <action>
    Étape 1 — Créer `apps/web/src/lib/mailer-templates/user-invitation.ts` :

    ```typescript
    /**
     * Template email — invitation utilisateur (Phase 8 D-04).
     * Réplique du pattern preinscription-reminder-template.ts : HTML inline CSS
     * compatible tous clients mail + texte fallback + OfConfig pour la brand.
     */
    import type { OfConfig } from '@/lib/of-config';

    function escapeHtml(s: string): string {
      return s
        .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
    }
    function formatExpiry(d: Date): string {
      return new Intl.DateTimeFormat('fr-FR', { dateStyle: 'long', timeStyle: 'short' }).format(d);
    }

    export interface InvitationEmailInput {
      firstName: string;
      publicUrl: string;
      expiresAt: Date;
      invitedByName: string;
    }

    export function renderInvitationEmail(
      input: InvitationEmailInput,
      of: OfConfig,
    ): { subject: string; html: string; text: string } {
      const { firstName, publicUrl, expiresAt, invitedByName } = input;
      const subject = `Bienvenue sur QualiOF — définissez votre mot de passe`;
      const text = [
        `Bonjour ${firstName},`,
        ``,
        `${invitedByName} vous a invité(e) à rejoindre QualiOF (${of.name}).`,
        `Pour définir votre mot de passe et accéder à l'application, cliquez sur ce lien :`,
        publicUrl,
        ``,
        `Ce lien est valable jusqu'au ${formatExpiry(expiresAt)}.`,
        ``,
        `À bientôt sur QualiOF.`,
      ].join('\n');
      const html = `
<!doctype html>
<html lang="fr"><body style="font-family:-apple-system,Segoe UI,Helvetica,sans-serif;margin:0;padding:24px;background:#f8fafc;color:#0f172a">
  <div style="max-width:560px;margin:0 auto;background:#ffffff;border-radius:12px;padding:32px;border:1px solid #e2e8f0">
    <h1 style="margin:0 0 16px;font-size:20px">Bienvenue sur QualiOF, ${escapeHtml(firstName)} 👋</h1>
    <p style="line-height:1.6;font-size:14px">
      ${escapeHtml(invitedByName)} vous a invité(e) à rejoindre <strong>${escapeHtml(of.name)}</strong>.
      Pour définir votre mot de passe et accéder à l'application, cliquez sur le bouton ci-dessous.
    </p>
    <p style="text-align:center;margin:28px 0">
      <a href="${escapeHtml(publicUrl)}"
         style="display:inline-block;background:#2563eb;color:#fff;padding:12px 22px;border-radius:8px;text-decoration:none;font-weight:600;font-size:14px">
        Définir mon mot de passe
      </a>
    </p>
    <p style="font-size:12px;color:#64748b;line-height:1.5">
      Ce lien est valable jusqu'au <strong>${escapeHtml(formatExpiry(expiresAt))}</strong>.<br>
      Si le bouton ne fonctionne pas, copiez-collez ce lien dans votre navigateur :<br>
      <span style="word-break:break-all">${escapeHtml(publicUrl)}</span>
    </p>
  </div>
  <p style="text-align:center;font-size:11px;color:#94a3b8;margin-top:16px">
    © ${new Date().getFullYear()} ${escapeHtml(of.name)}
  </p>
</body></html>`.trim();
      return { subject, html, text };
    }
    ```

    Étape 2 — Créer `apps/web/src/lib/mailer-templates/user-password-reset.ts` :
    Même structure, subject `"Réinitialisation de votre mot de passe QualiOF"`. Input : `{ firstName, publicUrl, expiresAt }` (pas d'invitedByName puisque c'est un reset). Texte d'accroche : "Vous avez demandé (ou un administrateur a demandé pour vous) la réinitialisation de votre mot de passe sur QualiOF." Reste identique en structure HTML.

    Étape 3 — Étendre `apps/web/src/lib/audit-log.ts` en AJOUTANT (après `logTenantSettingsChange`, sans modifier l'existant) :

    ```typescript
    /**
     * Helper AuditLog pour les actions sur entité 'User' (Phase 8 D-10).
     *
     * Convention actions :
     *  - 'users.invite' / 'users.invitation.resend'
     *  - 'users.disable' / 'users.enable'
     *  - 'users.role.change'
     *  - 'users.password.reset_requested' (admin) / 'users.password.set' (user lui-même via /invitation/[token])
     *  - 'auth.login.success' / 'auth.login.failed' (entity='User' aussi, entityId=user.id)
     */
    export async function logUserAction(opts: {
      tenantId: string;
      actorUserId: string | null;  // null pour login.failed sans user résolu
      targetUserId: string;
      action: string;
      diff?: Diff | Record<string, unknown>;
      ip?: string | null;
      userAgent?: string | null;
    }): Promise<void> {
      await prisma.auditLog.create({
        data: {
          tenantId: opts.tenantId,
          userId: opts.actorUserId,
          entity: 'User',
          entityId: opts.targetUserId,
          action: opts.action,
          diff: (opts.diff ?? {}) as never,
          ip: opts.ip ?? null,
          userAgent: opts.userAgent ?? null,
        },
      });
    }
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "export function renderInvitationEmail" apps/web/src/lib/mailer-templates/user-invitation.ts &&
      grep -q "export function renderPasswordResetEmail" apps/web/src/lib/mailer-templates/user-password-reset.ts &&
      grep -q "export async function logUserAction" apps/web/src/lib/audit-log.ts &&
      grep -q "entity: 'User'" apps/web/src/lib/audit-log.ts &&
      pnpm --filter @qualiof/web exec tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "escapeHtml" apps/web/src/lib/mailer-templates/user-invitation.ts` ≥ 3 (utilisé sur firstName, invitedByName, publicUrl, etc.)
    - `grep -c "of.name\|of\\.emailFrom" apps/web/src/lib/mailer-templates/user-invitation.ts` ≥ 1 (utilise OfConfig)
    - `grep -c "logUserAction" apps/web/src/lib/audit-log.ts` ≥ 1 (export ajouté)
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    Templates email rendus utilisables. Helper `logUserAction` exporté depuis `lib/audit-log.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tenant-users.ts server actions (6 exports)</name>
  <read_first>
    - apps/web/src/server/actions/tenant-settings.ts (pattern complet Phase 7 — Server Action discriminée, structure try/catch, AuditLog, revalidatePath)
    - apps/web/src/server/actions/__tests__/tenant-settings.test.ts (pattern Vitest mocks `@qualiof/db`, `@/lib/auth`, exactement ce qu'on va dupliquer)
    - apps/web/src/server/actions/preinscriptions.ts L20 (`randomUUID().replace(/-/g, '')` — pattern token)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md Example 2 (inviteUser code complet) + Example 4 (disableUser)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md D-04, D-05, D-06, D-10
  </read_first>
  <files>
    - apps/web/src/server/actions/tenant-users.ts (CRÉER : 6 server actions)
    - apps/web/src/server/actions/__tests__/tenant-users.test.ts (CRÉER : ≥ 10 tests Vitest)
  </files>
  <behavior>
    - `inviteUser(input: InviteUserInput)` : (1) requireRole(['ADMIN']) ; (2) validate via inviteUserSchema ; (3) email unique check ; (4) `prisma.$transaction` qui crée User (hashedPwd='', invitedAt=now, invitedBy=admin.id) + UserInvitation (token=randomUUID().replace(/-/g,''), expiresAt=now+7d, role from input) ; (5) loadOfConfig(admin.tenantId) + sendMail(renderInvitationEmail(...)) ; (6) logUserAction(action='users.invite', diff={ email, role, invitationId }) ; (7) revalidatePath('/app/parametres/utilisateurs') ; (8) return { ok: true, data: { userId, dryRun: mailResult.dryRun } }.
    - `disableUser(userId: string)` : (1) requireRole(['ADMIN']) ; (2) protect self-disable (if userId === admin.id return error) ; (3) findUnique scoped to admin.tenantId, ensure not already disabled ; (4) update user.disabledAt=now() ; (5) lucia.invalidateUserSessions(userId) ; (6) logUserAction(action='users.disable', diff={ email: before.email }) ; (7) revalidatePath ; (8) return { ok: true }.
    - `enableUser(userId: string)` : (1) requireRole(['ADMIN']) ; (2) findUnique scoped tenantId, ensure disabled ; (3) update user.disabledAt=null ; (4) logUserAction(action='users.enable', diff={ email }) ; (5) revalidatePath ; (6) return { ok: true }.
    - `resetUserPassword(userId: string)` : (1) requireRole(['ADMIN']) ; (2) findUnique scoped tenantId ; (3) create new UserInvitation (token, 7d expiry, role=user.role, userId, invitedBy=admin.id) ; (4) loadOfConfig + sendMail(renderPasswordResetEmail) ; (5) logUserAction(action='users.password.reset_requested', diff={ invitationId }) ; (6) revalidatePath ; (7) return { ok: true, data: { dryRun } }.
    - `changeUserRole(input: ChangeUserRoleInput)` : (1) requireRole(['ADMIN']) ; (2) validate via changeUserRoleSchema ; (3) protect self-demote (if userId===admin.id and role!=='ADMIN' return error) ; (4) findUnique scoped tenantId ; (5) update user.role + logUserAction(action='users.role.change', diff={ role: { before, after } }) ; (6) revalidatePath ; (7) return { ok: true }.
    - `resendInvitation(userId: string)` : (1) requireRole(['ADMIN']) ; (2) findUnique user + latest UserInvitation ; (3) invalidate previous unused invitations (usedAt=now or just create fresh) — simplest: createw fresh UserInvitation with new token, 7d expiry, same role as user ; (4) sendMail(renderInvitationEmail) ; (5) logUserAction(action='users.invitation.resend') ; (6) return { ok: true, data: { dryRun } }.
    - Each catch block converts UnauthorizedError → `{ ok: false, error: e.message }`, ForbiddenError → `{ ok: false, error: e.message }`, autres → re-throw (NB : NE PAS swallow les erreurs Zod ou autres).
    - All `where:` clauses on prisma.user use BOTH `id` AND `tenantId: admin.tenantId` (multi-tenant safety).
  </behavior>
  <action>
    Étape 1 — Créer `apps/web/src/server/actions/tenant-users.ts` (350 LOC env.). Squelette :

    ```typescript
    'use server';

    /**
     * Server actions Utilisateurs (Phase 8 D-04 à D-06).
     *
     * Toutes les actions sont ADMIN only via requireRole(['ADMIN']).
     * Toutes les mutations sont scopées par admin.tenantId.
     * Toutes les actions loggent dans AuditLog (entity='User').
     *
     * Pattern discriminé `{ ok: true, data?: T }` | `{ ok: false, error, fieldErrors? }`.
     */

    import { randomUUID } from 'node:crypto';
    import { revalidatePath } from 'next/cache';
    import { prisma } from '@qualiof/db';
    import { lucia } from '@/lib/auth';
    import { requireRole, UnauthorizedError, ForbiddenError } from '@/lib/rbac';
    import {
      inviteUserSchema, changeUserRoleSchema,
      type InviteUserInput, type ChangeUserRoleInput,
    } from '@qualiof/shared';
    import { sendMail } from '@/lib/mailer';
    import { renderInvitationEmail } from '@/lib/mailer-templates/user-invitation';
    import { renderPasswordResetEmail } from '@/lib/mailer-templates/user-password-reset';
    import { loadOfConfig } from '@/lib/of-config';
    import { logUserAction } from '@/lib/audit-log';

    const INVITATION_VALIDITY_DAYS = 7;
    const REVAL_PATH = '/app/parametres/utilisateurs';

    export type ActionResult<T = undefined> =
      | { ok: true; data?: T }
      | { ok: false; error: string; fieldErrors?: Record<string, string[] | undefined> };

    function generateToken(): string {
      return randomUUID().replace(/-/g, '');
    }
    function invitationExpiry(): Date {
      return new Date(Date.now() + INVITATION_VALIDITY_DAYS * 86400 * 1000);
    }
    function publicInvitationUrl(token: string): string {
      const base = process.env.NEXTAUTH_URL || 'http://localhost:3002';
      return `${base.replace(/\/$/, '')}/invitation/${token}`;
    }

    // Helper pour catcher Unauthorized/Forbidden uniformément
    async function withAdmin<T>(fn: (admin: { id: string; tenantId: string; firstName: string; lastName: string }) => Promise<T>): Promise<T | { ok: false; error: string }> {
      try {
        const admin = await requireRole(['ADMIN']);
        return await fn(admin as never);
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) {
          return { ok: false, error: e.message };
        }
        throw e;
      }
    }
    ```

    Puis IMPLÉMENTER les 6 actions :

    ```typescript
    // ─── 1. inviteUser ──────────────────────────────────────────────────────
    export async function inviteUser(input: InviteUserInput): Promise<ActionResult<{ userId: string; dryRun?: boolean }>> {
      try {
        const admin = await requireRole(['ADMIN']);
        const parsed = inviteUserSchema.safeParse(input);
        if (!parsed.success) return { ok: false, error: 'Validation échouée', fieldErrors: parsed.error.flatten().fieldErrors };
        const { email, firstName, lastName, role } = parsed.data;

        const existing = await prisma.user.findUnique({ where: { email } });
        if (existing) return { ok: false, error: 'Email déjà utilisé', fieldErrors: { email: ['Email déjà utilisé'] } };

        const token = generateToken();
        const expiresAt = invitationExpiry();
        const { newUser, invitation } = await prisma.$transaction(async (tx) => {
          const newUser = await tx.user.create({
            data: { tenantId: admin.tenantId, email, hashedPwd: '', firstName, lastName, role, invitedAt: new Date(), invitedBy: admin.id },
          });
          const invitation = await tx.userInvitation.create({
            data: { tenantId: admin.tenantId, email, token, role, expiresAt, userId: newUser.id, invitedBy: admin.id },
          });
          return { newUser, invitation };
        });

        const of = await loadOfConfig(admin.tenantId);
        const { subject, html, text } = renderInvitationEmail({
          firstName, publicUrl: publicInvitationUrl(token), expiresAt,
          invitedByName: `${admin.firstName} ${admin.lastName}`,
        }, of);
        const mailResult = await sendMail({ to: email, subject, html, text });

        await logUserAction({
          tenantId: admin.tenantId, actorUserId: admin.id, targetUserId: newUser.id,
          action: 'users.invite', diff: { email, role, invitationId: invitation.id },
        });

        revalidatePath(REVAL_PATH);
        return { ok: true, data: { userId: newUser.id, dryRun: mailResult.dryRun } };
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
    }

    // ─── 2. disableUser ─────────────────────────────────────────────────────
    export async function disableUser(userId: string): Promise<ActionResult> {
      try {
        const admin = await requireRole(['ADMIN']);
        if (userId === admin.id) return { ok: false, error: 'Vous ne pouvez pas désactiver votre propre compte' };
        const before = await prisma.user.findFirst({ where: { id: userId, tenantId: admin.tenantId }, select: { id: true, email: true, disabledAt: true } });
        if (!before) return { ok: false, error: 'Utilisateur introuvable' };
        if (before.disabledAt) return { ok: false, error: 'Utilisateur déjà désactivé' };
        await prisma.user.update({ where: { id: userId }, data: { disabledAt: new Date() } });
        await lucia.invalidateUserSessions(userId);
        await logUserAction({ tenantId: admin.tenantId, actorUserId: admin.id, targetUserId: userId, action: 'users.disable', diff: { email: before.email } });
        revalidatePath(REVAL_PATH);
        return { ok: true };
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
    }

    // ─── 3. enableUser ──────────────────────────────────────────────────────
    export async function enableUser(userId: string): Promise<ActionResult> {
      try {
        const admin = await requireRole(['ADMIN']);
        const before = await prisma.user.findFirst({ where: { id: userId, tenantId: admin.tenantId }, select: { id: true, email: true, disabledAt: true } });
        if (!before) return { ok: false, error: 'Utilisateur introuvable' };
        if (!before.disabledAt) return { ok: false, error: 'Utilisateur déjà actif' };
        await prisma.user.update({ where: { id: userId }, data: { disabledAt: null } });
        await logUserAction({ tenantId: admin.tenantId, actorUserId: admin.id, targetUserId: userId, action: 'users.enable', diff: { email: before.email } });
        revalidatePath(REVAL_PATH);
        return { ok: true };
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
    }

    // ─── 4. resetUserPassword ──────────────────────────────────────────────
    export async function resetUserPassword(userId: string): Promise<ActionResult<{ dryRun?: boolean }>> {
      try {
        const admin = await requireRole(['ADMIN']);
        const target = await prisma.user.findFirst({ where: { id: userId, tenantId: admin.tenantId }, select: { id: true, email: true, firstName: true, role: true } });
        if (!target) return { ok: false, error: 'Utilisateur introuvable' };

        const token = generateToken();
        const expiresAt = invitationExpiry();
        const invitation = await prisma.userInvitation.create({
          data: { tenantId: admin.tenantId, email: target.email, token, role: target.role, expiresAt, userId: target.id, invitedBy: admin.id },
        });
        const of = await loadOfConfig(admin.tenantId);
        const { subject, html, text } = renderPasswordResetEmail({
          firstName: target.firstName, publicUrl: publicInvitationUrl(token), expiresAt,
        }, of);
        const mailResult = await sendMail({ to: target.email, subject, html, text });
        await logUserAction({
          tenantId: admin.tenantId, actorUserId: admin.id, targetUserId: userId,
          action: 'users.password.reset_requested', diff: { invitationId: invitation.id },
        });
        revalidatePath(REVAL_PATH);
        return { ok: true, data: { dryRun: mailResult.dryRun } };
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
    }

    // ─── 5. changeUserRole ─────────────────────────────────────────────────
    export async function changeUserRole(input: ChangeUserRoleInput): Promise<ActionResult> {
      try {
        const admin = await requireRole(['ADMIN']);
        const parsed = changeUserRoleSchema.safeParse(input);
        if (!parsed.success) return { ok: false, error: 'Validation échouée', fieldErrors: parsed.error.flatten().fieldErrors };
        const { userId, role } = parsed.data;
        if (userId === admin.id && role !== 'ADMIN') return { ok: false, error: 'Vous ne pouvez pas vous retirer le rôle ADMIN vous-même' };
        const before = await prisma.user.findFirst({ where: { id: userId, tenantId: admin.tenantId }, select: { id: true, role: true } });
        if (!before) return { ok: false, error: 'Utilisateur introuvable' };
        if (before.role === role) return { ok: true };  // no-op
        await prisma.user.update({ where: { id: userId }, data: { role } });
        await logUserAction({
          tenantId: admin.tenantId, actorUserId: admin.id, targetUserId: userId,
          action: 'users.role.change', diff: { role: { before: before.role, after: role } },
        });
        revalidatePath(REVAL_PATH);
        return { ok: true };
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
    }

    // ─── 6. resendInvitation ───────────────────────────────────────────────
    export async function resendInvitation(userId: string): Promise<ActionResult<{ dryRun?: boolean }>> {
      try {
        const admin = await requireRole(['ADMIN']);
        const target = await prisma.user.findFirst({ where: { id: userId, tenantId: admin.tenantId }, select: { id: true, email: true, firstName: true, role: true } });
        if (!target) return { ok: false, error: 'Utilisateur introuvable' };

        const token = generateToken();
        const expiresAt = invitationExpiry();
        await prisma.userInvitation.create({
          data: { tenantId: admin.tenantId, email: target.email, token, role: target.role, expiresAt, userId: target.id, invitedBy: admin.id },
        });
        const of = await loadOfConfig(admin.tenantId);
        const { subject, html, text } = renderInvitationEmail({
          firstName: target.firstName, publicUrl: publicInvitationUrl(token), expiresAt,
          invitedByName: `${admin.firstName} ${admin.lastName}`,
        }, of);
        const mailResult = await sendMail({ to: target.email, subject, html, text });
        await logUserAction({
          tenantId: admin.tenantId, actorUserId: admin.id, targetUserId: userId,
          action: 'users.invitation.resend',
        });
        return { ok: true, data: { dryRun: mailResult.dryRun } };
      } catch (e) {
        if (e instanceof UnauthorizedError || e instanceof ForbiddenError) return { ok: false, error: e.message };
        throw e;
      }
    }
    ```

    NOTE technique : `requireRole(['ADMIN'])` retourne un Lucia User dont `.id`, `.tenantId`, `.firstName`, `.lastName` proviennent du `getUserAttributes` étendu en 08-01. Cast en `LuciaUser & { id, tenantId, firstName, lastName }` ou bien typer le retour de `requireRole` correctement dans `lib/rbac.ts`.

    Étape 2 — Tests `apps/web/src/server/actions/__tests__/tenant-users.test.ts` (≥ 10 tests Vitest). Mocks à reproduire (pattern Phase 7) :

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    vi.mock('@qualiof/db', () => ({
      prisma: {
        user: { findUnique: vi.fn(), findFirst: vi.fn(), create: vi.fn(), update: vi.fn() },
        userInvitation: { create: vi.fn() },
        auditLog: { create: vi.fn() },
        $transaction: vi.fn(),
      },
    }));
    vi.mock('@/lib/auth', () => ({
      lucia: { invalidateUserSessions: vi.fn() },
      validateRequest: vi.fn(),
    }));
    vi.mock('@/lib/rbac', async () => {
      const actual = await vi.importActual<typeof import('@/lib/rbac')>('@/lib/rbac');
      return { ...actual, requireRole: vi.fn() };
    });
    vi.mock('@/lib/mailer', () => ({ sendMail: vi.fn().mockResolvedValue({ ok: true, dryRun: true }) }));
    vi.mock('@/lib/of-config', () => ({ loadOfConfig: vi.fn().mockResolvedValue({ name: 'StartAcademy', emailFrom: 'test@x.fr' }) }));
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }));
    ```

    Tests à écrire (≥ 10) :
    1. `inviteUser` : ADMIN → crée user + invitation + envoie mail + AuditLog logged
    2. `inviteUser` : LECTEUR → return `{ ok: false, error: 'Rôle ... non autorisé' }` (ForbiddenError catchée)
    3. `inviteUser` : email déjà utilisé → return `{ ok: false, fieldErrors: { email: [...] } }`
    4. `inviteUser` : input invalide (email='bad') → return `{ ok: false, fieldErrors }`
    5. `disableUser` : ADMIN sur autre user → update + invalidateUserSessions appelé avec userId + AuditLog logged
    6. `disableUser` : ADMIN sur soi-même → return `{ ok: false, error: /vous-même/i }`
    7. `disableUser` : user déjà désactivé → return `{ ok: false, error: /déjà désactivé/ }`
    8. `enableUser` : ADMIN sur user désactivé → update disabledAt=null + AuditLog logged
    9. `resetUserPassword` : crée UserInvitation + sendMail appelé + AuditLog 'users.password.reset_requested'
    10. `changeUserRole` : protect self-demote (ADMIN ne peut pas se mettre LECTEUR)
    11. `changeUserRole` : no-op si même role (no AuditLog créé)
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "export async function inviteUser" apps/web/src/server/actions/tenant-users.ts &&
      grep -q "export async function disableUser" apps/web/src/server/actions/tenant-users.ts &&
      grep -q "export async function enableUser" apps/web/src/server/actions/tenant-users.ts &&
      grep -q "export async function resetUserPassword" apps/web/src/server/actions/tenant-users.ts &&
      grep -q "export async function changeUserRole" apps/web/src/server/actions/tenant-users.ts &&
      grep -q "export async function resendInvitation" apps/web/src/server/actions/tenant-users.ts &&
      grep -q "lucia.invalidateUserSessions" apps/web/src/server/actions/tenant-users.ts &&
      grep -cE "users\\.(invite|disable|enable|password\\.reset_requested|role\\.change|invitation\\.resend)" apps/web/src/server/actions/tenant-users.ts &&
      pnpm --filter @qualiof/web test --run src/server/actions/__tests__/tenant-users.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function" apps/web/src/server/actions/tenant-users.ts` ≥ 6
    - `grep -c "requireRole(\\['ADMIN'\\])" apps/web/src/server/actions/tenant-users.ts` ≥ 6 (ADMIN-only)
    - `grep -c "lucia.invalidateUserSessions" apps/web/src/server/actions/tenant-users.ts` ≥ 1 (disableUser)
    - `grep -c "logUserAction\\|prisma.auditLog.create" apps/web/src/server/actions/tenant-users.ts` ≥ 6 (1 par action)
    - `grep -c "users\\.invite\\|users\\.disable\\|users\\.enable\\|users\\.password\\.reset_requested\\|users\\.role\\.change\\|users\\.invitation\\.resend" apps/web/src/server/actions/tenant-users.ts` ≥ 6
    - `grep -c "tenantId: admin.tenantId" apps/web/src/server/actions/tenant-users.ts` ≥ 6 (multi-tenant scope)
    - `pnpm --filter @qualiof/web test --run src/server/actions/__tests__/tenant-users.test.ts` : ≥ 10 tests verts
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    6 server actions admin exposées + 10 tests Vitest verts + tsc clean. Toutes les conventions AuditLog `users.*` posées D-10 sont écrites par au moins une action.
  </done>
</task>

</tasks>

<verification>
- 6 server actions exportées depuis `apps/web/src/server/actions/tenant-users.ts`
- 2 email templates exportés depuis `apps/web/src/lib/mailer-templates/`
- `logUserAction` exporté depuis `apps/web/src/lib/audit-log.ts`
- Tests Vitest verts (≥ 10 tests tenant-users)
- `pnpm --filter @qualiof/web exec tsc --noEmit` clean
</verification>

<success_criteria>
- 6 actions ADMIN-only : inviteUser, disableUser, enableUser, resetUserPassword, changeUserRole, resendInvitation
- AuditLog convention `users.*` couverte (6 actions)
- Multi-tenant scope respecté (admin.tenantId partout)
- self-disable / self-demote protections en place
- Lucia session invalidation lors d'un disableUser
- Email dry-run géré (sendMail retourne `{ dryRun: true }` propagé à UI)
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-utilisateurs-et-rbac/08-02-SUMMARY.md`
</output>