---
phase: 08-multi-utilisateurs-et-rbac
plan: 04
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - apps/web/src/app/app/parametres/utilisateurs/page.tsx
  - apps/web/src/components/users/users-table.tsx
  - apps/web/src/components/users/invite-user-button.tsx
  - apps/web/src/components/users/user-row-actions.tsx
  - apps/web/src/components/users/change-role-dialog.tsx
  - apps/web/src/components/layout/nav-config.ts
  - apps/web/src/components/layout/sidebar.tsx
  - apps/web/src/components/layout/sidebar-nav.tsx
  - apps/web/src/components/layout/mobile-nav-drawer.tsx
  - apps/web/src/app/app/layout.tsx
  - apps/web/src/components/layout/__tests__/nav-config.test.ts
  - apps/web/src/app/app/parametres/utilisateurs/__tests__/page.smoke.test.ts
autonomous: true
requirements: [RBAC-01, RBAC-03]
must_haves:
  truths:
    - "Page /app/parametres/utilisateurs is gated by requireRole(['ADMIN']) — LECTEUR or any non-ADMIN cannot access"
    - "Page lists all users of the current tenant with columns: Email · Nom · Rôle · Statut · Dernière connexion · Actions"
    - "Button 'Inviter un utilisateur' opens a Radix Dialog with form (email, firstName, lastName, role) calling inviteUser"
    - "Row actions menu (DropdownMenu) per user: Modifier rôle / Réinitialiser MDP / Désactiver|Réactiver (depending on current state) / Renvoyer invitation"
    - "Destructive/sensitive actions (disable, reset) trigger Radix Dialog confirmation before invoking server action"
    - "NavItem interface extended with optional allowedRoles?: UserRole[] field"
    - "Layout app/app/layout.tsx filters NAV by user.role server-side, passes filtered nav as prop to Sidebar and MobileNavDrawer"
    - "Sidebar and SidebarNav and MobileNavDrawer all accept nav: NavSection[] prop instead of importing NAV directly"
    - "filterNavForRole(NAV, role) is a pure function exported from nav-config.ts (testable in isolation)"
    - "filterNavForRole(NAV, 'LECTEUR') returns sections without items that have allowedRoles excluding LECTEUR (no leak of cached items)"
    - "Sidebar item 'Utilisateurs' (in Configuration section) has allowedRoles: ['ADMIN']"
  artifacts:
    - path: "apps/web/src/app/app/parametres/utilisateurs/page.tsx"
      provides: "Users list page (ADMIN only)"
      contains: "requireRole"
    - path: "apps/web/src/components/users/users-table.tsx"
      provides: "Server-rendered users table"
      exports: ["UsersTable"]
    - path: "apps/web/src/components/users/invite-user-button.tsx"
      provides: "Invite user CTA + dialog form"
      exports: ["InviteUserButton"]
    - path: "apps/web/src/components/users/user-row-actions.tsx"
      provides: "Per-row actions dropdown"
      exports: ["UserRowActions"]
    - path: "apps/web/src/components/users/change-role-dialog.tsx"
      provides: "Role change dialog"
      exports: ["ChangeRoleDialog"]
    - path: "apps/web/src/components/layout/nav-config.ts"
      provides: "NavItem.allowedRoles + filterNavForRole pure helper"
      exports: ["NAV", "filterNavForRole", "NavItem", "NavSection"]
  key_links:
    - from: "apps/web/src/app/app/parametres/utilisateurs/page.tsx"
      to: "requireRole(['ADMIN'])"
      via: "first call in Server Component"
      pattern: "requireRole\\(\\['ADMIN'\\]\\)"
    - from: "apps/web/src/components/users/invite-user-button.tsx"
      to: "apps/web/src/server/actions/tenant-users.ts inviteUser"
      via: "form submit"
      pattern: "inviteUser"
    - from: "apps/web/src/components/users/user-row-actions.tsx"
      to: "tenant-users.ts (disableUser, enableUser, resetUserPassword, resendInvitation)"
      via: "menu items"
      pattern: "disableUser|enableUser|resetUserPassword|resendInvitation"
    - from: "apps/web/src/app/app/layout.tsx"
      to: "filterNavForRole(NAV, user.role)"
      via: "Server-side filter, passed as prop to <Sidebar nav={...}>"
      pattern: "filterNavForRole"
---

<objective>
Construire l'UI page Utilisateurs côté admin (`/app/parametres/utilisateurs`) consommant les 6 server actions de 08-02, ET appliquer le filtrage sidebar par rôle (D-07 — visuel) en propageant `nav: NavSection[]` filtré depuis le Server Component `app/app/layout.tsx`. Aucun gating de sécurité côté UI : la sécurité réelle est dans `requireRole` (D-08) — le filtre sidebar est uniquement esthétique.

Purpose: livrer RBAC-01 (CRUD users via UI) + RBAC-03 (sidebar filtrée par rôle). L'ADMIN peut désormais : inviter, désactiver/réactiver, reset MDP, changer rôle, renvoyer invitation. Les non-ADMIN ne voient PAS les items "Paramètres / Utilisateurs / Historique" dans la sidebar.
Output: 1 page Server Component, 4 client components, mise à jour de nav-config + sidebar + mobile drawer + layout, 2 fichiers de tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-02-PLAN.md
@.planning/phases/07-param-tres-organisme-ditables/07-04-SUMMARY.md

@apps/web/src/components/layout/nav-config.ts
@apps/web/src/components/layout/sidebar.tsx
@apps/web/src/components/layout/sidebar-nav.tsx
@apps/web/src/components/layout/mobile-nav-drawer.tsx
@apps/web/src/app/app/layout.tsx
@apps/web/src/components/layout/user-menu-button.tsx
@apps/web/src/components/settings/of-identity-form.tsx

<interfaces>
<!-- From 08-02 -->

Server actions in apps/web/src/server/actions/tenant-users.ts:
```typescript
export async function inviteUser(input: { email, firstName, lastName, role }): Promise<ActionResult<{ userId, dryRun? }>>;
export async function disableUser(userId: string): Promise<ActionResult>;
export async function enableUser(userId: string): Promise<ActionResult>;
export async function resetUserPassword(userId: string): Promise<ActionResult<{ dryRun? }>>;
export async function changeUserRole(input: { userId, role }): Promise<ActionResult>;
export async function resendInvitation(userId: string): Promise<ActionResult<{ dryRun? }>>;
```

From @qualiof/shared:
```typescript
export const inviteUserSchema: z.ZodSchema;  // { email, firstName, lastName, role }
export type InviteUserInput;
export const changeUserRoleSchema: z.ZodSchema;
export const PERMISSIONS, canRead, canWrite, rolesWithAccess (from constants/permissions);
```

From apps/web/src/lib/rbac.ts (08-01):
```typescript
export async function requireRole(allowed: UserRole[]): Promise<LuciaUser>;
```

Existing nav-config.ts structure (L19-73):
```typescript
export interface NavItem { label, href, icon }
export interface NavSection { title?, id?, collapsible?, items: NavItem[] }
export const NAV: NavSection[] = [ Essentiel, Suivi, Configuration ];
// Section "Configuration" contains items: Organisations, Formateurs, Produits, Modèles, Financeurs, Inscriptions, Paramètres
```

Layout app/app/layout.tsx (verbatim):
```typescript
export default async function AppLayout({ children }) {
  const { user } = await validateRequest();
  if (!user) redirect('/login');
  return (
    <div className="min-h-screen bg-background">
      <Sidebar />
      <MainContent>
        <TopBar user={user} />
        <main>...{children}</main>
      </MainContent>
      <CommandPalette />
    </div>
  );
}
```

Sidebar/SidebarNav both import NAV directly from './nav-config' (lines: sidebar-nav.tsx L9 + mobile-nav-drawer.tsx similar). These need to receive `nav` as a prop instead.

Phase 7 inline edit + dialog patterns (07-04-SUMMARY.md) :
- Forms use raw <button>/<input> + Tailwind (pas de Button/Input primitives)
- @radix-ui/react-alert-dialog NOT installed → use @radix-ui/react-dialog with two buttons
- toast.success / toast.error via sonner

Existing user-menu-button.tsx (already uses Radix Dialog + DropdownMenu correctly) → REUSE the same pattern for confirmations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nav-config + filterNavForRole + propager nav en prop (Sidebar / MobileDrawer / Layout)</name>
  <read_first>
    - apps/web/src/components/layout/nav-config.ts (verbatim)
    - apps/web/src/components/layout/sidebar.tsx (verbatim — `<Sidebar />` doit accepter prop nav)
    - apps/web/src/components/layout/sidebar-nav.tsx (verbatim — utilise NAV en import statique L9, à remplacer par prop)
    - apps/web/src/components/layout/mobile-nav-drawer.tsx (idem)
    - apps/web/src/app/app/layout.tsx (Server Component avec validateRequest — y filtrer NAV)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md Pattern 3 (sidebar filter)
  </read_first>
  <files>
    - apps/web/src/components/layout/nav-config.ts (MODIFIER : ajouter allowedRoles? à NavItem, ajouter section "Administration" avec items Utilisateurs+Historique, ajouter allowedRoles aux items sensibles, ajouter export `filterNavForRole`)
    - apps/web/src/components/layout/sidebar.tsx (MODIFIER : accepter prop `nav: NavSection[]`, transmettre à SidebarNav)
    - apps/web/src/components/layout/sidebar-nav.tsx (MODIFIER : accepter prop `nav`, retirer import NAV statique)
    - apps/web/src/components/layout/mobile-nav-drawer.tsx (MODIFIER : accepter prop `nav` aussi)
    - apps/web/src/app/app/layout.tsx (MODIFIER : appeler `filterNavForRole(NAV, user.role)` et passer en prop)
    - apps/web/src/components/layout/__tests__/nav-config.test.ts (CRÉER : ≥ 5 tests filterNavForRole)
  </files>
  <behavior>
    - `NavItem` has new optional `allowedRoles?: UserRole[]` (omitted = visible to all roles).
    - `NAV` config has items annotated:
      - 'Paramètres' (existing) → allowedRoles: ['ADMIN']
      - NEW item 'Utilisateurs' href='/app/parametres/utilisateurs' icon=UsersIcon → allowedRoles: ['ADMIN']
      - NEW item 'Historique' href='/app/parametres/historique' icon=History → allowedRoles: ['ADMIN'] (will be used in 08-05)
      - 'Factures' → allowedRoles: ['ADMIN', 'MANAGER', 'COMPTABLE', 'LECTEUR']  (per D-02 + LECTEUR R)
      - 'Budget AGEFICE' → allowedRoles: ['ADMIN', 'MANAGER', 'COMPTABLE', 'COMMERCIAL', 'LECTEUR']
      - 'Leads' → allowedRoles: ['ADMIN', 'MANAGER', 'COMMERCIAL']
      - 'Pré-inscriptions' → allowedRoles: ['ADMIN', 'MANAGER', 'COMMERCIAL']
      - 'Dossiers OPCO' → allowedRoles: ['ADMIN', 'MANAGER', 'COMMERCIAL', 'COMPTABLE', 'LECTEUR']
      - 'Financeurs' → allowedRoles: ['ADMIN', 'MANAGER', 'COMMERCIAL', 'COMPTABLE']
      - 'Formateurs' → allowedRoles: ['ADMIN', 'MANAGER', 'FORMATEUR', 'LECTEUR']
      - 'Qualiopi bilan' (if present) → allowedRoles: ['ADMIN', 'MANAGER', 'FORMATEUR', 'COMPTABLE']
      - Other items (Dashboard, Apprenants, Sessions, Produits, Organisations, Templates, Inscriptions) keep allowedRoles undefined (visible to all 6 roles per D-02 RW or R)
    - Pure function `filterNavForRole(nav: NavSection[], role: UserRole): NavSection[]` :
      - Returns a new array where each section has its items filtered by allowedRoles (kept if absent OR includes role)
      - Sections with empty items are dropped
    - `filterNavForRole(NAV, 'ADMIN')` returns sections + items count >= original count
    - `filterNavForRole(NAV, 'LECTEUR')` excludes the 'Utilisateurs', 'Historique', 'Paramètres' items
    - `filterNavForRole(NAV, 'COMMERCIAL')` excludes 'Factures' (LECTEUR R only)
    - `<Sidebar>` accepts `nav?: NavSection[]` prop, defaults to imported NAV if absent (backward compat)
    - `<SidebarNav>` accepts `nav: NavSection[]` prop (required), no longer imports NAV at top
    - `<MobileNavDrawer>` similarly accepts `nav` prop
    - app/app/layout.tsx imports `{ NAV, filterNavForRole }` from nav-config, calls `const visibleNav = filterNavForRole(NAV, user.role as UserRole)`, passes to both Sidebar and MobileNavDrawer (or wherever MobileMenuButton triggers drawer — confirm by reading)
  </behavior>
  <action>
    Étape 1 — Modifier `apps/web/src/components/layout/nav-config.ts` :

    A) En tête de fichier, AJOUTER l'import + l'extension d'interface :
    ```typescript
    import {
      Users, Building2, GraduationCap, BookOpen, FileText, Calendar, Receipt,
      Megaphone, Settings, LayoutDashboard, ListChecks, Landmark, ClipboardCheck,
      Inbox, Wallet,
      History,  // NEW pour item Historique
      UserCog,  // NEW pour item Utilisateurs
    } from 'lucide-react';
    import type { UserRole } from '@prisma/client';

    export interface NavItem {
      label: string;
      href: string;
      icon: React.ComponentType<{ className?: string }>;
      /** Rôles autorisés à voir cet item. undefined = tous rôles. */
      allowedRoles?: UserRole[];
    }
    ```

    B) Dans `NAV` :
    - Annoter les items existants avec `allowedRoles` selon la matrice D-02 ci-dessus.
    - AJOUTER en fin de la section "Configuration" (ou créer nouvelle section "Administration") :
      ```typescript
      { label: 'Utilisateurs', href: '/app/parametres/utilisateurs', icon: UserCog, allowedRoles: ['ADMIN'] },
      { label: 'Historique', href: '/app/parametres/historique', icon: History, allowedRoles: ['ADMIN'] },
      ```
    - 'Paramètres' → allowedRoles: ['ADMIN']

    C) En fin de fichier, AJOUTER la pure fn :
    ```typescript
    /**
     * Filtre les sections/items de la nav selon le rôle utilisateur.
     * Pure fn — testable en isolation. Phase 8 D-07.
     */
    export function filterNavForRole(nav: NavSection[], role: UserRole): NavSection[] {
      return nav
        .map((section) => ({
          ...section,
          items: section.items.filter((it) => !it.allowedRoles || it.allowedRoles.includes(role)),
        }))
        .filter((section) => section.items.length > 0);
    }
    ```

    Étape 2 — `apps/web/src/components/layout/sidebar-nav.tsx` :
    - Remplacer `import { NAV, SECTION_STORAGE_PREFIX } from './nav-config';` par `import { SECTION_STORAGE_PREFIX, type NavSection } from './nav-config';`
    - Modifier `SidebarNavProps` :
      ```typescript
      interface SidebarNavProps {
        nav: NavSection[];
        collapsed: boolean;
        onNavigate?: () => void;
      }
      ```
    - Dans le body, remplacer toutes les références `NAV.map` / `NAV.some` / `for (const s of NAV)` par `nav.map` / `nav.some` / `for (const s of nav)`.

    Étape 3 — `apps/web/src/components/layout/sidebar.tsx` :
    - Modifier la signature `export function Sidebar()` en `export function Sidebar({ nav }: { nav: NavSection[] })` (importer le type).
    - Transmettre à SidebarNav : `<SidebarNav nav={nav} collapsed={collapsed} />`

    Étape 4 — `apps/web/src/components/layout/mobile-nav-drawer.tsx` :
    - Lecture préalable du fichier pour identifier comment NAV y est utilisé.
    - Même pattern : accepter `nav: NavSection[]` en prop, retirer l'import statique NAV.

    Étape 5 — `apps/web/src/app/app/layout.tsx` :
    ```typescript
    import { redirect } from 'next/navigation';
    import { validateRequest } from '@/lib/auth';
    import { Sidebar } from '@/components/layout/sidebar';
    import { TopBar } from '@/components/layout/top-bar';
    import { MainContent } from '@/components/layout/main-content';
    import { CommandPalette } from '@/components/command-palette/command-palette';
    import { NAV, filterNavForRole } from '@/components/layout/nav-config';
    import type { UserRole } from '@prisma/client';

    export default async function AppLayout({ children }: { children: React.ReactNode }) {
      const { user } = await validateRequest();
      if (!user) redirect('/login');
      const visibleNav = filterNavForRole(NAV, user.role as UserRole);

      return (
        <div className="min-h-screen bg-background">
          <Sidebar nav={visibleNav} />
          <MainContent>
            <TopBar user={user} nav={visibleNav} />  {/* TopBar transmet à MobileNavDrawer ; ajuster si la prop diffère */}
            <main className="flex-1 p-4 md:p-8 max-w-screen-2xl w-full mx-auto">{children}</main>
          </MainContent>
          <CommandPalette />
        </div>
      );
    }
    ```
    NB : Si `TopBar` ne propage pas la nav, ajuster `TopBar` pour passer `nav` à `<MobileMenuButton>` / `<MobileNavDrawer>`. Lire d'abord `top-bar.tsx` et `mobile-menu-button.tsx` pour identifier le chemin de propagation.

    Étape 6 — Tests `apps/web/src/components/layout/__tests__/nav-config.test.ts` (≥ 5) :
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { NAV, filterNavForRole } from '../nav-config';

    describe('filterNavForRole', () => {
      it('returns all sections for ADMIN', () => {
        const filtered = filterNavForRole(NAV, 'ADMIN');
        expect(filtered.length).toBeGreaterThan(0);
        // Utilisateurs is admin-only → must appear
        const allItems = filtered.flatMap(s => s.items.map(i => i.href));
        expect(allItems).toContain('/app/parametres/utilisateurs');
      });
      it('hides Utilisateurs and Historique and Paramètres for LECTEUR', () => {
        const filtered = filterNavForRole(NAV, 'LECTEUR');
        const allItems = filtered.flatMap(s => s.items.map(i => i.href));
        expect(allItems).not.toContain('/app/parametres/utilisateurs');
        expect(allItems).not.toContain('/app/parametres/historique');
        expect(allItems).not.toContain('/app/parametres');
      });
      it('hides Factures for FORMATEUR', () => {
        const filtered = filterNavForRole(NAV, 'FORMATEUR');
        const allItems = filtered.flatMap(s => s.items.map(i => i.href));
        expect(allItems).not.toContain('/app/factures');
      });
      it('shows Leads only for ADMIN/MANAGER/COMMERCIAL', () => {
        for (const role of ['FORMATEUR', 'COMPTABLE', 'LECTEUR'] as const) {
          const filtered = filterNavForRole(NAV, role);
          const allItems = filtered.flatMap(s => s.items.map(i => i.href));
          expect(allItems).not.toContain('/app/leads');
        }
      });
      it('drops sections that become empty after filtering', () => {
        const filtered = filterNavForRole(NAV, 'LECTEUR');
        for (const section of filtered) {
          expect(section.items.length).toBeGreaterThan(0);
        }
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "allowedRoles?: UserRole\[\]" apps/web/src/components/layout/nav-config.ts &&
      grep -q "export function filterNavForRole" apps/web/src/components/layout/nav-config.ts &&
      grep -q "/app/parametres/utilisateurs" apps/web/src/components/layout/nav-config.ts &&
      grep -q "filterNavForRole" apps/web/src/app/app/layout.tsx &&
      grep -q "nav: NavSection\[\]" apps/web/src/components/layout/sidebar-nav.tsx &&
      pnpm --filter @qualiof/web test --run src/components/layout/__tests__/nav-config.test.ts &&
      pnpm --filter @qualiof/web exec tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "allowedRoles" apps/web/src/components/layout/nav-config.ts` ≥ 6 (au moins 6 items annotés ADMIN/MANAGER/etc.)
    - `grep -c "filterNavForRole" apps/web/src/components/layout/nav-config.ts` ≥ 1 (export)
    - `grep -c "filterNavForRole" apps/web/src/app/app/layout.tsx` ≥ 1 (usage)
    - `grep -c "nav.map\|nav\\." apps/web/src/components/layout/sidebar-nav.tsx` ≥ 2 (utilise bien la prop)
    - `grep -c "/app/parametres/utilisateurs\\|/app/parametres/historique" apps/web/src/components/layout/nav-config.ts` ≥ 2 (les 2 nouveaux items)
    - Tests Vitest filterNavForRole : ≥ 5 passes
    - `tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    Nav config étendue avec allowedRoles. Filter pure fn testée. Layout app/app/layout.tsx propage la nav filtrée à Sidebar et MobileNavDrawer.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Page Utilisateurs + 4 client components (table / invite / row actions / role dialog)</name>
  <read_first>
    - apps/web/src/app/app/parametres/page.tsx (pattern Server Component qui rend des sections — REUSE structure)
    - apps/web/src/components/settings/of-identity-form.tsx (pattern client form avec react-hook-form + setError + toast)
    - apps/web/src/components/layout/user-menu-button.tsx (pattern Radix Dialog confirmation + DropdownMenu — REUSE pour disable confirmation + row actions)
    - apps/web/src/server/actions/tenant-users.ts (signatures exactes des 6 actions — 08-02)
    - apps/web/src/lib/rbac.ts (requireRole — 08-01)
  </read_first>
  <files>
    - apps/web/src/app/app/parametres/utilisateurs/page.tsx (CRÉER : Server Component, requireRole(['ADMIN']), liste users)
    - apps/web/src/components/users/users-table.tsx (CRÉER : tableau colonnes)
    - apps/web/src/components/users/invite-user-button.tsx (CRÉER : Radix Dialog + form react-hook-form)
    - apps/web/src/components/users/user-row-actions.tsx (CRÉER : DropdownMenu + 2 Dialogs confirmation)
    - apps/web/src/components/users/change-role-dialog.tsx (CRÉER : Radix Dialog avec Select)
    - apps/web/src/app/app/parametres/utilisateurs/__tests__/page.smoke.test.ts (CRÉER)
  </files>
  <behavior>
    - GET `/app/parametres/utilisateurs` :
      - First line: `const admin = await requireRole(['ADMIN'])` — throws UnauthorizedError/ForbiddenError, caught by `app/app/error.tsx`
      - Lists `prisma.user.findMany({ where: { tenantId: admin.tenantId }, orderBy: [{ disabledAt: 'asc' }, { lastName: 'asc' }] })`
      - Renders header "Utilisateurs (N)" + InviteUserButton + UsersTable
    - InviteUserButton (client) :
      - Bouton "Inviter un utilisateur" ouvre Radix Dialog
      - Form : email (input email), firstName, lastName, role (select with 6 options)
      - Submit → `inviteUser(input)` → if ok: toast.success (avec hint dry-run si data.dryRun), close dialog, reset form, router.refresh OR revalidatePath déclenchée côté action
      - On error fieldErrors: setError, toast.error
    - UsersTable (server, reçoit users in props from page) :
      - Cols : Email · Nom (firstName + lastName) · Rôle (badge) · Statut (Actif/Désactivé) · Dernière connexion (formatée fr-FR) · Actions (UserRowActions)
      - Rangée disabled : grise + tag "Désactivé" rouge
      - Self-row (admin.id === user.id) : badge "Vous"
    - UserRowActions (client) :
      - DropdownMenu Radix (chevron icon) avec :
        - "Modifier le rôle" → ouvre `<ChangeRoleDialog userId={...} currentRole={...} />`
        - "Renvoyer l'invitation" → `resendInvitation(userId)` direct (pas de confirmation)
        - "Réinitialiser le mot de passe" → ouvre Radix Dialog confirmation → si ok: `resetUserPassword(userId)`
        - Séparateur
        - Si user.disabledAt null : "Désactiver" (rouge) → Dialog confirmation → `disableUser(userId)`
        - Si user.disabledAt !null : "Réactiver" → `enableUser(userId)` direct
      - Tous les calls action utilisent `useTransition`, affichent toast.success/error
    - ChangeRoleDialog (client) :
      - Form simple : select avec 6 UserRole, bouton "Enregistrer"
      - Submit → `changeUserRole({ userId, role })` → toast + close
  </behavior>
  <action>
    Étape 1 — `apps/web/src/app/app/parametres/utilisateurs/page.tsx` :

    ```typescript
    import { requireRole } from '@/lib/rbac';
    import { prisma } from '@qualiof/db';
    import { UsersTable } from '@/components/users/users-table';
    import { InviteUserButton } from '@/components/users/invite-user-button';

    export const dynamic = 'force-dynamic';

    export default async function UsersAdminPage() {
      const admin = await requireRole(['ADMIN']);
      const users = await prisma.user.findMany({
        where: { tenantId: admin.tenantId },
        orderBy: [{ disabledAt: 'asc' }, { lastName: 'asc' }],
        select: { id: true, email: true, firstName: true, lastName: true, role: true,
                  disabledAt: true, lastLoginAt: true, invitedAt: true },
      });
      return (
        <div className="space-y-6">
          <div className="flex flex-wrap items-end justify-between gap-3">
            <div>
              <h1 className="text-2xl font-bold">Utilisateurs</h1>
              <p className="text-sm text-muted-foreground">{users.length} utilisateur(s) enregistré(s)</p>
            </div>
            <InviteUserButton />
          </div>
          <UsersTable users={users} currentUserId={admin.id} />
        </div>
      );
    }
    ```

    Étape 2 — `apps/web/src/components/users/users-table.tsx` (server component pour le rendu, mais peut importer le client component UserRowActions) :

    ```typescript
    import { UserRowActions } from './user-row-actions';
    import type { UserRole } from '@prisma/client';

    interface UserRow {
      id: string; email: string; firstName: string; lastName: string;
      role: UserRole; disabledAt: Date | null; lastLoginAt: Date | null; invitedAt: Date | null;
    }
    export function UsersTable({ users, currentUserId }: { users: UserRow[]; currentUserId: string }) {
      const fmtDate = (d: Date | null) => d ? new Intl.DateTimeFormat('fr-FR', { dateStyle: 'short', timeStyle: 'short' }).format(d) : '—';
      return (
        <div className="rounded-lg border border-border overflow-x-auto -mx-4 sm:mx-0">
          <table className="w-full text-sm">
            <thead className="bg-muted/50 text-xs uppercase">
              <tr>
                <th className="text-left p-3">Email</th>
                <th className="text-left p-3">Nom</th>
                <th className="text-left p-3">Rôle</th>
                <th className="text-left p-3">Statut</th>
                <th className="text-left p-3">Dernière connexion</th>
                <th className="text-right p-3">Actions</th>
              </tr>
            </thead>
            <tbody>
              {users.map((u) => (
                <tr key={u.id} className={u.disabledAt ? 'opacity-50' : ''}>
                  <td className="p-3 font-medium">{u.email}{u.id === currentUserId && <span className="ml-2 text-[10px] px-1.5 py-0.5 rounded bg-primary-50 text-primary-700 font-semibold">vous</span>}</td>
                  <td className="p-3">{u.firstName} {u.lastName}</td>
                  <td className="p-3"><span className="text-xs px-1.5 py-0.5 rounded bg-slate-100">{u.role}</span></td>
                  <td className="p-3">{u.disabledAt ? <span className="text-xs px-1.5 py-0.5 rounded bg-red-50 text-red-700">Désactivé</span> : <span className="text-xs px-1.5 py-0.5 rounded bg-emerald-50 text-emerald-700">Actif</span>}</td>
                  <td className="p-3 text-xs text-muted-foreground">{fmtDate(u.lastLoginAt)}</td>
                  <td className="p-3 text-right"><UserRowActions userId={u.id} userEmail={u.email} userRole={u.role} disabled={u.disabledAt !== null} isSelf={u.id === currentUserId} /></td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      );
    }
    ```

    Étape 3 — `apps/web/src/components/users/invite-user-button.tsx` (client, Radix Dialog + RHF) :

    Pattern de référence : `user-menu-button.tsx` (déjà utilise Dialog.Root + Dialog.Portal + Dialog.Overlay + Dialog.Content) → DUPLIQUER cette structure + form intérieur avec react-hook-form + zodResolver(inviteUserSchema) + select 6 rôles.

    Squelette :
    ```typescript
    'use client';
    import { useState, useTransition } from 'react';
    import * as Dialog from '@radix-ui/react-dialog';
    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { toast } from 'sonner';
    import { Mail } from 'lucide-react';
    import { inviteUserSchema, type InviteUserInput } from '@qualiof/shared';
    import { inviteUser } from '@/server/actions/tenant-users';

    const ROLES = ['ADMIN', 'MANAGER', 'COMMERCIAL', 'FORMATEUR', 'COMPTABLE', 'LECTEUR'] as const;

    export function InviteUserButton() {
      const [open, setOpen] = useState(false);
      const [pending, startTransition] = useTransition();
      const { register, handleSubmit, setError, reset, formState: { errors } } = useForm<InviteUserInput>({
        resolver: zodResolver(inviteUserSchema),
        defaultValues: { email: '', firstName: '', lastName: '', role: 'LECTEUR' },
      });
      const onSubmit = (data: InviteUserInput) => {
        startTransition(async () => {
          const result = await inviteUser(data);
          if (result.ok) {
            toast.success(result.data?.dryRun ? `Invitation envoyée (mode dev — mail non réellement envoyé)` : `Invitation envoyée à ${data.email}`);
            reset(); setOpen(false);
          } else {
            if (result.fieldErrors) for (const [k, msg] of Object.entries(result.fieldErrors)) if (msg?.[0]) setError(k as keyof InviteUserInput, { message: msg[0] });
            toast.error(result.error);
          }
        });
      };
      return (
        <Dialog.Root open={open} onOpenChange={setOpen}>
          <Dialog.Trigger asChild>
            <button type="button" className="inline-flex items-center gap-2 rounded-md bg-primary text-white text-sm font-medium px-3 py-2 hover:bg-primary/90"><Mail className="h-4 w-4" />Inviter un utilisateur</button>
          </Dialog.Trigger>
          <Dialog.Portal>
            <Dialog.Overlay className="fixed inset-0 bg-foreground/40 backdrop-blur-sm z-40" />
            <Dialog.Content className="fixed left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 z-50 w-[480px] max-w-[90vw] rounded-lg border border-border bg-white p-6 shadow-xl">
              <Dialog.Title className="text-lg font-semibold mb-1">Inviter un utilisateur</Dialog.Title>
              <Dialog.Description className="text-sm text-muted-foreground mb-4">Un email avec lien d'activation (valable 7 jours) sera envoyé.</Dialog.Description>
              <form onSubmit={handleSubmit(onSubmit)} className="space-y-3">
                <div>
                  <label className="block text-sm font-medium mb-1">Email</label>
                  <input type="email" {...register('email')} className="w-full rounded-md border border-border px-3 py-2 text-sm" />
                  {errors.email && <p className="text-xs text-red-600 mt-1">{errors.email.message}</p>}
                </div>
                <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  <div>
                    <label className="block text-sm font-medium mb-1">Prénom</label>
                    <input {...register('firstName')} className="w-full rounded-md border border-border px-3 py-2 text-sm" />
                    {errors.firstName && <p className="text-xs text-red-600 mt-1">{errors.firstName.message}</p>}
                  </div>
                  <div>
                    <label className="block text-sm font-medium mb-1">Nom</label>
                    <input {...register('lastName')} className="w-full rounded-md border border-border px-3 py-2 text-sm" />
                    {errors.lastName && <p className="text-xs text-red-600 mt-1">{errors.lastName.message}</p>}
                  </div>
                </div>
                <div>
                  <label className="block text-sm font-medium mb-1">Rôle</label>
                  <select {...register('role')} className="w-full rounded-md border border-border px-3 py-2 text-sm">
                    {ROLES.map(r => <option key={r} value={r}>{r}</option>)}
                  </select>
                  {errors.role && <p className="text-xs text-red-600 mt-1">{errors.role.message}</p>}
                </div>
                <div className="flex justify-end gap-2 pt-2">
                  <Dialog.Close asChild>
                    <button type="button" className="rounded-md border border-border px-3 py-2 text-sm">Annuler</button>
                  </Dialog.Close>
                  <button type="submit" disabled={pending} className="rounded-md bg-primary text-white text-sm font-medium px-3 py-2 hover:bg-primary/90 disabled:opacity-50">
                    {pending ? 'Envoi…' : 'Envoyer l\'invitation'}
                  </button>
                </div>
              </form>
            </Dialog.Content>
          </Dialog.Portal>
        </Dialog.Root>
      );
    }
    ```

    Étape 4 — `apps/web/src/components/users/user-row-actions.tsx` (client, DropdownMenu + 2 Dialogs confirmation) :

    Pattern de référence : `user-menu-button.tsx` (DropdownMenu + Dialog confirmation). Reproduire pour 5 actions:
    - "Modifier le rôle" → setRoleDialogOpen(true) → rend `<ChangeRoleDialog open={roleDialogOpen} onOpenChange={setRoleDialogOpen} userId={userId} currentRole={userRole} />`
    - "Renvoyer l'invitation" → resendInvitation(userId) direct + toast
    - "Réinitialiser le mot de passe" → setResetDialogOpen(true) → Dialog "Confirmer ? Envoyer un email à {userEmail}" → resetUserPassword(userId)
    - "Désactiver" ou "Réactiver" selon `disabled` prop ; Désactiver passe par Dialog confirmation (action destructive), Réactiver direct.
    - Si `isSelf && !disabled` : griser "Désactiver"

    Étape 5 — `apps/web/src/components/users/change-role-dialog.tsx` (client) :
    ```typescript
    'use client';
    // Dialog avec select 6 roles → submit changeUserRole({ userId, role })
    // toast success/error, fermer dialog
    ```

    Étape 6 — Smoke test `apps/web/src/app/app/parametres/utilisateurs/__tests__/page.smoke.test.ts` :
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    const src = readFileSync(resolve(__dirname, '../page.tsx'), 'utf-8');
    describe('users page smoke', () => {
      it('calls requireRole(["ADMIN"]) first', () => {
        expect(src).toMatch(/requireRole\(\['ADMIN'\]\)/);
      });
      it('queries users scoped to tenantId', () => {
        expect(src).toMatch(/prisma\.user\.findMany/);
        expect(src).toMatch(/tenantId:\s*admin\.tenantId/);
      });
      it('renders InviteUserButton and UsersTable', () => {
        expect(src).toMatch(/InviteUserButton/);
        expect(src).toMatch(/UsersTable/);
      });
      it('imports lucide-react icons that are used', () => {
        // ensure no undefined icon reference
        expect(src).not.toMatch(/<[A-Z][a-zA-Z]+\s/g) || true;  // structural — actual icon check à compléter selon page
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "requireRole(\\['ADMIN'\\])" "apps/web/src/app/app/parametres/utilisateurs/page.tsx" &&
      grep -q "export function UsersTable" "apps/web/src/components/users/users-table.tsx" &&
      grep -q "export function InviteUserButton" "apps/web/src/components/users/invite-user-button.tsx" &&
      grep -q "export function UserRowActions" "apps/web/src/components/users/user-row-actions.tsx" &&
      grep -q "export function ChangeRoleDialog" "apps/web/src/components/users/change-role-dialog.tsx" &&
      grep -q "inviteUser\\|disableUser\\|enableUser\\|resetUserPassword\\|changeUserRole\\|resendInvitation" "apps/web/src/components/users/user-row-actions.tsx" &&
      pnpm --filter @qualiof/web test --run "src/app/app/parametres/utilisateurs/__tests__/page.smoke.test.ts" &&
      pnpm --filter @qualiof/web exec tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireRole" apps/web/src/app/app/parametres/utilisateurs/page.tsx` ≥ 1
    - `grep -c "tenantId: admin.tenantId" apps/web/src/app/app/parametres/utilisateurs/page.tsx` ≥ 1 (multi-tenant scope)
    - `ls apps/web/src/components/users/*.tsx | wc -l` ≥ 4 (users-table, invite-user-button, user-row-actions, change-role-dialog ; + set-password-form de 08-03 = 5)
    - `grep -c "@radix-ui/react-dialog\\|@radix-ui/react-dropdown-menu" apps/web/src/components/users/*.tsx` ≥ 3 (Dialog dans invite + row-actions + change-role)
    - `grep -c "toast" apps/web/src/components/users/*.tsx` ≥ 4 (success/error feedback partout)
    - `grep -c "useTransition" apps/web/src/components/users/*.tsx` ≥ 3
    - Smoke test : ≥ 3 passes
    - `tsc --noEmit` : 0 erreur
    - `pnpm --filter @qualiof/web build` reste vert
  </acceptance_criteria>
  <done>
    Page /app/parametres/utilisateurs fonctionnelle : admin invite, désactive/réactive, reset MDP, change rôle, renvoie invitation. Sidebar filtrée par rôle (visuellement). Tout passe par les server actions de 08-02.
  </done>
</task>

</tasks>

<verification>
- Manuel : connecter en ADMIN → voir page Utilisateurs + items "Utilisateurs" et "Paramètres" dans sidebar.
- Manuel : connecter en LECTEUR (créer un user LECTEUR via inviteUser puis accepter via 08-03) → "Utilisateurs" et "Paramètres" doivent disparaître de la sidebar.
- Manuel : si LECTEUR tape directement `/app/parametres/utilisateurs` → error.tsx page (ForbiddenError thrown par requireRole).
- Automated : tests filterNavForRole + smoke page passent ; tsc + build clean.
</verification>

<success_criteria>
- RBAC-01 livré : page CRUD users opérationnelle pour ADMIN
- RBAC-03 livré : sidebar filtrée par rôle (UI uniquement, sécurité dans requireRole)
- Sidebar items configurés selon matrice D-02 PERMISSIONS
- 4 client components créés + 1 server component
- Self-protection : ADMIN ne peut pas se désactiver ni se retirer ADMIN
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-utilisateurs-et-rbac/08-04-SUMMARY.md`
</output>