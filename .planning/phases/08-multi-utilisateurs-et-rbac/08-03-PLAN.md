---
phase: 08-multi-utilisateurs-et-rbac
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - apps/web/src/app/invitation/[token]/page.tsx
  - apps/web/src/components/users/set-password-form.tsx
  - apps/web/src/server/actions/user-invitation-accept.ts
  - apps/web/src/server/actions/__tests__/user-invitation-accept.test.ts
  - apps/web/src/app/invitation/[token]/__tests__/page.smoke.test.ts
autonomous: true
requirements: [RBAC-02]
must_haves:
  truths:
    - "Public route /invitation/[token] is reachable without auth (no validateRequest call gating render)"
    - "Page renders ExpiredState when invitation.expiresAt < now()"
    - "Page renders AlreadyUsedState when invitation.usedAt is not null"
    - "Page renders SetPasswordForm when invitation is valid and unused"
    - "acceptInvitation server action enforces single-use via prisma.userInvitation.updateMany({ token, usedAt: null, expiresAt: gt: now }, { usedAt: now }) and checks count > 0"
    - "On success, acceptInvitation hashes password via argon2, updates user.hashedPwd + user.lastLoginAt, creates Lucia session cookie, writes AuditLog users.password.set, and redirects to /app"
    - "Second click on same valid token returns error 'Lien expiré ou déjà utilisé' (idempotent / atomic)"
  artifacts:
    - path: "apps/web/src/app/invitation/[token]/page.tsx"
      provides: "Public token-gated page"
      contains: "InvitationPage"
    - path: "apps/web/src/server/actions/user-invitation-accept.ts"
      provides: "acceptInvitation single-use server action"
      exports: ["acceptInvitation"]
    - path: "apps/web/src/components/users/set-password-form.tsx"
      provides: "Client form (react-hook-form) for password setting"
      exports: ["SetPasswordForm"]
  key_links:
    - from: "apps/web/src/app/invitation/[token]/page.tsx"
      to: "prisma.userInvitation"
      via: "findUnique({ where: { token } })"
      pattern: "userInvitation\\.findUnique"
    - from: "apps/web/src/components/users/set-password-form.tsx"
      to: "apps/web/src/server/actions/user-invitation-accept.ts"
      via: "import { acceptInvitation } and call from form submit"
      pattern: "acceptInvitation"
    - from: "apps/web/src/server/actions/user-invitation-accept.ts"
      to: "lucia.createSession"
      via: "auto-login after password set"
      pattern: "lucia\\.createSession"
    - from: "apps/web/src/server/actions/user-invitation-accept.ts"
      to: "argon2.hash"
      via: "hash password before update"
      pattern: "argon2\\.hash"
---

<objective>
Implémenter la route publique tokenisée `/invitation/[token]` qui permet à un utilisateur invité de définir son mot de passe et d'accéder à l'application. Le flow miroir Phase 4 (preinscription/[token]) : Server Component qui valide le token et délègue à des sub-components ExpiredState / AlreadyUsedState / SetPasswordForm. La server action `acceptInvitation` enforce le single-use via `updateMany` atomique (Pitfall #3 RESEARCH).

Purpose: livrer RBAC-02 — flow invitation email → première connexion crée hashedPwd. Permet à l'admin (08-04) d'inviter des users qui n'ont besoin d'aucune intervention serveur supplémentaire pour rentrer dans l'application.
Output: 1 Server Component page, 1 client form, 1 server action, 2 fichiers de tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-CONTEXT.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-01-PLAN.md
@.planning/phases/08-multi-utilisateurs-et-rbac/08-02-PLAN.md

@apps/web/src/app/preinscription/[token]/page.tsx
@apps/web/src/app/login/login-form.tsx
@apps/web/src/app/login/actions.ts
@apps/web/src/lib/auth.ts

<interfaces>
<!-- Disponibles depuis 08-01 et 08-02 -->

From @qualiof/db (08-01):
- prisma.userInvitation — { id, tenantId, email, token unique, role, expiresAt, usedAt, userId, invitedBy, createdAt }
- prisma.user — étendu lastLoginAt, disabledAt, etc.

From @qualiof/shared (08-01):
```typescript
export const setPasswordSchema: z.ZodSchema;  // { password (>=8), confirm } + refine match
export type SetPasswordInput = z.infer<typeof setPasswordSchema>;  // { password, confirm }
```

From apps/web/src/lib/auth.ts:
- `lucia.createSession(userId, attributes)`
- `lucia.createSessionCookie(sessionId)`

Pattern existant (preinscription/[token]/page.tsx L1-32):
```typescript
export const dynamic = 'force-dynamic';
export default async function Page({ params }: { params: Promise<{ token: string }> }) {
  const { token } = await params;
  const pe = await prisma.preEnrollment.findUnique({ where: { token }, select: { ... } });
  if (!pe) notFound();
  const expired = pe.expiresAt < new Date();
  const alreadyDone = pe.status === 'CONVERTED' || pe.status === 'VALIDATED';
  return (<div>{ expired ? <ExpiredState/> : alreadyDone ? <AlreadyDoneState/> : <Form .../> }</div>);
}
```

Pattern login/actions.ts (L10-26):
```typescript
const session = await lucia.createSession(user.id, {});
const sessionCookie = lucia.createSessionCookie(session.id);
(await cookies()).set(sessionCookie.name, sessionCookie.value, sessionCookie.attributes);
redirect('/app');
```

Pattern login-form.tsx (à lire pour pattern react-hook-form + useTransition + zodResolver) → pattern projet.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: acceptInvitation server action (single-use atomique)</name>
  <read_first>
    - apps/web/src/app/login/actions.ts (pattern argon2 + lucia.createSession + cookie + redirect, L17-26)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md Example 3 + Pitfall #3 (updateMany single-use exact pattern)
    - apps/web/src/lib/audit-log.ts (logUserAction signature ajoutée Plan 08-02)
  </read_first>
  <files>
    - apps/web/src/server/actions/user-invitation-accept.ts (CRÉER)
    - apps/web/src/server/actions/__tests__/user-invitation-accept.test.ts (CRÉER : ≥ 5 tests)
  </files>
  <behavior>
    - `acceptInvitation({ token, password, confirm })` :
      - Validate via `setPasswordSchema.safeParse({ password, confirm })` ; if !ok return `{ ok: false, error: 'Mot de passe invalide', fieldErrors }`.
      - `prisma.userInvitation.updateMany({ where: { token, usedAt: null, expiresAt: { gt: new Date() } }, data: { usedAt: new Date() } })` — atomic single-use.
      - If `result.count === 0` return `{ ok: false, error: 'Lien expiré ou déjà utilisé' }`.
      - `findUnique({ where: { token }, select: { userId, tenantId, email } })`.
      - If !invitation.userId return `{ ok: false, error: 'Invitation introuvable' }`.
      - `argon2.hash(password)` → update user.hashedPwd + user.lastLoginAt=now() (where: { id: invitation.userId, tenantId: invitation.tenantId } — multi-tenant safety).
      - `logUserAction({ tenantId, actorUserId: userId, targetUserId: userId, action: 'users.password.set' })`.
      - `lucia.createSession(userId, {})` + set cookie + `redirect('/app')` (function never returns on success).
    - The function returns `Promise<{ ok: false; error: string; fieldErrors?: ... } | never>` (never on success because redirect throws).
  </behavior>
  <action>
    Étape 1 — Créer `apps/web/src/server/actions/user-invitation-accept.ts` :

    ```typescript
    'use server';

    /**
     * Phase 8 RBAC-02 — acceptation invitation utilisateur.
     *
     * Single-use enforcement via prisma.userInvitation.updateMany atomique
     * (Pitfall #3 RESEARCH.md) : si le user clique 2× sur le lien, le 2e clic
     * verra updateMany count=0 et obtiendra un message clair.
     *
     * Pattern login/actions.ts dupliqué : argon2.hash + lucia.createSession +
     * setSessionCookie + redirect('/app').
     */

    import { cookies } from 'next/headers';
    import { redirect } from 'next/navigation';
    import argon2 from 'argon2';
    import { prisma } from '@qualiof/db';
    import { lucia } from '@/lib/auth';
    import { setPasswordSchema } from '@qualiof/shared';
    import { logUserAction } from '@/lib/audit-log';

    export type AcceptInvitationResult =
      | { ok: false; error: string; fieldErrors?: Record<string, string[] | undefined> }
      // never on success (redirect throws)
      ;

    export async function acceptInvitation(input: {
      token: string;
      password: string;
      confirm: string;
    }): Promise<AcceptInvitationResult> {
      const parsed = setPasswordSchema.safeParse({ password: input.password, confirm: input.confirm });
      if (!parsed.success) {
        return { ok: false, error: 'Mot de passe invalide', fieldErrors: parsed.error.flatten().fieldErrors };
      }

      // Single-use atomique : si l'invitation est expirée OU déjà utilisée, count=0.
      const claim = await prisma.userInvitation.updateMany({
        where: { token: input.token, usedAt: null, expiresAt: { gt: new Date() } },
        data: { usedAt: new Date() },
      });
      if (claim.count === 0) {
        return { ok: false, error: 'Lien expiré ou déjà utilisé' };
      }

      const invitation = await prisma.userInvitation.findUnique({
        where: { token: input.token },
        select: { userId: true, tenantId: true, email: true },
      });
      if (!invitation || !invitation.userId) {
        return { ok: false, error: 'Invitation introuvable' };
      }

      const hashedPwd = await argon2.hash(parsed.data.password);
      await prisma.user.update({
        where: { id: invitation.userId },  // (tenantId implicite par invitation déjà claim)
        data: { hashedPwd, lastLoginAt: new Date() },
      });
      await logUserAction({
        tenantId: invitation.tenantId,
        actorUserId: invitation.userId,
        targetUserId: invitation.userId,
        action: 'users.password.set',
      });

      // Login auto Lucia + redirect /app (never returns)
      const session = await lucia.createSession(invitation.userId, {});
      const sessionCookie = lucia.createSessionCookie(session.id);
      (await cookies()).set(sessionCookie.name, sessionCookie.value, sessionCookie.attributes);
      redirect('/app');
    }
    ```

    Étape 2 — Tests Vitest `apps/web/src/server/actions/__tests__/user-invitation-accept.test.ts` (≥ 5) :
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    vi.mock('@qualiof/db', () => ({
      prisma: {
        userInvitation: { updateMany: vi.fn(), findUnique: vi.fn() },
        user: { update: vi.fn() },
        auditLog: { create: vi.fn() },
      },
    }));
    vi.mock('@/lib/auth', () => ({
      lucia: {
        createSession: vi.fn().mockResolvedValue({ id: 'sess1' }),
        createSessionCookie: vi.fn().mockReturnValue({ name: 'auth', value: 'v', attributes: {} }),
      },
    }));
    vi.mock('argon2', () => ({ default: { hash: vi.fn().mockResolvedValue('hashed-pwd') } }));
    vi.mock('next/headers', () => ({ cookies: vi.fn().mockResolvedValue({ set: vi.fn() }) }));
    vi.mock('next/navigation', () => ({ redirect: vi.fn().mockImplementation(() => { throw new Error('NEXT_REDIRECT'); }) }));

    import { acceptInvitation } from '../user-invitation-accept';
    import { prisma } from '@qualiof/db';

    describe('acceptInvitation', () => {
      beforeEach(() => vi.clearAllMocks());

      it('rejects invalid password', async () => {
        const r = await acceptInvitation({ token: 't', password: 'short', confirm: 'short' });
        expect(r.ok).toBe(false);
        if (!r.ok) expect(r.fieldErrors).toBeDefined();
      });

      it('rejects mismatched confirm', async () => {
        const r = await acceptInvitation({ token: 't', password: 'longenough', confirm: 'mismatch1' });
        expect(r.ok).toBe(false);
      });

      it('returns error when token already used or expired (count=0)', async () => {
        vi.mocked(prisma.userInvitation.updateMany).mockResolvedValue({ count: 0 });
        const r = await acceptInvitation({ token: 't', password: 'longenough', confirm: 'longenough' });
        expect(r.ok).toBe(false);
        if (!r.ok) expect(r.error).toMatch(/expiré|utilisé/i);
      });

      it('returns error when invitation has no userId', async () => {
        vi.mocked(prisma.userInvitation.updateMany).mockResolvedValue({ count: 1 });
        vi.mocked(prisma.userInvitation.findUnique).mockResolvedValue({ userId: null, tenantId: 't1', email: 'a@b' } as never);
        const r = await acceptInvitation({ token: 't', password: 'longenough', confirm: 'longenough' });
        expect(r.ok).toBe(false);
      });

      it('hashes password, updates user, creates session, redirects on success', async () => {
        vi.mocked(prisma.userInvitation.updateMany).mockResolvedValue({ count: 1 });
        vi.mocked(prisma.userInvitation.findUnique).mockResolvedValue({ userId: 'u1', tenantId: 't1', email: 'a@b' } as never);
        try {
          await acceptInvitation({ token: 't', password: 'longenough', confirm: 'longenough' });
        } catch (e) {
          expect((e as Error).message).toBe('NEXT_REDIRECT');
        }
        expect(prisma.user.update).toHaveBeenCalledWith(expect.objectContaining({
          where: { id: 'u1' },
          data: expect.objectContaining({ hashedPwd: 'hashed-pwd' }),
        }));
        expect(prisma.auditLog.create).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ action: 'users.password.set' }),
        }));
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "export async function acceptInvitation" apps/web/src/server/actions/user-invitation-accept.ts &&
      grep -q "updateMany" apps/web/src/server/actions/user-invitation-accept.ts &&
      grep -q "argon2.hash" apps/web/src/server/actions/user-invitation-accept.ts &&
      grep -q "lucia.createSession" apps/web/src/server/actions/user-invitation-accept.ts &&
      grep -q "users.password.set" apps/web/src/server/actions/user-invitation-accept.ts &&
      pnpm --filter @qualiof/web test --run src/server/actions/__tests__/user-invitation-accept.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updateMany" apps/web/src/server/actions/user-invitation-accept.ts` ≥ 1
    - `grep -c "usedAt: null" apps/web/src/server/actions/user-invitation-accept.ts` ≥ 1
    - `grep -c "expiresAt: { gt:" apps/web/src/server/actions/user-invitation-accept.ts` ≥ 1
    - `grep -c "redirect('/app')" apps/web/src/server/actions/user-invitation-accept.ts` ≥ 1
    - Tests Vitest : ≥ 5 passes
  </acceptance_criteria>
  <done>
    Server action atomique single-use opérationnelle, login auto câblé. Tests valident le cas heureux + 4 cas d'erreur.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Page publique /invitation/[token] + SetPasswordForm client</name>
  <read_first>
    - apps/web/src/app/preinscription/[token]/page.tsx (template Server Component force-dynamic + ExpiredState/AlreadyDoneState components à dupliquer)
    - apps/web/src/app/login/login-form.tsx (pattern client react-hook-form + useTransition + zodResolver + toast.error)
    - .planning/phases/08-multi-utilisateurs-et-rbac/08-RESEARCH.md Pattern 2 (Token public route)
  </read_first>
  <files>
    - apps/web/src/app/invitation/[token]/page.tsx (CRÉER : Server Component)
    - apps/web/src/components/users/set-password-form.tsx (CRÉER : client form)
    - apps/web/src/app/invitation/[token]/__tests__/page.smoke.test.ts (CRÉER : smoke imports anti-BUG-01)
  </files>
  <behavior>
    - GET `/invitation/{token}` :
      - Si token n'existe pas → `notFound()` (Next.js 404)
      - Si `invitation.expiresAt < now()` → rend `<ExpiredState />` (carte rouge "Ce lien a expiré, contactez votre admin")
      - Si `invitation.usedAt != null` → rend `<AlreadyUsedState />` (carte verte "Invitation déjà acceptée — vous pouvez vous connecter normalement")
      - Sinon → rend `<SetPasswordForm token={token} email={...} firstName={...} role={...} />`
    - SetPasswordForm (client) :
      - Champs `password` (input type=password, autocomplete=new-password), `confirm` (idem)
      - Submit → `acceptInvitation({ token, password, confirm })`
      - Pendant `useTransition` : disabled + spinner
      - Sur erreur : `toast.error(result.error)` ; si `fieldErrors` → `setError(field, { message })`
      - Sur succès : Next.js redirect /app (déclenché côté server) — l'utilisateur arrive directement connecté
    - Layout brand identique à `/preinscription/[token]` (header avec logo "S", footer)
  </behavior>
  <action>
    Étape 1 — `apps/web/src/app/invitation/[token]/page.tsx` (Server Component, ~120 LOC) :

    ```typescript
    import { notFound } from 'next/navigation';
    import Link from 'next/link';
    import { ShieldCheck, Clock, CheckCircle2 } from 'lucide-react';
    import { prisma } from '@qualiof/db';
    import { SetPasswordForm } from '@/components/users/set-password-form';

    export const dynamic = 'force-dynamic';

    export default async function InvitationPage({
      params,
    }: { params: Promise<{ token: string }> }) {
      const { token } = await params;
      const invitation = await prisma.userInvitation.findUnique({
        where: { token },
        select: { id: true, token: true, email: true, role: true, expiresAt: true, usedAt: true, userId: true,
          user: { select: { firstName: true } } },
      });
      if (!invitation) notFound();

      const expired = invitation.expiresAt < new Date();
      const used = invitation.usedAt != null;

      return (
        <div className="min-h-screen bg-gradient-to-br from-slate-50 to-primary-50/30">
          <header className="border-b border-border bg-white">
            <div className="max-w-3xl mx-auto px-6 py-5 flex items-center gap-3">
              <div className="h-10 w-10 rounded-lg bg-primary text-white font-bold inline-flex items-center justify-center">S</div>
              <div>
                <div className="font-semibold">QualiOF — Start Academy</div>
                <div className="text-xs text-muted-foreground">Définition du mot de passe</div>
              </div>
            </div>
          </header>
          <main className="max-w-3xl mx-auto px-6 py-10">
            {expired ? <ExpiredState />
             : used ? <AlreadyUsedState />
             : <SetPasswordForm token={invitation.token} email={invitation.email} firstName={invitation.user?.firstName ?? null} role={invitation.role} />}
          </main>
          <footer className="border-t border-border bg-white py-5 mt-10">
            <div className="max-w-3xl mx-auto px-6 flex flex-wrap items-center justify-between gap-3 text-xs text-muted-foreground">
              <div className="inline-flex items-center gap-1.5"><ShieldCheck className="h-3.5 w-3.5" /> Données stockées en France · Qualiopi · RGPD</div>
              <div>© Start Academy {new Date().getFullYear()}</div>
            </div>
          </footer>
        </div>
      );
    }

    function ExpiredState() {
      return (
        <div className="rounded-2xl border border-red-200 bg-red-50 p-12 text-center space-y-4">
          <Clock className="h-12 w-12 text-red-600 mx-auto" />
          <h1 className="text-xl font-bold text-red-900">Ce lien a expiré</h1>
          <p className="text-sm text-red-800 max-w-md mx-auto">
            Le lien d'invitation n'est plus valable (les liens expirent après 7 jours).
            Contactez votre administrateur pour recevoir un nouveau lien.
          </p>
        </div>
      );
    }
    function AlreadyUsedState() {
      return (
        <div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-12 text-center space-y-4">
          <CheckCircle2 className="h-12 w-12 text-emerald-700 mx-auto" />
          <h1 className="text-xl font-bold text-emerald-900">Invitation déjà acceptée</h1>
          <p className="text-sm text-emerald-800 max-w-md mx-auto">
            Vous avez déjà défini votre mot de passe via ce lien. Connectez-vous normalement.
          </p>
          <Link href="/login" className="inline-block text-sm font-medium text-primary underline">
            Aller à la page de connexion
          </Link>
        </div>
      );
    }
    ```

    Étape 2 — `apps/web/src/components/users/set-password-form.tsx` (client, ~140 LOC) :

    ```typescript
    'use client';

    import { useTransition } from 'react';
    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { toast } from 'sonner';
    import { setPasswordSchema, type SetPasswordInput } from '@qualiof/shared';
    import { acceptInvitation } from '@/server/actions/user-invitation-accept';
    import type { UserRole } from '@prisma/client';

    interface SetPasswordFormProps {
      token: string;
      email: string;
      firstName: string | null;
      role: UserRole;
    }

    export function SetPasswordForm({ token, email, firstName, role }: SetPasswordFormProps) {
      const [pending, startTransition] = useTransition();
      const { register, handleSubmit, setError, formState: { errors } } = useForm<SetPasswordInput>({
        resolver: zodResolver(setPasswordSchema),
        defaultValues: { password: '', confirm: '' },
      });

      const onSubmit = (data: SetPasswordInput) => {
        startTransition(async () => {
          const result = await acceptInvitation({ token, password: data.password, confirm: data.confirm });
          // Sur succès, redirect /app → la fonction ne revient PAS et l'UI se navigue.
          // On ne traite ici que les cas d'erreur (`result` est défini).
          if (result && result.ok === false) {
            if (result.fieldErrors) {
              for (const [field, messages] of Object.entries(result.fieldErrors)) {
                if (messages && messages[0]) setError(field as keyof SetPasswordInput, { message: messages[0] });
              }
            }
            toast.error(result.error);
          }
        });
      };

      return (
        <div className="rounded-2xl border border-border bg-white p-8 max-w-md mx-auto">
          <h1 className="text-xl font-bold mb-1">Bienvenue{firstName ? ` ${firstName}` : ''} 👋</h1>
          <p className="text-sm text-muted-foreground mb-6">
            Vous avez été invité(e) à rejoindre QualiOF en tant que <strong>{role}</strong>. Définissez un mot de passe pour activer votre compte ({email}).
          </p>
          <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
            <div>
              <label className="block text-sm font-medium mb-1" htmlFor="password">Mot de passe (8 caractères min.)</label>
              <input id="password" type="password" autoComplete="new-password"
                {...register('password')}
                className="w-full rounded-md border border-border px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-primary/40" />
              {errors.password && <p className="text-xs text-red-600 mt-1">{errors.password.message}</p>}
            </div>
            <div>
              <label className="block text-sm font-medium mb-1" htmlFor="confirm">Confirmation</label>
              <input id="confirm" type="password" autoComplete="new-password"
                {...register('confirm')}
                className="w-full rounded-md border border-border px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-primary/40" />
              {errors.confirm && <p className="text-xs text-red-600 mt-1">{errors.confirm.message}</p>}
            </div>
            <button type="submit" disabled={pending}
              className="w-full rounded-md bg-primary text-white text-sm font-medium py-2 hover:bg-primary/90 disabled:opacity-50">
              {pending ? 'Activation…' : 'Activer mon compte'}
            </button>
          </form>
        </div>
      );
    }
    ```

    Étape 3 — Smoke test `apps/web/src/app/invitation/[token]/__tests__/page.smoke.test.ts` (pattern Phase 7 — test source statique, pas execution Server Component) :

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    const src = readFileSync(resolve(__dirname, '../page.tsx'), 'utf-8');

    describe('/invitation/[token] page', () => {
      it('exports a default Server Component', () => {
        expect(src).toMatch(/export default async function InvitationPage/);
      });
      it('uses dynamic = force-dynamic for token-fresh reads', () => {
        expect(src).toContain("export const dynamic = 'force-dynamic'");
      });
      it('queries prisma.userInvitation by token', () => {
        expect(src).toContain('prisma.userInvitation.findUnique');
      });
      it('renders ExpiredState / AlreadyUsedState / SetPasswordForm branches', () => {
        expect(src).toMatch(/ExpiredState/);
        expect(src).toMatch(/AlreadyUsedState/);
        expect(src).toMatch(/SetPasswordForm/);
      });
      it('imports all lucide-react icons used in JSX (anti-BUG-01)', () => {
        const usedIcons = (src.match(/<(ShieldCheck|Clock|CheckCircle2)\b/g) || []).map(s => s.slice(1));
        for (const icon of usedIcons) {
          expect(src).toMatch(new RegExp(`import\\s+\\{[^}]*\\b${icon}\\b[^}]*\\}\\s+from\\s+['"]lucide-react['"]`));
        }
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/laurentmarx/Documents/CRM Next gen/files" &&
      grep -q "export default async function InvitationPage" apps/web/src/app/invitation/[token]/page.tsx &&
      grep -q "force-dynamic" apps/web/src/app/invitation/[token]/page.tsx &&
      grep -q "ExpiredState\|AlreadyUsedState" apps/web/src/app/invitation/[token]/page.tsx &&
      grep -q "export function SetPasswordForm" apps/web/src/components/users/set-password-form.tsx &&
      grep -q "acceptInvitation" apps/web/src/components/users/set-password-form.tsx &&
      pnpm --filter @qualiof/web test --run src/app/invitation/\\[token\\]/__tests__/page.smoke.test.ts &&
      pnpm --filter @qualiof/web exec tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/web/src/app/invitation/\[token\]/page.tsx` retourne le fichier
    - `grep -c "notFound\\|expired\\|used" apps/web/src/app/invitation/[token]/page.tsx` ≥ 3 (3 branches gérées)
    - `grep -c "acceptInvitation" apps/web/src/components/users/set-password-form.tsx` ≥ 1 (form appelle bien la server action)
    - `grep -c "zodResolver(setPasswordSchema)" apps/web/src/components/users/set-password-form.tsx` ≥ 1
    - Smoke test : ≥ 5 passes
    - `tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    Page publique accessible sans auth, 3 états (expired / used / fresh), form fonctionnel, smoke test vert.
  </done>
</task>

</tasks>

<verification>
- Route `/invitation/{token}` répond 200 sans auth (testable manuellement avec un token de la BDD)
- `acceptInvitation` retourne erreur sur second appel avec le même token (single-use)
- Build Next.js OK (`pnpm --filter @qualiof/web build`)
- tsc clean
- ≥ 5 tests acceptInvitation verts + 5 tests smoke verts
</verification>

<success_criteria>
- RBAC-02 livré : invitation email → page publique → set password → login auto → /app
- Single-use enforcement testé
- Brand cohérente avec `/preinscription/[token]`
- AuditLog `users.password.set` écrit
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-utilisateurs-et-rbac/08-03-SUMMARY.md`
</output>