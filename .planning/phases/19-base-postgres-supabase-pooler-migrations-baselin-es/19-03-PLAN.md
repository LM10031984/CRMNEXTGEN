---
phase: 19-base-postgres-supabase-pooler-migrations-baselin-es
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md
requirements: [DB-01, DB-02]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "db-smoke-cloud.ts s'exécute vert contre la base cloud réelle et prouve les 4 critères"
    - "Le round-trip poolé (5 hits) ne lève AUCUN 'prepared statement already exists'"
    - "similarity() > 0 et unaccent('Éléonore')='Eleonore' au runtime cloud"
    - "Un INSERT+delete UUID passe sans collision de PK (0 autoincrement dans le schéma → séquences non concernées, documenté)"
    - "19-SMOKE.md consigne les 4 preuves datées, gaté Laurent"
  artifacts:
    - path: ".planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md"
      provides: "Journal des preuves cloud DB-01/DB-02 gaté Laurent (calqué sur 18-SMOKE.md)"
      contains: "prepared statement"
  key_links:
    - from: "db:smoke:cloud"
      to: "base Supabase cloud réelle"
      via: "DATABASE_URL poolée + DIRECT_URL session"
      pattern: "ALL 4 CRITERIA PROVEN"
---

<objective>
PROUVER les 4 critères de la Phase 19 contre le Supabase réel et consigner les preuves dans `19-SMOKE.md`
(calqué sur `18-SMOKE.md`). Étape ops GATÉE Laurent — c'est le phase gate. Rien ici n'est un test hermétique :
ce sont des smoke runtime contre la base cloud (la suite Vitest a Prisma mocké et ne peut pas les prouver).

Purpose: Boucler DB-01 (migrate deploy/status déjà verts en 19-02) + DB-02 (round-trip poolé sans prepared statement, extensions runtime, INSERT sans collision PK). Sans ces preuves runtime, la phase n'est pas « saine ET prouvée ».

Output: `19-SMOKE.md` rempli, 4 critères VALIDÉS et datés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-RESEARCH.md
@.planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md

<interfaces>
Outils livrés en amont, à exécuter ici :
- `apps/web/scripts/db-smoke-cloud.ts` (plan 19-01) — 4 fonctions : round-trip poolé (5 hits), transaction interactive Serializable, extensions (similarity/unaccent), INSERT test UUID nettoyé. Loggue « [db-smoke] ALL 4 CRITERIA PROVEN » en succès.
- npm script `db:smoke:cloud` (plan 19-01) : `dotenv -e .env -- pnpm --filter @qualiof/web exec tsx scripts/db-smoke-cloud.ts`.
- `.env` câblé cloud (plan 19-02) : DATABASE_URL :6543 poolée, DIRECT_URL :5432 session.

Structure `18-SMOKE.md` à imiter (en-tête « Validations prod manuelles », pré-requis cochés, sections par critère avec colonne Résultat + date, section « bugs révélés par le smoke » si applicable).
</interfaces>

Facts (audités) :
- **0 `autoincrement()`** dans schema.prisma → toutes PK UUID. Critère #4 (INSERT sans collision) est trivialement vrai : aucune séquence à réaligner. On le DOCUMENTE + on le PROUVE par l'INSERT test du smoke. PAS de script `setval` à générer (aucune séquence liée à une colonne autoincrement).
- Chemin worker `bumpAndFinalize` (worker.ts:334, Serializable) : le smoke reproduit ce pattern via `proveSerializableInteractiveTx`. Si erreur 40001/prepared statement observée UNIQUEMENT sur ce chemin → repli documenté : router le worker vers la session URL :5432 (PrismaClient dédiée worker) — à noter comme dette Phase 20, PAS à implémenter ici.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer 19-SMOKE.md (squelette des 4 preuves) + documenter l'absence de séquences (critère #4)</name>
  <files>.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md</files>
  <read_first>
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md (modèle de structure)
    - apps/web/scripts/db-smoke-cloud.ts (les 4 fonctions dont ce fichier consigne les preuves)
    - packages/db/prisma/schema.prisma (confirmer 0 autoincrement pour la note critère #4)
  </read_first>
  <action>
    Créer `.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md` calqué sur `18-SMOKE.md` :
    - En-tête : « 19-SMOKE — Validations cloud manuelles Base Postgres Supabase (DB-01 / DB-02) ». Rappel : preuves RUNTIME contre le Supabase réel (`gntlqyscahbgjrmsbzil`, West EU Irlande), NON reproductibles en Vitest hermétique (Prisma mocké).
    - Section « Pré-requis » : URLs cloud câblées (:6543 poolée, :5432 session), extensions installées (plan 19-02), backup fait.
    - **4 sections de critères**, chacune avec un tableau (Étape | Commande | Attendu | Résultat | Date) :
      1. **DB-01 — migrate status/deploy vert** : coller les sorties `migrate status` (« up to date ») + `migrate deploy` (« No pending migrations ») consignées par le plan 19-02.
      2. **DB-02 critère #2 — round-trip poolé sans prepared statement** : `pnpm db:smoke:cloud` → attendu « [round-trip] 5 reads OK » + « [serializable-tx] interactive tx OK » SANS « prepared statement already exists ».
      3. **DB-02 critère #3 — extensions runtime** : attendu « [extensions] pg_trgm similarity=… , unaccent OK » (similarity>0, unaccent='Eleonore').
      4. **DB-02 critère #4 — INSERT sans collision PK** : attendu « [insert-test] UUID PK INSERT+delete OK ». **+ Note documentée** : « Le schéma n'a AUCUN `@default(autoincrement())` (audit `grep -c 'autoincrement()' schema.prisma` = 0) → toutes les PK sont des UUID générées côté Node. Il n'existe donc AUCUNE séquence Postgres liée à une PK → la collision de séquence post-restore est structurellement IMPOSSIBLE. Aucun `setval` requis. L'INSERT test le confirme empiriquement. »
    - Section « Repli documenté » : si le round-trip Serializable échoue (40001/prepared statement sur le chemin worker) → router le worker vers DIRECT_URL :5432 via une PrismaClient dédiée (dette Phase 20).
    - Colonnes « Résultat » laissées vides (remplies à la Task 2 après exécution réelle).
  </action>
  <verify>
    <automated>test -f .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md && grep -q 'autoincrement' .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md && echo SMOKE_SKELETON_OK</automated>
  </verify>
  <acceptance_criteria>
    - `19-SMOKE.md` existe
    - `grep -c 'prepared statement' 19-SMOKE.md` >= 1 (critère #2 documenté)
    - `grep -c 'similarity\|unaccent' 19-SMOKE.md` >= 1 (critère #3)
    - `grep -c 'autoincrement' 19-SMOKE.md` >= 1 (note séquences critère #4)
    - Les 4 sections de critères sont présentes (DB-01 status/deploy + DB-02 #2/#3/#4)
  </acceptance_criteria>
  <done>`19-SMOKE.md` structuré, 4 critères en tableaux, absence de séquence documentée (0 autoincrement), colonnes Résultat prêtes à remplir.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Exécuter db:smoke:cloud contre le Supabase réel (4 critères)</name>
  <action>Étape humaine (checkpoint bloquant) : exécuter `pnpm db:smoke:cloud` + `prisma migrate status` contre le Supabase réel, puis coller les 4 sorties dans 19-SMOKE.md. Voir how-to-verify pour les commandes et les cas d'échec.</action>
  <what-built>
    `db-smoke-cloud.ts` (19-01) + URLs cloud câblées + base baselinée (19-02) + `19-SMOKE.md` (Task 1). Prêt à prouver les 4 critères contre le Supabase réel.
  </what-built>
  <how-to-verify>
    Laurent (ou l'orchestrateur si Laurent délègue « je te laisse gérer » comme en Phase 18), avec le `.env` câblé cloud :

    1. Lancer le smoke complet :
       ```
       pnpm db:smoke:cloud
       ```
       Attendu en sortie : `[round-trip] 5 reads OK` → `[serializable-tx] interactive tx OK` → `[extensions] pg_trgm similarity=… , unaccent OK` → `[insert-test] UUID PK INSERT+delete OK` → **`[db-smoke] ALL 4 CRITERIA PROVEN`**. Exit 0. AUCUN « prepared statement already exists ».

    2. Vérifier une dernière fois via DIRECT_URL :
       ```
       DIRECT_URL="<session-pooler-url>" pnpm --filter @qualiof/db exec prisma migrate status
       ```
       Attendu : « Database schema is up to date! ».

    3. Coller les sorties dans `19-SMOKE.md` (colonnes Résultat + date du jour), critère par critère.

    Si un critère ÉCHOUE : consigner l'erreur exacte dans `19-SMOKE.md`. Cas prévus :
    - `prepared statement "s0" already exists` → vérifier que DATABASE_URL a bien `?pgbouncer=true` (retour plan 19-02 Task 1).
    - `FATAL: Tenant or user not found` → mauvais préfixe `aws-<N>` (retour checkpoint 19-02, relire le hostname au dashboard).
    - `similarity does not exist` → extension pg_trgm non installée ou search_path (retour 19-02 Task 3).
    - Erreur 40001 UNIQUEMENT sur la tx Serializable → appliquer le repli documenté (worker → :5432) et le noter en dette Phase 20.
  </how-to-verify>
  <resume-signal>Coller les 4 sorties dans 19-SMOKE.md et taper « approuvé » (ou décrire l'échec pour retour au plan concerné).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Finaliser 19-SMOKE.md avec les résultats datés + bilan DB-01/DB-02</name>
  <files>.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md</files>
  <read_first>
    - .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md (squelette à remplir)
    - .planning/phases/18-supabase-storage-migration-objets-direct-to-storage/18-SMOKE.md (section « RÉSULTATS DE VALIDATION » à imiter)
  </read_first>
  <action>
    Remplir les colonnes « Résultat » + date de `19-SMOKE.md` avec les sorties réelles collées au checkpoint. Ajouter en tête une section « ✅ RÉSULTATS DE VALIDATION — <date> » (comme 18-SMOKE.md) qui statue :
    - **DB-01 = VALIDÉ** si `migrate status` clean + `migrate deploy` « No pending migrations » via :5432.
    - **DB-02 = VALIDÉ** si round-trip 5 hits sans prepared statement + serializable tx OK + similarity>0 + unaccent='Eleonore' + INSERT UUID OK.
    - Consigner tout bug réel révélé par le smoke (section « bugs révélés », comme les 3 bugs 18-SMOKE) + les commits de correction éventuels.
    - Consigner la dette éventuelle (repli worker :5432 si 40001 observé → Phase 20).

    Ce fichier est le livrable de preuve de la Phase 19 (équivalent 18-SMOKE.md). NE PAS marquer VALIDÉ un critère non prouvé.
  </action>
  <verify>
    <automated>grep -qi 'RÉSULTATS DE VALIDATION\|VALIDÉ' .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-SMOKE.md && echo SMOKE_FILLED</automated>
  </verify>
  <acceptance_criteria>
    - `19-SMOKE.md` contient une section « RÉSULTATS DE VALIDATION » datée
    - Chaque critère (DB-01 status+deploy, DB-02 #2/#3/#4) porte un Résultat + date (pas de colonne Résultat vide)
    - Le statut DB-01/DB-02 (VALIDÉ ou échec détaillé) est explicite
    - Toute dette (repli worker :5432) ou bug révélé est consigné
  </acceptance_criteria>
  <done>`19-SMOKE.md` finalisé : 4 critères datés avec résultats réels, statut DB-01/DB-02 explicite, dette/bugs consignés. Phase 19 prouvée.</done>
</task>

</tasks>

<verification>
- `pnpm db:smoke:cloud` exit 0 avec « ALL 4 CRITERIA PROVEN », aucun « prepared statement already exists ».
- `prisma migrate status` (DIRECT_URL) = « up to date ».
- `19-SMOKE.md` : 4 critères validés et datés, statut DB-01/DB-02 explicite.
</verification>

<success_criteria>
- DB-01 prouvé runtime : `migrate status`/`deploy` verts via :5432 consignés.
- DB-02 prouvé runtime : round-trip poolé sans prepared statement (critère #2), extensions trigram+unaccent résolvent (critère #3), INSERT UUID sans collision PK + absence de séquence documentée (critère #4).
- `19-SMOKE.md` = journal de preuve gaté Laurent (calqué 18-SMOKE.md), les 4 critères VALIDÉS.
</success_criteria>

<output>
After completion, create `.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-03-SUMMARY.md`
</output>
