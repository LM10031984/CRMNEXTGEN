---
phase: 19-base-postgres-supabase-pooler-migrations-baselin-es
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - .env
  - .env.example
  - packages/db/prisma/migrations/0_init/migration.sql
  - packages/db/prisma/migrations/archived-db-push/README.md
requirements: [DB-01, DB-02]
autonomous: false
user_setup:
  - service: supabase-postgres
    why: "Câbler les URLs cloud (mot de passe DB) + lire le hostname pooler exact (project-specific)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection pooling → Transaction (mode) :6543 — ajouter ?pgbouncer=true&connection_limit=1"
      - name: DIRECT_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection pooling → Session (mode) :5432"
    dashboard_config:
      - task: "Activer les 4 extensions si absentes (pgcrypto, uuid-ossp, pg_trgm, unaccent)"
        location: "Supabase Dashboard → Database → Extensions (toggle) OU SQL Editor: create extension if not exists <ext> with schema extensions;"

must_haves:
  truths:
    - "DATABASE_URL pointe le transaction pooler :6543 avec ?pgbouncer=true&connection_limit=1"
    - "DIRECT_URL pointe le session pooler :5432 (IPv4, prepared statements OK), PAS db.<ref>.supabase.co"
    - "prisma migrate status est clean sur la base cloud et _prisma_migrations est peuplé"
    - "migrate deploy a tourné vert via DIRECT_URL :5432 (pas un db push)"
    - "Les 4 extensions sont activées côté Supabase"
  artifacts:
    - path: "packages/db/prisma/migrations/0_init/migration.sql"
      provides: "Baseline collapse du schéma courant (migrate diff --from-empty)"
      contains: "CREATE TABLE"
  key_links:
    - from: ".env DATABASE_URL"
      to: "Supavisor transaction pooler"
      via: "?pgbouncer=true&connection_limit=1"
      pattern: "6543.*pgbouncer=true"
    - from: ".env DIRECT_URL"
      to: "Supavisor session pooler"
      via: "port 5432"
      pattern: "pooler.supabase.com:5432"
---

<objective>
Câbler les deux URLs Supabase et RENDRE LA BASE CLOUD SAINE : baseliner le drift `db push` historique
(29 migrations qui ne reflètent plus l'état réel restauré) puis prouver `migrate deploy` + `migrate status`
verts via `DIRECT_URL` :5432. Étape ops GATÉE Laurent (mot de passe DB + hostname pooler + backup pg_dump
avant toute manipulation) — convention projet « destructif = étape séparée ».

Purpose: C'est le cœur de DB-01 (baseline + migrate deploy vert) et de la moitié de DB-02 (2 URLs câblées + extensions actives). Sans base saine, le worker et l'app ne peuvent pas parler au cloud proprement.

Output: `.env` cloud câblé, `0_init` baseline généré + résolu appliqué, `migrate status` clean, extensions actives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-RESEARCH.md

<interfaces>
Schéma Prisma (déjà prêt — NE PAS modifier) :
```
datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")   // transaction pooler :6543
  directUrl  = env("DIRECT_URL")     // session pooler :5432
  extensions = [pgcrypto, uuid_ossp(map: "uuid-ossp"), pg_trgm, unaccent]
}
```

État `.env` racine ACTUEL (à remplacer par les URLs cloud) :
```
DATABASE_URL="postgresql://qualiof:***@localhost:5432/qualiof?schema=public"
DIRECT_URL="postgresql://qualiof:***@localhost:5432/qualiof?schema=public"
```

Format cible EXACT (RESEARCH — hostname aws-<N> à LIRE au dashboard, jamais deviné) :
```
DATABASE_URL="postgres://postgres.gntlqyscahbgjrmsbzil:<PASSWORD>@aws-<N>-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgres://postgres.gntlqyscahbgjrmsbzil:<PASSWORD>@aws-<N>-eu-west-1.pooler.supabase.com:5432/postgres"
```
Projet Supabase = `gntlqyscahbgjrmsbzil`, région West EU (Irlande) → région pooler = `eu-west-1`.
</interfaces>

Facts (audités, ne pas re-vérifier) :
- 29 migrations sous `packages/db/prisma/migrations/` (init → add_quotes) + `migration_lock.toml`. Le drift `db push` (ex. `TrainingProduct.derouleJson`) signifie qu'elles ne reflètent plus l'état réel → **collapse en une baseline `0_init`** (Option A recherche, recommandée) après confirmation par `migrate status`.
- `psql` **absent** du Mac → le SQL manuel (extensions, éventuel setval) passe par le **SQL Editor du dashboard Supabase**, jamais `psql`.
- `.env.local.cloud-backup` existe (peut contenir le password DB staging) ; sinon Laurent le reset au dashboard.
- Backups Supabase daily existent (managed) mais la convention projet exige un `pg_dump`/snapshot AVANT toute manipulation de baseline.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Récupérer password DB + hostname pooler + confirmer backup</name>
  <action>Étape humaine (checkpoint bloquant) : Laurent fournit le mot de passe DB Postgres, les 2 chaînes pooler exactes (transaction :6543 et session :5432, préfixe aws-<N> lu au dashboard) et confirme le backup avant baseline. Voir how-to-verify pour le détail. Aucune écriture Claude tant que ces valeurs ne sont pas fournies.</action>
  <what-built>Rien encore — cette étape RÉCUPÈRE les secrets et l'état que Claude ne peut pas obtenir seul, AVANT toute mutation cloud.</what-built>
  <how-to-verify>
    Laurent, avant que Claude câble les URLs et baseline la base, il faut 3 choses depuis le **dashboard Supabase** (projet `gntlqyscahbgjrmsbzil`, West EU Irlande) :

    1. **Mot de passe DB Postgres** : Dashboard → Project Settings → Database → soit récupérer le password existant, soit « Reset database password ». (Vérifier d'abord s'il est dans `.env.local.cloud-backup` — sinon reset.)

    2. **Hostname pooler EXACT** : Dashboard → Project Settings → Database → **Connection pooling**. Copier la chaîne « Transaction » (:6543) ET « Session » (:5432). Le préfixe `aws-<N>` (aws-0 vs aws-1…) est **project-specific** — NE PAS le deviner (sinon `FATAL: Tenant or user not found`). Coller les deux chaînes ici.

    3. **Snapshot/backup AVANT baseline** (convention « destructif = étape séparée ») : confirmer qu'un backup daily Supabase récent existe (Dashboard → Database → Backups) OU déclencher un dump. La baseline `resolve --applied` n'exécute AUCUN SQL destructif, mais la manipulation de `_prisma_migrations` justifie le filet.

    Fournir : le password (ou confirmation reset), les 2 chaînes pooler complètes, et le OK backup.
  </how-to-verify>
  <resume-signal>Coller les 2 URLs pooler (transaction :6543 et session :5432) avec le password, et confirmer « backup OK, go baseline ».</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Câbler DATABASE_URL (:6543 pooler) + DIRECT_URL (:5432 session) dans .env et .env.example</name>
  <files>.env, .env.example</files>
  <read_first>
    - .env (voir les URLs localhost actuelles à remplacer)
    - .env.example (lignes 10-14 DATABASE_URL/DIRECT_URL — mettre à jour le commentaire cloud)
    - .env.local.cloud-backup (si le password DB cloud y est déjà)
    - packages/db/prisma/schema.prisma (confirmer url=env DATABASE_URL, directUrl=env DIRECT_URL — NE PAS modifier)
  </read_first>
  <action>
    Avec les valeurs fournies au checkpoint, remplacer dans **`.env` racine** les deux lignes :
    - `DATABASE_URL="postgres://postgres.gntlqyscahbgjrmsbzil:<PASSWORD>@aws-<N>-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"` — le suffixe `?pgbouncer=true&connection_limit=1` est **OBLIGATOIRE** (supprime `prepared statement "s0" already exists` — pitfall 2/critère #2).
    - `DIRECT_URL="postgres://postgres.gntlqyscahbgjrmsbzil:<PASSWORD>@aws-<N>-eu-west-1.pooler.supabase.com:5432/postgres"` — port **5432 session pooler**, PAS `db.<ref>.supabase.co` (IPv6-only), PAS :6543 (transaction = migrations cassent). AUCUN `?pgbouncer` sur DIRECT_URL (les migrations ont besoin des prepared statements).

    Utiliser le hostname `aws-<N>` EXACT collé par Laurent (ne rien deviner).

    Dans **`.env.example`** (lignes ~10-14) : conserver les valeurs localhost de dev MAIS mettre à jour le commentaire pour documenter le format cloud Supabase exact (les deux ports + `?pgbouncer=true&connection_limit=1` sur la poolée uniquement). Ne PAS mettre de vrai password dans `.env.example`.

    Ne PAS toucher au schéma ni à `env.ts` (les clés sont déjà déclarées/requises depuis Phase 17). Ne PAS lancer `prisma generate` ici.
  </action>
  <verify>
    <automated>grep -q 'pooler.supabase.com:6543' .env && grep -q 'pgbouncer=true' .env && grep -q 'pooler.supabase.com:5432' .env && echo URLS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pooler.supabase.com:6543.*pgbouncer=true.*connection_limit=1' .env` = 1 (DATABASE_URL poolée bien formée)
    - `grep -c 'pooler.supabase.com:5432' .env` = 1 (DIRECT_URL session)
    - `grep -c 'db.gntlqyscahbgjrmsbzil.supabase.co' .env` = 0 (PAS le direct IPv6)
    - `grep -c 'pgbouncer' .env` = 1 (uniquement sur DATABASE_URL, pas sur DIRECT_URL — la ligne DIRECT_URL ne doit PAS contenir pgbouncer)
    - `grep -c 'gntlqyscahbgjrmsbzil' .env` = 2 (les 2 URLs)
    - `.env.example` documente le format pooler cloud dans un commentaire (`grep -c '6543\|pgbouncer' .env.example` >= 1)
  </acceptance_criteria>
  <done>Les 2 URLs cloud sont câblées dans `.env` avec la sémantique Supavisor correcte (poolée :6543 ?pgbouncer=true&connection_limit=1 ; directe :5432 session sans pgbouncer), hostname aws-<N> réel, et `.env.example` documente le format.</done>
</task>

<task type="auto">
  <name>Task 2: Baseliner le drift db push — générer 0_init, archiver les 29 migrations, resolve --applied, migrate deploy + status verts</name>
  <files>packages/db/prisma/migrations/0_init/migration.sql, packages/db/prisma/migrations/archived-db-push/README.md</files>
  <read_first>
    - packages/db/prisma/schema.prisma (source du migrate diff --from-empty)
    - packages/db/prisma/migrations/migration_lock.toml (provider postgresql)
    - .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-RESEARCH.md (section « Baselining », Option A collapse — Pitfall 4 : la base est PLEINE → resolve --applied, jamais migrate deploy destructif)
  </read_first>
  <action>
    **Étape 0 — Constater l'état réel AVANT toute décision** (Open Question #2 recherche) :
    ```
    DIRECT_URL="<session-pooler-url>" pnpm --filter @qualiof/db exec prisma migrate status
    ```
    Attendu sur une base restaurée par dump (tables présentes, `_prisma_migrations` vide/incohérent) : « Database schema is not in sync » ou migrations non appliquées. Consigner la sortie.

    **Décision collapse vs resolve-29** (RESEARCH Option A recommandée) : le drift `db push` (`TrainingProduct.derouleJson` poussé hors migration, mémoire projet) signifie que les 29 migrations historiques ne reflètent PLUS l'état réel de la base restaurée. → **Collapse** : générer UNE baseline `0_init` du schéma courant et archiver les 29. NE PAS faire Option B (resolve les 29 une à une) : elle suppose que l'état DB = somme des 29, FAUX à cause du db push.

    **Étape 1 — Générer la baseline collapse** (`--from-empty`, aucune connexion requise) :
    ```
    mkdir -p packages/db/prisma/migrations/0_init
    pnpm --filter @qualiof/db exec prisma migrate diff \
      --from-empty \
      --to-schema-datamodel prisma/schema.prisma \
      --script > packages/db/prisma/migrations/0_init/migration.sql
    ```
    Vérifier que `migration.sql` contient bien les `CREATE TABLE` de toutes les tables (non vide, > 100 lignes).

    **Étape 2 — Archiver les 29 migrations historiques** (elles ne reflètent plus l'état réel db push) : déplacer les 29 dossiers `2026*_*` vers `packages/db/prisma/migrations/archived-db-push/` et créer `packages/db/prisma/migrations/archived-db-push/README.md` expliquant : « 29 migrations historiques archivées le 2026-07-04 lors du collapse baseline Phase 19. La base cloud restaurée (dump 2026-07-03) avait divergé via `db push` (ex. TrainingProduct.derouleJson) → l'historique linéaire ne correspondait plus à l'état réel. Baseline unique = `0_init`. Ne PAS ré-injecter ces migrations. » CONSERVER `migration_lock.toml` à la racine `migrations/`. Après archivage, seul `0_init/` + `migration_lock.toml` restent dans `migrations/`.

    **Étape 3 — Marquer la baseline APPLIQUÉE sur le cloud SANS l'exécuter** (Pitfall 4 : base PLEINE) :
    ```
    DIRECT_URL="<session-pooler-url>" pnpm --filter @qualiof/db exec prisma migrate resolve --applied 0_init
    ```
    Ceci peuple `_prisma_migrations` avec le checksum correct SANS re-créer les tables existantes.

    **Étape 4 — Prouver migrate deploy vert** (critère #1 : « migrate deploy a vraiment tourné vert ») :
    ```
    DIRECT_URL="<session-pooler-url>" pnpm --filter @qualiof/db exec prisma migrate deploy
    ```
    Attendu : « No pending migrations to apply. » — c'est la preuve que `migrate deploy` s'exécute vert via :5432 sur l'état baseliné (pas un db push).

    **Étape 5 — Vérifier clean** :
    ```
    DIRECT_URL="<session-pooler-url>" pnpm --filter @qualiof/db exec prisma migrate status
    ```
    Attendu : « Database schema is up to date! » + `_prisma_migrations` contient `0_init` appliqué.

    Toutes les commandes cloud passent DIRECT_URL :5432 explicitement (jamais :6543 pour migrate). Consigner chaque sortie brute dans le SUMMARY (elles alimenteront le 19-SMOKE.md du plan 19-03).

    Si `migrate status` signale un drift lié aux **extensions** (preview `postgresqlExtensions`, Pitfall 5) : le documenter comme **bénin** (extensions gérées côté Supabase), ne PAS lancer `migrate dev` contre le cloud, ne PAS retirer le bloc `extensions` du schéma.
  </action>
  <verify>
    <automated>test -f packages/db/prisma/migrations/0_init/migration.sql && grep -qi 'CREATE TABLE' packages/db/prisma/migrations/0_init/migration.sql && echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/prisma/migrations/0_init/migration.sql` existe et contient `CREATE TABLE` (grep -ci 'CREATE TABLE' >= 30 tables environ)
    - `ls packages/db/prisma/migrations/` liste `0_init`, `migration_lock.toml`, `archived-db-push` — et AUCUN dossier `2026*` restant à la racine migrations (`ls packages/db/prisma/migrations/2026* 2>/dev/null | wc -l` = 0)
    - `packages/db/prisma/migrations/archived-db-push/README.md` existe et contient « collapse baseline Phase 19 »
    - `packages/db/prisma/migrations/archived-db-push/` contient les 29 dossiers déplacés (`ls -d packages/db/prisma/migrations/archived-db-push/2026*/ | wc -l` = 29)
    - Sortie `prisma migrate status` consignée dans le SUMMARY = « up to date » / `_prisma_migrations` peuplé
    - Sortie `prisma migrate deploy` consignée = « No pending migrations » (vert via :5432)
  </acceptance_criteria>
  <done>Baseline `0_init` collapse générée, 29 migrations archivées, `resolve --applied 0_init` a peuplé `_prisma_migrations`, `migrate deploy` vert via DIRECT_URL :5432, `migrate status` clean. Critère #1 satisfait.</done>
</task>

<task type="auto">
  <name>Task 3: Activer et vérifier les 4 extensions (pgcrypto, uuid-ossp, pg_trgm, unaccent) côté Supabase</name>
  <files>packages/db/prisma/schema.prisma (lecture seule — confirmation)</files>
  <read_first>
    - packages/db/prisma/schema.prisma (bloc extensions déclaré ligne ~22)
    - .planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-RESEARCH.md (section Extensions — schéma `extensions`, unaccent confirmé disponible)
  </read_first>
  <action>
    Les 4 extensions déclarées dans le schéma (`pgcrypto, uuid_ossp(map:"uuid-ossp"), pg_trgm, unaccent`) doivent être ACTIVES côté Supabase pour que le critère #3 passe au runtime.

    `psql` étant absent du Mac, activer via le **SQL Editor du dashboard Supabase** (projet `gntlqyscahbgjrmsbzil`). Exécuter (idempotent) :
    ```sql
    create extension if not exists pgcrypto  with schema extensions;
    create extension if not exists "uuid-ossp" with schema extensions;
    create extension if not exists pg_trgm    with schema extensions;
    create extension if not exists unaccent   with schema extensions;
    ```
    Puis vérifier l'installation (toujours SQL Editor) :
    ```sql
    select extname from pg_extension where extname in ('pgcrypto','uuid-ossp','pg_trgm','unaccent') order by extname;
    ```
    Attendu : les 4 lignes présentes.

    Alternative dashboard : Database → Extensions → toggle ON pour les 4 (même effet).

    Consigner la liste des 4 extensions confirmées dans le SUMMARY (elle sera reprise dans 19-SMOKE.md). NE PAS tenter de créer les extensions via `prisma migrate dev` contre le cloud (Pitfall 5).

    Note : la preuve RUNTIME que trigram + unaccent RÉSOLVENT (search_path, `similarity()`/`unaccent()`) est faite par le script `db-smoke-cloud.ts` exécuté au plan 19-03 — ici on garantit seulement qu'elles sont INSTALLÉES.
  </action>
  <verify>
    <automated>grep -q 'pg_trgm' packages/db/prisma/schema.prisma && grep -q 'unaccent' packages/db/prisma/schema.prisma && echo EXT_DECLARED_OK</automated>
  </verify>
  <acceptance_criteria>
    - Le SUMMARY consigne la sortie `select extname from pg_extension ...` listant les 4 : pgcrypto, uuid-ossp, pg_trgm, unaccent
    - Le bloc `extensions = [pgcrypto, uuid_ossp(map: "uuid-ossp"), pg_trgm, unaccent]` reste intact dans schema.prisma (non modifié)
    - Aucune commande `psql` employée (SQL Editor dashboard uniquement)
  </acceptance_criteria>
  <done>Les 4 extensions sont confirmées installées côté Supabase (liste `pg_extension` consignée). La résolution runtime sera prouvée en 19-03 par le smoke.</done>
</task>

</tasks>

<verification>
- `.env` : DATABASE_URL :6543 `?pgbouncer=true&connection_limit=1`, DIRECT_URL :5432 session pooler.
- `packages/db/prisma/migrations/` : `0_init` + `migration_lock.toml` + `archived-db-push/` (29 archivées).
- `prisma migrate status` (DIRECT_URL) = clean, `_prisma_migrations` peuplé de `0_init`.
- `prisma migrate deploy` (DIRECT_URL) = « No pending migrations » (vert via :5432).
- 4 extensions listées dans `pg_extension`.
- Sorties CLI consignées dans le SUMMARY pour alimenter 19-SMOKE.md.
</verification>

<success_criteria>
- DB-01 : baseline collapse `0_init`, drift `db push` résolu, `migrate deploy` vert via DIRECT_URL :5432, `migrate status` clean (critère #1).
- DB-02 (moitié) : 2 URLs câblées avec la sémantique Supavisor correcte + 4 extensions installées.
- Aucun SQL destructif joué (resolve --applied n'exécute rien) ; backup confirmé avant manipulation (convention projet).
</success_criteria>

<output>
After completion, create `.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-02-SUMMARY.md`
(inclure les sorties brutes de `migrate status`/`migrate deploy` + liste extensions pour 19-03).
</output>
