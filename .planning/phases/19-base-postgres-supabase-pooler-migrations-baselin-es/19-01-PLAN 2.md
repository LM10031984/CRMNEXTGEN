---
phase: 19-base-postgres-supabase-pooler-migrations-baselin-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/db-smoke-cloud.ts
  - package.json
requirements: [DB-01, DB-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Un script tsx unique prouve les 4 critères de la phase contre la base cloud (round-trip poolé, extensions trigram/unaccent, INSERT sans collision PK)"
    - "Le round-trip lit/écrit 5 fois de suite pour révéler tout 'prepared statement already exists' du pooler transaction mode"
    - "Le script passe par bumpAndFinalize (Serializable, worker) OU une transaction interactive équivalente pour prouver le chemin worker sous pooler"
    - "Le script ne loggue jamais de PII (IDs et compteurs seulement)"
  artifacts:
    - path: "apps/web/scripts/db-smoke-cloud.ts"
      provides: "Smoke cloud DB-01/DB-02 : round-trip poolé + extensions + INSERT test"
      min_lines: 80
      contains: "similarity"
  key_links:
    - from: "apps/web/scripts/db-smoke-cloud.ts"
      to: "@qualiof/db prisma"
      via: "import { prisma }"
      pattern: "from '@qualiof/db'"
    - from: "apps/web/scripts/db-smoke-cloud.ts"
      to: "pg_trgm / unaccent"
      via: "$queryRawUnsafe"
      pattern: "similarity|unaccent"
---

<objective>
Construire le SCRIPT DE PREUVE cloud (DB-01/DB-02) — 100 % autonome, aucune base cloud requise pour l'écrire.
C'est le contrat/outil que le plan 19-03 (smoke gaté Laurent) exécutera contre le Supabase réel.

Purpose: Les critères de succès de la Phase 19 sont RUNTIME contre le cloud (pas des tests hermétiques Vitest — Prisma est mocké dans la suite). Il faut un runner tsx qui, une fois pointé sur `DATABASE_URL`/`DIRECT_URL` cloud, prouve mécaniquement les 4 critères. Ce plan LIVRE le script ; l'exécution réelle est gatée Laurent en 19-03 (destructif = étape séparée, cloud réel requis).

Output: `apps/web/scripts/db-smoke-cloud.ts` + npm script `db:smoke:cloud`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-RESEARCH.md

<interfaces>
<!-- Contrats extraits du codebase — l'exécuteur les utilise DIRECTEMENT, pas d'exploration. -->

From packages/db/src/index.ts (le client partagé — worker/CLI-safe, pas d'auth React) :
```typescript
export const prisma: PrismaClient;   // singleton, log ['error'] en non-dev
export * from '@prisma/client';      // types re-exportés
```
Import : `import { prisma } from '@qualiof/db';`

Modèles Prisma disponibles (schema.prisma, ~40 modèles) pour le round-trip :
- `prisma.tenant.findFirstOrThrow()` → un Tenant existe toujours (multi-tenant)
- `prisma.trainingSession.count({ where: { tenantId } })` → lecture répétable
- `prisma.auditLog` → table d'écriture jetable idéale pour l'INSERT test (id String @default(uuid()), tenantId, action, etc. — vérifier les champs requis dans schema.prisma AVANT d'écrire l'INSERT)

Chemin worker Serializable à prouver (critère #2, pitfall 1 recherche) :
apps/web/src/lib/closure/worker.ts:334 `bumpAndFinalize(batchId, status)` — `prisma.$transaction(async (tx) => {...}, { isolationLevel: 'Serializable' })`.
Ce plan ne l'IMPORTE PAS (il touche des ClosureBatch réels) ; il REPRODUIT le pattern : une `$transaction(async (tx) => { await tx.tenant.count(); }, { isolationLevel: 'Serializable' })` pour prouver qu'une transaction interactive Serializable passe sous le pooler transaction mode.

Pattern script existant à imiter (garde d'entrée robuste aux espaces dans le chemin — leçon 18-SMOKE bug #1) :
@apps/web/scripts/calendar-backfill.ts   (SÉQUENTIEL, DRY par défaut, main() gardée par pathToFileURL, finally prisma.$disconnect())
</interfaces>

Facts audités (ne pas re-vérifier) :
- `grep -c 'autoincrement()' packages/db/prisma/schema.prisma` = **0** → toutes les PK sont `@id @default(uuid())`. Le critère #4 (INSERT sans collision PK) est donc TRIVIALEMENT vrai : les UUID ne dépendent d'aucune séquence. Le script doit néanmoins PROUVER par un INSERT réel + le documenter.
- `psql` n'est **PAS installé** sur le Mac → toute vérif SQL passe par `$queryRaw`/`$queryRawUnsafe` (ce script) ou le SQL Editor du dashboard Supabase (plan 19-02). Ne pas écrire de commande `psql`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire db-smoke-cloud.ts (round-trip poolé + extensions + INSERT test UUID)</name>
  <files>apps/web/scripts/db-smoke-cloud.ts, package.json</files>
  <read_first>
    - apps/web/scripts/calendar-backfill.ts (pattern DRY, garde pathToFileURL, $disconnect en finally)
    - apps/web/scripts/migrate-storage.ts (autre script récent, garde d'entrée corrigée espaces %20)
    - packages/db/src/index.ts (le client `prisma` exporté)
    - packages/db/prisma/schema.prisma (champs requis de AuditLog OU d'un modèle jetable pour l'INSERT test ; confirmer id/tenantId/champs NOT NULL)
  </read_first>
  <action>
    Créer `apps/web/scripts/db-smoke-cloud.ts` (runner tsx, worker/CLI-safe : importe UNIQUEMENT `@qualiof/db`, aucun import React/auth). Structure EXACTE :

    1. `import { prisma } from '@qualiof/db';` puis `import { pathToFileURL } from 'node:url';`.

    2. Fonction `async function proveRoundTripPooled(): Promise<void>` (critère #2) :
       - `const t = await prisma.tenant.findFirstOrThrow();`
       - Boucle `for (let i = 0; i < 5; i++) { await prisma.trainingSession.count({ where: { tenantId: t.id } }); }` — 5 hits successifs révèlent tout `prepared statement "s0" already exists` du pooler recyclé (pitfall 2). Logger `console.log('[round-trip] 5 reads OK, tenant=%s', t.id)` (ID seulement, jamais de nom = RGPD).

    3. Fonction `async function proveSerializableInteractiveTx(): Promise<void>` (pitfall 1, chemin worker `bumpAndFinalize`) :
       - `await prisma.$transaction(async (tx) => { await tx.tenant.count(); await tx.trainingSession.count(); }, { isolationLevel: 'Serializable' });`
       - Logger `console.log('[serializable-tx] interactive tx OK under pooler');`. Prouve qu'une transaction interactive Serializable (le pattern exact de worker.ts:334) passe sous le pooler transaction mode :6543.

    4. Fonction `async function proveExtensions(): Promise<void>` (critère #3) :
       - `const trg = await prisma.$queryRawUnsafe<{ similarity: number }[]>("SELECT similarity('Dupont','Dupond') AS similarity");`
       - `const ua = await prisma.$queryRawUnsafe<{ unaccent: string }[]>("SELECT unaccent('Éléonore') AS unaccent");`
       - `if (!(trg[0].similarity > 0)) throw new Error('pg_trgm KO: similarity <= 0');`
       - `if (ua[0].unaccent !== 'Eleonore') throw new Error("unaccent KO: got " + ua[0].unaccent);`
       - Logger `console.log('[extensions] pg_trgm similarity=%s, unaccent OK', trg[0].similarity);`. Prouve pg_trgm + unaccent résolvent au runtime (search_path inclut le schéma `extensions`).

    5. Fonction `async function proveInsertNoPkCollision(): Promise<void>` (critère #4) :
       - Commentaire d'en-tête : « Toutes les PK sont @default(uuid()) (0 autoincrement() dans schema.prisma) → collision de séquence IMPOSSIBLE. On le prouve par un INSERT réel + rollback. »
       - `const t = await prisma.tenant.findFirstOrThrow();`
       - INSERER une ligne jetable dans un modèle à PK UUID (choisir AuditLog ou équivalent selon les champs NOT NULL confirmés au read_first) : `const created = await prisma.auditLog.create({ data: { tenantId: t.id, action: 'DB_SMOKE_TEST', /* remplir les champs NOT NULL réels */ } });`
       - `if (!created.id) throw new Error('INSERT KO: no id');`
       - NETTOYER immédiatement : `await prisma.auditLog.delete({ where: { id: created.id } });` (ne pas polluer la base cloud).
       - Logger `console.log('[insert-test] UUID PK INSERT+delete OK, id=%s (no sequence, no collision)', created.id);`.

    6. `async function main()` : appelle les 4 fonctions EN SÉQUENCE dans un `try/catch/finally`. `catch` → `console.error('[db-smoke] FAILED:', e); process.exitCode = 1;`. `finally` → `await prisma.$disconnect();`. En succès, logger `console.log('[db-smoke] ALL 4 CRITERIA PROVEN');`.

    7. Garde d'entrée robuste aux espaces (le chemin du repo contient « CRM Next gen » avec espaces → %20, leçon 18-SMOKE bug #1) : `if (import.meta.url === pathToFileURL(process.argv[1]).href) { void main(); }`. NE PAS utiliser `import.meta.url === 'file://' + process.argv[1]` (casse sur espaces).

    8. Ajouter dans `package.json` racine (section scripts) : `"db:smoke:cloud": "dotenv -e .env -- pnpm --filter @qualiof/web exec tsx scripts/db-smoke-cloud.ts"`. (dotenv charge le `.env` racine où seront les URLs cloud du plan 19-02 ; le script lit `DATABASE_URL` poolée par défaut via le client `prisma`.)

    Le script NE crée PAS de nouvelle clé env et NE modifie PAS `.env`. Il consomme `DATABASE_URL`/`DIRECT_URL` déjà validées au boot (Phase 17).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/scripts/db-smoke-cloud.ts` existe
    - `grep -c "from '@qualiof/db'" apps/web/scripts/db-smoke-cloud.ts` = 1
    - `grep -c "for (let i = 0; i < 5" apps/web/scripts/db-smoke-cloud.ts` >= 1 (round-trip 5 hits)
    - `grep -c "isolationLevel: 'Serializable'" apps/web/scripts/db-smoke-cloud.ts` = 1 (chemin worker)
    - `grep -c "similarity(" apps/web/scripts/db-smoke-cloud.ts` >= 1 ET `grep -c "unaccent(" apps/web/scripts/db-smoke-cloud.ts` >= 1
    - `grep -c "\\$disconnect" apps/web/scripts/db-smoke-cloud.ts` >= 1
    - `grep -c "pathToFileURL" apps/web/scripts/db-smoke-cloud.ts` >= 1 (garde robuste espaces)
    - `grep -c "DB_SMOKE_TEST\|\\.delete(" apps/web/scripts/db-smoke-cloud.ts` >= 1 (INSERT test nettoyé)
    - `grep -c '"db:smoke:cloud"' package.json` = 1
    - `grep -c "\\.name\|firstName\|lastName\|ribKey\|cni" apps/web/scripts/db-smoke-cloud.ts` = 0 côté console.log (aucun PII loggé — seulement des IDs)
    - `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Le script compile (tsc exit 0), couvre les 4 critères par 4 fonctions distinctes, se garde correctement en CLI, ne loggue aucun PII, et le npm script `db:smoke:cloud` existe. NON exécuté contre le cloud ici (gaté 19-03).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web exec tsc --noEmit` exit 0 (aucune régression type)
- Le script est purement additif : aucun fichier de production métier touché, aucun `.env` modifié.
- Suite Vitest complète inchangée (baseline 1163/1164, l'échec `shared-template.test.ts` MIME jpeg/jpg est PRÉ-EXISTANT hors scope) : `pnpm --filter @qualiof/web exec vitest run` puis `pnpm --filter @qualiof/shared exec vitest run`.
</verification>

<success_criteria>
- `apps/web/scripts/db-smoke-cloud.ts` existe, compile, couvre round-trip poolé (5 hits) + transaction interactive Serializable + extensions (trigram+unaccent) + INSERT test UUID nettoyé.
- npm script `db:smoke:cloud` câblé (dotenv .env → tsx).
- 0 PII loggé (IDs et compteurs seulement).
- Script NON exécuté contre le cloud (l'exécution est l'étape gatée Laurent du plan 19-03).
</success_criteria>

<output>
After completion, create `.planning/phases/19-base-postgres-supabase-pooler-migrations-baselin-es/19-01-SUMMARY.md`
</output>
