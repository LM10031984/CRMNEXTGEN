---
phase: 07-param-tres-organisme-ditables
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - apps/web/src/lib/closure/shared-template.ts
  - apps/web/src/lib/closure/__tests__/shared-template.test.ts
  - apps/web/src/lib/programme-template.ts
  - apps/web/src/lib/convention-template.ts
  - apps/web/src/server/actions/tenant-assets.ts
  - apps/web/src/server/actions/__tests__/tenant-assets.test.ts
autonomous: true
requirements: [SET-02]

must_haves:
  truths:
    - "loadAssetDataUrl(filenames, tenantId?) cherche d'abord public/of-assets/{tenantId}/, fallback src/assets/"
    - "invalidateAssetCache(tenantId) purge le fileCache module-scope pour le tenant donné"
    - "loadLogoColorDataUrl(tenantId?) et loadSignatureDataUrl(tenantId?, role) acceptent tenantId optionnel"
    - "programme-template.ts utilise loadLogoColorDataUrl(tenantId) (plus de logoCache local)"
    - "convention-template.ts utilise loadLogoColorDataUrl(tenantId) (plus de logoCache local)"
    - "uploadTenantLogo écrit dans public/of-assets/{tenantId}/logo.<ext>, met à jour Tenant.logoPath, log AuditLog action='parameters.upload.logo'"
    - "uploadTenantSignature(role) écrit dans signature-pedago.png OU signature-dirigeant.png selon role"
    - "resetTenantLogo / resetTenantSignature suppriment le fichier disque + reset Tenant.<path>=null + log AuditLog action='parameters.reset.logo'"
    - "Validation taille (5Mo logo, 2Mo signature) + MIME (PNG/JPG/SVG logo, PNG/JPG signature) appliquée"
  artifacts:
    - path: "apps/web/src/lib/closure/shared-template.ts"
      provides: "loadAssetDataUrl étendu avec tenantId + invalidateAssetCache export"
      exports: ["loadAssetDataUrl", "invalidateAssetCache", "loadLogoColorDataUrl", "loadSignatureDataUrl"]
    - path: "apps/web/src/server/actions/tenant-assets.ts"
      provides: "uploadTenantLogo, uploadTenantSignature, resetTenantLogo, resetTenantSignature"
      exports: ["uploadTenantLogo", "uploadTenantSignature", "resetTenantLogo", "resetTenantSignature"]
      min_lines: 100
  key_links:
    - from: "apps/web/src/server/actions/tenant-assets.ts"
      to: "apps/web/src/lib/closure/shared-template.ts"
      via: "invalidateAssetCache(tenantId)"
      pattern: "invalidateAssetCache"
    - from: "apps/web/src/server/actions/tenant-assets.ts"
      to: "apps/web/src/server/actions/tenant-settings.ts"
      via: "logTenantSettingsChange"
      pattern: "logTenantSettingsChange"
    - from: "apps/web/src/lib/programme-template.ts"
      to: "apps/web/src/lib/closure/shared-template.ts"
      via: "loadLogoColorDataUrl(tenantId)"
      pattern: "loadLogoColorDataUrl"
    - from: "apps/web/src/lib/convention-template.ts"
      to: "apps/web/src/lib/closure/shared-template.ts"
      via: "loadLogoColorDataUrl(tenantId)"
      pattern: "loadLogoColorDataUrl"
---

<objective>
Permettre l'upload UI du logo + 2 signatures (pédago + dirigeant) avec impact effectif sur les templates PDF (closure + programme + convention). Étendre `loadAssetDataUrl` pour chercher d'abord dans `public/of-assets/{tenantId}/`, fallback bundled `src/assets/`. Éliminer les `logoCache` locaux dans `programme-template.ts` et `convention-template.ts` qui rendent l'upload UI inopérant sur ces templates.

Purpose: D-04 (logo upload UI) + D-05 (signatures upload UI). Sans la cascade `programme-template` + `convention-template`, l'upload UI ne sert que les docs closure — Laurent ne verrait pas son nouveau logo sur programme/convention (pitfall majeur Finding 3 RESEARCH.md).

Output:
- `shared-template.ts` : `loadAssetDataUrl(filenames, tenantId?)` cascade FS + `invalidateAssetCache(tenantId)` export
- `programme-template.ts` + `convention-template.ts` : remplacent logoCache local par appel central
- `tenant-assets.ts` : 4 server actions (uploadTenantLogo, uploadTenantSignature, resetTenantLogo, resetTenantSignature)
- Tests Vitest sur loadAssetDataUrl (fixtures fs) + upload validation (taille/MIME)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md
@.planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md
@.planning/phases/07-param-tres-organisme-ditables/07-02-SUMMARY.md
@apps/web/src/lib/closure/shared-template.ts
@apps/web/src/server/actions/upload-apprenant-docs.ts

<interfaces>
<!-- From Plan 07-02 — reused -->
```ts
export async function logTenantSettingsChange(opts: {
  tenantId: string;
  userId: string;
  action: string; // 'parameters.upload.logo' | 'parameters.upload.signature.pedago' | 'parameters.upload.signature.dirigeant' | 'parameters.reset.logo' | 'parameters.reset.signature.pedago' | 'parameters.reset.signature.dirigeant'
  diff: Record<string, { before: unknown; after: unknown }>;
}): Promise<void>;
```

<!-- Contracts Plan 07-04 (UI) will consume -->
```ts
type AssetResult = { ok: true; path: string } | { ok: false; error: string };

export async function uploadTenantLogo(formData: FormData): Promise<AssetResult>;
export async function uploadTenantSignature(role: 'pedago' | 'dirigeant', formData: FormData): Promise<AssetResult>;
export async function resetTenantLogo(): Promise<{ ok: boolean; error?: string }>;
export async function resetTenantSignature(role: 'pedago' | 'dirigeant'): Promise<{ ok: boolean; error?: string }>;
```

<!-- Constraints -->
- MAX_LOGO_MB = 5
- MAX_SIGNATURE_MB = 2
- ALLOWED_LOGO_MIME = {'image/png', 'image/jpeg', 'image/svg+xml'}
- ALLOWED_SIGNATURE_MIME = {'image/png', 'image/jpeg'}
- Path filesystem : `path.join(process.cwd(), 'public', 'of-assets', tenantId, filename)`
- Path public URL (stored in Tenant.{logoPath|signaturePedagoPath|signatureDirigeantPath}) : `/of-assets/{tenantId}/{filename}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre loadAssetDataUrl avec tenantId + invalidateAssetCache + cascade programme/convention</name>
  <files>apps/web/src/lib/closure/shared-template.ts, apps/web/src/lib/closure/__tests__/shared-template.test.ts, apps/web/src/lib/programme-template.ts, apps/web/src/lib/convention-template.ts</files>
  <read_first>
    - apps/web/src/lib/closure/shared-template.ts (intégralement L1-100 + L420-520 — fonctions loadAssetDataUrl, loadLogoColorDataUrl, loadLogoWhiteDataUrl, loadSignatureDataUrl, fileCache Map)
    - apps/web/src/lib/programme-template.ts L60-80 (logoCache local L63-73) ET L320-340 (call site getOfConfig)
    - apps/web/src/lib/convention-template.ts L55-80 (logoCache local L59-69) ET L180-200 (call site getOfConfig)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 3 — extension exacte, Pitfall 1 — cache invalidation)
    - .planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md (D-04 logo, D-05 signatures — chemins exacts)
  </read_first>
  <behavior>
    - Test 1: `loadAssetDataUrl(['logo.png'], 'tenant1')` retourne le contenu de `public/of-assets/tenant1/logo.png` si présent (fixture créée)
    - Test 2: `loadAssetDataUrl(['logo.png'], 'tenant1')` fallback sur `src/assets/logo-start-academy.png` si fichier tenant absent (fixture absente)
    - Test 3: `loadAssetDataUrl(['logo.png'], undefined)` n'essaie PAS le path tenant, va directement aux fallbacks bundled
    - Test 4: cache key inclut tenantId — deux appels avec tenantId différents ne se polluent pas
    - Test 5: `invalidateAssetCache('tenant1')` supprime du fileCache toutes les entrées qui commencent par 'tenant1::'
    - Test 6: `loadLogoColorDataUrl('tenant1')` cherche dans l'ordre : logo.png, logo.jpg, logo.svg dans tenant dir, puis logo-start-academy.png fallback
    - Test 7: `loadSignatureDataUrl('tenant1', 'pedago')` cherche signature-pedago.png tenant dir, fallback signature-laurent.png puis tampon-signature-fusion.png
    - Test 8: `loadSignatureDataUrl('tenant1', 'dirigeant')` cherche signature-dirigeant.png tenant dir, fallback tampon-signature-fusion.png puis tampon-signature.png
  </behavior>
  <action>
    1. **Refactor `apps/web/src/lib/closure/shared-template.ts`** :

       a. **Modifier la signature de `loadAssetDataUrl`** (fonction interne) selon Finding 3 RESEARCH.md L200-244 :
          - Ajouter `tenantId?: string` second paramètre
          - Cache key = `${tenantId ?? '_default'}::${filenames.join('|')}`
          - **Priority 1** (si tenantId fourni) : cherche dans `path.join(process.cwd(), 'public', 'of-assets', tenantId, filename)` pour chaque filename
          - **Priority 2** (fallback existing) : cherche dans `path.join(process.cwd(), 'src', 'assets', filename)` pour chaque filename
          - Si aucun fichier trouvé : retourne `''` (comportement existant)

       b. **Exporter `invalidateAssetCache(tenantId: string): void`** :
          ```ts
          export function invalidateAssetCache(tenantId: string): void {
            const prefix = `${tenantId}::`;
            for (const key of Array.from(fileCache.keys())) {
              if (key.startsWith(prefix)) fileCache.delete(key);
            }
          }
          ```

       c. **Modifier les wrappers** pour propager `tenantId?` :
          ```ts
          export function loadLogoColorDataUrl(tenantId?: string): string {
            return loadAssetDataUrl(['logo.png', 'logo.jpg', 'logo.svg', 'logo-start-academy.png'], tenantId);
          }
          export function loadLogoWhiteDataUrl(tenantId?: string): string {
            return loadAssetDataUrl(['logo-white.png', 'logo-start-academy-white.png'], tenantId);
          }
          export function loadSignatureDataUrl(tenantId?: string, role: 'pedago' | 'dirigeant' = 'pedago'): string {
            const filename = role === 'pedago' ? 'signature-pedago.png' : 'signature-dirigeant.png';
            const fallbacks = role === 'pedago'
              ? [filename, 'signature-laurent.png', 'tampon-signature-fusion.png']
              : [filename, 'tampon-signature-fusion.png', 'tampon-signature.png'];
            return loadAssetDataUrl(fallbacks, tenantId);
          }
          ```

       d. **NOTER** : les call sites internes de shared-template.ts (L425, L511) qui appellent `loadLogoColorDataUrl()` ou `loadSignatureDataUrl()` doivent maintenant recevoir `tenantId` depuis le caller (Plan 07-01 a probablement déjà fait passer `of` aux templates closure → ces helpers doivent recevoir tenantId via data ou closure-pack-renderer doit le propager). **Si Plan 07-01 a déjà propagé `of` aux templates, ajouter `data.tenantId` au type data et le propager aux loadLogo/loadSignature.**

    2. **Refactor `apps/web/src/lib/programme-template.ts`** :
       - **Supprimer** le `logoCache` local L63-73 (et tout `fs.readFileSync` direct sur logo-start-academy.png).
       - **Importer** `import { loadLogoColorDataUrl } from './closure/shared-template';`
       - Remplacer l'appel local par `loadLogoColorDataUrl(data.tenantId)` (signature data déjà étendue avec tenantId si Plan 07-01 a fait son boulot ; sinon ajouter `tenantId?: string` au type ProgrammeData).

    3. **Refactor `apps/web/src/lib/convention-template.ts`** :
       - Idem : supprimer `logoCache` L59-69, importer `loadLogoColorDataUrl`, appeler avec `data.tenantId`.

    4. **Créer `apps/web/src/lib/closure/__tests__/shared-template.test.ts`** :
       - Pattern : `beforeEach` crée des fixtures fs dans `apps/web/public/of-assets/test-tenant/logo.png` (buffer PNG minimal valide), `afterEach` cleanup.
       - 8 tests selon `behavior` ci-dessus.
       - Test 5 (cache invalidation) : appelle loadAssetDataUrl → vérifie cache HIT → `invalidateAssetCache('tenant1')` → vérifie cache MISS.
       - Note: peut utiliser `vi.spyOn(fs, 'readFileSync')` pour observer combien de fois le disque est lu et confirmer le caching.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- src/lib/closure/__tests__/shared-template.test.ts --run && grep -c "loadLogoColorDataUrl" src/lib/programme-template.ts src/lib/convention-template.ts | grep -v ":0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "function loadAssetDataUrl\\(.*tenantId" apps/web/src/lib/closure/shared-template.ts` retourne 1 ligne
    - `grep -E "export function invalidateAssetCache" apps/web/src/lib/closure/shared-template.ts` retourne 1 ligne
    - `grep -E "export function loadLogoColorDataUrl\\(tenantId" apps/web/src/lib/closure/shared-template.ts` retourne 1 ligne
    - `grep -E "export function loadSignatureDataUrl\\(tenantId" apps/web/src/lib/closure/shared-template.ts` retourne 1 ligne
    - `grep -c "logoCache" apps/web/src/lib/programme-template.ts` = 0 (cache local supprimé)
    - `grep -c "logoCache" apps/web/src/lib/convention-template.ts` = 0
    - `grep -c "loadLogoColorDataUrl" apps/web/src/lib/programme-template.ts` ≥ 1
    - `grep -c "loadLogoColorDataUrl" apps/web/src/lib/convention-template.ts` ≥ 1
    - `pnpm --filter @qualiof/web test -- src/lib/closure/__tests__/shared-template.test.ts --run` : 8 tests verts
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    `loadAssetDataUrl` étendu avec param tenantId optionnel (cascade FS tenant → bundled). `invalidateAssetCache(tenantId)` exporté pour invalidation post-upload. `loadLogoColorDataUrl` + `loadSignatureDataUrl` propagent tenantId. `programme-template.ts` + `convention-template.ts` ont leur `logoCache` local supprimé et utilisent le helper central. 8 tests Vitest verts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server actions tenant-assets (upload + reset logo/signatures) avec AuditLog</name>
  <files>apps/web/src/server/actions/tenant-assets.ts, apps/web/src/server/actions/__tests__/tenant-assets.test.ts</files>
  <read_first>
    - apps/web/src/server/actions/upload-apprenant-docs.ts (pattern FormData + arrayBuffer + validation MIME/taille)
    - apps/web/src/server/actions/tenant-settings.ts (post-Plan 07-02 — réutiliser logTenantSettingsChange + computeDiff)
    - apps/web/src/lib/closure/shared-template.ts (post-Task 1 — invalidateAssetCache exporté)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 4 — pattern Server Action upload + Pitfall 1 — cache invalidation, Pitfall 6 — SVG sanitization, Pitfall 7 — process.cwd public)
    - .planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md (D-04 logo, D-05 signatures — chemins + types acceptés)
    - apps/web/src/lib/auth.ts (validateRequest)
  </read_first>
  <behavior>
    - Test 1: `uploadTenantLogo(formDataWithPng)` : valide File, écrit dans path tenant, met à jour Tenant.logoPath, appelle logTenantSettingsChange action='parameters.upload.logo', appelle invalidateAssetCache, retourne `{ ok: true, path }`
    - Test 2: `uploadTenantLogo(formDataWith6MoFile)` : retourne `{ ok: false, error: 'Logo > 5 Mo' }` sans toucher disque
    - Test 3: `uploadTenantLogo(formDataWithPdfMime)` : retourne `{ ok: false, error: 'Format autorisé : PNG, JPG, SVG' }`
    - Test 4: `uploadTenantSignature('pedago', formDataPng)` : écrit `signature-pedago.png`, met à jour Tenant.signaturePedagoPath, action='parameters.upload.signature.pedago'
    - Test 5: `uploadTenantSignature('dirigeant', formDataPng)` : écrit `signature-dirigeant.png`, met à jour Tenant.signatureDirigeantPath
    - Test 6: `uploadTenantSignature('pedago', formDataSvg)` : retourne erreur (SVG non-autorisé pour signature, seul PNG/JPG)
    - Test 7: `resetTenantLogo()` : supprime le fichier disque, set Tenant.logoPath=null, action='parameters.reset.logo'
    - Test 8: `resetTenantSignature('pedago')` : supprime signature-pedago.png, set Tenant.signaturePedagoPath=null
    - Test 9: Toutes les actions vérifient `validateRequest()` et returnent `{ ok: false, error }` si pas user
    - Test 10: Toutes les actions appellent `invalidateAssetCache(user.tenantId)` après modif
  </behavior>
  <action>
    Créer `apps/web/src/server/actions/tenant-assets.ts` (NEW) avec ce contenu structurel :

    ```ts
    'use server';

    import { promises as fs } from 'node:fs';
    import path from 'node:path';
    import { revalidatePath } from 'next/cache';
    import { prisma } from '@qualiof/db';
    import { validateRequest } from '@/lib/auth';
    import { invalidateAssetCache } from '@/lib/closure/shared-template';
    import { logTenantSettingsChange } from './tenant-settings';

    const MAX_LOGO_MB = 5;
    const MAX_SIGNATURE_MB = 2;
    const ALLOWED_LOGO_MIME = new Set(['image/png', 'image/jpeg', 'image/svg+xml']);
    const ALLOWED_SIGNATURE_MIME = new Set(['image/png', 'image/jpeg']);

    type AssetResult = { ok: true; path: string } | { ok: false; error: string };
    type SimpleResult = { ok: true } | { ok: false; error: string };

    function extFromMime(mime: string): string {
      if (mime === 'image/svg+xml') return 'svg';
      if (mime === 'image/jpeg') return 'jpg';
      return 'png';
    }

    function tenantAssetDir(tenantId: string): string {
      return path.join(process.cwd(), 'public', 'of-assets', tenantId);
    }

    async function unlinkIfExists(filePath: string): Promise<void> {
      try { await fs.unlink(filePath); } catch { /* OK si absent */ }
    }

    // ---------- LOGO ----------
    export async function uploadTenantLogo(formData: FormData): Promise<AssetResult> {
      const { user } = await validateRequest();
      if (!user) return { ok: false, error: 'Non authentifié' };

      const f = formData.get('file');
      if (!(f instanceof File) || f.size === 0) return { ok: false, error: 'Aucun fichier' };
      if (f.size > MAX_LOGO_MB * 1024 * 1024) return { ok: false, error: `Logo > ${MAX_LOGO_MB} Mo` };
      if (!ALLOWED_LOGO_MIME.has(f.type)) return { ok: false, error: 'Format autorisé : PNG, JPG, SVG' };

      const ext = extFromMime(f.type);
      const dir = tenantAssetDir(user.tenantId);
      await fs.mkdir(dir, { recursive: true });

      // Supprimer les variantes d'extension précédentes (logo.png, logo.jpg, logo.svg)
      await Promise.all(['png', 'jpg', 'svg'].map((e) => unlinkIfExists(path.join(dir, `logo.${e}`))));

      const filename = `logo.${ext}`;
      const buffer = Buffer.from(await f.arrayBuffer());
      await fs.writeFile(path.join(dir, filename), buffer);

      const publicPath = `/of-assets/${user.tenantId}/${filename}`;
      const before = await prisma.tenant.findUnique({ where: { id: user.tenantId }, select: { logoPath: true } });
      await prisma.tenant.update({ where: { id: user.tenantId }, data: { logoPath: publicPath } });

      invalidateAssetCache(user.tenantId);
      await logTenantSettingsChange({
        tenantId: user.tenantId,
        userId: user.id,
        action: 'parameters.upload.logo',
        diff: { logoPath: { before: before?.logoPath ?? null, after: publicPath } },
      });
      revalidatePath('/app/parametres');
      return { ok: true, path: publicPath };
    }

    export async function resetTenantLogo(): Promise<SimpleResult> {
      const { user } = await validateRequest();
      if (!user) return { ok: false, error: 'Non authentifié' };

      const before = await prisma.tenant.findUnique({ where: { id: user.tenantId }, select: { logoPath: true } });
      if (!before?.logoPath) return { ok: true }; // déjà reset

      const dir = tenantAssetDir(user.tenantId);
      await Promise.all(['png', 'jpg', 'svg'].map((e) => unlinkIfExists(path.join(dir, `logo.${e}`))));

      await prisma.tenant.update({ where: { id: user.tenantId }, data: { logoPath: null } });
      invalidateAssetCache(user.tenantId);
      await logTenantSettingsChange({
        tenantId: user.tenantId,
        userId: user.id,
        action: 'parameters.reset.logo',
        diff: { logoPath: { before: before.logoPath, after: null } },
      });
      revalidatePath('/app/parametres');
      return { ok: true };
    }

    // ---------- SIGNATURES ----------
    export async function uploadTenantSignature(
      role: 'pedago' | 'dirigeant',
      formData: FormData,
    ): Promise<AssetResult> {
      const { user } = await validateRequest();
      if (!user) return { ok: false, error: 'Non authentifié' };

      const f = formData.get('file');
      if (!(f instanceof File) || f.size === 0) return { ok: false, error: 'Aucun fichier' };
      if (f.size > MAX_SIGNATURE_MB * 1024 * 1024) return { ok: false, error: `Signature > ${MAX_SIGNATURE_MB} Mo` };
      if (!ALLOWED_SIGNATURE_MIME.has(f.type)) return { ok: false, error: 'Format autorisé : PNG, JPG' };

      const filename = role === 'pedago' ? 'signature-pedago.png' : 'signature-dirigeant.png';
      const dir = tenantAssetDir(user.tenantId);
      await fs.mkdir(dir, { recursive: true });

      const buffer = Buffer.from(await f.arrayBuffer());
      await fs.writeFile(path.join(dir, filename), buffer);

      const publicPath = `/of-assets/${user.tenantId}/${filename}`;
      const fieldName = role === 'pedago' ? 'signaturePedagoPath' : 'signatureDirigeantPath';
      const before = await prisma.tenant.findUnique({
        where: { id: user.tenantId },
        select: { signaturePedagoPath: true, signatureDirigeantPath: true },
      });
      const beforeVal = role === 'pedago' ? before?.signaturePedagoPath : before?.signatureDirigeantPath;
      await prisma.tenant.update({ where: { id: user.tenantId }, data: { [fieldName]: publicPath } });

      invalidateAssetCache(user.tenantId);
      await logTenantSettingsChange({
        tenantId: user.tenantId,
        userId: user.id,
        action: `parameters.upload.signature.${role}`,
        diff: { [fieldName]: { before: beforeVal ?? null, after: publicPath } },
      });
      revalidatePath('/app/parametres');
      return { ok: true, path: publicPath };
    }

    export async function resetTenantSignature(role: 'pedago' | 'dirigeant'): Promise<SimpleResult> {
      const { user } = await validateRequest();
      if (!user) return { ok: false, error: 'Non authentifié' };

      const fieldName = role === 'pedago' ? 'signaturePedagoPath' : 'signatureDirigeantPath';
      const filename = role === 'pedago' ? 'signature-pedago.png' : 'signature-dirigeant.png';

      const before = await prisma.tenant.findUnique({
        where: { id: user.tenantId },
        select: { signaturePedagoPath: true, signatureDirigeantPath: true },
      });
      const beforeVal = role === 'pedago' ? before?.signaturePedagoPath : before?.signatureDirigeantPath;
      if (!beforeVal) return { ok: true };

      await unlinkIfExists(path.join(tenantAssetDir(user.tenantId), filename));
      await prisma.tenant.update({ where: { id: user.tenantId }, data: { [fieldName]: null } });

      invalidateAssetCache(user.tenantId);
      await logTenantSettingsChange({
        tenantId: user.tenantId,
        userId: user.id,
        action: `parameters.reset.signature.${role}`,
        diff: { [fieldName]: { before: beforeVal, after: null } },
      });
      revalidatePath('/app/parametres');
      return { ok: true };
    }
    ```

    Créer `apps/web/src/server/actions/__tests__/tenant-assets.test.ts` :
    - Mock `validateRequest` pour retourner user fictif
    - Mock `prisma` via `vi.mock('@qualiof/db', ...)`
    - Mock `fs` via `vi.mock('node:fs', ...)` ET `vi.spyOn(fs.promises, 'writeFile')` pour observer les writes sans toucher au disque réel (sauf si tests utilisent un tmpdir)
    - Mock `invalidateAssetCache` et `logTenantSettingsChange` pour vérifier qu'elles sont appelées
    - 10 tests selon `behavior` ci-dessus
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- src/server/actions/__tests__/tenant-assets.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export async function uploadTenantLogo" apps/web/src/server/actions/tenant-assets.ts` retourne 1 ligne
    - `grep -E "export async function uploadTenantSignature" apps/web/src/server/actions/tenant-assets.ts` retourne 1 ligne
    - `grep -E "export async function resetTenantLogo" apps/web/src/server/actions/tenant-assets.ts` retourne 1 ligne
    - `grep -E "export async function resetTenantSignature" apps/web/src/server/actions/tenant-assets.ts` retourne 1 ligne
    - `grep -c "invalidateAssetCache" apps/web/src/server/actions/tenant-assets.ts` ≥ 4 (1 par action)
    - `grep -c "logTenantSettingsChange" apps/web/src/server/actions/tenant-assets.ts` ≥ 4
    - `grep -c "parameters.upload.logo" apps/web/src/server/actions/tenant-assets.ts` ≥ 1
    - `grep -c "parameters.upload.signature" apps/web/src/server/actions/tenant-assets.ts` ≥ 1
    - `grep -c "parameters.reset" apps/web/src/server/actions/tenant-assets.ts` ≥ 2
    - `grep -c "MAX_LOGO_MB\\|MAX_SIGNATURE_MB" apps/web/src/server/actions/tenant-assets.ts` ≥ 2
    - `pnpm --filter @qualiof/web test -- src/server/actions/__tests__/tenant-assets.test.ts --run` : 10 tests verts
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
  </acceptance_criteria>
  <done>
    4 server actions exportées (uploadTenantLogo, uploadTenantSignature, resetTenantLogo, resetTenantSignature). Validation taille + MIME. Side effects orchestrés : write/unlink disque, update Tenant.{logoPath|signaturePedagoPath|signatureDirigeantPath}, invalidateAssetCache, logTenantSettingsChange (5 actions namespacées parameters.upload.* et parameters.reset.*). 10 tests verts. Plan 07-04 peut consommer ces actions.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web test -- src/lib/closure/__tests__/shared-template.test.ts --run` : 8 tests verts
- `pnpm --filter @qualiof/web test -- src/server/actions/__tests__/tenant-assets.test.ts --run` : 10 tests verts
- `grep -c "logoCache" apps/web/src/lib/programme-template.ts apps/web/src/lib/convention-template.ts` = 0 (cascades nettoyés)
- `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
</verification>

<success_criteria>
- `loadAssetDataUrl(filenames, tenantId?)` cherche d'abord tenant dir, fallback bundled assets
- `invalidateAssetCache(tenantId)` exporté + appelé par toutes les actions upload/reset
- `programme-template.ts` + `convention-template.ts` consomment `loadLogoColorDataUrl(tenantId)` (cascade upload UI affecte ces templates aussi)
- 4 server actions assets exportées avec audit + validation
- 18 tests Vitest verts (8 shared-template + 10 tenant-assets)
</success_criteria>

<output>
Créer `.planning/phases/07-param-tres-organisme-ditables/07-03-SUMMARY.md` documentant :
- API surface tenant-assets actions (signatures + AssetResult discriminé)
- Liste des conventions AuditLog action `parameters.upload.logo / .signature.pedago / .signature.dirigeant / parameters.reset.*`
- Validation contraintes (5Mo logo, 2Mo signature, MIME types acceptés)
- Cascade tenantId propagée jusqu'aux templates programme/convention/closure
</output>
