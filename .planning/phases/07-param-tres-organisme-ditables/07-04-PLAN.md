---
phase: 07-param-tres-organisme-ditables
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - apps/web/src/app/app/parametres/page.tsx
  - apps/web/src/app/app/parametres/__tests__/page.smoke.test.ts
  - apps/web/src/components/settings/settings-section.tsx
  - apps/web/src/components/settings/of-identity-form.tsx
  - apps/web/src/components/settings/of-address-form.tsx
  - apps/web/src/components/settings/of-assets-form.tsx
  - apps/web/src/components/settings/of-invoicing-form.tsx
  - apps/web/src/components/settings/of-banking-form.tsx
  - apps/web/src/components/settings/of-email-form.tsx
  - apps/web/src/lib/iban-format.ts
  - apps/web/src/lib/__tests__/iban-format.test.ts
autonomous: true
requirements: [SET-01, SET-02, SET-03]

must_haves:
  truths:
    - "Page /app/parametres affiche 6 sections éditables : Identité / Adresse & mentions / Logo & signatures / Numérotation / RIB / Email"
    - "Chaque section a un bouton 'Modifier' qui bascule en mode édition (pattern Phase 5 inline edit)"
    - "Formulaires utilisent react-hook-form + zodResolver côté client (validation cliente)"
    - "Save action invoque la server action correspondante de tenant-settings.ts et affiche toast sonner sur succès"
    - "Erreurs Zod côté serveur sont affichées sous chaque champ via fieldErrors"
    - "Section Numérotation : AlertDialog confirmation si invoicePrefix change ET prisma.invoice.count > 0 (Pitfall 4 RESEARCH.md)"
    - "Section RIB : helper formatIban affiche l'IBAN avec espaces tous les 4 chars en read mode"
    - "Section Logo & signatures : thumbnail + bouton Remplacer + AlertDialog Restaurer par défaut"
    - "Sections existantes Utilisateurs (placeholder Phase 8) / OPCO référencés / Référentiel docs Qualiopi conservées en read-only"
    - "Smoke test ancré : page rend sans erreur runtime (anti-régression style BUG-01 FileText)"
  artifacts:
    - path: "apps/web/src/app/app/parametres/page.tsx"
      provides: "Page Server Component refactorée avec 6 sections éditables + 3 sections legacy read-only"
      min_lines: 100
    - path: "apps/web/src/components/settings/settings-section.tsx"
      provides: "Wrapper card mode read ↔ edit réutilisable par les 6 forms"
      exports: ["SettingsSection"]
    - path: "apps/web/src/components/settings/of-identity-form.tsx"
      provides: "Form SIRET/numDA/RCS/legalForm/name (SET-01)"
      exports: ["OfIdentityForm"]
    - path: "apps/web/src/components/settings/of-address-form.tsx"
      provides: "Form adresse + mentions légales (SET-02 texte)"
      exports: ["OfAddressForm"]
    - path: "apps/web/src/components/settings/of-assets-form.tsx"
      provides: "Form upload logo + signatures (SET-02 assets)"
      exports: ["OfAssetsForm"]
    - path: "apps/web/src/components/settings/of-invoicing-form.tsx"
      provides: "Form invoicePrefix avec AlertDialog discontinuité (SET-03)"
      exports: ["OfInvoicingForm"]
    - path: "apps/web/src/components/settings/of-banking-form.tsx"
      provides: "Form IBAN/BIC avec formatIban (SET-03)"
      exports: ["OfBankingForm"]
    - path: "apps/web/src/components/settings/of-email-form.tsx"
      provides: "Form emailFrom (SET-03)"
      exports: ["OfEmailForm"]
    - path: "apps/web/src/lib/iban-format.ts"
      provides: "formatIban(s) helper affichage espaces tous les 4 chars"
      exports: ["formatIban"]
  key_links:
    - from: "apps/web/src/components/settings/of-identity-form.tsx"
      to: "apps/web/src/server/actions/tenant-settings.ts"
      via: "updateTenantIdentity(input)"
      pattern: "updateTenantIdentity"
    - from: "apps/web/src/components/settings/of-address-form.tsx"
      to: "apps/web/src/server/actions/tenant-settings.ts"
      via: "updateTenantAddress(input)"
      pattern: "updateTenantAddress"
    - from: "apps/web/src/components/settings/of-invoicing-form.tsx"
      to: "apps/web/src/server/actions/tenant-settings.ts"
      via: "updateTenantBilling(input)"
      pattern: "updateTenantBilling"
    - from: "apps/web/src/components/settings/of-banking-form.tsx"
      to: "apps/web/src/server/actions/tenant-settings.ts"
      via: "updateTenantBilling(input)"
      pattern: "updateTenantBilling"
    - from: "apps/web/src/components/settings/of-email-form.tsx"
      to: "apps/web/src/server/actions/tenant-settings.ts"
      via: "updateTenantEmail(input)"
      pattern: "updateTenantEmail"
    - from: "apps/web/src/components/settings/of-assets-form.tsx"
      to: "apps/web/src/server/actions/tenant-assets.ts"
      via: "uploadTenantLogo / uploadTenantSignature / reset*"
      pattern: "uploadTenant|resetTenant"
---

<objective>
Transformer `/app/parametres/page.tsx` (read-only `dl/dt/dd`) en page éditable avec 6 sections inline-edit (pattern Phase 5 fiche apprenant). Consomme les server actions de 07-02 et 07-03 (les deux runs en parallèle wave 3, mais 07-04 dépend formellement de 07-02 pour les schémas et de 07-03 (effective) pour les upload actions — si 07-03 pas encore mergé, mocker temporairement les boutons upload).

Purpose: SET-01 + SET-02 + SET-03 livrés côté UI. Laurent peut éditer Identité OF, Adresse, Logo, Signatures, Numérotation, RIB, Email depuis l'interface sans toucher au .env ni au code.

Output:
- Refactor `parametres/page.tsx` : 6 sections éditables + sections existantes (Utilisateurs/OPCO/Docs) conservées read-only
- 6 form components + 1 wrapper `SettingsSection`
- Helper `iban-format.ts` (espaces tous les 4 chars)
- Smoke test page (anti-régression BUG-01 style)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md
@.planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md
@.planning/phases/07-param-tres-organisme-ditables/07-02-SUMMARY.md
@apps/web/src/app/app/parametres/page.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts

<interfaces>
<!-- Server actions from Plan 07-02 -->
```ts
import {
  updateTenantIdentity,
  updateTenantAddress,
  updateTenantBilling,
  updateTenantEmail,
} from '@/server/actions/tenant-settings';
// Returns: { ok: true } | { ok: false; error: string; fieldErrors?: Record<string, string[]> }
```

<!-- Server actions from Plan 07-03 -->
```ts
import {
  uploadTenantLogo,
  uploadTenantSignature,
  resetTenantLogo,
  resetTenantSignature,
} from '@/server/actions/tenant-assets';
```

<!-- Zod schemas + types from Plan 07-02 (consumed via zodResolver client-side) -->
```ts
import {
  tenantIdentitySchema, type TenantIdentityInput,
  tenantAddressSchema, type TenantAddressInput,
  tenantBillingSchema, type TenantBillingInput,
  tenantEmailSchema, type TenantEmailInput,
} from '@qualiof/shared/schemas/tenant';
```

<!-- 6 Sections layout (per Finding 8 RESEARCH.md) -->
1. Organisme — Identité légale (icon Building2): name, siret, numDA, rcs, legalForm
2. Adresse & mentions légales (icon MapPin): address (street/cp/city), legalMentions
3. Logo & signatures (icon Image): 3 upload zones (logo, signature pedago, signature dirigeant)
4. Numérotation factures (icon Hash): invoicePrefix + preview "Le prochain numéro sera : FAC-000042"
5. Coordonnées bancaires (icon CreditCard): iban (formatté), bic
6. Email expéditeur (icon Mail): emailFrom + info "Le mot de passe SMTP reste en config serveur"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper iban-format + SettingsSection wrapper + page.tsx refactor (Server Component shell)</name>
  <files>apps/web/src/lib/iban-format.ts, apps/web/src/lib/__tests__/iban-format.test.ts, apps/web/src/components/settings/settings-section.tsx, apps/web/src/app/app/parametres/page.tsx, apps/web/src/app/app/parametres/__tests__/page.smoke.test.ts</files>
  <read_first>
    - apps/web/src/app/app/parametres/page.tsx (intégralement — 126 lignes, comprendre le pattern Field local + sections existantes Utilisateurs/OPCO/Docs à conserver)
    - apps/web/src/app/app/apprenants/[id]/page.tsx ou tout autre exemple de section éditable Phase 5 (LearnerCompletenessBadge ou similaire — pattern inline edit)
    - apps/web/src/components/ui/card.tsx (primitive card)
    - apps/web/src/components/ui/button.tsx (primitive button)
    - apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts (pattern smoke test)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 8 — layout + edge cases UI)
    - .planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md (Claude's Discretion — inline edit pattern Phase 5)
  </read_first>
  <behavior>
    - Test 1 (iban-format): `formatIban('FR7612345678901234567890123')` retourne `'FR76 1234 5678 9012 3456 7890 123'`
    - Test 2 (iban-format): `formatIban(null)` retourne `''`
    - Test 3 (iban-format): `formatIban('FR76 1234 5678 9012 3456 7890 123')` retourne idem (idempotent)
    - Test 4 (iban-format): `formatIban('')` retourne `''`
    - Test 5 (smoke page): `import('@/app/app/parametres/page')` ne throw pas
    - Test 6 (smoke page): la page exporte un default function async (Server Component)
  </behavior>
  <action>
    1. **Créer `apps/web/src/lib/iban-format.ts`** :
       ```ts
       /**
        * Formate un IBAN avec un espace tous les 4 caractères.
        * Ex: 'FR7612345678901234567890123' → 'FR76 1234 5678 9012 3456 7890 123'
        * Idempotent (les espaces existants sont normalisés d'abord).
        */
       export function formatIban(s: string | null | undefined): string {
         if (!s) return '';
         const clean = s.replace(/\s/g, '').toUpperCase();
         if (!clean) return '';
         return clean.replace(/(.{4})/g, '$1 ').trim();
       }
       ```

    2. **Créer `apps/web/src/lib/__tests__/iban-format.test.ts`** avec 4 tests Vitest selon `behavior`.

    3. **Créer `apps/web/src/components/settings/settings-section.tsx`** (wrapper card) :
       ```tsx
       'use client';
       import { useState, type ReactNode } from 'react';
       import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
       import { Button } from '@/components/ui/button';
       import { type LucideIcon } from 'lucide-react';

       export interface SettingsSectionProps {
         icon: LucideIcon;
         title: string;
         description?: string;
         readView: ReactNode;
         editView: (onSaved: () => void, onCancel: () => void) => ReactNode;
         allowEdit?: boolean; // false → masque le bouton Modifier (lecture seule)
       }

       export function SettingsSection({ icon: Icon, title, description, readView, editView, allowEdit = true }: SettingsSectionProps) {
         const [editing, setEditing] = useState(false);
         return (
           <Card>
             <CardHeader className="flex flex-row items-start justify-between gap-2">
               <div className="flex items-start gap-2">
                 <Icon className="h-5 w-5 mt-0.5 text-muted-foreground" aria-hidden />
                 <div>
                   <CardTitle>{title}</CardTitle>
                   {description ? <p className="text-sm text-muted-foreground mt-1">{description}</p> : null}
                 </div>
               </div>
               {allowEdit && !editing ? (
                 <Button variant="outline" size="sm" onClick={() => setEditing(true)}>Modifier</Button>
               ) : null}
             </CardHeader>
             <CardContent>
               {editing
                 ? editView(() => setEditing(false), () => setEditing(false))
                 : readView}
             </CardContent>
           </Card>
         );
       }
       ```

    4. **Refactor `apps/web/src/app/app/parametres/page.tsx`** en Server Component :
       - Garder `PageHeader` + import `validateRequest` + `prisma.tenant.findUnique({ where: { id: user.tenantId } })` pour récupérer le Tenant complet
       - Récupérer aussi `prisma.invoice.count({ where: { tenantId: user.tenantId } })` pour la section Numérotation
       - Importer les 6 form components (qui seront créés Task 2)
       - Layout : une seule colonne `grid-cols-1 gap-6` (sections empilées per Finding 8 — recommandation A)
       - Rendu :
         - 6 nouvelles sections via les form components (Identité, Adresse, Logo&Signatures, Numérotation, RIB, Email)
         - 3 sections legacy conservées via `<SettingsSection allowEdit={false}>` (Utilisateurs placeholder Phase 8, OPCO référencés, Référentiel docs Qualiopi)
       - **NE PAS** importer FileText sans s'assurer qu'il vient bien de lucide-react (cf. anti-regression BUG-01).
       - Imports Lucide explicites : `Building2, MapPin, Image, Hash, CreditCard, Mail, Users, Sparkles, Settings`

    5. **Créer `apps/web/src/app/app/parametres/__tests__/page.smoke.test.ts`** sur le modèle de `apps/web/src/app/app/sessions/[id]/__tests__/page.smoke.test.ts` :
       ```ts
       import { describe, it, expect } from 'vitest';

       describe('parametres/page smoke', () => {
         it('imports without throwing (anti-regression BUG-01 style)', async () => {
           const mod = await import('@/app/app/parametres/page');
           expect(mod.default).toBeDefined();
           expect(typeof mod.default).toBe('function');
         });
       });
       ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- src/lib/__tests__/iban-format.test.ts src/app/app/parametres/__tests__/page.smoke.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export function formatIban" apps/web/src/lib/iban-format.ts` retourne 1 ligne
    - `grep -E "export function SettingsSection" apps/web/src/components/settings/settings-section.tsx` retourne 1 ligne
    - `grep -c "Building2\\|MapPin\\|Image\\|Hash\\|CreditCard\\|Mail" apps/web/src/app/app/parametres/page.tsx` ≥ 6 (icônes 6 sections)
    - `grep -c "OfIdentityForm\\|OfAddressForm\\|OfAssetsForm\\|OfInvoicingForm\\|OfBankingForm\\|OfEmailForm" apps/web/src/app/app/parametres/page.tsx` ≥ 6
    - `pnpm --filter @qualiof/web test -- src/lib/__tests__/iban-format.test.ts --run` : 4 tests verts
    - `pnpm --filter @qualiof/web test -- src/app/app/parametres/__tests__/page.smoke.test.ts --run` : 1-2 tests verts
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur sur les nouveaux fichiers (les forms importés ne sont pas encore créés → utiliser des stubs temporaires, voir Task 2)
  </acceptance_criteria>
  <done>
    Helper `formatIban` testé. Wrapper réutilisable `SettingsSection` (Card avec mode read ↔ edit). Page Server Component refactorée avec récupération Tenant + invoice count, importe les 6 forms (qui seront livrés Task 2). Smoke test ancré anti-régression. Sections legacy Utilisateurs/OPCO/Docs conservées.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6 Form components (Identity / Address / Invoicing / Banking / Email + Assets stub si 07-03 pas mergé)</name>
  <files>apps/web/src/components/settings/of-identity-form.tsx, apps/web/src/components/settings/of-address-form.tsx, apps/web/src/components/settings/of-assets-form.tsx, apps/web/src/components/settings/of-invoicing-form.tsx, apps/web/src/components/settings/of-banking-form.tsx, apps/web/src/components/settings/of-email-form.tsx</files>
  <read_first>
    - apps/web/src/server/actions/tenant-settings.ts (post-Plan 07-02 — signatures exactes des 4 actions)
    - apps/web/src/server/actions/tenant-assets.ts (post-Plan 07-03 — signatures upload/reset)
    - packages/shared/src/schemas/tenant.ts (schémas Zod réutilisables côté client via zodResolver)
    - apps/web/src/lib/iban-format.ts (Task 1)
    - apps/web/src/components/ui/dialog.tsx + apps/web/src/components/ui/alert-dialog.tsx (Radix primitives pour confirmation discontinuité)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 8 — sections détaillées, Pitfall 4 — AlertDialog discontinuité préfixe, Pitfall 5 — validation IBAN avec espaces)
  </read_first>
  <action>
    Chaque form component suit le pattern client suivant :
    ```tsx
    'use client';
    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { toast } from 'sonner';
    import { tenantIdentitySchema, type TenantIdentityInput } from '@qualiof/shared/schemas/tenant';
    import { updateTenantIdentity } from '@/server/actions/tenant-settings';
    // ...
    const form = useForm<TenantIdentityInput>({ resolver: zodResolver(tenantIdentitySchema), defaultValues });
    const onSubmit = async (input: TenantIdentityInput) => {
      const result = await updateTenantIdentity(input);
      if (result.ok) {
        toast.success('Identité enregistrée');
        onSaved();
      } else {
        if (result.fieldErrors) {
          for (const [field, errors] of Object.entries(result.fieldErrors)) {
            form.setError(field as keyof TenantIdentityInput, { message: errors?.[0] ?? 'Erreur' });
          }
        }
        toast.error(result.error);
      }
    };
    ```

    1. **`of-identity-form.tsx`** : props `{ initial: { name, siret, numDA, rcs, legalForm }; onSaved: () => void; onCancel: () => void }`. Champs Input :
       - name (required)
       - siret (placeholder "14 chiffres")
       - numDA (placeholder "Numéro de déclaration d'activité")
       - rcs
       - legalForm (Input texte libre — peut migrer vers Select avec enum LegalForm si Open Question 1 tranchée)
       - Bouton Submit + bouton Annuler (onCancel)
       - Calls `updateTenantIdentity`

    2. **`of-address-form.tsx`** : champs Input :
       - address.street
       - address.cp
       - address.city
       - legalMentions (Textarea, max 2000 chars, afficher compteur)
       - Calls `updateTenantAddress`

    3. **`of-assets-form.tsx`** (le plus complexe — 3 zones upload) :
       - Pour chaque asset (logo, signature pedago, signature dirigeant) :
         - Thumbnail courant (depuis Tenant.{logoPath|signaturePedagoPath|signatureDirigeantPath} ou fallback path bundled) avec `<img src={path + '?v=' + Date.now()} />` pour cache-busting (Pitfall 2 RESEARCH.md)
         - `<form action={uploadTenantLogo}><input type="file" name="file" accept="..." /><Button>Remplacer</Button></form>`
         - Pour signature : appeler `uploadTenantSignature.bind(null, 'pedago')` (ou via wrapper Server Action helper si bind non supporté côté form action)
         - Bouton "Restaurer par défaut" qui ouvre AlertDialog : "Cette action supprimera le fichier uploadé. Continuer ?" + bouton Confirm appelle `resetTenantLogo()` / `resetTenantSignature(role)`
       - Toast sonner sur succès/erreur

    4. **`of-invoicing-form.tsx`** :
       - Input `invoicePrefix` (regex 2-5 majuscules — validation client via Zod)
       - Affichage preview : "Le prochain numéro sera : {invoicePrefix}-{(invoiceCount + 1).toString().padStart(6, '0')}"
       - **Pitfall 4** : props `{ initial: { invoicePrefix }; invoiceCount: number; onSaved; onCancel }`. Au submit, SI `invoiceCount > 0 && newPrefix !== initial.invoicePrefix` → ouvrir AlertDialog "Vous avez déjà {invoiceCount} factures avec le préfixe '{initial.invoicePrefix}'. Changer maintenant créera une discontinuité dans la séquence (le compteur repartira de 1). Continuer ?" + bouton Confirmer/Annuler.
       - Calls `updateTenantBilling` (qui fusionne invoicePrefix + iban + bic — note: cette form gère SEULEMENT invoicePrefix, en passant iban/bic = valeurs courantes pour ne pas les reset)

    5. **`of-banking-form.tsx`** :
       - Input IBAN avec tooltip "Format : FR + 2 chiffres + 23 caractères, espaces autorisés"
       - Input BIC
       - En read mode : afficher IBAN via `formatIban(iban)` (espaces tous les 4 chars)
       - Calls `updateTenantBilling` (passe invoicePrefix courant pour ne pas le reset)

    6. **`of-email-form.tsx`** :
       - Input email `emailFrom`
       - Info box ℹ️ : "Le mot de passe SMTP est géré dans la configuration serveur, pas dans cette interface (sécurité)."
       - Calls `updateTenantEmail`

    **Note importante** : `updateTenantBilling` accepte le triplet { invoicePrefix, iban, bic }. Les forms 4 (invoicing) et 5 (banking) doivent passer les valeurs courantes pour les champs qu'elles ne modifient PAS. Sinon ils seraient écrasés à null. Alternative : split `updateTenantBilling` en `updateInvoicePrefix` + `updateBanking` server actions (mais ça ajoute du code Plan 07-02 — préférer passer le triplet complet ici).

    **Toast pattern** : `import { toast } from 'sonner';` (déjà installé) — `toast.success(...)` / `toast.error(...)`.
  </action>
  <verify>
    <automated>cd apps/web && ls src/components/settings/*.tsx | wc -l | grep -E "^\s*7\s*$" && pnpm exec tsc --noEmit 2>&1 | grep -E "components/settings/" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/web/src/components/settings/*.tsx | wc -l` ≥ 7 (settings-section + 6 forms)
    - `grep -E "export function OfIdentityForm" apps/web/src/components/settings/of-identity-form.tsx` retourne 1 ligne
    - `grep -E "export function OfAddressForm" apps/web/src/components/settings/of-address-form.tsx` retourne 1 ligne
    - `grep -E "export function OfAssetsForm" apps/web/src/components/settings/of-assets-form.tsx` retourne 1 ligne
    - `grep -E "export function OfInvoicingForm" apps/web/src/components/settings/of-invoicing-form.tsx` retourne 1 ligne
    - `grep -E "export function OfBankingForm" apps/web/src/components/settings/of-banking-form.tsx` retourne 1 ligne
    - `grep -E "export function OfEmailForm" apps/web/src/components/settings/of-email-form.tsx` retourne 1 ligne
    - `grep -c "updateTenantIdentity" apps/web/src/components/settings/of-identity-form.tsx` ≥ 1
    - `grep -c "updateTenantAddress" apps/web/src/components/settings/of-address-form.tsx` ≥ 1
    - `grep -c "updateTenantBilling" apps/web/src/components/settings/of-invoicing-form.tsx` ≥ 1
    - `grep -c "updateTenantBilling" apps/web/src/components/settings/of-banking-form.tsx` ≥ 1
    - `grep -c "updateTenantEmail" apps/web/src/components/settings/of-email-form.tsx` ≥ 1
    - `grep -c "uploadTenantLogo\\|uploadTenantSignature\\|resetTenantLogo\\|resetTenantSignature" apps/web/src/components/settings/of-assets-form.tsx` ≥ 2
    - `grep -E "AlertDialog" apps/web/src/components/settings/of-invoicing-form.tsx` ≥ 1 ligne (confirmation discontinuité)
    - `grep -c "formatIban" apps/web/src/components/settings/of-banking-form.tsx` ≥ 1
    - `grep -c "zodResolver" apps/web/src/components/settings/of-identity-form.tsx` ≥ 1
    - `grep -c "toast" apps/web/src/components/settings/*.tsx` ≥ 6 (toast par form)
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
    - `pnpm --filter @qualiof/web build` : compile sans erreur
  </acceptance_criteria>
  <done>
    6 form components créés sous `apps/web/src/components/settings/`, chacun :
    - Client component avec react-hook-form + zodResolver
    - Consomme la server action correspondante (tenant-settings.ts ou tenant-assets.ts)
    - Affiche toast sonner sur succès/erreur
    - Maps fieldErrors aux champs via form.setError
    - of-invoicing-form a AlertDialog discontinuité préfixe
    - of-banking-form utilise formatIban pour read mode
    Build TS+Next clean.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web test -- src/lib/__tests__/iban-format.test.ts src/app/app/parametres/__tests__/page.smoke.test.ts --run` : 5-6 tests verts
- `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
- `pnpm --filter @qualiof/web build` : page `/app/parametres` compile
- Manuel (delegated to Plan 07-05 verification) : ouvrir `/app/parametres`, voir 9 sections (6 éditables + 3 read-only), cliquer Modifier sur Identité, modifier name, Enregistrer → toast OK + recharger affiche la valeur saisie.
</verification>

<success_criteria>
- Page Server Component refactorée avec 6 sections éditables + 3 read-only legacy
- 6 form components fonctionnels avec validation Zod client+server
- Helper `formatIban` testé
- Smoke test page ancrée anti-régression
- AlertDialog discontinuité préfixe facture en place
- Toast sonner sur chaque save
- Build clean
</success_criteria>

<output>
Créer `.planning/phases/07-param-tres-organisme-ditables/07-04-SUMMARY.md` documentant :
- Liste des 9 sections de la page Paramètres (6 éditables + 3 legacy)
- Décisions UX : sections empilées (recommandation A Finding 8) vs tabs
- Pattern client component / server action consommation
- Notes sur Pitfall 4 (AlertDialog discontinuité) et Pitfall 2 (cache-busting `?v=Date.now()`)
- Open Question 1 (legalForm String vs enum) — gardé String, à arbitrer plus tard si Laurent demande Select
</output>
