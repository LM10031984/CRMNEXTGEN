---
phase: 07-param-tres-organisme-ditables
plan: 05
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .env.example
autonomous: true
requirements: [SET-01, SET-02, SET-03]

must_haves:
  truths:
    - "REQUIREMENTS.md marque SET-01, SET-02, SET-03 comme [x] DONE avec date et résumé d'implémentation"
    - "ROADMAP.md marque Phase 7 status = Complete + liste des 5 plans avec [x]"
    - "STATE.md incrémente completed_phases (2 → 3) + completed_plans (+5) + met à jour last_session + key decisions"
    - ".env.example contient un commentaire indiquant que les OF_* sont désormais fallback secondaire (BDD prioritaire)"
    - "Build apps/web compile clean : pnpm --filter @qualiof/web build retourne exit 0"
    - "Test suite passe : pnpm test (Turbo) retourne exit 0"
    - "grep regression vide : aucun process.env.OF_* hors of-config.ts"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traçabilité SET-01/02/03 livrés avec preuves"
      contains: "SET-01.*DONE"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 marquée Complete avec liste plans"
      contains: "07-01-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Compteurs progress incrémentés + last_session pointe sur /gsd:plan-phase 8"
      contains: "Phase 8"
    - path: ".env.example"
      provides: "Commentaire fallback secondaire pour OF_*"
      contains: "fallback"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Traceability table"
      pattern: "SET-01.*SET-02.*SET-03"
---

<objective>
Clôturer Phase 7 : mise à jour de la documentation projet (REQUIREMENTS, ROADMAP, STATE), ajout d'un commentaire dans `.env.example` pour expliquer que les OF_* deviennent fallback secondaire, et exécution finale des smoke tests (build + vitest complet) pour valider que rien n'est cassé dans le monorepo.

Purpose: Pattern bookkeeping établi en Phase 6 (Plan 06-04). Permettre à `/gsd:plan-phase 8` de démarrer avec un état projet cohérent (REQUIREMENTS à jour, STATE pointant Phase 8 next).

Output:
- 3 documents `.planning/` mis à jour avec résumés livraison Phase 7
- `.env.example` enrichi d'un commentaire fallback
- Preuves smoke build + test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-param-tres-organisme-ditables/07-01-SUMMARY.md
@.planning/phases/07-param-tres-organisme-ditables/07-02-SUMMARY.md
@.planning/phases/07-param-tres-organisme-ditables/07-03-SUMMARY.md
@.planning/phases/07-param-tres-organisme-ditables/07-04-SUMMARY.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke build + test full suite (gate avant bookkeeping)</name>
  <files>(aucune modification fichier — exécution commandes vérification)</files>
  <read_first>
    - .planning/phases/07-param-tres-organisme-ditables/07-01-SUMMARY.md (vérifier que migration Prisma appliquée)
    - .planning/phases/07-param-tres-organisme-ditables/07-02-SUMMARY.md
    - .planning/phases/07-param-tres-organisme-ditables/07-03-SUMMARY.md
    - .planning/phases/07-param-tres-organisme-ditables/07-04-SUMMARY.md
  </read_first>
  <action>
    1. **Smoke build apps/web** : `pnpm --filter @qualiof/web build` doit compiler sans erreur. Capturer la sortie (5 dernières lignes).
    2. **Smoke test full suite** : `pnpm test` (Turbo répercute sur @qualiof/shared + @qualiof/web). Doit retourner exit 0. Capturer le nombre de tests passés (cumul ~58 nouveaux pour Phase 7).
    3. **Grep regression drift OF** : `grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config"` DOIT être vide (preuve Plan 07-01 a bien fixé le drift Pitfall 3 RESEARCH.md).
    4. **Grep regression invoices numbering** : `grep -c "function nextInvoiceNumber" apps/web/src/server/actions/invoices.ts` DOIT retourner 0 (preuve helper extrait dans lib/numbering.ts via Plan 07-02).
    5. **Grep regression logoCache local** : `grep -c "logoCache" apps/web/src/lib/programme-template.ts apps/web/src/lib/convention-template.ts` DOIT retourner 0 dans les deux (preuve Plan 07-03 a bien cascade).
    6. **Optionnel** smoke route runtime (pattern BUG-01) : démarrer `pnpm --filter @qualiof/web dev` en background si possible, `curl -s http://localhost:3000/app/parametres -o /dev/null -w "%{http_code}"`. Acceptable : 200 ou 302 redirect vers /login (auth requise sans cookie). NE PAS accepter 500.
    7. Si tout vert : passer à Task 2 (bookkeeping). Si rouge : STOP, documenter l'erreur dans le SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && bash -c 'set -e; pnpm --filter @qualiof/web build 2>&1 | tail -5; pnpm test 2>&1 | tail -20; LEAK=$(grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config" || true); if [ -n "$LEAK" ]; then echo "DRIFT REGRESSION:"; echo "$LEAK"; exit 1; fi; LOGO_LEAK=$(grep -c "logoCache" apps/web/src/lib/programme-template.ts apps/web/src/lib/convention-template.ts | grep -v ":0" || true); if [ -n "$LOGO_LEAK" ]; then echo "LOGOCACHE REGRESSION:"; echo "$LOGO_LEAK"; exit 1; fi; echo "All smoke checks GREEN"'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @qualiof/web build` exit code = 0
    - `pnpm test` exit code = 0
    - `grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config"` retourne 0 ligne
    - `grep -c "function nextInvoiceNumber" apps/web/src/server/actions/invoices.ts` retourne 0
    - `grep -c "logoCache" apps/web/src/lib/programme-template.ts` retourne 0
    - `grep -c "logoCache" apps/web/src/lib/convention-template.ts` retourne 0
  </acceptance_criteria>
  <done>
    Build apps/web clean. Test suite verte. Aucune regression drift OF_*. Aucune regression invoices.ts numérotation locale. Aucune regression logoCache local. Phase 7 implementation prouvée stable, prête pour bookkeeping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bookkeeping REQUIREMENTS + ROADMAP + STATE + .env.example</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .env.example</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lignes 45-47 — SET-01/02/03 actuellement [ ] à transformer en [x])
    - .planning/ROADMAP.md (lignes 100-109 — Phase 7 + lignes 171-185 progress table)
    - .planning/STATE.md (intégralement)
    - .env.example (chercher la section OF_* — variables OF_NAME, OF_SIRET, etc.)
    - .planning/phases/07-param-tres-organisme-ditables/07-01-SUMMARY.md à 07-04-SUMMARY.md (sources de vérité pour résumés)
    - .planning/phases/06-dashboard-hierarchisation-et-a11y/06-04-PLAN.md (pattern bookkeeping Phase 6 — référence)
  </read_first>
  <action>
    Utiliser la date du jour pour les marquages (commande `date +%Y-%m-%d` pour récupérer la valeur). On notera cette date `<TODAY>` ci-dessous.

    1. **Mettre à jour `.planning/REQUIREMENTS.md`** (section 🟡 Paramètres organisme lignes 45-47) :
       - SET-01 : transformer `- [ ] **SET-01** : Édition SIRET / Déclaration d'activité / RCS / forme juridique dans Paramètres — page \`app/app/parametres/\` actuellement read-only.` en :
         `- [x] **SET-01** : Édition SIRET / Déclaration d'activité / RCS / forme juridique. **DONE <TODAY>** — migration Tenant (legalForm String?) + tenantIdentitySchema Zod (SIRET Luhn via isValidSiret) + Server Action updateTenantIdentity + AuditLog action='parameters.update' + UI section Identité avec inline edit + smoke test page. Plans 07-01/07-02/07-04.`
       - SET-02 : transformer en :
         `- [x] **SET-02** : Édition adresse + logo + mentions légales + signatures. **DONE <TODAY>** — migration Tenant (legalMentions @db.Text + logoPath + signaturePedagoPath + signatureDirigeantPath) + tenantAddressSchema + updateTenantAddress + tenant-assets.ts (uploadTenantLogo/uploadTenantSignature/resetTenantLogo/resetTenantSignature) + extension loadAssetDataUrl(filenames, tenantId?) avec invalidateAssetCache + cascade programme-template.ts/convention-template.ts (logoCache local supprimé) + UI sections Adresse & Logo&Signatures. Plans 07-01/07-02/07-03/07-04.`
       - SET-03 : transformer en :
         `- [x] **SET-03** : Préférences (préfixe factures, RIB, expéditeur SMTP). **DONE <TODAY>** — migration Tenant (invoicePrefix @default('FAC') + iban + bic + emailFrom) + tenantBillingSchema (IBAN FR + BIC regex, transform clean spaces) + tenantEmailSchema + updateTenantBilling/updateTenantEmail + lib/numbering.ts (getNextInvoiceNumber avec préfixe configurable, format {prefix}-NNNNNN 6 chiffres conservés) + UI sections Numérotation (AlertDialog discontinuité Pitfall 4)/RIB (formatIban helper)/Email. SMTP_PASSWORD reste en ENV (D-08 sécurité). Plans 07-01/07-02/07-04.`

    2. **Mettre à jour `.planning/ROADMAP.md`** :
       - Ligne 19 (overview phases) : `- [ ] **Phase 7: Paramètres organisme éditables**` → `- [x] **Phase 7: Paramètres organisme éditables**`
       - Lignes 100-109 (Phase 7 details) : remplacer le bloc `**Plans**: TBD` par :
         ```
         **Plans**:
         - [x] 07-01-PLAN.md — Migration Prisma Tenant (10 colonnes) + refactor of-config.ts async hybride BDD/ENV + cascade 12 callers + fix drift invoices.ts (const OF inline supprimé)
         - [x] 07-02-PLAN.md — Zod schemas (packages/shared/src/schemas/tenant.ts) + Server Actions tenant-settings (updateTenantIdentity/Address/Billing/Email) + helpers computeDiff/logTenantSettingsChange + lib/numbering.ts (getNextInvoiceNumber)
         - [x] 07-03-PLAN.md — tenant-assets.ts (upload/reset logo + 2 signatures) + extension loadAssetDataUrl(tenantId) + invalidateAssetCache + cascade programme/convention templates (logoCache local supprimé)
         - [x] 07-04-PLAN.md — UI Paramètres : page Server Component refactorée + 6 sections édition inline (SettingsSection + 6 form components) + helper formatIban + AlertDialog discontinuité préfixe + smoke test page
         - [x] 07-05-PLAN.md — Bookkeeping fin de phase (REQUIREMENTS/ROADMAP/STATE + smoke build/tests + commentaire .env.example fallback)
         ```
       - Table de progression (lignes ~171-185) : ligne Phase 7 → `| 7. Paramètres organisme éditables | 5/5 | Complete    | <TODAY> |`

    3. **Mettre à jour `.planning/STATE.md`** :
       - Frontmatter YAML :
         - `progress.completed_phases: 2` → `3`
         - `progress.completed_plans: 8` → `13` (8 + 5 plans Phase 7)
         - `stopped_at: "Completed 07-05-PLAN.md (Plan 07-05 — Bookkeeping fin de Phase 7). Prochaine étape : /gsd:plan-phase 8."`
         - `last_updated: "<ISO 8601 timestamp now via date -u +%Y-%m-%dT%H:%M:%S.000Z>"`
       - Section "## Current Position" : remplacer par :
         ```
         Phase: 8
         Plan: Not started
         ```
       - Section "### Roadmap Evolution" : ajouter une nouvelle ligne datée au-dessus de la dernière :
         ```
         - <TODAY> — Phase 7 closed : SET-01/02/03 livrés. 5 plans exécutés. (1) Plan 07-01 migration Tenant (legalForm/legalMentions/invoicePrefix/iban/bic/emailFrom/logoPath/signaturePedagoPath/signatureDirigeantPath/updatedAt) + refactor of-config.ts async hybride BDD/ENV + 12 callers migrés vers loadOfConfig + drift invoices.ts const OF supprimé. (2) Plan 07-02 4 schémas Zod tenant + 4 server actions tenant-settings + lib/numbering.ts. Premier usage AuditLog (action='parameters.update'). (3) Plan 07-03 tenant-assets.ts upload/reset + extension loadAssetDataUrl(tenantId) + invalidateAssetCache + cascade programme/convention (logoCache local supprimé). (4) Plan 07-04 page parametres refactorée + 6 sections éditables inline (SettingsSection + 6 forms) + formatIban + AlertDialog discontinuité préfixe. (5) Plan 07-05 bookkeeping + smoke.
         ```
       - Section "### Key Decisions Recorded" : ajouter (datés) :
         ```
         - <TODAY> — Stratégie hybride BDD ⤳ ENV pour OfConfig (D-01) : loadOfConfig(tenantId) lit BDD avec fallback ENV per-champ. ENV vars OF_* conservées comme fallback secondaire (.env.example commenté Phase 7).
         - <TODAY> — AuditLog : convention entity='Tenant' + actions namespacées (parameters.update, parameters.upload.logo, parameters.upload.signature.{pedago|dirigeant}, parameters.reset.*). À étendre Phase 8 RBAC pour UI consultation.
         - <TODAY> — legalForm gardé String libre (D-02 CONTEXT.md) — enum Prisma LegalForm existante mais migration vers Select reportée si Laurent demande explicitement.
         - <TODAY> — Numérotation factures : helper getNextInvoiceNumber extrait dans lib/numbering.ts. Format {prefix}-NNNNNN (6 chiffres conservés vs CONTEXT.md D-06 qui indiquait 4 — code existant préservé, marge confortable).
         ```
       - Pied de page : `*Last updated: <TODAY> — Phase 7 closed (SET-01 + SET-02 + SET-03)*`
       - Section "## Last session" : `Stopped at: Completed 07-05-PLAN.md. Prochaine étape : /gsd:plan-phase 8.`

    4. **Mettre à jour `.env.example`** : trouver le bloc des variables OF_* (commencer par grep `OF_NAME=` dans le fichier). Insérer JUSTE AU-DESSUS de la première ligne `OF_NAME=...` ce commentaire (préfixer chaque ligne par `#`) :
       ```
       # ----------------------------------------------------------------------------
       # Organisme de formation (OF) — Identité, adresse, RIB, email
       # ----------------------------------------------------------------------------
       # Phase 7 (<TODAY>) : Ces variables OF_* sont desormais FALLBACK SECONDAIRE.
       #   Si vous avez renseigne les valeurs dans /app/parametres (UI), la BDD prend
       #   le pas. Les ENV ci-dessous restent utiles comme valeurs initiales avant
       #   la premiere sauvegarde UI (D-01 hybrid BDD-fallback-ENV).
       # ----------------------------------------------------------------------------
       ```
       NE PAS supprimer les variables existantes — c'est leur rôle de fallback.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -c "SET-01.*DONE" .planning/REQUIREMENTS.md && grep -c "SET-02.*DONE" .planning/REQUIREMENTS.md && grep -c "SET-03.*DONE" .planning/REQUIREMENTS.md && grep -c "07-01-PLAN.md" .planning/ROADMAP.md && grep -E "Phase 7.*5/5.*Complete" .planning/ROADMAP.md && grep -E "completed_phases:\s*3" .planning/STATE.md && grep -E "completed_plans:\s*13" .planning/STATE.md && grep -c "fallback" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SET-01.*DONE" .planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "SET-02.*DONE" .planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "SET-03.*DONE" .planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "07-01-PLAN.md" .planning/ROADMAP.md` ≥ 1
    - `grep -c "07-05-PLAN.md" .planning/ROADMAP.md` ≥ 1
    - `grep -E "Phase 7.*5/5.*Complete" .planning/ROADMAP.md` retourne au moins 1 ligne
    - `grep -E "completed_phases:\s*3" .planning/STATE.md` retourne 1 ligne
    - `grep -E "completed_plans:\s*13" .planning/STATE.md` retourne 1 ligne
    - `grep -c "Phase 7 closed" .planning/STATE.md` ≥ 1
    - `grep -c "fallback" .env.example` ≥ 1
    - `grep -c "Phase 7" .env.example` ≥ 1
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md marque SET-01/02/03 [x] DONE avec résumés. ROADMAP.md marque Phase 7 Complete + 5 plans [x] + progress table à jour. STATE.md frontmatter incrémenté (3 phases / 13 plans) + sections Roadmap Evolution et Key Decisions enrichies + Current Position pointe Phase 8. .env.example commenté pour expliquer le rôle fallback des OF_*. Le projet est prêt pour `/gsd:plan-phase 8`.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web build` exit code 0
- `pnpm test` exit code 0
- `.planning/REQUIREMENTS.md` : SET-01, SET-02, SET-03 marqués [x] DONE
- `.planning/ROADMAP.md` : Phase 7 [x] avec liste 5 plans + progress table 5/5 Complete
- `.planning/STATE.md` : completed_phases=3, completed_plans=13, Current Position = Phase 8
- `.env.example` : commentaire fallback ajouté au-dessus du bloc OF_*
- 0 régression drift OF / logoCache / nextInvoiceNumber local
</verification>

<success_criteria>
- Phase 7 documentée comme livrée dans REQUIREMENTS + ROADMAP + STATE
- Smoke build + tests verts
- `.env.example` commenté pour les futurs développeurs
- Le projet est prêt pour `/gsd:plan-phase 8` (Multi-utilisateurs + RBAC, qui dépend formellement de Phase 7)
- Aucune régression sur les patterns établis Phase 1-6
</success_criteria>

<output>
Créer `.planning/phases/07-param-tres-organisme-ditables/07-05-SUMMARY.md` documentant :
- Statut final Phase 7 (5/5 plans complete)
- Compte total tests Vitest verts (cumul nouveaux Phase 7)
- Lien vers les 4 SUMMARYs des plans précédents
- Patterns réutilisables exportés pour Phase 8 (logTenantSettingsChange, computeDiff, AuditLog convention)
- Prochaine étape : `/gsd:plan-phase 8` (Multi-utilisateurs + RBAC)
</output>
