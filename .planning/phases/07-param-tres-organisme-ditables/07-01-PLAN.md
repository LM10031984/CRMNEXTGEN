---
phase: 07-param-tres-organisme-ditables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/
  - apps/web/src/lib/of-config.ts
  - apps/web/src/lib/__tests__/of-config.test.ts
  - apps/web/src/server/actions/invoices.ts
  - apps/web/src/server/actions/agefice-generator.ts
  - apps/web/src/server/actions/convention-generator.ts
  - apps/web/src/server/actions/dossier-reminder.ts
  - apps/web/src/lib/of-paged-footer.ts
  - apps/web/src/lib/of-pdf-footer.ts
  - apps/web/src/lib/preinscription-reminder-template.ts
  - apps/web/src/lib/convention-template.ts
  - apps/web/src/lib/programme-template.ts
  - apps/web/src/lib/closure/certificat-template.ts
  - apps/web/src/lib/closure/attestation-template.ts
  - apps/web/src/lib/closure/shared-template.ts
  - apps/web/.gitignore
  - apps/web/public/.gitkeep
autonomous: true
requirements: [SET-01, SET-02, SET-03]

must_haves:
  truths:
    - "Tenant Prisma model expose les 10 nouvelles colonnes (legalForm/legalMentions/invoicePrefix/iban/bic/emailFrom/logoPath/signaturePedagoPath/signatureDirigeantPath/updatedAt)"
    - "loadOfConfig(tenantId) est async et lit BDD avec fallback ENV per-champ (D-01 hybride)"
    - "Les 12 call sites de getOfConfig() utilisent loadOfConfig(tenantId) ou reçoivent of en paramètre"
    - "invoices.ts n'a plus de const OF = { ...process.env.OF_* } local — utilise await loadOfConfig(tenantId)"
    - "grep -rn 'process.env.OF_' apps/web/src/ --exclude='of-config*' retourne vide"
    - "apps/web/public/of-assets/ est dans .gitignore"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Tenant model étendu"
      contains: "legalForm"
    - path: "packages/db/prisma/migrations/"
      provides: "Migration SQL phase-07-tenant-settings"
      contains: "ALTER TABLE"
    - path: "apps/web/src/lib/of-config.ts"
      provides: "loadOfConfig async + getOfConfig sync legacy + resolveOfConfig helper"
      exports: ["loadOfConfig", "getOfConfig", "OfConfig"]
    - path: "apps/web/src/lib/__tests__/of-config.test.ts"
      provides: "Tests loadOfConfig hybride BDD/ENV per-champ"
      min_lines: 30
    - path: "apps/web/public/.gitkeep"
      provides: "Matérialise public/ pour upload future"
  key_links:
    - from: "apps/web/src/server/actions/invoices.ts"
      to: "apps/web/src/lib/of-config.ts"
      via: "await loadOfConfig(user.tenantId)"
      pattern: "loadOfConfig\\(.*tenantId.*\\)"
    - from: "apps/web/src/lib/of-config.ts"
      to: "prisma.tenant"
      via: "prisma.tenant.findUnique"
      pattern: "prisma\\.tenant\\.findUnique"
---

<objective>
Bloquant Phase 7. Étendre le modèle Prisma Tenant avec 10 nouvelles colonnes (toutes nullables — D-01 hybrid), refactorer `lib/of-config.ts` en async avec stratégie BDD-fallback-ENV, et migrer en cascade les 13 fichiers consommateurs (12 call sites de getOfConfig + 1 drift à fixer dans invoices.ts).

Purpose: Sortir Paramètres du read-only nécessite que la source de vérité bascule de ENV-only vers BDD-d'abord. Le drift `invoices.ts` (objet OF re-déclaré localement bypass getOfConfig) doit être éliminé sinon l'édition future de l'IBAN sera du dead code sur les factures.

Output:
- Schema Prisma Tenant étendu (10 colonnes nullables sauf invoicePrefix par défaut "FAC")
- Migration SQL appliquée (`pnpm --filter @qualiof/db db:migrate dev --name phase-07-tenant-settings`)
- `lib/of-config.ts` exporte `loadOfConfig(tenantId): Promise<OfConfig>` + `getOfConfig(): OfConfig` legacy + helper pur `resolveOfConfig(tenant | null)`
- 12 call sites migrés vers pre-resolve pattern (Option B Finding 2): l'appelant fait `const of = await loadOfConfig(user.tenantId)` puis passe `of` au template
- `invoices.ts:11-22` const OF = { ... } supprimé, remplacé par `await loadOfConfig(tenantId)`
- `.gitignore` + `.gitkeep` pour matérialiser `apps/web/public/of-assets/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md
@.planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Contracts that downstream plans (07-02 / 07-03 / 07-04) will consume. -->

Tenant extended model (after migration):
```prisma
model Tenant {
  id                     String   @id @default(uuid())
  name                   String
  siret                  String?
  numDA                  String?
  rcs                    String?
  legalForm              String?  // ou enum LegalForm — voir Open Question 1
  legalMentions          String?  @db.Text
  address                Json?
  invoicePrefix          String?  @default("FAC")
  iban                   String?
  bic                    String?
  emailFrom              String?
  logoPath               String?
  signaturePedagoPath    String?
  signatureDirigeantPath String?
  users                  User[]
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
}
```

of-config.ts exports:
```ts
export type OfConfig = { name; siret; rnq; addressStreet; addressCp; addressCity; phone; email; tvaIntra; iban; bic; emailFrom; respNom; respTitre; respCivilite; respSignTitre; contactNom; contactEmail; contactPhone; ... };

export async function loadOfConfig(tenantId: string): Promise<OfConfig>;
export function getOfConfig(): OfConfig; // legacy: tous champs en fallback ENV
function resolveOfConfig(tenant: Tenant | null): OfConfig; // helper interne pur
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre Tenant model + migration Prisma</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/, apps/web/.gitignore, apps/web/public/.gitkeep</files>
  <read_first>
    - packages/db/prisma/schema.prisma (model Tenant L24-33 + model LegalForm enum L172-184 + model AuditLog L989-1004)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 1 — schema extension)
    - .planning/phases/07-param-tres-organisme-ditables/07-CONTEXT.md (D-01 hybrid BDD/ENV, D-06 invoicePrefix default FAC, D-07 IBAN/BIC, D-08 emailFrom)
    - apps/web/.gitignore (état actuel)
  </read_first>
  <behavior>
    - Test (manuel via prisma format) : `pnpm --filter @qualiof/db prisma format` réussit sans erreur
    - Test (manuel via prisma migrate) : `pnpm --filter @qualiof/db db:migrate dev --name phase-07-tenant-settings` génère un fichier migration sans casser les rows existantes
    - Test : `pnpm --filter @qualiof/db prisma migrate status` rapporte la migration appliquée
    - Test : `grep -E "legalForm|legalMentions|invoicePrefix|iban|bic|emailFrom|logoPath|signaturePedagoPath|signatureDirigeantPath" packages/db/prisma/schema.prisma | wc -l` ≥ 9
  </behavior>
  <action>
    1. **Étendre `model Tenant`** dans `packages/db/prisma/schema.prisma` (L24-33) en ajoutant exactement ces colonnes après `address Json?` :
       - `legalForm              String?` (decision deferred: enum vs string libre — voir Open Question 1, prendre String pour conformité D-02 CONTEXT.md, à raffiner Plan 07-02 si Laurent valide enum)
       - `legalMentions          String?  @db.Text`
       - `invoicePrefix          String?  @default("FAC")` (per D-06, seul champ avec @default)
       - `iban                   String?` (per D-07)
       - `bic                    String?` (per D-07)
       - `emailFrom              String?` (per D-08)
       - `logoPath               String?` (per D-04, ex "/of-assets/{tenantId}/logo.png")
       - `signaturePedagoPath    String?` (per D-05)
       - `signatureDirigeantPath String?` (per D-05)
       - Ajouter aussi `updatedAt              DateTime @updatedAt` (absent aujourd'hui, utile pour audit + cache-busting)
    2. **Run `pnpm --filter @qualiof/db prisma format`** pour vérifier la syntaxe.
    3. **Run `pnpm --filter @qualiof/db db:migrate dev --name phase-07-tenant-settings`** pour générer + appliquer la migration. Prisma créera un dossier `migrations/<timestamp>_phase_07_tenant_settings/migration.sql` avec ALTER TABLE.
    4. **Run `pnpm --filter @qualiof/db db:generate`** pour régénérer les types Prisma Client.
    5. **Ajouter dans `apps/web/.gitignore`** :
       ```
       # Phase 7 — uploads runtime tenant assets
       public/of-assets/
       ```
    6. **Créer `apps/web/public/.gitkeep`** (fichier vide, juste pour matérialiser le dossier en repo — pitfall 7 RESEARCH.md).
  </action>
  <verify>
    <automated>cd packages/db && pnpm prisma format && pnpm prisma migrate status && grep -E "(legalForm|legalMentions|invoicePrefix|iban|bic|emailFrom|logoPath|signaturePedagoPath|signatureDirigeantPath)" prisma/schema.prisma | wc -l | grep -E "^\s*9\s*$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "legalForm" packages/db/prisma/schema.prisma` ≥ 1
    - `grep -c "@db.Text" packages/db/prisma/schema.prisma` ≥ 1 (pour legalMentions)
    - `grep -c '@default("FAC")' packages/db/prisma/schema.prisma` ≥ 1 (pour invoicePrefix)
    - `grep -c "@updatedAt" packages/db/prisma/schema.prisma` ≥ 1 sur Tenant
    - `ls packages/db/prisma/migrations/ | grep phase_07_tenant_settings` retourne un dossier
    - `grep -c "public/of-assets/" apps/web/.gitignore` ≥ 1
    - `ls apps/web/public/.gitkeep` retourne le fichier
    - `pnpm --filter @qualiof/db prisma migrate status` ne signale aucune migration pending
  </acceptance_criteria>
  <done>
    Schema Prisma Tenant étendu avec les 10 colonnes (9 nouvelles + updatedAt). Migration SQL générée + appliquée localement. Types Prisma Client régénérés. `apps/web/public/of-assets/` git-ignored et dossier matérialisé via .gitkeep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor of-config.ts en async avec helper resolve pur + tests Vitest</name>
  <files>apps/web/src/lib/of-config.ts, apps/web/src/lib/__tests__/of-config.test.ts</files>
  <read_first>
    - apps/web/src/lib/of-config.ts (intégralement — code actuel sync ENV-only)
    - packages/db/prisma/schema.prisma (Tenant model post-Task 1)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 2 — option B pre-resolve + Code Examples loadOfConfig)
    - apps/web/src/lib/auth.ts (pattern validateRequest pour vérifier tenantId disponible côté caller)
    - packages/shared/src/helpers/__tests__/siret.test.ts (pattern Vitest référence)
  </read_first>
  <behavior>
    - Test 1: `resolveOfConfig(null)` retourne tous champs depuis process.env.OF_* (legacy mode)
    - Test 2: `resolveOfConfig(tenant)` retourne `tenant.siret` quand non-null, fallback `process.env.OF_SIRET` sinon
    - Test 3: `resolveOfConfig(tenant)` retourne `tenant.iban` quand non-null, fallback `process.env.OF_IBAN` sinon
    - Test 4: `resolveOfConfig(tenant)` retourne `tenant.emailFrom` quand non-null, fallback `process.env.OF_EMAIL` sinon
    - Test 5: `resolveOfConfig(tenant)` parse `tenant.address` Json correctement (street/cp/ville)
    - Test 6: `loadOfConfig(tenantId)` appelle prisma.tenant.findUnique avec le bon where.id (test avec prisma mock)
    - Test 7: `getOfConfig()` (legacy sync) délègue à `resolveOfConfig(null)` — backward compat
    - Test 8: Champs trim() — espaces vides traités comme null
  </behavior>
  <action>
    1. **Refactor `apps/web/src/lib/of-config.ts`** selon le pattern Finding 2 + Code Examples RESEARCH.md :
       - Conserver `type OfConfig` (export)
       - Ajouter `import { prisma } from '@qualiof/db'` (ou équivalent — vérifier le path d'import singleton)
       - Créer helper privé `function pick(bdd: string | null | undefined, envKey: string, fallback = ''): string` qui retourne `bdd.trim()` si non-vide sinon `process.env[envKey]?.trim() ?? fallback`
       - Créer `function resolveOfConfig(t: Tenant | null): OfConfig` qui :
         - Parse `t?.address` comme `Record<string, string> | null`
         - Pour chaque champ OfConfig, applique pick avec l'ENV correspondante
         - Mapping exact :
           * name → pick(t?.name, 'OF_NAME', 'Start Academy')
           * siret → pick(t?.siret, 'OF_SIRET')
           * rnq → pick(t?.numDA, 'OF_RNQ')
           * addressStreet → pick(address?.street, 'OF_ADDRESS_STREET')
           * addressCp → pick(address?.cp, 'OF_ADDRESS_CP')
           * addressCity → pick(address?.city, 'OF_ADDRESS_CITY')
           * phone → pick(null, 'OF_PHONE')  // pas en BDD encore
           * email → pick(null, 'OF_EMAIL')  // anciens champs ENV
           * tvaIntra → pick(null, 'OF_TVA_INTRA')
           * iban → pick(t?.iban, 'OF_IBAN')
           * bic → pick(t?.bic, 'OF_BIC')
           * emailFrom → pick(t?.emailFrom, 'OF_EMAIL')  // emailFrom utilisé par mailer pour le From: header
           * respNom / respTitre / respCivilite / respSignTitre → pick(null, 'OF_RESP_*')
           * contactNom / contactEmail / contactPhone → pick(null, 'OF_CONTACT_*')
       - Exporter `async function loadOfConfig(tenantId: string): Promise<OfConfig>` qui fait `const t = await prisma.tenant.findUnique({ where: { id: tenantId } }); return resolveOfConfig(t);`
       - Exporter `function getOfConfig(): OfConfig { return resolveOfConfig(null); }` pour backward-compat (utilisé par of-paged-footer.ts et tout call site sans tenantId)
    2. **Créer `apps/web/src/lib/__tests__/of-config.test.ts`** avec Vitest 2.1.8 :
       - Import `{ resolveOfConfig, getOfConfig }` from '../of-config'
       - Mock `process.env` per test via `vi.stubEnv('OF_SIRET', '12345678901234')`
       - 8 tests selon `behavior` ci-dessus
       - Pas besoin de mocker Prisma pour `resolveOfConfig` (helper pur) — mais test `loadOfConfig` peut être skip ou simulé via fixture
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- src/lib/__tests__/of-config.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export async function loadOfConfig" apps/web/src/lib/of-config.ts` retourne 1 ligne
    - `grep -E "export function getOfConfig" apps/web/src/lib/of-config.ts` retourne 1 ligne
    - `grep -E "function resolveOfConfig" apps/web/src/lib/of-config.ts` retourne 1 ligne
    - `grep -E "prisma\\.tenant\\.findUnique" apps/web/src/lib/of-config.ts` retourne au moins 1 ligne
    - `grep -E "Promise<OfConfig>" apps/web/src/lib/of-config.ts` retourne 1 ligne
    - `pnpm --filter @qualiof/web test -- src/lib/__tests__/of-config.test.ts --run` : 8 tests verts
    - `pnpm --filter @qualiof/web exec tsc --noEmit` ne signale aucune erreur sur of-config.ts
  </acceptance_criteria>
  <done>
    `of-config.ts` expose `loadOfConfig(tenantId)` async + `getOfConfig()` sync legacy + helper interne `resolveOfConfig`. Stratégie BDD-fallback-ENV implémentée field-by-field. 8 tests Vitest verts. Pas de régression typings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrer les 12 call sites de getOfConfig() vers loadOfConfig(tenantId) + supprimer drift invoices.ts</name>
  <files>apps/web/src/server/actions/invoices.ts, apps/web/src/server/actions/agefice-generator.ts, apps/web/src/server/actions/convention-generator.ts, apps/web/src/server/actions/dossier-reminder.ts, apps/web/src/lib/of-paged-footer.ts, apps/web/src/lib/preinscription-reminder-template.ts, apps/web/src/lib/convention-template.ts, apps/web/src/lib/programme-template.ts, apps/web/src/lib/of-pdf-footer.ts, apps/web/src/lib/closure/certificat-template.ts, apps/web/src/lib/closure/shared-template.ts, apps/web/src/lib/closure/attestation-template.ts</files>
  <read_first>
    - apps/web/src/lib/of-config.ts (post-Task 2)
    - apps/web/src/server/actions/invoices.ts (intégralement — lire L1-100 ET le const OF inline L11-22 ET la fonction createInvoice qui consomme OF)
    - apps/web/src/server/actions/agefice-generator.ts L1-180 (call site L173)
    - apps/web/src/server/actions/convention-generator.ts L1-120 (call site L79)
    - apps/web/src/server/actions/dossier-reminder.ts L1-120 (call site L93)
    - apps/web/src/lib/of-paged-footer.ts L1-80 (call site L65 — appelé sans contexte tenantId, garder getOfConfig sync legacy)
    - apps/web/src/lib/of-pdf-footer.ts L1-50 (call site L28 — idem)
    - apps/web/src/lib/preinscription-reminder-template.ts L1-60 (call site L36)
    - apps/web/src/lib/convention-template.ts L1-200 (call site L183 — template fn, doit recevoir `of` en paramètre data)
    - apps/web/src/lib/programme-template.ts L1-350 (call site L323 — template fn, doit recevoir `of`)
    - apps/web/src/lib/closure/certificat-template.ts L1-60 (call site L25 — template fn)
    - apps/web/src/lib/closure/attestation-template.ts L1-60 (call site L25 — template fn)
    - apps/web/src/lib/closure/shared-template.ts L1-100 + L420-520 (call sites L425, L511 — helpers consommés par d'autres templates)
    - .planning/phases/07-param-tres-organisme-ditables/07-RESEARCH.md (Finding 2 — Option B Pre-resolve pattern, Pitfall 3 — drift invoices.ts, Risk Areas — cascade refactor)
  </read_first>
  <action>
    Stratégie **Pre-resolve Option B** (RESEARCH.md Finding 2) : les **Server Actions** (qui ont accès à `validateRequest()` donc `user.tenantId`) appellent `await loadOfConfig(user.tenantId)` et passent l'objet `of` aux templates en argument data. Les **templates** (fonctions pures de rendu HTML) ne font plus d'appel à `getOfConfig()` mais reçoivent `of: OfConfig` depuis l'appelant. Les **footers `of-paged-footer.ts` + `of-pdf-footer.ts`** sont appelés sans contexte tenantId (au boot/SSR) — ils continuent d'utiliser `getOfConfig()` sync legacy (fallback ENV).

    Étapes :

    1. **FIX DRIFT invoices.ts (CRITIQUE — Pitfall 3 RESEARCH.md)** : ouvrir `apps/web/src/server/actions/invoices.ts`.
       - **SUPPRIMER** intégralement les lignes 11-22 qui définissent `const OF = { iban: process.env.OF_IBAN ?? null, bic: ..., name: ..., siret: ..., address: ..., emailFrom: ..., phone: ... }`.
       - Dans chaque fonction qui consomme cet objet OF (typiquement `createInvoice`, fonctions de génération PDF), récupérer `user.tenantId` via `validateRequest()` (déjà fait), puis ajouter `const of = await loadOfConfig(user.tenantId);` au début de la fonction.
       - Remplacer toutes les références `OF.iban` → `of.iban`, `OF.bic` → `of.bic`, `OF.name` → `of.name`, `OF.siret` → `of.siret`, `OF.address` → `{ street: of.addressStreet, cp: of.addressCp, city: of.addressCity }`, `OF.emailFrom` → `of.emailFrom`, `OF.phone` → `of.phone`.
       - Ajouter `import { loadOfConfig } from '@/lib/of-config'` en haut.

    2. **agefice-generator.ts L173** : remplacer `const of = getOfConfig();` par `const of = await loadOfConfig(user.tenantId);`. Vérifier que `user.tenantId` est disponible dans le scope (validateRequest fait plus haut).

    3. **convention-generator.ts L79** : idem — `getOfConfig()` → `await loadOfConfig(user.tenantId)`.

    4. **dossier-reminder.ts L93** : idem — `getOfConfig()` → `await loadOfConfig(user.tenantId)`.

    5. **preinscription-reminder-template.ts L36** : si fonction async + a `tenantId` en paramètre data → utiliser `loadOfConfig(tenantId)`. Sinon (template fn pure) → recevoir `of` en paramètre `data: { ..., of: OfConfig }`. Vérifier qui appelle ce template et migrer l'appelant en conséquence.

    6. **convention-template.ts L183** : template fn pure — modifier signature pour recevoir `of: OfConfig` via data param. Supprimer l'appel à `getOfConfig()`. Mettre à jour les call sites (probablement `convention-generator.ts` qui passe maintenant `of` dans data).

    7. **programme-template.ts L323** : idem que convention-template — recevoir `of` via data param. Mettre à jour les call sites.

    8. **certificat-template.ts L25 + attestation-template.ts L25** : idem — recevoir `of` via data param. Mettre à jour les call sites dans le worker/server actions closure.

    9. **shared-template.ts L425 + L511** : ces helpers (probablement renderHeader/renderFooter) doivent recevoir `of` en paramètre au lieu d'appeler `getOfConfig()`. Mettre à jour les wrappers (`renderHtmlPage`, etc.) pour le propager.

    10. **of-paged-footer.ts L65 + of-pdf-footer.ts L28** : **GARDER `getOfConfig()`** (sync legacy). Ces fonctions sont appelées sans contexte tenantId (au boot / depuis `renderInvoiceHtml` en dehors d'une server action). C'est intentionnel — `getOfConfig()` retourne le fallback ENV pur (resolveOfConfig(null)), ce qui est OK comme défaut. **NOTE: une future Phase pourrait propager tenantId jusqu'aux footers, mais Phase 7 ne le requiert pas (l'IBAN/BIC remontent par `of` dans le BODY de la facture, le footer n'a que les coordonnées OF générales).**

    11. **GREP REGRESSION** : exécuter `grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config"`. Le résultat **DOIT être vide** — sinon il reste du drift à fixer.

    12. **SMOKE** : `pnpm --filter @qualiof/web exec tsc --noEmit` doit passer sans erreur. `pnpm --filter @qualiof/web build` doit compiler.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && bash -c 'set -e; LEAK=$(grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config" || true); if [ -n "$LEAK" ]; then echo "DRIFT REMAINS:"; echo "$LEAK"; exit 1; fi; pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | head -40'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config"` retourne 0 ligne (vide)
    - `grep -c "const OF = {" apps/web/src/server/actions/invoices.ts` retourne 0
    - `grep -c "loadOfConfig" apps/web/src/server/actions/invoices.ts` ≥ 1
    - `grep -c "loadOfConfig" apps/web/src/server/actions/agefice-generator.ts` ≥ 1
    - `grep -c "loadOfConfig" apps/web/src/server/actions/convention-generator.ts` ≥ 1
    - `grep -c "loadOfConfig" apps/web/src/server/actions/dossier-reminder.ts` ≥ 1
    - `grep -c "getOfConfig\\|loadOfConfig" apps/web/src/lib/of-paged-footer.ts` ≥ 1 (garde sync legacy)
    - `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
    - `pnpm --filter @qualiof/web build` : compile sans erreur
  </acceptance_criteria>
  <done>
    Les 12 call sites historiques de `getOfConfig()` sont migrés (server actions → loadOfConfig async, templates → reçoivent of en paramètre data, footers → restent sync legacy). Drift `invoices.ts` const OF supprimé. Aucun `process.env.OF_*` direct hors de `of-config.ts`. Build TS clean.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/db prisma migrate status` : migration `phase_07_tenant_settings` appliquée
- `pnpm --filter @qualiof/web test -- src/lib/__tests__/of-config.test.ts --run` : 8 tests verts
- `grep -rn "process.env.OF_" apps/web/src/ --include="*.ts" | grep -v "of-config"` : vide (preuve drift fixé)
- `pnpm --filter @qualiof/web exec tsc --noEmit` : 0 erreur
- `pnpm --filter @qualiof/web build` : compile complet
</verification>

<success_criteria>
- Schema Prisma Tenant étendu (9 nouvelles colonnes + updatedAt) + migration appliquée
- `loadOfConfig(tenantId): Promise<OfConfig>` exporté, lit BDD avec fallback ENV per-champ
- 12 call sites migrés + drift `invoices.ts` éliminé
- 0 `process.env.OF_*` direct hors `of-config.ts`
- `apps/web/public/of-assets/` gitignored + `apps/web/public/.gitkeep` matérialise le dossier
- Tests Vitest of-config verts (8 cas couvrent BDD/ENV hybrid per champ)
- Build TS clean
</success_criteria>

<output>
Créer `.planning/phases/07-param-tres-organisme-ditables/07-01-SUMMARY.md` documentant :
- Liste exacte des 10 colonnes ajoutées au Tenant (avec types + nullability)
- Lien vers la migration SQL générée
- Liste exacte des fichiers migrés vers `loadOfConfig` (12 + invoices.ts)
- Preuve grep `process.env.OF_` clean
- Decisions techniques retenues (legalForm String vs enum, pre-resolve Option B vs autres)
</output>
