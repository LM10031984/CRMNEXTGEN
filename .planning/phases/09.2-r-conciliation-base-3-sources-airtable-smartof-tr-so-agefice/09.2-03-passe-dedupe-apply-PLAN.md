---
phase: 09.2
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified: []
autonomous: false
requirements: [E2-dedupe]
must_haves:
  truths:
    - "Un pg_dump est pris AVANT l'apply dedupe (snapshot horodate)"
    - "Le dry-run dedupe est revu par un humain (diff) avant l'apply (checkpoint bloquant)"
    - "Les doublons sont fusionnes avec les garde-fous : email jamais cle de fusion seule (Nestenn), SIRET partages noms differents NON auto-merges (-> reliquat), 48 persons sans email non discriminees -> reliquat"
    - "Apres apply, aucun ExternalIdentity orphelin : tout pointe vers une entite existante (rejeu idempotent ne recree pas le doublon)"
    - "La suite complete reste verte"
    - "Airtable NON rejoue (justifie) : le snapshot nov2025 est deja en base (267 LegalLinks + ExternalIdentity 21 Org/216 Person) ; le repointage dedupe preserve les casquettes/OPCO sans re-import (RECONCILE-RULES 0.1)"
  artifacts: []
  key_links:
    - from: "dedupe.ts --apply"
      to: "ExternalIdentity / SessionParticipant / LegalLink / AgeficeProfile"
      via: "repointage FK transactionnel perdant->survivant"
      pattern: "externalIdentity"
---

<objective>
E2 etape 1 — Executer la passe de dedoublonnage des referentiels (Organizations puis Persons) via apps/web/scripts/dedupe.ts, avec le protocole --apply strict : dry-run -> revue de diff humaine -> pg_dump -> --apply. Cette etape mute la BDD de prod-LOCALE Start Academy : elle exige une revue humaine du diff avant tout apply (checkpoint bloquant), conformement aux garde-fous RECONCILE-RULES section 7.

Purpose: Premiere passe ordonnee. Le rejeu SmartOF (Plan 04) est idempotent via ExternalIdentity : si des doublons subsistent, le rejeu les recree. Donc on dedupe AVANT. Les garde-fous (section 3.2) sont deja codes dans dedupe.ts (verifies en RESEARCH) et l'invariant ExternalIdentity est prouve par dedupe.merge.test.ts (Plan 01).
Output: referentiels dedupliques, doublons ambigus routes vers le reliquat (traites en Plan 07), snapshot pg_dump conserve.

OMISSION TRACEE (exigence RECONCILE-RULES section 0) — import-airtable.ts N'EST PAS rejoue dans cette phase. Le snapshot Airtable (fige ~10/11/2025) est DEJA applique en base : 267 LegalLinks (casquettes EI/SALARIE/AGENT_COMMERCIAL + rattachement OPCO) + ExternalIdentity airtable (21 Org / 216 Person) existent (photo E0). C'est PRECISEMENT cette passe de dedupe qui PRESERVE cette autorite Airtable : le repointage FK perdant->survivant (invariant section 3.2, prouve par dedupe.merge.test.ts au Plan 01) deplace les LegalLink/ExternalIdentity Airtable vers les survivants sans les perdre. Rejouer import-airtable.ts serait un no-op au mieux, une regression au pire (le snapshot pointe vers des IDs d'avant fusion). Detail complet : RECONCILE-RULES section 0.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md

<interfaces>
apps/web/scripts/dedupe.ts (apres refacto Plan 01 — exports + tx, mecanisme inchange) :
- Dry-run : pnpm --filter @qualiof/web exec tsx scripts/dedupe.ts
- Apply   : pnpm --filter @qualiof/web exec tsx scripts/dedupe.ts --apply
- Exclusion par nom : --skip=pattern1,pattern2
- Detections : detectOrgsBySiret (SIRET commun + noms ressemblants seulement ; SIRET partage noms differents => ECARTE de l'auto-merge -> reliquat), detectEiSelfOrgsPerPerson, detectPersonsByName (garde-fou email 385-388 : SKIP si tous candidats emails distincts non-null).
- Choix canonique : SmartOF prioritaire (scoreOrg/scorePerson).
- Repointage : LegalLink, SessionParticipant(personId+sponsorOrgId), SessionTrainer, Invoice.payerOrgId, AgeficeProfile, SensitiveData, ExternalIdentity, Document, Contact, Lead, InternalComment. delete dup.
- LIMITE connue (RESEARCH) : ne repointe PAS OpcoSubmission/Task/BillingProfile -> a verifier au cas par cas en reliquat (Plan 07).

Photo E0 (RECONCILE 1) : Doublons SIRET = 4 SIRET -> 8 orgs ; doublons email = 6 emails -> 16 persons (dont Nestenn saintraphael@nestenn.com = 6 personnes DISTINCTES a NE PAS fusionner). 48 Person sans email.
Backup baseline : "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-E0-baseline-2026-06-10.sql.gz".
Container Postgres : qualiof_postgres (user qualiof, db qualiof).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run dedupe + pg_dump pre-apply</name>
  <read_first>
    - apps/web/scripts/dedupe.ts (detections + garde-fous)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 3.2 garde-fous + section 7 protocole)
  </read_first>
  <action>
    1. Verifier que dedupe.merge.test.ts est vert (prerequis dur) : pnpm --filter @qualiof/web exec vitest run scripts/__tests__/dedupe.merge.test.ts. Si rouge -> STOP, ne pas continuer.
    2. Lancer le DRY-RUN : pnpm --filter @qualiof/web exec tsx scripts/dedupe.ts  (sans --apply). Capturer toute la sortie dans /tmp/dedupe-dryrun-09.2.txt.
    3. Verifier dans la sortie : (a) Nestenn / emails partages noms differents NE sont PAS proposes a la fusion ; (b) les SIRET partages entre noms differents sont ECARTES (pas dans la liste des merges auto) ; (c) le nombre de merges Org proposes et Person proposes est plausible (<= 8 orgs, <= 16 persons - les groupes Nestenn exclus).
    4. Prendre le pg_dump pre-apply (OBLIGATOIRE avant --apply, section 7a) :
       docker exec qualiof_postgres pg_dump -U qualiof -d qualiof | gzip > "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-pre-dedupe-$(date +%Y%m%d-%H%M%S).sql.gz"
       Verifier que le fichier .sql.gz est cree et non vide.
    NE PAS lancer --apply dans cette tache.
  </action>
  <verify>
    <automated>test -s /tmp/dedupe-dryrun-09.2.txt && ls -S "/Users/laurentmarx/Documents/CRM Next gen/backups/"qualiof-pre-dedupe-*.sql.gz 2>/dev/null | head -1 | xargs test -s && echo DRYRUN_AND_DUMP_OK</automated>
  </verify>
  <acceptance_criteria>
    - dedupe.merge.test.ts vert (prerequis confirme).
    - /tmp/dedupe-dryrun-09.2.txt non vide contient la liste des merges proposes.
    - Aucun groupe Nestenn / email-partage-noms-differents dans les merges proposes (verifie en lecture).
    - Un pg_dump qualiof-pre-dedupe-*.sql.gz existe et est non vide.
    - Aucune mutation effectuee (dry-run seulement).
  </acceptance_criteria>
  <done>Dry-run revu, garde-fous confirmes a l'oeil, snapshot pg_dump pris. Pret pour le checkpoint humain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Revue humaine du diff dedupe avant apply</name>
  <action>PAUSE pour revue humaine. Aucune execution automatique : presenter les elements ci-dessous (what-built) a l'humain, attendre le resume-signal avant de passer a la tache suivante. Ne lancer aucun --apply ni mutation tant que la validation n'est pas recue.</action>
  <what-built>Le dry-run de dedupe.ts a ete genere (/tmp/dedupe-dryrun-09.2.txt) et un pg_dump pre-apply a ete pris. Aucune donnee n'a encore ete modifiee.</what-built>
  <how-to-verify>
    1. Ouvrir /tmp/dedupe-dryrun-09.2.txt.
    2. Verifier la liste des fusions Organizations proposees (canonique <- doublons) : chaque fusion doit etre legitime (meme entite reelle). Les SIRET partages entre 2 enseignes de NOMS DIFFERENTS ne doivent PAS y figurer (ils vont en reliquat).
    3. Verifier la liste des fusions Persons : AUCUN groupe ne doit fusionner 2 personnes distinctes partageant une boite mail d'agence (cas Nestenn saintraphael@nestenn.com = 6 personnes). Si un tel groupe apparait, refuser et demander un --skip cible.
    4. Confirmer que les 48 personnes sans email non discriminees ne sont PAS fusionnees a l'aveugle.
  </how-to-verify>
  <resume-signal>Repondre "approved" pour autoriser l'apply, OU lister les patterns a exclure (--skip=...) / les groupes a refuser.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply dedupe (apres approbation) + verification idempotence ExternalIdentity</name>
  <read_first>
    - apps/web/scripts/dedupe.ts
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 7b une seule etape apply a la fois)
  </read_first>
  <action>
    UNIQUEMENT apres le "approved" du checkpoint. Si des --skip ont ete demandes, les appliquer.
    1. Lancer l'APPLY : pnpm --filter @qualiof/web exec tsx scripts/dedupe.ts --apply  (+ --skip=... si demande). Capturer la sortie dans /tmp/dedupe-apply-09.2.txt.
    2. Verifier l'idempotence ExternalIdentity post-apply (aucun orphelin) via SQL read-only :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) AS orphan_person FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Person' AND NOT EXISTS (SELECT 1 FROM \"Person\" p WHERE p.id = ei.\"entityId\");"
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) AS orphan_org FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Organization' AND NOT EXISTS (SELECT 1 FROM \"Organization\" o WHERE o.id = ei.\"entityId\");"
       Les DEUX comptes DOIVENT etre 0.
    3. VERIF AIRTABLE PRESERVE (omission tracee, RECONCILE 0.1) : compter les LegalLink avant/apres pour confirmer qu'aucun n'a ete perdu par la fusion (repointes, pas supprimes) :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) AS nb_legallink FROM \"LegalLink\";"  (attendu : >= 267, le repointage ne detruit aucun lien ; seules d'eventuelles paires devenues strictement identiques apres fusion peuvent etre dedupliquees par la contrainte unique LegalLink — a noter le cas echeant). Consigner avant/apres dans le SUMMARY.
    4. Re-lancer un DRY-RUN dedupe (sans apply) : il ne doit plus proposer les fusions deja faites (idempotence). Capturer dans /tmp/dedupe-dryrun-post-09.2.txt.
    5. Lancer la suite de tests : pnpm --filter @qualiof/web test (verte attendue).
    NE PAS enchainer sur un autre script --apply (le rejeu SmartOF est le Plan 04, separe — section 7b).
  </action>
  <verify>
    <automated>docker exec qualiof_postgres psql -U qualiof -d qualiof -t -c "SELECT (SELECT count(*) FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Person' AND NOT EXISTS (SELECT 1 FROM \"Person\" p WHERE p.id=ei.\"entityId\")) + (SELECT count(*) FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Organization' AND NOT EXISTS (SELECT 1 FROM \"Organization\" o WHERE o.id=ei.\"entityId\"));" | tr -d ' \n' | grep -qx 0 && echo NO_ORPHAN_EI</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/dedupe-apply-09.2.txt montre les fusions effectivement appliquees (transaction par dup).
    - orphan_person = 0 ET orphan_org = 0 (aucun ExternalIdentity orphelin -> invariant section 3.2 tenu en reel).
    - Le dry-run post-apply ne propose plus les fusions deja faites (idempotence).
    - pnpm --filter @qualiof/web test : suite verte.
    - Le pg_dump pre-apply (Task 1) est conserve (rollback possible).
    - Les cas ambigus (SIRET partages, sans-email non discrimines) non fusionnes -> a router en reliquat Plan 07.
  </acceptance_criteria>
  <done>Referentiels dedupliques en reel, 0 ExternalIdentity orphelin, idempotence confirmee, tests verts, snapshot conserve. Passe SmartOF (Plan 04) debloquee.</done>
</task>

</tasks>

<verification>
- pg_dump pre-apply pris et conserve (section 7a).
- Checkpoint humain franchi (revue diff) avant apply (section 7b).
- Post-apply : 0 ExternalIdentity orphelin (Person + Org).
- Dry-run post-apply idempotent.
- Suite de tests verte.
- UNE SEULE etape --apply dans ce plan (dedupe), pas de chainage (section 7b).
</verification>

<success_criteria>
- Dedoublonnage execute avec garde-fous (email jamais cle seule, SIRET ambigus + sans-email -> reliquat).
- Invariant ExternalIdentity tenu en reel (0 orphelin).
- Snapshot pg_dump conserve, checkpoint humain respecte.
- Plan 04 (rejeu SmartOF) debloque.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-03-SUMMARY.md (liste fusions appliquees, comptes orphelins, cas reliquat a reporter Plan 07, chemin du pg_dump).
</output>
