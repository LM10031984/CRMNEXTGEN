---
phase: 09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice
plan: 01
subsystem: testing
tags: [prisma, vitest, dedupe, integration-test, postgres, reconciliation]

requires:
  - phase: 09
    provides: "scripts/dedupe.ts (détection + fusion doublons Person/Organization)"
provides:
  - "Client Prisma généré (bloquant Wave 0 levé — scripts tsx bootent sans 'Cannot find module .prisma/client')"
  - "Base dédiée qualiof_test (45 tables) + TEST_DATABASE_URL (isolation totale prod-locale)"
  - "dedupe.ts testable : mergeOrgsTx/mergePersonsTx/detectPersonsByName exportés + tx/client injectable, mécanisme FK INCHANGÉ"
  - "dedupe.merge.test.ts vert — 1ère intégration BDD réelle du repo, prouve invariant ExternalIdentity §3.2"
affects: ["09.2-03-passe-dedupe-apply", "réconciliation", "tests intégration BDD"]

tech-stack:
  added: []
  patterns:
    - "Test intégration BDD réelle sur base _test dédiée (transaction rollback-only + filet afterAll)"
    - "Garde d'environnement dure : REFUS si base ≠ /_test$/ (jamais la prod-locale)"
    - "Test de puissance / mutation : casser le repointage → test rouge → restaurer"
    - "Client Prisma injectable (param défaut = singleton) pour testabilité sans toucher la prod"

key-files:
  created:
    - "apps/web/scripts/__tests__/dedupe.merge.test.ts"
  modified:
    - "apps/web/scripts/dedupe.ts (extraction mergeOrgsTx/mergePersonsTx + exports + garde main + detectPersonsByName client injectable)"
    - "apps/web/vitest.config.ts (include += scripts/**)"
    - "apps/web/package.json (test script += dotenv -e ../../.env)"

key-decisions:
  - "Client Prisma hoisté par pnpm dans node_modules/.pnpm/ (pas packages/db/node_modules/.prisma/client du plan) — vérif réelle = boot @qualiof/db OK, pas le chemin codé en dur"
  - "detectPersonsByName(tenantId, client=prisma) rendu injectable : garde-fou sécurité 'jamais toucher qualiof' > consigne 'ne pas toucher les détections' (changement additif, CLI inchangé)"
  - "vitest include + package.json test script étendus : nécessaires pour que le test soit découvert ET vert dans `pnpm test` (acceptance)"
  - "legalForm requis sur Organization (enum LegalForm) — non capté par grep scalaires, ajouté 'EI' dans les fixtures Test B"

patterns-established:
  - "Intégration BDD réelle : PrismaClient propre lié à TEST_DATABASE_URL, jamais le singleton @qualiof/db"
  - "Mutation testing manuel au gate pour prouver qu'un test n'est pas complaisant"

requirements-completed: [E2-dedupe-test, E2-refacto]

duration: ~35min
completed: 2026-06-10
---

# Phase 09.2 — Plan 01 : Prisma generate + dedupe.merge.test (Wave 0)

**Les deux bloquants durs de la réconciliation sont levés : client Prisma généré et invariant de fusion ExternalIdentity prouvé par une intégration BDD réelle sur base dédiée — la passe de dédoublonnage `--apply` (Plan 03) est débloquée.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-06-10
- **Tasks:** 3/3
- **Files modified:** 4 (1 créé, 3 modifiés) + 2 commits atomiques
- **Commits:** `53942ea` (refacto dedupe.ts), `a6783ae` (test + config)

## Accomplishments

### Task 1 — Preflight (aucune mutation prod-locale)
- `pnpm --filter @qualiof/db db:generate` OK. Boot test `@qualiof/db` : `BOOT_OK tenant_count=1` (le blocage "Cannot find module .prisma/client" est levé).
- Docker : qualiof_postgres + redis + minio + gotenberg + weasyprint tous Up/healthy.
- Base **qualiof_test** créée + schéma poussé (`prisma db push`, 45 tables) + `TEST_DATABASE_URL` ajoutée à `.env` (isolation totale de la prod-locale `qualiof`).
- Fichier Tréso canonique `Tréso Agefice (2).xlsx` (94798 o) et backup E0 `qualiof-E0-baseline-2026-06-10.sql.gz` (565 Ko) confirmés. `PREFLIGHT_OK`.

### Task 2 — Refacto testabilité dedupe.ts (mécanisme INCHANGÉ)
- Extraction `mergeOrgsTx(tx, canonical, dup)` / `mergePersonsTx(tx, canonical, dup)` (un duplicate par appel, dans un `tx` injecté).
- `mergeOrgs`/`mergePersons` réécrits pour boucler + `prisma.$transaction((tx) => …Tx(...))` — comportement CLI inchangé.
- Exports nommés : mergeOrgs, mergePersons, mergeOrgsTx, mergePersonsTx, detectOrgsBySiret, detectPersonsByName, normalize.
- Garde `main()` : `isMain = import.meta.url === pathToFileURL(process.argv[1]).href` → la détection complète ne s'exécute QUE lancé directement, jamais à l'import du test.
- Justification écrite en tête de fichier (trace §0/7c RECONCILE-RULES). `tsc` clean (hors redirect-308).

### Task 3 — dedupe.merge.test.ts (PRÉREQUIS DUR §3.2)
- 3 tests verts sur **qualiof_test** :
  - **Test A (Person)** : fusion en transaction rollback-only → `ExternalIdentity.entityId` repointe vers le survivant + Person perdant supprimé.
  - **Test B (Organization)** : symétrique, entityType 'Organization'.
  - **Test C (garde-fou email / Nestenn)** : 2 Person même nom + emails distincts non-null → `detectPersonsByName` ne les propose PAS comme groupe.
- **Garde d'environnement prouvée** : `TEST_DATABASE_URL` pointant `qualiof` → `throw REFUS:` AVANT toute opération.
- **Anti-résidu** : 0 ligne TESTA/TESTORG-09.2/TEST-TENANT-09.2 sur `qualiof` (prod-locale jamais touchée) ET sur `qualiof_test` (rollback + filet afterAll).
- **Test de puissance / mutation** ([[feedback_test_de_puissance_mutation]]) : repointage `externalIdentity.updateMany` (Person) commenté → **Test A rouge** (B/C restent verts, mutation ciblée) → restauré → 3 verts. Le test n'est pas complaisant.

## Verification

- `pnpm --filter @qualiof/web test` : **870/870 verts** (106 fichiers), aucune régression, `update-session-details.test.ts` intouché. (Baseline 09.3 = 858 ; +3 dedupe.merge + autres tests 09.3 du même branche.)
- `tsc --noEmit` apps/web : **0 erreur hors `redirect-308.test.ts`** (6 erreurs pré-existantes WIP `next.config.mjs` non committé — documentées deferred, hors scope).
- AUCUN script mutant lancé sur `qualiof`, AUCUN `--apply`.

## Déviations (toutes nécessaires, additives)

| # | Plan | Réel | Justification |
|---|------|------|---------------|
| D1 | verify `test -d packages/db/node_modules/.prisma/client` | client hoisté par pnpm dans `node_modules/.pnpm/...` | Layout pnpm ; vraie vérité (boot @qualiof/db OK) prouvée à la place |
| D2 | "ne pas toucher les détections" | `detectPersonsByName(tenantId, client=prisma)` injectable | Garde-fou sécurité "jamais toucher qualiof" prime ; additif, CLI inchangé |
| D3 | files_modified = dedupe.ts + test | + `vitest.config.ts` + `package.json` | Test sous `scripts/` (hors include `src/**`) + besoin de `TEST_DATABASE_URL` dans `pnpm test` — exigés par l'acceptance "test inclus et vert" |
| D4 | fixtures Org `legalName` seul | + `legalForm: 'EI'` | `legalForm` (enum) est requis sur Organization |

## Next

Plan 03 (passe dedupe `--apply`) débloqué : l'invariant ExternalIdentity §3.2 est prouvé. ⚠️ Plan 02 (Wave 0 suite : clé tréso + gate + sonde lieux + RECONCILE-RULES) reste à exécuter avant les passes `--apply`.
