---
phase: 09.2
plan: 05
type: execute
wave: 4
depends_on: ["04"]
files_modified: []
autonomous: false
requirements: [E2-prices-non-agefice]
must_haves:
  truths:
    - "Un pg_dump est pris AVANT l'apply sync-smartof-prices"
    - "Le dry-run sync-smartof-prices est revu par un humain avant l'apply (checkpoint bloquant)"
    - "Les prix hors-AGEFICE sont alignes sur SmartOF (presetTarification) ; les overrides manuels sont preserves"
    - "Les 52 SP a 336 EUR (bug AGEFICE) ne sont PAS touches par cette passe (SP.priceHT ecrase seulement si ==0) — leur correction est reservee a la passe Treso (Plan 06)"
    - "La suite complete reste verte"
  artifacts: []
  key_links:
    - from: "sync-smartof-prices.ts --apply"
      to: "TrainingProduct.priceHT / TrainingSession.pricePerLearner / SessionParticipant.priceHT"
      via: "ecrasement conditionnel preservant overrides"
      pattern: "priceHT"
---

<objective>
E2 etape 3 — Rejouer apps/web/scripts/sync-smartof-prices.ts pour les prix HORS-AGEFICE, avec protocole --apply strict. Cette passe preserve les overrides manuels et NE corrige PAS les 52 SP a 336 EUR (AGEFICE) : SP.priceHT n'est ecrase que si == 0 (RESEARCH inventaire 3), donc les 336 EUR (!= 0) sont laisses intacts pour la passe Treso (Plan 06).

Purpose: Source de verite prix section 2 : hors-AGEFICE = SmartOF presetTarification.tarifs[0].budget[0].prixUnitaireHT ; AGEFICE = Treso. Cette passe traite le hors-AGEFICE uniquement, dans l'ordre strict (avant Treso).
Output: prix hors-AGEFICE alignes sur SmartOF, overrides preserves, 336 EUR intacts (corriges en Plan 06), snapshot pg_dump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md

<interfaces>
apps/web/scripts/sync-smartof-prices.ts :
- Dry  : pnpm --filter @qualiof/web exec tsx scripts/sync-smartof-prices.ts
- Apply: pnpm --filter @qualiof/web exec tsx scripts/sync-smartof-prices.ts --apply
- Filtre 1 session : ... SES-0093
- Auth Firebase (lib/smartof/client). Source prix : produit.presetTarification.tarifs[0].budget[0].prixUnitaireHT.
- Regles d'ecrasement (preserve overrides) :
    TrainingProduct.priceHT : ecrase si != (145-151).
    TrainingSession.pricePerLearner : ecrase SEULEMENT si 0 OU = ancien product.priceHT (valeur derivee), sinon override garde (154-161).
    SessionParticipant.priceHT : ecrase SEULEMENT si == 0 (164-174). CONSEQUENCE : les 52 SP a 336 EUR (!= 0) NE sont PAS corriges ici -> Plan 06 (Treso) les ecrase inconditionnellement.
- Le script ne distingue pas explicitement AGEFICE/hors-AGEFICE par flag : la separation tient au fait que les SP AGEFICE bugges (336, != 0) ne sont pas touches (regle ==0) et que les prix produits AGEFICE seront re-corriges par la passe Treso. Verifier dans le dry-run que les SP AGEFICE a 336 n'apparaissent PAS comme modifies.
Container Postgres : qualiof_postgres (user qualiof, db qualiof).
Note : SP.priceHT en EUROS (Decimal), pas cents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run sync-smartof-prices + pg_dump pre-apply</name>
  <read_first>
    - apps/web/scripts/sync-smartof-prices.ts (regles override 145-174)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 2 prix hors-AGEFICE + section 7)
    - reference_smartof_formule_prix_2026_06_03 (formule prix confirmee)
  </read_first>
  <action>
    1. DRY-RUN : pnpm --filter @qualiof/web exec tsx scripts/sync-smartof-prices.ts. Capturer dans /tmp/sync-prices-dryrun-09.2.txt.
    2. Verifier dans le dry-run : (a) les SP a 336 EUR (AGEFICE bug) ne figurent PAS dans les modifications proposees (regle ecrase si ==0) ; (b) les overrides manuels (pricePerLearner != valeur derivee) sont marques "override garde" ; (c) les prix produits hors-AGEFICE qui different de SmartOF sont proposes a l'alignement.
    3. pg_dump pre-apply (section 7a) :
       docker exec qualiof_postgres pg_dump -U qualiof -d qualiof | gzip > "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-pre-syncprices-$(date +%Y%m%d-%H%M%S).sql.gz"
    NE PAS lancer --apply.
  </action>
  <verify>
    <automated>test -s /tmp/sync-prices-dryrun-09.2.txt && ls -S "/Users/laurentmarx/Documents/CRM Next gen/backups/"qualiof-pre-syncprices-*.sql.gz 2>/dev/null | head -1 | xargs test -s && echo DRYRUN_DUMP_OK</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/sync-prices-dryrun-09.2.txt non vide.
    - Aucun SP a 336 EUR dans les modifications proposees (verifie en lecture).
    - Overrides manuels marques preserves.
    - pg_dump qualiof-pre-syncprices-*.sql.gz non vide.
    - Aucune mutation (dry-run).
  </acceptance_criteria>
  <done>Dry-run revu, 336 EUR confirmes intacts, overrides preserves, snapshot pris. Pret pour checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Revue humaine du dry-run prix hors-AGEFICE avant apply</name>
  <action>PAUSE pour revue humaine. Aucune execution automatique : presenter les elements ci-dessous (what-built) a l'humain, attendre le resume-signal avant de passer a la tache suivante. Ne lancer aucun --apply ni mutation tant que la validation n'est pas recue.</action>
  <what-built>Dry-run sync-smartof-prices (/tmp/sync-prices-dryrun-09.2.txt) + pg_dump pre-apply. Aucune donnee modifiee.</what-built>
  <how-to-verify>
    1. Ouvrir /tmp/sync-prices-dryrun-09.2.txt.
    2. Confirmer que les SP a 336 EUR (AGEFICE) NE sont PAS dans la liste des modifications (ils seront corriges par la passe Treso au Plan 06, pas ici).
    3. Confirmer que les overrides manuels de prix de session ne sont pas ecrases (marques "override garde").
    4. Verifier que les alignements de prix produits hors-AGEFICE proposes sont coherents avec SmartOF.
  </how-to-verify>
  <resume-signal>Repondre "approved" pour autoriser l'apply, OU lister les sessions a exclure / anomalies a corriger.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply sync-smartof-prices (apres approbation) + verification 336 intacts</name>
  <read_first>
    - apps/web/scripts/sync-smartof-prices.ts
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 7b)
  </read_first>
  <action>
    UNIQUEMENT apres "approved".
    1. APPLY : pnpm --filter @qualiof/web exec tsx scripts/sync-smartof-prices.ts --apply. Capturer dans /tmp/sync-prices-apply-09.2.txt.
    2. Verifier que les 336 EUR AGEFICE sont TOUJOURS la (ils doivent etre corriges plus tard par Treso, pas ici) :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) AS sp_336 FROM \"SessionParticipant\" sp JOIN \"Organization\" o ON o.id=sp.\"sponsorOrgId\" WHERE o.\"opcoCode\"='AGEFICE' AND sp.\"priceHT\"=336;"
       Le compte doit etre STABLE (proche de 52, non augmente, non corrige par cette passe).
    3. Suite de tests : pnpm --filter @qualiof/web test (verte).
    NE PAS enchainer sur la passe Treso (Plan 06, separe — section 7b).
  </action>
  <verify>
    <automated>test -s /tmp/sync-prices-apply-09.2.txt && cd apps/web && pnpm --filter @qualiof/web test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/sync-prices-apply-09.2.txt montre les alignements de prix hors-AGEFICE effectues.
    - count sp_336 AGEFICE stable (non corrige par cette passe — reserve a Plan 06).
    - Overrides manuels intacts (verification ponctuelle sur 1 session a override connu).
    - pnpm --filter @qualiof/web test : verte.
    - pg_dump pre-apply conserve.
  </acceptance_criteria>
  <done>Prix hors-AGEFICE alignes sur SmartOF, overrides preserves, 336 EUR intacts (Plan 06 les corrigera), tests verts. Passe Treso (Plan 06) debloquee.</done>
</task>

</tasks>

<verification>
- pg_dump pre-apply pris (section 7a) ; checkpoint humain franchi (section 7b).
- Post-apply : prix hors-AGEFICE alignes, overrides preserves, 336 EUR AGEFICE intacts.
- Suite verte ; UNE seule etape --apply (section 7b).
</verification>

<success_criteria>
- Prix hors-AGEFICE = source SmartOF, overrides manuels preserves.
- 336 EUR AGEFICE non touches (correction reservee a la passe Treso).
- Plan 06 (Treso) debloque.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-05-SUMMARY.md (alignements prix, compte 336 stable, overrides preserves, pg_dump).
</output>
