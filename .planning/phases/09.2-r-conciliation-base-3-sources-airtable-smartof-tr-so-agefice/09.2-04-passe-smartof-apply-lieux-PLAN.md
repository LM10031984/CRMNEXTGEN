---
phase: 09.2
plan: 04
type: execute
wave: 3
depends_on: ["03", "02"]
files_modified: []
autonomous: false
requirements: [E2-smartof, E2-lieux]
must_haves:
  truths:
    - "Le sondage lieux (sonde-lieux-smartof.ts) a tranche backfillable vs reliquat pour les sessions sans locationId AVANT la passe"
    - "Un pg_dump est pris AVANT l'apply import-from-smartof"
    - "Le dry-run import-from-smartof est revu par un humain avant l'apply (checkpoint bloquant)"
    - "Identite/descriptif (SmartOF gagne section 2) + sessions + inscriptions rejoues ; aucun ExternalIdentity orphelin recree (dedupe deja passe)"
    - "Les sessions a custom_fields lieu peuples recoivent leur locationId ; les autres restent en reliquat manuel (non inventees)"
    - "La suite complete reste verte"
  artifacts: []
  key_links:
    - from: "import-from-smartof.ts --apply"
      to: "Organization/Person/TrainingProduct/TrainingSession/SessionParticipant/Location"
      via: "upsert par ExternalIdentity (idempotent)"
      pattern: "ExternalIdentity"
---

<objective>
E2 etape 2 — Rejouer apps/web/scripts/import-from-smartof.ts (identite/descriptif + sessions + inscriptions) avec protocole --apply strict, APRES le dedoublonnage (Plan 03). Inclut la decision lieu : utiliser sonde-lieux-smartof.ts (Plan 02) pour trancher quelles sessions sans locationId sont backfillables (custom_fields peuples) vs reliquat manuel (le dry-run import ne le revele PAS — RESEARCH Pitfall 3).

Purpose: SmartOF est la source de verite identite/descriptif (section 2). Le rejeu est idempotent via ExternalIdentity. Comme le dedupe est deja passe (Plan 03, 0 orphelin), le rejeu ne recree pas de doublon. Le backfill lieu n'est possible que si SmartOF a peuple les custom_fields — d'ou le sondage prealable.
Output: base alignee sur SmartOF (identite/sessions/inscriptions), lieux backfilles quand possible, reliquat lieu documente, snapshot pg_dump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md

<interfaces>
apps/web/scripts/import-from-smartof.ts :
- Dry  : pnpm --filter @qualiof/web exec tsx scripts/import-from-smartof.ts
- Apply: pnpm --filter @qualiof/web exec tsx scripts/import-from-smartof.ts --apply
- Scopes : --only=entreprises,apprenants,formateurs,produits,sessions,inscriptions (defaut = tous).
- Auth Firebase requise MEME en dry-run (lit l'API live) : SMARTOF_FIREBASE_API_KEY + SMARTOF_EMAIL + SMARTOF_PASSWORD (.env).
- Upsert par ExternalIdentity (idempotent), fallback SIRET puis legalName (orgs). legalForm=AUTRE a la creation seulement. Produits : REECRIT programMd + objectives sur update (objectifs concatenes). Sessions : status mappe, productId requis sinon skip. Inscriptions : match Person email->nom, sponsor DI -> fallback LegalLink.
- resolveSessionLocation (769-802) : custom_field_1=rue / 2=CP / 3=ville -> find-or-create Location "Ville — rue". Si custom_fields vide -> null -> session reste sans lieu. EN DRY-RUN renvoie null (ligne 789) -> le dry-run N'AFFICHE PAS les lieux backfillables (Pitfall 3).

apps/web/scripts/sonde-lieux-smartof.ts (Plan 02) : read-only, ecrit /tmp/sonde-lieux-smartof.csv avec verdict BACKFILLABLE/RELIQUAT_MANUEL par session sans lieu.
Container Postgres : qualiof_postgres (user qualiof, db qualiof).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sondage lieux + dry-run import-from-smartof + pg_dump pre-apply</name>
  <read_first>
    - apps/web/scripts/import-from-smartof.ts (scopes + resolveSessionLocation + auth)
    - apps/web/scripts/sonde-lieux-smartof.ts (Plan 02)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 1 lieux + section 2 golden-record SmartOF + section 7 protocole)
  </read_first>
  <action>
    1. Lancer le SONDAGE lieux (read-only) : pnpm --filter @qualiof/web exec tsx scripts/sonde-lieux-smartof.ts. Lire /tmp/sonde-lieux-smartof.csv. Noter : combien de sessions sans lieu sont BACKFILLABLE (custom_fields peuples) vs RELIQUAT_MANUEL, et combien sont closure-relevant (participants > 0). Si les creds SmartOF echouent, STOP et signaler (le sondage et l'import partagent les memes creds).
    2. Lancer le DRY-RUN import-from-smartof complet : pnpm --filter @qualiof/web exec tsx scripts/import-from-smartof.ts. Capturer dans /tmp/smartof-dryrun-09.2.txt. (Rappel Pitfall 3 : le dry-run NE liste PAS les lieux backfillables — c'est le sondage qui fait foi.)
    3. Verifier dans le dry-run : pas de creation massive de doublons Org/Person (le dedupe etant passe). Le nombre d'upserts est plausible vs la photo E0.
    4. pg_dump pre-apply (OBLIGATOIRE, section 7a) :
       docker exec qualiof_postgres pg_dump -U qualiof -d qualiof | gzip > "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-pre-smartof-$(date +%Y%m%d-%H%M%S).sql.gz"
    NE PAS lancer --apply ici.
  </action>
  <verify>
    <automated>test -s /tmp/sonde-lieux-smartof.csv && test -s /tmp/smartof-dryrun-09.2.txt && ls -S "/Users/laurentmarx/Documents/CRM Next gen/backups/"qualiof-pre-smartof-*.sql.gz 2>/dev/null | head -1 | xargs test -s && echo SONDE_DRYRUN_DUMP_OK</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/sonde-lieux-smartof.csv produit, verdict backfillable/reliquat par session sans lieu, comptage closure-relevant note.
    - /tmp/smartof-dryrun-09.2.txt non vide, sans explosion de doublons.
    - pg_dump qualiof-pre-smartof-*.sql.gz non vide.
    - Aucune mutation (dry-run + sondage seulement).
  </acceptance_criteria>
  <done>Lieux tranches (CSV), dry-run revu, snapshot pris. Pret pour le checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Revue humaine du dry-run SmartOF + decision lieux avant apply</name>
  <action>PAUSE pour revue humaine. Aucune execution automatique : presenter les elements ci-dessous (what-built) a l'humain, attendre le resume-signal avant de passer a la tache suivante. Ne lancer aucun --apply ni mutation tant que la validation n'est pas recue.</action>
  <what-built>Sondage lieux genere (/tmp/sonde-lieux-smartof.csv) + dry-run import-from-smartof (/tmp/smartof-dryrun-09.2.txt) + pg_dump pre-apply. Aucune donnee modifiee.</what-built>
  <how-to-verify>
    1. Ouvrir /tmp/sonde-lieux-smartof.csv : confirmer pour chaque session sans lieu si elle est backfillable (custom_fields SmartOF peuples) ou doit aller en reliquat manuel (Plan 07). Les lieux ne seront JAMAIS inventes — uniquement repris de SmartOF.
    2. Ouvrir /tmp/smartof-dryrun-09.2.txt : verifier qu'aucune creation massive de doublons Org/Person n'apparait (le dedupe vient de passer). Verifier que les produits/sessions/inscriptions mis a jour sont coherents.
    3. Rappel section 2 : SmartOF gagne sur identite/descriptif ; un vide ne doit jamais ecraser une valeur. Confirmer que le rejeu ne vide pas de champs deja renseignes.
  </how-to-verify>
  <resume-signal>Repondre "approved" pour autoriser l'apply (eventuellement avec --only=... pour limiter le scope), OU lister les anomalies a corriger d'abord.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply import-from-smartof (apres approbation) + verification lieux + idempotence</name>
  <read_first>
    - apps/web/scripts/import-from-smartof.ts
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 7b une seule etape apply)
  </read_first>
  <action>
    UNIQUEMENT apres "approved".
    1. APPLY : pnpm --filter @qualiof/web exec tsx scripts/import-from-smartof.ts --apply (+ --only=... si demande). Capturer dans /tmp/smartof-apply-09.2.txt.
    2. Verifier le backfill lieu : compter les sessions sans locationId AVANT (photo E0 = 22) vs APRES :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) AS sans_lieu FROM \"TrainingSession\" WHERE \"locationId\" IS NULL;"
       Le compte doit avoir DIMINUE du nombre de sessions backfillables identifiees au sondage (pas forcement 0 ; le reste = reliquat manuel Plan 07).
    3. Verifier 0 ExternalIdentity orphelin (le rejeu ne doit pas casser l'invariant) — meme requete que Plan 03 :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -t -c "SELECT (SELECT count(*) FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Person' AND NOT EXISTS (SELECT 1 FROM \"Person\" p WHERE p.id=ei.\"entityId\")) + (SELECT count(*) FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Organization' AND NOT EXISTS (SELECT 1 FROM \"Organization\" o WHERE o.id=ei.\"entityId\"));"
    4. Suite de tests : pnpm --filter @qualiof/web test (verte).
    NE PAS enchainer sur sync-smartof-prices (Plan 05, separe — section 7b).
  </action>
  <verify>
    <automated>docker exec qualiof_postgres psql -U qualiof -d qualiof -t -c "SELECT (SELECT count(*) FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Person' AND NOT EXISTS (SELECT 1 FROM \"Person\" p WHERE p.id=ei.\"entityId\")) + (SELECT count(*) FROM \"ExternalIdentity\" ei WHERE ei.\"entityType\"='Organization' AND NOT EXISTS (SELECT 1 FROM \"Organization\" o WHERE o.id=ei.\"entityId\"));" | tr -d ' \n' | grep -qx 0 && echo NO_ORPHAN_EI</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/smartof-apply-09.2.txt montre les upserts effectues.
    - count sessions sans locationId APRES < 22 (diminue du nombre de backfillables ; le reste = reliquat Plan 07).
    - 0 ExternalIdentity orphelin (Person + Org).
    - Aucun champ renseigne vide par le rejeu (verification ponctuelle sur 1-2 orgs/persons connues vs valeurs SmartOF).
    - pnpm --filter @qualiof/web test : verte.
    - pg_dump pre-apply conserve.
  </acceptance_criteria>
  <done>SmartOF rejoue (identite/sessions/inscriptions), lieux backfilles quand possible, 0 orphelin, tests verts. Passe prix hors-AGEFICE (Plan 05) debloquee.</done>
</task>

</tasks>

<verification>
- Sondage lieux execute avant la passe ; reliquat lieu documente.
- pg_dump pre-apply pris (section 7a) ; checkpoint humain franchi (section 7b).
- Post-apply : sessions sans lieu diminuees, 0 ExternalIdentity orphelin, aucun champ ecrase par un vide.
- Suite verte ; UNE seule etape --apply (section 7b).
</verification>

<success_criteria>
- Base alignee sur SmartOF (identite/descriptif/sessions/inscriptions), idempotence preservee.
- Lieux backfilles quand custom_fields peuples ; reste en reliquat (jamais inventes).
- Plan 05 (sync prix hors-AGEFICE) debloque.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-04-SUMMARY.md (upserts, backfill lieu avant/apres, reliquat lieu, orphelins, pg_dump).
</output>
