---
phase: 09.2
plan: 06
type: execute
wave: 5
depends_on: ["05", "02"]
files_modified: []
autonomous: false
requirements: [E2-treso, E2-prix-bug]
must_haves:
  truths:
    - "Le bug prix 336 EUR x52 AGEFICE est corrige par la passe Treso (priceHT = montant Treso / nb SP matches, ecrasement inconditionnel si >0)"
    - "VERIFY = la VRAIE propriete : zero SP AUTO-rapproche divergent de sa ligne Treso (post-apply, re-dry-run AUTO ne propose plus aucun changement de prix). Un SP a 336 sur formation COURTE dont la ligne Treso confirme 336 est CORRECT et reste. Les SP non rapproches (AMBIGU/NO_MATCH) NE sont PAS force-corriges -> reliquat. La chute du compteur 336 est une CONSEQUENCE probable (si les 52 sont tous sur formations longues), PAS le critere — c'est au Treso de le dire, pas au compteur (reframe Plan 05 : 336/8h legitime)"
    - "La passe Treso utilise match-treso-agefice.ts (jeu de booleens CANONIQUE lu par l'UI), PAS import-treso-agefice.ts (jeu parallele invisible)"
    - "La cle Treso corrigee (Plan 02 : montant en departage, nb stagiaires en entree) est en vigueur ; aucun no-match silencieux (CSV reliquat)"
    - "Les 4 booleens canoniques sont OR-merges false->true uniquement ; les conflits descendants restent en base et sont a router en reliquat (Plan 07)"
    - "Le formateur par session est backfille depuis Treso sans ecraser un formateur saisi manuellement"
    - "DEUX applies (match-treso puis backfill-trainer) separes par un checkpoint et un pg_dump chacun"
  artifacts: []
  key_links:
    - from: "match-treso-agefice.ts --apply"
      to: "SessionParticipant.priceHT + 4 booleens canoniques"
      via: "ecrasement prix + OR-merge booleens"
      pattern: "factureEnvoyee"
    - from: "backfill-trainer-from-treso.ts --apply"
      to: "SessionTrainer"
      via: "creation LEAD si session sans trainer, skip si deja"
      pattern: "SessionTrainer"
---

<objective>
E2 etape 4 — La passe Treso AGEFICE, chantier MAJEUR : corriger le prix reel encaisse (bug 336 EUR x52) et les 4 booleens d'encaissement via match-treso-agefice.ts (cle corrigee en Plan 02), PUIS backfiller le formateur par session via backfill-trainer-from-treso.ts. DEUX applies distincts, chacun precede d'un dry-run + pg_dump + checkpoint humain (section 7b : jamais deux applies enchaines).

Purpose: Treso = source de verite prix/encaissement AGEFICE (section 2). C'est la passe qui repare la base partiellement fausse (Sigma SP.priceHT). On utilise match-treso-agefice.ts (booleens canoniques + prix), PAS import-treso-agefice.ts qui ecrit un jeu de booleens invisible dans l'UI (RESEARCH Pitfall 1).
Output: prix AGEFICE corriges, booleens canoniques OR-merges, formateurs backfilles, no-match -> reliquat, conflits descendants -> reliquat (Plan 07), 2 snapshots pg_dump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md

<interfaces>
apps/web/scripts/match-treso-agefice.ts (cle CORRIGEE en Plan 02) :
- Dry  : pnpm --filter @qualiof/web exec tsx scripts/match-treso-agefice.ts "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx"
- Apply: ... "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx" --apply
  (chemin xlsx = ARG POSITIONNEL obligatoire ; canonique section 5.1 au PARENT de /files)
- APPLY mute (329-409) : priceHT = montant / nb SP matches (ecrasement INCONDITIONNEL si >0 -> corrige 336) + 4 booleens CANONIQUES factureEnvoyee/validationOpco/remboursementOpco/paiementClient en OR-merge false->true uniquement.
- CSV verdict : /tmp/treso-matching.csv (verdict/score/session/runner-up/4 booleens).

packages/db/scripts/backfill-trainer-from-treso.ts :
- Dry  : pnpm --filter @qualiof/db backfill:trainer-treso
- Apply: pnpm --filter @qualiof/db backfill:trainer-treso -- --apply
- Source = "Tréso Agefice (2).xlsx" resolu via ROOT (= parent de /files, deja confirme present). Match session date +-5j + nom formation ; Person par nom strict. Cree SessionTrainer{role:LEAD,isPrimary:true} si session sans trainer ; promeut/ajoute CO sinon. IDEMPOTENT (skip si Person deja trainer). Ne jamais ecraser un formateur saisi.

NE PAS utiliser packages/db/scripts/import-treso-agefice.ts : il ecrit invoiceSent/opcoApproved/opcoReimbursed/paymentReceived (jeu PARALLELE non lu par l'UI) -> pastilles grises (RESEARCH Pitfall 1).
Conflit descendant (section 2) : base true / Treso false -> OR-merge laisse true (pas d'arbitrage auto) -> reliquat Plan 07.
Container Postgres : qualiof_postgres (user qualiof, db qualiof).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run match-treso (cle corrigee) + pg_dump pre-apply prix/booleens</name>
  <read_first>
    - apps/web/scripts/match-treso-agefice.ts (APRES correction Plan 02 : scoring section 4, apply 329-409)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 2 + section 4 + section 7)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (Pitfall 1 + Pitfall 2 + inventaire script 4)
  </read_first>
  <action>
    1. Confirmer la correction Plan 02 : grep "nbStagiaires" dans match-treso-agefice.ts ET vitest run scripts/__tests__/match-treso-scoring.test.ts vert. Si KO -> STOP.
    2. DRY-RUN : pnpm --filter @qualiof/web exec tsx scripts/match-treso-agefice.ts "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx". Capturer la sortie ; verifier /tmp/treso-matching.csv genere.
    3. Lire /tmp/treso-matching.csv : compter AUTO / AMBIGU / NO_MATCH. Verifier que les NO_MATCH sont explicites (aucune ligne Treso silencieusement ignoree). Sur les sessions a 336 EUR : verifier qu'elles matchent sur noms/dates (pas montant) et que le montant Treso propose != 336.
    3bis. EXIGENCE CSV — colonne `niveau_cle` (REPORTING SEUL, matching INCHANGE) : ajouter au CSV de match-treso-agefice.ts une colonne indiquant, pour chaque rapprochement, le NIVEAU DE CLE utilise : `CLE_PLEINE` (noms + dates + nb stagiaires concordants, score eleve) / `DEGRADE_DATES_ORG` (dates+org seuls, nb stagiaires vide/divergent) / `DEPARTAGE_MONTANT` (plusieurs candidats ex aequo departages par le montant). Derive de la mecanique scoreTresoRow/matchTresoRows (Plan 02) — meme reflexe que la colonne `confiance` ajoutee a backfill-trainer (reporting pur, on ne touche PAS le matching, garde-fou Laurent). But : la revue ligne a ligne (checkpoint) est PROPORTIONNEE — CLE_PLEINE survole, DEGRADE + DEPARTAGE_MONTANT verifies un a un contre l'Excel (c'est de la que sortent les faux positifs §7-f).
    4. pg_dump pre-apply (section 7a) :
       docker exec qualiof_postgres pg_dump -U qualiof -d qualiof | gzip > "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-pre-treso-match-$(date +%Y%m%d-%H%M%S).sql.gz"
    NE PAS --apply.
  </action>
  <verify>
    <automated>test -s /tmp/treso-matching.csv && grep -q "nbStagiaires" apps/web/scripts/match-treso-agefice.ts && ls -S "/Users/laurentmarx/Documents/CRM Next gen/backups/"qualiof-pre-treso-match-*.sql.gz 2>/dev/null | head -1 | xargs test -s && echo TRESO_MATCH_DRYRUN_OK</automated>
  </verify>
  <acceptance_criteria>
    - match-treso-agefice.ts contient la correction Plan 02 (grep nbStagiaires) et match-treso-scoring.test.ts vert.
    - /tmp/treso-matching.csv non vide, NO_MATCH explicites, comptage AUTO/AMBIGU/NO_MATCH note.
    - Sessions a 336 EUR : match sur noms/dates, montant Treso propose != 336.
    - pg_dump qualiof-pre-treso-match-*.sql.gz non vide.
    - Aucune mutation (dry-run).
  </acceptance_criteria>
  <done>Dry-run Treso revu (cle corrigee), CSV reliquat genere, snapshot pris. Pret pour checkpoint 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Revue humaine du dry-run match-treso (prix/booleens) avant apply</name>
  <action>PAUSE pour revue humaine. Aucune execution automatique : presenter les elements ci-dessous (what-built) a l'humain, attendre le resume-signal avant de passer a la tache suivante. Ne lancer aucun --apply ni mutation tant que la validation n'est pas recue.</action>
  <what-built>Dry-run match-treso-agefice (cle corrigee Plan 02) + /tmp/treso-matching.csv + pg_dump pre-apply prix/booleens. Aucune donnee modifiee.</what-built>
  <how-to-verify>
    1. Ouvrir /tmp/treso-matching.csv. Revue **ligne a ligne PROPORTIONNEE a la colonne `niveau_cle`** (§7-f) : `CLE_PLEINE` = survol ; `DEGRADE_DATES_ORG` et `DEPARTAGE_MONTANT` = verifier UN A UN contre l'Excel Treso (les 2 faux positifs connus — Franck SERVANT, SES-0061 — sortent precisement de ces 2 mecaniques). Pour chaque AUTO : le rapprochement session<->ligne Treso est-il correct ? Le montant Treso remplace-t-il un 336 errone par un montant plausible (ex 3024 sur formation longue) ?
    2. Verifier les AMBIGU et NO_MATCH : candidats du reliquat E3 (Plan 07). Confirmer qu'aucune ligne Treso n'est silencieusement ignoree. **Un 336 sur formation courte (8h) confirme par la Treso est LEGITIME et doit rester** (reframe Plan 05 : 336/8h = prix-jour) — ne PAS le "corriger".
    3. Verifier que les 4 booleens proposes (OR-merge false->true) sont coherents. Rappel : les conflits descendants (base true / Treso false) ne sont PAS corriges ici (audit separe Plan 07).
  </how-to-verify>
  <resume-signal>Repondre "approved" pour autoriser l'apply prix/booleens, OU lister les matches a refuser / ambigus a basculer en reliquat.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply match-treso (prix + booleens) + verification correction 336</name>
  <read_first>
    - apps/web/scripts/match-treso-agefice.ts (apply 329-409)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 7b)
  </read_first>
  <action>
    UNIQUEMENT apres "approved".
    1. APPLY : pnpm --filter @qualiof/web exec tsx scripts/match-treso-agefice.ts "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx" --apply. Capturer dans /tmp/treso-match-apply-09.2.txt.
    2. Verifier la VRAIE propriete (PAS un compteur 336 aveugle — cf. reframe Plan 05) : RE-LANCER le dry-run match-treso post-apply et asserter que, pour les lignes AUTO, AUCUN changement de prix n'est plus propose = tous les SP rapproches portent deja leur prix Treso (idempotence sur les AUTO = zero divergence rapprochee). Puis CARACTERISER les SP residuels a 336 un a un :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT s.code, p.\"durationHours\", sp.\"priceHT\", count(*) FROM \"SessionParticipant\" sp JOIN \"Organization\" o ON o.id=sp.\"sponsorOrgId\" JOIN \"TrainingSession\" s ON s.id=sp.\"sessionId\" JOIN \"TrainingProduct\" p ON p.id=s.\"productId\" WHERE o.\"opcoCode\"='AGEFICE' AND sp.\"priceHT\"=336 GROUP BY s.code, p.\"durationHours\", sp.\"priceHT\" ORDER BY p.\"durationHours\" DESC;"
       Chaque 336 residuel doit etre justifie : soit formation COURTE (8h) dont la Treso confirme 336 (LEGITIME, reste), soit AMBIGU/NO_MATCH (reliquat Plan 07). AUCUN 336 sur formation LONGUE (>8h) ne doit subsister sur une session AUTO-rapprochee — ca, c'est le vrai bug residuel.
    3. Verifier que les booleens canoniques ont bouge (UI les lit) :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) FILTER (WHERE \"factureEnvoyee\") AS fe, count(*) FILTER (WHERE \"validationOpco\") AS vo, count(*) FILTER (WHERE \"remboursementOpco\") AS ro, count(*) FILTER (WHERE \"paiementClient\") AS pc FROM \"SessionParticipant\";"
    4. Suite de tests : pnpm --filter @qualiof/web test (verte).
    NE PAS enchainer sur backfill-trainer (Task 4, apres checkpoint 2 — section 7b).
  </action>
  <verify>
    <automated>test -s /tmp/treso-match-apply-09.2.txt && docker exec qualiof_postgres psql -U qualiof -d qualiof -t -c "SELECT count(*) FROM \"SessionParticipant\" sp JOIN \"Organization\" o ON o.id=sp.\"sponsorOrgId\" JOIN \"TrainingSession\" s ON s.id=sp.\"sessionId\" JOIN \"TrainingProduct\" p ON p.id=s.\"productId\" WHERE o.\"opcoCode\"='AGEFICE' AND sp.\"priceHT\"=336 AND p.\"durationHours\" > 8;" | tr -d ' \n' > /tmp/long336.txt; echo "SP AGEFICE 336 sur formation LONGUE (>8h) restant : $(cat /tmp/long336.txt) — chacun DOIT etre justifie (reliquat AMBIGU/NO_MATCH), sinon bug residuel"</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/treso-match-apply-09.2.txt montre les ecritures prix + booleens.
    - GATE = la VRAIE propriete (PAS un seuil numerique aveugle qui force-corrigerait des 336 legitimes) : **zero SP AUTO-rapproche divergent de sa ligne Treso** (re-dry-run post-apply : les AUTO ne reproposent plus de changement de prix). C'est au Treso de dire quel prix est correct, pas au compteur.
    - **Caracterisation des 336 residuels, un a un** (requete action step 2) : chaque SP restant a 336 doit etre soit (a) formation COURTE 8h dont la ligne Treso CONFIRME 336 = LEGITIME (reste), soit (b) AMBIGU/NO_MATCH route en reliquat Plan 07. **AUCUN 336 sur formation LONGUE (>8h) d'une session AUTO-rapprochee** = ce serait le vrai bug non corrige. Les 336 longs en reliquat (non-AUTO) sont admis mais listes.
    - **Revue ligne a ligne proportionnee a la confiance du match** (§7-f) : les rapprochements a CLE PLEINE se survolent ; les DEGRADES (dates+org) et les DEPARTAGES PAR MONTANT se verifient UN A UN contre l'Excel Treso (les 2 faux positifs connus sortent de cette mecanique). Cf. colonne `niveau_cle` du CSV (Task 1).
    - Les 4 booleens canoniques (factureEnvoyee/validationOpco/remboursementOpco/paiementClient) ont des comptes coherents avec le Treso (>= valeurs E0 puisque OR-merge).
    - pnpm --filter @qualiof/web test : verte.
    - pg_dump pre-apply conserve.
  </acceptance_criteria>
  <done>Prix AGEFICE corriges (336 resolu), booleens canoniques OR-merges, tests verts. Pret pour la passe formateur (apres checkpoint 2).</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run backfill-trainer + pg_dump pre-apply formateur</name>
  <read_first>
    - packages/db/scripts/backfill-trainer-from-treso.ts (match session, findPersonByName, skip already-has-trainer)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 2 formateur + section 7)
  </read_first>
  <action>
    1. DRY-RUN : pnpm --filter @qualiof/db backfill:trainer-treso. Capturer dans /tmp/backfill-trainer-dryrun-09.2.txt.
    2. Verifier dans la sortie : (a) les sessions sans trainer recevront un LEAD ; (b) les sessions avec formateur saisi manuellement sont SKIP (non ecrasees) ; (c) les Person formateurs sont resolus par nom (pas de faux match grossier).
    3. pg_dump pre-apply (section 7a) :
       docker exec qualiof_postgres pg_dump -U qualiof -d qualiof | gzip > "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-pre-treso-trainer-$(date +%Y%m%d-%H%M%S).sql.gz"
    NE PAS --apply.
  </action>
  <verify>
    <automated>test -s /tmp/backfill-trainer-dryrun-09.2.txt && ls -S "/Users/laurentmarx/Documents/CRM Next gen/backups/"qualiof-pre-treso-trainer-*.sql.gz 2>/dev/null | head -1 | xargs test -s && echo TRAINER_DRYRUN_OK</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/backfill-trainer-dryrun-09.2.txt non vide.
    - Sessions avec formateur saisi marquees SKIP (non ecrasees).
    - pg_dump qualiof-pre-treso-trainer-*.sql.gz non vide.
    - Aucune mutation (dry-run).
  </acceptance_criteria>
  <done>Dry-run formateur revu, snapshot pris. Pret pour checkpoint 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Revue humaine du dry-run backfill formateur avant apply</name>
  <action>PAUSE pour revue humaine. Aucune execution automatique : presenter les elements ci-dessous (what-built) a l'humain, attendre le resume-signal avant de passer a la tache suivante. Ne lancer aucun --apply ni mutation tant que la validation n'est pas recue.</action>
  <what-built>Dry-run backfill-trainer-from-treso (/tmp/backfill-trainer-dryrun-09.2.txt) + pg_dump pre-apply formateur. L'apply prix/booleens (Task 2) est deja fait ; celui-ci est SEPARE (section 7b).</what-built>
  <how-to-verify>
    1. Ouvrir /tmp/backfill-trainer-dryrun-09.2.txt : verifier les associations session<->formateur proposees (date +-5j + nom formation). Sont-elles correctes ?
    2. Confirmer qu'aucune session avec formateur deja saisi manuellement n'est ecrasee (skip).
    3. Verifier qu'il n'y a pas de faux match de nom de formateur (homonymes).
  </how-to-verify>
  <resume-signal>Repondre "approved" pour autoriser l'apply formateur, OU lister les associations a refuser.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Apply backfill-trainer (apres approbation) + verification</name>
  <read_first>
    - packages/db/scripts/backfill-trainer-from-treso.ts
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 7b)
  </read_first>
  <action>
    UNIQUEMENT apres le 2e "approved".
    1. APPLY : pnpm --filter @qualiof/db backfill:trainer-treso -- --apply. Capturer dans /tmp/backfill-trainer-apply-09.2.txt.
    2. Verifier le nombre de SessionTrainer crees et que les formateurs manuels sont intacts :
       docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) AS sessions_avec_trainer FROM \"TrainingSession\" s WHERE EXISTS (SELECT 1 FROM \"SessionTrainer\" st WHERE st.\"sessionId\"=s.id);"
    3. Re-lancer le dry-run backfill (idempotence) : il doit SKIP les sessions deja traitees.
    4. Suite de tests : pnpm --filter @qualiof/web test ET pnpm --filter @qualiof/db test (vertes).
  </action>
  <verify>
    <automated>test -s /tmp/backfill-trainer-apply-09.2.txt && pnpm --filter @qualiof/web test 2>&1 | tail -3 && pnpm --filter @qualiof/db test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/backfill-trainer-apply-09.2.txt montre les SessionTrainer crees.
    - Le compte sessions_avec_trainer a AUGMENTE.
    - Le dry-run post-apply SKIP les sessions traitees (idempotence).
    - Formateurs saisis manuellement intacts (verification ponctuelle).
    - Suites web + db vertes.
    - Les 2 pg_dump (match + trainer) conserves.
  </acceptance_criteria>
  <done>Formateurs backfilles depuis Treso (sans ecraser le manuel), idempotence confirmee, tests verts. La chaine E2 est complete. Gate E3 (Plan 07) debloque.</done>
</task>

</tasks>

<verification>
- 2 pg_dump pris (avant match, avant trainer) — section 7a.
- 2 checkpoints humains francais avant chaque apply — section 7b (jamais enchaines).
- Bug 336 corrige (sp_336 fortement diminue), booleens canoniques mis a jour (UI les lit).
- Formateurs backfilles sans ecraser le manuel ; idempotence confirmee.
- NO_MATCH/AMBIGU routes vers /tmp/treso-matching.csv (reliquat Plan 07).
- Suites web + db vertes.
- import-treso-agefice.ts (jeu parallele) JAMAIS lance.
</verification>

<success_criteria>
- Prix AGEFICE = source Treso (336 EUR x52 corrige), 4 booleens canoniques OR-merges.
- Formateurs par session backfilles, manuel preserve.
- Reliquat Treso (NO_MATCH/AMBIGU + conflits descendants) materialise pour Plan 07.
- Chaine E2 complete -> gate E3 (Plan 07) debloque.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-06-SUMMARY.md (comptes AUTO/AMBIGU/NO_MATCH, sp_336 avant/apres, booleens avant/apres, SessionTrainer crees, conflits descendants a router, 2 pg_dump).
</output>
