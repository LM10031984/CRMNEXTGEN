---
phase: 09.2
plan: 07
type: execute
wave: 6
depends_on: ["06", "02"]
files_modified:
  - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RELIQUAT.csv
autonomous: false
requirements: [E3-gate, E3-reliquat]
must_haves:
  truths:
    - "Gate global vert : |CA Treso (2).xlsx 3 onglets AGEFICE - CA base opcoCode=AGEFICE| / CA Treso < 2 % (gate-treso-ca.ts exit 0)"
    - "Gate ligne-a-ligne : chaque ecart figure dans le CSV reliquat avec un verdict (corrige OU explicitement accepte avec motif), 0 ligne sans verdict"
    - "Ecart inexplique SYMETRIQUE traite : ligne Treso sans SP ET SP AGEFICE sans ligne Treso (session fantome) ont tous deux un verdict"
    - "Le reliquat agrege TOUS les cas en attente : doublons SIRET ambigus, MULTI_MATCH/NO_MATCH Treso, 22 orgs SIRET invalide, 48 persons sans email non discriminees, conflits booleens descendants, sessions sans lieu non backfillees, OpcoSubmission/Task/BillingProfile non repointes"
    - "Laurent tranche chaque ligne du reliquat (checkpoint bloquant)"
  artifacts:
    - path: ".planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RELIQUAT.csv"
      provides: "CSV de travail unique : tous les ecarts + verdict par ligne"
      min_lines: 10
  key_links:
    - from: "gate-treso-ca.ts"
      to: "ratio < 0.02"
      via: "exit code 0/1"
      pattern: "GATE_GLOBAL"
    - from: "rapprochement-treso-bdd.ts"
      to: "ORPHELINS BDD (SP AGEFICE sans ligne Treso)"
      via: "section symetrique du gate"
      pattern: "ORPHELIN"
---

<objective>
E3 — Gate de coherence + reliquat. Apres la passe Treso (Plan 06), prouver que la base est fiable : (1) gate GLOBAL via gate-treso-ca.ts (Plan 02), ecart CA < 2 % ; (2) gate LIGNE-A-LIGNE via rapprochement-treso-bdd.ts (ecart symetrique) ; (3) consolider TOUS les cas en attente dans un CSV de reliquat unique, et faire trancher Laurent ligne par ligne (verdict : corrige / accepte avec motif). Gate vert = ecart < 2 % ET 0 ligne sans verdict (section 5).

Purpose: Un auditeur Qualiopi remarque autant une ligne Treso sans dossier base qu'un SP AGEFICE sans ligne Treso (session fantome). Le gate exige le double sens. Le CSV de travail est la trace d'audit qui transforme « ecart » en « ecart explique ».
Output: gate global vert, gate ligne-a-ligne avec 0 inexplique, CSV reliquat tranche par Laurent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-VALIDATION.md

<interfaces>
apps/web/scripts/gate-treso-ca.ts (Plan 02) : niveau GLOBAL. Imprime tresoTotal/baseTotal/ratio + GATE_GLOBAL_OK|FAIL, exit 0 si <2%.
apps/web/scripts/rapprochement-treso-bdd.ts : READ-ONLY. Categorise chaque ligne Treso : MATCH_OK / MATCH_DIFF (montant !=) / MULTI_MATCH / NOT_FOUND + section ORPHELINS BDD (218-234 = SP AGEFICE sans ligne Treso = sessions fantomes). Sortie /tmp/treso-rapprochement.csv. Lit /tmp/treso-agefice-extracted.json (produit par analyse-treso-agefice.ts). ATTENTION : il filtre par annee (136-140) — or section 5.1 INTERDIT le scoping annuel pour le gate. Mitigation : il garde candidates si aucun candidat dans l'annee (142) ; verifier qu'aucune ligne n'est faussement classee NOT_FOUND a cause du filtre annee. Si le filtre fausse le resultat, lancer une version read-only ad-hoc /tmp sans filtre annee (discretion Claude).
apps/web/scripts/match-treso-agefice.ts -> /tmp/treso-matching.csv (AUTO/AMBIGU/NO_MATCH du Plan 06).

Cas reliquat a consolider (sources) :
- Doublons SIRET ambigus (SIRET partage noms differents) : non auto-merges au Plan 03 (dedupe.ts les ecarte). SQL : SIRET partage par 2+ orgs de noms differents.
- MULTI_MATCH / NO_MATCH / AMBIGU Treso : /tmp/treso-matching.csv + /tmp/treso-rapprochement.csv.
- 22 orgs SIRET invalide (requiresCleanup) : SELECT ... WHERE requiresCleanup ... (cf. photo E0).
- 48 persons sans email non discriminees : Person sans email non fusionnees.
- Conflits booleens descendants (base true / Treso false) : SQL audit separe (Pitfall 6) -> SP.factureEnvoyee=true (et 3 autres) dont la ligne Treso correspondante = false.
- Sessions sans lieu non backfillees (reliquat lieu Plan 04, /tmp/sonde-lieux-smartof.csv RELIQUAT_MANUEL).
- OpcoSubmission/Task/BillingProfile potentiellement non repointes par dedupe (RESEARCH limite dedupe.ts) : verifier s'ils referencaient un perdant fusionne.
Container Postgres : qualiof_postgres (user qualiof, db qualiof). SP.priceHT en EUROS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate global (gate-treso-ca.ts) + gate ligne-a-ligne (rapprochement symetrique)</name>
  <read_first>
    - apps/web/scripts/gate-treso-ca.ts (Plan 02)
    - apps/web/scripts/rapprochement-treso-bdd.ts (categorisation + ORPHELINS 218-234 + filtre annee 136-142)
    - apps/web/scripts/analyse-treso-agefice.ts (produit /tmp/treso-agefice-extracted.json — necessaire au rapprochement ; attention chemins hardcodes, lancer pour generer le JSON)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 5 + 5.1 perimetre + ecart symetrique)
  </read_first>
  <action>
    1. GATE GLOBAL : pnpm --filter @qualiof/web exec tsx scripts/gate-treso-ca.ts. Capturer la sortie (tresoTotal, baseTotal, ratio, verdict). Noter exit code.
    2. Generer le JSON Treso si rapprochement-treso-bdd.ts en a besoin : pnpm --filter @qualiof/web exec tsx scripts/analyse-treso-agefice.ts (produit /tmp/treso-agefice-extracted.json). Verifier que l'extraction couvre bien les 3 onglets AGEFICE.
    3. GATE LIGNE-A-LIGNE : pnpm --filter @qualiof/web exec tsx scripts/rapprochement-treso-bdd.ts. Lire /tmp/treso-rapprochement.csv. Extraire les categories : MATCH_DIFF, MULTI_MATCH, NOT_FOUND (ligne Treso sans SP) + ORPHELINS BDD (SP AGEFICE sans ligne Treso = sessions fantomes). VERIFIER que le filtre annee (136-142) ne classe pas faussement des lignes NOT_FOUND (section 5.1 : pas de scoping annuel). Si suspicion, relancer un comptage read-only ad-hoc sans filtre annee (SQL via docker exec) pour confirmer les vrais NOT_FOUND/ORPHELINS.
    4. Consigner les deux niveaux dans le SUMMARY : ratio global + verdict, et la liste des ecarts ligne-a-ligne (2 sens).
    Tout est READ-ONLY ici. Aucune mutation.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsx scripts/gate-treso-ca.ts; echo "GATE_EXIT=$?"; test -s /tmp/treso-rapprochement.csv && echo RAPPROCHEMENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - gate-treso-ca.ts imprime tresoTotal/baseTotal/ratio + verdict ; exit code capture (GATE_EXIT).
    - /tmp/treso-rapprochement.csv produit avec MATCH_DIFF/MULTI_MATCH/NOT_FOUND + ORPHELINS BDD.
    - Les deux sens de l'ecart symetrique sont denombres (ligne Treso sans SP ; SP AGEFICE sans ligne Treso).
    - Confirmation que le filtre annee ne fausse pas le comptage (section 5.1).
    - Aucune mutation.
  </acceptance_criteria>
  <done>Gate global mesure + gate ligne-a-ligne (symetrique) produits. Les ecarts sont denombres et prets a etre consolides en reliquat.</done>
</task>

<task type="auto">
  <name>Task 2: Construire le CSV de reliquat consolide (tous les cas en attente)</name>
  <read_first>
    - /tmp/treso-matching.csv (Plan 06) + /tmp/treso-rapprochement.csv (Task 1) + /tmp/sonde-lieux-smartof.csv (Plan 04)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 5 reliquat + section 3.2 garde-fous + section 2 conflit descendant)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (limite dedupe.ts : OpcoSubmission/Task/BillingProfile non repointes ; Pitfall 6 conflits descendants)
  </read_first>
  <action>
    Construire un CSV de travail UNIQUE : .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RELIQUAT.csv (utiliser l'outil Write pour le creer). Colonnes : categorie ; id/code concerne ; description courte ; donnees cle (montant base / montant Treso / SIRET / email / etc.) ; verdict (a remplir par Laurent : CORRIGE / ACCEPTE) ; motif.
    Y consolider, via requetes SQL read-only (docker exec qualiof_postgres psql -U qualiof -d qualiof) et lecture des CSV /tmp :
    1. Doublons SIRET ambigus : SELECT "siret", string_agg("legalName",' || ') FROM "Organization" WHERE "siret" IS NOT NULL GROUP BY "siret" HAVING count(*)>1; (noms differents = arbitrage manuel).
    2. MULTI_MATCH / NO_MATCH / AMBIGU Treso : depuis /tmp/treso-matching.csv et /tmp/treso-rapprochement.csv.
    3. NOT_FOUND (ligne Treso sans SP) ET ORPHELINS BDD (SP AGEFICE sans ligne Treso = session fantome) : depuis /tmp/treso-rapprochement.csv — les DEUX sens.
    4. 22 orgs SIRET invalide : SELECT id,"legalName","siret" FROM "Organization" WHERE "requiresCleanup"=true (ou critere SIRET invalide reel selon schema).
    5. 48 persons sans email non discriminees : SELECT id,"lastName","firstName" FROM "Person" WHERE "email" IS NULL.
    6. Conflits booleens descendants (Pitfall 6) : SELECT sp.id, s.code FROM "SessionParticipant" sp JOIN ... WHERE sp."factureEnvoyee"=true AND <ligne Treso correspondante = false> (croiser avec /tmp/treso-matching.csv qui porte les 4 booleens Treso). Lister les 4 booleens.
    7. Sessions sans lieu non backfillees : depuis /tmp/sonde-lieux-smartof.csv (RELIQUAT_MANUEL) + SELECT code FROM "TrainingSession" WHERE "locationId" IS NULL.
    8. OpcoSubmission/Task/BillingProfile potentiellement orphelins suite a fusion : verifier s'ils referencent un id de Person/Org disparu. SELECT count(*) FROM "OpcoSubmission" os WHERE NOT EXISTS (SELECT 1 FROM "Organization" o WHERE o.id=os.<orgFk>); idem Task/BillingProfile selon FK reelles (lire schema). Lister les orphelins eventuels.
    Chaque ligne = 1 cas, verdict VIDE (a remplir au checkpoint). Resumer en tete du CSV : nb total de cas par categorie.
  </action>
  <verify>
    <automated>test -s ".planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RELIQUAT.csv" && head -1 ".planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RELIQUAT.csv" | grep -qi "categorie" && echo RELIQUAT_OK</automated>
  </verify>
  <acceptance_criteria>
    - 09.2-RELIQUAT.csv existe, en-tete avec colonne categorie + verdict + motif.
    - Les 8 categories sont representees (ou explicitement notees "0 cas" si vide).
    - L'ecart symetrique (NOT_FOUND + ORPHELINS BDD) figure des deux cotes.
    - Aucune mutation BDD (read-only + ecriture du CSV doc).
  </acceptance_criteria>
  <done>CSV reliquat consolide pret pour arbitrage Laurent. Tous les ecarts y figurent (verdict vide).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Arbitrage du reliquat E3 par Laurent</name>
  <action>PAUSE pour revue humaine. Aucune execution automatique : presenter les elements ci-dessous (what-built) a l'humain, attendre le resume-signal avant de passer a la tache suivante. Ne lancer aucun --apply ni mutation tant que la validation n'est pas recue.</action>
  <decision>Trancher chaque ligne du reliquat 09.2-RELIQUAT.csv : verdict CORRIGE (action a mener) ou ACCEPTE (avec motif).</decision>
  <context>
    Le gate E3 (section 5) est vert UNIQUEMENT si : ecart CA global < 2 % ET 0 ligne sans verdict. Chaque ecart doit etre soit corrige, soit explicitement accepte avec motif (ex : « session fantome connue, dossier jamais depose », « 0 EUR Treso acte cas Steve Noel », « SIRET partage corrige manuellement »). C'est la trace d'audit attendue par un auditeur Qualiopi.
  </context>
  <options>
    <option id="par-ligne">
      <name>Verdict ligne par ligne dans le CSV</name>
      <pros>Trace d'audit complete, granularite maximale</pros>
      <cons>Plus long si beaucoup de lignes</cons>
    </option>
    <option id="par-categorie">
      <name>Verdict groupe par categorie quand le motif est identique</name>
      <pros>Rapide pour les categories homogenes (ex : 48 sans email = reliquat accepte en masse)</pros>
      <cons>Moins granulaire ; a reserver aux categories vraiment homogenes</cons>
    </option>
  </options>
  <resume-signal>Renseigner la colonne verdict (+ motif) pour CHAQUE ligne du CSV, puis repondre "reliquat tranche". Indiquer les corrections a mener (CORRIGE) qui necessiteront une action supplementaire.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Re-mesurer le gate global apres corrections + verifier 0 ligne sans verdict</name>
  <read_first>
    - apps/web/scripts/gate-treso-ca.ts
    - .planning/phases/.../09.2-RELIQUAT.csv (verdicts remplis)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 5)
  </read_first>
  <action>
    UNIQUEMENT apres "reliquat tranche".
    1. Si des lignes CORRIGE impliquent des corrections de donnees ponctuelles (ex : un SIRET partage a separer, un match Treso a refaire), les realiser via les scripts existants en mode cible (ex match-treso-agefice avec --apply sur 1 fichier, ou correction SQL ponctuelle documentee). pg_dump OBLIGATOIRE SANS EXCEPTION avant CHAQUE action corrective qui mute la BDD (section 7a) — pas "si elle mute" : toute correction de Task 3 qui ecrit en base est precedee d'un pg_dump horodate (docker exec qualiof_postgres pg_dump -U qualiof -d qualiof | gzip > "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-pre-e3-correction-$(date +%Y%m%d-%H%M%S).sql.gz"). Documenter chaque correction ET le snapshot associe.
    2. RE-LANCER le gate global : pnpm --filter @qualiof/web exec tsx scripts/gate-treso-ca.ts. Il doit imprimer GATE_GLOBAL_OK (ratio < 2 %) et exit 0.
    3. Verifier que le CSV reliquat n'a AUCUNE ligne sans verdict : compter les lignes dont la colonne verdict est vide -> doit etre 0.
    4. Suite de tests : pnpm --filter @qualiof/web test (verte).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsx scripts/gate-treso-ca.ts; test $? -eq 0 && echo GATE_GLOBAL_PASS</automated>
  </verify>
  <acceptance_criteria>
    - gate-treso-ca.ts exit 0 (ratio CA < 2 %) -> GATE_GLOBAL_PASS imprime.
    - 09.2-RELIQUAT.csv : 0 ligne avec verdict vide (gate ligne-a-ligne satisfait).
    - L'ecart symetrique est entierement verdicte (NOT_FOUND + ORPHELINS).
    - Les corrections CORRIGE realisees sont documentees (avec pg_dump si mutation).
    - pnpm --filter @qualiof/web test : verte.
  </acceptance_criteria>
  <done>Gate E3 vert : ecart CA < 2 % ET 0 ligne inexpliquee. La base est prouvee fiable. Dry-run temoin E4 (Plan 08) debloque.</done>
</task>

</tasks>

<verification>
- Gate global : gate-treso-ca.ts exit 0 (ecart < 2 %).
- Gate ligne-a-ligne : 0 ligne du reliquat sans verdict ; ecart symetrique traite des deux cotes.
- Reliquat tranche par Laurent (checkpoint decision).
- Corrections CORRIGE documentees (pg_dump OBLIGATOIRE sans exception avant chaque mutation, section 7a).
- Suite verte.
</verification>

<success_criteria>
- Gate de coherence section 5 vert : ecart CA AGEFICE < 2 % + 0 ligne inexpliquee (symetrique).
- 09.2-RELIQUAT.csv complet et tranche (trace d'audit).
- Plan 08 (dry-run temoin E4) debloque.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-07-SUMMARY.md (ratio global avant/apres, comptes ecarts symetriques, synthese reliquat par categorie + verdicts, corrections menees).
</output>
