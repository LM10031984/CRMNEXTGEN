---
phase: 09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice
plan: 02
subsystem: testing
tags: [treso, agefice, scoring, gate, smartof, lieux, vitest, reconciliation, read-only]

requires:
  - phase: 09.2-01
    provides: "Client Prisma généré, base qualiof_test, dedupe testable"
provides:
  - "Clé scoring Tréso conforme §4 : montant en départage (plus en entrée), nb stagiaires en entrée"
  - "scoreTresoRow/matchTresoRows exportés + testés (4 tests) — outille la passe Tréso Plan 06"
  - "gate-treso-ca.ts : gate global E3 (ratio CA Tréso vs base AGEFICE <2%) — outille Plan 07"
  - "sonde-lieux-smartof.ts : verdict backfillable vs reliquat des lieux SmartOF — outille Plan 04"
  - "Annexe RECONCILE-RULES : justification écrite de la seule modif de script de la phase"
affects: ["09.2-04-passe-smartof-apply-lieux", "09.2-06-passe-treso", "09.2-07-gate-e3-reliquat"]

tech-stack:
  added: []
  patterns:
    - "Scoring isolé en fonction pure exportée (scoreTresoRow) + orchestration testable (matchTresoRows générique)"
    - "Script gate read-only avec exit code branché sur seuil métier"
    - "Sonde read-only live SmartOF, échec creds explicite (jamais de donnée inventée)"

key-files:
  created:
    - "apps/web/scripts/__tests__/match-treso-scoring.test.ts"
    - "apps/web/scripts/gate-treso-ca.ts"
    - "apps/web/scripts/sonde-lieux-smartof.ts"
  modified:
    - "apps/web/scripts/match-treso-agefice.ts (clé §4 + extraction scoring + garde main)"
    - ".planning/.../RECONCILE-RULES.md (Annexe A — modification de script autorisée)"

key-decisions:
  - "Montant retiré du score d'entrée (bug 336€×52 le confortait) ; départage seul. nb stagiaires en entrée (+15)"
  - "matchTresoRows générique <T extends TresoLike> pour préserver les champs TresoRow downstream (APPLY/CSV)"
  - "0 backfillable sur 22 sessions sans lieu : custom_fields SmartOF vides en source (vérifié SES-0002) — vrai résultat, pas un faux négatif (probe de structure exécutée)"
  - "Invocation scripts SmartOF via `dotenv -e ../../.env -- tsx` : client.ts lit l'env au chargement (const module-level), l'ESM hoiste les imports avant le dotenv.config in-script"

patterns-established:
  - "Probe de structure SmartOF avant de faire confiance à un verdict 'vide' (anti faux-négatif silencieux)"

requirements-completed: [E2-treso-key, E3-gate-script, E2-lieux-sonde]

duration: ~40min
completed: 2026-06-10
---

# Phase 09.2 — Plan 02 : Clé tréso + gate + sonde lieux (Wave 0 suite)

**La passe Tréso (Plan 06) et le gate E3 (Plan 07) sont outillés avec une clé de rapprochement saine, et la décision lieux (Plan 04) est tranchée : aucune mutation BDD dans ce plan.**

## Performance

- **Duration:** ~40 min
- **Completed:** 2026-06-10
- **Tasks:** 3/3
- **Commits:** 3 atomiques (`fix clé tréso` / `gate-treso-ca` / `sonde-lieux-smartof`)

## Accomplishments

### Task 1 — Clé scoring Tréso §4 + 4 tests + justification
- **Montant retiré du score d'ENTRÉE** (bloc `+20 si montant ±5%` supprimé) → uniquement en **départage** entre candidats à score d'entrée égal (`|sig.total − t.montant|` minimal).
- **nb stagiaires en entrée** : `+15 si t.nbStagiaires === sig.session.participants.length` (dégradation explicite si vide/divergent).
- Extraction `scoreTresoRow(t, sig)` (entrée pure) + `matchTresoRows<T>(rows, sigs)` (départage + boost exclusion), exports nommés + garde `main()`. **APPLY inchangé** (priceHT=montant/nbSP, OR-merge booléens).
- `match-treso-scoring.test.ts` : 4 tests verts (montant hors entrée · nb stagiaires +15 · départage montant · no-match <40).
- **Annexe A** RECONCILE-RULES : AVANT/APRÈS/POURQUOI/date (seule modif de script de la phase, §7c).

### Task 2 — gate-treso-ca.ts (gate global E3, read-only)
- Lecture buffer du fichier canonique, 3 onglets AGEFICE agrégés (périmètre §5.1, pas de scoping exercice, lignes sans montant/total exclues).
- Base : `sum(SessionParticipant.priceHT)` WHERE `opcoCode='AGEFICE'`.
- **Dry actuel : Tréso 855 570 € vs base 261 951 € = 69,38 %** → `GATE_GLOBAL_FAIL` attendu **avant** la passe Tréso (cohérent mémoire écart CA ~65%). Le script tourne, calcule, exit branché sur 2%. Verdict réel = Plan 07.

### Task 3 — sonde-lieux-smartof.ts (read-only, live SmartOF)
- 22 sessions sans `locationId` (cohérent §1) → pull live SmartOF → lecture `custom_fields.custom_field_1/2/3`.
- **Verdict : 0 backfillable · 21 reliquat manuel · 1 introuvable.** Probe de structure exécutée (SES-0002) : custom_fields **réellement vides en source** → vrai résultat, pas un faux négatif.
- Échec creds → message explicite + exit 1, aucun lieu inventé. CSV `/tmp/sonde-lieux-smartof.csv` produit.
- **Conséquence Plan 04** : aucun backfill lieu automatique possible depuis SmartOF — les 22 sont du reliquat manuel.

## Verification

- `pnpm --filter @qualiof/web test` : **874/874 verts** (107 fichiers, +4 match-treso-scoring), aucune régression.
- `tsc --noEmit` apps/web : **0 erreur hors redirect-308** (WIP pré-existant).
- AUCUN `--apply`, AUCUNE mutation BDD (Task 2/3 read-only, Task 1 code+test).

## Déviations

| # | Plan | Réel | Justification |
|---|------|------|---------------|
| D1 | invocation `pnpm exec tsx` | `dotenv -e ../../.env -- tsx` pour sonde | client.ts lit l'env au chargement module ; ESM hoiste les imports avant dotenv.config in-script |
| D2 | grep verify `../.planning/...` | `../../.planning/...` | chemin relatif erroné dans le plan (apps/web → 2 niveaux jusqu'à .planning) |

## Next

Wave 0 complète (Plans 01+02). **Les passes `--apply` (Plans 03→06) mutent la prod-locale** : chacune exige dry-run → revue diff → pg_dump → apply (checkpoint humain). Prochain : Plan 03 (passe dedupe `--apply`).
