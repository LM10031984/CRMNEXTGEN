---
phase: 09.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/dedupe.ts
  - apps/web/scripts/__tests__/dedupe.merge.test.ts
autonomous: true
requirements: [E2-dedupe-test, E2-refacto]
must_haves:
  truths:
    - "Le client Prisma est genere : tout script tsx touchant @qualiof/db boote sans 'Cannot find module .prisma/client'"
    - "mergeOrgs / mergePersons sont exportes depuis dedupe.ts et acceptent un client transactionnel injecte, sans changer le mecanisme de repointage FK"
    - "dedupe.merge.test.ts PASSE sur la base DEDIEE qualiof_test : une fusion reelle reporte les ExternalIdentity du perdant sur le survivant (Person ET Organization), le perdant est supprime"
    - "GARDE D'ENVIRONNEMENT : le test REFUSE de tourner si TEST_DATABASE_URL n'est pas une base *_test ; la prod-locale qualiof n'est JAMAIS ecrite (0 ligne TESTA sur qualiof)"
    - "Le garde-fou anti-fusion email-seul est teste : detectPersonsByName ne propose pas 2 personnes meme nom + emails differents"
    - "La suite complete reste verte ; update-session-details.test.ts intouche"
  artifacts:
    - path: "apps/web/scripts/dedupe.ts"
      provides: "mergeOrgs/mergePersons exportes + signature tx injectable"
      contains: "export async function mergePersons"
    - path: "apps/web/scripts/__tests__/dedupe.merge.test.ts"
      provides: "Test integration BDD reelle transaction-rollback (1ere du repo)"
      min_lines: 60
  key_links:
    - from: "dedupe.merge.test.ts"
      to: "mergePersonsTx(tx, ...)"
      via: "appel direct dans une transaction rollback-only"
      pattern: "mergePersonsTx"
    - from: "mergeOrgs/mergePersons"
      to: "tx.externalIdentity.updateMany"
      via: "repointage entityId perdant vers survivant"
      pattern: "externalIdentity.updateMany"
---

<objective>
Wave 0 — Lever les deux bloquants durs avant TOUTE mutation BDD : (1) generer le client Prisma (absent du repo), (2) ecrire le PREREQUIS DUR dedupe.merge.test.ts qui prouve par une fusion REELLE en transaction que les ExternalIdentity du perdant pointent vers le survivant apres coup. C'est la PREMIERE integration BDD reelle du repo (les tests existants mockent tous Prisma). Pour le rendre testable, refacto mineure de dedupe.ts : exporter mergeOrgs/mergePersons et leur faire accepter un client transactionnel injecte.

Purpose: La passe de dedoublonnage (Plan 03 --apply) repose sur l'invariant « la fusion ne cree pas de doublon orphelin au rejeu idempotent ». Cet invariant est implemente (dedupe.ts:160-163 Org / 247-250 Person) mais NON teste. Tant que ce test n'est pas vert, aucun --apply dedupe n'est autorise (RECONCILE-RULES section 3.2).
Output: client Prisma genere, dedupe.ts testable, test vert et ferme (ne devient pas un gate ouvert permanent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-VALIDATION.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md

<interfaces>
Fichier modifie : apps/web/scripts/dedupe.ts (chemin REEL — PAS packages/db/scripts/)
- En tete : dotenv.config path '../../../.env' ; import { prisma } from '@qualiof/db'.
- Signatures ACTUELLES a refactoriser :
    async function mergeOrgs(canonical: string, duplicates: string[]): Promise<void>    // ligne 114
    async function mergePersons(canonical: string, duplicates: string[]): Promise<void> // ligne 182
    async function detectOrgsBySiret(tenantId: string)    // ligne 274
    async function detectPersonsByName(tenantId: string)  // ligne 365 (garde-fou email ~385-388 : SKIP si tous candidats ont emails TOUS distincts non-null)
- mergeOrgs ouvre AUJOURD'HUI sa propre transaction par duplicate :
    for (const dup of duplicates) prisma.$transaction(async (tx) => { repointage ; tx.externalIdentity.updateMany where entityType Organization entityId dup data entityId canonical ; tx.organization.delete where id dup })
  mergePersons idem avec entityType Person (247-250) + tx.person.delete.
- entityType ExternalIdentity = Organization/Person (PascalCase). entityType Document = organization/person (lowercase). Le test ne porte QUE sur ExternalIdentity.
- Client : import { prisma } from '@qualiof/db' (packages/db/src/index.ts re-exporte @prisma/client + namespace Prisma). Client ABSENT jusqu'a db:generate.
- Pattern test+export de reference : packages/db/scripts/__tests__/import-veille.idempotence.test.ts.
- Config test web : apps/web/vitest.config.ts (env node). Quick : pnpm --filter @qualiof/web test.
- ⚠️ SÉCURITÉ TEST BDD (garde-fou d'environnement, NON négociable) : le test NE DOIT JAMAIS écrire sur la base prod-locale `qualiof`. Il tourne sur une base DÉDIÉE `qualiof_test` via `TEST_DATABASE_URL` (= `postgresql://qualiof:<pwd>@localhost:5432/qualiof_test`). Le test instancie SON PROPRE `PrismaClient({ datasources: { db: { url: process.env.TEST_DATABASE_URL } } })` (PAS le singleton `@qualiof/db` lié à DATABASE_URL=qualiof) et passe le `tx` de CE client à mergePersonsTx/mergeOrgsTx. GARDE DURE en tête de fichier : si `TEST_DATABASE_URL` absent OU si le nom de base ne matche pas `/_test$/` → `throw new Error('REFUS: dedupe.merge.test exige une base _test dédiée')` AVANT toute opération. Rollback-only ne suffit pas : la garde est la première ligne de défense.
- Base de test = schéma seul, vide (pas de tenant) → le test crée son propre Tenant en transaction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight environnement — generer le client Prisma + verifier les bloquants</name>
  <read_first>
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (section Environment Availability + Pitfall 4)
    - CLAUDE.md (stack : pnpm 10, Postgres docker qualiof_postgres)
  </read_first>
  <action>
    Executer, dans cet ordre exact (cwd = racine /files) :
    1. pnpm --filter @qualiof/db db:generate  (= dotenv -e ../../.env -- prisma generate). DOIT reussir : cree packages/db/node_modules/.prisma/client (le client est genere DANS le package @qualiof/db, PAS a la racine node_modules).
    2. docker compose ps  — verifier que qualiof_postgres (et redis/gotenberg/weasyprint/minio si presents) sont healthy/Up. Si postgres KO : docker compose up -d postgres puis re-verifier.
    3. CREER LA BASE DE TEST DEDIEE (garde-fou #5, isole totalement la prod-locale) :
       a. docker exec qualiof_postgres psql -U qualiof -d postgres -c "CREATE DATABASE qualiof_test;" 2>&1 | grep -qE "CREATE DATABASE|already exists"  (idempotent : OK si deja presente).
       b. Pousser le schema sur la base de test (sans toucher qualiof) :
          DATABASE_URL="postgresql://qualiof:<pwd-identique-a-.env>@localhost:5432/qualiof_test" pnpm --filter @qualiof/db exec prisma db push --skip-generate
          (recuperer le mot de passe depuis .env DATABASE_URL ; ne PAS le logger en clair dans le SUMMARY).
       c. Exporter TEST_DATABASE_URL pour le test : l'ajouter dans .env (ou .env.test lu par vitest) = la meme URL pointant vers qualiof_test.
    4. Verifier la presence du fichier Treso au repertoire PARENT de /files :
       ls -la "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx" -> DOIT exister (~94798 octets, canonique RECONCILE-RULES 5.1).
    5. Verifier le backup baseline :
       ls -la "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-E0-baseline-2026-06-10.sql.gz" -> DOIT exister.
    NE PAS lancer de script mutant sur qualiof. Aucun --apply. Consigner les resultats dans le SUMMARY.
  </action>
  <verify>
    <automated>test -d packages/db/node_modules/.prisma/client && docker exec qualiof_postgres psql -U qualiof -d qualiof_test -c "SELECT 1 FROM \"Tenant\" LIMIT 0;" >/dev/null 2>&1 && test -f "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx" && test -f "/Users/laurentmarx/Documents/CRM Next gen/backups/qualiof-E0-baseline-2026-06-10.sql.gz" && echo PREFLIGHT_OK</automated>
  </verify>
  <acceptance_criteria>
    - test -d packages/db/node_modules/.prisma/client exit 0 (client genere dans le package, pas a la racine).
    - docker compose ps montre qualiof_postgres Up/healthy (capture dans SUMMARY).
    - Base qualiof_test existe ET porte le schema (la requete sur "Tenant" ne renvoie pas d'erreur "relation does not exist").
    - TEST_DATABASE_URL pointe vers qualiof_test (verifie dans .env / .env.test).
    - Tréso Agefice (2).xlsx ET qualiof-E0-baseline-2026-06-10.sql.gz presents au parent de /files.
    - La verify imprime PREFLIGHT_OK.
  </acceptance_criteria>
  <done>Client Prisma genere, Postgres up, base de test qualiof_test creee + schema pousse + TEST_DATABASE_URL configuree, fichier Treso canonique et backup E0 confirmes. La prod-locale qualiof n'a PAS ete touchee.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refacto testabilite dedupe.ts — exporter mergeOrgs/mergePersons + injecter tx</name>
  <read_first>
    - apps/web/scripts/dedupe.ts (integralite — surtout mergeOrgs 114-178 et mergePersons 182-270)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 0 + section 3.2)
    - packages/db/scripts/__tests__/import-veille.idempotence.test.ts (pattern export nomme)
  </read_first>
  <behavior>
    - mergePersons et mergeOrgs importables depuis dedupe.ts (export nomme) sans declencher main().
    - Appeles avec un client transactionnel tx injecte, ils font le MEME repointage FK qu'avant, dans la transaction du test.
    - Comportement CLI inchange : sans tx, ils ouvrent leur propre transaction comme aujourd'hui.
  </behavior>
  <action>
    Refacto STRICTEMENT « rendre testable », PAS « changer le mecanisme » (autorise par contraintes + 09.2-VALIDATION Wave 0). Aucune logique de repointage ne change.
    1. Extraire le corps de la transaction de mergeOrgs (le async (tx) =>) dans mergeOrgsTx(tx: Prisma.TransactionClient, canonical: string, dup: string): Promise<void> traitant UN dup. Idem mergePersonsTx(tx, canonical, dup).
    2. Reecrire mergeOrgs(canonical, duplicates) pour boucler et faire await prisma.$transaction((tx) => mergeOrgsTx(tx, canonical, dup)). Idem mergePersons. Comportement CLI inchange.
    3. Ajouter export sur : mergeOrgs, mergePersons, mergeOrgsTx, mergePersonsTx, detectPersonsByName, detectOrgsBySiret, normalize.
    4. Importer le namespace Prisma depuis @qualiof/db et typer tx: Prisma.TransactionClient.
    5. Empecher l'effet de bord a l'import : si main() s'execute inconditionnellement en bas du fichier, le garder derriere une garde "execute directement seulement". Le test importe le module, il ne doit PAS lancer la detection complete.
    NE PAS toucher : pickCanonical, les detections, le garde-fou email (385-388), le repointage FK.
    Justification ecrite : commentaire bloc en tete de dedupe.ts « Refacto 09.2 : extraction mergeOrgsTx/mergePersonsTx pour testabilite. Mecanisme de repointage FK INCHANGE. » (trace section 0/7c RECONCILE-RULES).
  </action>
  <verify>
    <automated>cd apps/web && grep -q "export async function mergePersons" scripts/dedupe.ts && grep -q "export async function mergeOrgs" scripts/dedupe.ts && grep -q "Prisma.TransactionClient" scripts/dedupe.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep "export async function mergeOrgs" et "export async function mergePersons" -> match.
    - grep "Prisma.TransactionClient" -> match (signature tx typee).
    - grep "mergeOrgsTx" et "mergePersonsTx" -> match (fonctions extraites).
    - tsc --noEmit clean (exit 0).
    - git diff montre AUCUNE modification du contenu du repointage FK (seulement extraction + export + garde main).
  </acceptance_criteria>
  <done>dedupe.ts expose mergeOrgs/mergePersons/mergeOrgsTx/mergePersonsTx avec tx injectable, tsc clean, mecanisme inchange, justification ecrite presente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dedupe.merge.test.ts — fusion reelle transaction-rollback (PREREQUIS DUR)</name>
  <read_first>
    - apps/web/scripts/dedupe.ts (APRES Task 2 : mergeOrgsTx/mergePersonsTx/detectPersonsByName exportes)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (section Validation Architecture — approche transaction-rollback, assertions minimales)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 3.2 invariant ExternalIdentity)
    - apps/web/vitest.config.ts (env node)
  </read_first>
  <behavior>
    - GARDE D'ENVIRONNEMENT (avant tout) : si TEST_DATABASE_URL absent OU nom de base ne matche pas /_test$/ -> le test echoue immediatement avec un message clair, AVANT toute creation. Jamais d'ecriture sur la base prod-locale qualiof.
    - Test A (Person, integration BDD reelle, sur qualiof_test) : creer un Tenant + Person A (survivant) + Person B (perdant) + ExternalIdentity{entityType:'Person', entityId:B} dans une transaction ; appeler mergePersonsTx(tx, A.id, B.id) ; asserter que l'ExternalIdentity pointe maintenant vers A (entityId === A.id) ET que Person B n'existe plus ; puis ROLLBACK (rien ne persiste).
    - Test B (Organization, symetrique) : Tenant + Org A + Org B + ExternalIdentity{entityType:'Organization', entityId:B} ; mergeOrgsTx(tx, A.id, B.id) ; asserter ExternalIdentity.entityId === A.id ET Org B supprimee ; ROLLBACK.
    - Test C (garde-fou email, cas Nestenn) : 2 Person meme nom+prenom, emails differents non-null ; detectPersonsByName ne les propose PAS comme groupe a fusionner. ROLLBACK.
    - Test D (TEST DE PUISSANCE / mutation, cf [[feedback_test_de_puissance_mutation]]) : prouver que Test A n'est pas complaisant. Au gate (manuel, hors suite auto) : casser temporairement mergePersonsTx (commenter le repointage externalIdentity.updateMany) -> Test A DOIT virer ROUGE -> restaurer -> revert. Documenter le resultat dans le SUMMARY. (Decrit en acceptance, executé au checkpoint Plan 03.)
  </behavior>
  <action>
    Creer apps/web/scripts/__tests__/dedupe.merge.test.ts (vitest). Integration BDD reelle sur base DEDIEE qualiof_test (PAS le singleton @qualiof/db lie a qualiof) — PAS de mock pour Tests A/B (un mock ne prouve pas le repointage FK reel).
    Structure :
    - import { PrismaClient, Prisma } from '@prisma/client' ; import { mergePersonsTx, mergeOrgsTx, detectPersonsByName } from '../dedupe'.
    - GARDE (top-level, avant tout) :
        const TEST_URL = process.env.TEST_DATABASE_URL;
        if (!TEST_URL || !/_test(\?|$)/.test(new URL(TEST_URL).pathname + (new URL(TEST_URL).search))) {
          throw new Error('REFUS: dedupe.merge.test exige TEST_DATABASE_URL pointant une base *_test dediee (jamais la prod-locale qualiof)');
        }
        const db = new PrismaClient({ datasources: { db: { url: TEST_URL } } });
        afterAll(async () => { await db.$disconnect(); });
      (Verifier le nom de base de facon robuste : extraire le pathname de l'URL, exiger qu'il finisse par "_test".)
    - Helper rollback : async function inRollback(fn) { await expect(db.$transaction(async (tx) => { await fn(tx); throw new Error('ROLLBACK'); })).rejects.toThrow('ROLLBACK'); }
    - Test A (dans inRollback) :
        const t = await tx.tenant.create({ data: { name: 'TEST-TENANT-09.2' } });
        const a = await tx.person.create({ data: { tenantId: t.id, lastName: 'TESTA', firstName: 'Surv' } });
        const b = await tx.person.create({ data: { tenantId: t.id, lastName: 'TESTA', firstName: 'Perd' } });
        const ei = await tx.externalIdentity.create({ data: { tenantId: t.id, entityType: 'Person', entityId: b.id, source: 'test-09.2', externalId: 'ei-' + b.id } });
        await mergePersonsTx(tx, a.id, b.id);
        expect((await tx.externalIdentity.findUnique({ where: { id: ei.id } }))?.entityId).toBe(a.id);
        expect(await tx.person.findUnique({ where: { id: b.id } })).toBeNull();
      (Adapter les champs requis non-nullables selon schema.prisma reel — Tenant.name requis ; ExternalIdentity a @@unique(source, externalId) et un tenantId — fournir des valeurs minimales valides. Lire le schema avant d'ecrire.)
    - Test B : symetrique organization.create (legalName + legalForm requis) / mergeOrgsTx / entityType 'Organization'.
    - Test C : creer Tenant + 2 Person meme nom, emails differents ; comme detectPersonsByName lit la base via un client, l'appeler avec le client de test sur ces donnees committees PUIS nettoyer en afterEach (DELETE cible sur tenantId du test) — OU si la signature accepte un client injecte, l'appeler dans la transaction. Asserter qu'aucun groupe ne contient les 2 ids (garde-fou Nestenn).
    Robustesse : base qualiof_test isolee + rollback par test. En filet : afterAll DELETE de tout enregistrement source='test-09.2' / name LIKE 'TEST-TENANT-09.2%' sur qualiof_test (jamais sur qualiof).
  </action>
  <verify>
    <automated>cd apps/web && npx dotenv -e ../../.env -- pnpm exec vitest run scripts/__tests__/dedupe.merge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest run dedupe.merge.test.ts : 3 tests PASS (A Person, B Organization, C garde-fou email) sur la base qualiof_test.
    - GARDE prouvee : en supprimant temporairement TEST_DATABASE_URL (ou en la pointant sur "qualiof"), le test ECHOUE avec "REFUS:" AVANT toute ecriture (le test refuse la prod-locale). Restaurer ensuite.
    - Anti-residu sur la PROD-LOCALE : docker exec qualiof_postgres psql -U qualiof -d qualiof -c "SELECT count(*) FROM \"Person\" WHERE \"lastName\"='TESTA'" renvoie 0 (la prod-locale n'a jamais ete touchee).
    - Anti-residu sur la base de test : meme requete sur -d qualiof_test renvoie 0 (rollback + filet afterAll effectifs).
    - Le test importe mergePersonsTx/mergeOrgsTx depuis ../dedupe (pas de copie de logique) ; aucun vi.mock de Prisma sur Tests A/B.
    - TEST DE PUISSANCE (gate, manuel) : commenter le repointage externalIdentity.updateMany dans mergePersonsTx -> re-run -> Test A vire ROUGE (entityId reste sur le perdant) -> restaurer -> re-run vert. Resultat consigne au SUMMARY ([[feedback_test_de_puissance_mutation]]). Si Test A reste vert apres mutation = test complaisant a corriger.
    - pnpm --filter @qualiof/web test : suite complete verte, aucune regression ; update-session-details.test.ts intouche.
  </acceptance_criteria>
  <done>dedupe.merge.test.ts vert sur qualiof_test : fusion reelle reporte ExternalIdentity Person ET Org, supprime le perdant, garde-fou email + garde d'environnement valides, test de puissance passe. Prod-locale qualiof JAMAIS touchee. Invariant 3.2 prouve. Passe dedupe (Plan 03) debloquee.</done>
</task>

</tasks>

<verification>
- pnpm --filter @qualiof/db db:generate OK (client present dans packages/db/node_modules/.prisma/client).
- Base de test qualiof_test creee + schema pousse + TEST_DATABASE_URL configuree (isolation totale de la prod-locale).
- tsc --noEmit clean sur apps/web.
- pnpm --filter @qualiof/web test : suite complete verte (aucune regression vs baseline), dedupe.merge.test.ts inclus et vert.
- Garde d'environnement prouvee (le test refuse une base non-_test) + test de puissance passe (mutation -> rouge -> restaure).
- 0 ligne TESTA sur qualiof (prod-locale jamais touchee) ET 0 sur qualiof_test (rollback + filet effectifs).
- AUCUN script mutant lance sur qualiof, AUCUN --apply : ce plan ne touche pas les donnees de prod-local.
</verification>

<success_criteria>
- Client Prisma genere (bloquant Wave 0 leve).
- dedupe.ts refactore testable (mergeOrgsTx/mergePersonsTx exportes, tx injectable), mecanisme de repointage inchange, justification ecrite.
- dedupe.merge.test.ts vert et ferme : prouve l'invariant ExternalIdentity perdant->survivant (Person + Org) + garde-fou email.
- Prerequis dur RECONCILE-RULES 3.2 satisfait -> la passe de dedoublonnage (Plan 03) est debloquee.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-01-SUMMARY.md (resultats preflight + diff dedupe.ts + sortie test).
</output>
