---
phase: 09.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/match-treso-agefice.ts
  - apps/web/scripts/__tests__/match-treso-scoring.test.ts
  - apps/web/scripts/gate-treso-ca.ts
  - apps/web/scripts/sonde-lieux-smartof.ts
  - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
autonomous: true
requirements: [E2-treso-key, E3-gate-script, E2-lieux-sonde]
must_haves:
  truths:
    - "Le montant n'est plus un critere d'ENTREE du scoring Treso : il ne sert qu'a departager des candidats a score d'entree egal (section 4)"
    - "Le nombre de stagiaires est un critere d'entree : compare t.nbStagiaires a sig.session.participants.length avec degradation explicite si vide/divergent"
    - "Aucun no-match silencieux : tout no-match Treso est route vers le CSV de reliquat"
    - "Un test unitaire prouve qu'une session a montant faux (336) ne 'gagne' plus par le montant, et qu'a dates+org egaux le montant departage"
    - "gate-treso-ca.ts calcule le total Treso (2).xlsx 3 onglets AGEFICE hors lignes sans montant vs SELECT sum(priceHT) opcoCode=AGEFICE, et imprime ratio + verdict <2%"
    - "sonde-lieux-smartof.ts liste en READ-ONLY les custom_field_1/2/3 SmartOF des sessions sans locationId"
    - "La modification de match-treso-agefice.ts est justifiee par ecrit dans RECONCILE-RULES.md"
  artifacts:
    - path: "apps/web/scripts/match-treso-agefice.ts"
      provides: "Scoring section 4 conforme (montant en departage, nb stagiaires en entree)"
      contains: "nbStagiaires"
    - path: "apps/web/scripts/__tests__/match-treso-scoring.test.ts"
      provides: "Test unitaire de la fonction de scoring"
      min_lines: 40
    - path: "apps/web/scripts/gate-treso-ca.ts"
      provides: "Gate global E3 : ratio CA Treso vs base AGEFICE"
      min_lines: 50
    - path: "apps/web/scripts/sonde-lieux-smartof.ts"
      provides: "Sondage read-only des lieux SmartOF des sessions sans locationId"
      min_lines: 30
  key_links:
    - from: "match-treso-agefice.ts"
      to: "/tmp/treso-matching.csv"
      via: "ecriture CSV reliquat (no-match explicite)"
      pattern: "treso-matching.csv"
    - from: "gate-treso-ca.ts"
      to: "Tréso Agefice (2).xlsx + SessionParticipant opcoCode=AGEFICE"
      via: "lecture xlsx 3 onglets + SELECT sum(priceHT)"
      pattern: "opcoCode"
---

<objective>
Wave 0 (suite) — Preparer la passe Treso et le gate E3, SANS toucher la BDD. Trois livrables :
1. Corriger la cle de rapprochement Treso (section 4) dans match-treso-agefice.ts : retirer le montant des criteres d'ENTREE (bug confirme lignes 247-250), ajouter le nb stagiaires en entree, montant UNIQUEMENT en departage. SEULE modification de script d'import autorisee par la phase — justifiee par ecrit dans RECONCILE-RULES.md.
2. Ecrire gate-treso-ca.ts (gate global E3, n'existe pas) : ratio |CA Treso - CA base AGEFICE| / CA Treso < 2 %.
3. Ecrire sonde-lieux-smartof.ts (read-only) : lister les custom_fields lieu SmartOF des sessions sans locationId pour trancher backfillable vs reliquat en Plan 04 (le dry-run import-from-smartof ne le revele PAS — RESEARCH Pitfall 3).

Purpose: La passe Treso (Plan 06) doit utiliser une cle conforme AVANT tout --apply (sinon un match « reussi » sur 336 EUR conforte le bug et empeche la correction). Le gate (Plan 07) a besoin de son script. Le sondage lieux conditionne le scope du backfill lieu (Plan 04).
Output: scoring Treso conforme + teste, script gate pret, script sondage pret, justification ecrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md
@.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-VALIDATION.md

<interfaces>
apps/web/scripts/match-treso-agefice.ts
- dotenv path '../../../.env'. import XLSX. Arg positionnel obligatoire = chemin xlsx (process.argv.slice(2)). APPLY = argv includes --apply.
- Scoring ACTUEL (~235-254), pour chaque ligne Treso t vs chaque sig :
    +50 si >=1 nom stagiaire matche ; +30 date debut +-2j ; +20 date fin +-2j ; +20 si montant +-5% (BUG section 4, lignes 247-250) ; +10 OPCO coherent.
  Seuils : >=70 AUTO, 40-70 AMBIGU, <40 NO_MATCH (boost 50->70 si 1 seul candidat).
- t expose : t.dateRange{start,end}, t.montant, t.stagiairesRaw, t.opcoLabel, t.nbStagiaires (parse mais JAMAIS score aujourd'hui).
- sig expose : sig.session{code,startDate,endDate,participants}, sig.total (somme priceHT base, potentiellement FAUSSE), sig.stagNames, sig.opcos.
- APPLY mute (329-409) : priceHT = montant / nb SP matches (ecrasement inconditionnel si >0, corrige les 336) + 4 booleens canoniques factureEnvoyee/validationOpco/remboursementOpco/paiementClient en OR-merge false->true. CSV /tmp/treso-matching.csv (ligne 323).
- helpers internes : normalize, dayDiff, splitStagiaires. Si la fn de scoring n'est pas isolable, l'extraire en export nomme (« rendre testable »).

Fichier canonique Treso (section 5.1) : "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx" (PARENT de /files). Onglets : AGEFICE 2024 / AGEFICE 2025 / AGEFICE 2026.
Cote base AGEFICE : SessionParticipant.priceHT = Decimal(10,2) en EUROS (PAS cents). SELECT sum(sp."priceHT") JOIN Organization o WHERE o."opcoCode"='AGEFICE'.
Reference read-only a IMITER (pas reutiliser) : rapprochement-treso-bdd.ts (ORPHELINS 218-234). analyse-treso-agefice.ts a chemins HARDCODES hors /files et lit 3 fichiers -> ne PAS reutiliser tel quel.
Parse FR a reutiliser : parseFrenchDateRange/parseFormationRange. TOTAL_KEYWORDS d'analyse-treso-agefice.ts pour exclure les totaux.
import-from-smartof.ts resolveSessionLocation (769-802) : custom_field_1=rue / 2=CP / 3=ville. Auth Firebase requise meme en read (SMARTOF_FIREBASE_API_KEY + SMARTOF_EMAIL + SMARTOF_PASSWORD). Client : apps/web/src/lib/smartof/client.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corriger la cle de scoring Treso (section 4) + test unitaire + justification ecrite</name>
  <read_first>
    - apps/web/scripts/match-treso-agefice.ts (integralite — scoring 230-280, parse nbStagiaires, apply 329-409, CSV 323)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 4 cascade + section 0/7c justification obligatoire)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (inventaire script 4 + Pitfall 2)
  </read_first>
  <behavior>
    - Score d'ENTREE sans montant : nom stagiaire (+50) + dates +-2j (+30/+20) + OPCO (+10) + nb stagiaires (nouveau). Le +20 montant (247-250) est SUPPRIME.
    - nb stagiaires : si t.nbStagiaires renseigne ET = sig.session.participants.length -> bonus (ex +15). Si vide/divergent -> pas de bonus (degradation a dates+org, pas de penalite) — section 4 etape 2.
    - Departage : entre 2+ candidats a meilleur score d'entree EGAL, le montant le plus proche de t.montant gagne. Le montant n'entre JAMAIS dans le score d'entree.
    - Une session a montant 336 (faux) ne gagne plus par le montant ; le match repose sur noms/dates/nb stagiaires.
    - No-match toujours route vers /tmp/treso-matching.csv (verdict NO_MATCH explicite, jamais silencieux).
  </behavior>
  <action>
    1. Si la boucle de scoring n'est pas isolable, extraire scoreTresoRow(t, sig): number + matchTresoRows(tresoRows, sigs) (seuils + departage montant) en exports nommes (« rendre testable »).
    2. SUPPRIMER le bloc +20 montant +-5% (~247-250).
    3. AJOUTER nb stagiaires en entree : if (t.nbStagiaires != null && t.nbStagiaires === sig.session.participants.length) score += 15. (Choisir le poids de sorte que AUTO>=70 reste atteignable par noms+dates ; documenter le bareme en commentaire.)
    4. DEPARTAGE par montant : a meilleur score d'entree egal, choisir la sig dont |sig.total - t.montant| est minimal. sig.total peut etre FAUX (336) — acceptable car le montant ne departage que des candidats deja qualifies par noms/dates. Documenter (section 4 etape 3).
    5. Garantir le routage no-match -> CSV (ligne 323) : verifier que les NO_MATCH y sont ecrits avec verdict.
    6. JUSTIFICATION ECRITE OBLIGATOIRE dans RECONCILE-RULES.md : nouvelle sous-section « Annexe — Modification de script autorisee (section 0/7c) » : fichier match-treso-agefice.ts, lignes touchees, AVANT (montant critere d'entree +20) / APRES (montant en departage, +nb stagiaires entree), POURQUOI le mecanisme actuel ne suffit pas (montant base faux -> match conforte le bug), date 2026-06-10. SEULE modif de script d'import de la phase.
    NE PAS toucher l'APPLY (priceHT = montant/nbSP, OR-merge booleens) — conforme section 2.
  </action>
  <action_test>
    Creer apps/web/scripts/__tests__/match-treso-scoring.test.ts (vitest, unitaire pur — pas de BDD) :
    - import { scoreTresoRow } (ou matchTresoRows) from '../match-treso-agefice'.
    - Test 1 « montant hors entree » : row t {noms, dates, montant=3024} ; sig identique sauf sig.total. Asserter scoreTresoRow(t, sig{total:336}) === scoreTresoRow(t, sig{total:3024}) (le montant n'influe pas sur le score d'entree).
    - Test 2 « nb stagiaires en entree » : t.nbStagiaires=3 ; sig participants.length=3 score > sig participants.length=5 (ceteris paribus). Asserter delta = +15 (poids retenu).
    - Test 3 « departage par montant » : matchTresoRows avec 2 sig a score d'entree egal, montants differents -> retour pointe vers la sig dont total est le plus proche de t.montant.
    - Test 4 « no-match » : t sans noms/dates qui matche -> verdict NO_MATCH, pas de crash, route reliquat.
    Mocker les objets minimaux conformes aux champs lus (t.dateRange/montant/nbStagiaires/stagiairesRaw/opcoLabel ; sig.session.startDate/endDate/participants, sig.total, sig.stagNames, sig.opcos).
  </action_test>
  <verify>
    <automated>cd apps/web && grep -q "nbStagiaires" scripts/match-treso-agefice.ts && grep -q "Modification de script autorisee" "../.planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md" && pnpm exec vitest run scripts/__tests__/match-treso-scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le bloc montant +-5% (+20) du scoring d'entree est SUPPRIME (grep manuel : plus de `score += 20` associe a un ratio montant).
    - grep "nbStagiaires" present dans le scoring de match-treso-agefice.ts.
    - RECONCILE-RULES.md contient « Modification de script autorisee » avec AVANT/APRES/POURQUOI/date.
    - vitest run match-treso-scoring.test.ts : 4 tests PASS.
    - /tmp/treso-matching.csv reste le sink des NO_MATCH (verifie en lecture du code).
    - Aucun --apply, aucune mutation BDD.
  </acceptance_criteria>
  <done>Cle Treso conforme section 4, 4 tests verts, justification ecrite. La passe Treso (Plan 06) utilisera un scoring sain.</done>
</task>

<task type="auto">
  <name>Task 2: gate-treso-ca.ts — gate global E3 (read-only, n'existe pas)</name>
  <read_first>
    - apps/web/scripts/analyse-treso-agefice.ts (parse onglets, TOTAL_KEYWORDS, colonne montant — POUR S'EN INSPIRER, chemins hardcodes a NE PAS copier)
    - apps/web/scripts/rapprochement-treso-bdd.ts (lecture base AGEFICE, ORPHELINS 218-234)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 5 + 5.1 perimetre EXACT)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (Code Examples — Total Treso scope pour le gate)
  </read_first>
  <action>
    Creer apps/web/scripts/gate-treso-ca.ts (read-only, AUCUNE mutation). Invocation : pnpm --filter @qualiof/web exec tsx scripts/gate-treso-ca.ts.
    - dotenv path '../../../.env' ; import { prisma } from '@qualiof/db' ; import XLSX ; import fs.
    - Chemin xlsx : "/Users/laurentmarx/Documents/CRM Next gen/Tréso Agefice (2).xlsx" (canonique section 5.1 ; resoudre via path.resolve(__dirname, '../../../..', 'Tréso Agefice (2).xlsx') comme le ROOT de backfill-trainer-from-treso.ts). Lecture : fs.readFileSync + XLSX.read(buf,{type:'buffer'}) (JAMAIS XLSX.readFile — memoire feedback_xlsx_buffer_read).
    - Cote Treso : pour chacun des 3 onglets AGEFICE 2024/2025/2026, sommer la colonne « Montant Total de la formation » (index via headers, cf. check-totaux-treso.ts / analyse-treso-agefice.ts). EXCLURE : lignes de total (TOTAL_KEYWORDS), lignes sans montant (cellule vide/__EMPTY), lignes hors AGEFICE. « Groupe X » multi-stagiaires compte UNE fois pour son montant total. Agreger les 3 onglets ensemble (PAS de scoping par exercice — section 5.1).
    - Cote base : SELECT COALESCE(sum(sp."priceHT"),0)::float AS total FROM "SessionParticipant" sp JOIN "Organization" o ON o.id = sp."sponsorOrgId" WHERE o."opcoCode" = 'AGEFICE' (via prisma.$queryRaw). priceHT en EUROS, cast float.
    - ratio = Math.abs(tresoTotal - baseTotal) / tresoTotal. Imprimer : tresoTotal, baseTotal, ecart absolu, ratio %, verdict GATE_GLOBAL_OK si ratio < 0.02 sinon GATE_GLOBAL_FAIL. process.exit(ratio < 0.02 ? 0 : 1).
    NE PAS scoper par annee. Niveau global uniquement ; le niveau ligne-a-ligne est en Plan 07.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "opcoCode" scripts/gate-treso-ca.ts && grep -q "AGEFICE 2024" scripts/gate-treso-ca.ts && grep -q "type: 'buffer'" scripts/gate-treso-ca.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - gate-treso-ca.ts existe, lit (2).xlsx en buffer (grep "type: 'buffer'"), agrege les 3 onglets AGEFICE (grep "AGEFICE 2024").
    - Requete base scope opcoCode='AGEFICE' (grep "opcoCode").
    - tsc --noEmit clean.
    - Le script imprime GATE_GLOBAL_OK / GATE_GLOBAL_FAIL et exit code coherent (0/1). (Le verdict reel sera evalue en Plan 07, APRES la passe Treso ; ici on valide juste que le script tourne sans crash et calcule un ratio.)
    - Aucune mutation BDD.
  </acceptance_criteria>
  <done>gate-treso-ca.ts pret : calcule le ratio CA Treso vs base AGEFICE conforme au perimetre 5.1, exit code branche sur le seuil 2%. Utilisable en Plan 07.</done>
</task>

<task type="auto">
  <name>Task 3: sonde-lieux-smartof.ts — sondage read-only des lieux des sessions sans locationId</name>
  <read_first>
    - apps/web/scripts/import-from-smartof.ts (resolveSessionLocation 769-802 : custom_field_1/2/3 ; auth Firebase getIdToken 57)
    - apps/web/src/lib/smartof/client.ts (client API, methodes session list)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-RESEARCH.md (Pitfall 3 + inventaire script 2)
    - .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/RECONCILE-RULES.md (section 1 : 22 sessions sans lieu, <=9 closure-relevant)
  </read_first>
  <action>
    Creer apps/web/scripts/sonde-lieux-smartof.ts (READ-ONLY, AUCUNE ecriture BDD ni SmartOF). But : trancher backfillable vs manuel AVANT la passe import-from-smartof (le dry-run ne revele PAS les lieux car resolveSessionLocation renvoie null en dry-run — Pitfall 3).
    - dotenv path '../../../.env' ; import { prisma } from '@qualiof/db' ; import le client SmartOF (lib/smartof/client.ts).
    - 1. Lister en base les sessions sans locationId : prisma.trainingSession.findMany({ where: { locationId: null }, select: { code, name, startDate, _count: { participants } } }) + leur ExternalIdentity SmartOF (source='smartof') pour avoir l'externalId.
    - 2. Pour chaque session, pull la session SmartOF correspondante via le client (read-only) et lire custom_field_1 (rue) / custom_field_2 (CP) / custom_field_3 (ville).
    - 3. Imprimer un tableau : code | nb participants | custom_field_1/2/3 presents ? | verdict propose : BACKFILLABLE (>=1 champ lieu non vide) vs RELIQUAT_MANUEL (tous vides). Resumer : N sessions sondees, X backfillables, Y reliquat, dont Z closure-relevant (nb participants > 0).
    - Ecrire la sortie dans /tmp/sonde-lieux-smartof.csv (review humaine, alimente le reliquat Plan 07).
    Si les creds SmartOF / le reseau echouent (auth Firebase), echouer proprement avec un message clair « creds SmartOF requis (SMARTOF_FIREBASE_API_KEY/EMAIL/PASSWORD) — sondage lieux impossible » et exit 1 — NE PAS inventer de lieux.
    AUCUNE mutation. AUCUN --apply.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "locationId: null" scripts/sonde-lieux-smartof.ts && grep -q "custom_field" scripts/sonde-lieux-smartof.ts && grep -q "sonde-lieux-smartof.csv" scripts/sonde-lieux-smartof.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - sonde-lieux-smartof.ts existe, requete les sessions sans locationId (grep "locationId: null"), lit les custom_field SmartOF (grep "custom_field"), ecrit /tmp/sonde-lieux-smartof.csv.
    - tsc --noEmit clean.
    - Le script est READ-ONLY : aucun prisma.*.update/create/delete, aucun --apply (grep -L sur update/create dans le script).
    - En cas d'echec creds SmartOF : message explicite + exit 1, pas de lieu invente.
  </acceptance_criteria>
  <done>sonde-lieux-smartof.ts pret : produit le verdict backfillable vs manuel par session sans lieu, alimente la decision du Plan 04 et le reliquat du Plan 07.</done>
</task>

</tasks>

<verification>
- tsc --noEmit clean sur apps/web (3 fichiers ajoutes + 1 modifie).
- pnpm --filter @qualiof/web test : match-treso-scoring.test.ts vert, suite complete sans regression.
- gate-treso-ca.ts et sonde-lieux-smartof.ts tournent sans crash (le verdict reel est evalue plus tard).
- RECONCILE-RULES.md : sous-section « Modification de script autorisee » presente.
- AUCUN --apply, AUCUNE mutation BDD dans ce plan (read-only + tests).
</verification>

<success_criteria>
- Cle Treso conforme section 4 (montant en departage, nb stagiaires en entree) + 4 tests unitaires verts + justification ecrite (seule modif script autorisee).
- gate-treso-ca.ts pret (gate global E3) conforme perimetre 5.1.
- sonde-lieux-smartof.ts pret (read-only) pour trancher les lieux.
- Plans 06 (Treso) et 07 (gate/reliquat) outilles.
</success_criteria>

<output>
Apres completion, creer .planning/phases/09.2-r-conciliation-base-3-sources-airtable-smartof-tr-so-agefice/09.2-02-SUMMARY.md (diff match-treso, sortie gate dry, sortie sonde, extrait RECONCILE-RULES annexe).
</output>
