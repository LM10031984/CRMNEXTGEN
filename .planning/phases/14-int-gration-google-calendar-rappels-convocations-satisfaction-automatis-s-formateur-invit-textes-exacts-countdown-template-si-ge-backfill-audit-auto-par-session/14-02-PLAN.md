---
phase: 14-google-calendar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/calendar/texts.ts
  - apps/web/src/lib/calendar/countdown.ts
  - apps/web/src/lib/calendar/__tests__/countdown.test.ts
  - apps/web/src/lib/calendar/__tests__/texts.test.ts
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "Les textes rappel et satisfaction-froid de Start Academy sont stockés MOT POUR MOT en constantes, avec interpolation des variables (formation, dates, horaires, lieu, formateur, countdown)"
    - "Le bloc SIÈGE Vence (accès/transports/restauration/hébergement) s'ajoute automatiquement quand l'adresse du lieu est le siège (618 Bd Jean Maurel inférieur, 06140 Vence)"
    - "Le countdown 'dans X jours' est calculé exactement par jour J-15 → veille (J-1)"
    - "Les horaires 9h-13h/14h-18h sont des constantes figées (pas de smart-calc)"
    - "Les 4 ids Drive (programme session, CGV, RI, Charte PSH) + le questionnaire froid C7.i30 sont centralisés"
  artifacts:
    - path: "apps/web/src/lib/calendar/texts.ts"
      provides: "renderRappelText(ctx) + renderFroidText(ctx) + détection siège + ids Drive + horaires const"
      contains: "618"
    - path: "apps/web/src/lib/calendar/countdown.ts"
      provides: "countdownLabel(dayIndex) → 'dans X jours' / 'demain'"
      contains: "dans"
  key_links:
    - from: "texts.ts"
      to: "countdown.ts"
      via: "import countdownLabel pour interpoler le rappel quotidien"
      pattern: "countdownLabel"
---

<objective>
Capturer et verrouiller en constantes les textes EXACTS de Start Academy (rappel quotidien + satisfaction à froid), le bloc SIÈGE Vence conditionnel, la formule countdown "dans X jours", les horaires figés et les ids Drive. Ce module est consommé par les builders d'événements (Plan 14-04).

Purpose: La conformité Qualiopi exige la formulation EXACTE. Centraliser tout le contenu texte dans un seul module testé évite la dérive et rend les variables interpolables.
Output: 2 modules (texts + countdown) + 2 fichiers de test.

DÉPENDANCE DE CONTENU (checkpoint) : le wording exact des textes n'est PAS dans CONTEXT.md verbatim. L'exécuteur doit récupérer les strings exactes auprès de Laurent OU depuis le rendu du pilote SES-0097 dans l'agenda « Rappel Formations ». NE PAS inventer le texte. Un checkpoint:decision est prévu en Task 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-int-gration-google-calendar-rappels-convocations-satisfaction-automatis-s-formateur-invit-textes-exacts-countdown-template-si-ge-backfill-audit-auto-par-session/14-CONTEXT.md

<interfaces>
<!-- Ids Drive statiques connus (CONTEXT.md "Specific Ideas") : -->
<!-- CGV: 11mfi7rl8BQFhETty4vGat3GmoGclBuFx -->
<!-- RI: 1o44Zg9dXdbyJpQ-U5Tpfbx8lZjcm-Qyf -->
<!-- Charte PSH: 1HxT_uy6UNIZBYGSl9gchT0sS9_DaidNM -->
<!-- Questionnaire froid C7.i30: 1uNEa7QemfEYKyYf5ywGIdvyshWTjhYjd -->
<!-- Horaires figés convention (memory feedback "anti smart-calc") : matin 9h-13h, après-midi 14h-18h -->
<!-- Adresse siège Vence : 618 Bd Jean Maurel inférieur, 06140 Vence -->
<!-- Texte rappel : commence par "Rappel – Votre formation … commence bientôt !" signé "Emma de Start Academy" -->
<!-- Texte froid : "Bonjour … chasser le prochain bien d'exception … L'équipe Start Academy" -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Verrouiller le wording EXACT des textes</name>
  <action>Pause pour decision utilisateur : obtenir les chaines de texte exactes (rappel / froid / bloc siege Vence) aupres de Laurent OU depuis le pilote SES-0097 AVANT d'ecrire texts.ts (Task 2). Ne rien coder tant que le wording n'est pas verrouille.</action>
  <decision>Quelles sont les chaînes EXACTES (mot pour mot) du texte rappel quotidien, du texte satisfaction-froid, et du bloc SIÈGE Vence ?</decision>
  <context>
    La conformité Qualiopi impose la formulation exacte de Start Academy. CONTEXT.md ne donne que les incipit/signatures ("Rappel – Votre formation … commence bientôt ! … Emma de Start Academy" ; "Bonjour … chasser le prochain bien d'exception … L'équipe Start Academy"). Le pilote SES-0097 (déjà créé via MCP dans l'agenda « Rappel Formations ») contient le rendu validé. Source de vérité = ce pilote OU Laurent directement.
  </context>
  <options>
    <option id="from-pilote">
      <name>Extraire depuis le pilote SES-0097</name>
      <pros>Texte déjà validé par Laurent, inclut variables réelles</pros>
      <cons>Nécessite de lire les événements SES-0097 via l'API (events.list privateExtendedProperty ou recherche par date)</cons>
    </option>
    <option id="from-laurent">
      <name>Laurent colle les 3 textes exacts</name>
      <pros>Source directe, sans ambiguïté</pros>
      <cons>Demande une action manuelle</cons>
    </option>
  </options>
  <resume-signal>Coller les 3 blocs de texte exacts (rappel / froid / bloc siège Vence) avec les placeholders de variables marqués (ex. {{formation}}, {{dates}}, {{horaires}}, {{lieu}}, {{formateur}}, {{countdown}}), OU "from-pilote: lire SES-0097".</resume-signal>
  <acceptance_criteria>
    - Les 3 blocs de texte exacts sont fournis OU une instruction claire de les lire depuis SES-0097
    - Les variables interpolables sont identifiées (formation, dates, horaires, lieu, formateur, countdown)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: countdown.ts (formule "dans X jours") + test</name>
  <read_first>
    - apps/web/src/lib/business-days.ts (style helper date pur du repo, fr-FR)
    - .planning/phases/14-.../14-CONTEXT.md (section "Rappels quotidiens J-15 → veille")
  </read_first>
  <behavior>
    - countdownLabel(15) → 'dans 15 jours'
    - countdownLabel(2) → 'dans 2 jours'
    - countdownLabel(1) → 'demain'
    - countdownLabel(0) → "aujourd'hui"
    - Fonction pure, aucun import Date externe, aucune dépendance réseau
  </behavior>
  <action>
    Créer `apps/web/src/lib/calendar/countdown.ts` :
    - Export `function countdownLabel(daysUntil: number): string`.
      Règle : 0 → "aujourd'hui", 1 → "demain", n>1 → `dans ${n} jours`.
    - Export aussi `function daysBetween(from: Date, to: Date): number` (différence en jours calendaires entiers, UTC, floor) — utilisé par l'orchestrateur pour calculer `daysUntil` à la date de chaque rappel. Pas de lib externe (le repo n'a ni dayjs ni date-fns — cf STACK.md).
    Créer `apps/web/src/lib/calendar/__tests__/countdown.test.ts` couvrant les 4 cas du behavior + 1 cas daysBetween.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/countdown.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `countdown.ts` exporte `countdownLabel(` et `daysBetween(`
    - `countdownLabel(1)` renvoie exactement `demain`, `countdownLabel(2)` renvoie exactement `dans 2 jours`
    - `pnpm vitest run src/lib/calendar/__tests__/countdown.test.ts` exit 0, ≥ 5 assertions
    - `grep -E "dayjs|date-fns" apps/web/src/lib/calendar/countdown.ts` retourne 0 ligne
  </acceptance_criteria>
  <done>countdownLabel + daysBetween purs et testés verts.</done>
</task>

<task type="auto">
  <name>Task 2: texts.ts (textes exacts + bloc siège + ids Drive + horaires)</name>
  <read_first>
    - apps/web/src/lib/calendar/countdown.ts (countdownLabel, créé Task 1)
    - apps/web/src/lib/calendar/colors.ts (style module const du dossier, Plan 14-01)
    - .planning/phases/14-.../14-CONTEXT.md (sections "Textes", "Pièces jointes", "Couleurs")
    - La sortie du checkpoint Task 0 (textes exacts)
  </read_first>
  <action>
    Créer `apps/web/src/lib/calendar/texts.ts` :
    - Export `const HORAIRES = { matin: '9h-13h', apresMidi: '14h-18h' } as const;` (FIGÉ — memory "anti smart-calc sur conventions métier", ne JAMAIS recalculer).
    - Export `const DRIVE_DOCS = { cgv: '11mfi7rl8BQFhETty4vGat3GmoGclBuFx', ri: '1o44Zg9dXdbyJpQ-U5Tpfbx8lZjcm-Qyf', chartePsh: '1HxT_uy6UNIZBYGSl9gchT0sS9_DaidNM', questionnaireFroid: '1uNEa7QemfEYKyYf5ywGIdvyshWTjhYjd' } as const;`
    - Export `const SIEGE_ADDRESS = '618 Bd Jean Maurel inférieur, 06140 Vence';` et `function isSiegeVence(addressText: string): boolean` qui détecte le siège par l'adresse (match sur "618" + "Jean Maurel" + "06140" OU "Vence", insensible à la casse/espaces). Robuste aux variantes ("Bd"/"Boulevard").
    - Export `const SIEGE_BLOCK = \`...\`;` = le bloc EXACT accès transports/route + restauration + hébergement (issu du checkpoint Task 0 / reference_rappel_siege_vence_qualiopi).
    - Export type `CalendarTextCtx = { formation: string; dates: string; horaires: string; lieu: string; formateur: string; countdown: string; isSiege: boolean };`
    - Export `function renderRappelText(ctx: CalendarTextCtx): string` : reproduit MOT POUR MOT le texte rappel de Start Academy (du checkpoint Task 0), interpole les variables, et concatène `SIEGE_BLOCK` quand `ctx.isSiege` est vrai (À LA PLACE du texte standard d'accès — cf memory "rappel siège Vence" : le bloc étendu REMPLACE le texte standard). Le countdown est injecté via `ctx.countdown` (déjà calculé par `countdownLabel`).
    - Export `function renderFroidText(ctx: CalendarTextCtx): string` : reproduit MOT POUR MOT le texte satisfaction-froid (du checkpoint Task 0), interpole les variables, inclut le lien Drive `DRIVE_DOCS.questionnaireFroid`.
    - Si le checkpoint a renvoyé "from-pilote", l'exécuteur lit d'abord SES-0097 via l'API (getCalendarClient de 14-01 n'est pas dépendance dure ici mais peut être réutilisé en script jetable de lecture) AVANT d'écrire les constantes.
    Créer `apps/web/src/lib/calendar/__tests__/texts.test.ts` : (a) `isSiegeVence('618 Bd Jean Maurel inférieur, 06140 Vence')` === true ; (b) `isSiegeVence('495 avenue de Cannes, 06210 Mandelieu')` === false ; (c) `renderRappelText` avec `isSiege:true` contient un fragment du SIEGE_BLOCK ; (d) `renderRappelText` interpole `ctx.countdown` ; (e) `renderFroidText` contient l'id Drive du questionnaire froid.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/texts.test.ts 2>&1 | tail -6 && grep -q "1uNEa7QemfEYKyYf5ywGIdvyshWTjhYjd" src/lib/calendar/texts.ts && grep -q "618" src/lib/calendar/texts.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `texts.ts` exporte `renderRappelText(`, `renderFroidText(`, `isSiegeVence(`, `HORAIRES`, `DRIVE_DOCS`, `SIEGE_BLOCK`
    - `texts.ts` contient les 4 ids Drive exacts (cgv/ri/chartePsh/questionnaireFroid)
    - `texts.ts` contient `'9h-13h'` et `'14h-18h'` en constantes (grep)
    - `isSiegeVence` retourne true pour l'adresse siège, false pour Mandelieu
    - `pnpm vitest run src/lib/calendar/__tests__/texts.test.ts` exit 0, ≥ 5 assertions
    - Aucun recalcul d'horaire (pas de `Math`/`durationHours/8` dans texts.ts pour les horaires — grep -c absent)
  </acceptance_criteria>
  <done>Textes exacts + bloc siège conditionnel + ids Drive + horaires figés, testés verts.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm vitest run src/lib/calendar/__tests__/texts.test.ts src/lib/calendar/__tests__/countdown.test.ts` exit 0
- Le wording des textes correspond mot pour mot à la source validée (checkpoint Task 0 / pilote SES-0097)
- `grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/calendar/texts.ts apps/web/src/lib/calendar/countdown.ts` retourne 0 ligne (worker-safe)
</verification>

<success_criteria>
Textes EXACTS verrouillés en constantes, bloc siège Vence conditionnel, countdown calculé, horaires figés, ids Drive centralisés. Prêt pour les builders Plan 14-04.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/14-.../14-02-SUMMARY.md`.
</output>
