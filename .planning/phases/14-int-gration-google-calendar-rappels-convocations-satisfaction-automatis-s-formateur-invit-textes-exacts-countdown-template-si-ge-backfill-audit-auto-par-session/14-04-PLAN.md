---
phase: 14-google-calendar
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - apps/web/src/lib/calendar/event-builders.ts
  - apps/web/src/lib/calendar/sync-session.ts
  - apps/web/src/lib/calendar/__tests__/event-builders.test.ts
  - apps/web/src/lib/calendar/__tests__/sync-session.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Pour une session, on construit 1 evenement formation + 15 rappels quotidiens countdown (J-15 -> veille) + 3 relances satisfaction froid (fin+1mois, +1mois15j, +2mois)"
    - "Chaque evenement porte sa cle deterministe dans extendedProperties.private.qualiof_key, son colorId, ses pieces jointes Drive et les invites"
    - "Le formateur reel est toujours invite ; les apprenants sont toujours dans les invites mais sendUpdates depend du mode (passe=none, futur=toggle)"
    - "syncSessionCalendar est idempotent : un re-run ne cree pas de doublon (lookup par cle Google + table de synchro)"
  artifacts:
    - path: "apps/web/src/lib/calendar/event-builders.ts"
      provides: "buildFormationEvent / buildRappelEvents (15) / buildFroidEvents (3) — retournent des requestBody Google purs"
      contains: "extendedProperties"
    - path: "apps/web/src/lib/calendar/sync-session.ts"
      provides: "syncSessionCalendar(input) orchestrateur idempotent worker-safe"
      contains: "syncSessionCalendar"
  key_links:
    - from: "sync-session.ts"
      to: "events.list privateExtendedProperty qualiof_key"
      via: "lookup avant insert pour decider insert/update/skip"
      pattern: "privateExtendedProperty"
    - from: "sync-session.ts"
      to: "upsertSyncRecord"
      via: "trace en base apres chaque insert"
      pattern: "upsertSyncRecord"
---

<objective>
Construire les builders d'evenements Google (formation + 15 rappels countdown + 3 relances froid) et l'orchestrateur syncSessionCalendar qui les upsert dans l'agenda de maniere idempotente, en appliquant la regle d'invites (formateur reel toujours, apprenants sendUpdates conditionnel) et les couleurs/pieces jointes. C'est le coeur metier de la Phase 14.

Purpose: Tout le contenu (textes 14-02), l'auth/idempotence/couleurs (14-01) et la trace (14-03) convergent ici en une fonction unique reutilisee par le backfill (14-05) et le hook auto (14-06).
Output: 2 modules lib (builders + orchestrateur) + 2 fichiers de test. Worker-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-int-gration-google-calendar-rappels-convocations-satisfaction-automatis-s-formateur-invit-textes-exacts-countdown-template-si-ge-backfill-audit-auto-par-session/14-CONTEXT.md

<interfaces>
Plan 14-01 : getCalendarClient(), CALENDAR_ID, CALENDAR_COLORS {formation:'7',rappel:'6',froid:'3'}, buildEventKey(code,type,dayIndex?), QUALIOF_KEY_PROP='qualiof_key', type CalendarEventType.
Plan 14-02 : renderRappelText(ctx), renderFroidText(ctx), countdownLabel(daysUntil), daysBetween(from,to), isSiegeVence(addr), HORAIRES, DRIVE_DOCS {cgv,ri,chartePsh,questionnaireFroid}, SIEGE_BLOCK, type CalendarTextCtx.
Plan 14-03 : upsertSyncRecord(rec), getSyncRecordsForSession(tenantId,sessionId), prisma.sessionCalendarSync.

Donnees session (extraction Prisma, cf _ses0097-trainer-email.ts + schema.prisma) :
- TrainingSession: code, name, startDate, endDate, location { name, address(Json) }, product { title }
- trainers: SessionTrainer[] { isPrimary, person { firstName, lastName, email } } — formateur principal = isPrimary, email reel.
- participants: SessionParticipant[] { person { firstName, lastName, email } }.

API Google (cf _google-test.ts) :
- cal.events.insert({ calendarId, sendUpdates, requestBody })
- cal.events.list({ calendarId, privateExtendedProperty: 'qualiof_key=<cle>' }) -> lookup idempotence
- cal.events.update({ calendarId, eventId, sendUpdates, requestBody })
- Evenement journee entiere : start:{date:'YYYY-MM-DD'}, end:{date:'YYYY-MM-DD'} (end exclusif).
- Pieces jointes : requestBody.attachments=[{fileUrl:'https://drive.google.com/file/d/<id>/view', title}], et il faut supportsAttachments:true sur insert/update. Alternative tolérée si attachments echoue : liens dans description.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: event-builders.ts (formation + 15 rappels + 3 froid) + test</name>
  <read_first>
    - apps/web/src/lib/calendar/texts.ts (renderRappelText/renderFroidText/HORAIRES/DRIVE_DOCS/isSiegeVence, Plan 14-02)
    - apps/web/src/lib/calendar/countdown.ts (countdownLabel/daysBetween, Plan 14-02)
    - apps/web/src/lib/calendar/colors.ts + idempotency.ts (Plan 14-01)
    - apps/web/scripts/_google-test.ts (forme requestBody events.insert)
    - .planning/phases/14-.../14-CONTEXT.md (sections "Evenements par session", "Couleurs", "Pieces jointes")
  </read_first>
  <behavior>
    - buildFormationEvent(ctx) retourne un requestBody avec start.date/end.date couvrant startDate->endDate, colorId '7', extendedProperties.private.qualiof_key = buildEventKey(code,'formation'), description = renderRappelText (texte de convocation), attachments = programme session + CGV + RI + Charte.
    - buildRappelEvents(ctx) retourne EXACTEMENT 15 requestBody (J-15 a J-1/veille). Chacun : journee du jour J-x, colorId '6', cle buildEventKey(code,'rappel',i), description = renderRappelText avec countdown=countdownLabel(daysUntil) du jour, reminders.overrides = popup (PAS d'email : useDefault:false, overrides:[{method:'popup', minutes:...}]).
    - buildFroidEvents(ctx) retourne EXACTEMENT 3 requestBody aux dates fin+1mois, fin+1mois+15j, fin+2mois ; colorId '3' ; cle buildEventKey(code,'froid',i) ; description = renderFroidText.
    - Si startDate est trop proche (< 15 jours dans le futur) ou passee, buildRappelEvents genere quand meme les 15 cles (backfill/audit) mais l'orchestrateur decidera du sendUpdates.
  </behavior>
  <action>
    Creer apps/web/src/lib/calendar/event-builders.ts. Definir un type SessionEventCtx (code, name, productTitle, startDate, endDate, locationName, locationAddressText, programmeDriveId?, trainerEmail, trainerName, learnerEmails: string[]).
    Implementer :
    - helper interne toDateStr(d: Date): string -> 'YYYY-MM-DD'.
    - buildAttachments(programmeDriveId?) -> attachments[] avec les 4 docs (programme session si fourni + DRIVE_DOCS.cgv/ri/chartePsh), fileUrl = `https://drive.google.com/file/d/${id}/view`.
    - buildFormationEvent(ctx): journee(s) start->end (end.date = endDate + 1 jour, exclusif), summary `[Formation] ${productTitle} — ${code}`, colorId CALENDAR_COLORS.formation, extendedProperties.private = { qualiof_key: buildEventKey(code,'formation') }, location = locationName + adresse, description = renderRappelText({...ctx, countdown:'', isSiege:isSiegeVence(locationAddressText)}), attachments.
    - buildRappelEvents(ctx): boucle i de 15 a 1 (15 evenements). Pour chaque, date = startDate - i jours ; daysUntil = i ; countdown = countdownLabel(i) ; cle buildEventKey(code,'rappel',i) ; colorId CALENDAR_COLORS.rappel ; description = renderRappelText({..., countdown, isSiege}) ; reminders = { useDefault:false, overrides:[{method:'popup', minutes:9*60}] } (rappel pop-up le matin, PAS d'email — CONTEXT.md). Retourne un tableau de 15.
    - buildFroidEvents(ctx): 3 dates (endDate +1 mois, +1 mois +15j, +2 mois) via Date.setMonth/setDate ; cle buildEventKey(code,'froid',i) ; colorId CALENDAR_COLORS.froid ; description = renderFroidText. Retourne un tableau de 3.
    Les builders sont PURS (pas d'appel API, pas de Prisma). HORAIRES injectes dans le ctx texte comme `${HORAIRES.matin} / ${HORAIRES.apresMidi}` (figes, jamais recalcules).
    Creer apps/web/src/lib/calendar/__tests__/event-builders.test.ts : (a) buildRappelEvents().length === 15 ; (b) buildFroidEvents().length === 3 ; (c) chaque event a extendedProperties.private.qualiof_key ; (d) formation colorId === '7', rappel === '6', froid === '3' ; (e) le rappel J-1 contient 'demain' ; (f) un rappel a reminders sans email (overrides method popup uniquement).
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/event-builders.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - event-builders.ts exporte buildFormationEvent(, buildRappelEvents(, buildFroidEvents(
    - buildRappelEvents retourne exactement 15 elements, buildFroidEvents exactement 3
    - Chaque requestBody contient extendedProperties.private.qualiof_key
    - colorId : formation '7', rappel '6', froid '3' (verifie par test)
    - Les rappels utilisent reminders.overrides method:'popup' sans method:'email' (grep + test)
    - pnpm vitest run src/lib/calendar/__tests__/event-builders.test.ts exit 0, >= 6 assertions
    - grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" event-builders.ts retourne 0 ligne
  </acceptance_criteria>
  <done>Builders purs (15 rappels + 3 froid + formation) avec cles/couleurs/attachments/reminders popup, testes verts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sync-session.ts (orchestrateur idempotent) + test</name>
  <read_first>
    - apps/web/src/lib/calendar/event-builders.ts (Task 1)
    - apps/web/src/lib/calendar/google-client.ts + idempotency.ts (Plan 14-01)
    - apps/web/src/lib/calendar/sync-state.ts (Plan 14-03)
    - apps/web/scripts/_google-test.ts (events.insert/list/delete usage)
    - .planning/phases/14-.../14-CONTEXT.md (sections "Invites (regle cle)", "Idempotent", "Modes")
  </read_first>
  <behavior>
    - syncSessionCalendar applique pour CHAQUE event construit la logique : lookup par cle (events.list privateExtendedProperty=qualiof_key=<cle>) -> si trouve, update (events.update) ; sinon insert (events.insert). Jamais de doublon.
    - Invites : trainerEmail toujours dans attendees. learnerEmails toujours dans attendees.
    - sendUpdates : si syncMode='backfill' et session passee -> 'none'. Si session future -> 'all' si toggle notifyLearners=true, sinon 'none'. Le formateur recoit selon le meme sendUpdates global de l'appel (la regle apprenants pilote sendUpdates ; le formateur est invite dans tous les cas).
    - Apres chaque insert/update, upsertSyncRecord trace (sessionId, eventKey, googleEventId, eventType, syncMode, sentUpdates).
    - Un second appel (re-run) sur la meme session ne fait que des update/skip (0 insert nouveau) car les cles existent deja.
  </behavior>
  <action>
    Creer apps/web/src/lib/calendar/sync-session.ts.
    Type d'entree SyncSessionInput : { tenantId; sessionId; ctx: SessionEventCtx; syncMode: 'backfill'|'auto'; isPastSession: boolean; notifyLearners: boolean }.
    Implementer syncSessionCalendar(input):
    1. const cal = getCalendarClient().
    2. Construire la liste complete d'events : [buildFormationEvent(ctx), ...buildRappelEvents(ctx), ...buildFroidEvents(ctx)].
    3. Calculer sendUpdates global : if (input.isPastSession) 'none'; else (input.notifyLearners ? 'all' : 'none'). (Regle CONTEXT.md : passe = none audit-only ; futur = toggle.)
    4. attendees = [{ email: ctx.trainerEmail }, ...ctx.learnerEmails.map(e=>({email:e}))] (formateur toujours present + apprenants toujours dans les invites).
    5. Pour chaque event : injecter attendees. Lookup via cal.events.list({ calendarId: CALENDAR_ID, privateExtendedProperty: `${QUALIOF_KEY_PROP}=${event.extendedProperties.private.qualiof_key}` }). Si items.length>0 -> cal.events.update({calendarId, eventId: items[0].id, sendUpdates, supportsAttachments:true, requestBody: event}). Sinon cal.events.insert({calendarId, sendUpdates, supportsAttachments:true, requestBody: event}).
    6. Apres insert/update, recuperer googleEventId et appeler upsertSyncRecord(...).
    7. Retourner un recap { inserted, updated, skipped, total }.
    Gestion d'erreur : try/catch par event (un echec n'arrete pas les autres), accumuler les erreurs dans le recap (errors:[]). Aucun import auth (worker-safe).
    Creer apps/web/src/lib/calendar/__tests__/sync-session.test.ts : mock getCalendarClient (events.list/insert/update) + mock sync-state.upsertSyncRecord. Tester : (a) 19 events traites au total (1+15+3) ; (b) 1er run = 19 insert ; (c) 2e run (events.list renvoie un item) = 19 update, 0 insert (preuve idempotence) ; (d) isPastSession=true -> sendUpdates 'none' sur tous les insert ; (e) future + notifyLearners=true -> sendUpdates 'all' ; (f) attendees contient toujours trainerEmail.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/sync-session.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - sync-session.ts exporte syncSessionCalendar(
    - L'orchestrateur fait events.list (privateExtendedProperty) AVANT insert -> update si trouve (idempotence Google)
    - Test prouve : 2e run = 0 insert, 19 update (re-run sans doublon)
    - sendUpdates = 'none' quand isPastSession, 'all' quand future + notifyLearners, 'none' quand future + !notifyLearners (verifie par test)
    - attendees contient toujours trainerEmail (verifie par test)
    - upsertSyncRecord appele apres chaque insert/update (verifie par test)
    - pnpm vitest run src/lib/calendar/__tests__/sync-session.test.ts exit 0, >= 6 assertions
    - grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" sync-session.ts retourne 0 ligne (worker-safe)
  </acceptance_criteria>
  <done>syncSessionCalendar idempotent (insert/update/skip par cle Google + trace base), regle invites/sendUpdates correcte, teste vert.</done>
</task>

</tasks>

<verification>
- cd apps/web && pnpm vitest run src/lib/calendar exit 0 (tout le dossier)
- Preuve d'idempotence dans sync-session.test.ts : 2e run = 0 insert nouveau
- grep -rE "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/calendar/event-builders.ts apps/web/src/lib/calendar/sync-session.ts retourne 0 ligne
- cd apps/web && pnpm tsc --noEmit (ou build) clean
</verification>

<success_criteria>
syncSessionCalendar produit 1 formation + 15 rappels countdown + 3 froid, idempotent, avec formateur invite reel, apprenants en invites (sendUpdates conditionnel), couleurs + pieces jointes + textes exacts. Reutilisable par backfill (14-05) et hook auto (14-06).
</success_criteria>

<output>
Apres completion, creer .planning/phases/14-.../14-04-SUMMARY.md.
</output>
