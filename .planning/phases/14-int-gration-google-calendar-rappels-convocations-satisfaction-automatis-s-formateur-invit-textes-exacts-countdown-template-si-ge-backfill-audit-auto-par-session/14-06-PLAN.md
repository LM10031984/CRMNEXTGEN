---
phase: 14-google-calendar
plan: 06
type: execute
wave: 4
depends_on: ["14-04", "14-05"]
files_modified:
  - apps/web/src/server/actions/calendar-sync.ts
  - apps/web/src/components/sessions/session-calendar-sync-toggle.tsx
  - apps/web/src/server/actions/__tests__/calendar-sync.test.ts
  - apps/web/src/app/app/sessions/[id]/page.tsx
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "Depuis la fiche session a venir, l'admin peut declencher la synchro Google Calendar avec un toggle envoyer reellement les invitations aux apprenants"
    - "La server action est un wrapper mince auth-gate (requireRole) au-dessus du core syncSessionCalendar (qui reste worker-safe)"
    - "Une nouvelle session planifiee peut etre synchronisee (bouton), en mode auto, avec le choix du toggle"
    - "Le re-run depuis l'UI ne cree pas de doublon (idempotence prouvee de bout en bout)"
    - "Chaque synchro via la server action ecrit un AuditLog sessions.calendarSynced (convention universelle [entity].[verb] du repo)"
  artifacts:
    - path: "apps/web/src/server/actions/calendar-sync.ts"
      provides: "syncSessionCalendarAction (wrapper auth) appelant le core + AuditLog sessions.calendarSynced"
      contains: "requireRole"
    - path: "apps/web/src/components/sessions/session-calendar-sync-toggle.tsx"
      provides: "Toggle + bouton Synchroniser l'agenda sur fiche session"
      contains: "syncSessionCalendarAction"
  key_links:
    - from: "session-calendar-sync-toggle.tsx"
      to: "syncSessionCalendarAction"
      via: "useTransition + sonner"
      pattern: "syncSessionCalendarAction"
    - from: "calendar-sync.ts"
      to: "syncSessionCalendar (core)"
      via: "wrapper mince apres requireRole + loadSessionEventCtx"
      pattern: "syncSessionCalendar\\("
    - from: "calendar-sync.ts"
      to: "prisma.auditLog"
      via: "auditLog.create apres succes du core"
      pattern: "sessions\\.calendarSynced"
---

<objective>
Brancher le mode "auto" : une server action auth-gatee (wrapper mince) au-dessus du core syncSessionCalendar, et une surface UI sur la fiche session (toggle "envoyer reellement aux apprenants" + bouton "Synchroniser l'agenda"). Verifier l'idempotence de bout en bout (re-run UI = pas de doublon).

Purpose: Rendre la synchro accessible a l'admin sur les nouvelles sessions sans CLI, en respectant strictement la frontiere auth/core (la server action porte requireRole, le core reste worker-safe).
Output: 1 server action wrapper (avec AuditLog) + 1 composant UI + 1 test + verification idempotence (checkpoint humain).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-int-gration-google-calendar-rappels-convocations-satisfaction-automatis-s-formateur-invit-textes-exacts-countdown-template-si-ge-backfill-audit-auto-par-session/14-CONTEXT.md

<interfaces>
Plan 14-04 : syncSessionCalendar(input) (core worker-safe), recap { inserted, updated, skipped, total }.
Plan 14-05 : loadSessionEventCtx(tenantId, sessionId).
Pattern server action repo (CONVENTIONS) : 'use server' au top, requireRole(['ADMIN','MANAGER']) -> User, retour discrimine { ok:true, ... } | { ok:false, error }, scope tenantId, revalidatePath.
Pattern AuditLog repo (CONVENTION UNIVERSELLE [entity].[verb]) : toute server action mutante ecrit un AuditLog. Forme exacte (cf sessions.ts updateSessionLogistics ligne 1146) : prisma.auditLog.create({ data: { tenantId: user.tenantId, userId: user.id, entity: 'TrainingSession', entityId: <sessionId>, action: '<entity>.<verb>', diff: {...} as Prisma.InputJsonValue } }). Ici action = 'sessions.calendarSynced'.
Pattern toggle UI existant : session-logistics-editor.tsx ('use client', useState + useTransition + toast sonner + action { ok }).
Regle invites (CONTEXT.md) : sessions a venir -> le toggle decide sendUpdates 'all' vs 'none' ; le formateur est toujours invite ; sessions passees -> trace seulement.
REGLE CRITIQUE worker auth (memory feedback) : NE JAMAIS importer une server action depuis le core/worker. Ici l'inverse est OK : la server action importe le core. Le core (sync-session.ts) ne doit JAMAIS importer cette action.
Fiche session : apps/web/src/app/app/sessions/[id]/page.tsx (inserer la section "Agenda / Rappels"). Chemin confirme reel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server action wrapper calendar-sync.ts (+ AuditLog) + test</name>
  <read_first>
    - apps/web/src/server/actions/sessions.ts (pattern server action + requireRole + revalidatePath, voir updateSessionLogistics ligne 645 ; pattern AuditLog `sessions.update` ligne 1146 : prisma.auditLog.create avec entity/entityId/action/diff)
    - apps/web/src/lib/rbac.ts (requireRole)
    - apps/web/src/lib/calendar/sync-session.ts + load-session-ctx.ts (core, Plans 14-04/05)
    - .planning/phases/14-.../14-CONTEXT.md (sections "Invites", "Modes : Auto")
  </read_first>
  <behavior>
    - syncSessionCalendarAction({ sessionId, notifyLearners }) : requireRole(['ADMIN','MANAGER']) -> user, charge ctx via loadSessionEventCtx(user.tenantId, sessionId), calcule isPastSession, appelle le core syncSessionCalendar avec syncMode 'auto', ecrit un AuditLog sessions.calendarSynced apres succes, retourne { ok:true, recap } ou { ok:false, error }.
    - Si ctx null ou trainerEmail vide -> { ok:false, error } sans appel API et SANS AuditLog (pas de mutation reussie a tracer).
    - revalidatePath de la fiche session apres succes.
  </behavior>
  <action>
    Creer apps/web/src/server/actions/calendar-sync.ts :
    - 'use server' en tete.
    - imports : requireRole from '@/lib/rbac' ; syncSessionCalendar from '@/lib/calendar/sync-session' ; loadSessionEventCtx from '@/lib/calendar/load-session-ctx' ; prisma + Prisma from '@qualiof/db' ; revalidatePath from 'next/cache'.
    - Export async function syncSessionCalendarAction(input: { sessionId: string; notifyLearners: boolean }) :
      1. const user = await requireRole(['ADMIN','MANAGER']).
      2. const ctx = await loadSessionEventCtx(user.tenantId, input.sessionId). if (!ctx || !ctx.trainerEmail) return { ok:false, error:'Session introuvable ou formateur sans email' }.
      3. const isPastSession = (ctx.endDate ?? ctx.startDate) < new Date().
      4. try {
           const recap = await syncSessionCalendar({ tenantId:user.tenantId, sessionId:input.sessionId, ctx, syncMode:'auto', isPastSession, notifyLearners:input.notifyLearners });
           // AuditLog : convention universelle repo [entity].[verb] (cf sessions.ts:1146)
           await prisma.auditLog.create({ data: { tenantId: user.tenantId, userId: user.id, entity: 'TrainingSession', entityId: input.sessionId, action: 'sessions.calendarSynced', diff: { recap, syncMode: 'auto', notifyLearners: input.notifyLearners } as Prisma.InputJsonValue } });
           revalidatePath('/app/sessions/' + input.sessionId);
           return { ok:true, recap };
         } catch(e){ return { ok:false, error:(e as Error).message }; }
    - Wrapper MINCE : aucune logique metier dupliquee, tout est dans le core (seul ajout cote action : auth-gate + AuditLog + revalidatePath, qui sont des responsabilites server-action, PAS du core worker-safe).
    Creer apps/web/src/server/actions/__tests__/calendar-sync.test.ts (mock rbac.requireRole, mock sync-session.syncSessionCalendar, mock load-session-ctx, mock @qualiof/db prisma.auditLog.create) : (a) requireRole appele avec ['ADMIN','MANAGER'] ; (b) ctx null -> { ok:false } sans appeler le core ET sans auditLog.create ; (c) succes -> core appele avec syncMode 'auto' et notifyLearners propage ; (d) tenantId du user passe au core (scope) ; (e) succes -> prisma.auditLog.create appele avec action 'sessions.calendarSynced' et entityId = sessionId.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/server/actions/__tests__/calendar-sync.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - calendar-sync.ts a 'use server' et exporte syncSessionCalendarAction(
    - requireRole(['ADMIN','MANAGER']) appele (verifie par test)
    - Le core syncSessionCalendar appele avec syncMode 'auto', tenantId du user, notifyLearners propage (verifie par test)
    - ctx null/sans email -> { ok:false } sans appel API et sans auditLog (verifie par test)
    - calendar-sync.ts contient action: 'sessions.calendarSynced' (grep -q "sessions.calendarSynced" src/server/actions/calendar-sync.ts retourne 0/match)
    - prisma.auditLog.create appele apres succes avec action 'sessions.calendarSynced' et entityId = sessionId (verifie par test)
    - Le core sync-session.ts n'importe PAS cette action (grep retourne 0)
    - pnpm vitest run src/server/actions/__tests__/calendar-sync.test.ts exit 0, >= 5 assertions
  </acceptance_criteria>
  <done>Server action wrapper mince auth-gatee testee verte, frontiere auth/core respectee, AuditLog sessions.calendarSynced ecrit (convention universelle [entity].[verb]).</done>
</task>

<task type="auto">
  <name>Task 2: UI toggle + bouton sur fiche session</name>
  <read_first>
    - apps/web/src/components/sessions/session-logistics-editor.tsx (pattern 'use client' + useState toggle + useTransition + sonner)
    - apps/web/src/server/actions/calendar-sync.ts (Task 1)
    - apps/web/src/app/app/sessions/[id]/page.tsx (point d'insertion sur la fiche session — chemin confirme reel)
    - .planning/phases/14-.../14-CONTEXT.md (section "Invites : Sessions a venir -> option a cocher")
  </read_first>
  <action>
    Creer apps/web/src/components/sessions/session-calendar-sync-toggle.tsx ('use client') :
    - Props : { sessionId: string; isPastSession: boolean }.
    - State notifyLearners (useState, defaut false) — checkbox "Envoyer reellement les invitations aux apprenants". ACTIVE seulement si !isPastSession. Pour une session passee : checkbox disabled + libelle "Session passee — invitations en trace uniquement (pas de notification)".
    - Bouton "Synchroniser l'agenda Google" -> useTransition -> await syncSessionCalendarAction({ sessionId, notifyLearners }). toast.success avec recap ('X crees, Y mis a jour') ou toast.error(error). Style coherent avec session-logistics-editor (bouton primary, Loader2 pending).
    - Inserer le composant dans la fiche session apps/web/src/app/app/sessions/[id]/page.tsx dans une section "Agenda / Rappels", en passant isPastSession calcule cote serveur (endDate < now).
    - Visibilite section : ADMIN/MANAGER (la server action re-verifie de toute facon).
  </action>
  <verify>
    <automated>cd apps/web && grep -q "syncSessionCalendarAction" src/components/sessions/session-calendar-sync-toggle.tsx && grep -q "session-calendar-sync-toggle" "src/app/app/sessions/[id]/page.tsx" && pnpm --filter @qualiof/web build 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - session-calendar-sync-toggle.tsx existe, 'use client', importe et appelle syncSessionCalendarAction
    - La checkbox notifyLearners est disabled quand isPastSession (verifie par lecture du composant)
    - Le composant est insere dans apps/web/src/app/app/sessions/[id]/page.tsx (grep session-calendar-sync-toggle dans ce fichier)
    - Toast succes affiche le recap (X crees / Y mis a jour)
    - pnpm --filter @qualiof/web build clean (route fiche session compile)
  </acceptance_criteria>
  <done>Toggle + bouton de synchro sur la fiche session (apps/web/src/app/app/sessions/[id]/page.tsx), branches sur la server action, build vert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification idempotence bout-en-bout (re-run = 0 doublon)</name>
  <action>Faire executer par Laurent la double-synchro depuis l'UI sur une session a venir (etapes dans how-to-verify) pour prouver l'idempotence : 1er run ~19 crees, 2e run 0 cree / ~19 mis a jour, 0 doublon dans l'agenda.</action>
  <read_first>
    - apps/web/src/lib/calendar/sync-session.ts (idempotence par cle Google)
    - apps/web/src/lib/calendar/sync-state.ts (table de trace)
    - .planning/phases/14-.../14-CONTEXT.md (section "Idempotent")
  </read_first>
  <what-built>
    Le bouton "Synchroniser l'agenda Google" sur la fiche d'une session a venir (idealement une session test type pilote SES-0097), declenchable 2 fois pour prouver l'idempotence.
  </what-built>
  <how-to-verify>
    1. Ouvrir la fiche d'une session a venir dans QualiOF. Verifier la section "Agenda / Rappels".
    2. Cliquer "Synchroniser l'agenda Google" (toggle OFF = pas de notif apprenants). Lire le toast : attendu ~19 crees (1 formation + 15 rappels + 3 froid).
    3. Ouvrir l'agenda Google « Rappel Formations » : verifier 1 evenement formation (bleu/colorId 7), 15 rappels quotidiens (orange/6) avec "dans X jours" decroissant, 3 relances froid (violet/3). Le formateur reel est invite. Les apprenants sont dans les invites.
    4. Re-cliquer "Synchroniser l'agenda Google". Lire le toast : attendu 0 cree, ~19 mis a jour (PREUVE idempotence). Verifier dans l'agenda : AUCUN doublon.
    5. Si lieu = siege Vence : verifier que la description contient le bloc accaccess/transports/restauration/hebergement etendu.
  </how-to-verify>
  <acceptance_criteria>
    - 1er run : ~19 evenements crees, couleurs correctes (7/6/3), formateur invite reel, apprenants en invites
    - Rappels : countdown "dans X jours" exact J-15 -> "demain" (J-1)
    - 2e run : 0 cree, ~19 mis a jour, AUCUN doublon dans l'agenda (idempotence prouvee)
    - Siege Vence : bloc etendu present quand applicable
    - Table SessionCalendarSync contient les lignes tracees pour la session
    - Un AuditLog sessions.calendarSynced est cree a chaque clic (verifiable via la page Historique)
  </acceptance_criteria>
  <resume-signal>Taper "approved" apres avoir constate 19 crees au 1er run, 0 doublon au 2e run, couleurs/invites/countdown corrects, OU decrire les ecarts.</resume-signal>
</task>

</tasks>

<verification>
- cd apps/web && pnpm vitest run src/server/actions/__tests__/calendar-sync.test.ts exit 0
- cd apps/web && pnpm --filter @qualiof/web build clean
- AuditLog : grep "sessions.calendarSynced" src/server/actions/calendar-sync.ts retourne 1 (convention [entity].[verb] respectee)
- Frontiere auth/core : grep "calendar-sync" dans src/lib/calendar/sync-session.ts retourne 0 (le core n'importe pas l'action)
- Checkpoint idempotence : 2e run UI = 0 doublon
</verification>

<success_criteria>
Mode auto operationnel via UI (toggle apprenants + bouton synchro), wrapper auth mince au-dessus du core worker-safe, AuditLog sessions.calendarSynced ecrit a chaque synchro (convention universelle), idempotence prouvee de bout en bout (re-run = 0 doublon). Phase 14 fonctionnellement complete.
</success_criteria>

<output>
Apres completion, creer .planning/phases/14-.../14-06-SUMMARY.md + un 14-SMOKE.md recapitulant la verification idempotence (1er run / 2e run / couleurs / invites / siege).
</output>
