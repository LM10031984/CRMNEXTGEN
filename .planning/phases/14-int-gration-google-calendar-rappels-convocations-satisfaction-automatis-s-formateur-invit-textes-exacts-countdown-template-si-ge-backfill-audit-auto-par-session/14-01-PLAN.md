---
phase: 14-google-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/calendar/google-client.ts
  - apps/web/src/lib/calendar/colors.ts
  - apps/web/src/lib/calendar/idempotency.ts
  - apps/web/src/lib/calendar/__tests__/idempotency.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Le worker peut obtenir un client Google Calendar authentifié depuis files/secrets/ sans aucun import server-action/rbac/react"
    - "Chaque événement reçoit une clé déterministe stable par (session, type, jour) qui permet un re-run sans doublon"
    - "Les 3 codes couleur Google sont centralisés en constantes (formation=7, rappel=6, froid=3)"
  artifacts:
    - path: "apps/web/src/lib/calendar/google-client.ts"
      provides: "getCalendarClient() lit oauth-client.json + google-token.json, retourne un calendar v3 + CALENDAR_ID exporté"
      contains: "google.calendar"
    - path: "apps/web/src/lib/calendar/idempotency.ts"
      provides: "buildEventKey(sessionCode, type, dayIndex) → string déterministe"
      contains: "qualiof"
    - path: "apps/web/src/lib/calendar/colors.ts"
      provides: "CALENDAR_COLORS map des 3 colorId"
      contains: "colorId"
  key_links:
    - from: "google-client.ts"
      to: "files/secrets/google-token.json"
      via: "fs.readFileSync refresh_token"
      pattern: "refresh_token"
---

<objective>
Poser la fondation worker-safe d'accès Google Calendar pour la Phase 14 : un client authentifié réutilisable (depuis les secrets OAuth déjà en place), un helper d'idempotence (clé déterministe par événement) et les constantes de couleur. Aucun appel à l'API n'est encore fait côté métier — c'est le socle que les builders et l'orchestrateur (Plan 14-04) consommeront.

Purpose: Centraliser l'auth + l'idempotence + les couleurs dans un module pur, sans import auth (règle worker BullMQ), pour que tout le reste de la phase s'appuie dessus sans réinventer.
Output: 3 modules lib + 1 fichier de test sur la clé d'idempotence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-int-gration-google-calendar-rappels-convocations-satisfaction-automatis-s-formateur-invit-textes-exacts-countdown-template-si-ge-backfill-audit-auto-par-session/14-CONTEXT.md

<interfaces>
<!-- googleapis usage déjà prouvé dans apps/web/scripts/_google-test.ts (NE PAS le modifier). -->
<!-- Pattern d'auth à reproduire (cwd = apps/web, secrets résolus en ../../secrets) : -->
```typescript
// _google-test.ts lignes 1-9 (référence, à transposer dans google-client.ts)
import fs from 'node:fs'; import path from 'node:path';
import { google } from 'googleapis';
const SECRETS = path.resolve(process.cwd(), '../../secrets');
const c = JSON.parse(fs.readFileSync(path.join(SECRETS,'oauth-client.json'),'utf8'));
const { client_id, client_secret } = c.installed ?? c.web ?? c;
const tok = JSON.parse(fs.readFileSync(path.join(SECRETS,'google-token.json'),'utf8'));
const auth = new google.auth.OAuth2(client_id, client_secret);
auth.setCredentials({ refresh_token: tok.refresh_token });
const cal = google.calendar({ version:'v3', auth });
```
<!-- googleapis ^173.0.0 déjà installé (apps/web/package.json) — pas de nouvelle dépendance. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: google-client.ts (auth worker-safe) + colors.ts</name>
  <read_first>
    - apps/web/scripts/_google-test.ts (pattern d'auth googleapis exact, lignes 1-9)
    - apps/web/scripts/_google-oauth-setup.ts (scope + chemin secrets ../../secrets)
    - apps/web/src/lib/closure/redis.ts (style module lib singleton du repo)
    - .planning/phases/14-.../14-CONTEXT.md (section "Accès Google" + couleurs)
  </read_first>
  <action>
    Créer `apps/web/src/lib/calendar/google-client.ts` :
    - Export `const CALENDAR_ID = 'c_a18d08db6df83139c06c26e91e4cdb59ac244baeac93fe5c237d66c628a578a5@group.calendar.google.com';`
    - Export `function getCalendarClient()` qui reproduit le pattern de `_google-test.ts` (lignes 1-9) : lit `oauth-client.json` + `google-token.json` depuis `path.resolve(process.cwd(), '../../secrets')`, construit `new google.auth.OAuth2(client_id, client_secret)`, `setCredentials({ refresh_token })`, retourne `google.calendar({ version:'v3', auth })`.
    - Singleton léger optionnel (mémoïser le client comme dans redis.ts) — acceptable mais non obligatoire.
    - Import UNIQUEMENT : `node:fs`, `node:path`, `googleapis`. INTERDIT : tout import depuis `@/server/actions`, `@/lib/rbac`, `@/lib/auth`, `validateRequest`, `requireRole`, ou `from 'react'` (règle worker BullMQ — sinon crash `react does not provide an export named 'cache'` au boot tsx).
    - Commentaire en tête expliquant la règle worker-safe + que les secrets sont gitignored.

    Créer `apps/web/src/lib/calendar/colors.ts` :
    - Export `const CALENDAR_COLORS = { formation: '7', rappel: '6', froid: '3' } as const;` (7=Paon/bleu, 6=Mandarine/orange, 3=Raisin/violet — cf CONTEXT.md "Couleurs"). Les colorId Google sont des strings.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "c_a18d08db6df83139c06c26e91e4cdb59ac244baeac93fe5c237d66c628a578a5" src/lib/calendar/google-client.ts && grep -Eq "colorId|formation: '7'" src/lib/calendar/colors.ts && ! grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" src/lib/calendar/google-client.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/calendar/google-client.ts` exporte `getCalendarClient(` et `CALENDAR_ID`
    - Le fichier contient la string exacte de l'id agenda `c_a18d08db6df83139c06c26e91e4cdb59ac244baeac93fe5c237d66c628a578a5@group.calendar.google.com`
    - `grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/calendar/google-client.ts` retourne 0 ligne (worker-safe)
    - `apps/web/src/lib/calendar/colors.ts` contient `formation: '7'`, `rappel: '6'`, `froid: '3'`
  </acceptance_criteria>
  <done>getCalendarClient() existe, worker-safe (0 import interdit), CALENDAR_ID + CALENDAR_COLORS exportés.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: idempotency.ts (clé déterministe) + test</name>
  <read_first>
    - apps/web/src/lib/closure/queue.ts (pattern jobId déterministe `${kind}:${participantId}` ligne 36)
    - .planning/phases/14-.../14-CONTEXT.md (section "Idempotent")
    - apps/web/src/lib/calendar/colors.ts (créé Task 1, même dossier)
  </read_first>
  <behavior>
    - buildEventKey('SES-0097', 'formation') → 'qualiof_ses-0097_formation' (appel SANS dayIndex pour formation — clé unique par session ; cohérent avec acceptance_criteria)
    - buildEventKey('SES-0097', 'rappel', 3) → 'qualiof_ses-0097_rappel_3' (dayIndex inclus)
    - buildEventKey('SES-0097', 'froid', 1) → 'qualiof_ses-0097_froid_1'
    - Même entrée → même sortie (déterministe, lowercase code, pas de timestamp)
    - La clé ne contient que [a-z0-9_-] (compatible extendedProperties.private)
  </behavior>
  <action>
    Créer `apps/web/src/lib/calendar/idempotency.ts` :
    - Export `type CalendarEventType = 'formation' | 'rappel' | 'froid';`
    - Export `function buildEventKey(sessionCode: string, type: CalendarEventType, dayIndex?: number): string`.
      Format : `qualiof_${sessionCode.toLowerCase()}_${type}` puis, si `dayIndex !== undefined`, suffixer `_${dayIndex}`. Pour `formation`, appeler sans dayIndex (clé unique par session).
      Aucune date/horodatage dans la clé (sinon non idempotent au re-run).
    - Export `const QUALIOF_KEY_PROP = 'qualiof_key';` (nom de la propriété sous `extendedProperties.private` que l'orchestrateur Plan 14-04 posera et requêtera).
    - Le helper sera utilisé en double : (a) écrit dans `extendedProperties.private.qualiof_key` à l'insert, (b) requêté via `events.list({ privateExtendedProperty: 'qualiof_key=<clé>' })` pour décider insert vs update vs skip.
    Créer `apps/web/src/lib/calendar/__tests__/idempotency.test.ts` couvrant les 5 cas du bloc behavior (Vitest, même style que les tests du repo : `import { describe, it, expect } from 'vitest'`).
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/idempotency.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `idempotency.ts` exporte `buildEventKey(`, `QUALIOF_KEY_PROP`, et le type `CalendarEventType`
    - `buildEventKey('SES-0097','formation')` renvoie exactement `qualiof_ses-0097_formation`
    - `buildEventKey('SES-0097','rappel',3)` renvoie exactement `qualiof_ses-0097_rappel_3`
    - `pnpm vitest run src/lib/calendar/__tests__/idempotency.test.ts` exit code 0, ≥ 5 assertions
    - `QUALIOF_KEY_PROP` vaut `'qualiof_key'`
  </acceptance_criteria>
  <done>buildEventKey déterministe testé vert, QUALIOF_KEY_PROP exporté.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm vitest run src/lib/calendar` exit 0
- `grep -rE "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/calendar/*.ts` retourne 0 ligne (worker-safe sur tout le dossier)
- `cd apps/web && pnpm tsc --noEmit` (ou `pnpm --filter @qualiof/web build`) clean sur les nouveaux fichiers
</verification>

<success_criteria>
getCalendarClient() worker-safe opérationnel, clé d'idempotence déterministe testée, couleurs centralisées. Socle prêt pour Plan 14-04.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/14-.../14-01-SUMMARY.md`.
</output>
