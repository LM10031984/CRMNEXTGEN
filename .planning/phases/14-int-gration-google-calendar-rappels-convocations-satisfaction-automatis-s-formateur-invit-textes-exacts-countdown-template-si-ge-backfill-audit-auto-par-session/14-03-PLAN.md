---
phase: 14-google-calendar
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/
  - apps/web/src/lib/calendar/sync-state.ts
  - apps/web/src/lib/calendar/__tests__/sync-state.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "L'état de synchro calendrier par session est traçable en base (quels eventId Google créés, quand, par quel mode)"
    - "Un re-run lit l'état existant et peut décider skip/update sans re-créer (deuxième filet d'idempotence en plus de la clé Google)"
    - "La migration est appliquée à Postgres local via prisma migrate deploy (pas juste generate)"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "model SessionCalendarSync (sessionId, eventKey, googleEventId, eventType, syncMode, sentUpdates, timestamps)"
      contains: "model SessionCalendarSync"
    - path: "apps/web/src/lib/calendar/sync-state.ts"
      provides: "upsertSyncRecord / getSyncRecordsForSession — wrappers Prisma scopes tenantId"
      contains: "sessionCalendarSync"
  key_links:
    - from: "sync-state.ts"
      to: "prisma.sessionCalendarSync"
      via: "upsert par (sessionId, eventKey)"
      pattern: "sessionCalendarSync\\.(upsert|findMany)"
---

<objective>
Ajouter une table Prisma additive pour tracer, par session, les evenements Google Calendar crees (cle + googleEventId + type + mode de synchro + sendUpdates applique). C'est le second filet d'idempotence (en complement de la cle extendedProperties.private cote Google) et la trace d'audit Qualiopi du backfill.

Purpose: Permettre un re-run sur (skip/update au lieu de dupliquer), garder une preuve d'audit (quelle session a quels evenements, quand, en quel mode), et debloquer l'orchestrateur Plan 14-04.
Output: 1 model Prisma + migration appliquee + module wrapper Prisma tenant-scope + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-int-gration-google-calendar-rappels-convocations-satisfaction-automatis-s-formateur-invit-textes-exacts-countdown-template-si-ge-backfill-audit-auto-par-session/14-CONTEXT.md

<interfaces>
Modele TrainingSession existant (schema.prisma:427) — la nouvelle table reference sessionId + tenantId.
Convention repo : tout model porte tenantId (multi-tenant), @@index pertinents.
Migration : memory feedback "prisma generate != prisma migrate" -> DOIT executer prisma migrate deploy sur Postgres local.
En sandbox, migrate dev est interactif -> utiliser db push --skip-generate puis generate, OU migrate deploy si une migration SQL est creee hors-sandbox.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: model SessionCalendarSync + migration appliquee</name>
  <read_first>
    - packages/db/prisma/schema.prisma (model TrainingSession ligne 427, conventions models/index, model Notification recent pour le style additif)
    - apps/web/src/lib/calendar/idempotency.ts (CalendarEventType + format de cle, Plan 14-01)
    - .planning/phases/14-.../14-CONTEXT.md (sections "Idempotent", "Modes")
  </read_first>
  <action>
    Dans packages/db/prisma/schema.prisma, ajouter un model ADDITIF (ne toucher a aucun model existant sauf ajout de la relation inverse sur TrainingSession).

    Champs du model SessionCalendarSync :
    - id String @id @default(uuid())
    - tenantId String
    - sessionId String + relation session TrainingSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
    - eventKey String   (= buildEventKey(...) deterministe du Plan 14-01)
    - googleEventId String   (id retourne par events.insert)
    - eventType String   ('formation' | 'rappel' | 'froid')
    - dayIndex Int?   (index du rappel quotidien / relance froid)
    - syncMode String   ('backfill' | 'auto')
    - sentUpdates String   ('all' | 'none' — sendUpdates applique a l'insert)
    - createdAt DateTime @default(now())
    - updatedAt DateTime @updatedAt
    - @@unique([sessionId, eventKey])
    - @@index([tenantId, sessionId])

    Ajouter sur TrainingSession la relation inverse : calendarSyncs SessionCalendarSync[]

    Generer + appliquer la migration sur Postgres LOCAL :
    - Hors sandbox : pnpm --filter @qualiof/db exec prisma migrate dev --name phase14_session_calendar_sync , puis confirmer avec prisma migrate deploy.
    - En sandbox (migrate dev interactif plante — memory feedback) : pnpm --filter @qualiof/db exec prisma db push --skip-generate puis pnpm --filter @qualiof/db exec prisma generate. Documenter dans le SUMMARY que prisma migrate deploy reste a faire avant cloud (dette connue, memory feedback "prisma migrate deploy").
    Verifier que le client Prisma regenere expose prisma.sessionCalendarSync.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "model SessionCalendarSync" packages/db/prisma/schema.prisma && grep -q "calendarSyncs SessionCalendarSync" packages/db/prisma/schema.prisma && echo SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contient model SessionCalendarSync avec @@unique([sessionId, eventKey])
    - TrainingSession contient la relation inverse calendarSyncs SessionCalendarSync[]
    - Le model expose tenantId, eventKey, googleEventId, eventType, syncMode, sentUpdates
    - prisma generate a tourne (prisma.sessionCalendarSync disponible) — verifie par sync-state.test.ts en Task 2
    - SUMMARY note l'etat reel de la migration (deploy fait OU db push + dette deploy)
  </acceptance_criteria>
  <done>Table additive en place, migration appliquee en local, client regenere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sync-state.ts (wrappers Prisma tenant-scopes) + test</name>
  <read_first>
    - packages/db/src/index.ts (singleton prisma export du repo)
    - apps/web/src/lib/closure/queue.ts (style wrapper lib + jobId idempotent)
    - apps/web/src/lib/calendar/idempotency.ts (buildEventKey, Plan 14-01)
    - .planning/phases/14-.../14-CONTEXT.md
  </read_first>
  <behavior>
    - upsertSyncRecord cree une ligne si (sessionId, eventKey) absent, sinon met a jour googleEventId/sentUpdates/updatedAt (jamais de doublon)
    - getSyncRecordsForSession(tenantId, sessionId) ne renvoie que les lignes du tenant scope (filtre tenantId obligatoire)
    - Un appel upsert repete avec la meme cle ne cree pas de seconde ligne
  </behavior>
  <action>
    Creer apps/web/src/lib/calendar/sync-state.ts :
    - import { prisma } from '@qualiof/db'
    - Export async function upsertSyncRecord(rec: { tenantId; sessionId; eventKey; googleEventId; eventType; dayIndex?; syncMode; sentUpdates }): Promise<void> — utilise prisma.sessionCalendarSync.upsert avec where { sessionId_eventKey: { sessionId, eventKey } } (cle composite @@unique).
    - Export async function getSyncRecordsForSession(tenantId: string, sessionId: string) — prisma.sessionCalendarSync.findMany({ where: { tenantId, sessionId } }). Le filtre tenantId est OBLIGATOIRE (defense-in-depth multi-tenant, convention repo).
    - INTERDIT : import server-action/rbac/validateRequest/requireRole/react (worker-safe, ce module est appele par les scripts/worker du Plan 14-04/05).
    Creer apps/web/src/lib/calendar/__tests__/sync-state.test.ts : mock de @qualiof/db (vi.mock) verifiant (a) upsert appele avec la cle composite, (b) findMany toujours appele avec tenantId dans le where, (c) double upsert meme cle = where identique. Suivre le style de mock Prisma deja utilise dans le repo (cf tests qualiopi-matrix / lead-notifications).
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/sync-state.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - sync-state.ts exporte upsertSyncRecord( et getSyncRecordsForSession(
    - upsert utilise la cle composite where { sessionId_eventKey: ... }
    - getSyncRecordsForSession passe tenantId dans le where (verifie par le test)
    - grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/calendar/sync-state.ts retourne 0 ligne
    - pnpm vitest run src/lib/calendar/__tests__/sync-state.test.ts exit 0, >= 3 assertions
  </acceptance_criteria>
  <done>Wrappers Prisma tenant-scopes worker-safe testes verts.</done>
</task>

</tasks>

<verification>
- grep model SessionCalendarSync schema.prisma OK + relation inverse OK
- cd apps/web && pnpm vitest run src/lib/calendar/__tests__/sync-state.test.ts exit 0
- prisma.sessionCalendarSync disponible (client regenere)
- grep -rE "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" apps/web/src/lib/calendar/sync-state.ts retourne 0 ligne (worker-safe)
</verification>

<success_criteria>
Table de synchro additive en place + migration appliquee en local + wrappers tenant-scopes worker-safe testes. Second filet d'idempotence + trace d'audit prets pour Plan 14-04.
</success_criteria>

<output>
Apres completion, creer .planning/phases/14-.../14-03-SUMMARY.md (noter explicitement l'etat de la migration : deploy fait OU db push + dette deploy).
</output>
