---
phase: 14-google-calendar
plan: 05
type: execute
wave: 3
depends_on: ["14-04"]
files_modified:
  - apps/web/scripts/calendar-purge-broken.ts
  - apps/web/scripts/calendar-backfill.ts
  - apps/web/src/lib/calendar/load-session-ctx.ts
  - apps/web/src/lib/calendar/__tests__/load-session-ctx.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "Les ~350 evenements .ics casses (sans invites) de l'agenda Rappel Formations sont purges via l'API avant le backfill propre"
    - "Le backfill cree les evenements pour toutes les sessions startDate >= 2025-03-01, SEQUENTIELLEMENT (pas en parallele), idempotent"
    - "Pour une session, le contexte calendrier (formateur principal email, emails apprenants, lieu, produit, dates, programme Drive) est charge depuis Prisma de maniere fiable"
  artifacts:
    - path: "apps/web/scripts/calendar-purge-broken.ts"
      provides: "Script jetable DRY-by-default qui liste puis supprime les events sans attendees"
      contains: "events.delete"
    - path: "apps/web/scripts/calendar-backfill.ts"
      provides: "Script one-shot sequentiel sessions >= 2025-03-01 appelant syncSessionCalendar"
      contains: "syncSessionCalendar"
    - path: "apps/web/src/lib/calendar/load-session-ctx.ts"
      provides: "loadSessionEventCtx(sessionId) -> SessionEventCtx depuis Prisma"
      contains: "trainers"
  key_links:
    - from: "calendar-backfill.ts"
      to: "syncSessionCalendar"
      via: "boucle sequentielle for...of await (pas Promise.all)"
      pattern: "for .* of .*sessions"
---

<objective>
Livrer les deux scripts operationnels du mode "backfill audit" : (1) purge des ~350 evenements .ics casses, (2) backfill sequentiel idempotent de toutes les sessions >= mars 2025 via syncSessionCalendar. Plus un helper de chargement du contexte session depuis Prisma, partage avec le hook auto (14-06).

Purpose: Produire la preuve d'audit Qualiopi (agenda propre + tous les rappels/convocations/froid retro-crees) sans doublon, en respectant la lecon "sequentiel pas parallele" (deadlocks closureBatch en parallele — memory).
Output: 2 scripts + 1 helper lib + 1 test. Checkpoint humain pour valider la purge avant suppression reelle.

Note AuditLog (exemption assumee) : les deux scripts (calendar-purge-broken.ts, calendar-backfill.ts) sont des scripts one-shot tsx, PAS des server actions mutantes — la convention universelle repo AuditLog [entity].[verb] ne s'applique donc pas ici (pas d'utilisateur authentifie, pas de requireRole). La synchro auto via UI (Plan 14-06) ecrit, elle, un AuditLog sessions.calendarSynced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-int-gration-google-calendar-rappels-convocations-satisfaction-automatis-s-formateur-invit-textes-exacts-countdown-template-si-ge-backfill-audit-auto-par-session/14-CONTEXT.md

<interfaces>
Plan 14-01 : getCalendarClient(), CALENDAR_ID.
Plan 14-04 : syncSessionCalendar(input), type SessionEventCtx.
Script reference scope : apps/web/scripts/_sessions-since-march2025.ts (where startDate>=2025-03-01, comptage past/future, froid encore en cours).
Script reference trainer email : apps/web/scripts/_ses0097-trainer-email.ts (trainers { isPrimary, person { firstName,lastName,email } }).
Script reference idempotent DRY/WRITE : apps/web/scripts/_align-ses0097.ts (pattern WRITE=process.env.WRITE==='1', find-or-create, --dry par defaut).
Lecon memory "generation masse" : runs PARALLELES = deadlocks -> generer SEQUENTIEL (for...of await), rattrapage sequentiel des echecs.
Scripts wires via dotenv -e ../../.env -- tsx (cf apps/web/package.json scripts worker:*).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: load-session-ctx.ts (contexte session depuis Prisma) + test</name>
  <read_first>
    - apps/web/scripts/_ses0097-trainer-email.ts (extraction trainers + email)
    - apps/web/scripts/_align-ses0097.ts (champs session/location/participant)
    - packages/db/prisma/schema.prisma (TrainingSession:427, SessionTrainer:487, SessionParticipant:586, Location:473, Person:139)
    - apps/web/src/lib/calendar/event-builders.ts (type SessionEventCtx attendu, Plan 14-04)
  </read_first>
  <behavior>
    - loadSessionEventCtx(tenantId, sessionId) renvoie un SessionEventCtx complet : code, name, productTitle, startDate, endDate, locationName, locationAddressText, trainerEmail/trainerName (formateur isPrimary, fallback 1er trainer), learnerEmails (participants avec email non nul, dedupliques).
    - Si pas de formateur principal -> prend le 1er trainer ; si aucun trainer -> trainerEmail vide + flag dans le retour pour que le script log un warning.
    - locationAddressText concatene l'adresse JSON du Location en string pour isSiegeVence.
  </behavior>
  <action>
    Creer apps/web/src/lib/calendar/load-session-ctx.ts :
    - import { prisma } from '@qualiof/db'.
    - Export async function loadSessionEventCtx(tenantId: string, sessionId: string): Promise<SessionEventCtx | null>.
      prisma.trainingSession.findFirst({ where: { id: sessionId, tenantId }, select: { code, name, startDate, endDate, product:{select:{title}}, location:{select:{name,address}}, trainers:{select:{isPrimary,person:{select:{firstName,lastName,email}}}}, participants:{select:{person:{select:{email}}}} } }).
      Formateur : trainers.find(isPrimary) ?? trainers[0]. trainerEmail = person.email ?? '' (log si vide). trainerName = `${firstName} ${lastName}`.
      learnerEmails = participants.map(p=>p.person.email).filter(Boolean) dedup via Set.
      locationAddressText : flatten address Json (street/postalCode/city) -> `${name} — ${street}, ${postalCode} ${city}`.
      programmeDriveId : laisser optionnel/undefined ici (le programme PDF Drive de la session peut etre branche plus tard ; documenter la TODO). Si une source existe deja en base (Document type PROGRAMME entityId=session avec driveId), la lire ; sinon undefined.
    - Worker-safe : aucun import auth.
    Creer apps/web/src/lib/calendar/__tests__/load-session-ctx.test.ts (mock @qualiof/db) : (a) formateur isPrimary choisi, (b) fallback 1er trainer si pas de primary, (c) learnerEmails dedupliques sans null, (d) where contient tenantId + sessionId.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/lib/calendar/__tests__/load-session-ctx.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - load-session-ctx.ts exporte loadSessionEventCtx(
    - Le findFirst where contient tenantId ET id session (verifie par test)
    - Formateur principal = isPrimary, fallback trainers[0] (verifie par test)
    - learnerEmails filtre les null et deduplique (verifie par test)
    - pnpm vitest run ... exit 0, >= 4 assertions
    - grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react)" load-session-ctx.ts retourne 0 ligne
  </acceptance_criteria>
  <done>Contexte session charge fiablement depuis Prisma, formateur reel + apprenants resolus, teste vert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Purge des ~350 evenements .ics casses</name>
  <action>Creer apps/web/scripts/calendar-purge-broken.ts (decrit ci-dessous dans what-built), lancer le DRY, faire valider l'echantillon par Laurent, puis executer WRITE=1 pour supprimer. Ne supprimer aucun event portant extendedProperties.private.qualiof_key.</action>
  <read_first>
    - apps/web/scripts/_google-test.ts (events.list/delete + sendUpdates:'none')
    - apps/web/scripts/_align-ses0097.ts (pattern DRY/WRITE)
    - .planning/phases/14-.../14-CONTEXT.md (section "Pre-requis d'execution : Purge")
  </read_first>
  <what-built>
    Script apps/web/scripts/calendar-purge-broken.ts cree :
    - Liste tous les events de l'agenda CALENDAR_ID (cal.events.list pagine via pageToken).
    - Identifie les "casses" = events SANS attendees ET SANS extendedProperties.private.qualiof_key (ceux importes par .ics, non geres par QualiOF). Critere a confirmer au run reel.
    - DRY par defaut : affiche le compte + un echantillon. WRITE=1 supprime via cal.events.delete({ calendarId, eventId, sendUpdates:'none' }) SEQUENTIELLEMENT.
    Wire script dans apps/web/package.json : "calendar:purge": "dotenv -e ../../.env -- tsx scripts/calendar-purge-broken.ts".
  </what-built>
  <how-to-verify>
    1. Lancer le DRY : cd apps/web && pnpm calendar:purge  -> verifier le compte annonce (attendu ~350) et l'echantillon (events sans invites).
    2. Confirmer qu'AUCUN event QualiOF (avec qualiof_key) n'est dans la liste a supprimer.
    3. Si OK, lancer la suppression reelle : WRITE=1 pnpm calendar:purge.
    4. Re-lancer le DRY : le compte de casses doit etre 0.
  </how-to-verify>
  <acceptance_criteria>
    - calendar-purge-broken.ts existe, DRY par defaut, WRITE=1 pour supprimer
    - Le script pagine (pageToken) pour couvrir tous les events
    - Critere de "casse" exclut tout event portant extendedProperties.private.qualiof_key (jamais supprimer un event QualiOF)
    - Script "calendar:purge" present dans apps/web/package.json
    - delete utilise sendUpdates:'none' (pas de notif aux apprenants lors de la purge)
  </acceptance_criteria>
  <resume-signal>Taper "approved" apres avoir verifie le DRY puis lance WRITE=1 et constate 0 casse restant, OU decrire les anomalies (faux positifs, events QualiOF dans la liste).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: calendar-backfill.ts (one-shot sequentiel idempotent)</name>
  <read_first>
    - apps/web/scripts/_sessions-since-march2025.ts (where + comptage past/future)
    - apps/web/src/lib/calendar/load-session-ctx.ts (Task 1)
    - apps/web/src/lib/calendar/sync-session.ts (syncSessionCalendar, Plan 14-04)
    - apps/web/scripts/_align-ses0097.ts (pattern DRY/WRITE sequentiel)
    - .planning/phases/14-.../14-CONTEXT.md (sections "Modes : Backfill", "Invites")
    - MEMORY : "generation masse" (sequentiel obligatoire, rattrapage sequentiel des echecs)
  </read_first>
  <action>
    Creer apps/web/scripts/calendar-backfill.ts :
    - const TENANT (depuis env ou la constante connue du repo, cf _align-ses0097.ts TENANT='db191440-...').
    - prisma.trainingSession.findMany({ where:{ tenantId:TENANT, startDate:{ gte:new Date('2025-03-01') } }, select:{ id, code, startDate, endDate }, orderBy:{ startDate:'asc' } }).
    - NOW = new Date(). Pour CHAQUE session, SEQUENTIELLEMENT (for...of avec await — JAMAIS Promise.all, lecon memory deadlocks) :
      * ctx = await loadSessionEventCtx(TENANT, session.id). Si null ou trainerEmail vide -> log warning + skip (compter dans "skipped").
      * isPastSession = (endDate ?? startDate) < NOW.
      * notifyLearners = false par defaut au backfill (les apprenants sont ajoutes en trace, sendUpdates:'none' impose par isPastSession ; pour les sessions futures incluses dans le backfill on garde notifyLearners=false pour ne pas spammer — le toggle reel se fait via le hook auto 14-06).
      * DRY par defaut : log ce qui serait fait. WRITE=1 : await syncSessionCalendar({ tenantId:TENANT, sessionId:session.id, ctx, syncMode:'backfill', isPastSession, notifyLearners:false }).
      * Accumuler recap (inserted/updated/skipped/errors) ; en cas d'erreur sur une session, LOG et CONTINUER (try/catch par session — un echec ne tue pas le run, lecon memory).
      * QUOTA Calendar : ~70 sessions x ~19 events ~= 2660 appels (sous le quota 10k/jour, mais sans garde). Apres chaque session traitee, attendre un delai minimal : await new Promise(r => setTimeout(r, 200)) (en mode WRITE uniquement) pour lisser la charge et limiter le risque de 429.
      * RE-RUN PARTIEL : si une session echoue (ex. 429 ou erreur reseau), elle reste rejouable seule (SESSION_CODE=... WRITE=1 pnpm calendar:backfill ou --only) ; grace a l'idempotence par cle (syncSessionCalendar insert/update), un re-run d'une session deja traitee ne cree aucun doublon — il fait du update/skip.
    - Afficher un recap final + la liste des sessions en echec (pour rattrapage sequentiel cible : SESSION_CODE=... WRITE=1 pnpm calendar:backfill ou un flag --only).
    - Wire "calendar:backfill": "dotenv -e ../../.env -- tsx scripts/calendar-backfill.ts" dans apps/web/package.json.
    Idempotence : le script peut etre relance sans creer de doublon (syncSessionCalendar fait insert/update par cle).
  </action>
  <verify>
    <automated>cd apps/web && grep -q "for .* of" scripts/calendar-backfill.ts && grep -q "syncSessionCalendar" scripts/calendar-backfill.ts && ! grep -q "Promise.all" scripts/calendar-backfill.ts && grep -q "calendar:backfill" package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - calendar-backfill.ts itere les sessions startDate >= 2025-03-01 SEQUENTIELLEMENT (for...of await), grep confirme absence de Promise.all
    - Appelle syncSessionCalendar avec syncMode:'backfill' et isPastSession calcule
    - try/catch par session (un echec continue le run) + recap des echecs pour rattrapage
    - DRY par defaut, WRITE=1 pour persister
    - Script "calendar:backfill" present dans apps/web/package.json
    - En mode WRITE, un delai minimal entre sessions (setTimeout ~200ms) lisse la charge API Calendar (garde quota/429)
    - Re-run partiel sur, idempotent : rejouer une session deja traitee ne cree aucun doublon (insert/update par cle) — permet le rattrapage cible apres un 429
  </acceptance_criteria>
  <done>Backfill sequentiel idempotent operationnel, DRY-first, resilient aux echecs par session.</done>
</task>

</tasks>

<verification>
- cd apps/web && pnpm vitest run src/lib/calendar/__tests__/load-session-ctx.test.ts exit 0
- DRY purge : ~350 casses listes, 0 event qualiof_key dans la liste ; apres WRITE=1 : 0 casse restant (checkpoint)
- DRY backfill : nombre de sessions = celui de _sessions-since-march2025.ts ; pas de Promise.all
- grep -rE "(server/actions|/rbac|validateRequest|requireRole)" apps/web/src/lib/calendar/load-session-ctx.ts retourne 0 ligne
</verification>

<success_criteria>
Agenda purge des .ics casses + backfill sequentiel idempotent de toutes les sessions >= mars 2025, avec formateur reel invite et apprenants en trace (sendUpdates none au backfill). Preuve d'audit Qualiopi prete.
</success_criteria>

<output>
Apres completion, creer .planning/phases/14-.../14-05-SUMMARY.md (noter le compte reel d'events purges + le recap du backfill : sessions traitees / inserted / skipped / echecs).
</output>
