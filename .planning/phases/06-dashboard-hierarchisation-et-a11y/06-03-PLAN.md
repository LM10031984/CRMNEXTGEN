---
phase: 06-dashboard-hierarchisation-et-a11y
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md
  - apps/web/src/components/ui/badge.tsx
autonomous: true
requirements:
  - UX-13
must_haves:
  truths:
    - "Un document d'audit a11y existe avec verdict par dimension (contraste badges, nav clavier, aria-label)"
    - "Le verdict du contraste Badge variants est PASS (WCAG AA 4.5:1 minimum sur text + bg)"
    - "Aucun <div onClick=...> sur élément non focusable n'a été ajouté/oublié (audit par grep)"
    - "Les boutons icon-only de la TopBar/sidebar ont un aria-label (cf Phase 4)"
  artifacts:
    - path: ".planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md"
      provides: "Rapport d'audit a11y manuel (verdict + commandes grep utilisées + sites OK / sites KO)"
      min_lines: 40
    - path: "apps/web/src/components/ui/badge.tsx"
      provides: "Composant Badge (vérifié / corrigé contraste si nécessaire)"
  key_links:
    - from: ".planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md"
      to: "apps/web/src/components/ui/badge.tsx"
      via: "Audit contraste documenté avec ratio WCAG par variant"
      pattern: "WCAG|ratio"
---

<objective>
Audit accessibilité ciblé sur le périmètre actuel (UX-13 audit 2026-05-12). CONTEXT.md scope l'audit à "visuel + grep ciblé, pas Lighthouse automatisé". Livrables : un document d'audit avec verdict par dimension + corrections ciblées si gap réel.

Purpose: Garantir un niveau a11y raisonnable sans tomber dans la course au score Lighthouse. Dette technique listée pour la milestone suivante si besoin.
Output: 1 rapport d'audit `06-A11Y-AUDIT.md` + corrections ponctuelles si Badge variant échoue le ratio WCAG AA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-CONTEXT.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-RESEARCH.md

<interfaces>
<!-- Badge variants à auditer (déjà existant). -->

From apps/web/src/components/ui/badge.tsx:
```typescript
type Variant = 'default' | 'success' | 'warning' | 'danger' | 'info' | 'muted' | 'primary';
// Couleurs actuelles :
// success: bg-emerald-50 text-emerald-700 border-emerald-200
// warning: bg-amber-50 text-amber-800 border-amber-200
// danger:  bg-red-50 text-red-700 border-red-200
// info:    bg-sky-50 text-sky-700 border-sky-200
// muted:   bg-slate-50 text-slate-600 border-slate-200
// primary: bg-primary-50 text-primary-700 border-primary-100
```

Tailwind palette : `emerald-50 ~ #ecfdf5`, `emerald-700 ~ #047857` → contrast ratio ~6.5:1 (PASS AA).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit a11y + génération du rapport 06-A11Y-AUDIT.md</name>
  <files>.planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md</files>
  <read_first>
    - apps/web/src/components/ui/badge.tsx
    - apps/web/src/components/layout/top-bar.tsx
    - apps/web/src/components/layout/sidebar-nav.tsx
    - apps/web/src/components/layout/mobile-menu-button.tsx
    - apps/web/src/components/ui/collapsible-section.tsx
    - .planning/phases/06-dashboard-hierarchisation-et-a11y/06-CONTEXT.md
  </read_first>
  <behavior>
    - Le rapport documente 4 dimensions d'audit avec verdict explicite :
      1. Contraste Badge variants (success, warning, danger, info, muted, primary) vs WCAG AA 4.5:1
      2. Navigation clavier (Tab/Enter/Esc) sur listes principales (sidebar, DataTable, dashboard)
      3. aria-label manquants sur boutons icon-only (TopBar, sidebar, page actions)
      4. <div onClick> non-focusables (anti-pattern)
    - Chaque dimension a un verdict PASS / PASS_AVEC_NOTE / FAIL et la commande grep utilisée.
    - Pour chaque FAIL, lister les fichiers concernés + la correction proposée.
    - Note : pas d'exécution Lighthouse (CONTEXT.md > Out of scope).
  </behavior>
  <action>
**Étape A — Collecter les preuves par grep.** Exécuter chacune des commandes ci-dessous depuis la racine du repo, et conserver les résultats pour le rapport :

1. **Contraste Badge** : `cat apps/web/src/components/ui/badge.tsx` — lire les 7 variants et reporter les classes Tailwind.
2. **Boutons icon-only sans aria-label** : `grep -rn "<button" apps/web/src --include="*.tsx" -A 5 | grep -B 1 "h-4 w-4\|h-5 w-5" | grep -v "aria-label\|aria-labelledby" | head -30`
3. **DIV onClick (anti-pattern)** : `grep -rn "<div[^>]*onClick" apps/web/src --include="*.tsx" | grep -v "tabIndex\|role=" | head -20`
4. **Liens icon-only sans aria-label** : `grep -rn "<Link" apps/web/src --include="*.tsx" -A 3 | grep -B 1 "h-4 w-4\|h-5 w-5" | grep -v "aria-label\|aria-labelledby\|>[A-ZÉÈÀa-zé]" | head -10`
5. **Inputs sans label** : `grep -rn "<input" apps/web/src --include="*.tsx" | grep -v "type=\"hidden\"" | grep -v "aria-label\|aria-labelledby\|id=" | head -10`
6. **Headings hierarchy** : `grep -rn "<h[1-6]" apps/web/src/app/app/page.tsx`

**Étape B — Évaluer le contraste Badge.** Les ratios pour les classes Tailwind 50/700 ou 50/800 sur fond clair sont notoirement ≥ 5:1. Vérifier mentalement chaque variant :

| Variant | Texte | Fond | Ratio approx | Verdict AA (≥ 4.5:1) |
|---|---|---|---|---|
| success | emerald-700 (#047857) | emerald-50 (#ecfdf5) | ~6.5:1 | PASS |
| warning | amber-800 (#92400e) | amber-50 (#fffbeb) | ~8:1 | PASS |
| danger | red-700 (#b91c1c) | red-50 (#fef2f2) | ~6.4:1 | PASS |
| info | sky-700 (#0369a1) | sky-50 (#f0f9ff) | ~5.6:1 | PASS |
| muted | slate-600 (#475569) | slate-50 (#f8fafc) | ~7.5:1 | PASS |
| primary | primary-700 | primary-50 | dépend du theme | À VÉRIFIER (lire `apps/web/tailwind.config.ts` ou `globals.css`) |
| default | foreground | muted | dépend tokens | À VÉRIFIER |

Si `primary-700` n'est pas suffisamment foncé OU si `foreground` sur `muted` ne passe pas (besoin de vérifier les valeurs dans tailwind.config.ts), le noter en PASS_AVEC_NOTE et proposer la correction (ex: forcer `text-primary-800`).

**Étape C — Rédiger `.planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md`** avec la structure suivante (ne pas dévier) :

```markdown
# Phase 6 — Audit a11y

**Date :** 2026-05-13
**Scope :** Périmètre actuel du CRM QualiOF (apps/web). Audit visuel + grep ciblé. Pas de Lighthouse (cf CONTEXT.md > Out of scope).

## Verdict global

PASS / PASS_AVEC_NOTES / FAIL — résumer en 1 ligne.

## 1. Contraste Badge variants

**Commande :** lecture de `apps/web/src/components/ui/badge.tsx`
**Critère :** WCAG AA = ratio contraste ≥ 4.5:1 (texte normal)

| Variant | Classes | Ratio approx | Verdict |
|---|---|---|---|
| success | bg-emerald-50 text-emerald-700 | ~6.5:1 | PASS |
| warning | bg-amber-50 text-amber-800 | ~8:1 | PASS |
| danger | bg-red-50 text-red-700 | ~6.4:1 | PASS |
| info | bg-sky-50 text-sky-700 | ~5.6:1 | PASS |
| muted | bg-slate-50 text-slate-600 | ~7.5:1 | PASS |
| primary | bg-primary-50 text-primary-700 | {ratio} | {verdict} |
| default | bg-muted text-foreground | {ratio} | {verdict} |

**Verdict :** {PASS / PASS_AVEC_NOTE / FAIL}
**Correctifs :** {liste fichiers + diff si FAIL, sinon "aucun"}

## 2. Navigation clavier sur listes

**Commande :** `grep -rn "<div[^>]*onClick" apps/web/src --include="*.tsx" | grep -v "tabIndex\|role="`
**Résultat :** {N occurrences}

| Pattern | Fichiers | Verdict |
|---|---|---|
| <Link> dans sidebar | sidebar-nav.tsx | PASS (Link natif Tab/Enter) |
| DataTable rowHref | components/ui/data-table.tsx | PASS (Link natif) |
| <div onClick> dialog overlay | create-organization-button.tsx ... | PASS_AVEC_NOTE (overlay clic-pour-fermer, le bouton interne est focusable) |

**Verdict :** {PASS / PASS_AVEC_NOTE / FAIL}

## 3. aria-label sur boutons icon-only

**Commande :** `grep -rn "<button" apps/web/src --include="*.tsx" -A 5 | grep -B 1 "h-4 w-4" | grep -v "aria-label"`
**Résultat :** {N candidats}

| Composant | aria-label présent | Notes |
|---|---|---|
| MobileMenuButton (Menu hamburger) | OUI ("Ouvrir le menu") | Phase 2 |
| TopBar Bell | OUI | Phase 4 |
| UserMenuButton (avatar) | OUI | Phase 4 |
| Generate{Doc}Button | {OUI/NON} | {fichiers concernés} |

**Verdict :** {PASS / PASS_AVEC_NOTE / FAIL}
**Correctifs :** {liste si FAIL}

## 4. <div onClick> non-focusables (anti-pattern)

**Commande :** `grep -rn "<div[^>]*onClick" apps/web/src --include="*.tsx"`
**Résultat :** {N occurrences}, {Y dans overlays Dialog acceptables}

| Fichier | Ligne | Contexte | Verdict |
|---|---|---|---|
| create-organization-button.tsx | 69 | overlay Dialog click-outside-to-close | ACCEPTABLE (le bouton de fermeture est dans un <button> Esc-actionnable) |

**Verdict :** {PASS / PASS_AVEC_NOTE}

## 5. Dette technique a11y (à reporter milestone v6)

- Lighthouse automatisé en CI : reporté (cf CONTEXT.md > Out of scope, CI v6).
- Focus-trap sur Radix Dialog : déjà géré par Radix (PASS implicite).
- Skip-link "Aller au contenu principal" : à ajouter en v6 si feedback utilisateur.

## Conclusion

L'audit visuel + grep ciblé du périmètre Phase 6 confirme un niveau a11y raisonnable. Aucun gap bloquant identifié. Les patterns Radix + Link natif + <button> avec aria-label dominent, ce qui satisfait WCAG AA pour le périmètre interne Start Academy.
```

Remplir les `{placeholders}` avec les vraies valeurs collectées à l'étape A et B. Si l'audit primary/default contraste révèle un gap → renseigner les correctifs ciblés (sera appliqué dans Task 2 si besoin).

**Étape D — Ne pas faire :**
- Pas d'install d'outil (pa11y, axe-core). Audit visuel + grep uniquement.
- Pas de Lighthouse / pa11y / axe-cli. CONTEXT.md > Out of scope.
- Pas de refonte typographique mobile (CONTEXT.md > Out of scope).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && test -f .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md && grep -q "Contraste Badge variants" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md && grep -q "Navigation clavier" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md && grep -q "aria-label" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md && wc -l .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md | awk '{exit ($1 < 40)}' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md` retourne 0.
    - Le fichier a au moins 40 lignes.
    - `grep "## 1. Contraste Badge variants" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md` retourne 1 ligne.
    - `grep "## 2. Navigation clavier sur listes" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md` retourne 1 ligne.
    - `grep "## 3. aria-label sur boutons icon-only" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md` retourne 1 ligne.
    - `grep "## 4. <div onClick>" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md` retourne 1 ligne.
    - `grep -E "Verdict.*PASS|Verdict.*FAIL|Verdict.*PASS_AVEC_NOTE" .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md` retourne au moins 4 lignes (une par dimension).
    - Pour chaque variant Badge (success/warning/danger/info/muted/primary), le ratio approximé est documenté.
  </acceptance_criteria>
  <done>Audit a11y exhaustif produit, 4 dimensions verdict, dette technique listée pour v6.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Correctifs Badge a11y si gap détecté (conditionnel)</name>
  <files>apps/web/src/components/ui/badge.tsx</files>
  <read_first>
    - .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md (audit Task 1)
    - apps/web/src/components/ui/badge.tsx
    - apps/web/tailwind.config.ts (résoudre primary-50/700 réels)
  </read_first>
  <behavior>
    - Si l'audit Task 1 a marqué TOUTES les dimensions PASS → ce Task est NO-OP (ne modifier aucun fichier, mentionner dans le SUMMARY).
    - Si l'audit a marqué un variant FAIL → appliquer la correction proposée dans 06-A11Y-AUDIT.md à `badge.tsx` UNIQUEMENT.
    - Aucune autre modification de fichier autorisée (les autres dimensions sont gérées ailleurs : sidebar, topbar = Phase 2/4).
  </behavior>
  <action>
**Étape A — Lire `06-A11Y-AUDIT.md`** (Task 1 output). Localiser la section "## 1. Contraste Badge variants" et noter le verdict + correctifs proposés.

**Étape B — Décision branche :**

**CAS 1 : Verdict global Badge = PASS (probable).** Ne RIEN modifier dans `badge.tsx`. Skip Étape C. Documenter dans le SUMMARY que Task 2 a été NO-OP et expliquer pourquoi.

**CAS 2 : Verdict Badge primary ou default = FAIL.** Appliquer le correctif minimal. Exemple si `primary-700` insuffisant sur `primary-50` :

Avant :
```ts
primary: 'bg-primary-50 text-primary-700 border-primary-100',
```
Après :
```ts
primary: 'bg-primary-50 text-primary-800 border-primary-200',
```

Exemple si `text-foreground` sur `bg-muted` insuffisant :
```ts
default: 'bg-muted text-foreground border-border',
```
→ vérifier les tokens `muted` et `foreground` dans `tailwind.config.ts` (ou globals.css). Si gap réel, durcir le texte (`text-slate-700` à la place de `text-foreground` par exemple). Ne pas toucher si la palette est OK.

**Étape C (CAS 2 uniquement) — Apply patch + smoke.** Modifier `apps/web/src/components/ui/badge.tsx` (1 ligne ou 2). Lancer `pnpm --filter @qualiof/web build` pour vérifier 0 erreur.

**Étape D — Ne pas faire :**
- Ne pas changer les `Variant` types.
- Ne pas réécrire le composant Badge en CVA.
- Ne pas ajouter de nouveau variant.
- Ne pas modifier les sites d'appel `<Badge variant="...">` (ils sont neutres face aux classes internes).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "type Variant" apps/web/src/components/ui/badge.tsx && grep -cE "success:|warning:|danger:|info:|muted:|primary:|default:" apps/web/src/components/ui/badge.tsx | awk '{exit ($1 < 7)}' && pnpm --filter @qualiof/web build > /tmp/build-06-03.log 2>&1 && tail -3 /tmp/build-06-03.log && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `apps/web/src/components/ui/badge.tsx` contient les 7 variants (`default`, `success`, `warning`, `danger`, `info`, `muted`, `primary`).
    - Le type `Variant` est inchangé (mêmes 7 valeurs).
    - `pnpm --filter @qualiof/web build` succeeds.
    - Si Task 2 était NO-OP : le diff git sur `badge.tsx` est vide (commande : `git diff apps/web/src/components/ui/badge.tsx`).
    - Si Task 2 a patché : le diff montre uniquement des changements de classes Tailwind dans `variants` (pas de changement d'API).
  </acceptance_criteria>
  <done>Badge.tsx soit inchangé (audit PASS), soit avec un correctif ciblé de classes Tailwind documenté dans 06-A11Y-AUDIT.md. Build OK.</done>
</task>

</tasks>

<verification>
- Audit document existe : `test -f .planning/phases/06-dashboard-hierarchisation-et-a11y/06-A11Y-AUDIT.md`
- Build OK : `pnpm --filter @qualiof/web build`
- Tests existants verts : `pnpm --filter @qualiof/web test`
</verification>

<success_criteria>
- 1 rapport d'audit a11y daté, 4 dimensions évaluées, verdict explicite par dimension
- Aucun changement parasite (pas de refonte typo, pas d'install d'outil)
- Build OK
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-hierarchisation-et-a11y/06-03-SUMMARY.md`
</output>
