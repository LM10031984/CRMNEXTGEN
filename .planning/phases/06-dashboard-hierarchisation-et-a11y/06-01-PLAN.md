---
phase: 06-dashboard-hierarchisation-et-a11y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/funder-codes.ts
  - apps/web/src/app/app/organisations/page.tsx
  - apps/web/src/app/app/organisations/[id]/page.tsx
  - apps/web/src/app/app/dossiers-opco/page.tsx
  - apps/web/src/app/app/dossiers-opco/envoyer/[id]/page.tsx
  - apps/web/src/app/app/sessions/[id]/page.tsx
  - apps/web/src/app/app/apprenants/[id]/page.tsx
  - apps/web/src/app/app/formateurs/[id]/page.tsx
  - apps/web/src/app/app/financeurs/page.tsx
  - apps/web/src/app/app/financeurs/[code]/page.tsx
  - apps/web/src/components/editors/legal-link-editor.tsx
  - apps/web/src/components/sessions/gap-row.tsx
  - apps/web/src/components/apprenants/learner-quick-view-button.tsx
autonomous: true
requirements:
  - UX-12
must_haves:
  truths:
    - "Les codes financeurs raw (OPCOMMERCE, OPCO_EP) n'apparaissent plus tels quels dans l'UI"
    - "Tout site UI affichant opcoCode passe par formatFunderCode (label user-friendly)"
    - "Le helper formatFunderCode est unique source de vérité pour le rendu (BDD garde le code raw)"
  artifacts:
    - path: "apps/web/src/lib/funder-codes.ts"
      provides: "FUNDER_LABELS map + formatFunderCode helper"
      exports: ["FUNDER_LABELS", "formatFunderCode"]
  key_links:
    - from: "Pages UI affichant opcoCode"
      to: "formatFunderCode helper"
      via: "import + appel sur la valeur"
      pattern: "formatFunderCode\\("
---

<objective>
Intégrer le helper `formatFunderCode` (déjà créé dans `apps/web/src/lib/funder-codes.ts`) dans tous les sites UI où le code financeur raw (`opcoCode`) est actuellement affiché. UX-12 de l'audit 2026-05-12 : harmoniser les libellés financeurs (OPCOMMERCE → "OPCO Commerce", OPCO_EP → "OPCO EP") pour cohérence visuelle.

Purpose: Sans transformer la BDD (qui garde les clés stables), faire que chaque badge / titre / sous-titre affichant un code financeur soit user-friendly.
Output: 1 helper exporté, 12 sites UI mis à jour, build OK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-CONTEXT.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-RESEARCH.md

<interfaces>
<!-- Helper déjà existant — l'executor doit l'IMPORTER, pas le redéfinir. -->

From apps/web/src/lib/funder-codes.ts:
```typescript
export const FUNDER_LABELS: Record<string, string> = {
  AGEFICE: 'AGEFICE',
  OPCO_EP: 'OPCO EP',
  OPCOMMERCE: 'OPCO Commerce',
  ATLAS: 'ATLAS',
  AKTO: 'AKTO',
  AFDAS: 'AFDAS',
  CONSTRUCTYS: 'Constructys',
  FIFPL: 'FIFPL',
  CPF: 'CPF',
  POLE_EMPLOI: 'Pôle Emploi',
  OF: 'Auto-financé',
};

export function formatFunderCode(code: string | null | undefined): string;
```

Import path: `import { formatFunderCode } from '@/lib/funder-codes';`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Vérifier/aligner funder-codes.ts vs CONTEXT.md</name>
  <files>apps/web/src/lib/funder-codes.ts</files>
  <read_first>
    - apps/web/src/lib/funder-codes.ts (lire l'état actuel)
    - .planning/phases/06-dashboard-hierarchisation-et-a11y/06-CONTEXT.md (sélection "UX-12 — Codes financeurs")
  </read_first>
  <behavior>
    - FUNDER_LABELS contient au minimum les codes documentés dans CONTEXT.md :
      AGEFICE, OPCO_EP, OPCOMMERCE, ATLAS, FIFPL, CPF, OF.
    - formatFunderCode null|undefined → '—'
    - formatFunderCode code inconnu → renvoie le code raw
    - formatFunderCode code connu → renvoie le label de la map
  </behavior>
  <action>
Lire `apps/web/src/lib/funder-codes.ts` en l'état. État actuel attendu (extrait) :

```ts
export const FUNDER_LABELS: Record<string, string> = {
  AGEFICE: 'AGEFICE',
  OPCO_EP: 'OPCO EP',
  OPCOMMERCE: 'OPCO Commerce',
  ATLAS: 'ATLAS',
  AKTO: 'AKTO',
  AFDAS: 'AFDAS',
  CONSTRUCTYS: 'Constructys',
  FIFPL: 'FIFPL',
  CPF: 'CPF',
  POLE_EMPLOI: 'Pôle Emploi',
  OF: 'Auto-financé',
};
```

CONTEXT.md (D-02) précise : `OF → 'OF (auto-financé)'` (label avec prefix "OF (..."). Aligner la valeur en remplaçant l'entrée :

```ts
OF: 'OF (auto-financé)',
```

Aucune autre modification. Pas de réécriture, pas d'ajout de codes. Si tous les autres codes sont présents (AGEFICE, OPCO_EP, OPCOMMERCE, ATLAS, AKTO, AFDAS, CONSTRUCTYS, FIFPL, CPF, POLE_EMPLOI, OF), ne pas en supprimer.

NB : ne pas ajouter `OPCO_COMMERCE` (variant) — CONTEXT.md > Out of scope dit explicitement "Pas de renommage des codes en BDD".
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -q "OF: 'OF (auto-financé)'" apps/web/src/lib/funder-codes.ts && grep -q "OPCOMMERCE: 'OPCO Commerce'" apps/web/src/lib/funder-codes.ts && grep -q "OPCO_EP: 'OPCO EP'" apps/web/src/lib/funder-codes.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "OF: 'OF (auto-financé)'" apps/web/src/lib/funder-codes.ts` retourne 1 ligne
    - `grep "OPCOMMERCE: 'OPCO Commerce'" apps/web/src/lib/funder-codes.ts` retourne 1 ligne
    - `grep "OPCO_EP: 'OPCO EP'" apps/web/src/lib/funder-codes.ts` retourne 1 ligne
    - `grep "export function formatFunderCode" apps/web/src/lib/funder-codes.ts` retourne 1 ligne
  </acceptance_criteria>
  <done>FUNDER_LABELS aligné avec CONTEXT.md (OF label = 'OF (auto-financé)'), pas de changement d'API.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Intégrer formatFunderCode dans toutes les vues affichant opcoCode</name>
  <files>
    apps/web/src/app/app/organisations/page.tsx,
    apps/web/src/app/app/organisations/[id]/page.tsx,
    apps/web/src/app/app/dossiers-opco/page.tsx,
    apps/web/src/app/app/dossiers-opco/envoyer/[id]/page.tsx,
    apps/web/src/app/app/sessions/[id]/page.tsx,
    apps/web/src/app/app/apprenants/[id]/page.tsx,
    apps/web/src/app/app/formateurs/[id]/page.tsx,
    apps/web/src/app/app/financeurs/page.tsx,
    apps/web/src/app/app/financeurs/[code]/page.tsx,
    apps/web/src/components/editors/legal-link-editor.tsx,
    apps/web/src/components/sessions/gap-row.tsx,
    apps/web/src/components/apprenants/learner-quick-view-button.tsx
  </files>
  <read_first>
    - apps/web/src/lib/funder-codes.ts (helper à importer)
    - apps/web/src/app/app/organisations/page.tsx
    - apps/web/src/app/app/organisations/[id]/page.tsx
    - apps/web/src/app/app/dossiers-opco/page.tsx
    - apps/web/src/app/app/dossiers-opco/envoyer/[id]/page.tsx
    - apps/web/src/app/app/sessions/[id]/page.tsx
    - apps/web/src/app/app/apprenants/[id]/page.tsx
    - apps/web/src/app/app/formateurs/[id]/page.tsx
    - apps/web/src/app/app/financeurs/page.tsx
    - apps/web/src/app/app/financeurs/[code]/page.tsx
    - apps/web/src/components/editors/legal-link-editor.tsx
    - apps/web/src/components/sessions/gap-row.tsx
    - apps/web/src/components/apprenants/learner-quick-view-button.tsx
  </read_first>
  <behavior>
    - Pour CHAQUE fichier listé, ajouter `import { formatFunderCode } from '@/lib/funder-codes';` (sauf s'il existe déjà).
    - Remplacer chaque affichage RAW d'un code financeur par un appel à `formatFunderCode(...)`.
    - Ne PAS modifier les requêtes Prisma (`opcoCode: 'AGEFICE'`, `where: { opcoCode: ... }`, `select: { opcoCode: true }`) : seul le RENDU change.
    - Ne PAS modifier les conditions logiques `opcoCode === 'AGEFICE'` : ce sont des comparaisons sur le code raw, à garder.
  </behavior>
  <action>
Implémente les substitutions ci-dessous, fichier par fichier. Chaque emplacement est identifié par sa ligne approximative (l'exécutant DOIT relire pour valider — les numéros peuvent dériver) et par le contenu à transformer.

**Règle générale :** ajouter en tête du fichier `import { formatFunderCode } from '@/lib/funder-codes';` (à grouper avec les autres imports `@/lib/*`).

### A. apps/web/src/app/app/organisations/page.tsx (ligne ~168)
Avant :
```tsx
row.opcoCode ? <Badge variant="default">{row.opcoCode}</Badge> : <span className="text-xs text-muted-foreground italic">—</span>,
```
Après :
```tsx
row.opcoCode ? <Badge variant="default">{formatFunderCode(row.opcoCode)}</Badge> : <span className="text-xs text-muted-foreground italic">—</span>,
```

### B. apps/web/src/app/app/organisations/[id]/page.tsx (lignes ~74 et ~85)
Avant ligne 74 (subtitle) :
```tsx
subtitle={org.legalForm + (org.opcoCode ? ` · ${org.opcoCode}` : '')}
```
Après :
```tsx
subtitle={org.legalForm + (org.opcoCode ? ` · ${formatFunderCode(org.opcoCode)}` : '')}
```
Avant ligne 85 (badge OPCO) :
```tsx
{org.opcoCode && <Badge variant="info">OPCO {org.opcoCode}</Badge>}
```
Après :
```tsx
{org.opcoCode && <Badge variant="info">{formatFunderCode(org.opcoCode)}</Badge>}
```
(le préfixe "OPCO" devient redondant avec les labels "OPCO EP", "OPCO Commerce" — on supprime le préfixe et on utilise le label complet)

### C. apps/web/src/app/app/dossiers-opco/page.tsx (lignes ~429-432)
Avant :
```tsx
{r.sponsorOrg?.opcoCode ? (
  ...
    {r.sponsorOrg.opcoCode}
```
Après :
```tsx
{r.sponsorOrg?.opcoCode ? (
  ...
    {formatFunderCode(r.sponsorOrg.opcoCode)}
```
NB : ligne 232 (`sponsorOrg: { opcoCode: 'AGEFICE' }`), ligne 340 (`sponsorOpcoCode: r.sponsorOrg.opcoCode ?? null`), ligne 434 (`r.sponsorOrg.opcoCode === 'AGEFICE'`) — NE PAS TOUCHER (ce sont des comparaisons / champs de données).

### D. apps/web/src/app/app/dossiers-opco/envoyer/[id]/page.tsx (ligne ~69)
Avant :
```tsx
const sponsorName = `${sub.sponsorOrg.legalName}${sub.sponsorOrg.opcoCode ? ` (${sub.sponsorOrg.opcoCode})` : ''}`;
```
Après :
```tsx
const sponsorName = `${sub.sponsorOrg.legalName}${sub.sponsorOrg.opcoCode ? ` (${formatFunderCode(sub.sponsorOrg.opcoCode)})` : ''}`;
```

### E. apps/web/src/app/app/sessions/[id]/page.tsx (ligne ~456)
Avant :
```tsx
{g.sponsor.opcoCode && <Badge variant="info">OPCO {g.sponsor.opcoCode}</Badge>}
```
Après :
```tsx
{g.sponsor.opcoCode && <Badge variant="info">{formatFunderCode(g.sponsor.opcoCode)}</Badge>}
```
NB : ligne 532 (`showAgefice={isEi || g.sponsor.opcoCode === 'AGEFICE'}`) — NE PAS TOUCHER.

### F. apps/web/src/app/app/apprenants/[id]/page.tsx (lignes ~530-532)
Avant :
```tsx
{p.sponsorOrg?.opcoCode && (
  ...
    {p.sponsorOrg.opcoCode}
```
Après :
```tsx
{p.sponsorOrg?.opcoCode && (
  ...
    {formatFunderCode(p.sponsorOrg.opcoCode)}
```
NB : lignes 244 et 551 (comparaisons `=== 'AGEFICE'`) — NE PAS TOUCHER.

### G. apps/web/src/app/app/formateurs/[id]/page.tsx (ligne ~236)
Avant :
```tsx
<Badge variant="info" className="mt-2">OPCO {subOrg.opcoCode}</Badge>
```
Après :
```tsx
<Badge variant="info" className="mt-2">{formatFunderCode(subOrg.opcoCode)}</Badge>
```

### H. apps/web/src/app/app/financeurs/page.tsx (ligne ~120)
Avant :
```tsx
<h3 className="font-semibold text-base">{opco.code}</h3>
```
Après :
```tsx
<h3 className="font-semibold text-base">{formatFunderCode(opco.code)}</h3>
```
Conserver `opco.name` (déjà user-friendly) inchangé en sous-titre.

### I. apps/web/src/components/editors/legal-link-editor.tsx (lignes ~127-128)
Avant :
```tsx
{link.organization.opcoCode && (
  <Badge variant="default">OPCO {link.organization.opcoCode}</Badge>
)}
```
Après :
```tsx
{link.organization.opcoCode && (
  <Badge variant="default">{formatFunderCode(link.organization.opcoCode)}</Badge>
)}
```

### J. apps/web/src/components/sessions/gap-row.tsx (ligne ~167)
Avant :
```tsx
{link.organization.opcoCode && <Badge variant="info">{link.organization.opcoCode}</Badge>}
```
Après :
```tsx
{link.organization.opcoCode && <Badge variant="info">{formatFunderCode(link.organization.opcoCode)}</Badge>}
```

### K. apps/web/src/components/apprenants/learner-quick-view-button.tsx (ligne ~126)
Avant :
```tsx
{l.opcoCode && ` · OPCO ${l.opcoCode}`}
```
Après :
```tsx
{l.opcoCode && ` · ${formatFunderCode(l.opcoCode)}`}
```

### L. apps/web/src/app/app/financeurs/[code]/page.tsx (ligne 292)
Avant :
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{opco.code}</h1>
```
Après :
```tsx
<h1 className="text-2xl font-semibold tracking-tight">{formatFunderCode(opco.code)}</h1>
```
NB : ligne 95 (`const code = rawCode.toUpperCase();`), ligne 101 (`where: { ... opcoCode: code }`), ligne 137 (`sponsorOrg: { opcoCode: code }`), ligne 302 (`opco.code === 'AGEFICE'`) — NE PAS TOUCHER (clés Prisma + comparaison logique). Le sous-titre `opco.name` ligne 295 reste user-friendly inchangé.

**Rappel patterns à ne pas toucher (NÉGATIFS) :**
- `opcoCode: 'AGEFICE'` dans une clause `where`
- `opcoCode: { ... }` dans un `select`
- `opcoCode === 'X'` dans une condition logique
- `participant.sponsorOrg?.opcoCode === 'AGEFICE'` (logique métier)
- `const code = rawCode.toUpperCase();` (param de route → BDD lookup)
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -l "formatFunderCode" apps/web/src/app/app/organisations/page.tsx apps/web/src/app/app/organisations/\[id\]/page.tsx apps/web/src/app/app/dossiers-opco/page.tsx apps/web/src/app/app/sessions/\[id\]/page.tsx apps/web/src/app/app/apprenants/\[id\]/page.tsx apps/web/src/app/app/formateurs/\[id\]/page.tsx apps/web/src/app/app/financeurs/page.tsx apps/web/src/app/app/financeurs/\[code\]/page.tsx apps/web/src/components/editors/legal-link-editor.tsx apps/web/src/components/sessions/gap-row.tsx apps/web/src/components/apprenants/learner-quick-view-button.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Au moins 11 fichiers contiennent l'import et l'appel à `formatFunderCode` (10 sites historiques + financeurs/[code]/page.tsx).
    - `grep -rn "OPCO {.*opcoCode}" apps/web/src --include="*.tsx"` retourne 0 ligne (toutes les occurrences "OPCO {code}" supprimées).
    - `grep -rn "from '@/lib/funder-codes'" apps/web/src` retourne au moins 11 lignes.
    - `grep -rn "opcoCode === 'AGEFICE'" apps/web/src` retourne TOUJOURS les occurrences logique (au moins 3) — preuve qu'on n'a pas accidentellement modifié la logique métier.
    - `grep -n "{opco.code}" apps/web/src/app/app/financeurs/\[code\]/page.tsx` ne retourne plus le `<h1>` raw (mais peut retourner d'autres usages comme `opco.code === 'AGEFICE'` qui sont logiques).
    - `pnpm --filter @qualiof/web build` OK (pas d'import manquant, pas de TS error).
  </acceptance_criteria>
  <done>12 fichiers refactorés, build clean, badges et titres affichent "OPCO Commerce" au lieu de "OPCOMMERCE" et "OPCO EP" au lieu de "OPCO OPCO_EP", page financeur/[code] affiche le label user-friendly en titre.</done>
</task>

</tasks>

<verification>
- `grep "formatFunderCode" apps/web/src --include="*.tsx" -rn | wc -l` ≥ 12
- `pnpm --filter @qualiof/web build` succeeds
- Badge organisations list : la cellule OPCO affiche "OPCO Commerce" (et plus "OPCOMMERCE") quand `row.opcoCode === 'OPCOMMERCE'`
- Page `/app/financeurs/OPCOMMERCE` affiche "OPCO Commerce" en `<h1>` (et plus "OPCOMMERCE" raw)
- Aucune modification de la couche données (`opcoCode` reste raw côté Prisma/BDD)
</verification>

<success_criteria>
- formatFunderCode aligné avec CONTEXT.md (OF label = 'OF (auto-financé)')
- 12 sites UI utilisent formatFunderCode pour le rendu
- Build OK, smoke test (page.smoke.test.ts) toujours vert
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-hierarchisation-et-a11y/06-01-SUMMARY.md`
</output>
