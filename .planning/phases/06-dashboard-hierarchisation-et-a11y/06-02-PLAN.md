---
phase: 06-dashboard-hierarchisation-et-a11y
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/app/page.tsx
  - apps/web/src/components/ui/collapsible-section.tsx
autonomous: true
requirements:
  - UX-11
must_haves:
  truths:
    - "Le dashboard /app rend 4 PrioCard en haut (CA encaissé, AGEFICE consommé/restant, Sessions à venir, Taux remplissage)"
    - "Les 14 KPI secondaires (CA + Cashflow + Performance) sont dans une CollapsibleSection 'Indicateurs détaillés' fermée par défaut"
    - "L'état ouvert/fermé persiste via localStorage (clé `qualiof-collapse-dashboard-detailed`)"
    - "Le reste du dashboard (Alertes, Pipeline, Top, Chart, Satisfaction, Récentes) demeure visible inchangé"
  artifacts:
    - path: "apps/web/src/app/app/page.tsx"
      provides: "Dashboard hiérarchisé : PrioCard x4 + CollapsibleSection"
      contains: "PrioCard"
    - path: "apps/web/src/components/ui/collapsible-section.tsx"
      provides: "CollapsibleSection client component avec persistance localStorage"
      exports: ["CollapsibleSection"]
  key_links:
    - from: "apps/web/src/app/app/page.tsx"
      to: "apps/web/src/components/ui/collapsible-section.tsx"
      via: "import"
      pattern: "from '@/components/ui/collapsible-section'"
    - from: "apps/web/src/app/app/page.tsx"
      to: "apps/web/src/lib/dashboard-stats.ts"
      via: "getDashboardStats(tenantId, year)"
      pattern: "getDashboardStats\\("
---

<objective>
Valider et finaliser la hiérarchisation visuelle du dashboard `/app` (UX-11 audit 2026-05-12). 4 KPI prioritaires en grand format en haut + 14 KPI secondaires dans une section repliable fermée par défaut. Le draft est déjà en place dans `page.tsx` (PrioCard + CollapsibleSection) — ce plan vérifie l'état, ajuste si nécessaire, et garantit l'intégrité du rendu.

Purpose: Réduire la densité anxiogène (14 KPI à plat = surcharge cognitive) en mettant l'essentiel en avant et en repliant le détail accessible à 1 clic.
Output: page.tsx hiérarchisé conforme à CONTEXT.md + composant CollapsibleSection robuste (idempotent localStorage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-CONTEXT.md
@.planning/phases/06-dashboard-hierarchisation-et-a11y/06-RESEARCH.md

<interfaces>
<!-- Composant CollapsibleSection déjà existant — l'executor doit l'IMPORTER. -->

From apps/web/src/components/ui/collapsible-section.tsx:
```typescript
interface CollapsibleSectionProps {
  id: string;                             // utilisé pour localStorage key
  title: React.ReactNode;
  subtitle?: React.ReactNode;
  icon?: React.ComponentType<{ className?: string }>;
  defaultOpen?: boolean;
  children: React.ReactNode;
  className?: string;
}
export function CollapsibleSection(props: CollapsibleSectionProps): JSX.Element;
```

Storage key: `qualiof-collapse-{id}` (préfixe géré en interne).

PrioCard est défini localement dans page.tsx, lignes ~351-397 (réutiliser, ne pas dupliquer ailleurs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Auditer + finaliser hiérarchisation dashboard (page.tsx)</name>
  <files>apps/web/src/app/app/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/page.tsx (lire l'intégralité 1-555)
    - apps/web/src/components/ui/collapsible-section.tsx (signature import)
    - .planning/phases/06-dashboard-hierarchisation-et-a11y/06-CONTEXT.md (sélection "UX-11 — Hiérarchisation KPI")
  </read_first>
  <behavior>
    - La section "À l'essentiel" rend EXACTEMENT 4 `<PrioCard>` dans cet ordre :
      1. CA encaissé (icon=Euro, accent="success")
      2. AGEFICE {year} (icon=Wallet, accent="primary", valeur = `ageficeSummary.totalRemaining`)
      3. Sessions à venir (icon=Calendar, accent="default")
      4. Taux remplissage (icon=Target, accent dépend du taux)
    - La section "Indicateurs détaillés" est UN `<CollapsibleSection id="dashboard-detailed">` qui contient les 3 sous-sections (CA, Cashflow, Performance).
    - Aucune des autres sections (Alertes, Pipeline, Top, Chart, Satisfaction, Récentes) n'est wrappée dans CollapsibleSection (elles restent visibles d'office).
    - `defaultOpen` est absent OU à `false` (fermée par défaut) — conforme CONTEXT.md.
  </behavior>
  <action>
**Étape A — Audit (lire et comparer).** Lire `apps/web/src/app/app/page.tsx` lignes 1-555. Vérifier la présence des éléments suivants (l'état attendu est déjà majoritairement en place) :

1. Import `CollapsibleSection` ligne ~13 : `import { CollapsibleSection } from '@/components/ui/collapsible-section';`
2. Section "À l'essentiel" lignes ~103-144 :
   - Wrapper `<section>` avec heading "À l'essentiel" et icône `Sparkles`.
   - Grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`.
   - 4 enfants `<PrioCard>` dans l'ordre listé dans `<behavior>`.
3. Section "Indicateurs détaillés" lignes ~146-211 :
   - `<CollapsibleSection id="dashboard-detailed" title="Indicateurs détaillés" subtitle="CA, cashflow, volumes & moyennes" icon={BarChart3}>` (sans `defaultOpen`).
   - Contient 3 sous-divs : "Chiffre d'affaires {yearLabel}", "Cashflow", "Performance — moyennes & volumes".
4. Le reste de la page (Alertes/Pipeline, Top, Chart, Satisfaction, Sessions récentes) est INCHANGÉ.

**Étape B — Corrections ciblées si l'audit trouve un écart.** Pour CHAQUE écart trouvé, appliquer la transformation minimale ci-dessous :

- Si l'import `CollapsibleSection` est manquant → l'ajouter ligne 13 (groupé avec les autres imports `@/components/ui/*`).
- Si une PrioCard manque ou est dans le mauvais ordre → réordonner ou ajouter selon la liste exacte du `<behavior>`. Le code de référence pour AGEFICE :
  ```tsx
  <PrioCard
    icon={Wallet}
    label={`AGEFICE ${ageficeYear}`}
    value={fmtEUR.format(ageficeSummary.totalRemaining)}
    sub={`${ageficeSummary.withBudgetLeft} apprenant${ageficeSummary.withBudgetLeft > 1 ? 's' : ''} mobilisable${ageficeSummary.withBudgetLeft > 1 ? 's' : ''}`}
    accent="primary"
    href={`/app/budget-agefice?filter=has_budget_left&year=${ageficeYear}`}
  />
  ```
- Si `CollapsibleSection` est mal configurée (id différent, `defaultOpen=true`, etc.) → corriger pour matcher exactement `<CollapsibleSection id="dashboard-detailed" title="Indicateurs détaillés" subtitle="CA, cashflow, volumes & moyennes" icon={BarChart3}>`.
- Si une autre section (Alertes/Pipeline/Top/Chart/Satisfaction/Récentes) a été accidentellement wrappée dans CollapsibleSection → l'extraire (les retirer du wrapper, garder le `<section>` standalone).

**Étape C — Ne pas faire :**
- Ne pas modifier `dashboard-stats.ts` ni `budget-agefice.ts` (juste consommer).
- Ne pas modifier `PrioCard`, `CaCard`, `PerfCard` (fonctions locales) — leur API est déjà bonne.
- Ne pas changer la position de "Indicateurs détaillés" : elle est juste après "À l'essentiel" et avant "Alertes + Pipeline".
- Ne pas ajouter de nouvelle dépendance.

**Étape D — Smoke local.** Lancer `pnpm --filter @qualiof/web build` et vérifier 0 erreur TS.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -c "<PrioCard" apps/web/src/app/app/page.tsx | grep -q "^4$" && grep -q "id=\"dashboard-detailed\"" apps/web/src/app/app/page.tsx && grep -q "from '@/components/ui/collapsible-section'" apps/web/src/app/app/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<PrioCard" apps/web/src/app/app/page.tsx` retourne exactement `4`.
    - `grep -n "<CollapsibleSection" apps/web/src/app/app/page.tsx` retourne exactement 1 ligne.
    - `grep -n "id=\"dashboard-detailed\"" apps/web/src/app/app/page.tsx` retourne 1 ligne.
    - `grep -n "title=\"Indicateurs détaillés\"" apps/web/src/app/app/page.tsx` retourne 1 ligne.
    - `grep -n "from '@/components/ui/collapsible-section'" apps/web/src/app/app/page.tsx` retourne 1 ligne.
    - `grep -n "label=\"CA encaissé\"" apps/web/src/app/app/page.tsx` retourne 1 ligne (PrioCard #1).
    - `grep -n "label=\"Sessions à venir\"" apps/web/src/app/app/page.tsx` retourne 1 ligne (PrioCard #3).
    - `grep -n "label=\"Taux remplissage\"" apps/web/src/app/app/page.tsx` retourne au moins 1 ligne (PrioCard #4 ; il peut aussi y avoir un PerfCard dans la section détaillée — c'est OK).
    - `grep -n "defaultOpen=" apps/web/src/app/app/page.tsx` retourne 0 ligne OU `defaultOpen={false}` (jamais `true`).
    - `pnpm --filter @qualiof/web build` succeeds (no TS error).
  </acceptance_criteria>
  <done>Dashboard hiérarchisé conforme à CONTEXT.md UX-11. 4 PrioCard prioritaires. 14 KPI dans CollapsibleSection fermée par défaut. Build OK.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Vérifier robustesse CollapsibleSection (persistance + hydration)</name>
  <files>apps/web/src/components/ui/collapsible-section.tsx</files>
  <read_first>
    - apps/web/src/components/ui/collapsible-section.tsx (lire intégralité)
  </read_first>
  <behavior>
    - Le composant est 'use client' (directive en première ligne).
    - Il utilise `useState` + `useEffect` pour hydrater depuis localStorage (pas de SSR crash, ratio storage key `qualiof-collapse-{id}`).
    - Le try/catch entoure tout accès à `localStorage` (Safari incognito-safe).
    - Le bouton toggle a un rôle accessible (`<button>` natif suffit, type="button"), un `aria-expanded={open}` ou équivalent, et un aria-label.
    - Le contenu `{children}` n'est rendu qu'en `open === true` (sinon élidé pour éviter coût de rendu inutile).
  </behavior>
  <action>
**Étape A — Audit.** Lire `apps/web/src/components/ui/collapsible-section.tsx`. Vérifier les 5 points du `<behavior>` :
1. `'use client'` ligne 1.
2. `useState` + `useEffect` présents avec hydration depuis `localStorage.getItem(storageKey)`.
3. try/catch autour de `localStorage.getItem` ET `localStorage.setItem`.
4. Bouton natif `<button>` avec `aria-expanded={open}` et `aria-label`.
5. Contenu enfant rendu conditionnellement (`{open && <div>{children}</div>}` ou pattern équivalent).

**Étape B — Corrections.** Pour chaque point KO de l'audit, appliquer la correction minimale :

- Si `aria-expanded` manquant sur le bouton toggle : l'ajouter. Exemple :
  ```tsx
  <button
    type="button"
    onClick={toggle}
    aria-expanded={open}
    aria-controls={`collapsible-content-${id}`}
    aria-label={typeof title === 'string' ? `${open ? 'Replier' : 'Déplier'} ${title}` : (open ? 'Replier la section' : 'Déplier la section')}
    className="..."
  >
    {Icon && <Icon className="h-4 w-4" aria-hidden="true" />}
    <span>{title}</span>
    {subtitle && <span className="text-xs text-muted-foreground">{subtitle}</span>}
    <ChevronDown className={cn('h-4 w-4 transition-transform', open && 'rotate-180')} aria-hidden="true" />
  </button>
  ```
- Si le contenu est rendu indépendamment de `open` : conditionner avec `{open && (...)}` autour du wrapper enfant. Préserver un `id={`collapsible-content-${id}`}` sur le wrapper pour respecter `aria-controls`.
- Si try/catch manque sur `setItem` : l'ajouter (déjà présent sur `getItem`).
- Si la directive `'use client'` est absente : l'ajouter ligne 1.

**Étape C — Ne pas faire :**
- Ne pas réécrire le composant from scratch — c'est un audit + petits patchs.
- Ne pas changer l'API publique (`CollapsibleSectionProps`).
- Ne pas changer le préfixe `STORAGE_KEY_PREFIX = 'qualiof-collapse-'`.
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && head -1 apps/web/src/components/ui/collapsible-section.tsx | grep -q "'use client'" && grep -q "localStorage" apps/web/src/components/ui/collapsible-section.tsx && grep -q "aria-expanded" apps/web/src/components/ui/collapsible-section.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Ligne 1 du fichier = `'use client';`
    - `grep -c "try {" apps/web/src/components/ui/collapsible-section.tsx` ≥ 2 (un pour getItem, un pour setItem)
    - `grep -n "aria-expanded" apps/web/src/components/ui/collapsible-section.tsx` retourne 1 ligne.
    - `grep -n "aria-label" apps/web/src/components/ui/collapsible-section.tsx` retourne au moins 1 ligne.
    - `grep -n "STORAGE_KEY_PREFIX = 'qualiof-collapse-'" apps/web/src/components/ui/collapsible-section.tsx` retourne 1 ligne (préfixe inchangé).
    - `pnpm --filter @qualiof/web build` succeeds.
  </acceptance_criteria>
  <done>CollapsibleSection robuste : 'use client', persistance localStorage avec try/catch, bouton avec aria-expanded + aria-label, contenu rendu conditionnellement.</done>
</task>

</tasks>

<verification>
- `grep -c "<PrioCard" apps/web/src/app/app/page.tsx` = 4
- `grep "<CollapsibleSection" apps/web/src/app/app/page.tsx | wc -l` = 1
- `pnpm --filter @qualiof/web build` clean
- Visite manuelle locale `/app` : 4 cartes grandes en haut, section "Indicateurs détaillés" fermée, ouvrir une fois → reload → toujours ouverte (preuve localStorage)
</verification>

<success_criteria>
- Dashboard hiérarchisé conforme audit 2026-05-12 UX-11
- 4 KPI prioritaires identifiés et visibles d'office
- 10+ KPI secondaires accessibles via 1 toggle
- État de la section persiste entre visites
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-hierarchisation-et-a11y/06-02-SUMMARY.md`
</output>
