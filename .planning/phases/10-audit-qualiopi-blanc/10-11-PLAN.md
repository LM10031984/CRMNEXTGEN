---
phase: 10-audit-qualiopi-blanc
plan: 11
type: execute
wave: 5
depends_on: [10-04, 10-05, 10-06, 10-07, 10-08, 10-09, 10-10]
files_modified:
  - .planning/phases/10-audit-qualiopi-blanc/10-SMOKE.md
  - .planning/phases/10-audit-qualiopi-blanc/10-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - QBLANC-01
  - QBLANC-02
  - QBLANC-03
must_haves:
  truths:
    - "Smoke build clean (pnpm --filter @qualiof/web build)"
    - "Tous les tests Vitest passent (Plans 10-01..10-10 combinés)"
    - "10-SMOKE.md documente les flows manuels validés"
    - "REQUIREMENTS.md coche QBLANC-01, QBLANC-02, QBLANC-03 avec preuves d'exécution"
    - "STATE.md référence la nouvelle convention `qualiopiBlanc.*` (8e instance one-helper-per-entity)"
    - "ROADMAP.md Phase 10 passée à 11/11 plans + Status Complete"
  artifacts:
    - path: ".planning/phases/10-audit-qualiopi-blanc/10-SMOKE.md"
      provides: "Document checklist QA manuel (flows à valider par Laurent)"
    - path: ".planning/phases/10-audit-qualiopi-blanc/10-SUMMARY.md"
      provides: "Synthèse de la phase (livrables, métriques, conventions ajoutées)"
  key_links:
    - from: "STATE.md > Workflow Conventions"
      to: "Section qualiopiBlanc.* (8e instance)"
      via: "documentation pattern AuditLog one-helper-per-entity étendu"
      pattern: "qualiopiBlanc"
---

<objective>
Clôturer la Phase 10 par : (1) lancement build clean, (2) lancement tests globaux, (3) génération du PDF en runtime pour valider WeasyPrint, (4) checkpoint manuel Laurent pour vérification UI, (5) mise à jour bookkeeping (STATE/ROADMAP/REQUIREMENTS + SMOKE + SUMMARY).

Purpose: Finaliser la Phase 10 prête pour démo auditrice 03/07/2026 et garantir la stabilité avant le checkpoint hebdo.

Output: 1 checkpoint utilisateur + 5 fichiers de bookkeeping mis à jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-audit-qualiopi-blanc/10-CONTEXT.md
@.planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md (modèle de SUMMARY phase déjà clôturée)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lancer build + tests + capturer résultats</name>
  <files>(aucun fichier modifié — collecte d'output pour SMOKE.md)</files>
  <read_first>
    - apps/web/package.json (scripts disponibles)
  </read_first>
  <action>
    Lancer en séquence :

    1. **Smoke build Next.js** (DOIT être clean) :
    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files"
    rm -rf apps/web/.next
    pnpm --filter @qualiof/web build 2>&1 | tail -30
    ```
    Capturer : route count + size de `/app/audit-qualiopi-blanc` et `/app/audit-qualiopi-blanc/sessions` et `/app/audit-qualiopi-blanc/sessions/[id]`.

    2. **Tests Vitest globaux** :
    ```bash
    pnpm --filter @qualiof/web test --run 2>&1 | tail -20
    pnpm --filter @qualiof/db test --run 2>&1 | tail -10
    pnpm --filter @qualiof/shared test --run 2>&1 | tail -10
    ```
    Capturer le compteur total (xxx tests passing).

    3. **Type check global** :
    ```bash
    pnpm --filter @qualiof/web exec tsc --noEmit 2>&1 | head -20
    ```
    DOIT être clean (0 erreur sur les fichiers Phase 10).

    4. **Worker safety final check (grep récap)** :
    ```bash
    for f in \
      apps/web/src/lib/qualiopi-blanc-audit.ts \
      apps/web/src/lib/qualiopi-blanc/core.ts \
      apps/web/src/lib/qualiopi-blanc/session-completeness.ts \
      apps/web/src/lib/qualiopi-blanc/queue.ts \
      apps/web/src/lib/qualiopi-blanc/worker.ts \
      apps/web/src/lib/qualiopi-blanc/notification-email.ts \
      apps/web/src/lib/qualiopi-blanc-pdf-template.ts \
      apps/web/src/lib/qualiopi-indicators-catalog.ts \
      ; do
      echo "=== $f ==="
      grep -E "(server/actions|/rbac|validateRequest|requireRole|from ['\"]react|next/cache|next/navigation)" "$f" || echo "  OK (no forbidden imports)"
    done
    ```

    5. **Smoke worker manuel** (si Redis up + tenant a >= 1 session avec endDate ∈ [now+6j, now+7j]) :
    ```bash
    pnpm --filter @qualiof/web worker:qualiopi-blanc &
    sleep 5
    # Voir les logs (devrait afficher: started, cron registered)
    kill %1
    ```
    Sinon : noter "Smoke worker manuel non lancé (pas de session J+7 en BDD)".
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && rm -rf apps/web/.next && pnpm --filter @qualiof/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Build Next.js termine sans erreur fatale
    - Route `/app/audit-qualiopi-blanc` apparaît dans la liste de build
    - Tests Vitest : count global stable ou augmenté (Plans 10-01..10-10 ajoutent ~30+ tests)
    - Worker safety grep : 0 forbidden import sur les 8 fichiers ciblés
    - tsc --noEmit : 0 erreur sur les fichiers Phase 10
  </acceptance_criteria>
  <done>Build + tests + worker safety vérifiés. Output capturé pour SMOKE.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint Laurent — valider UI Audit Qualiopi blanc</name>
  <what-built>
    Phase 10 complète :
    - Page /app/audit-qualiopi-blanc (scoring 23 ind + 4 PrioCard + table groupée par critère)
    - Page /app/audit-qualiopi-blanc/sessions (liste sessions avec %)
    - Drill-down /app/audit-qualiopi-blanc/sessions/[id] (matrice docs Phase 9.1 réutilisée)
    - Sidebar entry "Audit Qualiopi blanc" (ShieldCheck icon, section Suivi)
    - Bouton "Recalculer" ADMIN+MANAGER (relance calc auto Ind 9/11/12/30)
    - Bouton "Télécharger rapport audit blanc" ADMIN+MANAGER (PDF WeasyPrint 24 pages)
    - Dialog "Modifier indicateur" ADMIN+MANAGER (statut + preuve + commentaire)
    - Worker BullMQ daily 9h Europe/Paris (alertes 7j avant fin session)
    - Notification cloche + email récap (kind qualiopiBlanc.sessionIncomplete)
    - AuditLog convention `qualiopiBlanc.*` (4 verbes : entry_updated, auto_recalculated, exported, notification_sent)
  </what-built>
  <how-to-verify>
    1. **Connecter QualiOF** : `curl -sI http://localhost:3010/login` (si KO, demander à Laurent de relancer `pnpm dev:full`)

    2. **Accès page principale (ADMIN ou MANAGER)** :
       - Naviguer vers `/app/audit-qualiopi-blanc`
       - Vérifier 4 PrioCard rendus (Conforme / Non conforme / En cours / NA)
       - Score % affiché en gros
       - Tableau 23 indicateurs groupés par critère 1-7
       - Boutons "Recalculer" + "Télécharger rapport audit blanc" visibles

    3. **Sidebar** :
       - Vérifier l'entrée "Audit Qualiopi blanc" avec icône bouclier dans section Suivi
       - Cliquer dessus = retour sur la page principale

    4. **Modifier un indicateur** :
       - Cliquer sur "Modifier" sur la ligne Ind 1 (Information accessible)
       - Dialog s'ouvre avec niveau attendu + sanction NC affichés
       - Changer statut en "Non conforme", ajouter preuve "Site web à compléter", commentaire libre
       - Cliquer Enregistrer → Toast success + ligne mise à jour
       - Recharger la page → modification persistée

    5. **Recalculer auto** :
       - Cliquer "Recalculer auto (Ind 9/11/12/30)"
       - Toast affiche le nombre d'indicateurs mis à jour
       - Les 4 indicateurs auto (9, 11, 12, 30) affichent badge "Auto"

    6. **Liste sessions** :
       - Naviguer vers `/app/audit-qualiopi-blanc/sessions` (depuis lien à câbler ou URL directe)
       - Voir liste des sessions avec colonne Complétude (% coloré vert/amber/rouge)
       - Cliquer une session → drill-down avec matrice docs Phase 9.1

    7. **PDF export** :
       - Cliquer "Télécharger rapport audit blanc"
       - Attendre génération PDF (~3-5s WeasyPrint)
       - Toast success affiche un lien "Télécharger"
       - Cliquer le lien → PDF s'ouvre avec :
         - 1ère page : Synthèse Start Academy + score + Top 3 risques
         - Pages 2-24 : 1 par indicateur avec niveau attendu + sanction + recommandation pour NC
         - Footer paged sur chaque page (SIRET + NDA + adresse)

    8. **Connexion en LECTEUR** :
       - Tester en LECTEUR : pas de bouton "Recalculer" ni "Télécharger" ni "Modifier"
       - Page accessible read-only

    9. **Worker (optionnel, si data J+7 disponible)** :
       - `pnpm --filter @qualiof/web worker:qualiopi-blanc` (terminal 2)
       - Voir logs "started" + "cron registered"
       - Pour déclencher manuellement : `pnpm --filter @qualiof/web exec tsx -e "import('./src/lib/qualiopi-blanc/worker').then(m => m.processQualiopiBlancJob({ id: 'test', data: { triggered_by: 'manual' } } as any))"`
       - Vérifier cloche Notifications affiche les sessions J+7 incomplètes
  </how-to-verify>
  <resume-signal>Type "approved" si tout OK, ou détailler les bugs constatés (numéro de bug + description).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Créer 10-SMOKE.md et 10-SUMMARY.md</name>
  <files>.planning/phases/10-audit-qualiopi-blanc/10-SMOKE.md, .planning/phases/10-audit-qualiopi-blanc/10-SUMMARY.md</files>
  <read_first>
    - .planning/phases/13-veille-qualiopi-integree/13-SUMMARY.md (template SUMMARY phase clôturée)
    - .planning/phases/13-veille-qualiopi-integree/13-SMOKE.md (template SMOKE)
  </read_first>
  <action>
    Créer `.planning/phases/10-audit-qualiopi-blanc/10-SMOKE.md` avec :

    ```markdown
    # Phase 10 — Smoke test Audit Qualiopi blanc

    **Date :** {date du jour}
    **Validateur :** Laurent (à compléter)

    ## Build Next.js
    {capturer output `pnpm build` depuis Task 1}

    ## Tests Vitest
    {capturer counts depuis Task 1}

    ## Worker safety
    {capturer grep récap depuis Task 1 — DOIT être 0 forbidden import par fichier}

    ## Flow UI manuel
    {checklist correspondant aux 9 étapes du checkpoint Task 2}

    ## Flow worker manuel
    {résultat du smoke worker Task 1}

    ## Bugs constatés
    {liste depuis checkpoint Laurent}
    ```

    Créer `.planning/phases/10-audit-qualiopi-blanc/10-SUMMARY.md` avec :

    ```markdown
    # Phase 10 — Audit Qualiopi blanc · SUMMARY

    **Status :** {Complete | Partial}
    **Period :** 2026-06-{date début} → 2026-06-{date fin}
    **Plans :** 11/11
    **Tests added :** {count}

    ## Requirements addressed
    - [x] QBLANC-01 — Scoring 23 indicateurs applicables Start Academy + détection dossiers incomplets
    - [x] QBLANC-02 — Notifications 7j (cloche + email)
    - [x] QBLANC-03 — PDF rapport audit blanc téléchargeable

    ## Livrables

    ### Code (nouveaux fichiers)
    - apps/web/src/lib/qualiopi-indicators-catalog.ts (23 entries + helpers)
    - apps/web/src/lib/qualiopi-blanc-audit.ts (8e instance one-helper-per-entity)
    - apps/web/src/lib/qualiopi-blanc/{core,queue,worker,notification-email,session-completeness}.ts (worker-safe)
    - apps/web/src/lib/qualiopi-blanc-pdf-template.ts (WeasyPrint CSS Paged Media)
    - apps/web/src/server/actions/{qualiopi-blanc,qualiopi-blanc-export}.ts
    - apps/web/src/components/audit-qualiopi-blanc/{indicator-table,indicator-edit-dialog,global-score-cards,recalc-button,export-pdf-button,sessions-list}.tsx
    - apps/web/src/app/app/audit-qualiopi-blanc/{page,sessions/page,sessions/[id]/page}.tsx
    - apps/web/scripts/qualiopi-blanc-worker.ts
    - packages/shared/src/schemas/qualiopi-blanc.ts

    ### Migrations Prisma
    - phase10_qualiopi_blanc_entry (QualiopiBlancEntry + enum QualiopiBlancStatus)
    - phase10_doctype_audit_blanc (DocType += AUDIT_BLANC)

    ## Conventions ajoutées

    ### Convention #8 — AuditLog one-helper-per-entity `qualiopiBlanc.*`
    8e instance après Phase 7/8/9/9.1/11/13. Helper `apps/web/src/lib/qualiopi-blanc-audit.ts`.
    4 verbes :
    - `qualiopiBlanc.entry_updated` (UI édition manuelle)
    - `qualiopiBlanc.auto_recalculated` (worker recalc)
    - `qualiopiBlanc.exported` (PDF généré, targetEntryId='BULK')
    - `qualiopiBlanc.notification_sent` (worker alerte 7j, targetEntryId='SESSION:{id}')

    ### Convention #3 — Catalogue centralisé code-driven (2e application après Phase 12)
    `apps/web/src/lib/qualiopi-indicators-catalog.ts` avec 23 entries + 5 helpers.

    ## Patterns réutilisés
    - Phase 9.1 `<ParticipantDocMatrix>` réutilisé tel quel pour drill-down session (D-11)
    - Phase 13 `lib/veille/queue.ts` + `worker.ts` cloné pour BullMQ daily
    - Phase 13 `lib/veille-audit-template.ts` cloné pour WeasyPrint CSS Paged Media
    - Phase 9 `Notification` + `<NotificationsBell>` étendus avec nouveau kind
    - Phase 7 `lib/mailer.ts` réutilisé (dry-run si SMTP_HOST empty)

    ## Worker safety
    Tous les modules `lib/qualiopi-blanc/*.ts` ainsi que `lib/qualiopi-blanc-audit.ts`, `lib/qualiopi-blanc-pdf-template.ts`, `lib/qualiopi-indicators-catalog.ts` respectent la mémoire `feedback_worker_no_react_imports` :
    - 0 import server-actions/rbac/validateRequest/requireRole/react/next/cache/next/navigation
    - Importables depuis `scripts/qualiopi-blanc-worker.ts` (BullMQ daily cron)

    ## Métriques
    - Plans : 11
    - Tests ajoutés : ~30+ (catalog 12, audit 3, core 12, session-completeness 4, worker email 6, payload 6, pdf-template 7)
    - Migrations : 2
    - Routes ajoutées : 3 (`/app/audit-qualiopi-blanc`, `/app/audit-qualiopi-blanc/sessions`, `/app/audit-qualiopi-blanc/sessions/[id]`)
    - Server actions ajoutées : 3 (updateEntry, recalcAutoStatuses, exportQualiopiBlancReport)
    - AuditLog convention : 8e instance complète (4 verbes)

    ## Deferred to V2
    - Multi-référentiel (Bilan/VAE/RNCP-RS)
    - Plans d'action correctifs auto-générés (Tasks avec deadlines)
    - API France Compétences
    - Comparaison historique versions audits
    - Multi-tenant flag `applicable` par indicateur
    - Bouton recalc one-shot manuel via `--once` script

    ## Hard deadline
    Audit officiel Samia ZIANI BCI **2026-07-03**. Phase 10 livrée avant **2026-06-26** = 1 semaine de tests utilisateur avant l'audit.
    ```
  </action>
  <verify>
    <automated>ls "/Users/laurentmarx/Documents/CRM Next gen/files/.planning/phases/10-audit-qualiopi-blanc/10-SMOKE.md" "/Users/laurentmarx/Documents/CRM Next gen/files/.planning/phases/10-audit-qualiopi-blanc/10-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - Les 2 fichiers existent
    - SUMMARY mentionne les 11 plans + 3 requirements + 8e AuditLog convention
    - SMOKE contient output build + tests + worker safety + bugs
  </acceptance_criteria>
  <done>SUMMARY + SMOKE créés.</done>
</task>

<task type="auto">
  <name>Task 4: Mettre à jour STATE.md / ROADMAP.md / REQUIREMENTS.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (section Workflow Conventions — chercher où documenter le pattern qualiopiBlanc.* et catalogue 2e application)
    - .planning/ROADMAP.md (section Phase 10 + tableau Progress)
    - .planning/REQUIREMENTS.md (entrées QBLANC-01/02/03)
  </read_first>
  <action>
    1. **REQUIREMENTS.md** — cocher les 3 entrées Phase 10 :

    ```markdown
    - [x] **QBLANC-01** : Audit Qualiopi blanc — pour chaque session/apprenant, scoring des 32 indicateurs via `QualiopiDocCatalog` + détection des dossiers incomplets. **DONE 2026-06-{date}** — Plan 10-01 (migration QualiopiBlancEntry) + 10-02 (catalogue 23 entries) + 10-03 (core worker-safe + server actions) + 10-04 (seed enrichi DocType→Indicateur) + 10-05 (page UI 4 PrioCard + table 23 lignes groupées par critère) + 10-06 (drill-down réutilise ParticipantDocMatrix Phase 9.1).

    - [x] **QBLANC-02** : Alertes proactives — notification 7j avant fin session si dossier Qualiopi incomplet. **DONE 2026-06-{date}** — Plan 10-07 (worker BullMQ daily 9h Europe/Paris + email récap) + 10-08 (extension NotificationsBell kind qualiopiBlanc.sessionIncomplete avec Zod payload schema).

    - [x] **QBLANC-03** : Simulation passage audit Qualiopi — rapport téléchargeable (PDF) avec verdict par indicateur. **DONE 2026-06-{date}** — Plan 10-09 (template WeasyPrint CSS Paged Media + footer paged + 1 synthèse + 23 indicateurs) + 10-10 (server action + bouton UI + AuditLog `qualiopiBlanc.exported` + Document type=AUDIT_BLANC).
    ```

    Ajouter la ligne dans le tableau Traceability si pas déjà fait :
    ```
    | 10 | Audit Qualiopi blanc | QBLANC-01, QBLANC-02, QBLANC-03 |
    ```

    Vérifier qu'à la fin du document, le compteur Coverage est cohérent.

    2. **ROADMAP.md** — section Phase 10 + tableau Progress :

    Dans la section `### Phase 10:` :
    ```markdown
    ### Phase 10: Audit Qualiopi blanc
    **Goal**: Simuler un audit Qualiopi avant passage réel + alerter sur dossiers incomplets.
    **Depends on**: Nothing
    **Requirements**: [QBLANC-01, QBLANC-02, QBLANC-03]
    **Plans:** 11/11 plans complete
    **Success Criteria** (what must be TRUE):
      1. Page Qualiopi blanc : score global + détail 23 indicateurs (D-01 — 21 ind annoncés dans CONTEXT, liste réelle 23 numéros)
      2. Pour chaque session : checklist auto 10 docs Qualiopi + scoring + drill-down (réutilise ParticipantDocMatrix Phase 9.1)
      3. Notification cloche + email 7j avant fin de session si dossier incomplet
      4. Bouton "Télécharger rapport audit blanc" → PDF WeasyPrint avec verdict par indicateur
    **Plans**:
    - [x] 10-01-PLAN.md — Foundation Prisma migration QualiopiBlancEntry + enum QualiopiBlancStatus
    - [x] 10-02-PLAN.md — Catalogue 23 indicateurs + helper AuditLog qualiopiBlanc.* (8e one-helper-per-entity)
    - [x] 10-03-PLAN.md — Core worker-safe (calcAutoStatus, computeGlobalScore, upsertEntry) + server actions wrapper
    - [x] 10-04-PLAN.md — Seed QualiopiDocCatalog enrichi pour Ind 9/11/12/30 mapping
    - [x] 10-05-PLAN.md — Page UI principale + sidebar + 4 PrioCard + tableau 23 ind
    - [x] 10-06-PLAN.md — Drill-down session (réutilise ParticipantDocMatrix Phase 9.1) + helper computeSessionCompleteness
    - [x] 10-07-PLAN.md — Worker BullMQ daily 9h Europe/Paris + Notifications + email récap
    - [x] 10-08-PLAN.md — Extension NotificationsBell + Zod payload schema
    - [x] 10-09-PLAN.md — Template WeasyPrint PDF (1 synthèse + 23 pages) + DocType += AUDIT_BLANC
    - [x] 10-10-PLAN.md — Server action export + bouton UI + Document MinIO + AuditLog exported
    - [x] 10-11-PLAN.md — Bookkeeping fin de phase (SMOKE/SUMMARY/STATE/ROADMAP/REQUIREMENTS)
    ```

    Dans le tableau Progress (en bas) :
    ```
    | 10. Audit Qualiopi blanc | 11/11 | Complete    | 2026-06-{date} |
    ```

    Et changer le marqueur `- [ ]` en `- [x]` dans la liste de phases en haut.

    3. **STATE.md** — ajouter à la section Workflow Conventions :

    Chercher dans STATE.md la section "Workflow Conventions" (probablement vers le milieu) et ajouter (ou créer si absente) une entrée :

    ```markdown
    ### Convention #8 — AuditLog one-helper-per-entity (8e instance — Phase 10)
    Helper : `apps/web/src/lib/qualiopi-blanc-audit.ts`
    Entity : `QualiopiBlancEntry` (saisie audit Qualiopi blanc)
    Verbes (4) : entry_updated, auto_recalculated, exported, notification_sent
    Pattern conformity : clone-strict `regulatoryWatch-audit.ts` Phase 13. Worker safety respecté (0 import auth/react).

    ### Convention #3 — Catalogue centralisé code-driven (2e application après Phase 12)
    Fichier : `apps/web/src/lib/qualiopi-indicators-catalog.ts`
    23 entries hardcodées (23 indicateurs Qualiopi applicables Start Academy V1).
    Helpers : `getById`, `getByCriterion`, `getBySource`, `countByStatus`, `getLabel`.
    Pattern conformity : clone structure `templates-catalog.ts` Phase 12.
    ```

    Ajouter aussi position courante :
    ```
    **Position courante :** Phase 10 complète (2026-06-{date}). Prochaine étape : checkpoint Laurent + démo auditrice Samia ZIANI BCI 2026-07-03.
    ```

    Commit avec message :
    ```
    docs(phase-10): bookkeeping fin de phase (SMOKE/SUMMARY/STATE/ROADMAP/REQUIREMENTS)
    ```
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -c "QBLANC-01\|QBLANC-02\|QBLANC-03" .planning/REQUIREMENTS.md && grep -c "10. Audit Qualiopi blanc" .planning/ROADMAP.md && grep -c "qualiopiBlanc" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md : QBLANC-01/02/03 cochés `[x]` avec date DONE
    - ROADMAP.md Phase 10 marqué Complete + tableau Progress mis à jour + 11 plans listés
    - STATE.md mentionne `qualiopiBlanc` dans conventions + position courante mise à jour
  </acceptance_criteria>
  <done>3 fichiers bookkeeping mis à jour.</done>
</task>

</tasks>

<verification>
- Build clean
- Tests >= count Phase 9 + ~30 (~745+)
- 11 plans Phase 10 cochés dans ROADMAP
- 3 REQs cochés dans REQUIREMENTS
- STATE référence convention qualiopiBlanc.* + catalogue 2e application
- Checkpoint Laurent : "approved" ou bugs documentés
</verification>

<success_criteria>
- [x] Smoke build + tests verts
- [x] Worker safety vérifié sur 8 fichiers
- [x] SUMMARY + SMOKE rédigés
- [x] STATE + ROADMAP + REQUIREMENTS mis à jour
- [x] Laurent valide ou liste les bugs (checkpoint)
</success_criteria>

<output>
After completion, no further SUMMARY needed — Phase 10 close.
</output>
