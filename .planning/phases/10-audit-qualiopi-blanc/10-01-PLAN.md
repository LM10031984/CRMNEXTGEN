---
phase: 10-audit-qualiopi-blanc
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql
  - apps/web/src/lib/__tests__/qualiopi-blanc-schema.smoke.test.ts
autonomous: true
requirements:
  - QBLANC-01
must_haves:
  truths:
    - "Migration `phase10_qualiopi_blanc_entry` créée et déployée sur Postgres local (prisma migrate deploy exécuté)"
    - "Table `QualiopiBlancEntry` existe dans Postgres avec @@unique([tenantId, indicatorNumber])"
    - "Enum `QualiopiBlancStatus` (CONFORME|NON_CONFORME|EN_COURS|NA) déployé"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "model QualiopiBlancEntry + enum QualiopiBlancStatus + relation Tenant.qualiopiBlancEntries[]"
      contains: "model QualiopiBlancEntry"
    - path: "packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql"
      provides: "DDL CREATE TABLE QualiopiBlancEntry + CREATE TYPE QualiopiBlancStatus"
    - path: "apps/web/src/lib/__tests__/qualiopi-blanc-schema.smoke.test.ts"
      provides: "Smoke test Prisma client expose QualiopiBlancEntry + 4 valeurs enum"
  key_links:
    - from: "QualiopiBlancEntry.tenantId"
      to: "Tenant.id"
      via: "FK onDelete: Cascade"
      pattern: "tenantId.*references: \\[id\\]"
    - from: "QualiopiBlancEntry.preuveDocumentId"
      to: "Document.id"
      via: "optional FK (preuveDocument Document?)"
      pattern: "preuveDocument.*Document"
---

<objective>
Créer le socle Prisma pour stocker les verdicts d'audit Qualiopi blanc (1 row par couple `tenantId × indicatorNumber`, donc 21 rows par tenant en V1).

Purpose: Sans cette migration, aucun stockage de verdict possible. C'est la fondation Wave 0 dont dépendent tous les plans suivants. Respect mémoire `feedback_prisma_migrate_deploy` — `prisma migrate deploy` exécuté (pas juste generate) pour éviter `The column X does not exist`.

Output: Schema Prisma + migration SQL appliquée + smoke test Prisma client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-audit-qualiopi-blanc/10-CONTEXT.md
@packages/db/prisma/schema.prisma
@./CLAUDE.md

<interfaces>
<!-- Schemas existants à référencer pour relations -->

From packages/db/prisma/schema.prisma:
```prisma
model Tenant {
  id String @id @default(uuid())
  // ...déjà existant
  users User[]
  notifications Notification[]
  regulatoryWatches RegulatoryWatch[]
  // -> AJOUTER: qualiopiBlancEntries QualiopiBlancEntry[]
}

model Document {
  id String @id @default(uuid())
  tenantId String
  // ...
}
```

<!-- Pattern Phase 13 RegulatoryWatch (migration additive avec enum) à cloner :
     packages/db/prisma/migrations/phase13_regulatory_watch/migration.sql -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajouter model QualiopiBlancEntry + enum QualiopiBlancStatus au schema Prisma</name>
  <files>packages/db/prisma/schema.prisma</files>
  <read_first>
    - packages/db/prisma/schema.prisma (lire model Tenant lignes 24-63 pour comprendre où ajouter relation + lire model Document lignes 966-985 pour FK optionnelle)
    - .planning/phases/10-audit-qualiopi-blanc/10-CONTEXT.md (section D-06 — structure exacte du modèle)
    - packages/db/prisma/schema.prisma section RegulatoryWatch (Phase 13) comme pattern clone
  </read_first>
  <behavior>
    - Test 1: prisma.qualiopiBlancEntry est défini dans le client généré
    - Test 2: 4 valeurs d'enum QualiopiBlancStatus accessibles : CONFORME, NON_CONFORME, EN_COURS, NA
    - Test 3: Relation Tenant.qualiopiBlancEntries traversable côté typescript
  </behavior>
  <action>
    Ajouter à `packages/db/prisma/schema.prisma` (à la fin, après les models Phase 13) :

    ```prisma
    /// Phase 10 — Audit Qualiopi blanc (QBLANC-01).
    /// 1 row par couple (tenantId, indicatorNumber). 21 rows par tenant en V1
    /// (indicateurs applicables Start Academy : 1, 2, 4, 5, 6, 9, 10, 11, 12, 17, 18, 19,
    /// 21, 22, 23, 24, 25, 26, 27, 28, 30, 31, 32). Les 11 non applicables (3, 7, 8, 13, 14,
    /// 15, 16, 20, 29) ne sont jamais créés (D-01 CONTEXT).
    ///
    /// `autoCalculated=true` : statut dérivé de la matrice docs Phase 9.1
    /// (Ind 9/11/12/30 — D-02 CONTEXT). Override manuel possible via UI.
    /// `autoCalculated=false` : saisie manuelle ADMIN+MANAGER (17 autres ind).
    enum QualiopiBlancStatus {
      CONFORME
      NON_CONFORME
      EN_COURS
      NA
    }

    model QualiopiBlancEntry {
      id                String              @id @default(uuid())
      tenantId          String
      tenant            Tenant              @relation(fields: [tenantId], references: [id], onDelete: Cascade)
      indicatorNumber   Int                 // 1..32, mais seuls les 21 applicables seront créés (D-01)
      status            QualiopiBlancStatus @default(EN_COURS)
      preuve            String?             @db.Text
      preuveDocumentId  String?             // FK optionnelle Document (upload MinIO)
      preuveDocument    Document?           @relation(fields: [preuveDocumentId], references: [id], onDelete: SetNull)
      commentaire       String?             @db.Text
      lastUpdatedBy     String?             // FK User.id (pas de relation Prisma pour éviter cycle, comme Phase 8 invitedBy)
      autoCalculated    Boolean             @default(false) // true = dérivé matrix docs (Ind 9/11/12/30)
      createdAt         DateTime            @default(now())
      updatedAt         DateTime            @updatedAt

      @@unique([tenantId, indicatorNumber])
      @@index([tenantId, status])
      @@index([tenantId, autoCalculated])
    }
    ```

    Modifier `model Tenant` (ligne ~58-60) pour ajouter la relation inverse :
    ```prisma
      users                      User[]
      notifications              Notification[]
      regulatoryWatches          RegulatoryWatch[]
      qualiopiBlancEntries       QualiopiBlancEntry[]   // <-- AJOUTER cette ligne
    ```

    Modifier `model Document` pour ajouter la relation inverse :
    ```prisma
      // (à la fin des champs/relations de Document, avant les @@index)
      qualiopiBlancEntries QualiopiBlancEntry[]
    ```

    Note D-06 : enum a 4 valeurs. NE PAS ajouter d'autres valeurs (D-04 figure scope V1).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && grep -n "model QualiopiBlancEntry\|enum QualiopiBlancStatus\|qualiopiBlancEntries" packages/db/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^enum QualiopiBlancStatus" packages/db/prisma/schema.prisma` retourne 1 ligne
    - `grep -n "^model QualiopiBlancEntry" packages/db/prisma/schema.prisma` retourne 1 ligne
    - `grep -n "@@unique(\[tenantId, indicatorNumber\])" packages/db/prisma/schema.prisma` retourne 1 ligne
    - `grep -n "qualiopiBlancEntries.*QualiopiBlancEntry\[\]" packages/db/prisma/schema.prisma` retourne 2 lignes (Tenant + Document)
    - Le bloc enum contient exactement les 4 valeurs : CONFORME, NON_CONFORME, EN_COURS, NA (grep -A 5 "enum QualiopiBlancStatus")
  </acceptance_criteria>
  <done>schema.prisma contient model + enum + 2 relations inverses (Tenant, Document). Aucune autre modification.</done>
</task>

<task type="auto">
  <name>Task 2: Générer la migration Prisma + l'appliquer sur Postgres local (prisma migrate deploy)</name>
  <files>packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql</files>
  <read_first>
    - packages/db/prisma/schema.prisma (vérifier que Task 1 est appliquée)
    - ~/.claude/projects/-Users-laurentmarx-Documents-CRM-Next-gen/memory/feedback_prisma_migrate_deploy.md
    - ~/.claude/projects/-Users-laurentmarx-Documents-CRM-Next-gen/memory/feedback_prisma_db_push_sandbox.md
    - packages/db/prisma/migrations/phase13_regulatory_watch/migration.sql (pattern reference si existe)
  </read_first>
  <action>
    Générer la migration via Prisma CLI :

    ```bash
    cd "/Users/laurentmarx/Documents/CRM Next gen/files"
    pnpm --filter @qualiof/db exec prisma migrate dev --name phase10_qualiopi_blanc_entry --create-only
    ```

    Si `migrate dev --create-only` plante en sandbox (interactif), fallback :
    ```bash
    pnpm --filter @qualiof/db exec prisma migrate diff \
      --from-schema-datasource packages/db/prisma/schema.prisma \
      --to-schema-datamodel packages/db/prisma/schema.prisma \
      --script > packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql
    mkdir -p packages/db/prisma/migrations/phase10_qualiopi_blanc_entry
    ```

    Vérifier que le SQL généré contient :
    - `CREATE TYPE "QualiopiBlancStatus" AS ENUM ('CONFORME', 'NON_CONFORME', 'EN_COURS', 'NA')`
    - `CREATE TABLE "QualiopiBlancEntry"` avec toutes les colonnes
    - `CREATE UNIQUE INDEX` sur `(tenantId, indicatorNumber)`
    - `CREATE INDEX` sur `(tenantId, status)` et `(tenantId, autoCalculated)`
    - 2 `ALTER TABLE` FK pour `tenantId` (Cascade) et `preuveDocumentId` (SetNull)

    Appliquer la migration sur la base locale (NON NÉGOCIABLE — feedback_prisma_migrate_deploy) :
    ```bash
    pnpm --filter @qualiof/db exec prisma migrate deploy
    pnpm --filter @qualiof/db exec prisma generate
    ```

    Si `migrate deploy` rejette (drift), utiliser fallback sandbox (feedback_prisma_db_push_sandbox) :
    ```bash
    pnpm --filter @qualiof/db exec prisma db push --skip-generate
    pnpm --filter @qualiof/db exec prisma generate
    ```

    Vérifier table effectivement créée en Postgres :
    ```bash
    psql -U qualiof -d qualiof -c "\d \"QualiopiBlancEntry\""
    ```
  </action>
  <verify>
    <automated>ls "/Users/laurentmarx/Documents/CRM Next gen/files/packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql" && grep -c "CREATE TABLE \"QualiopiBlancEntry\"\|CREATE TYPE \"QualiopiBlancStatus\"" "/Users/laurentmarx/Documents/CRM Next gen/files/packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql"</automated>
  </verify>
  <acceptance_criteria>
    - **Mode A (préféré — migrate dev/deploy)** : fichier `packages/db/prisma/migrations/phase10_qualiopi_blanc_entry/migration.sql` existe ET contient exactement 1 `CREATE TABLE "QualiopiBlancEntry"`, 1 `CREATE TYPE "QualiopiBlancStatus"` avec 4 valeurs, et `CREATE UNIQUE INDEX` sur `tenantId, indicatorNumber`.
    - **Mode B (fallback sandbox — db push)** : pas de fichier `migration.sql` mais `psql -c '\dt'` montre la table `QualiopiBlancEntry` présente (cf `feedback_prisma_db_push_sandbox`).
    - **Dans les 2 modes** : `psql -c '\d "QualiopiBlancEntry"'` retourne la table avec 11 colonnes (id, tenantId, indicatorNumber, status, preuve, preuveDocumentId, commentaire, lastUpdatedBy, autoCalculated, createdAt, updatedAt).
    - **Dans les 2 modes** : `pnpm --filter @qualiof/db exec prisma generate` se termine sans erreur.
    - **Trace** : indiquer dans le SUMMARY quel mode a été utilisé (A ou B) pour traçabilité prod.
  </acceptance_criteria>
  <done>Migration générée + appliquée + Prisma client regénéré. La table existe en Postgres et est accessible via `prisma.qualiopiBlancEntry.findMany()`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Smoke test Prisma client expose le nouveau modèle</name>
  <files>apps/web/src/lib/__tests__/qualiopi-blanc-schema.smoke.test.ts</files>
  <read_first>
    - apps/web/src/lib/__tests__ (lister pour pattern de smoke test existant)
    - apps/web/vitest.config.ts (pour confirmer config)
  </read_first>
  <behavior>
    - Test 1: prisma.qualiopiBlancEntry.findMany existe (méthode definie)
    - Test 2: enum QualiopiBlancStatus.CONFORME accessible via @qualiof/db
    - Test 3: tous les 4 statuts de l'enum sont définis
  </behavior>
  <action>
    Créer `apps/web/src/lib/__tests__/qualiopi-blanc-schema.smoke.test.ts` :

    ```typescript
    /**
     * Phase 10 Plan 01 — Smoke test schema Prisma Phase 10.
     * Vérifie que la migration `phase10_qualiopi_blanc_entry` est bien
     * appliquée et que le client Prisma expose le nouveau modèle + enum.
     */
    import { describe, it, expect } from 'vitest';
    import { prisma, QualiopiBlancStatus } from '@qualiof/db';

    describe('Phase 10 — QualiopiBlancEntry schema', () => {
      it('prisma client expose qualiopiBlancEntry model', () => {
        expect(prisma.qualiopiBlancEntry).toBeDefined();
        expect(typeof prisma.qualiopiBlancEntry.findMany).toBe('function');
        expect(typeof prisma.qualiopiBlancEntry.upsert).toBe('function');
      });

      it('enum QualiopiBlancStatus expose 4 valeurs', () => {
        expect(QualiopiBlancStatus.CONFORME).toBe('CONFORME');
        expect(QualiopiBlancStatus.NON_CONFORME).toBe('NON_CONFORME');
        expect(QualiopiBlancStatus.EN_COURS).toBe('EN_COURS');
        expect(QualiopiBlancStatus.NA).toBe('NA');
        expect(Object.keys(QualiopiBlancStatus)).toHaveLength(4);
      });
    });
    ```

    Vérifier que `@qualiof/db` re-exporte bien `QualiopiBlancStatus` :
    ```bash
    grep -n "export" packages/db/src/index.ts
    ```
    Si l'enum n'est pas exporté, ajouter dans `packages/db/src/index.ts` :
    ```typescript
    export { QualiopiBlancStatus } from '@prisma/client';
    ```
    (Prisma client expose nativement les enums via `@prisma/client`.)
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/qualiopi-blanc-schema.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `apps/web/src/lib/__tests__/qualiopi-blanc-schema.smoke.test.ts` existe
    - 2 tests passent (prisma client + enum 4 valeurs)
    - `pnpm vitest run` ne retourne aucune erreur d'import sur `@qualiof/db`
  </acceptance_criteria>
  <done>Smoke test vert (2 tests). Prisma client + enum sont exposés et utilisables.</done>
</task>

</tasks>

<verification>
- Migration appliquée : `psql -c '\d "QualiopiBlancEntry"'` retourne la structure complète
- Prisma client régénéré : `pnpm --filter @qualiof/web exec tsc --noEmit` ne renvoie aucune erreur de typage manquant
- Smoke test vert : 2/2 tests OK
</verification>

<success_criteria>
- [x] schema.prisma contient `model QualiopiBlancEntry` + `enum QualiopiBlancStatus` + 2 relations inverses
- [x] Migration SQL générée et appliquée sur Postgres local (prisma migrate deploy OU prisma db push)
- [x] Prisma client expose `prisma.qualiopiBlancEntry` (smoke test vert)
- [x] Aucune régression sur les tests existants (`pnpm test` ne casse pas)
</success_criteria>

<output>
After completion, create `.planning/phases/10-audit-qualiopi-blanc/10-01-SUMMARY.md`
</output>
