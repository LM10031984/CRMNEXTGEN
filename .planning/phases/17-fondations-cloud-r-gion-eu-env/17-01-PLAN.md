---
phase: 17-fondations-cloud-r-gion-eu-env
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md
autonomous: true
requirements: [CLOUDENV-01]
must_haves:
  truths:
    - "Un lecteur peut vérifier « région = EU » pour Supabase, Vercel, Railway/Fly et Upstash (conditionnel) dans un seul doc .planning/"
    - "Le doc liste le code région EU exact de chaque plateforme (eu-west-3, cdg1, europe-west4/cdg, eu-central-1)"
    - "Le doc distingue ce qui est irréversible (Supabase seule) de ce qui est mutable"
    - "Le doc contient une checklist « choisir EU explicitement » anti-défaut-US pour la création (Phase 18/19/20)"
  artifacts:
    - path: ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md"
      provides: "Verrouillage écrit auditable des régions EU des 4 plateformes AVANT toute création"
      contains: "eu-west-3"
  key_links: []
---

<objective>
Verrouiller PAR ÉCRIT la région EU cible des 4 plateformes cloud (Supabase, Vercel, Railway/Fly, Upstash conditionnel) dans un document `.planning/` auditable, AVANT toute création de projet (Phase 18+). C'est le garde-fou contre le défaut régional US silencieux (Vercel `iad1`, Supabase us-east) — un choix irréversible pour Supabase.

Purpose: Le critère 1 exige qu'« un lecteur puisse vérifier région = EU pour les 4 plateformes ». Ce doc est la source de vérité pré-création. Sans lui, un projet peut être créé en US par défaut → non-conformité RGPD/Qualiopi et, pour Supabase, immuable (recréer + migrer).
Output: `.planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-RESEARCH.md
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-VALIDATION.md

<decisions>
Décisions utilisateur verrouillées (plan-time Q&A avec Laurent — NON négociables) :
- **D-01 Région = Paris.** Supabase `eu-west-3` (Paris) + Vercel `cdg1` (Paris). Rationale : données stagiaires hébergées en France = argument RGPD/Qualiopi simple, latence excellente depuis Vence. Worker host (Railway/Fly) : région EU la plus proche — Railway `europe-west4` (Amsterdam) OU Fly `cdg` (Paris) — DOCUMENTER LES DEUX, décision finale Phase 20.
- **D-02 Upstash = conditionnel.** Documenter 3 plateformes FERMES (Supabase, Vercel, Railway/Fly) + Upstash en CONDITIONNEL : « si Redis retenu en Phase 20, alors région EU (eu-central-1 Frankfurt ou eu-west-1 Irlande) ». NE PAS créer de compte/DB Upstash dans cette phase.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer 17-REGIONS.md — verrouillage écrit des régions EU des 4 plateformes</name>
  <read_first>
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-RESEARCH.md (section « Régions EU cibles » — table plateforme/région/code/immutabilité vérifiée docs officielles 2026-07)
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-VALIDATION.md (critère 1 : commande grep de vérification `grep -E "eu-west-3|cdg1|europe-west4|eu-central-1"`)
    - .planning/REQUIREMENTS.md (CLOUDENV-01 wording exact)
  </read_first>
  <files>.planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md</files>
  <action>
    Créer le fichier `.planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md`. Le doc DOIT contenir, avec ces valeurs exactes (per D-01, D-02) :

    1. **Titre + date + statut** : `# Phase 17 — Régions cloud EU (verrouillage écrit)`, `**Verrouillé:** 2026-07-04`, mention « choix pré-création, à consulter AVANT toute création de projet Phase 18/19/20 ».

    2. **Décision région (D-01)** : une phrase claire = « Région retenue : **Paris**. Données stagiaires hébergées en France (argument RGPD/Qualiopi simple), latence excellente depuis Vence. » + note « alternative cohérente écartée : tout Frankfurt (co-localisation inter-services) — la résidence FR prime pour un OF français à 2-5 utilisateurs ».

    3. **Table des 4 plateformes** (colonnes : Plateforme | Rôle | Région EU cible | Code région | Statut décision | Immutable ? | Comment fixer EU explicitement). Lignes EXACTES :
       - `Supabase` | Postgres + Storage | Paris | `eu-west-3` | **FERME** | **OUI — recréer projet + migrer si erreur** | Choisir à la création du projet (dashboard). ⚠ Défaut souvent us-east
       - `Vercel` | App + functions | Paris | `cdg1` | **FERME** | Non (config `vercel.json`) | `vercel.json` `"regions": ["cdg1"]` + Project Settings default region. ⚠ Défaut = `iad1` (Washington)
       - `Railway / Fly (worker 3ᵉ hôte)` | 3 workers + Gotenberg + WeasyPrint | Amsterdam (Railway) **OU** Paris (Fly) | Railway `europe-west4` **/** Fly `cdg` | **FERME (EU)** — plateforme exacte tranchée Phase 20 | Non (change à chaud sauf volume) | Config-as-code region + preferred region compte
       - `Upstash (Redis)` | Queue BullMQ | Frankfurt **OU** Irlande | `eu-central-1` **/** `eu-west-1` | **CONDITIONNEL** — SEULEMENT si Redis retenu Phase 20 (WORK-02) | Non (Global 2.0 add/remove sans downtime) | `primary_region` à la création. NE PAS créer de DB en Phase 17

    4. **Section « Upstash conditionnel » (D-02)** : paragraphe expliquant que la décision milestone v6 (« Redis viré au profit de Postgres SKIP LOCKED ») coexiste encore avec le code BullMQ/ioredis, que la bascule est tranchée Phase 20 sur facturation 24h (WORK-02), et donc : « SI Redis est retenu → Upstash région EU `eu-central-1` (Frankfurt). SINON → pas d'Upstash, pas de 4ᵉ plateforme. Aucun compte/DB Upstash créé en Phase 17. »

    5. **Section « Irréversibilité — le vrai risque »** : préciser que SEULE Supabase est immuable (recréer + migrer). Vercel/Railway/Upstash sont mutables. Le vrai danger = le défaut US silencieux à la création.

    6. **Checklist pré-création (anti-défaut-US)** — cases à cocher Markdown `- [ ]`, une par plateforme ferme :
       - `- [ ] Supabase : région = eu-west-3 (Paris) sélectionnée AU MOMENT de la création (immuable — vérifier 2× avant Create)`
       - `- [ ] Vercel : vercel.json contient "regions": ["cdg1"] ET Project Settings > Functions > default region = cdg1 (sinon iad1)`
       - `- [ ] Railway/Fly : region EU (europe-west4 / cdg) fixée en config-as-code + preferred region compte`
       - `- [ ] Upstash : NON créé en Phase 17 — si retenu Phase 20, primary_region = eu-central-1`

    7. **Sources** : lien vers les docs officielles citées dans 17-RESEARCH.md § Sources (Supabase regions + change-project-region, Vercel region config, Railway regions, Upstash Global 2.0).

    Style : tables Markdown lisibles, ton pédagogique et auditable (un auditeur Qualiopi doit pouvoir lire « région = EU » sans ambiguïté).
  </action>
  <verify>
    <automated>test -f ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md" && grep -Eq "eu-west-3" ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md" && grep -Eq "cdg1" ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md" && grep -Eq "europe-west4" ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md" && grep -Eq "eu-central-1" ".planning/phases/17-fondations-cloud-r-gion-eu-env/17-REGIONS.md" && echo REGIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eu-west-3" 17-REGIONS.md` ≥ 1 (Supabase Paris)
    - `grep -c "cdg1" 17-REGIONS.md` ≥ 1 (Vercel Paris)
    - `grep -Ec "europe-west4|cdg" 17-REGIONS.md` ≥ 1 (Railway/Fly EU)
    - `grep -c "eu-central-1" 17-REGIONS.md` ≥ 1 (Upstash conditionnel)
    - `grep -ci "CONDITIONNEL" 17-REGIONS.md` ≥ 1 (Upstash marqué conditionnel per D-02)
    - `grep -ci "immuable\|irréversible\|recréer" 17-REGIONS.md` ≥ 1 (note irréversibilité Supabase)
    - `grep -c "\- \[ \]" 17-REGIONS.md` ≥ 4 (checklist pré-création 4 items)
    - Les 4 plateformes (Supabase, Vercel, Railway/Fly, Upstash) apparaissent chacune dans le doc
  </acceptance_criteria>
  <done>Le doc 17-REGIONS.md existe, liste les 4 plateformes avec région EU + code exact, marque Upstash conditionnel (D-02) et Paris comme décision (D-01), distingue l'irréversibilité Supabase, et porte une checklist pré-création anti-défaut-US de 4 items.</done>
</task>

</tasks>

<verification>
- `test -f` + les 4 greps région passent (commande verify Task 1).
- Lecture humaine : un auditeur peut lire « région = EU » pour les 4 plateformes sans ambiguïté (manual-only, cf. 17-VALIDATION.md).
</verification>

<success_criteria>
CLOUDENV-01 satisfait : région EU verrouillée et documentée pour les 4 plateformes AVANT toute création de projet. Le doc est auditable (grep-vérifiable) et intègre les décisions D-01 (Paris) et D-02 (Upstash conditionnel).
</success_criteria>

<output>
After completion, create `.planning/phases/17-fondations-cloud-r-gion-eu-env/17-01-SUMMARY.md`
</output>
