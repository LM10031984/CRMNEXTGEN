---
phase: 17-fondations-cloud-r-gion-eu-env
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/env-schemas.ts
  - packages/shared/src/env.ts
  - packages/shared/src/__tests__/env.test.ts
  - turbo.json
  - .env.example
  - apps/web/next.config.mjs
  - apps/web/scripts/closure-worker.ts
  - apps/web/scripts/closure-worker-postgres.ts
  - apps/web/src/lib/storage.ts
autonomous: true
requirements: [CLOUDENV-02]
must_haves:
  truths:
    - "Les 5 clés cloud (DIRECT_URL, STORAGE_PROVIDER, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, WEASYPRINT_URL) sont déclarées et validées par t3-env dans env.ts"
    - "Le boot de l'app (next build/dev) et du worker échoue fort si une clé cloud est malformée — createEnv() s'exécute réellement au boot"
    - "storage.ts ne lit plus process.env.STORAGE_PROVIDER/SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY brut — il consomme sharedEnv"
    - "L'alias périmé DOC_ENGINE_URL est retiré de env.ts, turbo.json et .env.example"
    - "turbo.json globalEnv déclare les 5 clés cloud (invalidation cache)"
  artifacts:
    - path: "packages/shared/src/env.ts"
      provides: "5 clés cloud déclarées + validées, DOC_ENGINE_URL retiré"
      contains: "STORAGE_PROVIDER"
    - path: "packages/shared/src/env-schemas.ts"
      provides: "schémas Zod isolés testables (STORAGE_PROVIDER, WEASYPRINT_URL, DIRECT_URL)"
      contains: "STORAGE_PROVIDER_SCHEMA"
    - path: "apps/web/next.config.mjs"
      provides: "chokepoint boot — import de sharedEnv qui force createEnv() au build/dev"
      contains: "@qualiof/shared/env"
    - path: "apps/web/scripts/closure-worker.ts"
      provides: "chokepoint boot worker BullMQ — import sharedEnv fail-loud au démarrage (ne passe pas par next.config.mjs)"
      contains: "@qualiof/shared/env"
    - path: "apps/web/scripts/closure-worker-postgres.ts"
      provides: "chokepoint boot worker Postgres SKIP LOCKED — import sharedEnv fail-loud au démarrage"
      contains: "@qualiof/shared/env"
    - path: "turbo.json"
      provides: "globalEnv à jour (5 clés cloud, DOC_ENGINE_URL retiré)"
      contains: "STORAGE_PROVIDER"
  key_links:
    - from: "apps/web/next.config.mjs"
      to: "packages/shared/src/env.ts"
      via: "import '@qualiof/shared/env' après loadEnv"
      pattern: "@qualiof/shared/env"
    - from: "apps/web/src/lib/storage.ts"
      to: "packages/shared/src/env.ts"
      via: "sharedEnv.STORAGE_PROVIDER / SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY"
      pattern: "sharedEnv\\."
---

<objective>
Rendre le boot de l'app FAIL-LOUD sur 5 clés cloud (`DIRECT_URL`, `STORAGE_PROVIDER`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `WEASYPRINT_URL`), retirer l'alias mort `DOC_ENGINE_URL`, et brancher le chokepoint de boot qui manque aujourd'hui.

Purpose: DÉCOUVERTE CRITIQUE recherche — `sharedEnv` (`packages/shared/src/env.ts`) n'est importé NULLE PART dans `apps/web`. Le barrel `@qualiof/shared` (`index.ts`) ne ré-exporte que helpers/constants/schemas, PAS `env`. `createEnv()` ne s'exécute donc JAMAIS au boot → déclarer des clés ne produit AUCUN fail-loud. L'affirmation CLAUDE.md « Boots fail loud at import time » est aujourd'hui fictive. Ce plan la rend RÉELLE en (a) déclarant les 5 clés et (b) important `sharedEnv` à un chokepoint de boot (next.config.mjs + 2 scripts worker).
Output: env.ts + env-schemas.ts refondus, turbo.json + .env.example synchronisés, chokepoint boot câblé, storage.ts migré vers sharedEnv, tests hermétiques étendus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-RESEARCH.md
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-VALIDATION.md

<interfaces>
<!-- État actuel vérifié par grep/Read 2026-07-04. L'exécuteur consomme ceci sans re-explorer. -->

packages/shared/package.json exports (le subpath `./env` EXISTE déjà, importable) :
```json
"exports": {
  ".": "./src/index.ts",
  "./env": "./src/env.ts"
}
```

packages/shared/src/env-schemas.ts (état actuel — module pur SANS effet de bord) :
```typescript
export const AI_PROVIDER_SCHEMA = z.enum(['ollama','openrouter','anthropic','qualiopi-gen']).default('ollama');
export const OPENROUTER_MODEL_FAST_SCHEMA = z.string().default('anthropic/claude-haiku-4.5');
```

packages/shared/src/env.ts — bloc `server` contient déjà (lignes ~36-38) :
```typescript
GOTENBERG_URL: z.string().url().default('http://localhost:3001'),
DOC_ENGINE_URL: z.string().url().default('http://localhost:5000'),   // ← À RETIRER (alias mort)
DOC_ENGINE_TOKEN: z.string().optional(),                             // ← garder (câblé en 17-03)
```
et `runtimeEnv` (lignes ~97-99) :
```typescript
GOTENBERG_URL: process.env.GOTENBERG_URL,
DOC_ENGINE_URL: process.env.DOC_ENGINE_URL,   // ← À RETIRER
DOC_ENGINE_TOKEN: process.env.DOC_ENGINE_TOKEN,
```

storage.ts (état actuel — reads bruts à migrer) :
```typescript
const PROVIDER = (process.env.STORAGE_PROVIDER ?? 'minio') as 'minio' | 'supabase';   // ligne 23
const SUPABASE_URL = process.env.SUPABASE_URL ?? '';                                   // ligne 49
const SUPABASE_SERVICE_ROLE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY ?? '';         // ligne 50
// storage.ts:54-64 : throw runtime existant — supabase() fait `if (!SUPABASE_URL || !SUPABASE_SERVICE_ROLE_KEY) throw new Error('Supabase Storage : ... requis ...')` → CONSERVER LE THROW CONDITIONNEL quel que soit son wording exact
```

next.config.mjs (état actuel — lignes 1-14) : `import { config as loadEnv } from 'dotenv'` puis
`loadEnv({ path: '../../.env' })` (ligne 11, SANS override) et
`loadEnv({ path: '../../.env.local', override: true })` (ligne 12, AVEC override), PUIS `const nextConfig = {...}` (ligne 15). L'import chokepoint va APRÈS les 2 loadEnv, AVANT nextConfig.
⚠ Conséquence pour le test négatif fail-loud : `.env.local` est chargé avec `override: true`. Si `.env.local` (ou `.env`) contient déjà `SUPABASE_URL`, il ÉCRASERAIT la valeur malformée passée en ligne de commande → le test négatif serait faussement vert. `SUPABASE_URL` est une clé cloud NEUVE (Phase 17) → absente des .env locaux aujourd'hui, donc la valeur CLI survit. L'exécuteur DOIT le vérifier avant de conclure (voir <verify> Task 2).

Prisma : schema.prisma:22 lit déjà `env("DIRECT_URL")` (donc DIRECT_URL doit être requise). En dev, `.env.local` pose `DIRECT_URL=DATABASE_URL` (no-op).
</interfaces>

<decisions>
- **D-03 (recherche Q3, reco (a)) :** Supabase reste `.optional()` en t3-env. On NE fait PAS de superRefine (createEnv n'expose pas .superRefine sur le retour). Le throw runtime DÉJÀ présent `storage.ts` (supabase() : si provider=supabase et clés vides) suffit au fail-loud au 1er usage cloud. Ne casse pas le dev local MinIO. Simplicité > élégance.
- **D-04 :** `STORAGE_PROVIDER` = `z.enum(['minio','supabase']).default('minio')`, `WEASYPRINT_URL` = `z.string().url().default('http://localhost:5001')`, `DIRECT_URL` = `z.string().url()` (requise, Prisma la lit toujours).
- **D-05 (recherche Pattern 2, A+B partiel) :** chokepoint boot = import `@qualiof/shared/env` dans next.config.mjs + les 2 scripts worker (couvre le fail-loud même si un call site oublie). Migration `storage.ts` vers sharedEnv incluse ici (B partiel). `pdf-render.ts` (WEASYPRINT_URL + Bearer) est traité en 17-03.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Déclarer les 5 clés cloud dans env-schemas + env.ts, retirer DOC_ENGINE_URL, étendre le test hermétique</name>
  <read_first>
    - packages/shared/src/env-schemas.ts (module pur — pattern des schémas isolés à répliquer)
    - packages/shared/src/env.ts (bloc server lignes 21-81 + runtimeEnv 86-131 — voir DOC_ENGINE_URL:37/98 à retirer, DOC_ENGINE_TOKEN:38/99 à garder)
    - packages/shared/src/__tests__/env.test.ts (pattern de test des schémas isolés — Phase 16-01)
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-RESEARCH.md (§ Architecture Patterns, § Code Examples — schémas exacts)
  </read_first>
  <behavior>
    - Test : `STORAGE_PROVIDER_SCHEMA.parse('s3-random')` → throw (enum fail loud)
    - Test : `STORAGE_PROVIDER_SCHEMA.parse(undefined)` → 'minio' (défaut)
    - Test : `STORAGE_PROVIDER_SCHEMA.parse('supabase')` → 'supabase'
    - Test : `DIRECT_URL_SCHEMA.parse('pas-une-url')` → throw
    - Test : `DIRECT_URL_SCHEMA.parse('postgresql://x:y@h:5432/d')` → ne throw pas
    - Test : `WEASYPRINT_URL_SCHEMA.parse(undefined)` → 'http://localhost:5001' (défaut)
    - Test : `WEASYPRINT_URL_SCHEMA.parse('pas-url')` → throw
  </behavior>
  <files>packages/shared/src/env-schemas.ts, packages/shared/src/env.ts, packages/shared/src/__tests__/env.test.ts</files>
  <action>
    **1. `packages/shared/src/env-schemas.ts`** — ajouter 3 schémas Zod isolés (après les 2 existants), valeurs EXACTES :
    ```typescript
    export const STORAGE_PROVIDER_SCHEMA = z.enum(['minio', 'supabase']).default('minio');
    export const WEASYPRINT_URL_SCHEMA = z.string().url().default('http://localhost:5001');
    export const DIRECT_URL_SCHEMA = z.string().url();
    ```

    **2. `packages/shared/src/env.ts`** :
    - Étendre l'import + re-export ligne 17-18 : `export { AI_PROVIDER_SCHEMA, OPENROUTER_MODEL_FAST_SCHEMA, STORAGE_PROVIDER_SCHEMA, WEASYPRINT_URL_SCHEMA, DIRECT_URL_SCHEMA } from './env-schemas';` ET `import { AI_PROVIDER_SCHEMA, OPENROUTER_MODEL_FAST_SCHEMA, STORAGE_PROVIDER_SCHEMA, WEASYPRINT_URL_SCHEMA, DIRECT_URL_SCHEMA } from './env-schemas';`
    - Dans le bloc `server{}` : **SUPPRIMER** la ligne `DOC_ENGINE_URL: z.string().url().default('http://localhost:5000'),` (ligne ~37). **GARDER** `DOC_ENGINE_TOKEN: z.string().optional(),`.
    - Dans `server{}`, ajouter (regrouper avec un commentaire `// Cloud v6 (Phase 17) — Supabase Postgres/Storage + WeasyPrint`) :
    ```typescript
    DIRECT_URL: DIRECT_URL_SCHEMA,                              // Prisma directUrl (schema.prisma:22) — requise
    STORAGE_PROVIDER: STORAGE_PROVIDER_SCHEMA,                  // 'minio' (défaut) | 'supabase'
    SUPABASE_URL: z.string().url().optional(),                 // requise en runtime si STORAGE_PROVIDER=supabase (throw storage.ts)
    SUPABASE_SERVICE_ROLE_KEY: z.string().optional(),          // secret, idem
    WEASYPRINT_URL: WEASYPRINT_URL_SCHEMA,                      // moteur PDF secondaire réel (:5001), remplace DOC_ENGINE_URL mort
    ```
    - Dans `runtimeEnv{}` : **SUPPRIMER** `DOC_ENGINE_URL: process.env.DOC_ENGINE_URL,` (ligne ~98). **GARDER** `DOC_ENGINE_TOKEN: process.env.DOC_ENGINE_TOKEN,`. Ajouter les 5 mappings :
    ```typescript
    DIRECT_URL: process.env.DIRECT_URL,
    STORAGE_PROVIDER: process.env.STORAGE_PROVIDER,
    SUPABASE_URL: process.env.SUPABASE_URL,
    SUPABASE_SERVICE_ROLE_KEY: process.env.SUPABASE_SERVICE_ROLE_KEY,
    WEASYPRINT_URL: process.env.WEASYPRINT_URL,
    ```

    **3. `packages/shared/src/__tests__/env.test.ts`** — importer les 3 nouveaux schémas depuis `../env-schemas` et ajouter les blocs de test décrits dans `<behavior>` (7 assertions ci-dessus, style `expect(() => ...).toThrow()` et `expect(SCHEMA.parse(x)).toBe(y)`). Ne PAS importer `../env` (garder hermétique).

    NE PAS toucher au chokepoint boot ici (Task 2). NE PAS toucher pdf-render.ts (17-03).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/shared exec vitest run src/__tests__/env.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STORAGE_PROVIDER_SCHEMA\|WEASYPRINT_URL_SCHEMA\|DIRECT_URL_SCHEMA" packages/shared/src/env-schemas.ts` = 3 (déclarations)
    - `grep -Ec "DIRECT_URL:|STORAGE_PROVIDER:|SUPABASE_URL:|SUPABASE_SERVICE_ROLE_KEY:|WEASYPRINT_URL:" packages/shared/src/env.ts` ≥ 10 (5 dans server + 5 dans runtimeEnv)
    - `grep -c "DOC_ENGINE_URL" packages/shared/src/env.ts` = 0 (alias mort retiré)
    - `grep -c "DOC_ENGINE_TOKEN" packages/shared/src/env.ts` = 2 (conservé, server + runtimeEnv)
    - `pnpm --filter @qualiof/shared exec vitest run src/__tests__/env.test.ts` → tous verts, ≥ 4 nouveaux tests ajoutés
    - `grep -c "STORAGE_PROVIDER_SCHEMA\|WEASYPRINT_URL_SCHEMA\|DIRECT_URL_SCHEMA" packages/shared/src/__tests__/env.test.ts` ≥ 3 (importés + testés)
  </acceptance_criteria>
  <done>Les 5 clés cloud sont déclarées (server + runtimeEnv) avec les schémas exacts D-04, DOC_ENGINE_URL est retiré de env.ts, DOC_ENGINE_TOKEN conservé, et le test hermétique passe avec 7 nouvelles assertions sur les 3 schémas isolés.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler le chokepoint boot (next.config.mjs + 2 workers), migrer storage.ts vers sharedEnv, synchroniser turbo.json + .env.example, prouver le fail-loud réel (build positif + négatif)</name>
  <read_first>
    - apps/web/next.config.mjs (lignes 1-14 : les 2 loadEnv dotenv — ligne 11 `.env` sans override, ligne 12 `.env.local` avec override:true — puis nextConfig ligne 15. L'import va entre loadEnv et nextConfig)
    - apps/web/scripts/closure-worker.ts (en-tête + premiers imports)
    - apps/web/scripts/closure-worker-postgres.ts (en-tête + premiers imports)
    - apps/web/src/lib/storage.ts (lignes 14-65 : reads bruts STORAGE_PROVIDER:23, SUPABASE_URL:49, SUPABASE_SERVICE_ROLE_KEY:50 + throw runtime conditionnel dans supabase() 54-64 à conserver quel que soit son wording)
    - turbo.json (globalEnv lignes 4-45 — DOC_ENGINE_URL:14 à retirer)
    - .env.example (lignes 24-28 : bloc GOTENBERG + DOC_ENGINE_URL:28 à remplacer)
    - packages/shared/src/env.ts (résultat de Task 1 — sharedEnv exporte maintenant les 5 clés)
  </read_first>
  <files>apps/web/next.config.mjs, apps/web/scripts/closure-worker.ts, apps/web/scripts/closure-worker-postgres.ts, apps/web/src/lib/storage.ts, turbo.json, .env.example</files>
  <action>
    **1. Chokepoint boot — `apps/web/next.config.mjs`** : ajouter APRÈS les 2 lignes `loadEnv(...)` (ligne 12) et AVANT `const nextConfig` (ligne 15) :
    ```javascript
    // Chokepoint fail-loud (Phase 17) : force createEnv() de @qualiof/shared/env à
    // s'exécuter au boot. Sans cet import, sharedEnv n'était chargé nulle part et la
    // validation t3-env ne tournait jamais (CLAUDE.md « fail loud » était fictif).
    // Une des 5 clés cloud manquante/malformée fait désormais échouer next build/dev.
    await import('@qualiof/shared/env');
    ```
    (Utiliser `await import(...)` dynamique car next.config.mjs charge dotenv d'abord ; l'import statique en tête s'évaluerait AVANT loadEnv → env vide. L'`await import` après loadEnv garantit que .env est chargé quand createEnv s'exécute.)

    **2. Chokepoint boot — worker : `apps/web/scripts/closure-worker.ts` ET `apps/web/scripts/closure-worker-postgres.ts`** : ajouter en TÊTE des imports (première ligne d'import du fichier, avant les autres) :
    ```typescript
    import '@qualiof/shared/env'; // fail-loud au démarrage worker (ne passe pas par next.config.mjs)
    ```
    (Ici l'import statique en tête est correct : les scripts worker sont lancés via tsx avec dotenv déjà chargé par leur commande pnpm/dotenv-cli. Si le worker charge lui-même dotenv plus bas, garder cet import après le chargement dotenv éventuel — vérifier l'en-tête de chaque script et placer l'import juste après tout `loadEnv`/`config()` dotenv s'il existe, sinon en toute première ligne.)

    **3. Migrer `apps/web/src/lib/storage.ts` vers sharedEnv** :
    - LIRE d'abord storage.ts INTÉGRALEMENT pour repérer le throw conditionnel dans `supabase()` (aujourd'hui `if (!SUPABASE_URL || !SUPABASE_SERVICE_ROLE_KEY) throw new Error('Supabase Storage : ... requis ...')`).
    - Ajouter en tête l'import : `import { sharedEnv } from '@qualiof/shared/env';`
    - Ligne ~23 : remplacer `const PROVIDER = (process.env.STORAGE_PROVIDER ?? 'minio') as 'minio' | 'supabase';` par `const PROVIDER = sharedEnv.STORAGE_PROVIDER;` (le type est déjà `'minio' | 'supabase'` via l'enum Zod — retirer le cast `as`).
    - Ligne ~49 : remplacer `const SUPABASE_URL = process.env.SUPABASE_URL ?? '';` par `const SUPABASE_URL = sharedEnv.SUPABASE_URL ?? '';`
    - Ligne ~50 : remplacer `const SUPABASE_SERVICE_ROLE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY ?? '';` par `const SUPABASE_SERVICE_ROLE_KEY = sharedEnv.SUPABASE_SERVICE_ROLE_KEY ?? '';`
    - **PRÉSERVER TEL QUEL le throw conditionnel** dans `supabase()` (si provider=supabase et l'une des 2 clés vide → throw) — c'est le fail-loud conditionnel Supabase per D-03. Ne PAS reformuler son message ni sa condition.
    - NE PAS toucher aux reads S3_* (déjà défaultés en env.ts, hors scope 5 clés).

    **4. `turbo.json` globalEnv** : RETIRER `"DOC_ENGINE_URL",` (ligne 14). AJOUTER les 5 clés dans le tableau globalEnv (n'importe où, garder cohérent avec le voisinage) :
    ```
    "DIRECT_URL",
    "STORAGE_PROVIDER",
    "SUPABASE_URL",
    "SUPABASE_SERVICE_ROLE_KEY",
    "WEASYPRINT_URL",
    ```
    (JSON valide — attention aux virgules.)

    **5. `.env.example`** : ligne 27-28, remplacer le bloc :
    ```
    # === Doc Engine (microservice Python à créer en palier 3) ===
    DOC_ENGINE_URL="http://localhost:5000"
    ```
    par :
    ```
    # === WeasyPrint (moteur PDF secondaire réel, remplace l'alias mort DOC_ENGINE_URL) ===
    WEASYPRINT_URL="http://localhost:5001"
    # Bearer optionnel sur Gotenberg/WeasyPrint (câblé Phase 17, enforcé server-side Phase 20/21)
    DOC_ENGINE_TOKEN=""
    ```
    ET ajouter, sous la ligne `DATABASE_URL=...` (ligne 10), un bloc cloud v6 :
    ```
    # === Cloud v6 (Phase 17) — Supabase Postgres + Storage ===
    # DIRECT_URL : connexion directe Prisma (migrate deploy). En dev local = DATABASE_URL (no-op).
    DIRECT_URL="postgresql://qualiof:qualiof_dev@localhost:5432/qualiof?schema=public"
    # STORAGE_PROVIDER : 'minio' (défaut local) | 'supabase' (cloud, Phase 18)
    STORAGE_PROVIDER="minio"
    # SUPABASE_* : requis SEULEMENT si STORAGE_PROVIDER=supabase
    SUPABASE_URL=""
    SUPABASE_SERVICE_ROLE_KEY=""
    ```
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && \
grep -q "@qualiof/shared/env" apps/web/next.config.mjs && \
grep -q "@qualiof/shared/env" apps/web/scripts/closure-worker.ts && \
grep -q "@qualiof/shared/env" apps/web/scripts/closure-worker-postgres.ts && \
test $(grep -c "process.env.STORAGE_PROVIDER\|process.env.SUPABASE_URL\|process.env.SUPABASE_SERVICE_ROLE_KEY" apps/web/src/lib/storage.ts) -eq 0 && \
test $(grep -c "sharedEnv\." apps/web/src/lib/storage.ts) -ge 3 && \
grep -Eq "throw new Error\(.*[Ss]upabase" apps/web/src/lib/storage.ts && \
test $(grep -c "DOC_ENGINE_URL" turbo.json .env.example) -eq 0 && \
node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8'))" && \
echo STRUCTURE_OK && \
echo "--- Garde-fou anti-poison dotenv : SUPABASE_URL ne doit PAS être posé par les .env locaux (sinon override:true écrase la valeur CLI du test négatif) ---" && \
{ grep -Eq "^\s*SUPABASE_URL\s*=" .env 2>/dev/null || grep -Eq "^\s*SUPABASE_URL\s*=" .env.local 2>/dev/null; } && { echo "WARN: SUPABASE_URL présent dans .env/.env.local — le test négatif ci-dessous doit utiliser DIRECT_URL (clé requise absente des .env) à la place"; NEGVAR=DIRECT_URL; } || NEGVAR=SUPABASE_URL; \
echo "--- Test POSITIF : build avec env local valide doit PASSER (prouve que await import('@qualiof/shared/env') ne casse pas le build ESM) ---" && \
pnpm --filter @qualiof/web build && echo BUILD_VALID_OK && \
echo "--- Test NÉGATIF : env cloud malformé doit faire THROW le build (createEnv fail-loud réel au boot) ---" && \
( env SUPABASE_URL="pas-url" STORAGE_PROVIDER=supabase DIRECT_URL="pas-url" SKIP_ENV_VALIDATION=false pnpm --filter @qualiof/web build 2>&1 || true ) | grep -qiE "invalid|zoderror|environment|url" && echo FAILLOUD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@qualiof/shared/env" apps/web/next.config.mjs` ≥ 1 (chokepoint next, via `await import`)
    - `grep -c "@qualiof/shared/env" apps/web/scripts/closure-worker.ts` ≥ 1 ET idem `closure-worker-postgres.ts` (chokepoint worker × 2)
    - `grep -c "process.env.STORAGE_PROVIDER\|process.env.SUPABASE_URL\|process.env.SUPABASE_SERVICE_ROLE_KEY" apps/web/src/lib/storage.ts` = 0 (migré vers sharedEnv)
    - `grep -c "sharedEnv\." apps/web/src/lib/storage.ts` ≥ 3 (les 3 reads migrés)
    - `grep -Ec "throw new Error\(.*[Ss]upabase" apps/web/src/lib/storage.ts` ≥ 1 (throw runtime conditionnel Supabase conservé, D-03 — grep robuste sur la logique throw+supabase, pas sur le wording exact)
    - `grep -Ec "DIRECT_URL|STORAGE_PROVIDER|SUPABASE_URL|SUPABASE_SERVICE_ROLE_KEY|WEASYPRINT_URL" turbo.json` ≥ 5 (5 clés globalEnv)
    - `grep -c "DOC_ENGINE_URL" turbo.json .env.example` = 0 (retiré des 2)
    - `grep -c "WEASYPRINT_URL" .env.example` ≥ 1 ET `grep -c "DIRECT_URL" .env.example` ≥ 1
    - `node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8'))"` → pas d'erreur (JSON valide)
    - **BUILD POSITIF** : `pnpm --filter @qualiof/web build` avec env local valide → exit 0 (prouve que le `await import('@qualiof/shared/env')` ajouté n'a pas cassé le build ESM/syntaxique). Marqueur `BUILD_VALID_OK` affiché.
    - **BUILD NÉGATIF (fail-loud réel, critère 2b)** : `SUPABASE_URL="pas-url" STORAGE_PROVIDER=supabase DIRECT_URL="pas-url" SKIP_ENV_VALIDATION=false pnpm --filter @qualiof/web build` → le build ÉCHOUE avec un message ZodError/`Invalid url`/environment (createEnv throw au boot). Marqueur `FAILLOUD_OK` affiché. ⚠ Garde-fou : si `.env`/`.env.local` posent déjà `SUPABASE_URL` (override:true dans next.config.mjs l'écraserait), utiliser `DIRECT_URL="pas-url"` (clé REQUISE et absente des .env locaux) comme variable malformée forçante — le verify le détecte et bascule automatiquement.
  </acceptance_criteria>
  <done>Le chokepoint boot importe sharedEnv dans next.config.mjs + les 2 workers, storage.ts consomme sharedEnv (0 process.env brut sur les 3 clés, throw conditionnel Supabase conservé), turbo.json globalEnv déclare les 5 clés et retire DOC_ENGINE_URL, .env.example documente les 5 clés et retire DOC_ENGINE_URL. Le fail-loud est PROUVÉ mécaniquement : build POSITIF vert (env valide) ET build NÉGATIF throw (env cloud malformé), la variable malformée forçante étant choisie pour ne pas être écrasée par le override:true de dotenv.</done>
</task>

</tasks>

<verification>
- Test hermétique env : `pnpm --filter @qualiof/shared exec vitest run src/__tests__/env.test.ts` vert (Task 1).
- Chokepoint + migration : les greps d'acceptance Task 2 verts + `turbo.json` JSON valide.
- **Preuve fail-loud réel intégrée à la <verify> de Task 2 (plus reportée au gate)** : build POSITIF (env valide) exit 0 = `BUILD_VALID_OK` ; build NÉGATIF (env cloud malformé, variable forçante non écrasée par dotenv override) throw = `FAILLOUD_OK`. Les deux prouvent que createEnv() s'exécute réellement au boot ET que l'`await import` n'a pas cassé le build.
- Full suite shared + web non régressée (baseline : seul échec toléré = `shared-template.test.ts:175` MIME jpeg/jpg PRÉ-EXISTANT hors scope).
</verification>

<success_criteria>
CLOUDENV-02 satisfait : 5 clés cloud déclarées + validées t3-env, boot fail-loud RÉEL et PROUVÉ (chokepoint next + worker ; build positif vert + build négatif throw dans la <verify>), 0 process.env brut restant sur STORAGE_PROVIDER/SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY dans storage.ts, turbo.json globalEnv à jour (5 clés + DOC_ENGINE_URL retiré), .env.example synchronisé.
</success_criteria>

<output>
After completion, create `.planning/phases/17-fondations-cloud-r-gion-eu-env/17-02-SUMMARY.md`. Noter dans le SUMMARY que Laurent doit ajouter les nouvelles clés (au moins `DIRECT_URL`) à son `.env.local` sinon le boot échoue après la refonte — c'est l'effet voulu (fail-loud) mais à signaler. Documenter aussi le résultat des deux builds (positif `BUILD_VALID_OK` + négatif `FAILLOUD_OK`) et quelle variable malformée forçante a été utilisée pour le test négatif (SUPABASE_URL ou fallback DIRECT_URL selon la présence dans les .env locaux).
</output>
