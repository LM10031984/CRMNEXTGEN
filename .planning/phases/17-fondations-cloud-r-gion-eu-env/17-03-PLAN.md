---
phase: 17-fondations-cloud-r-gion-eu-env
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - apps/web/src/lib/pdf-render.ts
  - apps/web/src/lib/__tests__/pdf-render.test.ts
autonomous: true
requirements: [CLOUDENV-03]
must_haves:
  truths:
    - "Un appel Gotenberg (renderHtmlToPdf) porte un header Authorization: Bearer issu de DOC_ENGINE_TOKEN quand le token est défini"
    - "Un appel WeasyPrint (renderHtmlToPdfWeasy) porte le même header Bearer"
    - "Sans DOC_ENGINE_TOKEN (dev local), aucun header Authorization n'est ajouté — les 2 fonctions marchent toujours"
    - "L'appel Gotenberg multipart ne fixe PAS de Content-Type manuel (boundary FormData préservé, footer HTML non régressé)"
    - "pdf-render.ts lit WEASYPRINT_URL et GOTENBERG_URL depuis sharedEnv, plus depuis process.env brut"
  artifacts:
    - path: "apps/web/src/lib/pdf-render.ts"
      provides: "Bearer DOC_ENGINE_TOKEN sur Gotenberg + WeasyPrint, URLs via sharedEnv"
      contains: "Authorization"
    - path: "apps/web/src/lib/__tests__/pdf-render.test.ts"
      provides: "Test mock fetch : Bearer présent sur les 2 fonctions, mutation-safe"
      contains: "Authorization"
  key_links:
    - from: "apps/web/src/lib/pdf-render.ts"
      to: "sharedEnv.DOC_ENGINE_TOKEN"
      via: "header Authorization: Bearer conditionnel"
      pattern: "Authorization.*Bearer"
    - from: "apps/web/src/lib/pdf-render.ts"
      to: "sharedEnv.WEASYPRINT_URL"
      via: "consommation env validée"
      pattern: "sharedEnv\\."
---

<objective>
Câbler `DOC_ENGINE_TOKEN` (déclaré `env.ts:38`, JAMAIS consommé jusqu'ici) en header `Authorization: Bearer` sur les 2 fonctions de rendu PDF de `pdf-render.ts` (Gotenberg + WeasyPrint), et migrer les URLs (`WEASYPRINT_URL`, `GOTENBERG_URL`) de `process.env` brut vers `sharedEnv`.

Purpose: Fondement de l'Option A « dual-ingress public authentifié » (décision v6). Les doc-engines seront exposés en HTTPS public (Phase 20/21) et il faut un Bearer côté client dès maintenant. Phase 17 câble UNIQUEMENT le CLIENT (le token optionnel → header omis si absent, dev local non cassé) ; l'enforcement server-side WeasyPrint/Gotenberg est Phase 20/21.
Output: pdf-render.ts avec Bearer conditionnel + URLs sharedEnv, nouveau test hermétique mutation-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-RESEARCH.md
@.planning/phases/17-fondations-cloud-r-gion-eu-env/17-VALIDATION.md

<interfaces>
<!-- État actuel pdf-render.ts vérifié par Read 2026-07-04. Après 17-02, sharedEnv exporte WEASYPRINT_URL/GOTENBERG_URL/DOC_ENGINE_TOKEN. -->

pdf-render.ts a 2 fonctions exportées :
```typescript
// ligne 13-14 : reads bruts actuels
const GOTENBERG_URL = process.env.GOTENBERG_URL ?? 'http://localhost:3001';
const WEASYPRINT_URL = process.env.WEASYPRINT_URL ?? 'http://localhost:5001';

// renderHtmlToPdf (Gotenberg, MULTIPART FormData) — fetch SANS headers aujourd'hui (ligne 44-47) :
const res = await fetch(`${GOTENBERG_URL}/forms/chromium/convert/html`, {
  method: 'POST',
  body: form,        // ← FormData : NE JAMAIS fixer Content-Type manuel (casse le boundary)
});

// renderHtmlToPdfWeasy (WeasyPrint, body string) — headers Content-Type seul (ligne 62-67) :
const res = await fetch(`${WEASYPRINT_URL}/pdf`, {
  method: 'POST',
  headers: { 'Content-Type': 'text/html; charset=utf-8' },
  body: html,
});
```

Après 17-02, `import { sharedEnv } from '@qualiof/shared/env';` expose :
- `sharedEnv.GOTENBERG_URL` (default 'http://localhost:3001')
- `sharedEnv.WEASYPRINT_URL` (default 'http://localhost:5001')
- `sharedEnv.DOC_ENGINE_TOKEN` (string | undefined — optional)
</interfaces>

<decisions>
- **D-06 (recherche Pattern 3 + Pitfall 3) :** le Bearer est CONDITIONNEL au token (`token ? { Authorization: ... } : {}`) pour ne pas casser le dev local sans token. Sur Gotenberg (multipart), n'ajouter QUE `Authorization` dans headers — laisser fetch générer le Content-Type/boundary. Sur WeasyPrint (body string), spread le Bearer À CÔTÉ du `Content-Type: text/html` existant.
- **Anti-pattern INTERDIT :** ne PAS ajouter `Content-Type: multipart/form-data` manuel sur Gotenberg → casserait le boundary → 400, tous les PDF cassent (facture, convocation, closure). Le footer HTML dans body FormData doit rester INTACT (CLAUDE.md).
- **Test de puissance (convention projet) au gate :** retirer le header Bearer dans pdf-render.ts → pdf-render.test.ts doit passer AU ROUGE → restaurer. À exécuter au phase gate.
</decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer le test hermétique pdf-render.test.ts (Bearer sur les 2 fonctions, mutation-safe) puis câbler le Bearer + migrer les URLs vers sharedEnv</name>
  <read_first>
    - apps/web/src/lib/pdf-render.ts (INTÉGRAL — 2 fonctions, structure fetch, multipart Gotenberg vs body string WeasyPrint)
    - packages/shared/src/env.ts (résultat 17-02 : sharedEnv.GOTENBERG_URL / WEASYPRINT_URL / DOC_ENGINE_TOKEN exportés)
    - apps/web/src/lib/closure/__tests__ OU tout test existant du dossier lib qui mocke fetch/env (pattern de mock hermétique établi Phase 16 : mock direct du module, vi.hoisted si besoin)
    - .planning/phases/17-fondations-cloud-r-gion-eu-env/17-RESEARCH.md (§ Pattern 3 Code Examples — câblage Bearer exact ; § Pitfall 3 — piège multipart)
  </read_first>
  <behavior>
    - Test 1 (Gotenberg avec token) : `DOC_ENGINE_TOKEN='tok-abc'` set → `renderHtmlToPdf('<p>x</p>')` → le fetch mocké vers `/forms/chromium/convert/html` reçoit `headers.Authorization === 'Bearer tok-abc'`
    - Test 2 (Gotenberg multipart préservé) : le même appel NE fixe PAS `Content-Type` dans headers (le body reste FormData, boundary auto) → `headers['Content-Type']` absent/undefined
    - Test 3 (WeasyPrint avec token) : `renderHtmlToPdfWeasy('<p>x</p>')` → fetch vers `/pdf` reçoit `headers.Authorization === 'Bearer tok-abc'` ET `headers['Content-Type'] === 'text/html; charset=utf-8'` (les deux coexistent)
    - Test 4 (sans token = dev local) : `DOC_ENGINE_TOKEN` undefined → les 2 fonctions n'ajoutent AUCUN header Authorization (absent/undefined) et ne throw pas
    - Mutation-safe : si on retire l'ajout du header Bearer dans la source, Test 1 et Test 3 passent au rouge.
  </behavior>
  <files>apps/web/src/lib/__tests__/pdf-render.test.ts, apps/web/src/lib/pdf-render.ts</files>
  <action>
    **1. RED — Créer `apps/web/src/lib/__tests__/pdf-render.test.ts`** (hermétique, Vitest) :
    - Mocker `@qualiof/shared/env` pour contrôler `sharedEnv.DOC_ENGINE_TOKEN` / `GOTENBERG_URL` / `WEASYPRINT_URL` par test (via `vi.mock('@qualiof/shared/env', ...)` + réglage par test, ou `vi.doMock` + import dynamique si le token doit varier — préférer un mock avec un objet mutable réglé avant chaque `it`).
    - Mocker `global.fetch` avec `vi.fn()` retournant `{ ok: true, arrayBuffer: async () => new ArrayBuffer(8) }`.
    - Écrire les 4 tests décrits dans `<behavior>`. Pour Test 1/3, asserter sur `fetchMock.mock.calls[0][1].headers` : lire la clé `Authorization` (attention : si le code construit un objet `Record<string,string>`, l'assertion lit directement `.Authorization` ; si `Headers`, adapter avec `.get('Authorization')` — construire côté source un objet plain `Record<string,string>` pour simplicité d'assertion).
    - Ajouter un commentaire de protocole de mutation (feedback_test_de_puissance_mutation) : « retirer l'ajout du Bearer → Test 1/3 rouge ».
    - Lancer le test → il doit ÉCHOUER (RED) car la source n'ajoute pas encore le Bearer.

    **2. GREEN — Modifier `apps/web/src/lib/pdf-render.ts`** :
    - Ajouter en tête : `import { sharedEnv } from '@qualiof/shared/env';`
    - Remplacer les 2 constantes lignes 13-14 :
      ```typescript
      const GOTENBERG_URL = sharedEnv.GOTENBERG_URL;
      const WEASYPRINT_URL = sharedEnv.WEASYPRINT_URL;
      ```
    - Ajouter un helper d'entête (juste après les constantes) :
      ```typescript
      function authHeaders(): Record<string, string> {
        const token = sharedEnv.DOC_ENGINE_TOKEN;
        return token ? { Authorization: `Bearer ${token}` } : {};
      }
      ```
    - Dans `renderHtmlToPdf` (Gotenberg) : ajouter `headers: authHeaders(),` à l'objet fetch. NE PAS ajouter de `Content-Type` (multipart FormData → boundary auto). Résultat :
      ```typescript
      const res = await fetch(`${GOTENBERG_URL}/forms/chromium/convert/html`, {
        method: 'POST',
        headers: authHeaders(),
        body: form,
      });
      ```
    - Dans `renderHtmlToPdfWeasy` (WeasyPrint) : spread le Bearer À CÔTÉ du Content-Type existant :
      ```typescript
      const res = await fetch(`${WEASYPRINT_URL}/pdf`, {
        method: 'POST',
        headers: { 'Content-Type': 'text/html; charset=utf-8', ...authHeaders() },
        body: html,
      });
      ```
    - Relancer le test → GREEN (4/4).

    NE PAS toucher au corps FormData / marges / footer HTML (préserver le rendu).
  </action>
  <verify>
    <automated>cd "/Users/laurentmarx/Documents/CRM Next gen/files" && pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/pdf-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "Authorization.*Bearer" apps/web/src/lib/pdf-render.ts` ≥ 1 (helper authHeaders présent)
    - `grep -c "headers: authHeaders()" apps/web/src/lib/pdf-render.ts` = 1 (Gotenberg : header sans Content-Type manuel)
    - `grep -c "...authHeaders()" apps/web/src/lib/pdf-render.ts` = 1 (WeasyPrint : spread à côté du Content-Type)
    - `grep -c "process.env.GOTENBERG_URL\|process.env.WEASYPRINT_URL" apps/web/src/lib/pdf-render.ts` = 0 (URLs migrées vers sharedEnv)
    - `grep -c "sharedEnv\." apps/web/src/lib/pdf-render.ts` ≥ 3 (GOTENBERG_URL + WEASYPRINT_URL + DOC_ENGINE_TOKEN)
    - `grep -c "multipart/form-data\|'Content-Type': 'multipart" apps/web/src/lib/pdf-render.ts` = 0 (piège Gotenberg évité — aucun Content-Type multipart manuel)
    - `grep -c "position:fixed\|footer" apps/web/src/lib/pdf-render.ts` inchangé vs baseline (footer HTML non régressé — le body FormData n'est pas touché)
    - `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/pdf-render.test.ts` → 4/4 verts
    - `grep -c "Authorization" apps/web/src/lib/__tests__/pdf-render.test.ts` ≥ 2 (assertions Bearer sur les 2 fonctions)
  </acceptance_criteria>
  <done>pdf-render.ts ajoute un helper authHeaders() qui pose Authorization: Bearer <DOC_ENGINE_TOKEN> conditionnellement sur Gotenberg (headers-only, pas de Content-Type manuel) et WeasyPrint (spread à côté de Content-Type), lit GOTENBERG_URL/WEASYPRINT_URL via sharedEnv, et le test hermétique 4/4 passe (mutation-safe : retrait du Bearer → rouge).</done>
</task>

</tasks>

<verification>
- Test unitaire : `pnpm --filter @qualiof/web exec vitest run src/lib/__tests__/pdf-render.test.ts` → 4/4 verts.
- Greps d'acceptance Task 1 verts (Bearer présent, pas de Content-Type multipart manuel, URLs sharedEnv, footer non régressé).
- **Test de puissance (phase gate, convention projet)** : retirer temporairement `headers: authHeaders(),` de Gotenberg → Test 1 rouge → restaurer → 4/4 (documenter dans SUMMARY, ne PAS committer la mutation).
- Full suite web non régressée (baseline : seul échec toléré = `shared-template.test.ts:175` MIME jpeg/jpg PRÉ-EXISTANT hors scope).
</verification>

<success_criteria>
CLOUDENV-03 satisfait : DOC_ENGINE_TOKEN câblé en Bearer sur les 2 chemins de rendu PDF (Gotenberg + WeasyPrint), conditionnel au token (dev local sans token non cassé), multipart Gotenberg préservé (0 Content-Type manuel, footer HTML intact), URLs migrées vers sharedEnv, test mutation-safe vert.
</success_criteria>

<output>
After completion, create `.planning/phases/17-fondations-cloud-r-gion-eu-env/17-03-SUMMARY.md`
</output>
