---
phase: 16-migration-ia-ollama-vers-claude-api
plan: 06
type: execute
wave: 4
depends_on: ["16-02", "16-03", "16-04", "16-05"]
files_modified:
  - .planning/phases/16-migration-ia-ollama-vers-claude-api/16-WITNESS.md
autonomous: false
requirements: [REQ-16-05, REQ-16-07]
must_haves:
  truths:
    - "Un pack closure témoin réel généré en AI_PROVIDER=openrouter produit 0 stub et du contenu Qualiopi varié/conforme"
    - "La base légale RGPD/DPA (sous-traitants OpenRouter + Anthropic) est tranchée AVANT toute mise en prod de la vision cloud"
  artifacts:
    - path: ".planning/phases/16-migration-ia-ollama-vers-claude-api/16-WITNESS.md"
      provides: "Trace du pack témoin (session, résultats, 0 stub) + statut du gate RGPD vision"
  key_links:
    - from: "AI_PROVIDER=openrouter"
      to: "pack closure réel"
      via: "worker BullMQ closure sur 1 session témoin"
      pattern: "0 stub"
---

<objective>
Valider la migration de bout en bout par un pack closure TÉMOIN réel (Claude cloud) et trancher le gate RGPD vision. La qualité prompt (D-04c) et le stub-rate ne sont mesurables qu'en réel (leçon "vu à l'œil", pas au hash). Ce plan a 2 checkpoints humains : (1) génération + revue du pack témoin par Laurent ; (2) confirmation du feu vert RGPD/DPA avant toute mise en prod de la vision cloud (D-02b, action HORS code mais bloquante).

Purpose: Phase gate — suite verte + 1 pack témoin réel 0-stub validé + RGPD vision tranché, AVANT /gsd:verify-work.
Output: 16-WITNESS.md (trace) + décisions gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-CONTEXT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pré-checks automatiques avant génération témoin</name>
  <read_first>
    - .planning/phases/16-migration-ia-ollama-vers-claude-api/16-VALIDATION.md (phase gate)
    - CLAUDE.md (port dev 3010, worker concurrency/timeout)
  </read_first>
  <action>
    1. Vérifier suite verte + tsc clean : `pnpm --filter @qualiof/web test`, `pnpm --filter @qualiof/web tsc --noEmit`, `pnpm --filter @qualiof/shared test`.
    2. Confirmer l'absence de call sites vision/veille legacy résiduels : `grep -rn "callOllama" apps/web/src/lib/veille/classify.ts apps/web/src/lib/preinscription-extractor.ts apps/web/src/lib/pdf-extract.ts` doit être vide. (Le closure garde le switch `callOllama` pour le mode dev local Ollama — c'est ATTENDU, ne pas le compter comme résidu.)
    3. Confirmer que `OPENROUTER_API_KEY` est renseignée dans `.env` (valeur non vide, ligne non commentée). Commande : `grep -v '^#' .env | grep -q 'OPENROUTER_API_KEY=.\+'`. Si absent/vide → ARRÊTER et demander à Laurent de renseigner `OPENROUTER_API_KEY` dans `.env` (obtenue sur openrouter.ai) — sans elle, le pack témoin (Task 2) échoue avec une erreur API opaque. Consigner ce pré-requis dans 16-WITNESS.md.
    Créer `16-WITNESS.md` avec une section "Pré-checks" consignant ces résultats (suite, grep legacy, présence clé API).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web tsc --noEmit && grep -v '^#' .env | grep -q 'OPENROUTER_API_KEY=.\+'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "callOllama" apps/web/src/lib/veille/classify.ts apps/web/src/lib/preinscription-extractor.ts apps/web/src/lib/pdf-extract.ts` retourne vide
    - `grep -v '^#' .env | grep -q 'OPENROUTER_API_KEY=.\+'` exit 0 (clé présente, non vide, non commentée)
    - `16-WITNESS.md` existe avec section Pré-checks (dont statut clé API)
    - tsc clean
  </acceptance_criteria>
  <done>Suite verte, aucun call site vision/veille legacy, OPENROUTER_API_KEY présente et non vide (sinon arrêt avec message clair pour Laurent).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration complète Ollama→Claude (env, veille, vision, closure tiers, prompts re-tunés v10). Le worker closure route via callLlm (Haiku fast / Sonnet quality) quand AI_PROVIDER=openrouter.</what-built>
  <how-to-verify>
    1. Renseigner `OPENROUTER_API_KEY` dans `.env` et passer `AI_PROVIDER=openrouter` (⚠ uniquement pour la génération témoin ; pour la vision, voir checkpoint RGPD ci-dessous avant toute prod).
    2. Lancer le worker + l'app (`pnpm dev:full`, port 3010).
    3. Générer un pack fin de formation sur 1 session témoin (~3-5 stagiaires ; référence : SES type 5 personnes ~12 min).
    4. Vérifier :
       - 0 stub servi (les 10 docs sont générés par Claude, pas de contenu neutre de repli). Contrôler `AIGenerationJob` : status='done', provider='openrouter', promptVersion='claude-v10-2026-07'.
       - Contenu Qualiopi conforme et VARIÉ (écarts de positionnement réalistes, satisfactions non uniformes, déroulé cohérent horaires 9h-13h/14h-18h, rapport formateur ancré session).
       - Déroulé + rapport = model Sonnet ; QCM/analyse/grille/positionnement/satisfaction = model Haiku (vérifiable dans AIGenerationJob.model si tracé, ou logs `[ollama-<task>] ✓ (model=cloud:<tier>)`).
    5. Consigner le résultat (session, durée, stub count, jugement qualité) dans `16-WITNESS.md`.
  </how-to-verify>
  <resume-signal>Tape "approuvé" si 0 stub + qualité OK, sinon décris les docs à re-tuner (retour possible sur 16-05).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Feu vert RGPD/DPA pour la mise en PROD de l'OCR vision cloud (CNI/RIB → OpenRouter + Anthropic).</decision>
  <context>
    D-02b : l'OCR vision envoie des PII (CNI/RIB) au cloud, rupture de l'isolement MinIO actuel. La base légale / le DPA (sous-traitants OpenRouter + Anthropic) est une action de conformité HORS code, mais BLOQUANTE avant prod vision. Le code vision (16-03) est livré et testé, mais ne doit pas passer AI_PROVIDER=openrouter pour la vision en prod sans ce feu vert.
  </context>
  <options>
    <option id="go">
      <name>DPA tranché — vision cloud autorisée en prod</name>
      <pros>Pilier #4 complet en cloud (OCR Haiku).</pros>
      <cons>Nécessite le DPA documenté au niveau organisme (hors code).</cons>
    </option>
    <option id="gate">
      <name>Vision maintenue en local (Ollama) jusqu'au DPA</name>
      <pros>Aucune PII au cloud ; closure/veille cloud quand même.</pros>
      <cons>Vision reste sur Ollama local temporairement (dette documentée).</cons>
    </option>
  </options>
  <resume-signal>Choisis : go (DPA OK, vision cloud prod) ou gate (vision local jusqu'au DPA). Consigne le choix dans 16-WITNESS.md.</resume-signal>
</task>

</tasks>

<verification>
- 16-WITNESS.md consigne : pré-checks verts, pack témoin (0 stub, qualité), décision RGPD vision.
- Suite complète + tsc verts (phase gate 16-VALIDATION.md).
</verification>

<success_criteria>
- REQ-16-07 : suite verte + pack témoin réel 0-stub validé par Laurent.
- REQ-16-05 : qualité prompts Claude confirmée en réel.
- Gate RGPD vision tranché (go/gate) et tracé.
</success_criteria>

<output>
Créer `.planning/phases/16-migration-ia-ollama-vers-claude-api/16-06-SUMMARY.md`
</output>
</content>
