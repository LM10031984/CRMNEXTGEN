---
phase: 16-migration-ia-ollama-vers-claude-api
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/lib/pdf-extract.ts
  - apps/web/src/lib/preinscription-extractor.ts
  - apps/web/src/lib/__tests__/pdf-extract.test.ts
  - apps/web/src/lib/__tests__/preinscription-extractor.test.ts
autonomous: true
requirements: [REQ-16-02]
must_haves:
  truths:
    - "L'OCR vision PDF/image route vers callLlm (imageBuffer → tier vision), plus vers callOllamaVision"
    - "L'extraction texte OCR-isé (CNI/RIB/CFP) route vers callLlm tier fast, sans modèle Ollama codé en dur"
    - "Le champ persisté aiModel reflète le modèle réel utilisé, plus 'qwen3:30b-a3b' codé en dur"
    - "Échec vision → retour null/warning (saisie manuelle admin), PAS de stub généré (D-03c)"
  artifacts:
    - path: "apps/web/src/lib/pdf-extract.ts"
      provides: "extractTextFromImage via callLlm({imageBuffer})"
      contains: "callLlm"
    - path: "apps/web/src/lib/preinscription-extractor.ts"
      provides: "extractOne via callLlm tier fast, aiModel dynamique"
      contains: "callLlm"
    - path: "apps/web/src/lib/__tests__/pdf-extract.test.ts"
      provides: "Test vision → callLlm avec imageBuffer (mock callLlm), échec→null"
    - path: "apps/web/src/lib/__tests__/preinscription-extractor.test.ts"
      provides: "Test extractDocsFromBuffers → callLlm tier fast (mock callLlm), pas de callOllama"
  key_links:
    - from: "apps/web/src/lib/pdf-extract.ts"
      to: "@/lib/llm-client"
      via: "callLlm({ imageBuffer })"
      pattern: "callLlm\\(\\{[\\s\\S]*imageBuffer"
    - from: "apps/web/src/lib/preinscription-extractor.ts"
      to: "@/lib/llm-client"
      via: "callLlm({ tier: 'fast' })"
      pattern: "callLlm\\("
---

<objective>
Migrer les 2 call sites vision/OCR du pilier #4 (pré-inscriptions IA) de `callOllamaVision`/`callOllama` vers `callLlm` : `pdf-extract.ts` (OCR image → `callLlm({ imageBuffer })` force tier vision) et `preinscription-extractor.ts` (extraction sur texte déjà OCR-isé → `callLlm({ tier: 'fast' })`, retirer le modèle Ollama hardcodé et le `aiModel:'qwen3:30b-a3b'` figé). Comportement d'échec conservé : null → saisie manuelle admin, PAS de stub (D-03c).

Purpose: Pilier #4 (OCR CNI/RIB/CFP, PII) bascule sur Claude vision. ⚠ RGPD (D-02b) : la mise en PROD vision est gatée par le DPA hors code — le code est livré mais la validation prod reste gatée (voir checkpoint RGPD Plan 16-06, wave 4).
Output: 2 call sites vision migrés + test Wave 0 pdf-extract + test mock-migration preinscription-extractor (couvre le chemin PII, pilier #4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-CONTEXT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md

<interfaces>
llm-client.ts : `callLlm({ imageBuffer })` détecte imageBuffer → force `tier='vision'` → content multimodal image_url data-URL (détecte mime JPEG/PNG/GIF/WebP). Signature quasi-identique à callOllamaVision. `resolveModel('fast')` est exporté et retourne le modèle fast résolu selon AI_PROVIDER (Haiku cloud, mistral-small local).
pdf-extract.ts actuel (L88-96) : `callOllamaVision({ imageBuffer: buffer, prompt: VISION_OCR_PROMPT, temperature: 0, maxTokens: 1500 })` → `r.raw.trim()`.
preinscription-extractor.ts actuel (L147-154) : `callOllama({ model: process.env.OLLAMA_MODEL_FAST, systemPrompt: SYSTEM_PROMPT, prompt, jsonOutput: true, temperature: 0 })` → `r.parsedJson`. Persiste `aiModel: 'qwen3:30b-a3b'` (L221). `extractDocsFromBuffers(files)` (L263) est une fonction pure sans side-effect DB (input Buffer + contentType) → idéale à tester sans mocker prisma.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrer pdf-extract.ts (vision) vers callLlm({imageBuffer}) + test Wave 0</name>
  <read_first>
    - apps/web/src/lib/pdf-extract.ts (imports + extractTextFromImage L87-105, VISION_OCR_PROMPT)
    - apps/web/src/lib/llm-client.ts (callLlm imageBuffer branch L106-118, detectImageMime)
  </read_first>
  <behavior>
    - Test 1: extractTextFromImage(buffer) → callLlm appelé avec `expect.objectContaining({ imageBuffer: <buffer> })` ; retourne { text, pages:1 } quand r.raw non vide.
    - Test 2: callLlm renvoie r.raw '' → warning OCR vide, text ''.
    - Test 3: callLlm throw → catch → { text:'', pages:0, warnings:[...] } (échec → null-équivalent, PAS de stub).
  </behavior>
  <action>
    Dans `apps/web/src/lib/pdf-extract.ts` :
    1. Remplacer l'import de `callOllamaVision` (depuis `@/lib/ai-ollama`) par `import { callLlm } from '@/lib/llm-client';`.
    2. Dans `extractTextFromImage`, remplacer `callOllamaVision({ imageBuffer: buffer, prompt: VISION_OCR_PROMPT, temperature: 0, maxTokens: 1500 })` par `callLlm({ imageBuffer: buffer, prompt: VISION_OCR_PROMPT, temperature: 0, maxTokens: 1500 })`. Garder `const text = r.raw.trim();` et toute la logique de warnings.
    3. Dans le message d'erreur du catch (~L104-108), retirer toute référence à `ollama pull qwen2.5vl` / Ollama vision → message neutre cloud-compatible (ex. "Échec OCR vision — vérifiez la clé OPENROUTER_API_KEY ou l'accès réseau, ou saisissez les champs manuellement.").
    4. Créer `apps/web/src/lib/__tests__/pdf-extract.test.ts` couvrant les 3 comportements. Mock `vi.mock('@/lib/llm-client', () => ({ callLlm: vi.fn() }))`. NE PAS appeler pdftoppm/réseau réel — tester UNIQUEMENT `extractTextFromImage` (input Buffer direct, mock callLlm). Si `extractTextFromImage` importe des dépendances lourdes au boot, mocker aussi (`@qualiof/db` si importé). Assertion mutation-safe : callLlm reçoit `imageBuffer`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- pdf-extract</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "callLlm" apps/web/src/lib/pdf-extract.ts`
    - `grep -c "callOllamaVision" apps/web/src/lib/pdf-extract.ts` == 0
    - `grep -qi "ollama pull" apps/web/src/lib/pdf-extract.ts` retourne rien (message d'erreur nettoyé)
    - `apps/web/src/lib/__tests__/pdf-extract.test.ts` existe
    - `pnpm --filter @qualiof/web test -- pdf-extract` exit 0
  </acceptance_criteria>
  <done>OCR image via callLlm vision, message d'erreur cloud, échec→null, test vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrer preinscription-extractor.ts (texte OCR-isé) vers callLlm tier fast + test mock-migration (PII)</name>
  <read_first>
    - apps/web/src/lib/preinscription-extractor.ts (import L18, extractOne L141-155, extractDocsFromBuffers L263, aiModel L221)
    - apps/web/src/lib/llm-client.ts (callLlm tier fast, resolveModel exporté, LlmResult.model)
    - apps/web/src/lib/__tests__/pdf-extract.test.ts (pattern vi.mock('@/lib/llm-client') du Task 1, à réutiliser)
  </read_first>
  <behavior>
    - Test 1 (câblage tier fast) : `extractDocsFromBuffers([{ kind:'CFP', buffer, contentType:'text/plain' }])` avec `callLlm` mocké renvoyant `{ parsedJson: {...}, model:'cloud-fast' }` → `callLlm` appelé avec `expect.objectContaining({ tier: 'fast', jsonOutput: true })`. (Mocker aussi `@/lib/pdf-extract` `extractTextFromFile` pour renvoyer un texte >20 char sans OCR réel.)
    - Test 2 (pas de chemin Ollama) : le mock de `callLlm` est la SEULE source LLM appelée ; aucun import/appel `callOllama`/`OLLAMA_MODEL_FAST` n'est exercé (garanti par le mock unique + les grep d'acceptance).
    - Test 3 (échec→null) : `callLlm` renvoie `{ parsedJson: null }` → le champ correspondant du résultat vaut `null` (PAS de stub inventé).
    - Mutation-of-power (convention Laurent) : casser le câblage (retirer `tier:'fast'` ou remettre `callOllama`) DOIT faire virer le Test 1 rouge ; restaurer.
  </behavior>
  <action>
    Dans `apps/web/src/lib/preinscription-extractor.ts` :
    1. Remplacer `import { callOllama } from '@/lib/ai-ollama';` par `import { callLlm, resolveModel } from '@/lib/llm-client';`.
    2. Dans `extractOne`, remplacer l'appel L147-153 par :
       ```typescript
       const r = await callLlm({
         tier: 'fast',
         systemPrompt: SYSTEM_PROMPT,
         prompt,
         jsonOutput: true,
         temperature: 0,
       });
       ```
       Retirer `model: process.env.OLLAMA_MODEL_FAST` (anti-pattern RESEARCH : laisser resolveModel gouverner le modèle par tier/provider). Garder `return (r.parsedJson as T) ?? null;` (échec → null, saisie manuelle, PAS de stub).
    3. Remplacer L221 `aiModel: 'qwen3:30b-a3b'` par `aiModel: resolveModel('fast')` (reflète le modèle fast réellement résolu selon AI_PROVIDER — Haiku en cloud, mistral-small en local). Mettre à jour le commentaire L144-146 (retirer la justification mistral-small vs qwen3).
    4. Worker-safety : callLlm est fetch pur → OK. Ne pas ajouter d'import interdit.
    Puis créer `apps/web/src/lib/__tests__/preinscription-extractor.test.ts` couvrant les comportements ci-dessus. Cible `extractDocsFromBuffers` (fonction pure, pas de side-effect DB → pas besoin de mocker prisma). Mocks : `vi.mock('@/lib/llm-client', () => ({ callLlm: vi.fn(), resolveModel: vi.fn(() => 'cloud-fast') }))` et `vi.mock('@/lib/pdf-extract', () => ({ extractTextFromFile: vi.fn().mockResolvedValue({ text: 'MR NOEL STEVE ...(>20 char)', pages:1, warnings:[] }) }))`. Si un import de boot lourd subsiste (`@qualiof/db`, storage), le mocker aussi.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- preinscription-extractor && pnpm --filter @qualiof/web tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "callLlm" apps/web/src/lib/preinscription-extractor.ts`
    - `grep -c "callOllama" apps/web/src/lib/preinscription-extractor.ts` == 0
    - `grep -c "qwen3:30b-a3b" apps/web/src/lib/preinscription-extractor.ts` == 0
    - `grep -q "resolveModel" apps/web/src/lib/preinscription-extractor.ts`
    - `grep -c "OLLAMA_MODEL_FAST" apps/web/src/lib/preinscription-extractor.ts` == 0 (hardcode retiré)
    - `apps/web/src/lib/__tests__/preinscription-extractor.test.ts` existe
    - `pnpm --filter @qualiof/web test -- preinscription-extractor` exit 0
    - `pnpm --filter @qualiof/web tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Extraction texte via callLlm tier fast, modèle dynamique persisté, échec→null, test mock-migration vert (pilier #4 PII couvert).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web test -- pdf-extract` vert et `pnpm --filter @qualiof/web test -- preinscription-extractor` vert.
- `grep -r callOllamaVision apps/web/src/lib/pdf-extract.ts` = 0 ; `grep callOllama apps/web/src/lib/preinscription-extractor.ts` = 0.
- `pnpm --filter @qualiof/web tsc --noEmit` clean.
- ⚠ RGPD D-02b : PROD vision reste gatée (checkpoint:decision RGPD dans Plan 16-06, wave 4) — le code est livré mais ne doit pas passer AI_PROVIDER=openrouter pour la vision en prod sans DPA.
</verification>

<success_criteria>
- REQ-16-02 : les 2 call sites vision migrés vers callLlm, modèle dynamique, échec→null (pas de stub), tests pdf-extract + preinscription-extractor verts.
</success_criteria>

<output>
Créer `.planning/phases/16-migration-ia-ollama-vers-claude-api/16-03-SUMMARY.md`
</output>
</content>
