---
phase: 16-migration-ia-ollama-vers-claude-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/env.ts
  - packages/shared/src/__tests__/env.test.ts
  - turbo.json
  - .env.example
autonomous: true
requirements: [REQ-16-01]
must_haves:
  truths:
    - "Boot ne crashe plus quand AI_PROVIDER=openrouter (enum accepte la valeur)"
    - "Les 7 clés OPENROUTER_* sont validées par t3-env (single source of truth)"
    - "OLLAMA_MODEL_VISION est déclarée dans env.ts (llm-client L23/L71 la lit — plus de clé env non déclarée)"
    - "Un AI_PROVIDER inconnu fait échouer le boot bruyamment (fail loud)"
  artifacts:
    - path: "packages/shared/src/env.ts"
      provides: "AI_PROVIDER enum + openrouter, 7 clés OPENROUTER_* + OLLAMA_MODEL_VISION déclarées server+runtimeEnv"
      contains: "openrouter"
    - path: "packages/shared/src/__tests__/env.test.ts"
      provides: "Test enum openrouter accepté + provider invalide rejeté"
    - path: "turbo.json"
      provides: "OPENROUTER_* dans globalEnv (cache invalidation)"
      contains: "OPENROUTER_API_KEY"
    - path: ".env.example"
      provides: "Documentation des clés OpenRouter + AI_PROVIDER=openrouter"
  key_links:
    - from: "packages/shared/src/env.ts"
      to: "AI_PROVIDER"
      via: "z.enum incluant 'openrouter'"
      pattern: "'ollama',\\s*'openrouter'"
---

<objective>
Rendre le provider `openrouter` boot-safe : ajouter `'openrouter'` à l'enum `AI_PROVIDER` de `packages/shared/src/env.ts` et déclarer/valider les 7 clés `OPENROUTER_*` que `llm-client.ts` lit aujourd'hui via `process.env` brut (hors validation t3-env). Déclarer aussi `OLLAMA_MODEL_VISION` que `llm-client.ts` lit (L23/L71) mais qui n'est PAS déclarée dans env.ts — clé env fantôme à régulariser (goal single source of truth de ce plan). Sans le fix enum, `AI_PROVIDER=openrouter` fait échouer le boot (`Invalid enum value`) — c'est la fondation qui débloque toute la phase.

Purpose: Env single source of truth (CLAUDE.md) + fail-loud au boot ; débloque les Waves 2/3 (vision, veille, closure, pack témoin).
Output: env.ts étendu (7 OPENROUTER_* + OLLAMA_MODEL_VISION) + test env.test.ts (Wave 0) + turbo.json globalEnv + .env.example documenté.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-CONTEXT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md

<interfaces>
Clés OPENROUTER_* lues par llm-client.ts (défauts codés) — à déclarer dans env.ts :

| Clé | Défaut codé (llm-client.ts) | Schéma |
|-----|-----------------------------|--------|
| OPENROUTER_BASE_URL | https://openrouter.ai/api/v1 | z.string().url().default(...) |
| OPENROUTER_API_KEY | '' | z.string().optional() |
| OPENROUTER_MODEL_FAST | anthropic/claude-haiku-4.5 | z.string().default(...) |
| OPENROUTER_MODEL_QUALITY | anthropic/claude-sonnet-4.6 | z.string().default(...) |
| OPENROUTER_MODEL_VISION | anthropic/claude-haiku-4.5 | z.string().default(...) |
| OPENROUTER_APP_NAME | QualiOF | z.string().default('QualiOF') |
| OPENROUTER_SITE_URL | http://localhost:3010 | z.string().url().default(...) |

Clé Ollama vision manquante dans env.ts (lue L23/L71 de llm-client.ts) — à déclarer aussi :

| Clé | Défaut codé (llm-client.ts) | Schéma |
|-----|-----------------------------|--------|
| OLLAMA_MODEL_VISION | llama3.2-vision:11b | z.string().default('llama3.2-vision:11b') |

env.ts actuel (§L32) : `AI_PROVIDER: z.enum(['ollama', 'anthropic', 'qualiopi-gen']).default('ollama')`
env.ts actuel (§L34-36) : OLLAMA_MODEL_FAST / _REASONING / _EMBED déclarées, mais PAS _VISION.
.env.example (L42) documente déjà `OLLAMA_MODEL_VISION="llama3.2-vision:11b"` — pas de modif .env.example requise pour cette clé (déjà présente).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre env.ts (enum openrouter + 7 clés OPENROUTER_* + OLLAMA_MODEL_VISION) avec test Wave 0</name>
  <read_first>
    - packages/shared/src/env.ts (le fichier à modifier — enum L32, OLLAMA_MODEL_* L34-36, runtimeEnv L67+)
    - apps/web/src/lib/llm-client.ts (L23/L71 : défaut OLLAMA_MODEL_VISION ; L26-32 : les défauts exacts des 7 clés OpenRouter, source de vérité)
    - packages/shared/src/schemas/tenant.ts (pattern de test Zod existant dans le package, si présent — sinon vitest simple)
  </read_first>
  <behavior>
    - Test 1: `AI_PROVIDER='openrouter'` → parse OK (pas de throw).
    - Test 2: `AI_PROVIDER='n-importe-quoi'` → le schéma rejette (provider invalide fail loud).
    - Test 3: sans OPENROUTER_MODEL_FAST défini → la valeur par défaut `anthropic/claude-haiku-4.5` est appliquée.
  </behavior>
  <action>
    1. Dans `packages/shared/src/env.ts`, modifier l'enum L32 :
       `AI_PROVIDER: z.enum(['ollama', 'openrouter', 'anthropic', 'qualiopi-gen']).default('ollama'),`
    2. Ajouter `OLLAMA_MODEL_VISION` dans le bloc `server`, à côté des autres OLLAMA_MODEL_* (après OLLAMA_MODEL_EMBED L36), avec le défaut EXACT de llm-client.ts L23 :
       - `OLLAMA_MODEL_VISION: z.string().default('llama3.2-vision:11b'),`
       (Cette clé est lue par llm-client.ts L23/L71 mais n'était PAS déclarée — régularisation single source of truth.)
    3. Ajouter dans le bloc `server` (après ANTHROPIC_MODEL L38, section IA) les 7 clés OPENROUTER_* avec les défauts EXACTS de llm-client.ts (voir <interfaces>) :
       - `OPENROUTER_BASE_URL: z.string().url().default('https://openrouter.ai/api/v1'),`
       - `OPENROUTER_API_KEY: z.string().optional(),` (llm-client throw déjà si vide en runtime — ne PAS rendre requis, sinon dev local Ollama casse)
       - `OPENROUTER_MODEL_FAST: z.string().default('anthropic/claude-haiku-4.5'),`
       - `OPENROUTER_MODEL_QUALITY: z.string().default('anthropic/claude-sonnet-4.6'),`
       - `OPENROUTER_MODEL_VISION: z.string().default('anthropic/claude-haiku-4.5'),`
       - `OPENROUTER_APP_NAME: z.string().default('QualiOF'),`
       - `OPENROUTER_SITE_URL: z.string().url().default('http://localhost:3010'),`
    4. Ajouter dans `runtimeEnv` (mapping `KEY: process.env.KEY`) les 8 clés ajoutées : `OLLAMA_MODEL_VISION` (à côté des autres OLLAMA_MODEL_* L83-85) + les 7 OPENROUTER_* (après AI_PROVIDER/OLLAMA_*).
    5. Créer `packages/shared/src/__tests__/env.test.ts` couvrant les 3 comportements ci-dessus. Utiliser `createEnv` importé ou re-parser via un `z.enum` local isolé si `sharedEnv` a des dépendances runtime bloquantes en test (SKIP_ENV_VALIDATION=true est déjà supporté L106). Préférer tester le schéma enum directement pour éviter d'avoir à fournir DATABASE_URL/AUTH_SECRET.
    Note : NE PAS toucher `ANTHROPIC_MODEL` (provider distinct, hors scope — RESEARCH State of the Art). `.env.example` documente déjà OLLAMA_MODEL_VISION (L42) — ne pas la dupliquer.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/shared test -- env</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'ollama', 'openrouter'" packages/shared/src/env.ts` (enum étendu)
    - `grep -q "OLLAMA_MODEL_VISION" packages/shared/src/env.ts` (clé vision Ollama déclarée)
    - `grep -c "OLLAMA_MODEL_VISION" packages/shared/src/env.ts` == 2 (server + runtimeEnv)
    - `grep -c "OPENROUTER_" packages/shared/src/env.ts` >= 14 (7 dans server + 7 dans runtimeEnv)
    - `packages/shared/src/__tests__/env.test.ts` existe
    - `pnpm --filter @qualiof/shared test -- env` exit 0
  </acceptance_criteria>
  <done>Boot accepte AI_PROVIDER=openrouter, 7 clés OPENROUTER_* + OLLAMA_MODEL_VISION validées, provider invalide rejeté ; test vert.</done>
</task>

<task type="auto">
  <name>Task 2: turbo.json globalEnv + .env.example (documentation + cache)</name>
  <read_first>
    - turbo.json (L14-21 : bloc globalEnv, OLLAMA_*/ANTHROPIC_* déjà listés)
    - .env.example (§L35-46 : section IA, à côté des clés Ollama/Anthropic)
  </read_first>
  <action>
    1. Dans `turbo.json` `globalEnv`, ajouter après ANTHROPIC_MODEL (L21) les 7 clés :
       `OPENROUTER_BASE_URL`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL_FAST`, `OPENROUTER_MODEL_QUALITY`, `OPENROUTER_MODEL_VISION`, `OPENROUTER_APP_NAME`, `OPENROUTER_SITE_URL`.
       (Sinon changer une clé OPENROUTER_* n'invalide pas le cache Turbo — STACK.md.) Vérifier au passage que `OLLAMA_MODEL_VISION` est présente dans globalEnv ; si absente, l'ajouter à côté des OLLAMA_MODEL_*.
    2. Dans `.env.example`, section IA (~L35-46), documenter le bloc OpenRouter avec commentaires FR :
       - Un commentaire "# --- OpenRouter (cloud Claude via gateway, milestone v6) ---"
       - `AI_PROVIDER=ollama` (défaut ; documenter la valeur `openrouter` en commentaire "# openrouter pour basculer sur Claude cloud")
       - Les 7 clés avec leurs défauts + un commentaire indiquant que `OPENROUTER_API_KEY` est requise si AI_PROVIDER=openrouter (obtenue sur openrouter.ai).
       (OLLAMA_MODEL_VISION est déjà documentée L42 — ne pas la dupliquer.)
  </action>
  <verify>
    <automated>grep -q "OPENROUTER_API_KEY" turbo.json && grep -q "OPENROUTER_API_KEY" .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OPENROUTER_" turbo.json` >= 7
    - `grep -q "OPENROUTER_API_KEY" .env.example`
    - `grep -q "openrouter" .env.example` (commentaire bascule documenté)
  </acceptance_criteria>
  <done>turbo.json invalide le cache sur changement OPENROUTER_* ; .env.example documente la bascule cloud.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/shared test -- env` vert.
- `pnpm --filter @qualiof/shared tsc --noEmit` (ou build shared) clean.
- Boot manuel : `AI_PROVIDER=openrouter pnpm --filter @qualiof/web build` ne throw pas `Invalid enum value` (peut échouer plus loin sur d'autres raisons hors scope).
</verification>

<success_criteria>
- env.ts accepte `openrouter` + valide les 7 clés OPENROUTER_* + OLLAMA_MODEL_VISION.
- turbo.json + .env.example à jour.
- Test env.test.ts vert (Wave 0 satisfait pour IA-01).
</success_criteria>

<output>
Créer `.planning/phases/16-migration-ia-ollama-vers-claude-api/16-01-SUMMARY.md`
</output>
</content>
</invoke>
