---
phase: 16-migration-ia-ollama-vers-claude-api
plan: 05
type: execute
wave: 3
depends_on: ["16-04"]
files_modified:
  - apps/web/src/lib/closure/qualiopi-prompts.ts
autonomous: true
requirements: [REQ-16-05]
must_haves:
  truths:
    - "PROMPT_VERSION est bumpé vers une version Claude tracée (mistral→claude distingué dans AIGenerationJob)"
    - "Les garde-fous MÉTIER Qualiopi (voix 1re/3e personne, ancrage individuel, Bloom, distribution A/B, cohérence horaire) sont CONSERVÉS intégralement"
    - "Les répétitions défensives ultra-redondantes spécifiques mistral (rappels JSON/markdown en excès, MAJUSCULES d'insistance) sont allégées sans perte de sémantique métier"
  artifacts:
    - path: "apps/web/src/lib/closure/qualiopi-prompts.ts"
      provides: "5 system prompts re-tunés pour Claude + PROMPT_VERSION bumpé"
      contains: "claude"
  key_links:
    - from: "qualiopi-prompts.ts PROMPT_VERSION"
      to: "AIGenerationJob.promptVersion"
      via: "runOllamaJson persiste PROMPT_VERSION"
      pattern: "PROMPT_VERSION\\s*="
---

<objective>
Re-tuner les 5 system prompts de `qualiopi-prompts.ts` (écrits pour `mistral-small:24b`) pour Claude, et bumper `PROMPT_VERSION`. Claude respecte mieux le format JSON → alléger les rappels défensifs redondants. MAIS conserver INTÉGRALEMENT les garde-fous MÉTIER Qualiopi (ce ne sont PAS des anti-dérape mistral — c'est la valeur d'audit). Traiter comme un changement observable (version tracée + validé sur pack témoin en 16-06).

Purpose: Qualité des docs sous Claude ; traçabilité mistral vs Claude via PROMPT_VERSION (leçon audit : re-gen a posteriori possible seulement si version tracée).
Output: 5 prompts re-tunés + PROMPT_VERSION bumpé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-CONTEXT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md

<interfaces>
qualiopi-prompts.ts : `export const PROMPT_VERSION = 'qualiopi-gen-v9-2026-06-18';` (L10) + 8 exports SYSTEM_PROMPT_* (QCM, ANALYSE_BESOIN, GRILLE_OBSERVATION, GRILLE_OBSERVATION_SESSION, COMPETENCIES, POSITIONNEMENT, SATISFACTION_CHAUD, SATISFACTION_FROID, NORMALIZE_PROGRAMME, DEROULE, RAPPORT_FORMATEUR).
CONTEXT parle des "5 system prompts" (QCM, AnalyseBesoin, Grille, Compétences/Positionnement, Déroulé) — mais re-tuner de façon COHÉRENTE l'ensemble du fichier (tous les SYSTEM_PROMPT_*) tant qu'on y est, avec la même politique.

RESEARCH — politique de re-tune :
- CONSERVER (garde-fous MÉTIER, non-négociables Qualiopi) : voix 1re/3e personne, ancrage individuel anti-jumelage, ancrage strict au thème, distribution A/B des niveaux, verbes de Bloom, cohérence horaire du déroulé.
- ALLÉGER (spécifiques mistral) : rappels ultra-redondants "ne pas wrapper en markdown / réponds UNIQUEMENT en JSON" répétés plusieurs fois, MAJUSCULES d'insistance en excès. Garder UNE instruction JSON claire (elle ne nuit pas).
- Le risque de régression est réel → validation sur pack témoin (16-06) AVANT verify-work.
- ⚠ Prompt figé au produit : re-tuner ne re-génère PAS les produits déjà figés (TrainingProduct.derouleJson) — coexistence mistral/Claude tracée par PROMPT_VERSION. Re-run des produits = HORS scope (dette documentée).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-tuner les system prompts pour Claude + bump PROMPT_VERSION</name>
  <read_first>
    - apps/web/src/lib/closure/qualiopi-prompts.ts (le fichier entier — PROMPT_VERSION L10 + tous les SYSTEM_PROMPT_*)
    - .planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md (§Prompt re-tuning : quoi conserver vs alléger)
  </read_first>
  <action>
    Dans `apps/web/src/lib/closure/qualiopi-prompts.ts` :
    1. Bumper `PROMPT_VERSION` : `'qualiopi-gen-v9-2026-06-18'` → `'claude-v10-2026-07'` (distingue mistral vs Claude dans AIGenerationJob.promptVersion).
    2. Pour CHAQUE SYSTEM_PROMPT_* :
       - ALLÉGER les rappels JSON/markdown redondants : si l'instruction "Réponds UNIQUEMENT en JSON valide, sans markdown, sans ``` , sans explication" apparaît 2+ fois, ne la garder qu'UNE fois (formulation claire, en fin de prompt). Retirer les répétitions.
       - RÉDUIRE les MAJUSCULES d'insistance excessives (ex. plusieurs mots en CAPS consécutifs pour marteler une consigne) en formulation normale — SANS supprimer la consigne elle-même.
       - CONSERVER MOT POUR MOT toute règle MÉTIER Qualiopi : voix 1re/3e personne, interdiction de citer le prénom, ancrage individuel anti-jumelage, ancrage strict au thème (n'introduire aucun sujet hors programme), distribution A/B des niveaux (positionnement), seuils de satisfaction (≥90% Très bien/Bien), verbes de Bloom, cohérence horaire 9h-13h/14h-18h du déroulé, seuils QCM. NE RIEN retirer de la sémantique métier.
    3. Ne PAS changer la structure de sortie attendue (les schémas Zod restent identiques — la chaîne prompt→LLM→Zod→null→stub est inchangée). Ne toucher qu'au texte des consignes.
    4. Ajouter en tête de fichier un commentaire daté expliquant le re-tune Claude (v10) : allègement des rappels format, conservation des garde-fous métier, coexistence mistral/Claude tracée par PROMPT_VERSION, re-run produits figés = hors scope.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PROMPT_VERSION = 'claude-v10-2026-07'" apps/web/src/lib/closure/qualiopi-prompts.ts`
    - Garde-fous métier préservés — présence d'au moins ces ancres : `grep -qi "Bloom" qualiopi-prompts.ts` ; `grep -qi "1re personne\|première personne" qualiopi-prompts.ts` ; `grep -qi "9h\|09:00\|horaire" qualiopi-prompts.ts` (cohérence horaire déroulé)
    - `pnpm --filter @qualiof/web tsc --noEmit` clean
    - `pnpm --filter @qualiof/web test` (suite complète) vert (aucun test ne doit régresser — les prompts ne sont pas assertés littéralement dans les tests mockés)
  </acceptance_criteria>
  <done>PROMPT_VERSION=claude-v10 ; prompts allégés côté format, garde-fous métier intacts ; tsc + suite verts.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web tsc --noEmit` clean.
- `pnpm --filter @qualiof/web test` suite complète verte.
- Revue manuelle qualité = validée sur pack témoin en 16-06 (D-04c : changement observable).
</verification>

<success_criteria>
- REQ-16-05 : 5+ prompts re-tunés pour Claude, PROMPT_VERSION bumpé et tracé, garde-fous métier conservés.
</success_criteria>

<output>
Créer `.planning/phases/16-migration-ia-ollama-vers-claude-api/16-05-SUMMARY.md`
</output>
