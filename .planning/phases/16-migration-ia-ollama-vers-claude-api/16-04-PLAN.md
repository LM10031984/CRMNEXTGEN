---
phase: 16-migration-ia-ollama-vers-claude-api
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/lib/closure/ollama-generators.ts
  - apps/web/src/lib/closure/__tests__/generators-routing.test.ts
autonomous: true
requirements: [REQ-16-04, REQ-16-06]
must_haves:
  truths:
    - "Chaque générateur closure passe le tier CONFORME à D-01a : déroulé + rapport formateur = quality (Sonnet), tout le reste = fast (Haiku)"
    - "Quand AI_PROVIDER=openrouter, les générateurs routent via callLlm avec le bon tier (plus callOllama)"
    - "Sur échec API répété, la chaîne retry (MAX_ATTEMPTS) puis null→stub reste intacte — PAS de fallback Ollama"
  artifacts:
    - path: "apps/web/src/lib/closure/ollama-generators.ts"
      provides: "Tiers corrigés par D-01a sur les ~9 générateurs"
      contains: "callLlm"
    - path: "apps/web/src/lib/closure/__tests__/generators-routing.test.ts"
      provides: "Test routage tier (mock callLlm) + retry→null(stub), mutation-safe"
      contains: "@/lib/llm-client"
  key_links:
    - from: "apps/web/src/lib/closure/ollama-generators.ts"
      to: "@/lib/llm-client"
      via: "tryOnce switch cloud ? callLlm({ tier }) : callOllama"
      pattern: "callLlm\\(\\{[\\s\\S]*tier"
    - from: "runOllamaJson"
      to: "failJob → null → stub"
      via: "MAX_ATTEMPTS boucle retry puis failJob"
      pattern: "MAX_ATTEMPTS"
---

<objective>
Aligner les tiers des générateurs closure sur la décision D-01a et prouver le routage + le fallback. Le routage `cloud ? callLlm : callOllama` existe DÉJÀ dans `tryOnce`/`runOllamaJson` (tous les générateurs y passent). Le travail réel : (a) corriger les `tier` passés à `runOllamaJson` pour matcher D-01a (aujourd'hui INVERSÉS pour plusieurs docs — audit "routage 17/06" avait tout mis en quality), (b) créer un test de routage mockant `callLlm` (Wave 0 : `generators-routing.test.ts`) couvrant le bon tier par générateur + le retry→null (→ stub), mutation-safe.

Purpose: Pilier #1 (pack closure) : docs volume/structurés en Haiku (coût), déroulé/rapport en Sonnet (qualité audit), fallback stub garanti.
Output: tiers corrigés + test routage/retry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-CONTEXT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md

<interfaces>
D-01a mapping tier CIBLE (source de vérité — CONTEXT) :
- **quality (Sonnet)** : déroulé pédagogique (`generate-deroule`), rapport formateur (`generate-rapport-formateur`).
- **fast (Haiku)** : QCM (`generate-qcm`), analyse besoin (`generate-analyse-besoin`), grille observation (`generate-grille`), grille session (`generate-grille-obs-session`), positionnement (`generate-positionnement`), satisfaction chaud (`generate-satisfaction-chaud`), satisfaction froid (`generate-satisfaction-froid`).
- Programme normalisé (`generate-programme` / generateNormalizedProgramme) : quality (rédactionnel critique figé au produit — garder quality).

ÉTAT ACTUEL des tiers (dernier arg positionnel de runOllamaJson) — à corriger :
| Générateur | Tier actuel | Tier CIBLE D-01a | Action |
|-----------|-------------|------------------|--------|
| generate-rapport-formateur (L441) | 'fast' | **quality** | changer |
| generate-qcm (L468) | 'quality' | **fast** | changer |
| generate-analyse-besoin (L561) | 'quality' | **fast** | changer |
| generate-grille (L613) | 'quality' | **fast** | changer |
| generate-positionnement (L725) | 'quality' | **fast** | changer |
| generate-satisfaction-chaud (L745-753) | (défaut fast) | fast | déjà OK |
| generate-satisfaction-froid (L774) | (défaut fast) | fast | déjà OK |
| generate-deroule (L873, L916) | 'quality' | quality | déjà OK |
| generate-programme (L1009) | 'quality' | quality | déjà OK |
| generate-grille-obs-session (L1146) | 'quality' | **fast** | changer |

runOllamaJson signature : `(taskName, systemPrompt, userPrompt, schema, refTable, refId, tenantId, modelOverride?, tier='fast')`.
tryOnce (L626) : `cloud = AI_PROVIDER==='openrouter'` → `callLlm({ tier, ... })` sinon `callOllama`.
MAX_ATTEMPTS = `Number(process.env.CLOSURE_OLLAMA_RETRIES ?? 2)` (1 initial + 1 retry) → sur échec final `failJob` + return null → le worker sert le stub (stub-content.ts). PAS de fallback Ollama à ajouter (D-03b : le switch env-piloté suffit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger les tiers des générateurs closure selon D-01a</name>
  <read_first>
    - apps/web/src/lib/closure/ollama-generators.ts (les 6 call sites runOllamaJson à corriger + tryOnce L626-682 + runOllamaJson L1027-1080)
    - apps/web/src/lib/llm-client.ts (resolveModel : tier fast→Haiku, quality→Sonnet en openrouter)
  </read_first>
  <action>
    Dans `apps/web/src/lib/closure/ollama-generators.ts`, changer le dernier argument positionnel `tier` des appels `runOllamaJson` pour matcher D-01a (voir table <interfaces>) :
    1. `generate-rapport-formateur` (~L441) : `'fast'` → `'quality'`. Mettre à jour le commentaire L399 (rapport = quality/Sonnet, doc rédactionnel critique audit D-01a).
    2. `generate-qcm` (~L468) : `'quality'` → `'fast'`. Commentaire : "docs volume/structurés → Haiku (D-01a)".
    3. `generate-analyse-besoin` (~L561) : `'quality'` → `'fast'`.
    4. `generate-grille` (~L613) : `'quality'` → `'fast'`.
    5. `generate-positionnement` (~L725) : `'quality'` → `'fast'`.
    6. `generate-grille-obs-session` (~L1146) : `'quality'` → `'fast'`.
    NE PAS toucher : déroulé (L873/L916 quality), programme (L1009 quality), satisfaction chaud/froid (défaut fast). Remplacer les commentaires "audit routage 17/06" par une référence "D-01a Phase 16 (fast=Haiku volume / quality=Sonnet rédactionnel critique)".
    NE PAS modifier tryOnce ni runOllamaJson (routage + retry déjà corrects). NE PAS ajouter de fallback Ollama (D-03b — le switch cloud env-piloté est la seule voie).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Bloc `generateRapportFormateur` : dernier arg runOllamaJson == `'quality'` (`grep -A12 "generate-rapport-formateur" ollama-generators.ts | grep -q "'quality'"`)
    - Bloc `generate-qcm` : `'fast'` ; `generate-analyse-besoin` : `'fast'` ; `generate-grille'` : `'fast'` ; `generate-positionnement` : `'fast'` ; `generate-grille-obs-session` : `'fast'`
    - `grep -c "audit routage 17/06" ollama-generators.ts` == 0 (commentaires mis à jour)
    - `pnpm --filter @qualiof/web tsc --noEmit` clean
  </acceptance_criteria>
  <done>Les tiers reflètent D-01a : déroulé+rapport=quality, tout le reste closure=fast (+ programme quality figé).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test routage tier + retry→null(stub), mutation-safe (Wave 0)</name>
  <read_first>
    - apps/web/src/lib/closure/ollama-generators.ts (les fns generate* exportées + runOllamaJson/tryOnce)
    - apps/web/src/lib/closure/__tests__/deroule-jour-partiel.test.ts (pattern de mock @qualiof/db + @/lib/ai-ollama + @/lib/llm-client au boot du module)
  </read_first>
  <behavior>
    - Test 1 (routage quality) : AI_PROVIDER=openrouter, callLlm mocké renvoyant un JSON valide → generateRapportFormateur appelle callLlm avec `expect.objectContaining({ tier: 'quality' })`.
    - Test 2 (routage fast) : generateQcmContent appelle callLlm avec `expect.objectContaining({ tier: 'fast' })`.
    - Test 3 (retry→null) : callLlm mocké rejette systématiquement (ou renvoie parsedJson null) N fois → le générateur retourne `null` après MAX_ATTEMPTS (→ le worker servira le stub). Asserter `callLlm` appelé exactement MAX_ATTEMPTS fois et résultat null.
  </behavior>
  <action>
    Créer `apps/web/src/lib/closure/__tests__/generators-routing.test.ts` :
    1. `process.env.AI_PROVIDER = 'openrouter'` et `process.env.CLOSURE_OLLAMA_RETRIES = '2'` en tête (avant import du module — via vi.hoisted ou beforeAll).
    2. Mock `vi.mock('@qualiof/db', () => ({ prisma: { aIGenerationJob: { create: vi.fn().mockResolvedValue({ id: 'j1' }), update: vi.fn() } } }))` (runOllamaJson écrit AIGenerationJob si tenantId non-null → passer tenantId=null aux générateurs pour éviter le besoin de mock complet, OU fournir le mock create/update).
    3. Mock `vi.mock('@/lib/llm-client', () => ({ callLlm: callLlmMock, resolveModel: vi.fn(() => 'anthropic/claude-x') }))` et `vi.mock('@/lib/ai-ollama', () => ({ callOllama: vi.fn() }))`.
    4. Test 1/2 : `callLlmMock.mockResolvedValue({ raw: '{...}', parsedJson: <objet Zod-valide minimal>, model:'m', provider:'openrouter', durationMs:1 })` — fournir un parsedJson qui passe le schéma du générateur (Rapport : `{ adaptations, groupe, bilan }` ; QCM : `{ questions: [...] }` avec ≥ le minimum). Appeler la fn, asserter `callLlmMock` reçu avec le bon tier via `expect.objectContaining({ tier })`.
    5. Test 3 : `callLlmMock.mockResolvedValue({ raw:'nope', parsedJson: null, ... })` (ou mockRejectedValue) → appeler generateQcmContent(tenantId=null) → attendre `null` + `expect(callLlmMock).toHaveBeenCalledTimes(2)` (MAX_ATTEMPTS).
    6. PROTOCOLE DE MUTATION (commentaire, jamais commité) : dans ollama-generators.ts, inverser le tier de generateRapportFormateur ('quality'→'fast') → Test 1 vire ROUGE → restaurer. Et retirer le backoff/boucle (attempt unique) → Test 3 `toHaveBeenCalledTimes(2)` vire ROUGE → restaurer. Documenter dans le fichier de test (feedback_test_de_puissance_mutation).
    Note : si construire un parsedJson Zod-valide est trop lourd pour QCM (post-process attachQcmScoring), cibler Test 2 sur un générateur fast plus simple à satisfaire (ex. generateAnalyseBesoinContent ou generatePositionnementContent) — l'important est de prouver `tier:'fast'`.
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- generators-routing</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/closure/__tests__/generators-routing.test.ts` existe
    - `grep -q "tier: 'quality'" apps/web/src/lib/closure/__tests__/generators-routing.test.ts`
    - `grep -q "tier: 'fast'" apps/web/src/lib/closure/__tests__/generators-routing.test.ts`
    - `grep -q "toHaveBeenCalledTimes" apps/web/src/lib/closure/__tests__/generators-routing.test.ts` (preuve retry)
    - `pnpm --filter @qualiof/web test -- generators-routing` exit 0
  </acceptance_criteria>
  <done>Routage tier prouvé par générateur (quality vs fast) + retry→null prouvé (MAX_ATTEMPTS), mutation-safe documenté.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web test -- generators-routing` vert.
- `pnpm --filter @qualiof/web test -- deroule` toujours vert (non régressé — mocks llm-client déjà en place).
- `pnpm --filter @qualiof/web tsc --noEmit` clean.
- `pnpm --filter @qualiof/web test` (suite complète) vert au merge de la vague.
</verification>

<success_criteria>
- REQ-16-04 : retry→stub prouvé (pas de fallback Ollama). REQ-16-06 : tiers conformes D-01a par générateur.
</success_criteria>

<output>
Créer `.planning/phases/16-migration-ia-ollama-vers-claude-api/16-04-SUMMARY.md`
</output>
