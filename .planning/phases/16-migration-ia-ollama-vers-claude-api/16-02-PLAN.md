---
phase: 16-migration-ia-ollama-vers-claude-api
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/lib/veille/classify.ts
  - apps/web/src/lib/veille/__tests__/classify.test.ts
autonomous: true
requirements: [REQ-16-03]
must_haves:
  truths:
    - "La classification veille route vers callLlm (tier fast) quand AI_PROVIDER=openrouter, plus vers callOllama en direct"
    - "Le provider/model tracés dans AIGenerationJob sont DYNAMIQUES (r.provider/r.model), plus 'ollama'/'mistral-small:24b' codés en dur"
    - "Les 4 scénarios existants (ok/malformé/OTHER/throw) restent couverts"
  artifacts:
    - path: "apps/web/src/lib/veille/classify.ts"
      provides: "classifyItem via callLlm tier fast, tracing dynamique"
      contains: "callLlm"
    - path: "apps/web/src/lib/veille/__tests__/classify.test.ts"
      provides: "Mock migré ai-ollama→llm-client, mutation-safe (tier fast)"
      contains: "@/lib/llm-client"
  key_links:
    - from: "apps/web/src/lib/veille/classify.ts"
      to: "@/lib/llm-client"
      via: "import callLlm + appel tier fast"
      pattern: "callLlm\\("
---

<objective>
Migrer le call site veille (`veille/classify.ts`) de `callOllama` direct vers `callLlm({ tier: 'fast' })` — patron le plus simple (call site isolé, worker-safe, déjà testé). Rendre le tracing `AIGenerationJob` dynamique (`provider`/`model` lus du résultat, plus codés en dur). Migrer le test qui mocke `@/lib/ai-ollama` vers `@/lib/llm-client`, avec preuve de mutation.

Purpose: Pilier veille Qualiopi bascule sur Claude cloud (Haiku en fast) sans casser la chaîne worker-safe.
Output: classify.ts routé via callLlm + test migré mutation-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-CONTEXT.md
@.planning/phases/16-migration-ia-ollama-vers-claude-api/16-RESEARCH.md

<interfaces>
llm-client.ts (patron cible) :
```typescript
export async function callLlm(opts: LlmCallOptions): Promise<LlmResult>;
// LlmResult : { raw, parsedJson, model, provider: 'ollama'|'openrouter', durationMs, usageTokensIn?, usageTokensOut? }
// LlmCallOptions : { tier?, model?, systemPrompt?, prompt, jsonOutput?, temperature?, maxTokens?, timeoutMs?, imageBuffer? }
```
classify.ts actuel : `callOllama({ model: OLLAMA_MODEL_VEILLE, systemPrompt, prompt, jsonOutput: true, temperature: 0.1, timeoutMs: 60_000 })` puis persiste `provider:'ollama', model: OLLAMA_MODEL_VEILLE` dans 3 branches AIGenerationJob (L74-75, L93-94, L107-109).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router classify.ts via callLlm + tracing dynamique + migration test</name>
  <read_first>
    - apps/web/src/lib/veille/classify.ts (le fichier — call site L61-68 + 3 branches AIGenerationJob)
    - apps/web/src/lib/llm-client.ts (callLlm + LlmResult : r.provider/r.model)
    - apps/web/src/lib/veille/__tests__/classify.test.ts (le test à migrer)
  </read_first>
  <behavior>
    - Test 1: callLlm renvoie JSON valide INDIC_23 conf=85 → ClassifyOutput correct + AIGenerationJob status='ok'.
    - Test 2: callLlm renvoie parsedJson null / Zod fail → null + status='error'.
    - Test 3: theme='OTHER' → ClassifyOutput + status='skipped_other'.
    - Test 4: callLlm throw (timeout) → null + status='error'.
    - Assertion mutation-safe : callLlm appelé avec `expect.objectContaining({ tier: 'fast' })`.
  </behavior>
  <action>
    Dans `apps/web/src/lib/veille/classify.ts` :
    1. Remplacer `import { callOllama } from '@/lib/ai-ollama';` par `import { callLlm } from '@/lib/llm-client';`.
    2. Supprimer `const OLLAMA_MODEL_VEILLE = 'mistral-small:24b'` (plus de modèle codé). Garder `PROMPT_VERSION_VEILLE` inchangé (versionner le re-tune plus tard — ici on ne re-tune PAS le prompt veille, seulement le backend).
    3. Remplacer l'appel L61 par :
       ```typescript
       const r = await callLlm({
         tier: 'fast',
         systemPrompt: SYSTEM_PROMPT_VEILLE_CLASSIFY,
         prompt: buildVeilleClassifyUserPrompt(input),
         jsonOutput: true,
         temperature: 0.1,
         timeoutMs: 60_000,
       });
       ```
    4. Dans les 3 branches `aIGenerationJob.create`, remplacer `provider: 'ollama'` par `provider: r.provider` et `model: OLLAMA_MODEL_VEILLE` par `model: r.model`. ATTENTION branche catch (L102-118) : `r` n'est pas défini si callLlm throw AVANT assignation → garder `provider: 'openrouter'`... NON : dans le catch on n'a pas r. Garder une valeur statique de repli dans le SEUL catch : `provider: (process.env.AI_PROVIDER ?? 'ollama') === 'openrouter' ? 'openrouter' : 'ollama'` et `model: 'unknown'` (l'échec précède la réponse). Les branches success/Zod-fail utilisent `r.provider`/`r.model`.
    5. Worker-safety : NE PAS introduire d'import React/server-action/rbac (feedback_worker_no_react_imports). callLlm est fetch pur → OK.

    Dans `apps/web/src/lib/veille/__tests__/classify.test.ts` :
    6. Remplacer `vi.mock('@/lib/ai-ollama', ...)` + `callOllamaMock` par `vi.mock('@/lib/llm-client', () => ({ callLlm: callLlmMock }))` (renommer le hoisted mock en `callLlmMock`).
    7. Dans chaque `mockResolvedValueOnce`, ajouter `provider: 'openrouter'` et remplacer `model: 'mistral-small:24b'` par `model: 'anthropic/claude-haiku-4.5'`.
    8. Remplacer l'assertion `expect.objectContaining({ model: 'mistral-small:24b' })` (Test 1) par `expect.objectContaining({ tier: 'fast' })`.
    9. Mettre à jour les assertions AIGenerationJob : `provider: 'openrouter'`, `model: 'anthropic/claude-haiku-4.5'` (Test 1). Test 4 (throw) : le provider tracé = celui du repli — asserter seulement `status:'error'` + `errorMsg` (ne pas figer le provider du catch).
    10. Documenter le PROTOCOLE DE MUTATION en commentaire (non commité) : inverser `tier:'fast'`→`'quality'` dans classify.ts → Test 1 vire ROUGE → restaurer. Prouve un vrai garde (feedback_test_de_puissance_mutation).
  </action>
  <verify>
    <automated>pnpm --filter @qualiof/web test -- classify</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "callLlm" apps/web/src/lib/veille/classify.ts`
    - `grep -c "callOllama" apps/web/src/lib/veille/classify.ts` == 0
    - `grep -q "r.provider" apps/web/src/lib/veille/classify.ts` (tracing dynamique)
    - `grep -q "@/lib/llm-client" apps/web/src/lib/veille/__tests__/classify.test.ts`
    - `grep -q "tier: 'fast'" apps/web/src/lib/veille/__tests__/classify.test.ts`
    - `pnpm --filter @qualiof/web test -- classify` exit 0
  </acceptance_criteria>
  <done>classifyItem route via callLlm tier fast, tracing dynamique, test migré + mutation-safe, 4 scénarios verts.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @qualiof/web test -- classify` vert (4 tests).
- `grep callOllama apps/web/src/lib/veille/classify.ts` = 0 occurrence.
- Worker-safe préservé : aucun nouvel import React/auth.
</verification>

<success_criteria>
- REQ-16-03 : veille migrée vers callLlm tier fast, provider/model dynamiques, test mutation-safe.
</success_criteria>

<output>
Créer `.planning/phases/16-migration-ia-ollama-vers-claude-api/16-02-SUMMARY.md`
</output>
