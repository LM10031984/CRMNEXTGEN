---
phase: 03-responsive-content-layouts
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/app/layout.tsx
  - apps/web/src/app/app/sessions/page.tsx
  - apps/web/src/app/app/apprenants/page.tsx
  - apps/web/src/app/app/dossiers-opco/page.tsx
  - apps/web/src/app/app/preinscriptions/page.tsx
  - apps/web/src/app/app/leads/page.tsx
  - apps/web/src/app/app/organisations/page.tsx
  - apps/web/src/app/app/produits/page.tsx
  - apps/web/src/app/app/formateurs/page.tsx
autonomous: false
requirements: [RESP-05]
user_setup: []

must_haves:
  truths:
    - "Sur viewport < 640px, les listings (DataTable) sont scrollables horizontalement, pas tronqués brutalement"
    - "Le `<main>` a un padding réduit (p-4) en mobile pour gagner de la largeur"
  artifacts:
    - apps/web/src/app/app/layout.tsx (p-4 md:p-8)
    - 8 pages list wrappées avec overflow-x-auto
  key_links:
    - "Wrapping au site d'usage = pas de modif du composant DataTable lui-même (réduit risque régression)"
---

<objective>
Rendre les listings (DataTable) scrollables horizontalement en mobile + réduire le padding du `<main>` en mobile pour gagner de la place.

Output: `<main>` p-4 md:p-8 + 8 pages list wrappées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/app/app/layout.tsx
@apps/web/src/components/ui/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Réduire padding `<main>` en mobile</name>
  <files>apps/web/src/app/app/layout.tsx</files>
  <read_first>
    - apps/web/src/app/app/layout.tsx (full)
  </read_first>
  <action>
    Dans `apps/web/src/app/app/layout.tsx`, modifier la className du `<main>` :
    AVANT : `<main className="flex-1 p-8 max-w-screen-2xl w-full mx-auto">`
    APRÈS : `<main className="flex-1 p-4 md:p-8 max-w-screen-2xl w-full mx-auto">`
    
    Gain mobile : 32px de padding vs 64px avant = +32px de largeur utilisable (×2 côtés = +64px de largeur réelle).
  </action>
  <verify>
    <automated>grep -q "flex-1 p-4 md:p-8 max-w-screen-2xl" apps/web/src/app/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `<main>` className contient `p-4 md:p-8`
    - Pas de régression Phase 1 (sticky header dépend pas de ce padding)
  </acceptance_criteria>
  <done>Main padding mobile-friendly</done>
</task>

<task type="auto">
  <name>Task 4.2: Wrapper listings avec overflow-x-auto</name>
  <files>
    apps/web/src/app/app/sessions/page.tsx,
    apps/web/src/app/app/apprenants/page.tsx,
    apps/web/src/app/app/dossiers-opco/page.tsx,
    apps/web/src/app/app/preinscriptions/page.tsx,
    apps/web/src/app/app/leads/page.tsx,
    apps/web/src/app/app/organisations/page.tsx,
    apps/web/src/app/app/produits/page.tsx,
    apps/web/src/app/app/formateurs/page.tsx
  </files>
  <read_first>
    - apps/web/src/app/app/sessions/page.tsx (full — identifier où le DataTable est rendu)
    - apps/web/src/app/app/apprenants/page.tsx
    - apps/web/src/components/ui/data-table.tsx (comprendre le rendu HTML)
  </read_first>
  <action>
    Pour chaque page list (8 fichiers), trouver le rendu `<DataTable ... />` (ou équivalent) et wrapper :
    
    AVANT :
    ```tsx
    <DataTable columns={...} data={...} />
    ```
    
    APRÈS :
    ```tsx
    <div className="overflow-x-auto -mx-4 sm:mx-0">
      <DataTable columns={...} data={...} />
    </div>
    ```
    
    Le `-mx-4 sm:mx-0` :
    - En mobile (< sm = 640px) : `-mx-4` neutralise le `p-4` du `<main>` → la table prend toute la largeur viewport
    - En >= sm : `mx-0` (margin nulle, padding du `<main>` reprend)
    
    **Si le DataTable est dans un `<Card>` ou wrapper existant**, ajouter le `overflow-x-auto` au wrapper ou créer un sous-wrapper. Adapter selon la structure réelle.
    
    Si certaines pages ne contiennent pas de DataTable (ex: page d'accueil section avec autre composant), skip et documenter.
  </action>
  <verify>
    <automated>grep -lE "overflow-x-auto -mx-4 sm:mx-0" apps/web/src/app/app/sessions/page.tsx apps/web/src/app/app/apprenants/page.tsx apps/web/src/app/app/dossiers-opco/page.tsx apps/web/src/app/app/preinscriptions/page.tsx apps/web/src/app/app/leads/page.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Au moins 5 des 8 pages list contiennent `overflow-x-auto -mx-4 sm:mx-0` (les autres 3 documentées si absent — peut-être pas de DataTable)
    - Aucune modification de `apps/web/src/components/ui/data-table.tsx` (composant inchangé)
  </acceptance_criteria>
  <done>Listings scrollables en mobile</done>
</task>

</tasks>

<verification>
- ✅ Grep wrapper
- ⏳ Visuel Laurent : `/app/sessions` à 390px → tableau visible, peut être scrollé H, pas de débordement page
</verification>

<output>
SUMMARY Plan 03-05 : listings wrappés + main padding mobile.
</output>
