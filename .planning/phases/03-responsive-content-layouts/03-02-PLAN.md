---
phase: 03-responsive-content-layouts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/app/dossiers-opco/page.tsx
  - apps/web/src/app/app/organisations/[id]/page.tsx
  - apps/web/src/app/app/financeurs/page.tsx
autonomous: true
requirements: [RESP-04]
user_setup: []

must_haves:
  truths:
    - "Toutes les grilles `grid-cols-N` (N>=2) des pages app/app/* ont au moins un variant responsive (sm:/md:)"
  artifacts:
    - 3 fichiers pages corrigés
  key_links:
    - "Pages list (sessions, apprenants) traitées dans Plan 03-04 (wrapping listings, pas grids internes)"
---

<objective>
Auditer et fixer les 3 grids des pages `app/app/*` qui n'ont pas de variant responsive.

Output: 3 fichiers modifiés, audit grep clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/app/app/dossiers-opco/page.tsx
@apps/web/src/app/app/organisations/[id]/page.tsx
@apps/web/src/app/app/financeurs/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Fix dossiers-opco page.tsx:506</name>
  <files>apps/web/src/app/app/dossiers-opco/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/dossiers-opco/page.tsx (ligne 506 + contexte 5 lignes autour pour comprendre le contenu)
  </read_first>
  <action>
    À la ligne ~506 de `apps/web/src/app/app/dossiers-opco/page.tsx`, remplacer :
    AVANT : `<div className="grid grid-cols-3 gap-2 text-xs">`
    APRÈS : `<div className="grid grid-cols-3 gap-2 text-xs">`  (si contenu compact à 3 cols OK partout)
    OU si la grille comporte des éléments plus larges (tester visuellement) :
    `<div className="grid grid-cols-2 sm:grid-cols-3 gap-2 text-xs">`
    
    **Décision :** Si le contenu tient à 3 cols même à 390px (text-xs = 12px, contenu probablement court), garder `grid-cols-3` sans préfixe → ajouter une exemption (commentaire ligne au-dessus) :
    ```tsx
    {/* grid-cols-3 OK même mobile : valeurs compactes (badges, chiffres) */}
    <div className="grid grid-cols-3 gap-2 text-xs">
    ```
    
    Sinon : `grid-cols-2 sm:grid-cols-3`. Choisir selon lecture du contenu.
  </action>
  <verify>
    <automated>grep -E "grid-cols-3.*gap-2 text-xs" apps/web/src/app/app/dossiers-opco/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Ligne 506 conserve `grid-cols-3` avec commentaire d'exemption OU passe à `grid-cols-2 sm:grid-cols-3`
    - Décision documentée en commentaire si exempted
  </acceptance_criteria>
  <done>Grid mini-stats ajustée</done>
</task>

<task type="auto">
  <name>Task 2.2: Fix organisations/[id]/page.tsx:212</name>
  <files>apps/web/src/app/app/organisations/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/organisations/[id]/page.tsx (ligne 212 + contexte)
  </read_first>
  <action>
    À la ligne ~212, dans le `{key, value}` map (formulaire 2 cols) :
    AVANT : `<div key={k} className="grid grid-cols-2 gap-2">`
    APRÈS : `<div key={k} className="grid grid-cols-1 sm:grid-cols-2 gap-2">`
  </action>
  <verify>
    <automated>grep -q "grid-cols-1 sm:grid-cols-2 gap-2" apps/web/src/app/app/organisations/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Contient `grid-cols-1 sm:grid-cols-2 gap-2` à la ligne ciblée
  </acceptance_criteria>
  <done>Fiche organisation responsive</done>
</task>

<task type="auto">
  <name>Task 2.3: Fix financeurs/page.tsx:129</name>
  <files>apps/web/src/app/app/financeurs/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/financeurs/page.tsx (ligne 129 + contexte)
  </read_first>
  <action>
    À la ligne ~129 (KPI 2 cols dans card financeur) :
    AVANT : `<div className="grid grid-cols-2 gap-3 mb-5">`
    APRÈS : `<div className="grid grid-cols-2 gap-3 mb-5">` (si contenu court 2 cols mobile OK)
    OU `<div className="grid grid-cols-1 sm:grid-cols-2 gap-3 mb-5">` selon visuel.
    
    **Choix par défaut :** garder `grid-cols-2` (KPI compacts, 2 chiffres tiennent en mobile), ajouter commentaire d'exemption si nécessaire.
  </action>
  <verify>
    <automated>grep -E "grid-cols-(2|1 sm:grid-cols-2) gap-3 mb-5" apps/web/src/app/app/financeurs/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Décision faite et commentée
  </acceptance_criteria>
  <done>Fiche financeur responsive</done>
</task>

</tasks>

<verification>
- ✅ Grep audit après cette plan : `grep -rE 'grid-cols-[2-9]+' apps/web/src/app/app/ | grep -vE '(sm|md|lg|xl):' | grep -v "^[^:]*://" | wc -l` doit être 0 OU = nb d'exemptions documentées
</verification>

<output>
SUMMARY pour Plan 03-05 : 3 grids pages fixées.
</output>
