---
phase: 03-responsive-content-layouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/app/page.tsx
autonomous: true
requirements: [RESP-04]
user_setup: []

must_haves:
  truths:
    - "Dashboard /app utilise des grilles qui refluent proprement de 390px à 1440px+"
    - "À 1024-1279px (lg), 4 KPI par ligne max (pas 6 ou 7) pour éviter compression"
    - "À >= 1280px (xl), densité maximale (6/7 cols)"
  artifacts:
    - apps/web/src/app/app/page.tsx (5 grilles ajustées)
  key_links:
    - "Premier impression utilisateur, le plus visible — à fixer en priorité"
---

<objective>
Densifier les 5 grilles du dashboard pour éviter la compression KPI à 1024-1279px (lg) tout en gardant la densité à >= 1280px (xl). Adresse l'audit "tronqué à 1456px".

Output: 5 grilles ajustées dans `apps/web/src/app/app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/src/app/app/page.tsx
@.planning/phases/03-responsive-content-layouts/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Ajuster les 5 grilles dashboard</name>
  <files>apps/web/src/app/app/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/page.tsx (lignes 109, 118, 140, 152, 198 — 5 grilles)
  </read_first>
  <action>
    Modifier les 5 className grid de `apps/web/src/app/app/page.tsx` :
    
    Ligne ~109 (KPI principaux) :
    AVANT : `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3`
    APRÈS : `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-3`
    
    Ligne ~118 (KPI secondaires "mt-3") :
    AVANT : `grid grid-cols-2 md:grid-cols-3 gap-3 mt-3`
    APRÈS : `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-3 gap-3 mt-3` (déjà OK essentiellement, ajout sm: pour cohérence)
    
    Ligne ~140 (AGEFICE/Sessions tiles) :
    AVANT : `grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-3`
    APRÈS : `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-4 xl:grid-cols-7 gap-3`
    
    Ligne ~152 (Pipeline / Financeurs) :
    AVANT : `grid grid-cols-1 lg:grid-cols-2 gap-4`
    APRÈS : conserver tel quel (déjà bien : 1 col mobile, 2 cols >= lg)
    
    Ligne ~198 (Stats par catégorie) :
    AVANT : `grid grid-cols-1 lg:grid-cols-3 gap-4`
    APRÈS : `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` (transition douce md→lg)
    
    **Important :** Ne modifier QUE les className des grids. Ne pas toucher au contenu, aux KPI cards elles-mêmes, ni à la structure.
  </action>
  <verify>
    <automated>grep -c "grid-cols-2 sm:grid-cols-3 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6" apps/web/src/app/app/page.tsx && grep -c "grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-4 xl:grid-cols-7" apps/web/src/app/app/page.tsx && ! grep -q "lg:grid-cols-6 gap-3" apps/web/src/app/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Ligne ~109 contient `xl:grid-cols-6`
    - Ligne ~140 contient `xl:grid-cols-7`
    - Plus de `lg:grid-cols-6` ou `lg:grid-cols-7` dans le fichier (densification reportée à xl)
    - Fichier reste valide TypeScript (build pass — vérifié globalement Plan 04)
  </acceptance_criteria>
  <done>Dashboard densifié : 4 cols max à lg, 6/7 cols à xl</done>
</task>

</tasks>

<!-- NOTE : update REQUIREMENTS.md dans Plan 03-05 (wave 2). -->

<verification>
- ✅ Grep checks
- ⏳ Visuel Laurent : `/app` à 1024 / 1280 / 1440 — KPI lisibles, pas de compression
</verification>

<output>
SUMMARY pour Plan 03-05 : annotation RESP-04 dashboard ajusté.
</output>
