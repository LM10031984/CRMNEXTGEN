{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile=false && pnpm --filter @qualiof/db exec prisma generate"
  },
  "deploy": {
    "startCommand": "pnpm --filter @qualiof/web workers:all",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
